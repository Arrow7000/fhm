import FHM.Core

-- These pure `Ty`-level lemmas now live in `Core` (they were originally developed
-- here). Core declares them without `@[simp]`, but the InferW proofs below rely on
-- them firing as simp lemmas, so re-grant the attribute here.
attribute [simp] Ty.openVars_arrow Ty.openVars_customTy

/-! ## Scaffolding for the algorithmic phase

Not used yet. Once we move from the declarative relation to algorithmic
inference, `.fvar`s stop being abstract/rigid type variables and become
*unification variables*, each pointing at an entry in a unification context. -/

/-- Either a constrained (`conc`) or unconstrained (`unspec`) unification var. -/
inductive TyMaybe where
  | unspec
  | conc (ty : Ty)

/-- The unification-var context. Each `.fvar` references an item in this list. -/
abbrev FvarCtx := List TyMaybe


/-! ## Algorithmic phase, step 1: substitution algebra

The declarative `TypeOfElabHM` treats `.fvar`s as rigid/abstract type variables.
The algorithm reinterprets them as *unification variables* and solves equality
constraints between monotypes by computing a most-general unifier (MGU).

A unification substitution maps `.fvar` names to types. We reuse the proven
`Ty.substFvars` machinery: a substitution is a `List (Nat × Ty)` applied
left-to-right, so **composition is list append** (`Subst.onTy_append`). This
algebra is shared scaffolding needed by *any* algorithmic presentation
(Algorithm W / M / J or constraint-based) — all of them rest on unification. -/

/-- A unification substitution: maps `.fvar` names to types, applied
    left-to-right via `Ty.substFvars`. Composition of `S` then `T` is `S ++ T`. -/
abbrev Subst := List (Nat × Ty)

/-- Apply a substitution to a monotype. -/
def Subst.onTy (S : Subst) : Ty → Ty := Ty.substFvars S

/-- Apply a substitution to a scheme. `substFvars` only rewrites free vars, so
    the scheme's bound vars (`.bvar`s `< paramCount`) are left untouched. -/
def Subst.onPolyTy (S : Subst) (M : PolyTy) : PolyTy :=
  { paramCount := M.paramCount, body := S.onTy M.body }

/-- Apply a substitution to a value environment. -/
def Subst.onEnv (S : Subst) (env : Env) : Env := env.map S.onPolyTy

/-- Apply a substitution to a typing context. Constructors are closed
    (`Ctor.closed`), so only the value env is affected. -/
def Subst.onCtx (S : Subst) (ctx : Ctx) : Ctx :=
  { env := S.onEnv ctx.env, ctors := ctx.ctors }

/-- `substFvars` of an append applies the prefix first, then the suffix — the
    elementary fact making list-append the composition of substitutions. -/
theorem Ty.substFvars_append (S T : Subst) (τ : Ty) :
    Ty.substFvars (S ++ T) τ = Ty.substFvars T (Ty.substFvars S τ) := by
  induction S generalizing τ with
  | nil => rfl
  | cons hd tl ih =>
    obtain ⟨Z, U⟩ := hd
    simp only [List.cons_append, Ty.substFvars]
    exact ih (Ty.substFvar Z U τ)

/-- Composition of substitutions is concatenation: `(S ++ T)` applies `S` first,
    then `T`. -/
theorem Subst.onTy_append (S T : Subst) (τ : Ty) :
    (S ++ T).onTy τ = T.onTy (S.onTy τ) := by
  simp only [Subst.onTy, Ty.substFvars_append]

@[simp] theorem Subst.onPolyTy_nil (M : PolyTy) : Subst.onPolyTy [] M = M := rfl

@[simp] theorem Subst.onEnv_nil (env : Env) : Subst.onEnv [] env = env := by
  show env.map (Subst.onPolyTy []) = env
  rw [show (Subst.onPolyTy [] : PolyTy → PolyTy) = id from funext Subst.onPolyTy_nil]
  exact List.map_id env

@[simp] theorem Subst.onCtx_nil (ctx : Ctx) : Subst.onCtx [] ctx = ctx := by
  simp only [Subst.onCtx, Subst.onEnv_nil]

theorem Subst.onPolyTy_append (S T : Subst) (M : PolyTy) :
    (S ++ T).onPolyTy M = T.onPolyTy (S.onPolyTy M) := by
  simp only [Subst.onPolyTy, Subst.onTy_append]

theorem Subst.onEnv_append (S T : Subst) (env : Env) :
    (S ++ T).onEnv env = T.onEnv (S.onEnv env) := by
  simp only [Subst.onEnv, List.map_map]
  apply List.map_congr_left
  intro M _
  exact Subst.onPolyTy_append S T M

theorem Subst.onCtx_append (S T : Subst) (ctx : Ctx) :
    (S ++ T).onCtx ctx = T.onCtx (S.onCtx ctx) := by
  simp only [Subst.onCtx, Subst.onEnv_append]


/-! ### Substitution preserves typing

Substituting over the *whole* context needs no environment-freshness side
condition (the freshness premise of `typ_subst_preservation` is vacuous with an
empty outer env) — only that each replacement type is locally-closed. This is
the workhorse for `Infer` soundness: applying the threaded substitution to a
`TypeOfElabHM` derivation yields another. -/

/-- Single-variable substitution preserves typing across the whole context.

    NOTE (scoped type variables): the new `Core` `typ_subst_preservation`
    *rewrites the term* — the substitution is pushed through the term's
    annotations (`Expr.substTyFvar`). So the honest conclusion now mentions
    `e.substTyFvar Z U`, not `e`. The fixed-term form is recovered by
    `TypeOfElabHM.onSubst_fixed` under the hypothesis that `S` fixes `e`'s
    annotation free vars. -/
theorem TypeOfElabHM.onSubstFvar {ctx : Ctx} {e : Expr} {τ : Ty} (Z : Nat) (U : Ty)
    (hU : U.IsLC) (h : TypeOfElabHM ctx e τ) :
    TypeOfElabHM (Subst.onCtx [(Z, U)] ctx) (e.substTyFvar Z U) (Subst.onTy [(Z, U)] τ) := by
  have key := TypeOfElabHM.typ_subst_preservation (ctors := ctx.ctors) (env_post := ctx.env)
    (env_outer := []) (Z := Z) (U := U) (by simp [Env.freeVars]) hU
    (by rw [List.append_nil]; exact h)
  rw [List.append_nil] at key
  exact key

/-- A whole substitution preserves typing, given each replacement is LC. The
    term is rewritten (`e.substTyFvars S`) — see `TypeOfElabHM.onSubstFvar`. -/
theorem TypeOfElabHM.onSubst {ctx : Ctx} {e : Expr} {τ : Ty} (S : Subst)
    (h_lc : ∀ p ∈ S, p.2.IsLC) (h : TypeOfElabHM ctx e τ) :
    TypeOfElabHM (S.onCtx ctx) (e.substTyFvars S) (S.onTy τ) := by
  induction S generalizing ctx e τ with
  | nil => simpa [Expr.substTyFvars] using h
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    have hU : U.IsLC := h_lc (Z, U) (List.mem_cons_self ..)
    have hS' : ∀ p ∈ S', p.2.IsLC := fun p hp => h_lc p (List.mem_cons_of_mem _ hp)
    have step := TypeOfElabHM.onSubstFvar Z U hU h
    have rest := ih hS' step
    rw [show ((Z, U) :: S') = [(Z, U)] ++ S' from rfl, Subst.onCtx_append, Subst.onTy_append]
    exact rest

/-- A type-fvar substitution whose keys avoid a term's annotation free vars
    leaves the term fixed (collapse of `Expr.substTyFvars` to a no-op). -/
theorem Expr.substTyFvars_eq_self_of_tyFreeVars_nil {e : Expr} (S : Subst)
    (h : e.tyFreeVars = []) : e.substTyFvars S = e := by
  apply Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars
  intro p _
  simp [h]

/-- **D1 fixed-term corollary.** When `S` fixes every annotation free variable of
    `e` (so `e.substTyFvars S = e`), substitution preservation keeps the *term*
    fixed: `TypeOfElabHM (S.onCtx ctx) e (S.onTy τ)`. This is the form `Infer.sound`
    needs — the inferred substitution only touches unification variables, never
    the (closed / rigid-skolem) annotation variables pinned by the escape
    discipline. -/
theorem TypeOfElabHM.onSubst_fixed {ctx : Ctx} {e : Expr} {τ : Ty} (S : Subst)
    (h_lc : ∀ p ∈ S, p.2.IsLC) (h_fix : e.substTyFvars S = e) (h : TypeOfElabHM ctx e τ) :
    TypeOfElabHM (S.onCtx ctx) e (S.onTy τ) := by
  have key := TypeOfElabHM.onSubst S h_lc h
  rwa [h_fix] at key

/-- Instantiation is a no-op when the substitution is the identity on every bvar
    that actually occurs (`< d`) and the type's bvars are all `< d`. -/
theorem Ty.instantiate_eq_self_of_bvars_lt {σ : Nat → Ty} {d : Nat} {t : Ty}
    (hσ : ∀ i, i < d → σ i = .bvar i) (h : ContainsBvarsUpTo d t) :
    Ty.instantiate σ t = t := by
  induction t using Ty.rec_strong with
  | prim _ => rfl
  | arrow a b iha ihb =>
    cases h with
    | arrow ha hb => simp only [Ty.instantiate, Ty.arrow.injEq]; exact ⟨iha ha, ihb hb⟩
  | bvar i => cases h with | bvar hlt => exact hσ i hlt
  | fvar n => rfl
  | customTy nm tys ih =>
    cases h with
    | customTy hall =>
      simp only [Ty.instantiate, Ty.customTy.injEq, true_and]
      induction tys with
      | nil => rfl
      | cons hd tl ihtl =>
        simp only [TyList.instantiate, List.cons.injEq]
        exact ⟨ih hd List.mem_cons_self (hall hd List.mem_cons_self),
               ihtl (fun t ht => ih t (List.mem_cons_of_mem _ ht))
                    (fun t ht => hall t (List.mem_cons_of_mem _ ht))⟩

/-- Offset opening (`Ty.openVarsFrom d`) fixes any type whose bvars are all `< d`
    — the opener only touches bvars `≥ d`. Used by the `openTyVars`-identity
    invariant (annotation bvars stay within their own scheme's arity). -/
theorem Ty.openVarsFrom_eq_self_of_containsBvars {d : Nat} {Xs : List Nat} {t : Ty}
    (h : ContainsBvarsUpTo d t) : Ty.openVarsFrom d Xs t = t := by
  unfold Ty.openVarsFrom
  exact Ty.instantiate_eq_self_of_bvars_lt (fun _ hi => if_pos hi) h


/-! ### Most-general unifier specification

`Unifies S τ₁ τ₂` says `S` equates the two monotypes; `IsMGU S τ₁ τ₂` adds that
*every* unifier factors through `S` (`S` is the least committal one). Because
composition is `++` and `(S ++ R).onTy τ = R.onTy (S.onTy τ)`, "`S'` factors
through `S`" means `∃ R, S' acts as (S then R)`.

The occurs check needs no new notion: `.fvar Z` occurs in `τ` exactly when
`Z ∈ τ.freeVars`. -/

/-- `S` makes `τ₁` and `τ₂` syntactically equal. -/
def Unifies (S : Subst) (τ₁ τ₂ : Ty) : Prop := S.onTy τ₁ = S.onTy τ₂

/-- `S` is a most-general unifier of `τ₁` and `τ₂`: it unifies them, and any
    other unifier `S'` is an extension of `S` (factors as `S` then some `R`). -/
structure IsMGU (S : Subst) (τ₁ τ₂ : Ty) : Prop where
  unifies : Unifies S τ₁ τ₂
  greatest : ∀ S', Unifies S' τ₁ τ₂ → ∃ R : Subst, ∀ τ, S'.onTy τ = R.onTy (S.onTy τ)


/-! ### The unification relation

`UnifyRel τ₁ τ₂ S` is the *graph of unification on success*: it holds when the
two monotypes unify and `S` is a resulting most-general unifier. Failure is the
*absence* of a derivation (constructor clash, arity clash, or occurs-check), so
no negative side-conditions are needed and there is no termination obligation —
this is the relation-first stage; a `unify` function comes later (stage 3).

Compound types thread the prefix substitution into the remaining sub-problems
(`UnifyRel (S₁.onTy b) (S₁.onTy d) S₂`), exactly as Algorithm W's unifier does.
The occurs check is the freshness premise `n ∉ τ.freeVars` on the var rules;
together with `substFvar_fresh` it is what makes `[(n, τ)]` an actual unifier. -/
mutual

inductive UnifyRel : Ty → Ty → Subst → Prop
  | prim {p} :
    UnifyRel (.prim p) (.prim p) []

  | fvarRefl {n} :
    UnifyRel (.fvar n) (.fvar n) []

  | fvarL {n τ} :
    τ ≠ .fvar n → -- are not the same or else `fvarRefl` applies
    n ∉ τ.freeVars → -- the occurs check: `n` doesn't occur in `τ`
    UnifyRel (.fvar n) τ [(n, τ)]

  | fvarR {n τ} :
    τ ≠ .fvar n → -- are not the same or else `fvarRefl` applies
    n ∉ τ.freeVars → -- the occurs check: `n` doesn't occur in `τ`
    UnifyRel τ (.fvar n) [(n, τ)]

  | arrow {a b c d S₁ S₂} :
    UnifyRel a c S₁ →
    UnifyRel (S₁.onTy b) (S₁.onTy d) S₂ →
    UnifyRel (.arrow a b) (.arrow c d) (S₁ ++ S₂)

  | customTy {nm tys₁ tys₂ S} :
    UnifyRelList tys₁ tys₂ S →
    UnifyRel (.customTy nm tys₁) (.customTy nm tys₂) S

/-- Pairwise unification of equal-length type lists, threading the substitution
    left-to-right. Used for the arguments of a custom type constructor. -/
inductive UnifyRelList : List Ty → List Ty → Subst → Prop
  | nil :
    UnifyRelList [] [] []
  | cons {t₁ t₂ ts₁ ts₂ S₁ S₂} :
    UnifyRel t₁ t₂ S₁ →
    UnifyRelList (ts₁.map S₁.onTy) (ts₂.map S₁.onTy) S₂ →
    UnifyRelList (t₁ :: ts₁) (t₂ :: ts₂) (S₁ ++ S₂)

end


/-! ### `onTy` distributes over the type formers -/

@[simp] theorem Subst.onTy_nil {τ : Ty} : Subst.onTy [] τ = τ := rfl

@[simp] theorem Subst.onTy_prim {S : Subst} {p : PrimTy} :
    S.onTy (.prim p) = .prim p := Ty.substFvars_prim

@[simp] theorem Subst.onTy_bvar {S : Subst} {i : Nat} :
    S.onTy (.bvar i) = .bvar i := Ty.substFvars_bvar

@[simp] theorem Subst.onTy_arrow {S : Subst} {a b : Ty} :
    S.onTy (.arrow a b) = .arrow (S.onTy a) (S.onTy b) := Ty.substFvars_arrow

@[simp] theorem Subst.onTy_customTy {S : Subst} {nm : TyName} {tys : List Ty} :
    S.onTy (.customTy nm tys) = .customTy nm (tys.map S.onTy) := Ty.substFvars_customTy

/-- Mapping a composed substitution over a list = mapping each factor in turn. -/
theorem Subst.map_onTy_append (S T : Subst) (ts : List Ty) :
    ts.map (S ++ T).onTy = (ts.map S.onTy).map T.onTy := by
  rw [List.map_map]
  apply List.map_congr_left
  intro x _
  exact Subst.onTy_append S T x


/-! ### Soundness, part 1: a derived substitution is a unifier -/

mutual

/-- Any substitution produced by `UnifyRel` actually unifies the two types. -/
theorem UnifyRel.unifies : {τ₁ τ₂ : Ty} → {S : Subst} → UnifyRel τ₁ τ₂ S →
    Unifies S τ₁ τ₂
  | _, _, _, .prim => rfl
  | _, _, _, .fvarRefl => rfl
  | _, _, _, .fvarL _ hocc => by
    simp [Unifies, Subst.onTy, Ty.substFvars, Ty.substFvar, Ty.substFvar_fresh hocc]
  | _, _, _, .fvarR _ hocc => by
    simp [Unifies, Subst.onTy, Ty.substFvars, Ty.substFvar, Ty.substFvar_fresh hocc]
  | _, _, _, .arrow h₁ h₂ => by
    have e1 := UnifyRel.unifies h₁
    have e2 := UnifyRel.unifies h₂
    simp only [Unifies, Subst.onTy_append, Subst.onTy_arrow] at e1 e2 ⊢
    rw [Ty.arrow.injEq]
    exact ⟨by rw [e1], e2⟩
  | _, _, _, .customTy hl => by
    have el := UnifyRelList.unifies hl
    simp only [Unifies, Subst.onTy_customTy, el]

/-- The list version: a list-unifier equalises the two lists pointwise. -/
theorem UnifyRelList.unifies : {ts₁ ts₂ : List Ty} → {S : Subst} →
    UnifyRelList ts₁ ts₂ S → ts₁.map S.onTy = ts₂.map S.onTy
  | _, _, _, .nil => rfl
  | _, _, _, .cons h₁ ht => by
    have e1 := UnifyRel.unifies h₁
    have et := UnifyRelList.unifies ht
    simp only [Unifies] at e1
    simp only [List.map_cons, Subst.onTy_append, Subst.map_onTy_append]
    rw [e1, et]

end


/-! ### Soundness, part 2: a derived substitution is *most general*

The backbone of the var cases: if `S'` already equates `.fvar n` with `U`, then
applying `S'` is unchanged by first substituting `[n ↦ U]`. -/

theorem Subst.onTy_substFvar {S' : Subst} {n : Nat} {U : Ty}
    (h : S'.onTy (.fvar n) = S'.onTy U) :
    ∀ τ, S'.onTy (Ty.substFvar n U τ) = S'.onTy τ := by
  intro τ
  induction τ using Ty.rec_strong with
  | prim p => rfl
  | bvar i => rfl
  | fvar m =>
    by_cases hm : m = n
    · subst hm
      simp only [Ty.substFvar, if_true]
      exact h.symm
    · simp only [Ty.substFvar, if_neg hm]
  | arrow a b iha ihb => simp only [Ty.substFvar, Subst.onTy_arrow, iha, ihb]
  | customTy nm tys ih =>
    simp only [Ty.substFvar, TyList.substFvar_eq_map, Subst.onTy_customTy, List.map_map]
    apply congrArg (Ty.customTy nm)
    apply List.map_congr_left
    intro t ht
    exact ih t ht

/-! Every substitution produced by `UnifyRel` is a *most general* unifier: any
    other unifier `S'` factors through it. The compound cases thread the
    sub-problem unifiers (`R₁` then `R₂`) and return the final `R₂`; the var
    cases use `onTy_substFvar`. -/
mutual

theorem UnifyRel.greatest : {τ₁ τ₂ : Ty} → {S : Subst} → UnifyRel τ₁ τ₂ S →
    ∀ S' : Subst, Unifies S' τ₁ τ₂ → ∃ R : Subst, ∀ τ, S'.onTy τ = R.onTy (S.onTy τ)
  | _, _, _, .prim, S', _ => ⟨S', fun τ => by simp only [Subst.onTy_nil]⟩
  | _, _, _, .fvarRefl, S', _ => ⟨S', fun τ => by simp only [Subst.onTy_nil]⟩
  | _, _, _, .fvarL _ _, S', hS' =>
    ⟨S', fun τ => (Subst.onTy_substFvar hS' τ).symm⟩
  | _, _, _, .fvarR _ _, S', hS' =>
    ⟨S', fun τ => (Subst.onTy_substFvar (Eq.symm hS') τ).symm⟩
  | _, _, _, @UnifyRel.arrow a b c d S₁ S₂ h₁ h₂, S', hS' => by
    simp only [Unifies, Subst.onTy_arrow, Ty.arrow.injEq] at hS'
    obtain ⟨hac, hbd⟩ := hS'
    obtain ⟨R₁, hR₁⟩ := UnifyRel.greatest h₁ S' hac
    have hR₁bd : Unifies R₁ (S₁.onTy b) (S₁.onTy d) := by
      show R₁.onTy (S₁.onTy b) = R₁.onTy (S₁.onTy d)
      rw [← hR₁ b, ← hR₁ d]; exact hbd
    obtain ⟨R₂, hR₂⟩ := UnifyRel.greatest h₂ R₁ hR₁bd
    refine ⟨R₂, fun τ => ?_⟩
    rw [Subst.onTy_append, ← hR₂ (S₁.onTy τ), hR₁ τ]
  | _, _, _, .customTy hl, S', hS' => by
    simp only [Unifies, Subst.onTy_customTy, Ty.customTy.injEq, true_and] at hS'
    exact UnifyRelList.greatest hl S' hS'

theorem UnifyRelList.greatest : {ts₁ ts₂ : List Ty} → {S : Subst} →
    UnifyRelList ts₁ ts₂ S → ∀ S' : Subst, ts₁.map S'.onTy = ts₂.map S'.onTy →
      ∃ R : Subst, ∀ τ, S'.onTy τ = R.onTy (S.onTy τ)
  | _, _, _, .nil, S', _ => ⟨S', fun τ => by simp only [Subst.onTy_nil]⟩
  | _, _, _, @UnifyRelList.cons t₁ t₂ ts₁ ts₂ S₁ S₂ h₁ ht, S', hS' => by
    simp only [List.map_cons, List.cons.injEq] at hS'
    obtain ⟨ht1t2, htail⟩ := hS'
    obtain ⟨R₁, hR₁⟩ := UnifyRel.greatest h₁ S' ht1t2
    have key : ∀ (l : List Ty), l.map (R₁.onTy ∘ S₁.onTy) = l.map S'.onTy := by
      intro l; apply List.map_congr_left; intro t _; exact (hR₁ t).symm
    have hlist : (ts₁.map S₁.onTy).map R₁.onTy = (ts₂.map S₁.onTy).map R₁.onTy := by
      rw [List.map_map, List.map_map, key, key]; exact htail
    obtain ⟨R₂, hR₂⟩ := UnifyRelList.greatest ht R₁ hlist
    refine ⟨R₂, fun τ => ?_⟩
    rw [Subst.onTy_append, ← hR₂ (S₁.onTy τ), hR₁ τ]

end

/-- Unification soundness, assembled: a derivation yields a most-general unifier. -/
theorem UnifyRel.isMGU {τ₁ τ₂ : Ty} {S : Subst} (h : UnifyRel τ₁ τ₂ S) :
    IsMGU S τ₁ τ₂ :=
  ⟨h.unifies, h.greatest⟩


/-! ### Local-closedness of substitutions

Applying an LC substitution preserves local-closedness, and unification of two
LC monotypes yields an LC substitution (each replacement is a sub-part of an LC
input). These feed the `Infer.lc` invariant. -/

/-- Applying a substitution whose replacements are all LC preserves LC. -/
theorem Subst.onTy_lc {S : Subst} (h_lc : ∀ p ∈ S, p.2.IsLC) :
    ∀ {τ : Ty}, τ.IsLC → (S.onTy τ).IsLC := by
  induction S with
  | nil => intro τ hτ; simpa using hτ
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    have hU : U.IsLC := h_lc (Z, U) (List.mem_cons_self ..)
    have hS' : ∀ p ∈ S', p.2.IsLC := fun p hp => h_lc p (List.mem_cons_of_mem _ hp)
    intro τ hτ
    rw [show ((Z, U) :: S') = [(Z, U)] ++ S' from rfl, Subst.onTy_append]
    exact ih hS' (Ty.IsLC.substFvar hU hτ)

mutual

/-- Unifying two locally-closed monotypes yields an LC substitution. -/
theorem UnifyRel.lc : {a b : Ty} → {S : Subst} → UnifyRel a b S →
    a.IsLC → b.IsLC → ∀ p ∈ S, p.2.IsLC
  | _, _, _, .prim, _, _ => by simp
  | _, _, _, .fvarRefl, _, _ => by simp
  | _, _, _, .fvarL _ _, _, hb => by
    intro p hp; rw [List.mem_singleton] at hp; subst hp; exact hb
  | _, _, _, .fvarR _ _, ha, _ => by
    intro p hp; rw [List.mem_singleton] at hp; subst hp; exact ha
  | _, _, _, .arrow h₁ h₂, ha, hb => by
    cases ha with | arrow ha_a ha_b => cases hb with | arrow hb_c hb_d =>
    have h1lc := UnifyRel.lc h₁ ha_a hb_c
    have h2lc := UnifyRel.lc h₂ (Subst.onTy_lc h1lc ha_b) (Subst.onTy_lc h1lc hb_d)
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact h1lc p hp
    · exact h2lc p hp
  | _, _, _, .customTy hl, ha, hb => by
    cases ha with | customTy ha_all => cases hb with | customTy hb_all =>
    exact UnifyRelList.lc hl ha_all hb_all

/-- List version: unifying two LC type lists yields an LC substitution. -/
theorem UnifyRelList.lc : {ts₁ ts₂ : List Ty} → {S : Subst} → UnifyRelList ts₁ ts₂ S →
    (∀ t ∈ ts₁, t.IsLC) → (∀ t ∈ ts₂, t.IsLC) → ∀ p ∈ S, p.2.IsLC
  | _, _, _, .nil, _, _ => by simp
  | _, _, _, @UnifyRelList.cons t₁ t₂ ts₁ ts₂ S₁ S₂ h₁ ht, hts₁, hts₂ => by
    have ht1 : t₁.IsLC := hts₁ t₁ (List.mem_cons_self ..)
    have ht2 : t₂.IsLC := hts₂ t₂ (List.mem_cons_self ..)
    have h1lc := UnifyRel.lc h₁ ht1 ht2
    have hmap₁ : ∀ t ∈ ts₁.map S₁.onTy, t.IsLC := by
      intro t htm; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp htm
      exact Subst.onTy_lc h1lc (hts₁ t0 (List.mem_cons_of_mem _ ht0))
    have hmap₂ : ∀ t ∈ ts₂.map S₁.onTy, t.IsLC := by
      intro t htm; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp htm
      exact Subst.onTy_lc h1lc (hts₂ t0 (List.mem_cons_of_mem _ ht0))
    have h2lc := UnifyRelList.lc ht hmap₁ hmap₂
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact h1lc p hp
    · exact h2lc p hp

end


/-! ## Algorithmic phase, step 2: the inference relation (Algorithm W, relational)

`Infer Φ ctx e Φ' S τ` is Algorithm W phrased as a *relation* (no function /
termination obligation yet — that is stage 3). It reads: starting with the
fresh-variable frontier `Φ` (every unification var in play is `< Φ`), expression
`e` infers type `τ` under the most-general substitution `S`, allocating fresh
vars up to the new frontier `Φ'`. `.fvar`s are the unification variables;
composition of the threaded substitutions is `++`.

This covers the full language (`primLit`, `pair`, `lambda`, `app`, `var`,
`letIn`, `fst`, `snd`, `ctor`, `match_`), bridging to `TypeOfElabHM` via soundness.

The plan: prove `Infer.sound` (algo type ⟹ declarative type, iterating
`typ_subst_preservation` and using `UnifyRel.isMGU`), then completeness. -/

/-- The `k` fresh unification-var names starting at frontier `Φ`. -/
def freshVars (Φ k : Nat) : List Nat := (List.range k).map (Φ + ·)

/-- Generalization candidates: the free unification vars of `τ` that are *not*
    fixed by `env` and *not* rigid. The `rigid` set carries the in-scope scoped
    type variables (the free type fvars of the bound expression's annotations):
    those are skolems bound by an enclosing signature and must stay rigid —
    generalizing over them is unsound (it would let an inner `let` treat a fixed
    scoped variable as if it were polymorphic). For an ordinary HM program (no
    free-variable annotations) `rigid = []`, recovering the classic
    `ftv(τ) \ ftv(env)`. -/
def genVars (rigid : List Nat) (env : Env) (τ : Ty) : List Nat :=
  τ.freeVars.filter (fun x => !env.freeVars.contains x && !rigid.contains x)

/-- The principal generalization of `τ` relative to `env`, excluding the `rigid`
    scoped type variables. Reuses the existing `Ty.closeOver` (`fvar` ↦ `bvar` by
    position), whose `closeOver_preserves_bvars` immediately gives
    `genScheme … |>.WF`. -/
def genScheme (rigid : List Nat) (env : Env) (τ : Ty) : PolyTy :=
  { paramCount := (genVars rigid env τ).length, body := Ty.closeOver (genVars rigid env τ) τ }

/-- A generalized scheme is well-formed when its body type is locally-closed —
    closing introduces only the `paramCount`-many fresh bound vars. -/
theorem genScheme_wf {rigid : List Nat} {env : Env} {τ : Ty} (hτ : τ.IsLC) :
    (genScheme rigid env τ).WF :=
  Ty.closeOver_preserves_bvars hτ

/-- `freeVars` is always duplicate-free (it dedups). -/
theorem Ty.freeVars_nodup {τ : Ty} : τ.freeVars.Nodup := by
  cases τ with
  | prim => simp [Ty.freeVars]
  | bvar => simp [Ty.freeVars]
  | fvar => simp [Ty.freeVars]
  | arrow a b => simp [Ty.freeVars, List.nodup_dedup]
  | customTy nm tys =>
    cases tys with
    | nil => simp [Ty.freeVars, TyList.freeVars]
    | cons hd tl => simp [Ty.freeVars, TyList.freeVars, List.nodup_dedup]

/-- The generalization candidates are duplicate-free. -/
theorem genVars_nodup {rigid : List Nat} {env : Env} {τ : Ty} : (genVars rigid env τ).Nodup :=
  Ty.freeVars_nodup.filter _

/-- Group generalization candidates: the free unification vars appearing in *any*
    of the group's solved monotypes `τs`, excluding env-fixed and rigid vars. This
    is the SHARED pool `G` for the whole recursive group, so every binding
    generalizes over the same names — keeping mutual recursion's type-sharing
    linked (the `letRec` analogue of `genVars`). -/
def genGroupVars (rigid : List Nat) (env : Env) (τs : List Ty) : List Nat :=
  (Ty.freeVarsList τs).filter (fun x => !env.freeVars.contains x && !rigid.contains x)

/-- Per-binding generalization of a recursive group: each `τⱼ` is generalized over
    the shared pool `genGroupVars rigid env τs` via `PolyTy.genGroup` (the body
    scheme `∀ (G ∩ ftv τⱼ). τⱼ`). Mirrors the declarative `Ms = τs.map (genGroup G)`. -/
def genGroupSchemes (rigid : List Nat) (env : Env) (τs : List Ty) : List PolyTy :=
  τs.map (PolyTy.genGroup (genGroupVars rigid env τs))

/-- `Ty.freeVarsList` is always duplicate-free (it dedups). -/
theorem Ty.freeVarsList_nodup {τs : List Ty} : (Ty.freeVarsList τs).Nodup := by
  cases τs with
  | nil => simp [Ty.freeVarsList]
  | cons hd tl => simp [Ty.freeVarsList, List.nodup_dedup]

/-- The group generalization candidates are duplicate-free. -/
theorem genGroupVars_nodup {rigid : List Nat} {env : Env} {τs : List Ty} :
    (genGroupVars rigid env τs).Nodup :=
  Ty.freeVarsList_nodup.filter _

/-! ### Decidable scheme-well-formedness and closedness checks

`Ty.bvarsBelow n` decides `ContainsBvarsUpTo n` (hence `PolyTy.WF` via the
body), and `t.freeVars = []` decides `NoFreeVars` — both needed by the
annotated-`let` arm of `inferCore` to validate a scheme annotation `σ` against
the declarative side conditions. -/

mutual
/-- Boolean: all `.bvar`s in `t` are `< n`. Decides `ContainsBvarsUpTo n t`. -/
def Ty.bvarsBelow (n : Nat) : Ty → Bool
  | .prim _          => true
  | .arrow a b       => Ty.bvarsBelow n a && Ty.bvarsBelow n b
  | .fvar _          => true
  | .bvar i          => decide (i < n)
  | .customTy _ tys  => TyList.bvarsBelow n tys
def TyList.bvarsBelow (n : Nat) : List Ty → Bool
  | []      => true
  | t :: ts => Ty.bvarsBelow n t && TyList.bvarsBelow n ts
end

theorem TyList.bvarsBelow_iff_forall {n : Nat} (tys : List Ty) :
    TyList.bvarsBelow n tys = true ↔ ∀ t ∈ tys, Ty.bvarsBelow n t = true := by
  induction tys with
  | nil => simp [TyList.bvarsBelow]
  | cons hd tl ih =>
    simp only [TyList.bvarsBelow, Bool.and_eq_true, List.mem_cons]
    rw [ih]
    constructor
    · rintro ⟨hhd, htl⟩ t (rfl | ht)
      · exact hhd
      · exact htl t ht
    · intro h; exact ⟨h hd (Or.inl rfl), fun t ht => h t (Or.inr ht)⟩

theorem Ty.bvarsBelow_iff {n : Nat} (t : Ty) :
    Ty.bvarsBelow n t = true ↔ ContainsBvarsUpTo n t := by
  induction t using Ty.rec_strong with
  | prim p => exact iff_of_true rfl .prim
  | fvar m => exact iff_of_true rfl .fvar
  | bvar i =>
    simp only [Ty.bvarsBelow, decide_eq_true_eq]
    exact ⟨fun h => .bvar h, fun h => by cases h with | bvar hlt => exact hlt⟩
  | arrow a b iha ihb =>
    simp only [Ty.bvarsBelow, Bool.and_eq_true, iha, ihb]
    exact ⟨fun ⟨ha, hb⟩ => .arrow ha hb, fun h => by cases h with | arrow ha hb => exact ⟨ha, hb⟩⟩
  | customTy nm tys ih =>
    simp only [Ty.bvarsBelow]
    rw [TyList.bvarsBelow_iff_forall]
    constructor
    · intro h; exact .customTy (fun t' ht' => (ih t' ht').mp (h t' ht'))
    · intro h
      cases h with
      | customTy hall => exact fun t' ht' => (ih t' ht').mpr (hall t' ht')

/-- Decidability of scheme well-formedness, via `Ty.bvarsBelow`. -/
theorem PolyTy.wf_iff_bvarsBelow {M : PolyTy} :
    Ty.bvarsBelow M.paramCount M.body = true ↔ M.WF := Ty.bvarsBelow_iff M.body

/-- A type has no free vars iff every var fails to be free in it. -/
theorem Ty.noFreeVars_of_forall_not_mem {t : Ty} (h : ∀ z, z ∉ t.freeVars) :
    NoFreeVars t := by
  induction t using Ty.rec_strong with
  | prim p => exact .prim
  | bvar i => exact .bvar
  | fvar n => exact absurd (List.mem_singleton.mpr rfl) (h n)
  | arrow a b iha ihb =>
    refine .arrow (iha fun z hz => ?_) (ihb fun z hz => ?_)
    · exact h z (by simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inl hz)
    · exact h z (by simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inr hz)
  | customTy nm tys ih =>
    refine .customTy (fun t' ht' => ih t' ht' fun z hz => ?_)
    exact h z (by rw [Ty.freeVars]; exact TyList.mem_freeVars_of_mem ht' hz)

/-- A type has no free vars iff its `freeVars` list is empty (decidable bridge). -/
theorem Ty.noFreeVars_iff_freeVars_nil {t : Ty} :
    NoFreeVars t ↔ t.freeVars = [] := by
  refine ⟨fun h => List.eq_nil_iff_forall_not_mem.mpr (fun z => h.not_mem_freeVars z), fun h => ?_⟩
  exact Ty.noFreeVars_of_forall_not_mem (fun z hz => by rw [h] at hz; simp at hz)

/-- How an algorithmic lambda chooses its parameter type and post-binder fresh
    counter from the (optional) annotation: unannotated → a fresh unification
    variable (consuming `Φ`); annotated → the annotation `T`, which must be a
    closed monotype (consuming no fresh var). -/
inductive LamSeed (Φ : Nat) : Option Ty → Ty → Nat → Prop
  | none : LamSeed Φ none (.fvar Φ) (Φ + 1)
  | some (T : Ty) : T.IsLC → LamSeed Φ (some T) T Φ

theorem LamSeed.le {Φ : Nat} {ann pt Φ₀} (h : LamSeed Φ ann pt Φ₀) : Φ ≤ Φ₀ := by
  cases h <;> omega

/-- The chosen parameter type is locally closed (no dangling type `bvar`s). It
    MAY carry free type variables now — those are scoped type variables bound by
    an enclosing signature (the relaxation enabling `ScopedTypeVariables`). -/
theorem LamSeed.pt_isLC {Φ : Nat} {ann pt Φ₀} (h : LamSeed Φ ann pt Φ₀) : pt.IsLC := by
  cases h with
  | none => exact ContainsBvarsUpTo.fvar
  | some _ hlc => exact hlc

/-- A `LamSeed`'s annotation is fixed by `openVarsFrom` (the annotated case is
    locally closed, hence bvar-free). -/
theorem LamSeed.ann_openVarsFrom {Φ : Nat} {ann pt Φ₀} (h : LamSeed Φ ann pt Φ₀)
    (d : Nat) (Xs : List Nat) : ann.map (Ty.openVarsFrom d Xs) = ann := by
  cases h with
  | none => rfl
  | some _ hlc =>
    simp only [Option.map_some, Option.some.injEq]
    exact Ty.openVarsFrom_eq_self_of_containsBvars (hlc.mono (Nat.zero_le d))

/-! ### Closing scoped type variables in a term (the elaboration inverse of `openTyVars`)

The annotated binders (`letInAnn`, `letRecAnn`) infer the *opened* bound expression
(`rhs.openTyVars Ys`), so its elaboration comes back with the scheme's variables
resolved to fresh `fvar`s `Ys`. To place that elaboration back in the closed scope of
`.letIn (some σ) rhs body` we must **close** it over `Ys` — replacing `fvar Ys[i]` by
the scheme's `bvar`. `Expr.closeTyVars` is the structural inverse of `Expr.openTyVars`:
it mirrors `Expr.openTyVarsAux`'s depth tracking exactly, using `Ty.closeOverFrom`
(close at depth `d`, producing `bvar (d + i)`) in place of `Ty.openVarsFrom`. This is
the term-level realisation of type-passing for scoped variables: a use that
instantiated the enclosing scheme's own variable gets a `bvar` tyArg pointing at that
binder. -/

mutual
/-- Close `fvar`s in `vars` to `bvar (d + idx)` (depth-shifted `Ty.closeOver`). -/
def Ty.closeOverFrom (d : Nat) (vars : List Nat) : Ty → Ty
  | .prim p          => .prim p
  | .arrow a b       => .arrow (a.closeOverFrom d vars) (b.closeOverFrom d vars)
  | .bvar i          => .bvar i
  | .customTy nm tys => .customTy nm (TyList.closeOverFrom d vars tys)
  | .fvar n          =>
      match vars.idxOf? n with
      | some i => .bvar (d + i)
      | none   => .fvar n
def TyList.closeOverFrom (d : Nat) (vars : List Nat) : List Ty → List Ty
  | []        => []
  | hd :: tl  => hd.closeOverFrom d vars :: TyList.closeOverFrom d vars tl
end

mutual
/-- Close the scoped type variables `Xs` inside a term's annotations and var/ctor
    tyArgs: the structural inverse of `Expr.openTyVarsAux` (same `d` discipline). -/
def Expr.closeTyVarsAux (d : Nat) (Xs : List Nat) : Expr → Expr
  | .primLit p          => .primLit p
  | .lambda ann body    => .lambda (ann.map (Ty.closeOverFrom d Xs)) (body.closeTyVarsAux d Xs)
  | .app f arg          => .app (f.closeTyVarsAux d Xs) (arg.closeTyVarsAux d Xs)
  | .letIn (some σ) rhs body =>
      .letIn (some { σ with body := Ty.closeOverFrom (d + σ.paramCount) Xs σ.body })
        (rhs.closeTyVarsAux (d + σ.paramCount) Xs)
        (body.closeTyVarsAux d Xs)
  | .letIn none rhs body =>
      .letIn none (rhs.closeTyVarsAux d Xs) (body.closeTyVarsAux d Xs)
  | .var i tyArgs       => .var i (tyArgs.map (Ty.closeOverFrom d Xs))
  | .ctor c             => .ctor c
  | .match_ scrut branches =>
      .match_ (scrut.closeTyVarsAux d Xs) (BranchList.closeTyVarsAux d Xs branches)
  | .letRec bindings body =>
      .letRec (RecGroup.closeTyVarsAux d Xs bindings) (body.closeTyVarsAux d Xs)
  | .letRecAnn schemes bindings body =>
      .letRecAnn schemes (RecGroupAnn.closeTyVarsAux d Xs schemes bindings)
        (body.closeTyVarsAux d Xs)

private def BranchList.closeTyVarsAux (d : Nat) (Xs : List Nat) :
    List (MatchPattern × Expr) → List (MatchPattern × Expr)
  | []                  => []
  | (pat, body) :: rest => (pat, body.closeTyVarsAux d Xs) :: BranchList.closeTyVarsAux d Xs rest

private def RecGroup.closeTyVarsAux (d : Nat) (Xs : List Nat) : List Expr → List Expr
  | []        => []
  | e :: rest => e.closeTyVarsAux d Xs :: RecGroup.closeTyVarsAux d Xs rest

private def RecGroupAnn.closeTyVarsAux (d : Nat) (Xs : List Nat) :
    List PolyTy → List Expr → List Expr
  | _,       []        => []
  | [],      e :: rest => e.closeTyVarsAux d Xs :: RecGroupAnn.closeTyVarsAux d Xs [] rest
  | σ :: ss, e :: rest =>
      e.closeTyVarsAux (d + σ.paramCount) Xs :: RecGroupAnn.closeTyVarsAux d Xs ss rest
end

/-- Close the scoped type variables `Xs` of an enclosing scheme through a term
    (top level, `d = 0`): the inverse of `Expr.openTyVars`. -/
def Expr.closeTyVars (Xs : List Nat) (e : Expr) : Expr := e.closeTyVarsAux 0 Xs

/-- A term's *scoped* (annotation) type variables: the free type vars of its
    lambda/let/`letRecAnn` scheme annotations only, **excluding** the emitted
    `var`/`ctor` `tyArgs`. The emitted tyArgs are inference openings (generalisable),
    not user-written rigid scoped variables, so they must not enter the rigid set
    that `genScheme` excludes from generalisation — otherwise let-polymorphism through
    a polymorphic var-use would be lost (the var's fresh opening would be frozen
    rigid). This is the correct rigid set: a term's *own* annotation scoped vars are
    caught here, while outer scoped vars are excluded from generalisation via
    `env.freeVars`. Mirrors `Expr.tyFreeVars` with the `var`/`ctor` cases emptied. -/
def Expr.annTyFreeVars : Expr → List Nat
  | .primLit _          => []
  | .lambda ann body    => (ann.elim [] Ty.freeVars) ++ body.annTyFreeVars
  | .app f arg          => f.annTyFreeVars ++ arg.annTyFreeVars
  | .letIn ann rhs body => (ann.elim [] (fun σ => σ.body.freeVars)) ++ rhs.annTyFreeVars ++ body.annTyFreeVars
  | .var _ _            => []
  | .ctor _             => []
  | .match_ scrut branches => scrut.annTyFreeVars ++ BranchList.annTyFreeVars branches
  | .letRec bindings body => RecGroup.annTyFreeVars bindings ++ body.annTyFreeVars
  | .letRecAnn schemes bindings body =>
      SchemeList.annTyFreeVars schemes ++ RecGroup.annTyFreeVars bindings ++ body.annTyFreeVars
where
  BranchList.annTyFreeVars : List (MatchPattern × Expr) → List Nat
  | []                  => []
  | (_, body) :: rest   => body.annTyFreeVars ++ Expr.annTyFreeVars.BranchList.annTyFreeVars rest
  RecGroup.annTyFreeVars : List Expr → List Nat
  | []        => []
  | e :: rest => e.annTyFreeVars ++ Expr.annTyFreeVars.RecGroup.annTyFreeVars rest
  SchemeList.annTyFreeVars : List PolyTy → List Nat
  | []        => []
  | σ :: rest => σ.body.freeVars ++ Expr.annTyFreeVars.SchemeList.annTyFreeVars rest

/-- `TyList.closeOverFrom` is a `map` (mirrors `TyList.closeOver_eq_map`). -/
theorem TyList.closeOverFrom_eq_map (d : Nat) (Ys : List Nat) (l : List Ty) :
    TyList.closeOverFrom d Ys l = l.map (Ty.closeOverFrom d Ys) := by
  induction l with
  | nil => rfl
  | cons hd tl ih => simp only [TyList.closeOverFrom, List.map_cons, ih]

/-- `idxOf?` locates the element (Core has this privately). -/
private theorem List.getElem?_of_idxOf? {α : Type*} [BEq α] [LawfulBEq α]
    {l : List α} {a : α} {i : Nat} (h : l.idxOf? a = some i) : l[i]? = some a := by
  induction l generalizing i with
  | nil => simp [List.idxOf?_nil] at h
  | cons x xs ih =>
    rw [List.idxOf?_cons] at h
    split at h
    · rename_i hxa
      simp only [Option.some.injEq] at h
      subst h
      simp [eq_of_beq hxa]
    · obtain ⟨j, hj, rfl⟩ := Option.map_eq_some_iff.mp h
      simpa using ih hj

/-- Depth-general close-then-reopen-at-the-same-names is the identity. `t`'s bvars
    must be `< d` (so the `bvar (d+i)` introduced by closing are genuinely new).
    Depth-general analog of Core's `Ty.openVars_closeOver_self`. -/
theorem Ty.openVarsFrom_closeOverFrom_self {d : Nat} {Ys : List Nat} :
    ∀ {t : Ty}, ContainsBvarsUpTo d t → Ty.openVarsFrom d Ys (Ty.closeOverFrom d Ys t) = t := by
  intro t ht
  induction t using Ty.rec_strong with
  | prim p => rfl
  | bvar i =>
    cases ht with
    | bvar hlt =>
      simp only [Ty.closeOverFrom, Ty.openVarsFrom, Ty.instantiate, if_pos hlt]
  | fvar n =>
    simp only [Ty.closeOverFrom]
    cases h_idx : Ys.idxOf? n with
    | none => simp only [Ty.openVarsFrom, Ty.instantiate]
    | some i =>
      have hYi : Ys[i]? = some n := List.getElem?_of_idxOf? h_idx
      simp only [Ty.openVarsFrom, Ty.instantiate, if_neg (by omega : ¬ d + i < d),
        Nat.add_sub_cancel_left, hYi, Option.elim]
  | arrow a b iha ihb =>
    cases ht with
    | arrow ha hb =>
      simp only [Ty.closeOverFrom, Ty.openVarsFrom, Ty.instantiate] at iha ihb ⊢
      rw [iha ha, ihb hb]
  | customTy nm tys ih =>
    cases ht with
    | customTy hall =>
      simp only [Ty.closeOverFrom, TyList.closeOverFrom_eq_map, Ty.openVarsFrom, Ty.instantiate,
        TyList.instantiate_eq_map, List.map_map]
      apply congrArg (Ty.customTy nm)
      conv_rhs => rw [← List.map_id tys]
      apply List.map_congr_left
      intro t ht_mem
      have hres := ih t ht_mem (hall t ht_mem)
      simpa only [Ty.openVarsFrom, Function.comp_apply, id_eq] using hres

/-- Depth-general close-then-open-at-different-names = rename `Ys ↦ Xs`. -/
theorem Ty.openVarsFrom_closeOverFrom_rename {d : Nat} {Ys Xs : List Nat}
    (hYnodup : Ys.Nodup) (hlen : Xs.length = Ys.length) (hYXdisj : ∀ y ∈ Ys, y ∉ Xs) :
    ∀ {t : Ty}, ContainsBvarsUpTo d t →
      Ty.openVarsFrom d Xs (Ty.closeOverFrom d Ys t)
        = Ty.substFvars (Ys.zip (Xs.map (Ty.fvar ·))) t := by
  have h_freshV : ∀ Y ∈ Ys, Y ∉ Ty.freeVarsList (Xs.map (Ty.fvar ·)) := by
    intro Y hY hc
    refine hYXdisj Y hY ?_
    clear hY hlen hYnodup hYXdisj
    induction Xs with
    | nil => simp only [List.map_nil, Ty.freeVarsList] at hc; exact absurd hc List.not_mem_nil
    | cons x xs ih =>
      simp only [List.map_cons, Ty.freeVarsList, List.mem_dedup, List.mem_append] at hc
      cases hc with
      | inl h => simp only [Ty.freeVars, List.mem_singleton] at h; exact h ▸ List.mem_cons_self
      | inr h => exact List.mem_cons_of_mem _ (ih h)
  intro t ht
  induction t using Ty.rec_strong with
  | prim p => simp only [Ty.closeOverFrom, Ty.openVarsFrom, Ty.instantiate, Ty.substFvars_prim]
  | bvar i =>
    cases ht with
    | bvar hlt =>
      simp only [Ty.closeOverFrom, Ty.openVarsFrom, Ty.instantiate, if_pos hlt, Ty.substFvars_bvar]
  | fvar n =>
    simp only [Ty.closeOverFrom]
    cases h_idx : Ys.idxOf? n with
    | none =>
      simp only [Ty.openVarsFrom, Ty.instantiate]
      refine (Ty.substFvars_eq_self_of_no_key ?_).symm
      intro p hp hc
      simp only [Ty.freeVars, List.mem_singleton] at hc
      subst hc
      exact (List.idxOf?_eq_none_iff.mp h_idx) (List.of_mem_zip hp).1
    | some i =>
      have hYi : Ys[i]? = some n := List.getElem?_of_idxOf? h_idx
      obtain ⟨hi, -⟩ := List.getElem?_eq_some_iff.mp hYi
      have hxi : Xs[i]? = some Xs[i] := List.getElem?_eq_getElem (by omega)
      have hvi : (Xs.map (Ty.fvar ·))[i]? = some (Ty.fvar Xs[i]) := by
        rw [List.getElem?_map, hxi]; rfl
      simp only [Ty.openVarsFrom, Ty.instantiate, if_neg (by omega : ¬ d + i < d),
        Nat.add_sub_cancel_left, hxi, Option.elim]
      exact (Ty.substFvars_zip_fvar_eq (by rw [List.length_map]; exact hlen) hYnodup h_freshV hYi hvi).symm
  | arrow a b iha ihb =>
    cases ht with
    | arrow ha hb =>
      simp only [Ty.closeOverFrom, Ty.openVarsFrom, Ty.instantiate, Ty.substFvars_arrow] at iha ihb ⊢
      rw [iha ha, ihb hb]
  | customTy nm tys ih =>
    cases ht with
    | customTy hall =>
      simp only [Ty.closeOverFrom, TyList.closeOverFrom_eq_map, Ty.openVarsFrom, Ty.instantiate,
        TyList.instantiate_eq_map, Ty.substFvars_customTy, List.map_map]
      apply congrArg (Ty.customTy nm)
      apply List.map_congr_left
      intro t ht_mem
      have hres := ih t ht_mem (hall t ht_mem)
      simpa only [Ty.openVarsFrom, Function.comp_apply] using hres

/-- Closing over `Ys` removes every `Ys` from a type's free vars (depth-general
    analog of Core's `Ty.not_mem_closeOver_freeVars`). -/
theorem Ty.not_mem_closeOverFrom_freeVars {d : Nat} {Ys : List Nat} {g : Nat} (hg : g ∈ Ys) :
    ∀ {t : Ty}, g ∉ (Ty.closeOverFrom d Ys t).freeVars := by
  intro t
  induction t using Ty.rec_strong with
  | prim p => simp [Ty.closeOverFrom, Ty.freeVars]
  | bvar i => simp [Ty.closeOverFrom, Ty.freeVars]
  | fvar n =>
    simp only [Ty.closeOverFrom]
    cases h_idx : Ys.idxOf? n with
    | none =>
      simp only [Ty.freeVars, List.mem_singleton]
      intro hc; exact (List.idxOf?_eq_none_iff.mp h_idx) (hc ▸ hg)
    | some i => simp [Ty.freeVars]
  | arrow a b iha ihb =>
    simp only [Ty.closeOverFrom, Ty.freeVars, List.mem_dedup, List.mem_append, not_or]
    exact ⟨iha, ihb⟩
  | customTy nm tys ih =>
    simp only [Ty.closeOverFrom, Ty.freeVars, TyList.closeOverFrom_eq_map]
    rw [TyList.not_mem_freeVars_iff]
    intro t ht
    obtain ⟨t'', _, rfl⟩ := List.mem_map.mp ht
    exact ih t'' (by assumption)

private theorem RecGroup.closeTyVarsAux_eq_map (d : Nat) (Xs : List Nat) (bs : List Expr) :
    RecGroup.closeTyVarsAux d Xs bs = bs.map (·.closeTyVarsAux d Xs) := by
  induction bs with
  | nil => rfl
  | cons hd tl ih => simp only [RecGroup.closeTyVarsAux, List.map_cons, ih]

private theorem BranchList.closeTyVarsAux_eq_map {d : Nat} {Xs : List Nat}
    {brs : List (MatchPattern × Expr)} :
    BranchList.closeTyVarsAux d Xs brs = brs.map (fun pb => (pb.1, pb.2.closeTyVarsAux d Xs)) := by
  induction brs with
  | nil => rfl
  | cons hd tl ih => obtain ⟨p, b⟩ := hd; simp only [BranchList.closeTyVarsAux, List.map_cons, ih]

/-- `RecGroupAnn` close-then-reopen-at-the-same-names is the identity, given the
    per-binding identity. Direct recursion on `(schemes, bindings)` (no `eq_zip`,
    avoiding the `shieldDepths`-recomputation under the nested `open (close …)`). -/
private theorem RecGroupAnn.openClose_self (Ys : List Nat) :
    ∀ (schemes : List PolyTy) (bindings : List Expr) (d : Nat),
      Expr.TyBvarBounded.RecGroupAnn d schemes bindings →
      (∀ e ∈ bindings, ∀ d', e.TyBvarBounded d' →
        (e.closeTyVarsAux d' Ys).openTyVarsAux d' Ys = e) →
      RecGroupAnn.openTyVarsAux d Ys schemes (RecGroupAnn.closeTyVarsAux d Ys schemes bindings)
        = bindings := by
  intro schemes bindings
  induction bindings generalizing schemes with
  | nil => intro d _ _; cases schemes <;> rfl
  | cons hd tl ih =>
    intro d hbb hself
    cases schemes with
    | nil =>
      simp only [Expr.TyBvarBounded.RecGroupAnn] at hbb
      simp only [RecGroupAnn.closeTyVarsAux, RecGroupAnn.openTyVarsAux, List.cons.injEq]
      exact ⟨hself hd List.mem_cons_self d hbb.1,
             ih [] d hbb.2 (fun e he => hself e (List.mem_cons_of_mem _ he))⟩
    | cons σ ss =>
      simp only [Expr.TyBvarBounded.RecGroupAnn] at hbb
      simp only [RecGroupAnn.closeTyVarsAux, RecGroupAnn.openTyVarsAux, List.cons.injEq]
      exact ⟨hself hd List.mem_cons_self (d + σ.paramCount) hbb.1,
             ih ss d hbb.2 (fun e he => hself e (List.mem_cons_of_mem _ he))⟩

/-- **Term-level close-then-reopen-at-the-same-names = identity.** The structural
    lift of `Ty.openVarsFrom_closeOverFrom_self` over a `TyBvarBounded` term. -/
theorem Expr.openTyVarsAux_closeTyVarsAux_self {Ys : List Nat} :
    ∀ (e : Expr) (d : Nat), e.TyBvarBounded d →
      (e.closeTyVarsAux d Ys).openTyVarsAux d Ys = e := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p => intro d _; rfl
  | ctor nm => intro d _; rfl
  | var n tyArgs =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.closeTyVarsAux, Expr.openTyVarsAux, Expr.var.injEq, true_and, List.map_map]
    conv_rhs => rw [← List.map_id tyArgs]
    apply List.map_congr_left
    intro t ht
    simp only [Function.comp_def, id_eq]
    exact Ty.openVarsFrom_closeOverFrom_self (hb t ht)
  | app f arg ihf iharg =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.closeTyVarsAux, Expr.openTyVarsAux, Expr.app.injEq]
    exact ⟨ihf d hb.1, iharg d hb.2⟩
  | lambda ann body ih =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.closeTyVarsAux, Expr.openTyVarsAux, Expr.lambda.injEq]
    refine ⟨?_, ih d hb.2⟩
    cases ann with
    | none => rfl
    | some t =>
      simp only [Option.map_some, Option.some.injEq]
      exact Ty.openVarsFrom_closeOverFrom_self (hb.1 t rfl)
  | letIn ann rhs body ihrhs ihbody =>
    intro d hb
    cases ann with
    | none =>
      simp only [Expr.TyBvarBounded] at hb
      simp only [Expr.closeTyVarsAux, Expr.openTyVarsAux, Expr.letIn.injEq]
      exact ⟨trivial, ihrhs d hb.1, ihbody d hb.2⟩
    | some σ =>
      simp only [Expr.TyBvarBounded] at hb
      obtain ⟨pc, sbody⟩ := σ
      simp only [Expr.closeTyVarsAux, Expr.openTyVarsAux, Expr.letIn.injEq]
      refine ⟨?_, ihrhs (d + pc) hb.2.1, ihbody d hb.2.2⟩
      rw [Ty.openVarsFrom_closeOverFrom_self hb.1]
  | match_ scrut branches ihscrut ihbranches =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    obtain ⟨hs, hbr⟩ := hb
    rw [Expr.TyBvarBounded.BranchList_iff] at hbr
    simp only [Expr.closeTyVarsAux, Expr.openTyVarsAux, BranchList.closeTyVarsAux_eq_map,
      BranchList.openTyVarsAux_eq_map, Expr.match_.injEq, List.map_map]
    refine ⟨ihscrut d hs, ?_⟩
    conv_rhs => rw [← List.map_id branches]
    apply List.map_congr_left
    rintro ⟨p, b⟩ hpb
    simp only [Function.comp_def, id_eq]
    rw [ihbranches p b hpb d (hbr p b hpb)]
  | letRec bindings body ih_bindings ih_body =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    obtain ⟨hbs, hb'⟩ := hb
    rw [Expr.TyBvarBounded.RecGroup_iff] at hbs
    simp only [Expr.closeTyVarsAux, Expr.openTyVarsAux, RecGroup.closeTyVarsAux_eq_map,
      RecGroup.openTyVarsAux_eq_map, Expr.letRec.injEq, List.map_map]
    refine ⟨?_, ih_body d hb'⟩
    conv_rhs => rw [← List.map_id bindings]
    apply List.map_congr_left
    intro e he
    simp only [Function.comp_def, id_eq]
    exact ih_bindings e he d (hbs e he)
  | letRecAnn schemes bindings body ih_bindings ih_body =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    obtain ⟨hsc, hbs, hb'⟩ := hb
    simp only [Expr.closeTyVarsAux, Expr.openTyVarsAux, Expr.letRecAnn.injEq, true_and]
    exact ⟨RecGroupAnn.openClose_self Ys schemes bindings d hbs ih_bindings, ih_body d hb'⟩

/-- Top-level (`d = 0`) close-then-reopen identity. -/
theorem Expr.openTyVars_closeTyVars_self {Ys : List Nat} {e : Expr} (he : e.TyBvarBounded 0) :
    (e.closeTyVars Ys).openTyVars Ys = e :=
  Expr.openTyVarsAux_closeTyVarsAux_self e 0 he

/-- A term with no `letRecAnn` nodes. In Stage 1 the `Infer` relation has no
    `letRecAnn` rule, so every `Infer`-derived term satisfies this — which is what
    discharges the only non-uniform case of the close-back freshness below (the
    `letRecAnn` schemes are left untouched by `openTyVars`/`closeTyVars`, so closing
    does not remove `Ys` from them). -/
def Expr.NoRecAnn : Expr → Prop
  | .primLit _          => True
  | .lambda _ body      => body.NoRecAnn
  | .app f arg          => f.NoRecAnn ∧ arg.NoRecAnn
  | .letIn _ rhs body   => rhs.NoRecAnn ∧ body.NoRecAnn
  | .var _ _            => True
  | .ctor _             => True
  | .match_ scrut branches => scrut.NoRecAnn ∧ Expr.NoRecAnn.BranchList branches
  | .letRec bindings body  => Expr.NoRecAnn.RecGroup bindings ∧ body.NoRecAnn
  | .letRecAnn _ _ _    => False
where
  BranchList : List (MatchPattern × Expr) → Prop
  | []                  => True
  | (_, b) :: rest      => b.NoRecAnn ∧ Expr.NoRecAnn.BranchList rest
  RecGroup : List Expr → Prop
  | []        => True
  | e :: rest => e.NoRecAnn ∧ Expr.NoRecAnn.RecGroup rest

theorem Expr.NoRecAnn.BranchList_iff {brs : List (MatchPattern × Expr)} :
    Expr.NoRecAnn.BranchList brs ↔ ∀ p b, (p, b) ∈ brs → b.NoRecAnn := by
  induction brs with
  | nil => simp [Expr.NoRecAnn.BranchList]
  | cons hd tl ih =>
    obtain ⟨p, b⟩ := hd
    simp only [Expr.NoRecAnn.BranchList, ih, List.mem_cons, Prod.mk.injEq]
    constructor
    · rintro ⟨hb, hrest⟩ p' b' (heq | hmem)
      · obtain ⟨rfl, rfl⟩ := heq; exact hb
      · exact hrest p' b' hmem
    · intro h; exact ⟨h p b (Or.inl ⟨rfl, rfl⟩), fun p' b' hmem => h p' b' (Or.inr hmem)⟩

theorem Expr.NoRecAnn.RecGroup_iff {bs : List Expr} :
    Expr.NoRecAnn.RecGroup bs ↔ ∀ e ∈ bs, e.NoRecAnn := by
  induction bs with
  | nil => simp [Expr.NoRecAnn.RecGroup]
  | cons hd tl ih =>
    simp only [Expr.NoRecAnn.RecGroup, ih, List.mem_cons]
    constructor
    · rintro ⟨hb, hrest⟩ e (rfl | hmem)
      · exact hb
      · exact hrest e hmem
    · intro h; exact ⟨h hd (Or.inl rfl), fun e hmem => h e (Or.inr hmem)⟩

/-- Closing over `Ys` removes every `Ys` from a (`letRecAnn`-free) term's free type
    vars: `openTyVars`/`closeTyVars` leave `letRecAnn` schemes untouched, so they are
    the only place `Ys` could survive — excluded by `NoRecAnn`. -/
theorem Expr.not_mem_closeTyVarsAux_tyFreeVars {Ys : List Nat} {g : Nat} (hg : g ∈ Ys) :
    ∀ (e : Expr) (d : Nat), e.NoRecAnn → g ∉ (e.closeTyVarsAux d Ys).tyFreeVars := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p => intro d _; simp [Expr.closeTyVarsAux, Expr.tyFreeVars]
  | ctor nm => intro d _; simp [Expr.closeTyVarsAux, Expr.tyFreeVars]
  | var n tyArgs =>
    intro d _
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, List.mem_flatMap, not_exists, not_and]
    intro t ht hc
    obtain ⟨t', _, rfl⟩ := List.mem_map.mp ht
    exact Ty.not_mem_closeOverFrom_freeVars hg hc
  | lambda ann body ih =>
    intro d hno
    simp only [Expr.NoRecAnn] at hno
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, List.mem_append, not_or]
    refine ⟨?_, ih d hno⟩
    cases ann with
    | none => simp [Option.elim]
    | some t => simp only [Option.map_some, Option.elim]; exact Ty.not_mem_closeOverFrom_freeVars hg
  | app f arg ihf iharg =>
    intro d hno
    simp only [Expr.NoRecAnn] at hno
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, List.mem_append, not_or]
    exact ⟨ihf d hno.1, iharg d hno.2⟩
  | letIn ann rhs body ihrhs ihbody =>
    intro d hno
    simp only [Expr.NoRecAnn] at hno
    cases ann with
    | none =>
      simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, Option.elim, List.nil_append,
        List.mem_append, not_or]
      exact ⟨ihrhs d hno.1, ihbody d hno.2⟩
    | some σ =>
      obtain ⟨pc, sbody⟩ := σ
      simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, Option.elim, List.mem_append, not_or]
      exact ⟨⟨Ty.not_mem_closeOverFrom_freeVars hg, ihrhs (d + pc) hno.1⟩, ihbody d hno.2⟩
  | match_ scrut branches ihscrut ihbranches =>
    intro d hno
    simp only [Expr.NoRecAnn] at hno
    obtain ⟨hs, hbr⟩ := hno
    rw [Expr.NoRecAnn.BranchList_iff] at hbr
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, BranchList.closeTyVarsAux_eq_map,
      List.mem_append, not_or]
    refine ⟨ihscrut d hs, ?_⟩
    intro hc
    rw [show (Expr.tyFreeVars.BranchList.tyFreeVars
        (branches.map (fun pb => (pb.1, pb.2.closeTyVarsAux d Ys)))) = _ from rfl] at hc
    induction branches with
    | nil => simp [Expr.tyFreeVars.BranchList.tyFreeVars] at hc
    | cons hd tl ihtl =>
      obtain ⟨p, b⟩ := hd
      simp only [List.map_cons, Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append] at hc
      cases hc with
      | inl h => exact ihbranches p b (List.mem_cons_self ..) d (hbr p b (List.mem_cons_self ..)) h
      | inr h => exact ihtl (fun p' b' hm => ihbranches p' b' (List.mem_cons_of_mem _ hm))
                   (fun p' b' hm => hbr p' b' (List.mem_cons_of_mem _ hm)) h
  | letRec bindings body ih_bindings ih_body =>
    intro d hno
    simp only [Expr.NoRecAnn] at hno
    obtain ⟨hbs, hb⟩ := hno
    rw [Expr.NoRecAnn.RecGroup_iff] at hbs
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, RecGroup.closeTyVarsAux_eq_map,
      List.mem_append, not_or]
    refine ⟨?_, ih_body d hb⟩
    intro hc
    induction bindings with
    | nil => simp [Expr.tyFreeVars.RecGroup.tyFreeVars] at hc
    | cons hd tl ihtl =>
      simp only [List.map_cons, Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at hc
      cases hc with
      | inl h => exact ih_bindings hd (List.mem_cons_self ..) d (hbs hd (List.mem_cons_self ..)) h
      | inr h => exact ihtl (fun e hm => ih_bindings e (List.mem_cons_of_mem _ hm))
                   (fun e hm => hbs e (List.mem_cons_of_mem _ hm)) h
  | letRecAnn schemes bindings body _ _ =>
    intro d hno
    exact (hno : (False : Prop)).elim

/-- **Term-level close-then-open = rename** (for `letRecAnn`-free `TyBvarBounded`
    terms): the elaboration form used by the annotated-binder soundness. -/
theorem Expr.openTyVars_closeTyVars_rename {Ys Xs : List Nat} {e : Expr}
    (he : e.TyBvarBounded 0) (hno : e.NoRecAnn)
    (h_len : Ys.length = Xs.length) (h_Ys_nodup : Ys.Nodup) (h_Ys_Xs : ∀ y ∈ Ys, y ∉ Xs) :
    (e.closeTyVars Ys).openTyVars Xs = e.substTyFvars (Ys.zip (Xs.map (Ty.fvar ·))) := by
  have hfresh : ∀ y ∈ Ys, y ∉ (e.closeTyVars Ys).tyFreeVars := fun y hy =>
    Expr.not_mem_closeTyVarsAux_tyFreeVars hy e 0 hno
  rw [← Expr.substTyFvars_zip_openTyVars h_len h_Ys_nodup hfresh h_Ys_Xs,
      Expr.openTyVars_closeTyVars_self he]

/-! ### Freshness-based close-back (generalises the Stage-1 `NoRecAnn` blanket)

Once the elaborator emits `letRecAnn` nodes (Phase 4), the Stage-1 invariant
"`Infer` outputs are `letRecAnn`-free" (`Infer.eOut_noRecAnn`) no longer holds, so the
close-then-open=rename kernel above can no longer discharge its `letRecAnn` case by
`NoRecAnn`. But that case only mattered because `closeTyVarsAux`/`openTyVarsAux` leave
`letRecAnn` *scheme bodies* untouched — the sole place a closed-over `Ys` can survive.
The variables we ever close over are **fresh** skolems, which by construction never
appear in any (source-derived) scheme body. So we re-base the kernel on that genuine
freshness fact, captured by `Expr.recAnnSchemeFreeVars` (the scheme-body free vars). -/

/-- The free type variables sitting in the positions that `closeTyVarsAux` /
    `openTyVarsAux` leave **untouched**: the bodies of `letRecAnn` schemes (including
    those of any nested `letRecAnn`). Closing a term over `Ys` strips every `Ys`
    occurrence *except* these, so `∀ y ∈ Ys, y ∉ e.recAnnSchemeFreeVars` is exactly the
    side-condition the close-then-open=rename kernel needs once the elaborator can emit
    `letRecAnn`. It generalises the Stage-1 `NoRecAnn` blanket (for which this set is
    `[]`); fresh skolems discharge it for free. -/
def Expr.recAnnSchemeFreeVars : Expr → List Nat
  | .primLit _             => []
  | .lambda _ body         => body.recAnnSchemeFreeVars
  | .app f arg             => f.recAnnSchemeFreeVars ++ arg.recAnnSchemeFreeVars
  | .letIn _ rhs body      => rhs.recAnnSchemeFreeVars ++ body.recAnnSchemeFreeVars
  | .var _ _               => []
  | .ctor _                => []
  | .match_ scrut branches => scrut.recAnnSchemeFreeVars ++ BranchList.recAnnSchemeFreeVars branches
  | .letRec bindings body  => RecGroup.recAnnSchemeFreeVars bindings ++ body.recAnnSchemeFreeVars
  | .letRecAnn schemes bindings body =>
      Expr.tyFreeVars.SchemeList.tyFreeVars schemes
        ++ RecGroup.recAnnSchemeFreeVars bindings ++ body.recAnnSchemeFreeVars
where
  BranchList.recAnnSchemeFreeVars : List (MatchPattern × Expr) → List Nat
  | []             => []
  | (_, b) :: rest => b.recAnnSchemeFreeVars ++ Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars rest
  RecGroup.recAnnSchemeFreeVars : List Expr → List Nat
  | []        => []
  | e :: rest => e.recAnnSchemeFreeVars ++ Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars rest

/-- Membership in a binding group's `recAnnSchemeFreeVars` is membership in some
    binding's. -/
theorem Expr.mem_recAnnSchemeFreeVars_RecGroup {bs : List Expr} {g : Nat} :
    g ∈ Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars bs
      ↔ ∃ e ∈ bs, g ∈ e.recAnnSchemeFreeVars := by
  induction bs with
  | nil => simp [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars]
  | cons hd tl ih =>
    simp only [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars, List.mem_append, ih,
      List.mem_cons, exists_eq_or_imp]

/-- The annotated-group close keeps the schemes' bodies untouched but descends into each
    binding (depth-shifted by its scheme), so a `g` absent from every binding's
    `recAnnSchemeFreeVars` survives nowhere in the closed bindings. -/
private theorem RecGroupAnn.not_mem_closeTyVarsAux_tyFreeVars {Ys : List Nat} {g : Nat}
    (_hg : g ∈ Ys) :
    ∀ (schemes : List PolyTy) (bindings : List Expr) (d : Nat),
      (∀ e ∈ bindings, ∀ d', g ∉ e.recAnnSchemeFreeVars →
          g ∉ (e.closeTyVarsAux d' Ys).tyFreeVars) →
      (∀ e ∈ bindings, g ∉ e.recAnnSchemeFreeVars) →
      g ∉ Expr.tyFreeVars.RecGroup.tyFreeVars
            (RecGroupAnn.closeTyVarsAux d Ys schemes bindings) := by
  intro schemes bindings
  induction bindings generalizing schemes with
  | nil =>
    intro d _ _
    cases schemes <;>
      simp [RecGroupAnn.closeTyVarsAux, Expr.tyFreeVars.RecGroup.tyFreeVars]
  | cons hd tl ih =>
    intro d ihB hfree
    cases schemes with
    | nil =>
      simp only [RecGroupAnn.closeTyVarsAux, Expr.tyFreeVars.RecGroup.tyFreeVars,
        List.mem_append, not_or]
      exact ⟨ihB hd List.mem_cons_self d (hfree hd List.mem_cons_self),
        ih [] d (fun e he => ihB e (List.mem_cons_of_mem _ he))
          (fun e he => hfree e (List.mem_cons_of_mem _ he))⟩
    | cons σ ss =>
      simp only [RecGroupAnn.closeTyVarsAux, Expr.tyFreeVars.RecGroup.tyFreeVars,
        List.mem_append, not_or]
      exact ⟨ihB hd List.mem_cons_self (d + σ.paramCount) (hfree hd List.mem_cons_self),
        ih ss d (fun e he => ihB e (List.mem_cons_of_mem _ he))
          (fun e he => hfree e (List.mem_cons_of_mem _ he))⟩

/-- Freshness-based generalisation of `not_mem_closeTyVarsAux_tyFreeVars`: closing over
    `Ys` removes `g ∈ Ys` from the term's free type vars provided `g` is not buried in an
    (untouched) `letRecAnn` scheme body. -/
theorem Expr.not_mem_closeTyVarsAux_tyFreeVars_of_fresh {Ys : List Nat} {g : Nat}
    (hg : g ∈ Ys) :
    ∀ (e : Expr) (d : Nat), g ∉ e.recAnnSchemeFreeVars →
      g ∉ (e.closeTyVarsAux d Ys).tyFreeVars := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p => intro d _; simp [Expr.closeTyVarsAux, Expr.tyFreeVars]
  | ctor nm => intro d _; simp [Expr.closeTyVarsAux, Expr.tyFreeVars]
  | var n tyArgs =>
    intro d _
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, List.mem_flatMap, not_exists, not_and]
    intro t ht hc
    obtain ⟨t', _, rfl⟩ := List.mem_map.mp ht
    exact Ty.not_mem_closeOverFrom_freeVars hg hc
  | lambda ann body ih =>
    intro d hfree
    simp only [Expr.recAnnSchemeFreeVars] at hfree
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, List.mem_append, not_or]
    refine ⟨?_, ih d hfree⟩
    cases ann with
    | none => simp [Option.elim]
    | some t => simp only [Option.map_some, Option.elim]; exact Ty.not_mem_closeOverFrom_freeVars hg
  | app f arg ihf iharg =>
    intro d hfree
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append, not_or] at hfree
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, List.mem_append, not_or]
    exact ⟨ihf d hfree.1, iharg d hfree.2⟩
  | letIn ann rhs body ihrhs ihbody =>
    intro d hfree
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append, not_or] at hfree
    cases ann with
    | none =>
      simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, Option.elim, List.nil_append,
        List.mem_append, not_or]
      exact ⟨ihrhs d hfree.1, ihbody d hfree.2⟩
    | some σ =>
      obtain ⟨pc, sbody⟩ := σ
      simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, Option.elim, List.mem_append, not_or]
      exact ⟨⟨Ty.not_mem_closeOverFrom_freeVars hg, ihrhs (d + pc) hfree.1⟩, ihbody d hfree.2⟩
  | match_ scrut branches ihscrut ihbranches =>
    intro d hfree
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append, not_or] at hfree
    obtain ⟨hs, hbr⟩ := hfree
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, BranchList.closeTyVarsAux_eq_map,
      List.mem_append, not_or]
    refine ⟨ihscrut d hs, ?_⟩
    intro hc
    rw [show (Expr.tyFreeVars.BranchList.tyFreeVars
        (branches.map (fun pb => (pb.1, pb.2.closeTyVarsAux d Ys)))) = _ from rfl] at hc
    induction branches with
    | nil => simp [Expr.tyFreeVars.BranchList.tyFreeVars] at hc
    | cons hd tl ihtl =>
      obtain ⟨p, b⟩ := hd
      simp only [List.map_cons, Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append] at hc
      simp only [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars, List.mem_append,
        not_or] at hbr
      cases hc with
      | inl h => exact ihbranches p b (List.mem_cons_self ..) d hbr.1 h
      | inr h => exact ihtl (fun p' b' hm => ihbranches p' b' (List.mem_cons_of_mem _ hm)) hbr.2 h
  | letRec bindings body ih_bindings ih_body =>
    intro d hfree
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append, not_or] at hfree
    obtain ⟨hbs, hb⟩ := hfree
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, RecGroup.closeTyVarsAux_eq_map,
      List.mem_append, not_or]
    refine ⟨?_, ih_body d hb⟩
    intro hc
    induction bindings with
    | nil => simp [Expr.tyFreeVars.RecGroup.tyFreeVars] at hc
    | cons hd tl ihtl =>
      simp only [List.map_cons, Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at hc
      simp only [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars, List.mem_append,
        not_or] at hbs
      cases hc with
      | inl h => exact ih_bindings hd (List.mem_cons_self ..) d hbs.1 h
      | inr h => exact ihtl (fun e hm => ih_bindings e (List.mem_cons_of_mem _ hm)) hbs.2 h
  | letRecAnn schemes bindings body ih_bindings ih_body =>
    intro d hfree
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append, not_or] at hfree
    obtain ⟨⟨hsch, hbs⟩, hb⟩ := hfree
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, List.mem_append, not_or]
    refine ⟨⟨hsch, ?_⟩, ih_body d hb⟩
    exact RecGroupAnn.not_mem_closeTyVarsAux_tyFreeVars hg schemes bindings d ih_bindings
      (fun e he hc => hbs (Expr.mem_recAnnSchemeFreeVars_RecGroup.mpr ⟨e, he, hc⟩))

/-- **Term-level close-then-open = rename**, freshness form: the variant of
    `openTyVars_closeTyVars_rename` whose `letRecAnn`-safety comes from the closed-over
    `Ys` being absent from every (untouched) scheme body, rather than from `NoRecAnn`. -/
theorem Expr.openTyVars_closeTyVars_rename_of_fresh {Ys Xs : List Nat} {e : Expr}
    (he : e.TyBvarBounded 0) (hfresh : ∀ y ∈ Ys, y ∉ e.recAnnSchemeFreeVars)
    (h_len : Ys.length = Xs.length) (h_Ys_nodup : Ys.Nodup) (h_Ys_Xs : ∀ y ∈ Ys, y ∉ Xs) :
    (e.closeTyVars Ys).openTyVars Xs = e.substTyFvars (Ys.zip (Xs.map (Ty.fvar ·))) := by
  have hf : ∀ y ∈ Ys, y ∉ (e.closeTyVars Ys).tyFreeVars := fun y hy =>
    Expr.not_mem_closeTyVarsAux_tyFreeVars_of_fresh hy e 0 (hfresh y hy)
  rw [← Expr.substTyFvars_zip_openTyVars h_len h_Ys_nodup hf h_Ys_Xs,
      Expr.openTyVars_closeTyVars_self he]

/-- `NoRecAnn` terms have no untouched scheme positions: `recAnnSchemeFreeVars = []`.
    Lets the existing `NoRecAnn`-based close-back sites feed the freshness kernel until
    they migrate to genuine freshness. -/
theorem Expr.recAnnSchemeFreeVars_eq_nil_of_noRecAnn {e : Expr} (h : e.NoRecAnn) :
    e.recAnnSchemeFreeVars = [] := by
  induction e using Expr.rec_strong with
  | primLit p => rfl
  | ctor nm => rfl
  | var n tyArgs => rfl
  | lambda ann body ih => simp only [Expr.NoRecAnn] at h; simp [Expr.recAnnSchemeFreeVars, ih h]
  | app f arg ihf iharg =>
    simp only [Expr.NoRecAnn] at h
    simp [Expr.recAnnSchemeFreeVars, ihf h.1, iharg h.2]
  | letIn ann rhs body ihrhs ihbody =>
    simp only [Expr.NoRecAnn] at h
    simp [Expr.recAnnSchemeFreeVars, ihrhs h.1, ihbody h.2]
  | match_ scrut branches ihscrut ihbranches =>
    simp only [Expr.NoRecAnn] at h
    obtain ⟨hs, hbr⟩ := h
    rw [Expr.NoRecAnn.BranchList_iff] at hbr
    simp only [Expr.recAnnSchemeFreeVars, ihscrut hs, List.nil_append]
    induction branches with
    | nil => rfl
    | cons hd tl ihtl =>
      obtain ⟨p, b⟩ := hd
      simp only [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars,
        ihbranches p b (List.mem_cons_self ..) (hbr p b (List.mem_cons_self ..)), List.nil_append]
      exact ihtl (fun p' b' hm => ihbranches p' b' (List.mem_cons_of_mem _ hm))
        (fun p' b' hm => hbr p' b' (List.mem_cons_of_mem _ hm))
  | letRec bindings body ih_bindings ih_body =>
    simp only [Expr.NoRecAnn] at h
    obtain ⟨hbs, hb⟩ := h
    rw [Expr.NoRecAnn.RecGroup_iff] at hbs
    simp only [Expr.recAnnSchemeFreeVars, ih_body hb, List.append_nil]
    induction bindings with
    | nil => rfl
    | cons hd tl ihtl =>
      simp only [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars,
        ih_bindings hd (List.mem_cons_self ..) (hbs hd (List.mem_cons_self ..)), List.nil_append]
      exact ihtl (fun e hm => ih_bindings e (List.mem_cons_of_mem _ hm))
        (fun e hm => hbs e (List.mem_cons_of_mem _ hm))
  | letRecAnn schemes bindings body _ _ => exact (h : (False : Prop)).elim

/-- The scheme-body free vars are among all the term's free type vars. -/
theorem Expr.recAnnSchemeFreeVars_subset_tyFreeVars {e : Expr} {g : Nat} :
    g ∈ e.recAnnSchemeFreeVars → g ∈ e.tyFreeVars := by
  induction e using Expr.rec_strong with
  | primLit p => intro h; exact h
  | ctor nm => intro h; exact h
  | var n tyArgs => intro h; simp [Expr.recAnnSchemeFreeVars] at h
  | lambda ann body ih =>
    intro h
    simp only [Expr.recAnnSchemeFreeVars] at h
    simp only [Expr.tyFreeVars, List.mem_append]
    exact Or.inr (ih h)
  | app f arg ihf iharg =>
    intro h
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at h
    simp only [Expr.tyFreeVars, List.mem_append]
    exact h.imp ihf iharg
  | letIn ann rhs body ihr ihb =>
    intro h
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at h
    simp only [Expr.tyFreeVars, List.mem_append]
    rcases h with h | h
    · exact Or.inl (Or.inr (ihr h))
    · exact Or.inr (ihb h)
  | match_ scrut branches ihs ihbr =>
    intro h
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at h
    simp only [Expr.tyFreeVars, List.mem_append]
    rcases h with h | h
    · exact Or.inl (ihs h)
    · refine Or.inr ?_
      induction branches with
      | nil => simp [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars] at h
      | cons hd tl ihtl =>
        obtain ⟨p, b⟩ := hd
        simp only [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars, List.mem_append] at h
        simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]
        rcases h with h | h
        · exact Or.inl (ihbr p b (List.mem_cons_self ..) h)
        · exact Or.inr (ihtl (fun p' b' hm => ihbr p' b' (List.mem_cons_of_mem _ hm)) h)
  | letRec bindings body ih_bindings ih_body =>
    intro h
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at h
    simp only [Expr.tyFreeVars, List.mem_append]
    rcases h with h | h
    · refine Or.inl ?_
      induction bindings with
      | nil => simp [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars] at h
      | cons hd tl ihtl =>
        simp only [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars, List.mem_append] at h
        simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]
        rcases h with h | h
        · exact Or.inl (ih_bindings hd (List.mem_cons_self ..) h)
        · exact Or.inr (ihtl (fun e' hm => ih_bindings e' (List.mem_cons_of_mem _ hm)) h)
    · exact Or.inr (ih_body h)
  | letRecAnn schemes bindings body ih_bindings ih_body =>
    intro h
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at h
    simp only [Expr.tyFreeVars, List.mem_append]
    rcases h with (h | h) | h
    · exact Or.inl (Or.inl h)
    · refine Or.inl (Or.inr ?_)
      induction bindings with
      | nil => simp [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars] at h
      | cons hd tl ihtl =>
        simp only [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars, List.mem_append] at h
        simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]
        rcases h with h | h
        · exact Or.inl (ih_bindings hd (List.mem_cons_self ..) h)
        · exact Or.inr (ihtl (fun e' hm => ih_bindings e' (List.mem_cons_of_mem _ hm)) h)
    · exact Or.inr (ih_body h)

/-- Membership in a branch list's `recAnnSchemeFreeVars` is membership in some branch
    body's. -/
theorem Expr.mem_recAnnSchemeFreeVars_BranchList {brs : List (MatchPattern × Expr)} {g : Nat} :
    g ∈ Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars brs
      ↔ ∃ pb ∈ brs, g ∈ pb.2.recAnnSchemeFreeVars := by
  induction brs with
  | nil => simp [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars]
  | cons hd tl ih =>
    obtain ⟨p, b⟩ := hd
    simp only [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars, List.mem_append, ih,
      List.mem_cons, exists_eq_or_imp]

/-- The annotated-group close keeps the schemes' bodies untouched and recurses into each
    binding (depth-shifted by its scheme), preserving `recAnnSchemeFreeVars`. -/
private theorem RecGroupAnn.recAnnSchemeFreeVars_closeTyVarsAux {Xs : List Nat} :
    ∀ (schemes : List PolyTy) (bindings : List Expr) (d : Nat),
      (∀ e ∈ bindings, ∀ d', (e.closeTyVarsAux d' Xs).recAnnSchemeFreeVars = e.recAnnSchemeFreeVars) →
      Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars
          (RecGroupAnn.closeTyVarsAux d Xs schemes bindings)
        = Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars bindings := by
  intro schemes bindings
  induction bindings generalizing schemes with
  | nil => intro d _; cases schemes <;> rfl
  | cons hd tl ih =>
    intro d ihB
    cases schemes with
    | nil =>
      simp only [RecGroupAnn.closeTyVarsAux, Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars]
      rw [ihB hd List.mem_cons_self d, ih [] d (fun e he => ihB e (List.mem_cons_of_mem _ he))]
    | cons σ ss =>
      simp only [RecGroupAnn.closeTyVarsAux, Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars]
      rw [ihB hd List.mem_cons_self (d + σ.paramCount),
          ih ss d (fun e he => ihB e (List.mem_cons_of_mem _ he))]

/-- `closeTyVarsAux` preserves `recAnnSchemeFreeVars` (it leaves `letRecAnn` scheme
    bodies untouched and only renames closed-over names in the touched positions). -/
theorem Expr.recAnnSchemeFreeVars_closeTyVarsAux {Xs : List Nat} :
    ∀ (e : Expr) (d : Nat), (e.closeTyVarsAux d Xs).recAnnSchemeFreeVars = e.recAnnSchemeFreeVars := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p => intro d; simp only [Expr.closeTyVarsAux, Expr.recAnnSchemeFreeVars]
  | ctor c => intro d; simp only [Expr.closeTyVarsAux, Expr.recAnnSchemeFreeVars]
  | var i tyArgs => intro d; simp only [Expr.closeTyVarsAux, Expr.recAnnSchemeFreeVars]
  | lambda ann body ih => intro d; simp only [Expr.closeTyVarsAux, Expr.recAnnSchemeFreeVars, ih]
  | app f arg ihf iharg => intro d; simp only [Expr.closeTyVarsAux, Expr.recAnnSchemeFreeVars, ihf, iharg]
  | letIn ann rhs body ihr ihb =>
    intro d
    cases ann with
    | none => simp only [Expr.closeTyVarsAux, Expr.recAnnSchemeFreeVars, ihr, ihb]
    | some σ => obtain ⟨pc, sb⟩ := σ; simp only [Expr.closeTyVarsAux, Expr.recAnnSchemeFreeVars, ihr, ihb]
  | match_ scrut branches ihs ihbr =>
    intro d
    simp only [Expr.closeTyVarsAux, Expr.recAnnSchemeFreeVars, BranchList.closeTyVarsAux_eq_map, ihs]
    congr 1
    induction branches with
    | nil => rfl
    | cons hd tl ihtl =>
      obtain ⟨p, b⟩ := hd
      simp only [List.map_cons]
      change (b.closeTyVarsAux d Xs).recAnnSchemeFreeVars ++ _
          = b.recAnnSchemeFreeVars ++ Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars tl
      rw [ihbr p b List.mem_cons_self d, ihtl (fun p' b' hmem => ihbr p' b' (List.mem_cons_of_mem _ hmem))]
  | letRec bindings body ihbs ihb =>
    intro d
    simp only [Expr.closeTyVarsAux, Expr.recAnnSchemeFreeVars, RecGroup.closeTyVarsAux_eq_map, ihb]
    congr 1
    induction bindings with
    | nil => rfl
    | cons hd tl ihtl =>
      simp only [List.map_cons]
      change (hd.closeTyVarsAux d Xs).recAnnSchemeFreeVars ++ _
          = hd.recAnnSchemeFreeVars ++ Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars tl
      rw [ihbs hd List.mem_cons_self d, ihtl (fun e' hmem => ihbs e' (List.mem_cons_of_mem _ hmem))]
  | letRecAnn schemes bindings body ihbs ihb =>
    intro d
    simp only [Expr.closeTyVarsAux, Expr.recAnnSchemeFreeVars, ihb]
    congr 1
    congr 1
    exact RecGroupAnn.recAnnSchemeFreeVars_closeTyVarsAux schemes bindings d (fun e he d' => ihbs e he d')

/-- `closeTyVars` preserves `recAnnSchemeFreeVars` (top level). -/
theorem Expr.recAnnSchemeFreeVars_closeTyVars {Xs : List Nat} {e : Expr} :
    (e.closeTyVars Xs).recAnnSchemeFreeVars = e.recAnnSchemeFreeVars :=
  Expr.recAnnSchemeFreeVars_closeTyVarsAux e 0

/-- The annotated-group open keeps the schemes' bodies untouched and recurses into each
    binding, preserving `recAnnSchemeFreeVars`. -/
private theorem RecGroupAnn.recAnnSchemeFreeVars_openTyVarsAux {Xs : List Nat} :
    ∀ (schemes : List PolyTy) (bindings : List Expr) (d : Nat),
      (∀ e ∈ bindings, ∀ d', (e.openTyVarsAux d' Xs).recAnnSchemeFreeVars = e.recAnnSchemeFreeVars) →
      Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars
          (RecGroupAnn.openTyVarsAux d Xs schemes bindings)
        = Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars bindings := by
  intro schemes bindings
  induction bindings generalizing schemes with
  | nil => intro d _; cases schemes <;> rfl
  | cons hd tl ih =>
    intro d ihB
    cases schemes with
    | nil =>
      simp only [RecGroupAnn.openTyVarsAux, Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars]
      rw [ihB hd List.mem_cons_self d, ih [] d (fun e he => ihB e (List.mem_cons_of_mem _ he))]
    | cons σ ss =>
      simp only [RecGroupAnn.openTyVarsAux, Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars]
      rw [ihB hd List.mem_cons_self (d + σ.paramCount),
          ih ss d (fun e he => ihB e (List.mem_cons_of_mem _ he))]

/-- `openTyVarsAux` preserves `recAnnSchemeFreeVars` (scheme bodies are untouched). -/
theorem Expr.recAnnSchemeFreeVars_openTyVarsAux {Xs : List Nat} :
    ∀ (e : Expr) (d : Nat), (e.openTyVarsAux d Xs).recAnnSchemeFreeVars = e.recAnnSchemeFreeVars := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p => intro d; simp only [Expr.openTyVarsAux, Expr.recAnnSchemeFreeVars]
  | ctor c => intro d; simp only [Expr.openTyVarsAux, Expr.recAnnSchemeFreeVars]
  | var i tyArgs => intro d; simp only [Expr.openTyVarsAux, Expr.recAnnSchemeFreeVars]
  | lambda ann body ih => intro d; simp only [Expr.openTyVarsAux, Expr.recAnnSchemeFreeVars, ih]
  | app f arg ihf iharg => intro d; simp only [Expr.openTyVarsAux, Expr.recAnnSchemeFreeVars, ihf, iharg]
  | letIn ann rhs body ihr ihb =>
    intro d
    cases ann with
    | none => simp only [Expr.openTyVarsAux, Expr.recAnnSchemeFreeVars, ihr, ihb]
    | some σ => simp only [Expr.openTyVarsAux, Expr.recAnnSchemeFreeVars, ihr, ihb]
  | match_ scrut branches ihs ihbr =>
    intro d
    simp only [Expr.openTyVarsAux, Expr.recAnnSchemeFreeVars, BranchList.openTyVarsAux_eq_map, ihs]
    congr 1
    induction branches with
    | nil => rfl
    | cons hd tl ihtl =>
      obtain ⟨p, b⟩ := hd
      simp only [List.map_cons]
      change (b.openTyVarsAux d Xs).recAnnSchemeFreeVars ++ _
          = b.recAnnSchemeFreeVars ++ Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars tl
      rw [ihbr p b List.mem_cons_self d, ihtl (fun p' b' hmem => ihbr p' b' (List.mem_cons_of_mem _ hmem))]
  | letRec bindings body ihbs ihb =>
    intro d
    simp only [Expr.openTyVarsAux, Expr.recAnnSchemeFreeVars, RecGroup.openTyVarsAux_eq_map, ihb]
    congr 1
    induction bindings with
    | nil => rfl
    | cons hd tl ihtl =>
      simp only [List.map_cons]
      change (hd.openTyVarsAux d Xs).recAnnSchemeFreeVars ++ _
          = hd.recAnnSchemeFreeVars ++ Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars tl
      rw [ihbs hd List.mem_cons_self d, ihtl (fun e' hmem => ihbs e' (List.mem_cons_of_mem _ hmem))]
  | letRecAnn schemes bindings body ihbs ihb =>
    intro d
    simp only [Expr.openTyVarsAux, Expr.recAnnSchemeFreeVars, ihb]
    congr 1
    congr 1
    exact RecGroupAnn.recAnnSchemeFreeVars_openTyVarsAux schemes bindings d (fun e he d' => ihbs e he d')

/-- Opening scoped type variables leaves the untouched scheme-body positions
    unchanged, hence preserves `recAnnSchemeFreeVars`. -/
theorem Expr.recAnnSchemeFreeVars_openTyVars {Xs : List Nat} {e : Expr} :
    (e.openTyVars Xs).recAnnSchemeFreeVars = e.recAnnSchemeFreeVars :=
  Expr.recAnnSchemeFreeVars_openTyVarsAux e 0

/-- Term-var shifting preserves `recAnnSchemeFreeVars` (it only renames term `bvar`s). -/
theorem Expr.recAnnSchemeFreeVars_shiftFrom (e : Expr) (t n : Nat) :
    (e.shiftFrom t n).recAnnSchemeFreeVars = e.recAnnSchemeFreeVars := by
  induction e using Expr.rec_strong generalizing t with
  | primLit p => rfl
  | ctor c => rfl
  | var i tyArgs => simp only [Expr.shiftFrom]; split <;> rfl
  | lambda ann body ih => simp only [Expr.shiftFrom, Expr.recAnnSchemeFreeVars, ih]
  | app f arg ihf iharg => simp only [Expr.shiftFrom, Expr.recAnnSchemeFreeVars, ihf, iharg]
  | letIn ann rhs body ihr ihb => simp only [Expr.shiftFrom, Expr.recAnnSchemeFreeVars, ihr, ihb]
  | match_ scrut branches ihs ihbr =>
    simp only [Expr.shiftFrom, Expr.recAnnSchemeFreeVars, ihs]
    congr 1
    induction branches with
    | nil => rfl
    | cons hd tl ihtl =>
      obtain ⟨p, b⟩ := hd
      change (b.shiftFrom (t + p.bindCount) n).recAnnSchemeFreeVars
          ++ _ = b.recAnnSchemeFreeVars ++ Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars tl
      rw [ihbr p b List.mem_cons_self,
          ihtl (fun p' b' hmem => ihbr p' b' (List.mem_cons_of_mem _ hmem))]
  | letRec bindings body ihbs ihb =>
    simp only [Expr.shiftFrom, Expr.recAnnSchemeFreeVars, ihb]
    congr 1
    generalize t + bindings.length = thr
    induction bindings with
    | nil => rfl
    | cons hd tl ihtl =>
      change (hd.shiftFrom thr n).recAnnSchemeFreeVars
          ++ _ = hd.recAnnSchemeFreeVars ++ Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars tl
      rw [ihbs hd List.mem_cons_self thr,
          ihtl (fun e' hmem => ihbs e' (List.mem_cons_of_mem _ hmem))]
  | letRecAnn schemes bindings body ihbs ihb =>
    simp only [Expr.shiftFrom, Expr.recAnnSchemeFreeVars, ihb]
    congr 1
    congr 1
    generalize t + bindings.length = thr
    induction bindings with
    | nil => rfl
    | cons hd tl ihtl =>
      change (hd.shiftFrom thr n).recAnnSchemeFreeVars
          ++ _ = hd.recAnnSchemeFreeVars ++ Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars tl
      rw [ihbs hd List.mem_cons_self thr,
          ihtl (fun e' hmem => ihbs e' (List.mem_cons_of_mem _ hmem))]

/-- Close each elaborated `letRec` binding over the variables actually generalised
    for it (`G ∩ ftv τⱼ = Ty.genFilter G τⱼ`), matching the per-binding scheme
    `PolyTy.genGroup G τⱼ`. The shared pool is `G = genGroupVars rigid env τs`. -/
def Expr.closeRecGroup (rigid : List Nat) (env : Env) (bindingsOut : List Expr)
    (τs : List Ty) : List Expr :=
  (bindingsOut.zip τs).map
    (fun p => p.1.closeTyVars (Ty.genFilter (genGroupVars rigid env τs) p.2))

/-- Λ-outside encoding of an elaborated `letRec` (one nest level per member). Given
    `(i, τ)` for member `i` with shared gen-pool `G`, bind the per-binding scheme
    `Mᵢ = genGroup G τᵢ` to a fresh copy of the *monomorphic* group `rawBindings`
    (its outer de Bruijn refs shifted past the `i`-enclosing wrappers — there are
    `n - 1 - i` of them) projecting member `i`, with the generalised variables
    `gvᵢ = genFilter G τᵢ` closed back over that copy. The members are processed
    outermost-first; calling with member `0` last (innermost) keeps the body's
    group de Bruijn indices unchanged (`M₀` at `0`, …, `M₍ₙ₋₁₎` at `n-1`). -/
def Expr.letRecElabNest (G : List Nat) (n : Nat) (rawBindings : List Expr) :
    List (Nat × Ty) → Expr → Expr
  | [], body => body
  | (i, τ) :: rest, body =>
      .letIn (some (PolyTy.genGroup G τ))
        (Expr.closeTyVars (Ty.genFilter G τ)
          (.letRec (rawBindings.map (·.shiftFrom n (n - 1 - i))) (.var i [])))
        (Expr.letRecElabNest G n rawBindings rest body)

/-- The full Λ-outside elaborated `letRec`: stack the per-member wrappers with
    member `0` innermost (so the body sees `M₀ … M₍ₙ₋₁₎` at indices `0 … n-1`,
    matching `genGroupSchemes`). -/
def Expr.letRecElab (G : List Nat) (rawBindings : List Expr) (τs : List Ty)
    (body : Expr) : Expr :=
  Expr.letRecElabNest G rawBindings.length rawBindings
    (((List.range rawBindings.length).zip τs).reverse) body

/-- Term-var shifting leaves all *type* annotations untouched, so it preserves the
    set of free type variables. -/
theorem Expr.shiftFrom_tyFreeVars (e : Expr) (t n : Nat) :
    (e.shiftFrom t n).tyFreeVars = e.tyFreeVars := by
  induction e using Expr.rec_strong generalizing t with
  | primLit p => rfl
  | ctor c => rfl
  | var i tyArgs => simp only [Expr.shiftFrom]; split <;> rfl
  | lambda ann body ih => simp only [Expr.shiftFrom, Expr.tyFreeVars, ih]
  | app f arg ihf iharg => simp only [Expr.shiftFrom, Expr.tyFreeVars, ihf, iharg]
  | letIn ann rhs body ihr ihb => simp only [Expr.shiftFrom, Expr.tyFreeVars, ihr, ihb]
  | match_ scrut branches ihs ihbr =>
    simp only [Expr.shiftFrom, Expr.tyFreeVars, ihs]
    congr 1
    induction branches with
    | nil => rfl
    | cons hd tl ihtl =>
      obtain ⟨p, b⟩ := hd
      change (b.shiftFrom (t + p.bindCount) n).tyFreeVars
          ++ _ = b.tyFreeVars ++ Expr.tyFreeVars.BranchList.tyFreeVars tl
      rw [ihbr p b List.mem_cons_self,
          ihtl (fun p' b' hmem => ihbr p' b' (List.mem_cons_of_mem _ hmem))]
  | letRec bindings body ihbs ihb =>
    simp only [Expr.shiftFrom, Expr.tyFreeVars, ihb]
    congr 1
    generalize t + bindings.length = thr
    induction bindings with
    | nil => rfl
    | cons hd tl ihtl =>
      change (hd.shiftFrom thr n).tyFreeVars
          ++ _ = hd.tyFreeVars ++ Expr.tyFreeVars.RecGroup.tyFreeVars tl
      rw [ihbs hd List.mem_cons_self thr,
          ihtl (fun e' hmem => ihbs e' (List.mem_cons_of_mem _ hmem))]
  | letRecAnn schemes bindings body ihbs ihb =>
    simp only [Expr.shiftFrom, Expr.tyFreeVars, ihb]
    congr 1
    congr 1
    generalize t + bindings.length = thr
    induction bindings with
    | nil => rfl
    | cons hd tl ihtl =>
      change (hd.shiftFrom thr n).tyFreeVars
          ++ _ = hd.tyFreeVars ++ Expr.tyFreeVars.RecGroup.tyFreeVars tl
      rw [ihbs hd List.mem_cons_self thr,
          ihtl (fun e' hmem => ihbs e' (List.mem_cons_of_mem _ hmem))]

/-- Term-var shifting preserves `NoRecAnn` (it rebuilds every node shape unchanged;
    `letRecAnn` never appears under the hypothesis). -/
theorem Expr.NoRecAnn.shiftFrom (n : Nat) {e : Expr} :
    ∀ t, e.NoRecAnn → (e.shiftFrom t n).NoRecAnn := by
  induction e using Expr.rec_strong with
  | primLit p => intro t _; exact trivial
  | ctor c => intro t _; exact trivial
  | var i tyArgs => intro t _; simp only [Expr.shiftFrom]; split <;> exact trivial
  | lambda ann body ih =>
    intro t hno; simp only [Expr.NoRecAnn] at hno
    simp only [Expr.shiftFrom, Expr.NoRecAnn]; exact ih (t + 1) hno
  | app f arg ihf iharg =>
    intro t hno; simp only [Expr.NoRecAnn] at hno
    simp only [Expr.shiftFrom, Expr.NoRecAnn]; exact ⟨ihf t hno.1, iharg t hno.2⟩
  | letIn ann rhs body ihr ihb =>
    intro t hno; simp only [Expr.NoRecAnn] at hno
    simp only [Expr.shiftFrom, Expr.NoRecAnn]; exact ⟨ihr t hno.1, ihb (t + 1) hno.2⟩
  | match_ scrut branches ihs ihbr =>
    intro t hno; simp only [Expr.NoRecAnn] at hno
    simp only [Expr.shiftFrom, Expr.NoRecAnn]
    refine ⟨ihs t hno.1, ?_⟩
    obtain ⟨_, hbr⟩ := hno
    induction branches with
    | nil => exact trivial
    | cons hd tl ihtl =>
      obtain ⟨p, b⟩ := hd
      exact ⟨ihbr p b List.mem_cons_self (t + p.bindCount) hbr.1,
             ihtl (fun p' b' hmem => ihbr p' b' (List.mem_cons_of_mem _ hmem)) hbr.2⟩
  | letRec bindings body ihbs ihb =>
    intro t hno; simp only [Expr.NoRecAnn] at hno
    simp only [Expr.shiftFrom, Expr.NoRecAnn]
    refine ⟨?_, ihb (t + bindings.length) hno.2⟩
    obtain ⟨hbs, _⟩ := hno
    generalize t + bindings.length = thr
    induction bindings with
    | nil => exact trivial
    | cons hd tl ihtl =>
      exact ⟨ihbs hd List.mem_cons_self thr hbs.1,
             ihtl (fun e' hmem => ihbs e' (List.mem_cons_of_mem _ hmem)) hbs.2⟩
  | letRecAnn schemes bindings body _ _ => intro t hno; exact hno.elim

/-! Algorithm W as a type-directed **elaboration** relation: the subject `eIn` is
    the unelaborated skeleton and the new output index `eOut` carries the elaborated
    (type-passing) term. The runnable term is `eOut.substTyFvars S`. Mutually defined
    with `InferBranches`/`InferRecGroup` (the `match_`/`letRec` threaders), which
    each thread out their elaborated sub-terms. -/
mutual
inductive Infer : Nat → Ctx → Expr → Nat → Subst → Expr → Ty → Prop
  | primLitUnit {Φ ctx} :
    Infer Φ ctx (.primLit .unit) Φ [] (.primLit .unit) (.prim .unit)
  | primLitInt {Φ ctx n} :
    Infer Φ ctx (.primLit (.int n)) Φ [] (.primLit (.int n)) (.prim .int)
  | primLitNat {Φ ctx n} :
    Infer Φ ctx (.primLit (.nat n)) Φ [] (.primLit (.nat n)) (.prim .nat)
  -- | primLitBool {Φ ctx b} :
  --   Infer Φ ctx (.primLit (.bool b)) Φ [] (.primLit (.bool b)) (.prim .bool)
  | primLitStr {Φ ctx s} :
    Infer Φ ctx (.primLit (.str s)) Φ [] (.primLit (.str s)) (.prim .str)
  | lambda {Φ ctx ann paramTy body Φ₀ Φ' S bodyOut τb} :
    LamSeed Φ ann paramTy Φ₀ →
    Infer Φ₀ { ctx with env := PolyTy.mkTrivial paramTy :: ctx.env } body Φ' S bodyOut τb →
    Infer Φ ctx (.lambda ann body) Φ' S (.lambda ann bodyOut) (.arrow (S.onTy paramTy) τb)
  | app {Φ ctx f arg Φ₁ Φ₂ S₁ S₂ S₃ fOut argOut τf τa} :
    Infer Φ ctx f Φ₁ S₁ fOut τf →
    Infer Φ₁ (S₁.onCtx ctx) arg Φ₂ S₂ argOut τa →
    UnifyRel (S₂.onTy τf) (.arrow τa (.fvar Φ₂)) S₃ →
    Infer Φ ctx (.app f arg) (Φ₂ + 1) (S₁ ++ S₂ ++ S₃) (.app fOut argOut) (S₃.onTy (.fvar Φ₂))
  | var {Φ ctx i tyArgsIn polyTy} :
    ctx.env[i]? = some polyTy →
    -- Type-passing elaboration: the *input* var carries an ignored `tyArgsIn`
    -- (the skeleton). The *output* var's `tyArgs` are the scheme's fresh opening
    -- block `freshVars Φ paramCount` (as fvars); the final substitution `S` later
    -- resolves these to concrete types (`eOut.substTyFvars S`). `tyArgs.length =
    -- paramCount` holds by construction (`freshVars` has length `paramCount`).
    Infer Φ ctx (.var i tyArgsIn) (Φ + polyTy.paramCount) []
      (.var i ((freshVars Φ polyTy.paramCount).map Ty.fvar))
      (polyTy.openVars (freshVars Φ polyTy.paramCount))
  | ctor {Φ ctx name ctor} :
    LookupList.get? ctx.ctors name = some ctor →
    Infer Φ ctx (.ctor name) (Φ + ctor.paramCount) []
      (.ctor name)
      (ctor.toTy.openVars (freshVars Φ ctor.paramCount))
  | letIn {Φ ctx rhs body Φ₁ Φ₂ S₁ S₂ rhsOut bodyOut τ₁ τ₂} :
    Infer Φ ctx rhs Φ₁ S₁ rhsOut τ₁ →
    Infer Φ₁
      { (S₁.onCtx ctx) with
        env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env }
      body Φ₂ S₂ bodyOut τ₂ →
    -- Elaboration: generalise `rhs` to `genScheme …` and store the closed rhs (its
    -- generalised vars closed back to scheme `bvar`s) under that scheme.
    Infer Φ ctx (.letIn none rhs body) Φ₂ (S₁ ++ S₂)
      (.letIn (some (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁))
        ((rhsOut.substTyFvars S₁).closeTyVars (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)) bodyOut)
      τ₂
  /-- Annotated `let` (scoped type variables, D2 ordering). The bound scheme is
      the annotation `σ` (`σ.WF`; it MAY carry free type vars — outer scoped
      vars). Allocate `σ`'s skolems `Ys = freshVars Φ σ.paramCount` **first**, then
      infer the bound expression already opened at `Ys` (`rhs.openTyVars Ys`,
      matching the spec's `openBoundTyVars`), so its scoped vars resolve to the
      rigid `Ys`. Unify the rhs type `τ₁` against `σ.openVars Ys` (both may mention
      `Ys`), producing `Schk`. Escape conditions keep `Ys` rigid: none is bound by
      the **whole** rhs+unify substitution `S₁ ++ Schk` (so the signature is no
      more general than `rhs` actually is), and none leaks into the threaded body
      context (so `Ys` can be renamed to the cofinite fresh names). The output
      closes the elaborated rhs back over the skolems `Ys`. -/
  | letInAnn {Φ N ctx σ rhs body Φ₁ Φ₂ S₁ Schk S₂ rhsOut bodyOut τ₁ τ₂} :
    σ.WF →
    Φ ≤ N →
    Infer (N + σ.paramCount) ctx (rhs.openTyVars (freshVars N σ.paramCount)) Φ₁ S₁ rhsOut τ₁ →
    UnifyRel τ₁ (σ.openVars (freshVars N σ.paramCount)) Schk →
    (∀ y ∈ freshVars N σ.paramCount, y ∉ (S₁ ++ Schk).map Prod.fst) →
    (∀ y ∈ freshVars N σ.paramCount, y ∉ (Schk.onCtx (S₁.onCtx ctx)).env.freeVars) →
    Infer Φ₁
      { (Schk.onCtx (S₁.onCtx ctx)) with env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env }
      body Φ₂ S₂ bodyOut τ₂ →
    Infer Φ ctx (.letIn (some σ) rhs body) Φ₂ (S₁ ++ Schk ++ S₂)
      (.letIn (some σ) ((rhsOut.substTyFvars (S₁ ++ Schk)).closeTyVars (freshVars N σ.paramCount)) bodyOut)
      τ₂
  | match_ {Φ ctx scrut branches Φ₁ Φ₂ S₁ S₂ scrutOut branchesOut τs} :
    Infer Φ ctx scrut Φ₁ S₁ scrutOut τs →
    branches ≠ [] →
    InferBranches (Φ₁ + 1) (S₁.onCtx ctx) τs (.fvar Φ₁) branches Φ₂ S₂ branchesOut →
    Infer Φ ctx (.match_ scrut branches) Φ₂ (S₁ ++ S₂) (.match_ scrutOut branchesOut)
      (S₂.onTy (.fvar Φ₁))
  /-- (Mutually) recursive binding group, monomorphic recursion (Damas–Milner /
      Pottier `LetRec`; mirrors `TypeOfElabHM.letRec`). Allocate the group's fresh
      monotype vars `β = freshVars Φ n` (`n = bindings.length`), bind the group
      MONOMORPHICALLY at those `β`s in the RHS context, thread inference over the
      bindings unifying each RHS against its `βⱼ` (`InferRecGroup`), then generalize
      the SOLVED monotypes `S₁.onTy βⱼ` over the shared group pool and type the body
      under those per-binding schemes (`genGroupSchemes`). The output closes each
      elaborated binding over its own generalised variables (`Expr.closeRecGroup`). -/
  | letRec {Φ ctx bindings body Φ₁ Φ₂ S₁ S₂ bindingsOut bodyOut τ₂} :
    InferRecGroup (Φ + bindings.length)
        { ctx with env := (freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i)) ++ ctx.env }
        bindings
        ((freshVars Φ bindings.length).map Ty.fvar)
        Φ₁ S₁ bindingsOut →
    Infer Φ₁
      { (S₁.onCtx ctx) with
        env := genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
                 ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)))
               ++ (S₁.onCtx ctx).env }
      body Φ₂ S₂ bodyOut τ₂ →
    Infer Φ ctx (.letRec bindings body) Φ₂ (S₁ ++ S₂)
      (Expr.letRecElab
        (genGroupVars (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
          ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i))))
        (bindingsOut.map (·.substTyFvars S₁))
        ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)))
        bodyOut)
      τ₂
  /-- (Mutually) recursive binding group with **annotated polymorphic recursion**
      (Pottier's `LetRecPoly`; mirrors `TypeOfElabHM.letRecAnn`). The schemes are
      GIVEN (annotations), so this is **check-and-elaborate**, not infer-the-recursion —
      hence decidable (Henglein; unannotated poly-recursion is undecidable). The whole
      group is in scope at its FULL declared `schemes` in BOTH the RHSs and the body, so
      recursive occurrences may instantiate `σⱼ` at different types (the polymorphism).
      Each binding is checked scheme-relatively by `InferRecGroupAnn` — the per-binding
      `letInAnn` mechanics: skolemise `σⱼ`, infer the binding opened at the skolems
      against `σⱼ`'s opening, escape-check the skolems rigid, emit it closed back over
      them (after the binding/check MGU). The output is a genuine `letRecAnn` node (the
      only Core form that types polymorphic recursion); its bindings are already closed
      back by `InferRecGroupAnn`. -/
  | letRecAnn {Φ ctx schemes bindings body Φ₁ Φ₂ S₁ S₂ bindingsOut bodyOut τ₂} :
    (∀ σ ∈ schemes, σ.WF) →
    bindings.length = schemes.length →
    InferRecGroupAnn Φ { ctx with env := schemes ++ ctx.env } bindings schemes Φ₁ S₁ bindingsOut →
    Infer Φ₁ (S₁.onCtx { ctx with env := schemes ++ ctx.env }) body Φ₂ S₂ bodyOut τ₂ →
    Infer Φ ctx (.letRecAnn schemes bindings body) Φ₂ (S₁ ++ S₂)
      (.letRecAnn schemes bindingsOut bodyOut) τ₂

/-- Threads inference through a `match_`'s branch list. Carries the scrutinee type
    `scrutTy` (which each *named* pattern constrains to its ADT by unifying it with a
    fresh `customTy` instance) and a running result type `ρ` that each branch body's
    type is unified against, with substitutions propagated to the next branch. An
    all-wildcard list leaves `scrutTy` free. The output index threads the elaborated
    branch list. -/
inductive InferBranches :
    Nat → Ctx → Ty → Ty → List (MatchPattern × Expr) → Nat → Subst → List (MatchPattern × Expr) → Prop
  | nil {Φ ctx scrutTy ρ} :
    InferBranches Φ ctx scrutTy ρ [] Φ [] []
  | cons {Φ ctx scrutTy ρ c n body rest ctor Φ₁ Φ₂ S₀ S₁ S₂ S₃ bodyOut restOut τb} :
    LookupList.get? ctx.ctors c = some ctor →
    n = ctor.contents.length →
    UnifyRel scrutTy
      (.customTy ctor.tyName ((freshVars Φ ctor.paramCount).map (Ty.fvar ·))) S₀ →
    Infer (Φ + ctor.paramCount)
      { (S₀.onCtx ctx) with
        env := (ctor.contents.map (Ty.openWith
            (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
          ++ (S₀.onCtx ctx).env }
      body Φ₁ S₁ bodyOut τb →
    UnifyRel τb (S₁.onTy (S₀.onTy ρ)) S₂ →
    InferBranches Φ₁ (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx)))
      (S₂.onTy (S₁.onTy (S₀.onTy scrutTy))) (S₂.onTy (S₁.onTy (S₀.onTy ρ))) rest Φ₂ S₃ restOut →
    InferBranches Φ ctx scrutTy ρ ((.named c n, body) :: rest) Φ₂ (S₀ ++ S₁ ++ S₂ ++ S₃)
      ((.named c n, bodyOut) :: restOut)
  /-- A wildcard branch: infer `body` in the *unextended* context (it binds
      nothing) and imposes no constraint on `scrutTy`; unify its type against the
      running result type `ρ`, continue. -/
  | consWild {Φ ctx scrutTy ρ body rest Φ₁ Φ₂ S₁ S₂ S₃ bodyOut restOut τb} :
    Infer Φ ctx body Φ₁ S₁ bodyOut τb →
    UnifyRel τb (S₁.onTy ρ) S₂ →
    InferBranches Φ₁ (S₂.onCtx (S₁.onCtx ctx))
      (S₂.onTy (S₁.onTy scrutTy)) (S₂.onTy (S₁.onTy ρ)) rest Φ₂ S₃ restOut →
    InferBranches Φ ctx scrutTy ρ ((.wildcard, body) :: rest) Φ₂ (S₁ ++ S₂ ++ S₃)
      ((.wildcard, bodyOut) :: restOut)

/-- Threads inference through a `letRec`'s recursive group. Carries the per-binding
    target monotypes `targets` (the group's fresh shared vars `βⱼ`, as `Ty`s): each
    binding's inferred type is unified against its (substituted) target, and the
    remaining targets are pushed through the running substitution — exactly like
    `InferBranches` threads the running result type. The output index threads the
    RAW elaborated bindings (closing over the per-binding gen-vars happens in the
    enclosing `letRec` rule). -/
inductive InferRecGroup : Nat → Ctx → List Expr → List Ty → Nat → Subst → List Expr → Prop
  | nil {Φ ctx} : InferRecGroup Φ ctx [] [] Φ [] []
  | cons {Φ ctx e rest β βs Φ₁ Φ₂ S₁ S₂ S₃ eOut restOut τ} :
    Infer Φ ctx e Φ₁ S₁ eOut τ →
    UnifyRel τ (S₁.onTy β) S₂ →
    InferRecGroup Φ₁ (S₂.onCtx (S₁.onCtx ctx)) rest (βs.map (fun b => S₂.onTy (S₁.onTy b))) Φ₂ S₃ restOut →
    InferRecGroup Φ ctx (e :: rest) (β :: βs) Φ₂ (S₁ ++ S₂ ++ S₃) (eOut :: restOut)

/-- Threads check-and-elaborate through an **annotated** recursive group (the analogue
    of `InferRecGroup` for `letRecAnn`). The whole group is already in scope at its full
    `schemes` in `ctx` (the enclosing `Infer.letRecAnn` rule adds them), so — unlike the
    monomorphic `InferRecGroup`, whose targets are mutable fresh `βⱼ` — the `schemes`
    targets are RIGID user annotations that thread UNCHANGED (only the *context* is
    pushed through the running substitution). Each binding `e` is handled exactly like
    `letInAnn`: skolemise its scheme `σ` (`Ys = freshVars N σ.paramCount`), infer
    `e.openTyVars Ys` against `σ.openVars Ys`, escape-check `Ys` rigid (none bound by
    `S₁ ++ Schk`, none leaking into the threaded context), and emit the elaborated
    binding **closed back over `Ys` after applying `S₁ ++ Schk`** (close-after-MGU). The
    output threads the already-closed bindings. -/
inductive InferRecGroupAnn : Nat → Ctx → List Expr → List PolyTy → Nat → Subst → List Expr → Prop
  | nil {Φ ctx} : InferRecGroupAnn Φ ctx [] [] Φ [] []
  | cons {Φ N ctx σ schemes e rest Φ₁ Φ₂ S₁ Schk S₂ eOut restOut τ} :
    Φ ≤ N →
    Infer (N + σ.paramCount) ctx (e.openTyVars (freshVars N σ.paramCount)) Φ₁ S₁ eOut τ →
    UnifyRel τ (σ.openVars (freshVars N σ.paramCount)) Schk →
    (∀ y ∈ freshVars N σ.paramCount, y ∉ (S₁ ++ Schk).map Prod.fst) →
    (∀ y ∈ freshVars N σ.paramCount, y ∉ (Schk.onCtx (S₁.onCtx ctx)).env.freeVars) →
    InferRecGroupAnn Φ₁ (Schk.onCtx (S₁.onCtx ctx)) rest schemes Φ₂ S₂ restOut →
    InferRecGroupAnn Φ ctx (e :: rest) (σ :: schemes) Φ₂ (S₁ ++ Schk ++ S₂)
      (((eOut.substTyFvars (S₁ ++ Schk)).closeTyVars (freshVars N σ.paramCount)) :: restOut)
end


/-! ### The elaborated output is `letRecAnn`-free

`Infer` never produces a `letRecAnn` node (there is no `letRecAnn` rule in Stage 1),
so every elaborated output `eOut` satisfies `NoRecAnn`. This is the hypothesis the
close-back kernel (`Expr.openTyVars_closeTyVars_rename`) needs for the annotated
binder soundness cases. -/

/-- `closeTyVarsAux` preserves `NoRecAnn`: it rebuilds every node shape unchanged
    (`letRecAnn` maps to `letRecAnn`, excluded by the hypothesis). -/
theorem Expr.NoRecAnn.closeTyVarsAux {Xs : List Nat} :
    ∀ (e : Expr) (d : Nat), e.NoRecAnn → (e.closeTyVarsAux d Xs).NoRecAnn := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p => intro d _; exact trivial
  | ctor nm => intro d _; exact trivial
  | var n tyArgs => intro d _; exact trivial
  | lambda ann body ih =>
    intro d hno
    simp only [Expr.NoRecAnn] at hno
    simp only [Expr.closeTyVarsAux, Expr.NoRecAnn]
    exact ih d hno
  | app f arg ihf iharg =>
    intro d hno
    simp only [Expr.NoRecAnn] at hno
    simp only [Expr.closeTyVarsAux, Expr.NoRecAnn]
    exact ⟨ihf d hno.1, iharg d hno.2⟩
  | letIn ann rhs body ihrhs ihbody =>
    intro d hno
    cases ann with
    | none =>
      simp only [Expr.NoRecAnn] at hno
      simp only [Expr.closeTyVarsAux, Expr.NoRecAnn]
      exact ⟨ihrhs d hno.1, ihbody d hno.2⟩
    | some σ =>
      obtain ⟨pc, sbody⟩ := σ
      simp only [Expr.NoRecAnn] at hno
      simp only [Expr.closeTyVarsAux, Expr.NoRecAnn]
      exact ⟨ihrhs (d + pc) hno.1, ihbody d hno.2⟩
  | match_ scrut branches ihscrut ihbranches =>
    intro d hno
    simp only [Expr.NoRecAnn] at hno
    obtain ⟨hs, hbr⟩ := hno
    rw [Expr.NoRecAnn.BranchList_iff] at hbr
    simp only [Expr.closeTyVarsAux, Expr.NoRecAnn, BranchList.closeTyVarsAux_eq_map]
    refine ⟨ihscrut d hs, ?_⟩
    rw [Expr.NoRecAnn.BranchList_iff]
    intro p b hpb
    obtain ⟨⟨p', b'⟩, hmem, heq⟩ := List.mem_map.mp hpb
    simp only [Prod.mk.injEq] at heq
    obtain ⟨_, rfl⟩ := heq
    exact ihbranches p' b' hmem d (hbr p' b' hmem)
  | letRec bindings body ih_bindings ih_body =>
    intro d hno
    simp only [Expr.NoRecAnn] at hno
    obtain ⟨hbs, hb⟩ := hno
    rw [Expr.NoRecAnn.RecGroup_iff] at hbs
    simp only [Expr.closeTyVarsAux, Expr.NoRecAnn, RecGroup.closeTyVarsAux_eq_map]
    refine ⟨?_, ih_body d hb⟩
    rw [Expr.NoRecAnn.RecGroup_iff]
    intro e he
    obtain ⟨e', hmem, rfl⟩ := List.mem_map.mp he
    exact ih_bindings e' hmem d (hbs e' hmem)
  | letRecAnn schemes bindings body _ _ => intro d hno; exact (hno : (False : Prop)).elim

/-- `closeTyVars` preserves `NoRecAnn` (top-level `d = 0`). -/
theorem Expr.NoRecAnn.closeTyVars {Xs : List Nat} {e : Expr} (he : e.NoRecAnn) :
    (e.closeTyVars Xs).NoRecAnn := Expr.NoRecAnn.closeTyVarsAux e 0 he

/-- The Λ-outside nest is `letRecAnn`-free: every wrapper is a `letIn` around a
    `closeTyVars`-of-`letRec` (both `NoRecAnn`-allowed) over shifted `NoRecAnn`
    bindings, bottoming at a `NoRecAnn` body. -/
theorem Expr.letRecElabNest_noRecAnn {G : List Nat} {n : Nat} {rawBindings : List Expr}
    (hraw : ∀ e ∈ rawBindings, e.NoRecAnn) :
    ∀ (members : List (Nat × Ty)) {body : Expr}, body.NoRecAnn →
      (Expr.letRecElabNest G n rawBindings members body).NoRecAnn := by
  intro members
  induction members with
  | nil => intro body hbody; exact hbody
  | cons hd tl ih =>
    obtain ⟨i, τ⟩ := hd
    intro body hbody
    refine ⟨Expr.NoRecAnn.closeTyVars ⟨?_, trivial⟩, ih hbody⟩
    rw [Expr.NoRecAnn.RecGroup_iff]
    intro e he
    obtain ⟨e', he', rfl⟩ := List.mem_map.mp he
    exact (hraw e' he').shiftFrom (n - 1 - i) n

theorem Expr.letRecElab_noRecAnn {G : List Nat} {rawBindings : List Expr} {τs : List Ty}
    {body : Expr} (hraw : ∀ e ∈ rawBindings, e.NoRecAnn) (hbody : body.NoRecAnn) :
    (Expr.letRecElab G rawBindings τs body).NoRecAnn :=
  Expr.letRecElabNest_noRecAnn hraw _ hbody

/-! ### `substTyFvars` preserves `NoRecAnn` / `TyBvarBounded` (close-after-`S₁` fix)

A type-substitution only rewrites type annotations, never the term's node shapes,
so it preserves `NoRecAnn`; and with LC images it preserves the type-`bvar` bound
(`ContainsBvarsUpTo.substFvar`). Needed because the corrected `letIn`/`letRec`
`eOut` closes `rhsOut.substTyFvars S₁`. -/

theorem Expr.substTyFvar_noRecAnn (Z : Nat) (U : Ty) {e : Expr} :
    e.NoRecAnn → (e.substTyFvar Z U).NoRecAnn := by
  induction e using Expr.rec_strong with
  | primLit p => intro _; exact trivial
  | ctor c => intro _; exact trivial
  | var i tyArgs => intro _; exact trivial
  | lambda ann body ih =>
    intro hno; simp only [Expr.NoRecAnn] at hno
    simp only [Expr.substTyFvar, Expr.NoRecAnn]; exact ih hno
  | app f arg ihf iharg =>
    intro hno; simp only [Expr.NoRecAnn] at hno
    simp only [Expr.substTyFvar, Expr.NoRecAnn]; exact ⟨ihf hno.1, iharg hno.2⟩
  | letIn ann rhs body ihr ihb =>
    intro hno; simp only [Expr.NoRecAnn] at hno
    simp only [Expr.substTyFvar, Expr.NoRecAnn]; exact ⟨ihr hno.1, ihb hno.2⟩
  | match_ scrut branches ihs ihbr =>
    intro hno; simp only [Expr.NoRecAnn] at hno
    simp only [Expr.substTyFvar, Expr.NoRecAnn]
    refine ⟨ihs hno.1, ?_⟩
    obtain ⟨_, hbr⟩ := hno
    induction branches with
    | nil => exact trivial
    | cons hd tl ihtl =>
      obtain ⟨p, b⟩ := hd
      exact ⟨ihbr p b List.mem_cons_self hbr.1,
             ihtl (fun p' b' hmem => ihbr p' b' (List.mem_cons_of_mem _ hmem)) hbr.2⟩
  | letRec bindings body ihbs ihb =>
    intro hno; simp only [Expr.NoRecAnn] at hno
    simp only [Expr.substTyFvar, Expr.NoRecAnn]
    refine ⟨?_, ihb hno.2⟩
    obtain ⟨hbs, _⟩ := hno
    induction bindings with
    | nil => exact trivial
    | cons hd tl ihtl =>
      exact ⟨ihbs hd List.mem_cons_self hbs.1,
             ihtl (fun e' hmem => ihbs e' (List.mem_cons_of_mem _ hmem)) hbs.2⟩
  | letRecAnn schemes bindings body _ _ => intro hno; exact hno.elim

theorem Expr.substTyFvars_noRecAnn {S : List (Nat × Ty)} {e : Expr} (hno : e.NoRecAnn) :
    (e.substTyFvars S).NoRecAnn := by
  induction S generalizing e with
  | nil => exact hno
  | cons hd S' ih => obtain ⟨Z, U⟩ := hd; exact ih (Expr.substTyFvar_noRecAnn Z U hno)

theorem Expr.substTyFvar_tyBvarBounded (Z : Nat) {U : Ty} (hU : U.IsLC) {e : Expr} :
    ∀ d, e.NoRecAnn → e.TyBvarBounded d → (e.substTyFvar Z U).TyBvarBounded d := by
  induction e using Expr.rec_strong with
  | primLit p => intro d _ _; exact trivial
  | ctor c => intro d _ _; exact trivial
  | var i tyArgs =>
    intro d _ hb; simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.substTyFvar, Expr.TyBvarBounded]
    intro t ht; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
    exact ContainsBvarsUpTo.substFvar hU (hb t0 ht0)
  | lambda ann body ih =>
    intro d hno hb
    simp only [Expr.NoRecAnn] at hno; simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.substTyFvar, Expr.TyBvarBounded]
    refine ⟨?_, ih d hno hb.2⟩
    intro t ht; obtain ⟨t', ht', rfl⟩ := Option.map_eq_some_iff.mp ht
    exact ContainsBvarsUpTo.substFvar hU (hb.1 t' ht')
  | app f arg ihf iharg =>
    intro d hno hb
    simp only [Expr.NoRecAnn] at hno; simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.substTyFvar, Expr.TyBvarBounded]
    exact ⟨ihf d hno.1 hb.1, iharg d hno.2 hb.2⟩
  | letIn ann rhs body ihr ihb =>
    intro d hno hb
    cases ann with
    | none =>
      simp only [Expr.NoRecAnn] at hno; simp only [Expr.TyBvarBounded] at hb
      simp only [Expr.substTyFvar]
      exact ⟨ihr d hno.1 hb.1, ihb d hno.2 hb.2⟩
    | some σ =>
      obtain ⟨pc, sb⟩ := σ
      simp only [Expr.NoRecAnn] at hno; simp only [Expr.TyBvarBounded] at hb
      simp only [Expr.substTyFvar]
      exact ⟨ContainsBvarsUpTo.substFvar hU hb.1, ihr (d + pc) hno.1 hb.2.1, ihb d hno.2 hb.2.2⟩
  | match_ scrut branches ihs ihbr =>
    intro d hno hb
    simp only [Expr.NoRecAnn] at hno; simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.substTyFvar, Expr.TyBvarBounded]
    refine ⟨ihs d hno.1 hb.1, ?_⟩
    obtain ⟨_, hnobr⟩ := hno
    obtain ⟨_, hbbr⟩ := hb
    induction branches with
    | nil => exact trivial
    | cons hd tl ihtl =>
      obtain ⟨p, b⟩ := hd
      exact ⟨ihbr p b List.mem_cons_self d hnobr.1 hbbr.1,
             ihtl (fun p' b' hmem => ihbr p' b' (List.mem_cons_of_mem _ hmem)) hnobr.2 hbbr.2⟩
  | letRec bindings body ihbs ihb =>
    intro d hno hb
    simp only [Expr.NoRecAnn] at hno; simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.substTyFvar, Expr.TyBvarBounded]
    refine ⟨?_, ihb d hno.2 hb.2⟩
    obtain ⟨hnobs, _⟩ := hno
    obtain ⟨hbbs, _⟩ := hb
    induction bindings with
    | nil => exact trivial
    | cons hd tl ihtl =>
      exact ⟨ihbs hd List.mem_cons_self d hnobs.1 hbbs.1,
             ihtl (fun e' hmem => ihbs e' (List.mem_cons_of_mem _ hmem)) hnobs.2 hbbs.2⟩
  | letRecAnn schemes bindings body _ _ => intro d hno _; exact hno.elim

/-- A scheme's body is preserved by `PolyTy.substFvars` (only the body is rewritten). -/
theorem PolyTy.body_substFvars {S : List (Nat × Ty)} {σ : PolyTy} :
    (PolyTy.substFvars S σ).body = Ty.substFvars S σ.body := by
  induction S generalizing σ with
  | nil => rfl
  | cons hd tl ih => obtain ⟨Z, U⟩ := hd; rw [PolyTy.substFvars, ih, PolyTy.substFvar, Ty.substFvars]

/-- `PolyTy.substFvars` keeps the scheme's parameter count. -/
theorem PolyTy.paramCount_substFvars {S : List (Nat × Ty)} {σ : PolyTy} :
    (PolyTy.substFvars S σ).paramCount = σ.paramCount := by
  induction S generalizing σ with
  | nil => rfl
  | cons hd tl ih => obtain ⟨Z, U⟩ := hd; rw [PolyTy.substFvars, ih, PolyTy.substFvar]

/-- A whole substitution with LC images preserves the type-bvar bound. -/
theorem ContainsBvarsUpTo.substFvars {S : List (Nat × Ty)} (hS : ∀ p ∈ S, p.2.IsLC)
    {n : Nat} {t : Ty} (ht : ContainsBvarsUpTo n t) :
    ContainsBvarsUpTo n (Ty.substFvars S t) := by
  induction S generalizing t with
  | nil => exact ht
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    exact ih (fun p hp => hS p (List.mem_cons_of_mem _ hp))
      (ContainsBvarsUpTo.substFvar (hS (Z, U) List.mem_cons_self) ht)

/-- The annotated-group `substTyFvars` preserves the shielded depth bound per binding
    (the schemes thread unchanged, only the binding terms are mapped). -/
private theorem RecGroupAnn.substTyFvars_tyBvarBounded {S : List (Nat × Ty)} :
    ∀ (schemes : List PolyTy) (bindings : List Expr) (d : Nat),
      (∀ e ∈ bindings, ∀ {d'}, e.TyBvarBounded d' → (e.substTyFvars S).TyBvarBounded d') →
      Expr.TyBvarBounded.RecGroupAnn d schemes bindings →
      Expr.TyBvarBounded.RecGroupAnn d (schemes.map (PolyTy.substFvars S))
        (bindings.map (·.substTyFvars S)) := by
  intro schemes bindings
  induction bindings generalizing schemes with
  | nil => intro d _ _; exact trivial
  | cons hd tl ih =>
    intro d ihB hbb
    cases schemes with
    | nil =>
      obtain ⟨hhd, hrest⟩ := hbb
      exact ⟨ihB hd List.mem_cons_self hhd,
        ih [] d (fun e he => ihB e (List.mem_cons_of_mem _ he)) hrest⟩
    | cons σ ss =>
      obtain ⟨hhd, hrest⟩ := hbb
      refine ⟨?_, ih ss d (fun e he => ihB e (List.mem_cons_of_mem _ he)) hrest⟩
      rw [PolyTy.paramCount_substFvars]
      exact ihB hd List.mem_cons_self hhd

/-- A type-substitution with LC images preserves `TyBvarBounded`. (Re-based off
    `NoRecAnn`: `substTyFvars` only rewrites type annotations by LC types — which carry
    no dangling bvars — even through `letRecAnn` schemes (`PolyTy.substFvars`, keeping
    `paramCount`) and bindings (`RecGroupAnn`), so the bound is preserved.) -/
theorem Expr.substTyFvars_tyBvarBounded {S : List (Nat × Ty)} (hS : ∀ p ∈ S, p.2.IsLC) :
    ∀ {e : Expr} {d : Nat}, e.TyBvarBounded d → (e.substTyFvars S).TyBvarBounded d := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p =>
    intro d _
    rw [Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (by simp [Expr.tyFreeVars])]; trivial
  | ctor c =>
    intro d _
    rw [Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (by simp [Expr.tyFreeVars])]; trivial
  | var i tyArgs =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    rw [Expr.substTyFvars_var]
    simp only [Expr.TyBvarBounded]
    intro t ht
    obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
    exact ContainsBvarsUpTo.substFvars hS (hb t0 ht0)
  | lambda ann body ih =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    rw [Expr.substTyFvars_lambda]
    refine ⟨?_, ih hb.2⟩
    intro t ht
    obtain ⟨t', ht', rfl⟩ := Option.map_eq_some_iff.mp ht
    exact ContainsBvarsUpTo.substFvars hS (hb.1 t' ht')
  | app f arg ihf iharg =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    rw [Expr.substTyFvars_app]
    exact ⟨ihf hb.1, iharg hb.2⟩
  | letIn ann rhs body ihr ihb =>
    intro d hb
    rw [Expr.substTyFvars_letIn]
    cases ann with
    | none =>
      simp only [Expr.TyBvarBounded, Option.map_none] at hb ⊢
      exact ⟨ihr hb.1, ihb hb.2⟩
    | some σ =>
      simp only [Expr.TyBvarBounded, Option.map_some] at hb ⊢
      refine ⟨?_, ihr hb.2.1, ihb hb.2.2⟩
      exact ContainsBvarsUpTo.substFvars hS hb.1
  | match_ scrut branches ihs ihbr =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    rw [Expr.substTyFvars_match]
    refine ⟨ihs hb.1, ?_⟩
    rw [Expr.TyBvarBounded.BranchList_iff] at hb ⊢
    intro p b hpb
    obtain ⟨⟨p', b'⟩, hmem, heq⟩ := List.mem_map.mp hpb
    simp only [Prod.mk.injEq] at heq
    obtain ⟨_, rfl⟩ := heq
    exact ihbr p' b' hmem (hb.2 p' b' hmem)
  | letRec bindings body ihbs ihb =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    rw [Expr.substTyFvars_letRec]
    refine ⟨?_, ihb hb.2⟩
    rw [Expr.TyBvarBounded.RecGroup_iff] at hb ⊢
    intro e he
    obtain ⟨e', hmem, rfl⟩ := List.mem_map.mp he
    exact ihbs e' hmem (hb.1 e' hmem)
  | letRecAnn schemes bindings body ihbs ihb =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    rw [Expr.substTyFvars_letRecAnn]
    obtain ⟨hsch, hrga, hbody⟩ := hb
    refine ⟨?_, RecGroupAnn.substTyFvars_tyBvarBounded schemes bindings d ihbs hrga, ihb hbody⟩
    intro σ' hσ'
    obtain ⟨σ0, hσ0, rfl⟩ := List.mem_map.mp hσ'
    rw [PolyTy.body_substFvars, PolyTy.paramCount_substFvars]
    exact ContainsBvarsUpTo.substFvars hS (hsch σ0 hσ0)

/-! ### Invariant layer for `Infer` soundness -/

/-! The fresh-variable frontier only ever grows (`Infer.frontier_le`). -/
mutual
theorem Infer.frontier_le {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ) : Φ ≤ Φ' := by
  cases h with
  | primLitUnit => omega
  | primLitInt => omega
  | primLitNat => omega
  -- | primLitBool => omega
  | primLitStr => omega
  | lambda hseed hbody => have := Infer.frontier_le hbody; have := hseed.le; omega
  | app hf harg _ => have := Infer.frontier_le hf; have := Infer.frontier_le harg; omega
  | var => omega
  | ctor => omega
  | letIn hrhs hbody => have := Infer.frontier_le hrhs; have := Infer.frontier_le hbody; omega
  | letInAnn _ _hΦN hrhs _ _ _ hbody =>
    have := Infer.frontier_le hrhs; have := Infer.frontier_le hbody; omega
  | match_ hscrut _ hbr =>
    have := Infer.frontier_le hscrut; have := InferBranches.frontier_le hbr; omega
  | letRec hgroup hbody =>
    have := InferRecGroup.frontier_le hgroup; have := Infer.frontier_le hbody; omega
  | letRecAnn _ _ hgroup hbody =>
    have := InferRecGroupAnn.frontier_le hgroup; have := Infer.frontier_le hbody; omega
termination_by e.size
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.size, Expr.size_openTyVars]; omega)
theorem InferBranches.frontier_le {Φ ctx scrutTy ρ brs Φ' S brsOut}
    (h : InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut) :
    Φ ≤ Φ' := by
  cases h with
  | nil => omega
  | cons _ _ _ hbody _ hrest =>
    have := Infer.frontier_le hbody; have := InferBranches.frontier_le hrest; omega
  | consWild hbody _ hrest =>
    have := Infer.frontier_le hbody; have := InferBranches.frontier_le hrest; omega
termination_by Expr.sizeBranches brs
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)
theorem InferRecGroup.frontier_le {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut) : Φ ≤ Φ' := by
  cases h with
  | nil => omega
  | cons he _ hrest =>
    have := Infer.frontier_le he; have := InferRecGroup.frontier_le hrest; omega
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)
theorem InferRecGroupAnn.frontier_le {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut) : Φ ≤ Φ' := by
  cases h with
  | nil => omega
  | cons hΦN hinfer _ _ _ hrest =>
    have := Infer.frontier_le hinfer; have := InferRecGroupAnn.frontier_le hrest; omega
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end

/-! ### Scoped-variable regime (replaces the old closed-annotation invariants)

The closed-regime invariants `Infer.tyFreeVars_eq_nil` and the `sorry`-gated
`Infer.openTyVarsAux_eq_self` are gone. With D2 the bound expression is inferred
*already opened* at the rigid skolems, so an accepted term's annotation free vars
are no longer empty — they are the in-scope skolems. Soundness now threads an
ambient rigid-skolem set `K` (the substitution provably avoids `K`, via the escape
discipline), and the cofinite premise is recovered by renaming `K` to fresh names
through `Expr.substTyFvars_zip_openTyVars` (the Core bridge). -/

/-- A context is well-formed when every scheme in its env is well-formed. -/
def CtxWF (ctx : Ctx) : Prop := ∀ M ∈ ctx.env, M.WF

theorem freshVars_nodup {Φ k : Nat} : (freshVars Φ k).Nodup :=
  (List.nodup_range).map (fun _ _ h => by omega)

@[simp] theorem freshVars_length (Φ k : Nat) : (freshVars Φ k).length = k := by
  simp [freshVars]

/-- Every freshly-allocated name is at least the frontier `Φ`. Lets us conclude a
    skolem block `freshVars Φ k` is disjoint from any set of names below `Φ`
    (e.g. the ambient skolems `K`, all introduced at earlier frontiers). -/
theorem freshVars_ge {Φ k : Nat} : ∀ y ∈ freshVars Φ k, Φ ≤ y := by
  intro y hy
  simp only [freshVars, List.mem_map, List.mem_range] at hy
  obtain ⟨i, _, rfl⟩ := hy
  omega

/-- Every freshly-allocated name is below the post-allocation frontier `Φ + k`. -/
theorem freshVars_lt {Φ k : Nat} : ∀ y ∈ freshVars Φ k, y < Φ + k := by
  intro y hy
  simp only [freshVars, List.mem_map, List.mem_range] at hy
  obtain ⟨i, hi, rfl⟩ := hy
  omega

/-- A whole substitution (LC replacements) preserves any bvar bound. -/
theorem Subst.onTy_containsBvars {S : Subst} (h_lc : ∀ p ∈ S, p.2.IsLC) :
    ∀ {n : Nat} {τ : Ty}, ContainsBvarsUpTo n τ → ContainsBvarsUpTo n (S.onTy τ) := by
  induction S with
  | nil => intro n τ hτ; simpa using hτ
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    have hU : U.IsLC := h_lc (Z, U) (List.mem_cons_self ..)
    have hS' : ∀ p ∈ S', p.2.IsLC := fun p hp => h_lc p (List.mem_cons_of_mem _ hp)
    intro n τ hτ
    rw [show ((Z, U) :: S') = [(Z, U)] ++ S' from rfl, Subst.onTy_append]
    exact ih hS' (ContainsBvarsUpTo.substFvar hU hτ)

/-- A whole substitution preserves scheme well-formedness. -/
theorem Subst.onPolyTy_wf {S : Subst} (h_lc : ∀ p ∈ S, p.2.IsLC) {M : PolyTy}
    (hM : M.WF) : (S.onPolyTy M).WF :=
  Subst.onTy_containsBvars h_lc hM

/-- A whole substitution preserves context well-formedness. -/
theorem Subst.onCtx_wf {S : Subst} (h_lc : ∀ p ∈ S, p.2.IsLC) {ctx : Ctx}
    (h : CtxWF ctx) : CtxWF (S.onCtx ctx) := by
  intro M hM
  simp only [Subst.onCtx, Subst.onEnv] at hM
  obtain ⟨M0, hM0, rfl⟩ := List.mem_map.mp hM
  exact Subst.onPolyTy_wf h_lc (h M0 hM0)

/-- Instantiating all bvars below `n` with LC types yields an LC type. -/
theorem Ty.instantiate_isLC {σ : Nat → Ty} {n : Nat}
    (hσ : ∀ i, i < n → (σ i).IsLC) {ty : Ty} (hty : ContainsBvarsUpTo n ty) :
    (ty.instantiate σ).IsLC := by
  induction ty using Ty.rec_strong with
  | prim p => exact .prim
  | bvar i => cases hty with | bvar hlt => exact hσ i hlt
  | fvar m => exact .fvar
  | arrow a b iha ihb => cases hty with | arrow ha hb => exact .arrow (iha ha) (ihb hb)
  | customTy nm tys ih =>
    cases hty with
    | customTy hall =>
      simp only [Ty.instantiate, TyList.instantiate_eq_map]
      apply ContainsBvarsUpTo.customTy
      intro t ht
      obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
      exact ih t0 ht0 (hall t0 ht0)

/-- Opening a type whose bvars are `< Xs.length` with fresh names is LC. -/
theorem Ty.openVars_isLC {Xs : List Nat} {n : Nat} {ty : Ty}
    (hty : ContainsBvarsUpTo n ty) (hn : n ≤ Xs.length) :
    (Ty.openVars Xs ty).IsLC := by
  simp only [Ty.openVars]
  refine Ty.instantiate_isLC (fun i hi => ?_) hty
  rw [List.getElem?_eq_getElem (show i < Xs.length by omega)]
  exact .fvar

/-- Opening a well-formed scheme with enough fresh names is LC. -/
theorem PolyTy.openVars_isLC {Xs : List Nat} {M : PolyTy}
    (hM : M.WF) (hn : M.paramCount ≤ Xs.length) : (M.openVars Xs).IsLC :=
  Ty.openVars_isLC hM hn

/-- Opening a type whose bvars are `< n ≤ |Vs|` with LC args is LC. -/
theorem Ty.openWith_isLC {Vs : List Ty} {n : Nat} {X : Ty}
    (hVs : ∀ v ∈ Vs, v.IsLC) (hX : ContainsBvarsUpTo n X) (hn : n ≤ Vs.length) :
    (Ty.openWith Vs X).IsLC := by
  simp only [Ty.openWith]
  refine Ty.instantiate_isLC (fun i hi => ?_) hX
  simp only [List.getElem?_eq_getElem (show i < Vs.length by omega), Option.getD_some]
  exact hVs _ (List.getElem_mem _)

/-- A `match_` branch's pattern bindings (ctor contents opened with the type
    args) extend a WF context to a WF context, given LC type args of the right
    arity. -/
theorem branchBindings_wf {ctorr : Ctor} {ta : List Ty} {ctx : Ctx}
    (hctx : CtxWF ctx) (hta : ∀ t ∈ ta, t.IsLC) (hpc : ctorr.paramCount = ta.length) :
    CtxWF { ctx with
      env := (ctorr.contents.map (Ty.openWith ta)).map PolyTy.mkTrivial ++ ctx.env } := by
  intro M hM
  rw [List.mem_append] at hM
  rcases hM with hM | hM
  · obtain ⟨t, ht, rfl⟩ := List.mem_map.mp hM
    obtain ⟨c, hc, rfl⟩ := List.mem_map.mp ht
    show (Ty.openWith ta c).IsLC
    exact Ty.openWith_isLC hta (hpc ▸ ctorr.bound c hc) (le_of_eq hpc)
  · exact hctx M hM

/-! Local-closedness invariant (`Infer.lc`): from a well-formed context, `Infer`
    yields an LC type and a substitution whose replacements are all LC. (Context
    well-formedness of `S.onCtx ctx` follows separately via `Subst.onCtx_wf`.) -/
mutual
theorem Infer.lc {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ) :
    CtxWF ctx → τ.IsLC ∧ (∀ p ∈ S, p.2.IsLC) := by
  cases h with
  | primLitUnit => intro _; exact ⟨.prim, by simp⟩
  | primLitInt => intro _; exact ⟨.prim, by simp⟩
  | primLitNat => intro _; exact ⟨.prim, by simp⟩
  -- | primLitBool => intro _; exact ⟨.prim, by simp⟩
  | primLitStr => intro _; exact ⟨.prim, by simp⟩
  | lambda hseed hbody =>
    intro hctx
    cases hseed with
    | none =>
      obtain ⟨hb_lc, hb_s⟩ := Infer.lc hbody (by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact ContainsBvarsUpTo.fvar
        · exact hctx M hM)
      exact ⟨.arrow (Subst.onTy_lc hb_s ContainsBvarsUpTo.fvar) hb_lc, hb_s⟩
    | some _ hpc =>
      obtain ⟨hb_lc, hb_s⟩ := Infer.lc hbody (by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact hpc
        · exact hctx M hM)
      exact ⟨.arrow (Subst.onTy_lc hb_s hpc) hb_lc, hb_s⟩
  | app hf harg huni =>
    intro hctx
    obtain ⟨hf_lc, hf_s⟩ := Infer.lc hf hctx
    obtain ⟨harg_lc, harg_s⟩ := Infer.lc harg (Subst.onCtx_wf hf_s hctx)
    have hs3 := huni.lc (Subst.onTy_lc harg_s hf_lc) (.arrow harg_lc ContainsBvarsUpTo.fvar)
    refine ⟨Subst.onTy_lc hs3 ContainsBvarsUpTo.fvar, ?_⟩
    intro p hp; rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · exact hf_s p hp
    · exact harg_s p hp
    · exact hs3 p hp
  | var hlook =>
    intro hctx
    exact ⟨PolyTy.openVars_isLC (hctx _ (List.mem_of_getElem? hlook)) (by simp), by simp⟩
  | ctor hlook =>
    intro _
    exact ⟨PolyTy.openVars_isLC (Ctor.toTy_wf _) (by simp [Ctor.toTy]), by simp⟩
  | letIn hrhs hbody =>
    intro hctx
    obtain ⟨hrhs_lc, hrhs_s⟩ := Infer.lc hrhs hctx
    obtain ⟨hbody_lc, hbody_s⟩ := Infer.lc hbody (by
      intro M hM
      rcases List.mem_cons.mp hM with rfl | hM
      · exact genScheme_wf hrhs_lc
      · exact (Subst.onCtx_wf hrhs_s hctx) M hM)
    refine ⟨hbody_lc, ?_⟩
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact hrhs_s p hp
    · exact hbody_s p hp
  | letInAnn hσwf _hΦN hrhs huni _hesc1 _hesc2 hbody =>
    intro hctx
    expose_names
    obtain ⟨hrhs_lc, hrhs_s⟩ := Infer.lc hrhs hctx
    have hSchk_lc : ∀ p ∈ Schk, p.2.IsLC :=
      UnifyRel.lc huni hrhs_lc (PolyTy.openVars_isLC hσwf (by simp))
    obtain ⟨hbody_lc, hbody_s⟩ := Infer.lc hbody (by
      intro M hM
      rcases List.mem_cons.mp hM with rfl | hM
      · exact hσwf
      · exact (Subst.onCtx_wf hSchk_lc (Subst.onCtx_wf hrhs_s hctx)) M hM)
    refine ⟨hbody_lc, ?_⟩
    intro p hp; rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · exact hrhs_s p hp
    · exact hSchk_lc p hp
    · exact hbody_s p hp
  | match_ hscrut hne hbr =>
    intro hctx
    obtain ⟨hτs_lc, hS₁⟩ := Infer.lc hscrut hctx
    obtain ⟨hρ_lc, hS₂⟩ := InferBranches.lc hbr
      (Subst.onCtx_wf hS₁ hctx)
      hτs_lc
      ContainsBvarsUpTo.fvar
    refine ⟨hρ_lc, ?_⟩
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact hS₁ p hp
    · exact hS₂ p hp
  | letRec hgroup hbody =>
    intro hctx
    have hS₁ := InferRecGroup.lc hgroup
      (by
        intro M hM
        rcases List.mem_append.mp hM with hM | hM
        · obtain ⟨x, _, rfl⟩ := List.mem_map.mp hM
          exact ContainsBvarsUpTo.fvar
        · exact hctx M hM)
      (by
        intro β hβ
        obtain ⟨x, _, rfl⟩ := List.mem_map.mp hβ
        exact ContainsBvarsUpTo.fvar)
    obtain ⟨hbody_lc, hbody_s⟩ := Infer.lc hbody
      (by
        intro M hM
        rcases List.mem_append.mp hM with hM | hM
        · simp only [genGroupSchemes] at hM
          obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
          obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ
          exact PolyTy.genGroup_wf (Subst.onTy_lc hS₁ ContainsBvarsUpTo.fvar)
        · exact (Subst.onCtx_wf hS₁ hctx) M hM)
    refine ⟨hbody_lc, ?_⟩
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact hS₁ p hp
    · exact hbody_s p hp
  | letRecAnn hwf hlen hgroup hbody =>
    intro hctx
    expose_names
    have hctxσ : CtxWF { ctx with env := schemes ++ ctx.env } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hwf M hM
      · exact hctx M hM
    have hS₁ := InferRecGroupAnn.lc hgroup hctxσ hwf
    obtain ⟨hbody_lc, hS₂⟩ := Infer.lc hbody (Subst.onCtx_wf hS₁ hctxσ)
    refine ⟨hbody_lc, ?_⟩
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact hS₁ p hp
    · exact hS₂ p hp
termination_by e.size
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.size, Expr.size_openTyVars]; omega)
theorem InferBranches.lc {Φ ctx scrutTy ρ brs Φ' S brsOut}
    (h : InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut)
    (hctx : CtxWF ctx) (hscrutTy : scrutTy.IsLC) (hρ : ρ.IsLC) :
    (S.onTy ρ).IsLC ∧ (∀ p ∈ S, p.2.IsLC) := by
  cases h with
  | nil => exact ⟨by simpa using hρ, by simp⟩
  | cons hlook hn huni0 hbody huni hrest =>
    have hS₀ := huni0.lc hscrutTy
      (.customTy (fun t ht => by obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht; exact ContainsBvarsUpTo.fvar))
    obtain ⟨hτb_lc, hS₁⟩ := Infer.lc hbody
      (branchBindings_wf (Subst.onCtx_wf hS₀ hctx)
        (fun t ht => by
          obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
          obtain ⟨x, _, rfl⟩ := List.mem_map.mp hv
          exact Subst.onTy_lc hS₀ ContainsBvarsUpTo.fvar)
        (by simp))
    have hS₂ := huni.lc hτb_lc (Subst.onTy_lc hS₁ (Subst.onTy_lc hS₀ hρ))
    obtain ⟨hres, hS₃⟩ := InferBranches.lc hrest
      (Subst.onCtx_wf hS₂ (Subst.onCtx_wf hS₁ (Subst.onCtx_wf hS₀ hctx)))
      (Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ (Subst.onTy_lc hS₀ hscrutTy)))
      (Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ (Subst.onTy_lc hS₀ hρ)))
    refine ⟨?_, ?_⟩
    · rw [Subst.onTy_append, Subst.onTy_append, Subst.onTy_append]; exact hres
    · intro p hp; rw [List.mem_append, List.mem_append, List.mem_append] at hp
      rcases hp with ((hp | hp) | hp) | hp
      · exact hS₀ p hp
      · exact hS₁ p hp
      · exact hS₂ p hp
      · exact hS₃ p hp
  | consWild hbody huni hrest =>
    obtain ⟨hτb_lc, hS₁⟩ := Infer.lc hbody hctx
    have hS₂ := huni.lc hτb_lc (Subst.onTy_lc hS₁ hρ)
    obtain ⟨hres, hS₃⟩ := InferBranches.lc hrest
      (Subst.onCtx_wf hS₂ (Subst.onCtx_wf hS₁ hctx))
      (Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ hscrutTy))
      (Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ hρ))
    refine ⟨?_, ?_⟩
    · rw [Subst.onTy_append, Subst.onTy_append]; exact hres
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hS₁ p hp
      · exact hS₂ p hp
      · exact hS₃ p hp
termination_by Expr.sizeBranches brs
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)
theorem InferRecGroup.lc {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut)
    (hctx : CtxWF ctx) (htargets : ∀ β ∈ targets, β.IsLC) :
    (∀ p ∈ S, p.2.IsLC) := by
  cases h with
  | nil => simp
  | cons he huni hrest =>
    obtain ⟨hτ, hS₁⟩ := Infer.lc he hctx
    have hS₂ := UnifyRel.lc huni hτ
      (Subst.onTy_lc hS₁ (htargets _ (List.mem_cons_self ..)))
    have hS₃ := InferRecGroup.lc hrest
      (Subst.onCtx_wf hS₂ (Subst.onCtx_wf hS₁ hctx))
      (by
        intro β' hβ'
        obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hβ'
        exact Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ (htargets b (List.mem_cons_of_mem _ hb))))
    intro p hp; rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · exact hS₁ p hp
    · exact hS₂ p hp
    · exact hS₃ p hp
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)
theorem InferRecGroupAnn.lc {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut)
    (hctx : CtxWF ctx) (hsch : ∀ σ ∈ schemes, σ.WF) :
    (∀ p ∈ S, p.2.IsLC) := by
  cases h with
  | nil => simp
  | cons hΦN hinfer huni _ _ hrest =>
    expose_names
    obtain ⟨hτ, hS₁⟩ := Infer.lc hinfer hctx
    have hσopen : (σ.openVars (freshVars N σ.paramCount)).IsLC :=
      PolyTy.openVars_isLC (hsch σ List.mem_cons_self) (by simp)
    have hSchk := UnifyRel.lc huni hτ hσopen
    have hS₂ := InferRecGroupAnn.lc hrest
      (Subst.onCtx_wf hSchk (Subst.onCtx_wf hS₁ hctx))
      (fun σ' hσ' => hsch σ' (List.mem_cons_of_mem _ hσ'))
    intro p hp; rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · exact hS₁ p hp
    · exact hSchk p hp
    · exact hS₂ p hp
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end

private theorem List.forall₂_self_map {α β} {R : α → β → Prop} {f : α → β} :
    ∀ {l : List α}, (∀ x ∈ l, R x (f x)) → List.Forall₂ R l (l.map f)
  | [], _ => .nil
  | _ :: _, h =>
    .cons (h _ (List.mem_cons_self ..))
      (List.forall₂_self_map (fun x hx => h x (List.mem_cons_of_mem _ hx)))

/-- Opening a scheme body (bvars `< Xs.length`) with fresh *names* is an
    instantiation by those names-as-`fvar`s. Bridges the `var` rule: the
    algorithm's `openVars` result is what the declarative `var` rule's
    `InstantiatesBy` premise demands. -/
theorem InstantiatesBy.openVars {Xs : List Nat} {n : Nat} {ty : Ty}
    (hty : ContainsBvarsUpTo n ty) (hn : n ≤ Xs.length) :
    InstantiatesBy (Xs.map (Ty.fvar ·)) ty (ty.openVars Xs) := by
  induction ty using Ty.rec_strong with
  | prim p => exact .prim
  | fvar m => exact .fvar
  | bvar i =>
    cases hty with
    | bvar hlt =>
      have hi : i < Xs.length := by omega
      simp only [Ty.openVars, Ty.instantiate, List.getElem?_eq_getElem hi, Option.elim_some]
      exact .bvar (by simp [List.getElem?_map, List.getElem?_eq_getElem hi])
  | arrow a b iha ihb => cases hty with | arrow ha hb => exact .arrow (iha ha) (ihb hb)
  | customTy nm tys ih =>
    cases hty with
    | customTy hball =>
      simp only [Ty.openVars, Ty.instantiate, TyList.instantiate_eq_map]
      exact .customTy (List.forall₂_self_map (fun t ht => ih t ht (hball t ht)))


/-! ### Cofinite-generalization machinery for the `letIn` soundness case -/

/-- A generalization candidate is, by construction, not fixed by the env. -/
theorem genVars_not_mem {rigid : List Nat} {env : Env} {τ : Ty} {g : Nat}
    (h : g ∈ genVars rigid env τ) : g ∉ env.freeVars := by
  simp only [genVars, List.mem_filter, Bool.and_eq_true] at h
  simpa using h.2.1

/-- A generalization candidate is, by construction, not one of the rigid scoped
    type variables (the bound expression's annotation fvars). This is what keeps
    the `let` from generalizing over an in-scope skolem. -/
theorem genVars_not_mem_rigid {rigid : List Nat} {env : Env} {τ : Ty} {g : Nat}
    (h : g ∈ genVars rigid env τ) : g ∉ rigid := by
  simp only [genVars, List.mem_filter, Bool.and_eq_true] at h
  simpa using h.2.2

/-- A substitution whose domain avoids `Xs` commutes with opening by `Xs`. -/
theorem Subst.onTy_openVars {S : Subst} {Xs : List Nat}
    (h_lc : ∀ p ∈ S, p.2.IsLC) (h_fresh : ∀ p ∈ S, p.1 ∉ Xs) :
    ∀ {ty : Ty}, S.onTy (Ty.openVars Xs ty) = Ty.openVars Xs (S.onTy ty) := by
  induction S with
  | nil => intro ty; simp only [Subst.onTy_nil]
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    have hU : U.IsLC := h_lc (Z, U) (List.mem_cons_self ..)
    have hZ : Z ∉ Xs := h_fresh (Z, U) (List.mem_cons_self ..)
    intro ty
    simp only [Subst.onTy, Ty.substFvars]
    rw [Ty.substFvar_openVars hU hZ]
    exact ih (fun p hp => h_lc p (List.mem_cons_of_mem _ hp))
             (fun p hp => h_fresh p (List.mem_cons_of_mem _ hp))

@[simp] theorem Ty.openVars_prim {Xs : List Nat} {p : PrimTy} :
    Ty.openVars Xs (.prim p) = .prim p := rfl

private theorem TyList.closeOver_eq_map (gs : List Nat) (tys : List Ty) :
    TyList.closeOver gs tys = tys.map (Ty.closeOver gs) := by
  induction tys with
  | nil => rfl
  | cons hd tl ih => simp [TyList.closeOver, ih]

/-- The free vars of an opening are among the original free vars or the opening
    names. -/
theorem Ty.freeVars_openVars_subset {Xs : List Nat} {t : Ty} :
    ∀ z ∈ (Ty.openVars Xs t).freeVars, z ∈ t.freeVars ∨ z ∈ Xs := by
  induction t using Ty.rec_strong with
  | prim p => intro z hz; simp [Ty.openVars, Ty.instantiate, Ty.freeVars] at hz
  | fvar n => intro z hz; left; simpa only [Ty.openVars, Ty.instantiate, Ty.freeVars] using hz
  | bvar i =>
    intro z hz
    simp only [Ty.openVars, Ty.instantiate] at hz
    cases hh : Xs[i]? with
    | none => rw [hh] at hz; simp [Ty.freeVars] at hz
    | some x =>
      rw [hh] at hz
      simp only [Option.elim_some, Ty.freeVars, List.mem_singleton] at hz
      subst hz; exact .inr (List.mem_of_getElem? hh)
  | arrow a b iha ihb =>
    intro z hz
    rw [Ty.openVars_arrow] at hz
    simp only [Ty.freeVars, List.mem_dedup, List.mem_append] at hz ⊢
    rcases hz with hz | hz
    · rcases iha z hz with h | h
      · exact .inl (.inl h)
      · exact .inr h
    · rcases ihb z hz with h | h
      · exact .inl (.inr h)
      · exact .inr h
  | customTy nm tys ih =>
    intro z hz
    rw [Ty.openVars_customTy, Ty.freeVars] at hz
    obtain ⟨t', ht', hzt'⟩ := TyList.mem_freeVars_iff.mp hz
    obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
    rcases ih t0 ht0 z hzt' with h | h
    · exact .inl (by rw [Ty.freeVars]; exact TyList.mem_freeVars_of_mem ht0 h)
    · exact .inr h

/-- A closed type's opening has free vars only among the opening names. -/
theorem Ty.freeVars_openVars_closed {Xs : List Nat} {t : Ty} (hcl : NoFreeVars t)
    {z : Nat} (hz : z ∈ (Ty.openVars Xs t).freeVars) : z ∈ Xs := by
  rcases Ty.freeVars_openVars_subset z hz with h | h
  · exact absurd h (hcl.not_mem_freeVars z)
  · exact h

/-! ### Variable-tracking lemmas for substitutions and `UnifyRel`
    (relocated here so the honest soundness can use them)

`Subst.mem_freeVars_onTy` bounds the free vars of a substituted type by the
input's vars plus the substitution's range; `UnifyRel.range_mem` / `.dom_mem`
locate a derived substitution's range/domain inside the inputs' vars; and
`UnifyRel.eliminates` is the occurs-check at the variable-set level (a domain
variable never survives in any image). -/

/-- `TyList.freeVars` membership characterisation (the `customTy` payload). -/
theorem mem_TyList_freeVars {tys : List Ty} {v : Nat} :
    v ∈ TyList.freeVars tys ↔ ∃ t ∈ tys, v ∈ t.freeVars := by
  constructor
  · intro h
    by_contra hc
    push_neg at hc
    exact (TyList.not_mem_freeVars_iff.mpr hc) h
  · rintro ⟨t, ht, hv⟩
    exact TyList.mem_freeVars_of_mem ht hv

/-- Free vars introduced by a single-variable substitution come from the input
    or from the replacement. -/
theorem Ty.mem_freeVars_substFvar {Z : Nat} {U x : Ty} {v : Nat}
    (hv : v ∈ (Ty.substFvar Z U x).freeVars) : v ∈ x.freeVars ∨ v ∈ U.freeVars := by
  induction x using Ty.rec_strong with
  | prim p => simp [Ty.substFvar, Ty.freeVars] at hv
  | bvar i => simp [Ty.substFvar, Ty.freeVars] at hv
  | fvar m =>
    simp only [Ty.substFvar] at hv
    by_cases hm : m = Z
    · simp only [if_pos hm] at hv; exact Or.inr hv
    · simp only [if_neg hm, Ty.freeVars, List.mem_singleton] at hv
      subst hv; exact Or.inl (by simp [Ty.freeVars])
  | arrow a b iha ihb =>
    simp only [Ty.substFvar, Ty.freeVars, List.mem_dedup, List.mem_append] at hv
    rcases hv with h | h
    · rcases iha h with h' | h'
      · exact Or.inl (by simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inl h')
      · exact Or.inr h'
    · rcases ihb h with h' | h'
      · exact Or.inl (by simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inr h')
      · exact Or.inr h'
  | customTy nm tys ih =>
    simp only [Ty.substFvar, Ty.freeVars, TyList.substFvar_eq_map] at hv
    rw [mem_TyList_freeVars] at hv
    obtain ⟨t', ht', hvt'⟩ := hv
    obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
    rcases ih t0 ht0 hvt' with h | h
    · exact Or.inl (by simp only [Ty.freeVars]; exact TyList.mem_freeVars_of_mem ht0 h)
    · exact Or.inr h

/-- Free vars introduced by a whole substitution come from the input or from
    one of the substitution's replacements. -/
theorem Subst.mem_freeVars_onTy {S : Subst} {x : Ty} {v : Nat}
    (hv : v ∈ (S.onTy x).freeVars) : v ∈ x.freeVars ∨ ∃ p ∈ S, v ∈ p.2.freeVars := by
  induction S generalizing x with
  | nil => exact Or.inl (by simpa [Subst.onTy] using hv)
  | cons hd tl ih =>
    obtain ⟨Z, U⟩ := hd
    rw [show ((Z, U) :: tl) = [(Z, U)] ++ tl from rfl, Subst.onTy_append] at hv
    rcases ih hv with h | h
    · have he : Subst.onTy [(Z, U)] x = Ty.substFvar Z U x := rfl
      rw [he] at h
      rcases Ty.mem_freeVars_substFvar h with h' | h'
      · exact Or.inl h'
      · exact Or.inr ⟨(Z, U), List.mem_cons_self, h'⟩
    · obtain ⟨p, hp, hvp⟩ := h
      exact Or.inr ⟨p, List.mem_cons_of_mem _ hp, hvp⟩

/-- Injecting a sub-type's free var into a compound type's free vars. -/
theorem Ty.mem_freeVars_arrowL {a b : Ty} {v : Nat} (h : v ∈ a.freeVars) :
    v ∈ (Ty.arrow a b).freeVars := by
  simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inl h
theorem Ty.mem_freeVars_arrowR {a b : Ty} {v : Nat} (h : v ∈ b.freeVars) :
    v ∈ (Ty.arrow a b).freeVars := by
  simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inr h
theorem Ty.mem_freeVars_customTy {nm : TyName} {tys : List Ty} {t : Ty} {v : Nat}
    (ht : t ∈ tys) (h : v ∈ t.freeVars) : v ∈ (Ty.customTy nm tys).freeVars := by
  simp only [Ty.freeVars]; exact TyList.mem_freeVars_of_mem ht h

/-- A variable not occurring in the replacement does not survive substituting it. -/
theorem Ty.not_mem_freeVars_substFvar_self {n : Nat} {U x : Ty}
    (hU : n ∉ U.freeVars) : n ∉ (Ty.substFvar n U x).freeVars := by
  induction x using Ty.rec_strong with
  | prim p => simp [Ty.substFvar, Ty.freeVars]
  | bvar i => simp [Ty.substFvar, Ty.freeVars]
  | fvar m =>
    simp only [Ty.substFvar]
    by_cases hm : m = n
    · simp only [if_pos hm]; exact hU
    · simp only [if_neg hm, Ty.freeVars, List.mem_singleton]; exact fun hc => hm hc.symm
  | arrow a b iha ihb =>
    simp only [Ty.substFvar, Ty.freeVars, List.mem_dedup, List.mem_append, not_or]
    exact ⟨iha, ihb⟩
  | customTy nm tys ih =>
    simp only [Ty.substFvar, Ty.freeVars, TyList.substFvar_eq_map]
    intro hc
    rw [mem_TyList_freeVars] at hc
    obtain ⟨t', ht', hvt'⟩ := hc
    obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
    exact ih t0 ht0 hvt'

/-! The range of a `UnifyRel`-substitution lies within the inputs' free vars. -/
mutual
theorem UnifyRel.range_mem : {a b : Ty} → {S : Subst} → UnifyRel a b S →
    ∀ p ∈ S, ∀ v ∈ p.2.freeVars, v ∈ a.freeVars ∨ v ∈ b.freeVars
  | _, _, _, .prim => by simp
  | _, _, _, .fvarRefl => by simp
  | _, _, _, .fvarL _ _ => by
    intro p hp v hv; rw [List.mem_singleton] at hp; subst hp; exact Or.inr hv
  | _, _, _, .fvarR _ _ => by
    intro p hp v hv; rw [List.mem_singleton] at hp; subst hp; exact Or.inl hv
  | _, _, _, @UnifyRel.arrow a b c d S₁ S₂ h₁ h₂ => by
    intro p hp v hv
    rw [List.mem_append] at hp
    rcases hp with hp | hp
    · rcases UnifyRel.range_mem h₁ p hp v hv with h | h
      · exact Or.inl (Ty.mem_freeVars_arrowL h)
      · exact Or.inr (Ty.mem_freeVars_arrowL h)
    · rcases UnifyRel.range_mem h₂ p hp v hv with h | h
      · rcases Subst.mem_freeVars_onTy h with hb | ⟨q, hq, hvq⟩
        · exact Or.inl (Ty.mem_freeVars_arrowR hb)
        · rcases UnifyRel.range_mem h₁ q hq v hvq with h' | h'
          · exact Or.inl (Ty.mem_freeVars_arrowL h')
          · exact Or.inr (Ty.mem_freeVars_arrowL h')
      · rcases Subst.mem_freeVars_onTy h with hd | ⟨q, hq, hvq⟩
        · exact Or.inr (Ty.mem_freeVars_arrowR hd)
        · rcases UnifyRel.range_mem h₁ q hq v hvq with h' | h'
          · exact Or.inl (Ty.mem_freeVars_arrowL h')
          · exact Or.inr (Ty.mem_freeVars_arrowL h')
  | _, _, _, .customTy hl => by
    intro p hp v hv
    rcases UnifyRelList.range_mem hl p hp v hv with ⟨t, ht, h⟩ | ⟨t, ht, h⟩
    · exact Or.inl (Ty.mem_freeVars_customTy ht h)
    · exact Or.inr (Ty.mem_freeVars_customTy ht h)
theorem UnifyRelList.range_mem : {as bs : List Ty} → {S : Subst} → UnifyRelList as bs S →
    ∀ p ∈ S, ∀ v ∈ p.2.freeVars,
      (∃ t ∈ as, v ∈ t.freeVars) ∨ (∃ t ∈ bs, v ∈ t.freeVars)
  | _, _, _, .nil => by simp
  | _, _, _, @UnifyRelList.cons t₁ t₂ ts₁ ts₂ S₁ S₂ h₁ ht => by
    intro p hp v hv
    rw [List.mem_append] at hp
    rcases hp with hp | hp
    · rcases UnifyRel.range_mem h₁ p hp v hv with h | h
      · exact Or.inl ⟨t₁, List.mem_cons_self, h⟩
      · exact Or.inr ⟨t₂, List.mem_cons_self, h⟩
    · rcases UnifyRelList.range_mem ht p hp v hv with ⟨t, ht', hvt⟩ | ⟨t, ht', hvt⟩
      · obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
        rcases Subst.mem_freeVars_onTy hvt with hb | ⟨q, hq, hvq⟩
        · exact Or.inl ⟨t0, List.mem_cons_of_mem _ ht0, hb⟩
        · rcases UnifyRel.range_mem h₁ q hq v hvq with h' | h'
          · exact Or.inl ⟨t₁, List.mem_cons_self, h'⟩
          · exact Or.inr ⟨t₂, List.mem_cons_self, h'⟩
      · obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
        rcases Subst.mem_freeVars_onTy hvt with hb | ⟨q, hq, hvq⟩
        · exact Or.inr ⟨t0, List.mem_cons_of_mem _ ht0, hb⟩
        · rcases UnifyRel.range_mem h₁ q hq v hvq with h' | h'
          · exact Or.inl ⟨t₁, List.mem_cons_self, h'⟩
          · exact Or.inr ⟨t₂, List.mem_cons_self, h'⟩
end

/-! The domain of a `UnifyRel`-substitution lies within the inputs' free vars. -/
mutual
theorem UnifyRel.dom_mem : {a b : Ty} → {S : Subst} → UnifyRel a b S →
    ∀ p ∈ S, p.1 ∈ a.freeVars ∨ p.1 ∈ b.freeVars
  | _, _, _, .prim => by simp
  | _, _, _, .fvarRefl => by simp
  | _, _, _, .fvarL _ _ => by
    intro p hp; rw [List.mem_singleton] at hp; subst hp; exact Or.inl (by simp [Ty.freeVars])
  | _, _, _, .fvarR _ _ => by
    intro p hp; rw [List.mem_singleton] at hp; subst hp; exact Or.inr (by simp [Ty.freeVars])
  | _, _, _, @UnifyRel.arrow a b c d S₁ S₂ h₁ h₂ => by
    intro p hp
    rw [List.mem_append] at hp
    rcases hp with hp | hp
    · rcases UnifyRel.dom_mem h₁ p hp with h | h
      · exact Or.inl (Ty.mem_freeVars_arrowL h)
      · exact Or.inr (Ty.mem_freeVars_arrowL h)
    · rcases UnifyRel.dom_mem h₂ p hp with h | h
      · rcases Subst.mem_freeVars_onTy h with hb | ⟨q, hq, hvq⟩
        · exact Or.inl (Ty.mem_freeVars_arrowR hb)
        · rcases UnifyRel.range_mem h₁ q hq p.1 hvq with h' | h'
          · exact Or.inl (Ty.mem_freeVars_arrowL h')
          · exact Or.inr (Ty.mem_freeVars_arrowL h')
      · rcases Subst.mem_freeVars_onTy h with hd | ⟨q, hq, hvq⟩
        · exact Or.inr (Ty.mem_freeVars_arrowR hd)
        · rcases UnifyRel.range_mem h₁ q hq p.1 hvq with h' | h'
          · exact Or.inl (Ty.mem_freeVars_arrowL h')
          · exact Or.inr (Ty.mem_freeVars_arrowL h')
  | _, _, _, .customTy hl => by
    intro p hp
    rcases UnifyRelList.dom_mem hl p hp with ⟨t, ht, h⟩ | ⟨t, ht, h⟩
    · exact Or.inl (Ty.mem_freeVars_customTy ht h)
    · exact Or.inr (Ty.mem_freeVars_customTy ht h)
theorem UnifyRelList.dom_mem : {as bs : List Ty} → {S : Subst} → UnifyRelList as bs S →
    ∀ p ∈ S, (∃ t ∈ as, p.1 ∈ t.freeVars) ∨ (∃ t ∈ bs, p.1 ∈ t.freeVars)
  | _, _, _, .nil => by simp
  | _, _, _, @UnifyRelList.cons t₁ t₂ ts₁ ts₂ S₁ S₂ h₁ ht => by
    intro p hp
    rw [List.mem_append] at hp
    rcases hp with hp | hp
    · rcases UnifyRel.dom_mem h₁ p hp with h | h
      · exact Or.inl ⟨t₁, List.mem_cons_self, h⟩
      · exact Or.inr ⟨t₂, List.mem_cons_self, h⟩
    · rcases UnifyRelList.dom_mem ht p hp with ⟨t, ht', hvt⟩ | ⟨t, ht', hvt⟩
      · obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
        rcases Subst.mem_freeVars_onTy hvt with hb | ⟨q, hq, hvq⟩
        · exact Or.inl ⟨t0, List.mem_cons_of_mem _ ht0, hb⟩
        · rcases UnifyRel.range_mem h₁ q hq p.1 hvq with h' | h'
          · exact Or.inl ⟨t₁, List.mem_cons_self, h'⟩
          · exact Or.inr ⟨t₂, List.mem_cons_self, h'⟩
      · obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
        rcases Subst.mem_freeVars_onTy hvt with hb | ⟨q, hq, hvq⟩
        · exact Or.inr ⟨t0, List.mem_cons_of_mem _ ht0, hb⟩
        · rcases UnifyRel.range_mem h₁ q hq p.1 hvq with h' | h'
          · exact Or.inl ⟨t₁, List.mem_cons_self, h'⟩
          · exact Or.inr ⟨t₂, List.mem_cons_self, h'⟩
end

/-! The occurs check at the variable-set level: a domain variable of a
    `UnifyRel`-substitution never survives in any of its images. -/
mutual
theorem UnifyRel.eliminates : {a b : Ty} → {S : Subst} → UnifyRel a b S →
    ∀ p ∈ S, ∀ (x : Ty), p.1 ∉ (S.onTy x).freeVars
  | _, _, _, .prim => by simp
  | _, _, _, .fvarRefl => by simp
  | _, _, _, .fvarL _ hocc => by
    intro p hp x; rw [List.mem_singleton] at hp; subst hp
    exact Ty.not_mem_freeVars_substFvar_self hocc
  | _, _, _, .fvarR _ hocc => by
    intro p hp x; rw [List.mem_singleton] at hp; subst hp
    exact Ty.not_mem_freeVars_substFvar_self hocc
  | _, _, _, @UnifyRel.arrow a b c d S₁ S₂ h₁ h₂ => by
    intro p hp x hc
    rw [Subst.onTy_append] at hc
    rw [List.mem_append] at hp
    rcases hp with hp | hp
    · rcases Subst.mem_freeVars_onTy hc with h | ⟨q, hq, hvq⟩
      · exact UnifyRel.eliminates h₁ p hp x h
      · rcases UnifyRel.range_mem h₂ q hq p.1 hvq with h' | h'
        · exact UnifyRel.eliminates h₁ p hp b h'
        · exact UnifyRel.eliminates h₁ p hp d h'
    · exact UnifyRel.eliminates h₂ p hp (S₁.onTy x) hc
  | _, _, _, .customTy hl => by
    intro p hp x hc
    exact UnifyRelList.eliminates hl p hp x hc
theorem UnifyRelList.eliminates : {as bs : List Ty} → {S : Subst} → UnifyRelList as bs S →
    ∀ p ∈ S, ∀ (x : Ty), p.1 ∉ (S.onTy x).freeVars
  | _, _, _, .nil => by simp
  | _, _, _, @UnifyRelList.cons t₁ t₂ ts₁ ts₂ S₁ S₂ h₁ ht => by
    intro p hp x hc
    rw [Subst.onTy_append] at hc
    rw [List.mem_append] at hp
    rcases hp with hp | hp
    · rcases Subst.mem_freeVars_onTy hc with h | ⟨q, hq, hvq⟩
      · exact UnifyRel.eliminates h₁ p hp x h
      · rcases UnifyRelList.range_mem ht q hq p.1 hvq with ⟨t, ht', hvt⟩ | ⟨t, ht', hvt⟩
        · obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
          exact UnifyRel.eliminates h₁ p hp t0 hvt
        · obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
          exact UnifyRel.eliminates h₁ p hp t0 hvt
    · exact UnifyRelList.eliminates ht p hp (S₁.onTy x) hc
end

/-- The principal generalization `genScheme env τ₁` is a `HasSchemeVars` witness:
    if `rhs` types as `τ₁` then it types at every fresh opening of `genScheme`.
    (The opening renames the generalization candidates `genVars` — which are not
    fixed by the env, `genVars_not_mem` — to the fresh `Xs`.) -/
theorem genScheme_hasSchemeVars {ctx : Ctx} {rhs : Expr} {τ₁ : Ty}
    (hrhs : TypeOfElabHM ctx rhs τ₁) (hlc : τ₁.IsLC) :
    HasSchemeVars (genVars rhs.tyFreeVars ctx.env τ₁) ctx rhs
      (genScheme rhs.tyFreeVars ctx.env τ₁) := by
  intro Xs hXfresh
  -- Type-passing `HasSchemeVars` instantiates the bound term (`rhs.instTy (Xs.map fvar)`);
  -- but `rhs` is type-bvar-bounded (it has a typing), so `instTy` is the identity.
  rw [Expr.instTy_eq_self_of_tyBvarBounded (TypeOfElabHM.tyBvarBounded hrhs)]
  obtain ⟨hXlen, hXnodup, hXavoid⟩ := hXfresh
  have hgs_X : ∀ g ∈ genVars rhs.tyFreeVars ctx.env τ₁, g ∉ Xs := fun g hg hc => hXavoid g hc hg
  have hrename : TypeOfElabHM ctx rhs
      (Ty.substFvars ((genVars rhs.tyFreeVars ctx.env τ₁).zip (Xs.map (Ty.fvar ·))) τ₁) := by
    have h0 := TypeOfElabHM.typ_substs_preservation
      ((genVars rhs.tyFreeVars ctx.env τ₁).zip (Xs.map (Ty.fvar ·)))
      (fun p hp => genVars_not_mem (List.of_mem_zip hp).1)
      (fun p hp => by
        obtain ⟨x, _, hx⟩ := List.mem_map.mp (List.of_mem_zip hp).2
        exact hx ▸ ContainsBvarsUpTo.fvar) hrhs
    -- The rename keys are generalization candidates, hence avoid `rhs`'s
    -- annotation fvars (`genVars_not_mem_rigid`); the term collapses back.
    rwa [Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars
      (fun p hp => genVars_not_mem_rigid (List.of_mem_zip hp).1)] at h0
  rw [← Ty.openVars_closeOver_rename hlc genVars_nodup hXlen hgs_X] at hrename
  exact hrename

/-- Applying a substitution to a *closed* scheme (no free type vars in its body)
    is a no-op. -/
theorem Subst.onPolyTy_eq_self_of_closed {S : Subst} {σ : PolyTy}
    (h : NoFreeVars σ.body) : S.onPolyTy σ = σ := by
  obtain ⟨pc, b⟩ := σ
  simp only [Subst.onPolyTy, Subst.onTy, PolyTy.mk.injEq, true_and]
  exact Ty.substFvars_eq_self_of_no_key (fun p _ => h.not_mem_freeVars p.1)

/-- Push a substitution through a `HasSchemeVars` witness: opening commutes with
    `S` (which avoids the fresh names by the cofinite `L`). -/
theorem HasSchemeVars.onSubst {L : List Nat} {ctx : Ctx} {rhs : Expr} {M : PolyTy}
    (h : HasSchemeVars L ctx rhs M) (S : Subst) (hS : ∀ p ∈ S, p.2.IsLC)
    (hS_rhs : ∀ p ∈ S, p.1 ∉ rhs.tyFreeVars) :
    HasSchemeVars (L ++ S.map Prod.fst) (S.onCtx ctx) rhs (S.onPolyTy M) := by
  intro Xs hXfresh
  obtain ⟨hXlen, hXnodup, hXavoid⟩ := hXfresh
  have hX_S : ∀ p ∈ S, p.1 ∉ Xs := fun p hp hc =>
    hXavoid p.1 hc (List.mem_append_right _ (List.mem_map.mpr ⟨p, hp, rfl⟩))
  have hwit := h Xs ⟨hXlen, hXnodup, fun x hx hc => hXavoid x hx (List.mem_append_left _ hc)⟩
  -- Type-passing `HasSchemeVars` opens the bound term (`rhs.instTy (Xs.map fvar) =
  -- rhs.openTyVars Xs`); `S` fixes it since `S` avoids both `rhs`'s annotation fvars
  -- (`hS_rhs`) and the fresh `Xs` (`hX_S`).
  rw [Expr.instTy_fvar_eq_openTyVars] at hwit
  have hfin := TypeOfElabHM.onSubst_fixed S hS
    (Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars
      (fun p hp hc => (Expr.tyFreeVars_openTyVars hc).elim (fun h => hS_rhs p hp h)
        (fun h => hX_S p hp h)))
    hwit
  show TypeOfElabHM (S.onCtx ctx) (rhs.instTy (Xs.map Ty.fvar)) (Ty.openVars Xs (S.onTy M.body))
  rw [Expr.instTy_fvar_eq_openTyVars, ← Subst.onTy_openVars hS hX_S]
  exact hfin

/-! ### Free-var subset lemmas for the close-back operations (for `Infer.locality`)

Closing scoped variables only ever maps `fvar`s to `bvar`s, never the reverse, so
it never *introduces* a free type var. These let the locality lemma reduce the
closed-back elaborated sub-terms to their pre-close free vars. -/

/-- `Ty.closeOverFrom` introduces no new free vars. -/
theorem Ty.freeVars_closeOverFrom_subset {d : Nat} {vars : List Nat} :
    ∀ {τ : Ty} {g : Nat}, g ∈ (Ty.closeOverFrom d vars τ).freeVars → g ∈ τ.freeVars := by
  intro τ
  induction τ using Ty.rec_strong with
  | prim p => intro g h; simp [Ty.closeOverFrom, Ty.freeVars] at h
  | bvar i => intro g h; simp [Ty.closeOverFrom, Ty.freeVars] at h
  | fvar n =>
    intro g h
    simp only [Ty.closeOverFrom] at h
    cases hidx : vars.idxOf? n with
    | none => rw [hidx] at h; exact h
    | some i => rw [hidx] at h; simp [Ty.freeVars] at h
  | arrow a b iha ihb =>
    intro g h
    simp only [Ty.closeOverFrom, Ty.freeVars, List.mem_dedup, List.mem_append] at h ⊢
    rcases h with h | h
    · exact .inl (iha h)
    · exact .inr (ihb h)
  | customTy nm tys ih =>
    intro g h
    simp only [Ty.closeOverFrom, Ty.freeVars, TyList.closeOverFrom_eq_map] at h
    rw [mem_TyList_freeVars] at h
    obtain ⟨t', ht', hgt'⟩ := h
    obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
    rw [Ty.freeVars]
    exact TyList.mem_freeVars_of_mem ht0 (ih t0 ht0 hgt')

/-- The annotated-group close keeps free-var subset per binding (depth-shifted by its
    scheme), so a `g` in the closed group's free vars came from some binding's. -/
private theorem RecGroupAnn.tyFreeVars_closeTyVarsAux_subset {Xs : List Nat} {g : Nat} :
    ∀ (schemes : List PolyTy) (bindings : List Expr) (d : Nat),
      (∀ e ∈ bindings, ∀ d', g ∈ (e.closeTyVarsAux d' Xs).tyFreeVars → g ∈ e.tyFreeVars) →
      g ∈ Expr.tyFreeVars.RecGroup.tyFreeVars (RecGroupAnn.closeTyVarsAux d Xs schemes bindings) →
      g ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings := by
  intro schemes bindings
  induction bindings generalizing schemes with
  | nil =>
    intro d _ h
    cases schemes <;>
      simp only [RecGroupAnn.closeTyVarsAux, Expr.tyFreeVars.RecGroup.tyFreeVars,
        List.not_mem_nil] at h
  | cons hd tl ih =>
    intro d ihB h
    cases schemes with
    | nil =>
      simp only [RecGroupAnn.closeTyVarsAux, Expr.tyFreeVars.RecGroup.tyFreeVars,
        List.mem_append] at h ⊢
      rcases h with h | h
      · exact .inl (ihB hd List.mem_cons_self d h)
      · exact .inr (ih [] d (fun e he => ihB e (List.mem_cons_of_mem _ he)) h)
    | cons σ ss =>
      simp only [RecGroupAnn.closeTyVarsAux, Expr.tyFreeVars.RecGroup.tyFreeVars,
        List.mem_append] at h ⊢
      rcases h with h | h
      · exact .inl (ihB hd List.mem_cons_self (d + σ.paramCount) h)
      · exact .inr (ih ss d (fun e he => ihB e (List.mem_cons_of_mem _ he)) h)

/-- `Expr.closeTyVarsAux` introduces no new free type vars. (Re-based off `NoRecAnn`:
    closing only ever *removes* free vars — even through `letRecAnn` scheme bodies,
    which it leaves untouched — so the subset holds unconditionally.) -/
theorem Expr.tyFreeVars_closeTyVarsAux_subset {Xs : List Nat} {g : Nat} :
    ∀ (e : Expr) (d : Nat), g ∈ (e.closeTyVarsAux d Xs).tyFreeVars → g ∈ e.tyFreeVars := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p => intro d h; simp [Expr.closeTyVarsAux, Expr.tyFreeVars] at h
  | ctor nm => intro d h; simp [Expr.closeTyVarsAux, Expr.tyFreeVars] at h
  | var n tyArgs =>
    intro d h
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, List.mem_flatMap] at h ⊢
    obtain ⟨t, ht, hgt⟩ := h
    obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
    exact ⟨t0, ht0, Ty.freeVars_closeOverFrom_subset hgt⟩
  | lambda ann body ih =>
    intro d h
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, List.mem_append] at h ⊢
    rcases h with h | h
    · refine .inl ?_
      cases ann with
      | none => simp [Option.elim] at h
      | some t => simp only [Option.map_some, Option.elim] at h ⊢; exact Ty.freeVars_closeOverFrom_subset h
    · exact .inr (ih d h)
  | app f arg ihf iharg =>
    intro d h
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, List.mem_append] at h ⊢
    rcases h with h | h
    · exact .inl (ihf d h)
    · exact .inr (iharg d h)
  | letIn ann rhs body ihrhs ihbody =>
    intro d h
    cases ann with
    | none =>
      simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, Option.elim, List.nil_append,
        List.mem_append] at h ⊢
      rcases h with h | h
      · exact .inl (ihrhs d h)
      · exact .inr (ihbody d h)
    | some σ =>
      obtain ⟨pc, sbody⟩ := σ
      simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, Option.elim, List.mem_append] at h ⊢
      rcases h with (h | h) | h
      · exact .inl (.inl (Ty.freeVars_closeOverFrom_subset h))
      · exact .inl (.inr (ihrhs (d + pc) h))
      · exact .inr (ihbody d h)
  | match_ scrut branches ihscrut ihbranches =>
    intro d h
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, BranchList.closeTyVarsAux_eq_map,
      List.mem_append] at h ⊢
    rcases h with h | h
    · exact .inl (ihscrut d h)
    · refine .inr ?_
      induction branches with
      | nil => simp [Expr.tyFreeVars.BranchList.tyFreeVars] at h
      | cons hd tl ihtl =>
        obtain ⟨p, b⟩ := hd
        simp only [List.map_cons, Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append] at h ⊢
        rcases h with h | h
        · exact .inl (ihbranches p b (List.mem_cons_self ..) d h)
        · exact .inr (ihtl (fun p' b' hm => ihbranches p' b' (List.mem_cons_of_mem _ hm)) h)
  | letRec bindings body ih_bindings ih_body =>
    intro d h
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, RecGroup.closeTyVarsAux_eq_map,
      List.mem_append] at h ⊢
    rcases h with h | h
    · refine .inl ?_
      induction bindings with
      | nil => simp [Expr.tyFreeVars.RecGroup.tyFreeVars] at h
      | cons hd tl ihtl =>
        simp only [List.map_cons, Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at h ⊢
        rcases h with h | h
        · exact .inl (ih_bindings hd (List.mem_cons_self ..) d h)
        · exact .inr (ihtl (fun e hm => ih_bindings e (List.mem_cons_of_mem _ hm)) h)
    · exact .inr (ih_body d h)
  | letRecAnn schemes bindings body ih_bindings ih_body =>
    intro d h
    simp only [Expr.closeTyVarsAux, Expr.tyFreeVars, List.mem_append] at h ⊢
    rcases h with (h | h) | h
    · exact .inl (.inl h)
    · exact .inl (.inr (RecGroupAnn.tyFreeVars_closeTyVarsAux_subset schemes bindings d ih_bindings h))
    · exact .inr (ih_body d h)

/-- `Expr.closeTyVars` introduces no new free type vars (top level). -/
theorem Expr.tyFreeVars_closeTyVars_subset {Xs : List Nat} {e : Expr} {g : Nat}
    (h : g ∈ (e.closeTyVars Xs).tyFreeVars) : g ∈ e.tyFreeVars :=
  Expr.tyFreeVars_closeTyVarsAux_subset e 0 h

/-- All free type vars of `τ` are below `Φ`. The `fvar` analogue of
    `ContainsBvarsUpTo`; clean to push through substitution. -/
inductive Ty.BelowFvars (Φ : Nat) : Ty → Prop
  | prim : Ty.BelowFvars Φ (.prim p)
  | arrow : Ty.BelowFvars Φ a → Ty.BelowFvars Φ b → Ty.BelowFvars Φ (.arrow a b)
  | bvar : Ty.BelowFvars Φ (.bvar i)
  | fvar : i < Φ → Ty.BelowFvars Φ (.fvar i)
  | customTy : (∀ t ∈ tys, Ty.BelowFvars Φ t) → Ty.BelowFvars Φ (.customTy nm tys)

theorem Ty.BelowFvars.mono {Φ Φ' : Nat} {τ : Ty} (hle : Φ ≤ Φ')
    (h : Ty.BelowFvars Φ τ) : Ty.BelowFvars Φ' τ := by
  induction h with
  | prim => exact .prim
  | arrow _ _ iha ihb => exact .arrow iha ihb
  | bvar => exact .bvar
  | fvar hlt => exact .fvar (by omega)
  | customTy _ ih => exact .customTy (fun t ht => ih t ht)

/-- `substFvar` by a below-`Φ` type preserves below-`Φ`. -/
theorem Ty.BelowFvars.substFvar {Φ Z : Nat} {U τ : Ty}
    (hU : Ty.BelowFvars Φ U) (h : Ty.BelowFvars Φ τ) :
    Ty.BelowFvars Φ (Ty.substFvar Z U τ) := by
  induction τ using Ty.rec_strong with
  | prim _ => exact .prim
  | arrow a b iha ihb => cases h with | arrow ha hb => exact .arrow (iha ha) (ihb hb)
  | bvar i => simp only [Ty.substFvar]; exact .bvar
  | fvar m =>
    simp only [Ty.substFvar]
    by_cases hm : m = Z
    · simp only [if_pos hm]; exact hU
    · simp only [if_neg hm]; cases h with | fvar hlt => exact .fvar hlt
  | customTy nm tys ih =>
    cases h with
    | customTy hall =>
      simp only [Ty.substFvar]
      apply Ty.BelowFvars.customTy
      rw [TyList.substFvar_eq_map]
      intro t ht
      obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
      exact ih t0 ht0 (hall t0 ht0)

/-- A whole substitution with below-`Φ` replacements preserves below-`Φ`. -/
theorem Subst.onTy_belowFvars {Φ : Nat} {S : Subst} (hS : ∀ p ∈ S, Ty.BelowFvars Φ p.2) :
    ∀ {τ : Ty}, Ty.BelowFvars Φ τ → Ty.BelowFvars Φ (S.onTy τ) := by
  induction S with
  | nil => intro τ hτ; simpa using hτ
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    have hU : Ty.BelowFvars Φ U := hS (Z, U) (List.mem_cons_self ..)
    have hS' : ∀ p ∈ S', Ty.BelowFvars Φ p.2 := fun p hp => hS p (List.mem_cons_of_mem _ hp)
    intro τ hτ
    rw [show ((Z, U) :: S') = [(Z, U)] ++ S' from rfl, Subst.onTy_append]
    exact ih hS' (Ty.BelowFvars.substFvar hU hτ)

/-- Bridge to the `freeVars` characterisation: a below-`Φ` type's free vars are
    all `< Φ` (so any `w ≥ Φ` is fresh for it). -/
theorem Ty.BelowFvars.mem_lt {Φ : Nat} {τ : Ty} (h : Ty.BelowFvars Φ τ) :
    ∀ v ∈ τ.freeVars, v < Φ := by
  induction h with
  | prim => intro v hv; simp [Ty.freeVars] at hv
  | bvar => intro v hv; simp [Ty.freeVars] at hv
  | fvar hlt => intro v hv; simp only [Ty.freeVars, List.mem_singleton] at hv; exact hv ▸ hlt
  | arrow _ _ iha ihb =>
    intro v hv; simp only [Ty.freeVars, List.mem_dedup, List.mem_append] at hv
    rcases hv with h | h
    · exact iha v h
    · exact ihb v h
  | customTy hall ih =>
    intro v hv
    simp only [Ty.freeVars] at hv
    by_contra hge
    refine (TyList.not_mem_freeVars_iff.mpr ?_) hv
    intro t ht hc
    exact hge (ih t ht v hc)

/-- Every scheme body of `ctx` has its free type vars below the frontier `Φ`
    (the "new_tv" discipline: vars `Infer` allocates `≥ Φ` are genuinely fresh). -/
def CtxBelow (Φ : Nat) (ctx : Ctx) : Prop := ∀ M ∈ ctx.env, Ty.BelowFvars Φ M.body

/-- A whole substitution preserves context-below (with frontier growth). -/
theorem Subst.onCtx_below {Φ Φ' : Nat} {S : Subst} {ctx : Ctx}
    (hS : ∀ p ∈ S, Ty.BelowFvars Φ' p.2) (hle : Φ ≤ Φ') (hb : CtxBelow Φ ctx) :
    CtxBelow Φ' (S.onCtx ctx) := by
  intro M hM
  simp only [Subst.onCtx, Subst.onEnv] at hM
  obtain ⟨M0, hM0, rfl⟩ := List.mem_map.mp hM
  exact Subst.onTy_belowFvars hS ((hb M0 hM0).mono hle)

/-- Opening a below-`Φ` type with fresh names all `< Φ` stays below-`Φ`. -/
theorem Ty.openVars_belowFvars {Φ : Nat} {Xs : List Nat} {τ : Ty}
    (hτ : Ty.BelowFvars Φ τ) (hXs : ∀ x ∈ Xs, x < Φ) :
    Ty.BelowFvars Φ (Ty.openVars Xs τ) := by
  induction τ using Ty.rec_strong with
  | prim p => exact .prim
  | arrow a b iha ihb => cases hτ with | arrow ha hb => exact .arrow (iha ha) (ihb hb)
  | bvar i =>
    simp only [Ty.openVars, Ty.instantiate]
    cases h : Xs[i]? with
    | none => exact .bvar
    | some x => exact .fvar (hXs x (List.mem_of_getElem? h))
  | fvar n => cases hτ with | fvar hlt => exact .fvar hlt
  | customTy nm tys ih =>
    cases hτ with
    | customTy hall =>
      simp only [Ty.openVars_customTy]
      apply Ty.BelowFvars.customTy
      intro t' ht'
      obtain ⟨t, ht, rfl⟩ := List.mem_map.mp ht'
      exact ih t ht (hall t ht)

/-- A type with no free variables is below any frontier. -/
theorem Ty.BelowFvars.of_noFreeVars {Φ : Nat} {τ : Ty} (h : NoFreeVars τ) :
    Ty.BelowFvars Φ τ := by
  induction h with
  | prim => exact .prim
  | arrow _ _ iha ihb => exact .arrow iha ihb
  | bvar => exact .bvar
  | customTy _ ih => exact .customTy (fun t ht => ih t ht)

/-- Converse of `Ty.BelowFvars.mem_lt`: all free vars `< Φ` gives `BelowFvars Φ`. -/
theorem Ty.BelowFvars.of_freeVars_lt {Φ : Nat} {τ : Ty}
    (h : ∀ v ∈ τ.freeVars, v < Φ) : Ty.BelowFvars Φ τ := by
  induction τ using Ty.rec_strong with
  | prim p => exact .prim
  | bvar i => exact .bvar
  | fvar n => exact .fvar (h n (by simp [Ty.freeVars]))
  | arrow a b iha ihb =>
    refine .arrow (iha fun v hv => h v ?_) (ihb fun v hv => h v ?_)
    · simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inl hv
    · simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inr hv
  | customTy nm tys ih =>
    refine .customTy fun t ht => ih t ht (fun v hv => h v ?_)
    simp only [Ty.freeVars]
    exact TyList.mem_freeVars_of_mem ht hv

/-- Opening with below-`Φ` args preserves below-`Φ`-ness. -/
theorem Ty.openWith_belowFvars {Φ : Nat} {Vs : List Ty} {X : Ty}
    (hVs : ∀ v ∈ Vs, Ty.BelowFvars Φ v) (hX : Ty.BelowFvars Φ X) :
    Ty.BelowFvars Φ (Ty.openWith Vs X) := by
  induction X using Ty.rec_strong with
  | prim p => exact .prim
  | fvar n => cases hX with | fvar hlt => exact .fvar hlt
  | bvar i =>
    simp only [Ty.openWith, Ty.instantiate]
    cases h : Vs[i]? with
    | none => simp only [Option.getD_none]; exact .bvar
    | some v => simp only [Option.getD_some]; exact hVs v (List.mem_of_getElem? h)
  | arrow a b iha ihb => cases hX with | arrow ha hb => exact .arrow (iha ha) (ihb hb)
  | customTy nm tys ih =>
    cases hX with
    | customTy hall =>
      simp only [Ty.openWith, Ty.instantiate, TyList.instantiate_eq_map]
      exact .customTy (fun t' ht' => by
        obtain ⟨t, ht, rfl⟩ := List.mem_map.mp ht'; exact ih t ht (hall t ht))

/-- A `match_` branch's pattern bindings stay below `Φ` (ctor contents are closed,
    so opening with below-`Φ` type args yields below-`Φ` bindings). -/
theorem branchBindings_below {Φ : Nat} {ctorr : Ctor} {ta : List Ty} {ctx : Ctx}
    (hctx : CtxBelow Φ ctx) (hta : ∀ t ∈ ta, Ty.BelowFvars Φ t) :
    CtxBelow Φ { ctx with
      env := (ctorr.contents.map (Ty.openWith ta)).map PolyTy.mkTrivial ++ ctx.env } := by
  intro M hM
  rw [List.mem_append] at hM
  rcases hM with hM | hM
  · obtain ⟨t, ht, rfl⟩ := List.mem_map.mp hM
    obtain ⟨c, hc, rfl⟩ := List.mem_map.mp ht
    show Ty.BelowFvars Φ (Ty.openWith ta c)
    exact Ty.openWith_belowFvars hta (Ty.BelowFvars.of_noFreeVars (ctorr.closed c hc))
  · exact hctx M hM

mutual

/-- Unifying two below-`Φ` monotypes yields a below-`Φ` substitution. -/
theorem UnifyRel.belowFvars {Φ : Nat} : {a b : Ty} → {S : Subst} → UnifyRel a b S →
    Ty.BelowFvars Φ a → Ty.BelowFvars Φ b → ∀ p ∈ S, Ty.BelowFvars Φ p.2
  | _, _, _, .prim, _, _ => by simp
  | _, _, _, .fvarRefl, _, _ => by simp
  | _, _, _, .fvarL _ _, _, hb => by
    intro p hp; rw [List.mem_singleton] at hp; subst hp; exact hb
  | _, _, _, .fvarR _ _, ha, _ => by
    intro p hp; rw [List.mem_singleton] at hp; subst hp; exact ha
  | _, _, _, .arrow h₁ h₂, ha, hb => by
    cases ha with | arrow ha_a ha_b => cases hb with | arrow hb_c hb_d =>
    have h1lc := UnifyRel.belowFvars h₁ ha_a hb_c
    have h2lc := UnifyRel.belowFvars h₂ (Subst.onTy_belowFvars h1lc ha_b) (Subst.onTy_belowFvars h1lc hb_d)
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact h1lc p hp
    · exact h2lc p hp
  | _, _, _, .customTy hl, ha, hb => by
    cases ha with | customTy ha_all => cases hb with | customTy hb_all =>
    exact UnifyRelList.belowFvars hl ha_all hb_all

/-- List version: unifying two below-`Φ` type lists yields a below-`Φ` substitution. -/
theorem UnifyRelList.belowFvars {Φ : Nat} : {ts₁ ts₂ : List Ty} → {S : Subst} → UnifyRelList ts₁ ts₂ S →
    (∀ t ∈ ts₁, Ty.BelowFvars Φ t) → (∀ t ∈ ts₂, Ty.BelowFvars Φ t) → ∀ p ∈ S, Ty.BelowFvars Φ p.2
  | _, _, _, .nil, _, _ => by simp
  | _, _, _, @UnifyRelList.cons t₁ t₂ ts₁ ts₂ S₁ S₂ h₁ ht, hts₁, hts₂ => by
    have ht1 : Ty.BelowFvars Φ t₁ := hts₁ t₁ (List.mem_cons_self ..)
    have ht2 : Ty.BelowFvars Φ t₂ := hts₂ t₂ (List.mem_cons_self ..)
    have h1lc := UnifyRel.belowFvars h₁ ht1 ht2
    have hmap₁ : ∀ t ∈ ts₁.map S₁.onTy, Ty.BelowFvars Φ t := by
      intro t htm; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp htm
      exact Subst.onTy_belowFvars h1lc (hts₁ t0 (List.mem_cons_of_mem _ ht0))
    have hmap₂ : ∀ t ∈ ts₂.map S₁.onTy, Ty.BelowFvars Φ t := by
      intro t htm; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp htm
      exact Subst.onTy_belowFvars h1lc (hts₂ t0 (List.mem_cons_of_mem _ ht0))
    have h2lc := UnifyRelList.belowFvars ht hmap₁ hmap₂
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact h1lc p hp
    · exact h2lc p hp

end

/-- Closing over `gs` only removes free vars, so it preserves below-`Φ`. -/
theorem Ty.BelowFvars.closeOver {Φ : Nat} {gs : List Nat} :
    ∀ {τ : Ty}, Ty.BelowFvars Φ τ → Ty.BelowFvars Φ (Ty.closeOver gs τ) := by
  intro τ h
  induction τ using Ty.rec_strong with
  | prim p => exact .prim
  | bvar i => exact .bvar
  | fvar n =>
    rw [Ty.closeOver.eq_5]
    cases h_idx : gs.idxOf? n with
    | none => cases h with | fvar hlt => exact .fvar hlt
    | some i => exact .bvar
  | arrow a b iha ihb => cases h with | arrow ha hb => exact .arrow (iha ha) (ihb hb)
  | customTy nm tys ih =>
    cases h with
    | customTy hall =>
      simp only [Ty.closeOver, TyList.closeOver_eq_map]
      apply Ty.BelowFvars.customTy
      intro t' ht'; obtain ⟨t, ht, rfl⟩ := List.mem_map.mp ht'
      exact ih t ht (hall t ht)

/-! Regularity: any declaratively-typed term has a locally-closed type. (Each
    rule produces an LC type; `var`/`ctor` via `InstantiatesBy.preserves_bvars`
    on LC `tyArgs`, `lambda` via the LC param premise, the rest by induction.) -/
mutual
theorem TypeOfElabHM.regular : {ctx : Ctx} → {e : Expr} → {τ : Ty} →
    TypeOfElabHM ctx e τ → τ.IsLC
  | _, _, _, .primLitUnit => .prim
  | _, _, _, .primLitInt => .prim
  | _, _, _, .primLitNat => .prim
  -- | _, _, _, .primLitBool => .prim
  | _, _, _, .primLitStr => .prim
  | _, _, _, .lambda hpc _ _ hbody => .arrow hpc (TypeOfElabHM.regular hbody)
  | _, _, _, .app hf _ => by
    have := TypeOfElabHM.regular hf; cases this with | arrow _ hret => exact hret
  | _, _, _, .letIn _ _ _ _ hbody => TypeOfElabHM.regular hbody
  | _, _, _, .var _ htyargs _ hinst => InstantiatesBy.preserves_bvars htyargs hinst
  | _, _, _, .ctor _ htyargs hinst => InstantiatesBy.preserves_bvars htyargs hinst
  | _, _, _, @TypeOfElabHM.match_ _ _ _ branches _ hscrut hne hbrs => by
    obtain ⟨hd, tl, rfl⟩ := List.exists_cons_of_ne_nil hne
    exact TypeOfElabMatchBranch.regular (hbrs hd (List.mem_cons_self ..))
  | _, _, _, .letRec _ _ _ _ _ _ _ hbody => TypeOfElabHM.regular hbody
  | _, _, _, .letRecAnn _ _ _ _ hbody => TypeOfElabHM.regular hbody

theorem TypeOfElabMatchBranch.regular : {ctx : Ctx} → {br : MatchPattern × Expr} →
    {scrutTy : Ty} → {rt : Ty} →
    TypeOfElabMatchBranch ctx br scrutTy rt → rt.IsLC
  | _, _, _, _, .mk _ _ _ _ _ _ _ hbody => TypeOfElabHM.regular hbody
  | _, _, _, _, .wildcard hbody => TypeOfElabHM.regular hbody
end

/-- A `letRecAnn` scheme's body free vars are among the scheme list's (InferW-local
    copy of the Core-private `Expr.mem_schemeList_tyFreeVars`; lets the `letRecAnn`
    arms turn the scheme-list free-var bound into per-scheme bounds). -/
theorem Expr.scheme_body_mem_schemeList_tyFreeVars {σ : PolyTy} {y : Nat}
    {ss : List PolyTy} (hmem : σ ∈ ss) (hy : y ∈ σ.body.freeVars) :
    y ∈ Expr.tyFreeVars.SchemeList.tyFreeVars ss := by
  induction ss with
  | nil => exact absurd hmem List.not_mem_nil
  | cons hd tl ih =>
    simp only [Expr.tyFreeVars.SchemeList.tyFreeVars, List.mem_append]
    rcases List.mem_cons.mp hmem with h | h
    · subst h; exact .inl hy
    · exact .inr (ih h)

/-! Frontier invariant (`Infer.belowFvars`): from a context whose schemes are below
    the input frontier `Φ`, `Infer` yields a type and a substitution whose
    replacements are all below the *output* frontier `Φ'` (so `mono` everything up
    to `Φ'`). (Named `belowFvars` rather than `below`, since `Infer.below` is
    reserved by Lean's auto-generated recursor for the `Infer` inductive.) -/
mutual
theorem Infer.belowFvars {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ) :
    CtxBelow Φ ctx → (∀ y ∈ e.tyFreeVars, y < Φ) →
    Ty.BelowFvars Φ' τ ∧ (∀ p ∈ S, Ty.BelowFvars Φ' p.2) := by
  cases h with
  | primLitUnit => intro _ _; exact ⟨.prim, by simp⟩
  | primLitInt => intro _ _; exact ⟨.prim, by simp⟩
  | primLitNat => intro _ _; exact ⟨.prim, by simp⟩
  -- | primLitBool => intro _ _; exact ⟨.prim, by simp⟩
  | primLitStr => intro _ _; exact ⟨.prim, by simp⟩
  | lambda hseed hbody =>
    intro hctx htfv
    cases hseed with
    | none =>
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append] at htfv
      obtain ⟨hb_τ, hb_s⟩ := Infer.belowFvars hbody (by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact .fvar (by omega)
        · exact (hctx M hM).mono (by omega))
        (fun y hy => by have := htfv y hy; omega)
      have hfl := Infer.frontier_le hbody
      exact ⟨.arrow (Subst.onTy_belowFvars hb_s (.fvar (by omega))) hb_τ, hb_s⟩
    | some _ hcl =>
      expose_names
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at htfv
      have hparamTy : Ty.BelowFvars Φ paramTy :=
        Ty.BelowFvars.of_freeVars_lt (fun v hv => htfv v (.inl hv))
      obtain ⟨hb_τ, hb_s⟩ := Infer.belowFvars hbody (by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact hparamTy
        · exact hctx M hM)
        (fun y hy => htfv y (.inr hy))
      exact ⟨.arrow (Subst.onTy_belowFvars hb_s (hparamTy.mono (Infer.frontier_le hbody))) hb_τ, hb_s⟩
  | app hf harg huni =>
    intro hctx htfv
    simp only [Expr.tyFreeVars, List.mem_append] at htfv
    obtain ⟨hf_τ, hf_s⟩ := Infer.belowFvars hf hctx (fun y hy => htfv y (.inl hy))
    have hctx1 := Subst.onCtx_below hf_s (Infer.frontier_le hf) hctx
    obtain ⟨harg_τ, harg_s⟩ := Infer.belowFvars harg hctx1
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) (Infer.frontier_le hf))
    have h1 := Infer.frontier_le hf
    have h2 := Infer.frontier_le harg
    refine ⟨Subst.onTy_belowFvars
        (UnifyRel.belowFvars huni
          (Subst.onTy_belowFvars (fun p hp => (harg_s p hp).mono (by omega)) (hf_τ.mono (by omega)))
          (.arrow (harg_τ.mono (by omega)) (.fvar (by omega))))
        (.fvar (by omega)), ?_⟩
    intro p hp; rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · exact (hf_s p hp).mono (by omega)
    · exact (harg_s p hp).mono (by omega)
    · exact UnifyRel.belowFvars huni
        (Subst.onTy_belowFvars (fun p hp => (harg_s p hp).mono (by omega)) (hf_τ.mono (by omega)))
        (.arrow (harg_τ.mono (by omega)) (.fvar (by omega))) p hp
  | var hlook =>
    intro hctx _
    refine ⟨?_, by simp⟩
    exact Ty.openVars_belowFvars ((hctx _ (List.mem_of_getElem? hlook)).mono (by omega))
      (fun x hx => by have := freshVars_lt x hx; omega)
  | ctor hlook =>
    intro _ _
    refine ⟨?_, by simp⟩
    refine Ty.openVars_belowFvars
      (Ty.BelowFvars.of_noFreeVars (Ctor.toTy_body_noFreeVars _))
      (fun x hx => by have := freshVars_lt x hx; omega)
  | letIn hrhs hbody =>
    intro hctx htfv
    simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append] at htfv
    obtain ⟨hr_τ, hr_s⟩ := Infer.belowFvars hrhs hctx (fun y hy => htfv y (.inl hy))
    have hctx1 := Subst.onCtx_below hr_s (Infer.frontier_le hrhs) hctx
    obtain ⟨hb_τ, hb_s⟩ := Infer.belowFvars hbody (by
      intro M hM
      rcases List.mem_cons.mp hM with rfl | hM
      · exact hr_τ.closeOver
      · exact hctx1 M hM)
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) (Infer.frontier_le hrhs))
    refine ⟨hb_τ, ?_⟩
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact (hr_s p hp).mono (Infer.frontier_le hbody)
    · exact hb_s p hp
  | letInAnn hσwf hΦN hrhs huni _hesc1 _hesc2 hbody =>
    intro hctx htfv
    expose_names
    simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at htfv
    have hrle := Infer.frontier_le hrhs
    have hctx_pc : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hctx M hM).mono (by omega)
    have hσbody : Ty.BelowFvars Φ σ.body :=
      Ty.BelowFvars.of_freeVars_lt (fun v hv => htfv v (.inl (.inl hv)))
    obtain ⟨hr_τ, hr_s⟩ := Infer.belowFvars hrhs hctx_pc (fun y hy => by
      rcases Expr.tyFreeVars_openTyVars hy with h | h
      · have := htfv y (.inl (.inr h)); omega
      · have := freshVars_lt y h; omega)
    have hσopen : Ty.BelowFvars Φ₁
        (σ.openVars (freshVars N σ.paramCount)) :=
      Ty.openVars_belowFvars (hσbody.mono (by omega))
        (fun x hx => by have := freshVars_lt x hx; omega)
    have hSchk : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 :=
      UnifyRel.belowFvars huni hr_τ hσopen
    have hctx1 : CtxBelow Φ₁ (Schk.onCtx (S₁.onCtx ctx)) :=
      Subst.onCtx_below hSchk (le_refl _) (Subst.onCtx_below hr_s hrle hctx_pc)
    obtain ⟨hb_τ, hb_s⟩ := Infer.belowFvars hbody (by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact hσbody.mono (by omega)
      · exact hctx1 M hM)
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) (by omega))
    have hble := Infer.frontier_le hbody
    refine ⟨hb_τ, ?_⟩
    intro p hp; rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · exact (hr_s p hp).mono (by omega)
    · exact (hSchk p hp).mono (by omega)
    · exact hb_s p hp
  | match_ hscrut hne hbr =>
    intro hctx htfv
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at htfv
    obtain ⟨hτs, hS₁⟩ := Infer.belowFvars hscrut hctx (fun y hy => htfv y (.inl hy))
    have hle1 := Infer.frontier_le hscrut
    have hbrctx : CtxBelow (Φ₁ + 1) (S₁.onCtx ctx) :=
      Subst.onCtx_below (fun p hp => (hS₁ p hp).mono (by omega)) (by omega) hctx
    obtain ⟨hρ_below, hS₂⟩ := InferBranches.belowFvars hbr
      hbrctx
      (hτs.mono (by omega))
      (.fvar (by omega))
      (fun y hy => by have := htfv y (.inr hy); omega)
    refine ⟨hρ_below, ?_⟩
    have hbrle := InferBranches.frontier_le hbr
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact (hS₁ p hp).mono (by omega)
    · exact hS₂ p hp
  | letRec hgroup hbody =>
    intro hctx htfv
    simp only [Expr.tyFreeVars, List.mem_append] at htfv
    have hgle := InferRecGroup.frontier_le hgroup
    have hS₁ := InferRecGroup.belowFvars hgroup
      (by
        intro M hM; rcases List.mem_append.mp hM with hM | hM
        · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
          exact .fvar (by have := freshVars_lt x hx; omega)
        · exact (hctx M hM).mono (by omega))
      (by
        intro β hβ; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hβ
        exact .fvar (by have := freshVars_lt x hx; omega))
      (fun y hy => lt_of_lt_of_le (htfv y (.inl hy)) (by omega))
    obtain ⟨hb_τ, hb_s⟩ := Infer.belowFvars hbody
      (by
        intro M hM; rcases List.mem_append.mp hM with hM | hM
        · simp only [genGroupSchemes] at hM
          obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
          obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
          simp only [PolyTy.genGroup]
          exact (Subst.onTy_belowFvars hS₁ (.fvar (by have := freshVars_lt x hx; omega))).closeOver
        · exact (Subst.onCtx_below hS₁ (by omega) hctx) M hM)
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) (by omega))
    have hble := Infer.frontier_le hbody
    refine ⟨hb_τ, ?_⟩
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact (hS₁ p hp).mono hble
    · exact hb_s p hp
  | letRecAnn hwf hlen hgroup hbody =>
    intro hctx htfv
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at htfv
    have hSch : ∀ σ ∈ schemes, Ty.BelowFvars Φ σ.body := fun σ hσ =>
      Ty.BelowFvars.of_freeVars_lt (fun v hv =>
        htfv v (.inl (.inl (Expr.scheme_body_mem_schemeList_tyFreeVars hσ hv))))
    have hctxσ : CtxBelow Φ { ctx with env := schemes ++ ctx.env } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hSch M hM
      · exact hctx M hM
    have hgle := InferRecGroupAnn.frontier_le hgroup
    have hS₁ := InferRecGroupAnn.belowFvars hgroup hctxσ hSch
      (fun y hy => htfv y (.inl (.inr hy)))
    obtain ⟨hb_τ, hb_s⟩ := Infer.belowFvars hbody
      (Subst.onCtx_below hS₁ hgle hctxσ)
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) hgle)
    have hble := Infer.frontier_le hbody
    refine ⟨hb_τ, ?_⟩
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact (hS₁ p hp).mono hble
    · exact hb_s p hp
termination_by e.size
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.size, Expr.size_openTyVars]; omega)
theorem InferBranches.belowFvars {Φ ctx scrutTy ρ brs Φ' S brsOut}
    (h : InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut)
    (hctx : CtxBelow Φ ctx) (hscrutTy : Ty.BelowFvars Φ scrutTy) (hρ : Ty.BelowFvars Φ ρ)
    (htfv : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars brs, y < Φ) :
    Ty.BelowFvars Φ' (S.onTy ρ) ∧ (∀ p ∈ S, Ty.BelowFvars Φ' p.2) := by
  cases h with
  | nil => exact ⟨by simpa using hρ, by simp⟩
  | cons hlook hn huni0 hbody huni hrest =>
    expose_names
    simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append] at htfv
    have hS₀ : ∀ p ∈ S₀, Ty.BelowFvars (Φ + ctor.paramCount) p.2 :=
      UnifyRel.belowFvars huni0 (hscrutTy.mono (by omega))
        (.customTy (fun t ht => by
          obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
          exact .fvar (by have := freshVars_lt x hx; omega)))
    obtain ⟨hτb, hS₁⟩ := Infer.belowFvars hbody
      (branchBindings_below (ctorr := ctor)
        (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
        (Subst.onCtx_below hS₀ (by omega) hctx)
        (fun t ht => by
          obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
          obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv
          exact Subst.onTy_belowFvars hS₀ (.fvar (by have := freshVars_lt x hx; omega))))
      (fun y hy => by have := htfv y (.inl hy); omega)
    have hle0 : Φ + ctor.paramCount ≤ Φ₁ := Infer.frontier_le hbody
    have hS₀ρ : Ty.BelowFvars Φ₁ (S₀.onTy ρ) :=
      (Subst.onTy_belowFvars hS₀ (hρ.mono (by omega))).mono hle0
    have hS₀scrut : Ty.BelowFvars Φ₁ (S₀.onTy scrutTy) :=
      (Subst.onTy_belowFvars hS₀ (hscrutTy.mono (by omega))).mono hle0
    have hS₁ρ := Subst.onTy_belowFvars hS₁ hS₀ρ
    have hS₂ := UnifyRel.belowFvars huni hτb hS₁ρ
    have hctx1 : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx))) :=
      Subst.onCtx_below hS₂ (le_refl _)
        (Subst.onCtx_below hS₁ (le_refl _)
          (Subst.onCtx_below (fun p hp => (hS₀ p hp).mono hle0) (by omega) hctx))
    obtain ⟨hres, hS₃⟩ := InferBranches.belowFvars hrest hctx1
      (Subst.onTy_belowFvars hS₂ (Subst.onTy_belowFvars hS₁ hS₀scrut))
      (Subst.onTy_belowFvars hS₂ (Subst.onTy_belowFvars hS₁ hS₀ρ))
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) (by omega))
    have hbrle := InferBranches.frontier_le hrest
    refine ⟨?_, ?_⟩
    · rw [Subst.onTy_append, Subst.onTy_append, Subst.onTy_append]; exact hres
    · intro p hp; rw [List.mem_append, List.mem_append, List.mem_append] at hp
      rcases hp with ((hp | hp) | hp) | hp
      · exact (hS₀ p hp).mono (by omega)
      · exact (hS₁ p hp).mono (by omega)
      · exact (hS₂ p hp).mono (by omega)
      · exact hS₃ p hp
  | consWild hbody huni hrest =>
    simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append] at htfv
    obtain ⟨hτb, hS₁⟩ := Infer.belowFvars hbody hctx
      (fun y hy => htfv y (.inl hy))
    have hle1 := Infer.frontier_le hbody
    have hS₁ρ := Subst.onTy_belowFvars hS₁ (hρ.mono hle1)
    have hS₂ := UnifyRel.belowFvars huni hτb hS₁ρ
    have hctx1 := Subst.onCtx_below hS₂ (le_refl _) (Subst.onCtx_below hS₁ hle1 hctx)
    obtain ⟨hres, hS₃⟩ := InferBranches.belowFvars hrest hctx1
      (Subst.onTy_belowFvars hS₂ (Subst.onTy_belowFvars hS₁ (hscrutTy.mono hle1)))
      (Subst.onTy_belowFvars hS₂ (Subst.onTy_belowFvars hS₁ (hρ.mono hle1)))
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) hle1)
    have hbrle := InferBranches.frontier_le hrest
    refine ⟨?_, ?_⟩
    · rw [Subst.onTy_append, Subst.onTy_append]; exact hres
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact (hS₁ p hp).mono (by omega)
      · exact (hS₂ p hp).mono (by omega)
      · exact hS₃ p hp
termination_by Expr.sizeBranches brs
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)
theorem InferRecGroup.belowFvars {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut)
    (hctx : CtxBelow Φ ctx) (htargets : ∀ β ∈ targets, Ty.BelowFvars Φ β)
    (htfv : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ) :
    (∀ p ∈ S, Ty.BelowFvars Φ' p.2) := by
  cases h with
  | nil => simp
  | cons he huni hrest =>
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at htfv
    obtain ⟨hτ, hS₁⟩ := Infer.belowFvars he hctx (fun y hy => htfv y (.inl hy))
    have hle1 := Infer.frontier_le he
    have hS₂ := UnifyRel.belowFvars huni hτ
      (Subst.onTy_belowFvars hS₁ ((htargets _ (List.mem_cons_self ..)).mono hle1))
    have hbrle := InferRecGroup.frontier_le hrest
    have hS₃ := InferRecGroup.belowFvars hrest
      (Subst.onCtx_below hS₂ (le_refl _) (Subst.onCtx_below hS₁ hle1 hctx))
      (by
        intro β' hβ'
        obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hβ'
        exact Subst.onTy_belowFvars hS₂
          (Subst.onTy_belowFvars hS₁ ((htargets b (List.mem_cons_of_mem _ hb)).mono hle1)))
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) hle1)
    intro p hp; rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · exact (hS₁ p hp).mono hbrle
    · exact (hS₂ p hp).mono hbrle
    · exact hS₃ p hp
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)
theorem InferRecGroupAnn.belowFvars {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut)
    (hctx : CtxBelow Φ ctx) (hsch : ∀ σ ∈ schemes, Ty.BelowFvars Φ σ.body)
    (htfv : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ) :
    (∀ p ∈ S, Ty.BelowFvars Φ' p.2) := by
  cases h with
  | nil => simp
  | cons hΦN hinfer huni _ _ hrest =>
    expose_names
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at htfv
    have hσbody : Ty.BelowFvars Φ σ.body := hsch σ List.mem_cons_self
    have hrle := Infer.frontier_le hinfer
    have hΦΦ₁ : Φ ≤ Φ₁ := by omega
    have hctx_pc : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hctx M hM).mono (by omega)
    obtain ⟨hr_τ, hr_s⟩ := Infer.belowFvars hinfer hctx_pc (fun y hy => by
      rcases Expr.tyFreeVars_openTyVars hy with hh | hh
      · have := htfv y (.inl hh); omega
      · have := freshVars_lt y hh; omega)
    have hσopen : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
      Ty.openVars_belowFvars (hσbody.mono (by omega))
        (fun x hx => by have := freshVars_lt x hx; omega)
    have hSchk : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hr_τ hσopen
    have hctx1 : CtxBelow Φ₁ (Schk.onCtx (S₁.onCtx ctx)) :=
      Subst.onCtx_below hSchk (le_refl _) (Subst.onCtx_below hr_s hrle hctx_pc)
    have hbrle := InferRecGroupAnn.frontier_le hrest
    have hS₂ := InferRecGroupAnn.belowFvars hrest hctx1
      (fun σ' hσ' => (hsch σ' (List.mem_cons_of_mem _ hσ')).mono hΦΦ₁)
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) hΦΦ₁)
    intro p hp; rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · exact (hr_s p hp).mono hbrle
    · exact (hSchk p hp).mono hbrle
    · exact hS₂ p hp
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end


/-! ### M2: substitution domain bound + elaborated-output free-var locality

`Infer.dom_below` extends the frontier discipline to the substitution **domain**
(`∀ p ∈ S, p.1 < Φ'`, via `UnifyRel.dom_mem` + `Infer.belowFvars`).
`Infer.eOut_avoid` is the *locality* of the elaborated output (avoid form): a
variable that is below the input frontier and avoids both the context env and the
skeleton's annotation free vars cannot appear in `eOut`/`τ`/the substitution
range. Together with idempotency (`Infer.eliminates`, M3) these give the prefix-fix
corollary (M4) that the honest soundness needs. -/

mutual
theorem Infer.dom_below {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ) :
    CtxBelow Φ ctx → (∀ y ∈ e.tyFreeVars, y < Φ) → (∀ p ∈ S, p.1 < Φ') := by
  cases h with
  | primLitUnit => intro _ _; simp
  | primLitInt => intro _ _; simp
  | primLitNat => intro _ _; simp
  | primLitStr => intro _ _; simp
  | lambda hseed hbody =>
    intro hctx htfv
    cases hseed with
    | none =>
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append] at htfv
      exact Infer.dom_below hbody (by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact .fvar (by omega)
        · exact (hctx M hM).mono (by omega))
        (fun y hy => by have := htfv y hy; omega)
    | some _ hcl =>
      expose_names
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at htfv
      have hparamTy : Ty.BelowFvars Φ paramTy :=
        Ty.BelowFvars.of_freeVars_lt (fun v hv => htfv v (.inl hv))
      exact Infer.dom_below hbody (by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact hparamTy
        · exact hctx M hM)
        (fun y hy => htfv y (.inr hy))
  | app hf harg huni =>
    intro hctx htfv
    simp only [Expr.tyFreeVars, List.mem_append] at htfv
    have hfle := Infer.frontier_le hf
    have hargle := Infer.frontier_le harg
    obtain ⟨hf_τ, hf_s⟩ := Infer.belowFvars hf hctx (fun y hy => htfv y (.inl hy))
    have hf_dom := Infer.dom_below hf hctx (fun y hy => htfv y (.inl hy))
    have hctx1 := Subst.onCtx_below hf_s hfle hctx
    obtain ⟨harg_τ, harg_s⟩ := Infer.belowFvars harg hctx1
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) hfle)
    have harg_dom := Infer.dom_below harg hctx1
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) hfle)
    intro p hp
    rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · have := hf_dom p hp; omega
    · have := harg_dom p hp; omega
    · rcases UnifyRel.dom_mem huni p hp with h | h
      · have hb := Subst.onTy_belowFvars harg_s (hf_τ.mono hargle)
        have := hb.mem_lt p.1 h; omega
      · simp only [Ty.freeVars, List.mem_dedup, List.mem_append, List.mem_singleton] at h
        rcases h with h | h
        · have := harg_τ.mem_lt p.1 h; omega
        · omega
  | var => intro _ _; simp
  | ctor => intro _ _; simp
  | letIn hrhs hbody =>
    intro hctx htfv
    simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append] at htfv
    have hrle := Infer.frontier_le hrhs
    obtain ⟨hr_τ, hr_s⟩ := Infer.belowFvars hrhs hctx (fun y hy => htfv y (.inl hy))
    have hr_dom := Infer.dom_below hrhs hctx (fun y hy => htfv y (.inl hy))
    have hctx1 := Subst.onCtx_below hr_s hrle hctx
    have hb_dom := Infer.dom_below hbody (by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact hr_τ.closeOver
      · exact hctx1 M hM)
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) hrle)
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · have := hr_dom p hp; have := Infer.frontier_le hbody; omega
    · exact hb_dom p hp
  | letInAnn hσwf hΦN hrhs huni _hesc1 _hesc2 hbody =>
    intro hctx htfv
    expose_names
    simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at htfv
    have hrle := Infer.frontier_le hrhs
    have hctx_pc : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hctx M hM).mono (by omega)
    have hσbody : Ty.BelowFvars Φ σ.body :=
      Ty.BelowFvars.of_freeVars_lt (fun v hv => htfv v (.inl (.inl hv)))
    obtain ⟨hr_τ, hr_s⟩ := Infer.belowFvars hrhs hctx_pc (fun y hy => by
      rcases Expr.tyFreeVars_openTyVars hy with h | h
      · have := htfv y (.inl (.inr h)); omega
      · have := freshVars_lt y h; omega)
    have hr_dom := Infer.dom_below hrhs hctx_pc (fun y hy => by
      rcases Expr.tyFreeVars_openTyVars hy with h | h
      · have := htfv y (.inl (.inr h)); omega
      · have := freshVars_lt y h; omega)
    have hσopen : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
      Ty.openVars_belowFvars (hσbody.mono (by omega))
        (fun x hx => by have := freshVars_lt x hx; omega)
    have hSchk : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hr_τ hσopen
    have hctx1 : CtxBelow Φ₁ (Schk.onCtx (S₁.onCtx ctx)) :=
      Subst.onCtx_below hSchk (le_refl _) (Subst.onCtx_below hr_s hrle hctx_pc)
    have hb_dom := Infer.dom_below hbody (by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact hσbody.mono (by omega)
      · exact hctx1 M hM)
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) (by omega))
    have hble := Infer.frontier_le hbody
    intro p hp; rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · have := hr_dom p hp; omega
    · rcases UnifyRel.dom_mem huni p hp with h | h
      · have := hr_τ.mem_lt p.1 h; omega
      · have := hσopen.mem_lt p.1 h; omega
    · exact hb_dom p hp
  | match_ hscrut hne hbr =>
    intro hctx htfv
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at htfv
    have hle1 := Infer.frontier_le hscrut
    obtain ⟨hτs, hS₁⟩ := Infer.belowFvars hscrut hctx (fun y hy => htfv y (.inl hy))
    have hsc_dom := Infer.dom_below hscrut hctx (fun y hy => htfv y (.inl hy))
    have hbrctx : CtxBelow (Φ₁ + 1) (S₁.onCtx ctx) :=
      Subst.onCtx_below (fun p hp => (hS₁ p hp).mono (by omega)) (by omega) hctx
    have hbr_dom := InferBranches.dom_below hbr hbrctx (hτs.mono (by omega)) (.fvar (by omega))
      (fun y hy => by have := htfv y (.inr hy); omega)
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · have := hsc_dom p hp; have := InferBranches.frontier_le hbr; omega
    · exact hbr_dom p hp
  | letRec hgroup hbody =>
    intro hctx htfv
    simp only [Expr.tyFreeVars, List.mem_append] at htfv
    have hgle := InferRecGroup.frontier_le hgroup
    have hg_dom := InferRecGroup.dom_below hgroup
      (by intro M hM; rcases List.mem_append.mp hM with hM | hM
          · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
            exact .fvar (by have := freshVars_lt x hx; omega)
          · exact (hctx M hM).mono (by omega))
      (by intro β hβ; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hβ
          exact .fvar (by have := freshVars_lt x hx; omega))
      (fun y hy => lt_of_lt_of_le (htfv y (.inl hy)) (by omega))
    have hS₁ := InferRecGroup.belowFvars hgroup
      (by intro M hM; rcases List.mem_append.mp hM with hM | hM
          · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
            exact .fvar (by have := freshVars_lt x hx; omega)
          · exact (hctx M hM).mono (by omega))
      (by intro β hβ; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hβ
          exact .fvar (by have := freshVars_lt x hx; omega))
      (fun y hy => lt_of_lt_of_le (htfv y (.inl hy)) (by omega))
    have hb_dom := Infer.dom_below hbody (by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · simp only [genGroupSchemes] at hM
        obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
        obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
        simp only [PolyTy.genGroup]
        exact (Subst.onTy_belowFvars hS₁ (.fvar (by have := freshVars_lt x hx; omega))).closeOver
      · exact (Subst.onCtx_below hS₁ (by omega) hctx) M hM)
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) (by omega))
    have hble := Infer.frontier_le hbody
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · have := hg_dom p hp; omega
    · exact hb_dom p hp
  | letRecAnn hwf hlen hgroup hbody =>
    intro hctx htfv
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at htfv
    have hSch : ∀ σ ∈ schemes, Ty.BelowFvars Φ σ.body := fun σ hσ =>
      Ty.BelowFvars.of_freeVars_lt (fun v hv =>
        htfv v (.inl (.inl (Expr.scheme_body_mem_schemeList_tyFreeVars hσ hv))))
    have hctxσ : CtxBelow Φ { ctx with env := schemes ++ ctx.env } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hSch M hM
      · exact hctx M hM
    have hgle := InferRecGroupAnn.frontier_le hgroup
    have hg_dom := InferRecGroupAnn.dom_below hgroup hctxσ hSch
      (fun y hy => htfv y (.inl (.inr hy)))
    have hS₁bel := InferRecGroupAnn.belowFvars hgroup hctxσ hSch
      (fun y hy => htfv y (.inl (.inr hy)))
    have hb_dom := Infer.dom_below hbody
      (Subst.onCtx_below hS₁bel hgle hctxσ)
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) hgle)
    have hble := Infer.frontier_le hbody
    intro p hp; rw [List.mem_append] at hp
    rcases hp with hp | hp
    · have := hg_dom p hp; omega
    · exact hb_dom p hp
termination_by e.size
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.size, Expr.size_openTyVars]; omega)
theorem InferBranches.dom_below {Φ ctx scrutTy ρ brs Φ' S brsOut}
    (h : InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut)
    (hctx : CtxBelow Φ ctx) (hscrutTy : Ty.BelowFvars Φ scrutTy) (hρ : Ty.BelowFvars Φ ρ)
    (htfv : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars brs, y < Φ) :
    (∀ p ∈ S, p.1 < Φ') := by
  cases h with
  | nil => simp
  | cons hlook hn huni0 hbody huni hrest =>
    expose_names
    simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append] at htfv
    have hS₀ : ∀ p ∈ S₀, Ty.BelowFvars (Φ + ctor.paramCount) p.2 :=
      UnifyRel.belowFvars huni0 (hscrutTy.mono (by omega))
        (.customTy (fun t ht => by
          obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
          exact .fvar (by have := freshVars_lt x hx; omega)))
    have hS₀dom : ∀ p ∈ S₀, p.1 < Φ + ctor.paramCount := by
      intro p hp
      rcases UnifyRel.dom_mem huni0 p hp with h | h
      · have := (hscrutTy.mono (show Φ ≤ Φ + ctor.paramCount by omega)).mem_lt p.1 h; omega
      · simp only [Ty.freeVars] at h
        rw [mem_TyList_freeVars] at h
        obtain ⟨t, ht, hgt⟩ := h
        obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
        simp only [Ty.freeVars, List.mem_singleton] at hgt
        have := freshVars_lt x hx; omega
    have hbctx := branchBindings_below (ctorr := ctor)
        (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
        (Subst.onCtx_below hS₀ (by omega) hctx)
        (fun t ht => by
          obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
          obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv
          exact Subst.onTy_belowFvars hS₀ (.fvar (by have := freshVars_lt x hx; omega)))
    obtain ⟨hτb, hS₁⟩ := Infer.belowFvars hbody hbctx (fun y hy => by have := htfv y (.inl hy); omega)
    have hS₁dom := Infer.dom_below hbody hbctx (fun y hy => by have := htfv y (.inl hy); omega)
    have hle0 : Φ + ctor.paramCount ≤ Φ₁ := Infer.frontier_le hbody
    have hS₀ρ : Ty.BelowFvars Φ₁ (S₀.onTy ρ) :=
      (Subst.onTy_belowFvars hS₀ (hρ.mono (by omega))).mono hle0
    have hS₀scrut : Ty.BelowFvars Φ₁ (S₀.onTy scrutTy) :=
      (Subst.onTy_belowFvars hS₀ (hscrutTy.mono (by omega))).mono hle0
    have hS₁ρ := Subst.onTy_belowFvars hS₁ hS₀ρ
    have hS₂ := UnifyRel.belowFvars huni hτb hS₁ρ
    have hS₂dom : ∀ p ∈ S₂, p.1 < Φ₁ := by
      intro p hp
      rcases UnifyRel.dom_mem huni p hp with h | h
      · have := hτb.mem_lt p.1 h; omega
      · have := hS₁ρ.mem_lt p.1 h; omega
    have hctx1 : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx))) :=
      Subst.onCtx_below hS₂ (le_refl _)
        (Subst.onCtx_below hS₁ (le_refl _)
          (Subst.onCtx_below (fun p hp => (hS₀ p hp).mono hle0) (by omega) hctx))
    have hrest_dom := InferBranches.dom_below hrest hctx1
      (Subst.onTy_belowFvars hS₂ (Subst.onTy_belowFvars hS₁ hS₀scrut))
      (Subst.onTy_belowFvars hS₂ (Subst.onTy_belowFvars hS₁ hS₀ρ))
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) (by omega))
    have hbrle := InferBranches.frontier_le hrest
    intro p hp; rw [List.mem_append, List.mem_append, List.mem_append] at hp
    rcases hp with ((hp | hp) | hp) | hp
    · have := hS₀dom p hp; omega
    · have := hS₁dom p hp; omega
    · have := hS₂dom p hp; omega
    · exact hrest_dom p hp
  | consWild hbody huni hrest =>
    expose_names
    simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append] at htfv
    have hle1 := Infer.frontier_le hbody
    obtain ⟨hτb, hS₁⟩ := Infer.belowFvars hbody hctx (fun y hy => htfv y (.inl hy))
    have hS₁dom := Infer.dom_below hbody hctx (fun y hy => htfv y (.inl hy))
    have hS₁ρ := Subst.onTy_belowFvars hS₁ (hρ.mono hle1)
    have hS₂ := UnifyRel.belowFvars huni hτb hS₁ρ
    have hS₂dom : ∀ p ∈ S₂, p.1 < Φ₁ := by
      intro p hp
      rcases UnifyRel.dom_mem huni p hp with h | h
      · have := hτb.mem_lt p.1 h; omega
      · have := hS₁ρ.mem_lt p.1 h; omega
    have hctx1 := Subst.onCtx_below hS₂ (le_refl _) (Subst.onCtx_below hS₁ hle1 hctx)
    have hrest_dom := InferBranches.dom_below hrest hctx1
      (Subst.onTy_belowFvars hS₂ (Subst.onTy_belowFvars hS₁ (hscrutTy.mono hle1)))
      (Subst.onTy_belowFvars hS₂ (Subst.onTy_belowFvars hS₁ (hρ.mono hle1)))
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) hle1)
    have hbrle := InferBranches.frontier_le hrest
    intro p hp; rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · have := hS₁dom p hp; omega
    · have := hS₂dom p hp; omega
    · exact hrest_dom p hp
termination_by Expr.sizeBranches brs
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)
theorem InferRecGroup.dom_below {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut)
    (hctx : CtxBelow Φ ctx) (htargets : ∀ β ∈ targets, Ty.BelowFvars Φ β)
    (htfv : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ) :
    (∀ p ∈ S, p.1 < Φ') := by
  cases h with
  | nil => simp
  | cons he huni hrest =>
    expose_names
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at htfv
    have hle1 := Infer.frontier_le he
    obtain ⟨hτ, hS₁⟩ := Infer.belowFvars he hctx (fun y hy => htfv y (.inl hy))
    have hS₁dom := Infer.dom_below he hctx (fun y hy => htfv y (.inl hy))
    have hS₁β := Subst.onTy_belowFvars hS₁ ((htargets _ (List.mem_cons_self ..)).mono hle1)
    have hS₂ := UnifyRel.belowFvars huni hτ hS₁β
    have hS₂dom : ∀ p ∈ S₂, p.1 < Φ₁ := by
      intro p hp
      rcases UnifyRel.dom_mem huni p hp with h | h
      · have := hτ.mem_lt p.1 h; omega
      · have := hS₁β.mem_lt p.1 h; omega
    have hctx1 := Subst.onCtx_below hS₂ (le_refl _) (Subst.onCtx_below hS₁ hle1 hctx)
    have hrest_dom := InferRecGroup.dom_below hrest hctx1
      (by intro β' hβ'; obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hβ'
          exact Subst.onTy_belowFvars hS₂
            (Subst.onTy_belowFvars hS₁ ((htargets b (List.mem_cons_of_mem _ hb)).mono hle1)))
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) hle1)
    have hbrle := InferRecGroup.frontier_le hrest
    intro p hp; rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · have := hS₁dom p hp; omega
    · have := hS₂dom p hp; omega
    · exact hrest_dom p hp
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)
theorem InferRecGroupAnn.dom_below {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut)
    (hctx : CtxBelow Φ ctx) (hsch : ∀ σ ∈ schemes, Ty.BelowFvars Φ σ.body)
    (htfv : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ) :
    (∀ p ∈ S, p.1 < Φ') := by
  cases h with
  | nil => simp
  | cons hΦN hinfer huni _ _ hrest =>
    expose_names
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at htfv
    have hσbody : Ty.BelowFvars Φ σ.body := hsch σ List.mem_cons_self
    have hrle := Infer.frontier_le hinfer
    have hΦΦ₁ : Φ ≤ Φ₁ := by omega
    have hctx_pc : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hctx M hM).mono (by omega)
    have hΦropen : ∀ y ∈ (e.openTyVars (freshVars N σ.paramCount)).tyFreeVars, y < N + σ.paramCount :=
      fun y hy => by
        rcases Expr.tyFreeVars_openTyVars hy with hh | hh
        · have := htfv y (.inl hh); omega
        · have := freshVars_lt y hh; omega
    obtain ⟨hr_τ, hr_s⟩ := Infer.belowFvars hinfer hctx_pc hΦropen
    have hr_dom := Infer.dom_below hinfer hctx_pc hΦropen
    have hσopen : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
      Ty.openVars_belowFvars (hσbody.mono (by omega))
        (fun x hx => by have := freshVars_lt x hx; omega)
    have hSchk : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hr_τ hσopen
    have hSchkdom : ∀ p ∈ Schk, p.1 < Φ₁ := by
      intro p hp
      rcases UnifyRel.dom_mem huni p hp with hh | hh
      · exact hr_τ.mem_lt p.1 hh
      · exact hσopen.mem_lt p.1 hh
    have hctx1 : CtxBelow Φ₁ (Schk.onCtx (S₁.onCtx ctx)) :=
      Subst.onCtx_below hSchk (le_refl _) (Subst.onCtx_below hr_s hrle hctx_pc)
    have hbrle := InferRecGroupAnn.frontier_le hrest
    have hrest_dom := InferRecGroupAnn.dom_below hrest hctx1
      (fun σ' hσ' => (hsch σ' (List.mem_cons_of_mem _ hσ')).mono hΦΦ₁)
      (fun y hy => lt_of_lt_of_le (htfv y (.inr hy)) hΦΦ₁)
    intro p hp; rw [List.mem_append, List.mem_append] at hp
    rcases hp with (hp | hp) | hp
    · have := hr_dom p hp; omega
    · have := hSchkdom p hp; omega
    · exact hrest_dom p hp
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end

/-- A var avoiding both the context env and a substitution's range avoids the
    substituted context env. -/
theorem Subst.onCtx_avoid {S : Subst} {ctx : Ctx} {w : Nat}
    (hctx : ∀ M ∈ ctx.env, w ∉ M.body.freeVars) (hS : ∀ p ∈ S, w ∉ p.2.freeVars) :
    ∀ M ∈ (S.onCtx ctx).env, w ∉ M.body.freeVars := by
  intro M hM
  simp only [Subst.onCtx, Subst.onEnv] at hM
  obtain ⟨M0, hM0, rfl⟩ := List.mem_map.mp hM
  intro hc
  simp only [Subst.onPolyTy] at hc
  rcases Subst.mem_freeVars_onTy hc with h | ⟨p, hp, hvp⟩
  · exact hctx M0 hM0 h
  · exact hS p hp hvp

/-- `RecGroup.tyFreeVars` membership decomposes to a member binding. -/
theorem Expr.mem_recGroupTyFreeVars {L : List Expr} {w : Nat}
    (h : w ∈ Expr.tyFreeVars.RecGroup.tyFreeVars L) : ∃ e ∈ L, w ∈ e.tyFreeVars := by
  induction L with
  | nil => simp [Expr.tyFreeVars.RecGroup.tyFreeVars] at h
  | cons hd tl ih =>
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at h
    rcases h with h | h
    · exact ⟨hd, List.mem_cons_self, h⟩
    · obtain ⟨e, he, hwe⟩ := ih h; exact ⟨e, List.mem_cons_of_mem _ he, hwe⟩

/-- Every free type var of the Λ-outside nest comes from a member monotype `τⱼ`
    (the schemes `Mⱼ`/closed copies only contain `τⱼ`'s vars), a raw binding, or the
    body — shifting and closing introduce no new type vars. -/
theorem Expr.mem_tyFreeVars_letRecElabNest {G : List Nat} {n : Nat} {rawBindings : List Expr} :
    ∀ {members : List (Nat × Ty)} {body : Expr} {y : Nat},
      y ∈ (Expr.letRecElabNest G n rawBindings members body).tyFreeVars →
      (∃ p ∈ members, y ∈ p.2.freeVars) ∨ (∃ e ∈ rawBindings, y ∈ e.tyFreeVars)
        ∨ y ∈ body.tyFreeVars := by
  intro members
  induction members with
  | nil => intro body y hy; exact Or.inr (Or.inr hy)
  | cons hd tl ih =>
    obtain ⟨i, τ⟩ := hd
    intro body y hy
    simp only [Expr.letRecElabNest, Expr.tyFreeVars, Option.elim_some, List.mem_append] at hy
    rcases hy with (hy | hy) | hy
    · refine Or.inl ⟨(i, τ), List.mem_cons_self, ?_⟩
      simp only [PolyTy.genGroup] at hy
      exact Ty.freeVars_closeOver_subset hy
    · have hy2 := Expr.tyFreeVars_closeTyVars_subset hy
      rw [Expr.tyFreeVars, List.mem_append] at hy2
      rcases hy2 with hy2 | hy2
      · obtain ⟨e', he', hye'⟩ := Expr.mem_recGroupTyFreeVars hy2
        obtain ⟨e'', he'', rfl⟩ := List.mem_map.mp he'
        refine Or.inr (Or.inl ⟨e'', he'', ?_⟩)
        rwa [Expr.shiftFrom_tyFreeVars] at hye'
      · simp [Expr.tyFreeVars] at hy2
    · rcases ih hy with h | h | h
      · obtain ⟨p, hp, hyp⟩ := h
        exact Or.inl ⟨p, List.mem_cons_of_mem _ hp, hyp⟩
      · exact Or.inr (Or.inl h)
      · exact Or.inr (Or.inr h)

theorem Expr.mem_tyFreeVars_letRecElab {G : List Nat} {rawBindings : List Expr} {τs : List Ty}
    {body : Expr} {y : Nat}
    (hy : y ∈ (Expr.letRecElab G rawBindings τs body).tyFreeVars) :
    (∃ τ ∈ τs, y ∈ τ.freeVars) ∨ (∃ e ∈ rawBindings, y ∈ e.tyFreeVars) ∨ y ∈ body.tyFreeVars := by
  simp only [Expr.letRecElab] at hy
  rcases Expr.mem_tyFreeVars_letRecElabNest hy with h | h | h
  · obtain ⟨p, hp, hyp⟩ := h
    exact Or.inl ⟨p.2, (List.of_mem_zip (List.mem_reverse.mp hp)).2, hyp⟩
  · exact Or.inr (Or.inl h)
  · exact Or.inr (Or.inr h)

/-- Push a var-avoidance through a substitution (avoid form of `Subst.onTy`). -/
theorem Subst.notMemOnTy {S : Subst} {w : Nat} {τ : Ty}
    (hS : ∀ p ∈ S, w ∉ p.2.freeVars) (hτ : w ∉ τ.freeVars) : w ∉ (S.onTy τ).freeVars := by
  intro hc
  rcases Subst.mem_freeVars_onTy hc with h | ⟨p, hp, hvp⟩
  · exact hτ h
  · exact hS p hp hvp

/-- A var avoiding all opening args (and the body) avoids the opened type. -/
theorem Ty.not_mem_freeVars_openWith {Vs : List Ty} {w : Nat} (hVs : ∀ v ∈ Vs, w ∉ v.freeVars) :
    ∀ {X : Ty}, w ∉ X.freeVars → w ∉ (Ty.openWith Vs X).freeVars := by
  intro X
  induction X using Ty.rec_strong with
  | prim p => intro _; simp [Ty.openWith, Ty.instantiate, Ty.freeVars]
  | fvar n => intro hX; simpa [Ty.openWith, Ty.instantiate, Ty.freeVars] using hX
  | bvar i =>
    intro _
    simp only [Ty.openWith, Ty.instantiate]
    cases h : Vs[i]? with
    | none => simp [Ty.freeVars]
    | some v => simp only [Option.getD_some]; exact hVs v (List.mem_of_getElem? h)
  | arrow a b iha ihb =>
    intro hX
    simp only [Ty.freeVars, List.mem_dedup, List.mem_append, not_or] at hX
    simp only [Ty.openWith, Ty.instantiate, Ty.freeVars, List.mem_dedup, List.mem_append, not_or]
    exact ⟨iha hX.1, ihb hX.2⟩
  | customTy nm tys ih =>
    intro hX
    have hX' : ∀ t ∈ tys, w ∉ t.freeVars := fun t ht hc =>
      hX (by rw [Ty.freeVars]; exact TyList.mem_freeVars_of_mem ht hc)
    simp only [Ty.openWith, Ty.instantiate, TyList.instantiate_eq_map, Ty.freeVars]
    intro hc
    rw [mem_TyList_freeVars] at hc
    obtain ⟨t', ht', hwt'⟩ := hc
    obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
    exact ih t0 ht0 (hX' t0 ht0) hwt'

/-- `RecGroup.tyFreeVars` membership reconstruction (`_of` direction). -/
theorem Expr.mem_recGroupTyFreeVars_of {L : List Expr} {e : Expr} {w : Nat}
    (he : e ∈ L) (hw : w ∈ e.tyFreeVars) : w ∈ Expr.tyFreeVars.RecGroup.tyFreeVars L := by
  induction L with
  | nil => exact absurd he List.not_mem_nil
  | cons hd tl ih =>
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]
    rcases List.mem_cons.mp he with h | h
    · subst h; exact Or.inl hw
    · exact Or.inr (ih h)

/-- `BranchList.tyFreeVars` membership decomposition. -/
theorem Expr.mem_branchListTyFreeVars {brs : List (MatchPattern × Expr)} {w : Nat}
    (h : w ∈ Expr.tyFreeVars.BranchList.tyFreeVars brs) : ∃ pb ∈ brs, w ∈ pb.2.tyFreeVars := by
  induction brs with
  | nil => simp [Expr.tyFreeVars.BranchList.tyFreeVars] at h
  | cons hd tl ih =>
    obtain ⟨p, b⟩ := hd
    simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append] at h
    rcases h with h | h
    · exact ⟨(p, b), List.mem_cons_self, h⟩
    · obtain ⟨pb, hpb, hwpb⟩ := ih h; exact ⟨pb, List.mem_cons_of_mem _ hpb, hwpb⟩

/-- `BranchList.tyFreeVars` membership reconstruction (`_of` direction). -/
theorem Expr.mem_branchListTyFreeVars_of {brs : List (MatchPattern × Expr)} {p : MatchPattern}
    {b : Expr} {w : Nat} (hmem : (p, b) ∈ brs) (hw : w ∈ b.tyFreeVars) :
    w ∈ Expr.tyFreeVars.BranchList.tyFreeVars brs := by
  induction brs with
  | nil => exact absurd hmem List.not_mem_nil
  | cons hd tl ih =>
    obtain ⟨p', b'⟩ := hd
    simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]
    rcases List.mem_cons.mp hmem with h | h
    · rw [Prod.mk.injEq] at h; obtain ⟨_, hb⟩ := h; subst hb; exact Or.inl hw
    · exact Or.inr (ih h)

/-- A free var of an iterated `substFvars`-image comes from the original type or
    one of the substituted-in image types. -/
theorem Ty.mem_freeVars_substFvars {S : List (Nat × Ty)} {t : Ty} {w : Nat}
    (h : w ∈ (Ty.substFvars S t).freeVars) : w ∈ t.freeVars ∨ ∃ p ∈ S, w ∈ p.2.freeVars := by
  induction S generalizing t with
  | nil => exact Or.inl h
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    rcases ih h with hh | ⟨p, hp, hwp⟩
    · rcases Ty.mem_freeVars_substFvar hh with h1 | h1
      · exact Or.inl h1
      · exact Or.inr ⟨(Z, U), List.mem_cons_self, h1⟩
    · exact Or.inr ⟨p, List.mem_cons_of_mem _ hp, hwp⟩

/-- A free type var of a `letRecAnn`'s `substFvars`-mapped scheme list comes from the
    original schemes or the substitution range. -/
private theorem SchemeList.mem_tyFreeVars_substFvars {S : List (Nat × Ty)} {w : Nat} :
    ∀ (schemes : List PolyTy),
      w ∈ Expr.tyFreeVars.SchemeList.tyFreeVars (schemes.map (PolyTy.substFvars S)) →
      w ∈ Expr.tyFreeVars.SchemeList.tyFreeVars schemes ∨ ∃ p ∈ S, w ∈ p.2.freeVars := by
  intro schemes
  induction schemes with
  | nil => intro h; simp [Expr.tyFreeVars.SchemeList.tyFreeVars] at h
  | cons σ ss ih =>
    intro h
    simp only [List.map_cons, Expr.tyFreeVars.SchemeList.tyFreeVars, List.mem_append,
      PolyTy.body_substFvars] at h ⊢
    rcases h with h | h
    · rcases Ty.mem_freeVars_substFvars h with hh | hh
      · exact .inl (.inl hh)
      · exact .inr hh
    · rcases ih h with hh | hh
      · exact .inl (.inr hh)
      · exact .inr hh

/-- A free type var of `e.substTyFvars S` comes from `e` or one of the image types.
    (Re-based off `NoRecAnn`: `substTyFvars` rewrites `letRecAnn` schemes by
    `PolyTy.substFvars` and recurses into bindings/body, so the membership split holds
    unconditionally.) Uses the public `substTyFvars_*` distribution lemmas. -/
theorem Expr.mem_tyFreeVars_substTyFvars {S : List (Nat × Ty)} {w : Nat} :
    ∀ {e : Expr}, w ∈ (e.substTyFvars S).tyFreeVars →
      w ∈ e.tyFreeVars ∨ ∃ p ∈ S, w ∈ p.2.freeVars := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p =>
    intro h
    rw [Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (by simp [Expr.tyFreeVars])] at h
    simp [Expr.tyFreeVars] at h
  | ctor c =>
    intro h
    rw [Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (by simp [Expr.tyFreeVars])] at h
    simp [Expr.tyFreeVars] at h
  | var i tyArgs =>
    intro h
    rw [Expr.substTyFvars_var] at h
    simp only [Expr.tyFreeVars, List.mem_flatMap] at h
    obtain ⟨t', ht', hwt'⟩ := h
    obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
    rcases Ty.mem_freeVars_substFvars hwt' with hh | hh
    · exact Or.inl (by simp only [Expr.tyFreeVars, List.mem_flatMap]; exact ⟨t0, ht0, hh⟩)
    · exact Or.inr hh
  | lambda ann body ih =>
    intro h
    rw [Expr.substTyFvars_lambda] at h
    cases ann with
    | none =>
      simp only [Expr.tyFreeVars, Option.map_none, Option.elim_none, List.nil_append] at h ⊢
      exact ih h
    | some t =>
      simp only [Expr.tyFreeVars, Option.map_some, Option.elim_some, List.mem_append] at h ⊢
      rcases h with h | h
      · rcases Ty.mem_freeVars_substFvars h with hh | hh
        · exact Or.inl (Or.inl hh)
        · exact Or.inr hh
      · rcases ih h with hh | hh
        · exact Or.inl (Or.inr hh)
        · exact Or.inr hh
  | app f arg ihf iharg =>
    intro h
    rw [Expr.substTyFvars_app] at h
    simp only [Expr.tyFreeVars, List.mem_append] at h ⊢
    rcases h with h | h
    · rcases ihf h with hh | hh
      · exact Or.inl (Or.inl hh)
      · exact Or.inr hh
    · rcases iharg h with hh | hh
      · exact Or.inl (Or.inr hh)
      · exact Or.inr hh
  | letIn ann rhs body ihr ihb =>
    intro h
    rw [Expr.substTyFvars_letIn] at h
    cases ann with
    | none =>
      simp only [Expr.tyFreeVars, Option.map_none, Option.elim_none, List.nil_append,
        List.mem_append] at h ⊢
      rcases h with h | h
      · rcases ihr h with hh | hh
        · exact Or.inl (Or.inl hh)
        · exact Or.inr hh
      · rcases ihb h with hh | hh
        · exact Or.inl (Or.inr hh)
        · exact Or.inr hh
    | some σ =>
      simp only [Expr.tyFreeVars, Option.map_some, Option.elim_some, List.mem_append] at h ⊢
      rcases h with (h | h) | h
      · rcases Ty.mem_freeVars_substFvars h with hh | hh
        · exact Or.inl (Or.inl (Or.inl hh))
        · exact Or.inr hh
      · rcases ihr h with hh | hh
        · exact Or.inl (Or.inl (Or.inr hh))
        · exact Or.inr hh
      · rcases ihb h with hh | hh
        · exact Or.inl (Or.inr hh)
        · exact Or.inr hh
  | match_ scrut branches ihs ihbr =>
    intro h
    rw [Expr.substTyFvars_match] at h
    simp only [Expr.tyFreeVars, List.mem_append] at h ⊢
    rcases h with h | h
    · rcases ihs h with hh | hh
      · exact Or.inl (Or.inl hh)
      · exact Or.inr hh
    · obtain ⟨pb, hpb, hwpb⟩ := Expr.mem_branchListTyFreeVars h
      obtain ⟨pb0, hpb0, rfl⟩ := List.mem_map.mp hpb
      obtain ⟨p0, b0⟩ := pb0
      rcases ihbr p0 b0 hpb0 hwpb with hh | hh
      · exact Or.inl (Or.inr (Expr.mem_branchListTyFreeVars_of hpb0 hh))
      · exact Or.inr hh
  | letRec bindings body ihbs ihb =>
    intro h
    rw [Expr.substTyFvars_letRec] at h
    simp only [Expr.tyFreeVars, List.mem_append] at h ⊢
    rcases h with h | h
    · obtain ⟨e', he', hwe'⟩ := Expr.mem_recGroupTyFreeVars h
      obtain ⟨e, he, rfl⟩ := List.mem_map.mp he'
      rcases ihbs e he hwe' with hh | hh
      · exact Or.inl (Or.inl (Expr.mem_recGroupTyFreeVars_of he hh))
      · exact Or.inr hh
    · rcases ihb h with hh | hh
      · exact Or.inl (Or.inr hh)
      · exact Or.inr hh
  | letRecAnn schemes bindings body ihbs ihb =>
    intro h
    rw [Expr.substTyFvars_letRecAnn] at h
    simp only [Expr.tyFreeVars, List.mem_append] at h ⊢
    rcases h with (h | h) | h
    · rcases SchemeList.mem_tyFreeVars_substFvars schemes h with hh | hh
      · exact Or.inl (Or.inl (Or.inl hh))
      · exact Or.inr hh
    · obtain ⟨e', he', hwe'⟩ := Expr.mem_recGroupTyFreeVars h
      obtain ⟨e, he, rfl⟩ := List.mem_map.mp he'
      rcases ihbs e he hwe' with hh | hh
      · exact Or.inl (Or.inl (Or.inr (Expr.mem_recGroupTyFreeVars_of he hh)))
      · exact Or.inr hh
    · rcases ihb h with hh | hh
      · exact Or.inl (Or.inr hh)
      · exact Or.inr hh

/-- A var avoiding `e`'s free type vars and the substitution range avoids
    `e.substTyFvars S`'s free type vars. -/
theorem Expr.notMem_tyFreeVars_substTyFvars {S : List (Nat × Ty)} {e : Expr} {w : Nat}
    (hwe : w ∉ e.tyFreeVars) (hwS : ∀ p ∈ S, w ∉ p.2.freeVars) :
    w ∉ (e.substTyFvars S).tyFreeVars := by
  intro hc
  rcases Expr.mem_tyFreeVars_substTyFvars hc with h | ⟨p, hp, hwp⟩
  · exact hwe h
  · exact hwS p hp hwp

mutual
/-- **Locality (avoid form).** A var below the input frontier that avoids the
    context env and the skeleton's annotation free vars also avoids the inferred
    substitution range, result type, and elaborated output. This is the
    characterisation the prefix-fix corollary (M4) needs. -/
theorem Infer.eOut_avoid {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ) :
    ∀ {w : Nat}, w < Φ → (∀ M ∈ ctx.env, w ∉ M.body.freeVars) → w ∉ e.tyFreeVars →
    (∀ p ∈ S, w ∉ p.2.freeVars) ∧ w ∉ τ.freeVars ∧ w ∉ eOut.tyFreeVars := by
  cases h with
  | primLitUnit => intro w _ _ _; exact ⟨by simp, by simp [Ty.freeVars], by simp [Expr.tyFreeVars]⟩
  | primLitInt => intro w _ _ _; exact ⟨by simp, by simp [Ty.freeVars], by simp [Expr.tyFreeVars]⟩
  | primLitNat => intro w _ _ _; exact ⟨by simp, by simp [Ty.freeVars], by simp [Expr.tyFreeVars]⟩
  | primLitStr => intro w _ _ _; exact ⟨by simp, by simp [Ty.freeVars], by simp [Expr.tyFreeVars]⟩
  | var hlook =>
    intro w hwΦ hctx _
    refine ⟨by simp, ?_, ?_⟩
    · intro hc
      rcases Ty.freeVars_openVars_subset w hc with h | h
      · exact hctx _ (List.mem_of_getElem? hlook) h
      · have := freshVars_ge w h; omega
    · simp only [Expr.tyFreeVars, List.mem_flatMap, not_exists, not_and]
      intro t ht hwt
      obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
      simp only [Ty.freeVars, List.mem_singleton] at hwt
      have := freshVars_ge x hx; omega
  | ctor hlook =>
    intro w hwΦ _ _
    refine ⟨by simp, ?_, by simp [Expr.tyFreeVars]⟩
    intro hc
    rcases Ty.freeVars_openVars_subset w hc with h | h
    · exact (Ctor.toTy_body_noFreeVars _).not_mem_freeVars w h
    · have := freshVars_ge w h; omega
  | lambda hseed hbody =>
    intro w hwΦ hctx hwe
    cases hseed with
    | none =>
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append] at hwe
      obtain ⟨hbS, hbτ, hbo⟩ := Infer.eOut_avoid hbody (w := w) (by omega)
        (by intro M hM; rcases List.mem_cons.mp hM with rfl | hM
            · intro hc; simp only [PolyTy.mkTrivial, Ty.freeVars, List.mem_singleton] at hc; omega
            · exact hctx M hM)
        hwe
      refine ⟨hbS, ?_, ?_⟩
      · simp only [Ty.freeVars, List.mem_dedup, List.mem_append, not_or]
        refine ⟨?_, hbτ⟩
        intro hc
        rcases Subst.mem_freeVars_onTy hc with h | ⟨p, hp, hvp⟩
        · simp only [Ty.freeVars, List.mem_singleton] at h; omega
        · exact hbS p hp hvp
      · simpa only [Expr.tyFreeVars, Option.elim_none, List.nil_append] using hbo
    | some T hT =>
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append, not_or] at hwe
      obtain ⟨hbS, hbτ, hbo⟩ := Infer.eOut_avoid hbody (w := w) hwΦ
        (by intro M hM; rcases List.mem_cons.mp hM with rfl | hM
            · simpa only [PolyTy.mkTrivial] using hwe.1
            · exact hctx M hM)
        hwe.2
      refine ⟨hbS, ?_, ?_⟩
      · simp only [Ty.freeVars, List.mem_dedup, List.mem_append, not_or]
        exact ⟨Subst.notMemOnTy hbS hwe.1, hbτ⟩
      · simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append, not_or]
        exact ⟨hwe.1, hbo⟩
  | app hf harg huni =>
    intro w hwΦ hctx hwe
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append, not_or] at hwe
    obtain ⟨hfS, hfτ, hfo⟩ := Infer.eOut_avoid hf (w := w) hwΦ hctx hwe.1
    have hfle := Infer.frontier_le hf
    have hargle := Infer.frontier_le harg
    obtain ⟨haS, haτ, hao⟩ := Infer.eOut_avoid harg (w := w) (by omega)
      (Subst.onCtx_avoid hctx hfS) hwe.2
    have hS₃ : ∀ p ∈ S₃, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni p hp w hwp with h | h
      · exact Subst.notMemOnTy haS hfτ h
      · simp only [Ty.freeVars, List.mem_dedup, List.mem_append, List.mem_singleton] at h
        rcases h with h | h
        · exact haτ h
        · omega
    refine ⟨?_, ?_, ?_⟩
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hfS p hp
      · exact haS p hp
      · exact hS₃ p hp
    · intro hc
      rcases Subst.mem_freeVars_onTy hc with h | ⟨q, hq, hvq⟩
      · simp only [Ty.freeVars, List.mem_singleton] at h; omega
      · exact hS₃ q hq hvq
    · simp only [Expr.tyFreeVars, List.mem_append, not_or]
      exact ⟨hfo, hao⟩
  | letIn hrhs hbody =>
    intro w hwΦ hctx hwe
    expose_names
    simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append, not_or] at hwe
    obtain ⟨hrS, hrτ, hro⟩ := Infer.eOut_avoid hrhs (w := w) hwΦ hctx hwe.1
    have hrle := Infer.frontier_le hrhs
    have hMbody : w ∉ (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁).body.freeVars :=
      fun hc => hrτ (Ty.freeVars_closeOver_subset hc)
    obtain ⟨hbS, hbτ, hbo⟩ := Infer.eOut_avoid hbody (w := w) (by omega)
      (by intro M hM; rcases List.mem_cons.mp hM with rfl | hM
          · exact hMbody
          · exact Subst.onCtx_avoid hctx hrS M hM)
      hwe.2
    refine ⟨?_, hbτ, ?_⟩
    · intro p hp; rw [List.mem_append] at hp
      rcases hp with hp | hp
      · exact hrS p hp
      · exact hbS p hp
    · simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append, not_or]
      exact ⟨⟨hMbody,
        fun hc => Expr.notMem_tyFreeVars_substTyFvars hro hrS
          (Expr.tyFreeVars_closeTyVars_subset hc)⟩, hbo⟩
  | letInAnn hσwf hΦN hrhs huni _hesc1 _hesc2 hbody =>
    intro w hwΦ hctx hwe
    expose_names
    simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append, not_or] at hwe
    have hrle := Infer.frontier_le hrhs
    obtain ⟨hrS, hrτ, hro⟩ := Infer.eOut_avoid hrhs (w := w) (by omega) hctx (by
      intro hc
      rcases Expr.tyFreeVars_openTyVars hc with h | h
      · exact hwe.1.2 h
      · have := freshVars_ge w h; omega)
    have hσopen : w ∉ (σ.openVars (freshVars N σ.paramCount)).freeVars := by
      intro hc
      rcases Ty.freeVars_openVars_subset w hc with h | h
      · exact hwe.1.1 h
      · have := freshVars_ge w h; omega
    have hSchk : ∀ p ∈ Schk, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni p hp w hwp with h | h
      · exact hrτ h
      · exact hσopen h
    obtain ⟨hbS, hbτ, hbo⟩ := Infer.eOut_avoid hbody (w := w) (by omega)
      (by intro M hM; rcases List.mem_cons.mp hM with rfl | hM
          · exact hwe.1.1
          · exact Subst.onCtx_avoid (Subst.onCtx_avoid hctx hrS) hSchk M hM)
      hwe.2
    refine ⟨?_, hbτ, ?_⟩
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hrS p hp
      · exact hSchk p hp
      · exact hbS p hp
    · simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append, not_or]
      exact ⟨⟨hwe.1.1,
        fun hc => Expr.notMem_tyFreeVars_substTyFvars hro
          (fun p hp => (List.mem_append.mp hp).elim (hrS p) (hSchk p))
          (Expr.tyFreeVars_closeTyVars_subset hc)⟩, hbo⟩
  | match_ hscrut hne hbr =>
    intro w hwΦ hctx hwe
    simp only [Expr.tyFreeVars, List.mem_append, not_or] at hwe
    obtain ⟨hsS, hsτ, hso⟩ := Infer.eOut_avoid hscrut (w := w) hwΦ hctx hwe.1
    have hle1 := Infer.frontier_le hscrut
    obtain ⟨hbrS, hbrρ, hbro⟩ := InferBranches.eOut_avoid hbr (w := w) (by omega)
      (Subst.onCtx_avoid hctx hsS) hsτ
      (by intro hc; simp only [Ty.freeVars, List.mem_singleton] at hc; omega) hwe.2
    refine ⟨?_, hbrρ, ?_⟩
    · intro p hp; rw [List.mem_append] at hp
      rcases hp with hp | hp
      · exact hsS p hp
      · exact hbrS p hp
    · simp only [Expr.tyFreeVars, List.mem_append, not_or]
      exact ⟨hso, hbro⟩
  | letRec hgroup hbody =>
    intro w hwΦ hctx hwe
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append, not_or] at hwe
    have hgle := InferRecGroup.frontier_le hgroup
    obtain ⟨hgS, hgo⟩ := InferRecGroup.eOut_avoid hgroup (w := w) (by omega)
      (by intro M hM; rcases List.mem_append.mp hM with hM | hM
          · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
            simp only [PolyTy.mkTrivial, Ty.freeVars, List.mem_singleton]
            have := freshVars_ge x hx; omega
          · exact hctx M hM)
      (by intro β hβ; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hβ
          simp only [Ty.freeVars, List.mem_singleton]; have := freshVars_ge x hx; omega)
      hwe.1
    have hgenBody : ∀ M ∈ (genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
            ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)))), w ∉ M.body.freeVars := by
      intro M hM
      simp only [genGroupSchemes] at hM
      obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
      obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
      simp only [PolyTy.genGroup]
      intro hc
      have hc' := Ty.freeVars_closeOver_subset hc
      rcases Subst.mem_freeVars_onTy hc' with h | ⟨p, hp, hvp⟩
      · simp only [Ty.freeVars, List.mem_singleton] at h; have := freshVars_ge x hx; omega
      · exact hgS p hp hvp
    obtain ⟨hbS, hbτ, hbo⟩ := Infer.eOut_avoid hbody (w := w) (by omega)
      (by intro M hM; rcases List.mem_append.mp hM with hM | hM
          · exact hgenBody M hM
          · exact Subst.onCtx_avoid hctx hgS M hM)
      hwe.2
    refine ⟨?_, hbτ, ?_⟩
    · intro p hp; rw [List.mem_append] at hp
      rcases hp with hp | hp
      · exact hgS p hp
      · exact hbS p hp
    · intro hc
      rcases Expr.mem_tyFreeVars_letRecElab hc with h | h | h
      · obtain ⟨τ, hτ, hwτ⟩ := h
        obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
        rcases Subst.mem_freeVars_onTy hwτ with h' | ⟨p, hp, hvp⟩
        · simp only [Ty.freeVars, List.mem_singleton] at h'
          have := freshVars_ge x hx; omega
        · exact hgS p hp hvp
      · obtain ⟨e, he, hwe'⟩ := h
        obtain ⟨e0, he0, rfl⟩ := List.mem_map.mp he
        exact Expr.notMem_tyFreeVars_substTyFvars (hgo e0 he0) hgS hwe'
      · exact hbo h
  | letRecAnn hwf hlen hgroup hbody =>
    intro w hwΦ hctx hwe
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append, not_or] at hwe
    have hsch : ∀ σ ∈ schemes, w ∉ σ.body.freeVars := fun σ hσ hc =>
      hwe.1.1 (Expr.scheme_body_mem_schemeList_tyFreeVars hσ hc)
    have hctxσ : ∀ M ∈ ({ ctx with env := schemes ++ ctx.env } : Ctx).env, w ∉ M.body.freeVars := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hsch M hM
      · exact hctx M hM
    obtain ⟨hgS, hgo⟩ := InferRecGroupAnn.eOut_avoid hgroup (w := w) hwΦ hctxσ hsch hwe.1.2
    have hgle := InferRecGroupAnn.frontier_le hgroup
    obtain ⟨hbS, hbτ, hbo⟩ := Infer.eOut_avoid hbody (w := w) (by omega)
      (Subst.onCtx_avoid hctxσ hgS) hwe.2
    refine ⟨?_, hbτ, ?_⟩
    · intro p hp; rw [List.mem_append] at hp
      rcases hp with hp | hp
      · exact hgS p hp
      · exact hbS p hp
    · simp only [Expr.tyFreeVars, List.mem_append, not_or]
      refine ⟨⟨hwe.1.1, ?_⟩, hbo⟩
      intro hc
      obtain ⟨e', he', hwe'⟩ := Expr.mem_recGroupTyFreeVars hc
      exact hgo e' he' hwe'
termination_by e.size
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.size, Expr.size_openTyVars]; omega)
theorem InferBranches.eOut_avoid {Φ ctx scrutTy ρ brs Φ' S brsOut}
    (h : InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut) :
    ∀ {w : Nat}, w < Φ → (∀ M ∈ ctx.env, w ∉ M.body.freeVars) → w ∉ scrutTy.freeVars →
    w ∉ ρ.freeVars → w ∉ Expr.tyFreeVars.BranchList.tyFreeVars brs →
    (∀ p ∈ S, w ∉ p.2.freeVars) ∧ w ∉ (S.onTy ρ).freeVars ∧
      w ∉ Expr.tyFreeVars.BranchList.tyFreeVars brsOut := by
  cases h with
  | nil =>
    intro w _ _ _ hρ _
    exact ⟨by simp, by simpa using hρ, by simp [Expr.tyFreeVars.BranchList.tyFreeVars]⟩
  | cons hlook hn huni0 hbody huni hrest =>
    intro w hwΦ hctx hscrut hρ hbrs
    expose_names
    simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append, not_or] at hbrs
    have hS₀ : ∀ p ∈ S₀, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni0 p hp w hwp with h | h
      · exact hscrut h
      · simp only [Ty.freeVars] at h
        rw [mem_TyList_freeVars] at h
        obtain ⟨t, ht, hgt⟩ := h
        obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
        simp only [Ty.freeVars, List.mem_singleton] at hgt
        have := freshVars_ge x hx; omega
    have hta : ∀ v ∈ (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy), w ∉ v.freeVars := by
      intro v hv hwv
      obtain ⟨v0, hv0, rfl⟩ := List.mem_map.mp hv
      obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv0
      rcases Subst.mem_freeVars_onTy hwv with h | ⟨p, hp, hvp⟩
      · simp only [Ty.freeVars, List.mem_singleton] at h; have := freshVars_ge x hx; omega
      · exact hS₀ p hp hvp
    have hle0 := Infer.frontier_le hbody
    obtain ⟨hbS, hbτ, hbo⟩ := Infer.eOut_avoid hbody (w := w) (by omega)
      (by intro M hM; rcases List.mem_append.mp hM with hM | hM
          · obtain ⟨t, ht, rfl⟩ := List.mem_map.mp hM
            obtain ⟨c, hc, rfl⟩ := List.mem_map.mp ht
            simpa only [PolyTy.mkTrivial] using
              Ty.not_mem_freeVars_openWith hta ((ctor.closed c hc).not_mem_freeVars w)
          · exact Subst.onCtx_avoid hctx hS₀ M hM)
      hbrs.1
    have hS₂ : ∀ p ∈ S₂, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni p hp w hwp with h | h
      · exact hbτ h
      · exact Subst.notMemOnTy hbS (Subst.notMemOnTy hS₀ hρ) h
    obtain ⟨hrS, hrρ, hro⟩ := InferBranches.eOut_avoid hrest (w := w) (by omega)
      (Subst.onCtx_avoid (Subst.onCtx_avoid (Subst.onCtx_avoid hctx hS₀) hbS) hS₂)
      (Subst.notMemOnTy hS₂ (Subst.notMemOnTy hbS (Subst.notMemOnTy hS₀ hscrut)))
      (Subst.notMemOnTy hS₂ (Subst.notMemOnTy hbS (Subst.notMemOnTy hS₀ hρ)))
      hbrs.2
    refine ⟨?_, ?_, ?_⟩
    · intro p hp; rw [List.mem_append, List.mem_append, List.mem_append] at hp
      rcases hp with ((hp | hp) | hp) | hp
      · exact hS₀ p hp
      · exact hbS p hp
      · exact hS₂ p hp
      · exact hrS p hp
    · rw [Subst.onTy_append, Subst.onTy_append, Subst.onTy_append]; exact hrρ
    · simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append, not_or]
      exact ⟨hbo, hro⟩
  | consWild hbody huni hrest =>
    intro w hwΦ hctx hscrut hρ hbrs
    expose_names
    simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append, not_or] at hbrs
    have hle1 := Infer.frontier_le hbody
    obtain ⟨hbS, hbτ, hbo⟩ := Infer.eOut_avoid hbody (w := w) hwΦ hctx hbrs.1
    have hS₂ : ∀ p ∈ S₂, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni p hp w hwp with h | h
      · exact hbτ h
      · exact Subst.notMemOnTy hbS hρ h
    obtain ⟨hrS, hrρ, hro⟩ := InferBranches.eOut_avoid hrest (w := w) (by omega)
      (Subst.onCtx_avoid (Subst.onCtx_avoid hctx hbS) hS₂)
      (Subst.notMemOnTy hS₂ (Subst.notMemOnTy hbS hscrut))
      (Subst.notMemOnTy hS₂ (Subst.notMemOnTy hbS hρ))
      hbrs.2
    refine ⟨?_, ?_, ?_⟩
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hbS p hp
      · exact hS₂ p hp
      · exact hrS p hp
    · rw [Subst.onTy_append, Subst.onTy_append]; exact hrρ
    · simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append, not_or]
      exact ⟨hbo, hro⟩
termination_by Expr.sizeBranches brs
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)
theorem InferRecGroup.eOut_avoid {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut) :
    ∀ {w : Nat}, w < Φ → (∀ M ∈ ctx.env, w ∉ M.body.freeVars) →
    (∀ β ∈ targets, w ∉ β.freeVars) → w ∉ Expr.tyFreeVars.RecGroup.tyFreeVars bindings →
    (∀ p ∈ S, w ∉ p.2.freeVars) ∧ (∀ e ∈ bindingsOut, w ∉ e.tyFreeVars) := by
  cases h with
  | nil => intro w _ _ _ _; exact ⟨by simp, by simp⟩
  | cons he huni hrest =>
    intro w hwΦ hctx htargets hbinds
    expose_names
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append, not_or] at hbinds
    have hle1 := Infer.frontier_le he
    obtain ⟨heS, heτ, heo⟩ := Infer.eOut_avoid he (w := w) hwΦ hctx hbinds.1
    have hS₂ : ∀ p ∈ S₂, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni p hp w hwp with h | h
      · exact heτ h
      · exact Subst.notMemOnTy heS (htargets _ (List.mem_cons_self ..)) h
    obtain ⟨hrS, hro⟩ := InferRecGroup.eOut_avoid hrest (w := w) (by omega)
      (Subst.onCtx_avoid (Subst.onCtx_avoid hctx heS) hS₂)
      (by intro β' hβ'; obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hβ'
          exact Subst.notMemOnTy hS₂ (Subst.notMemOnTy heS (htargets b (List.mem_cons_of_mem _ hb))))
      hbinds.2
    refine ⟨?_, ?_⟩
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact heS p hp
      · exact hS₂ p hp
      · exact hrS p hp
    · intro e' he'
      rcases List.mem_cons.mp he' with rfl | hmem
      · exact heo
      · exact hro e' hmem
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)
theorem InferRecGroupAnn.eOut_avoid {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut) :
    ∀ {w : Nat}, w < Φ → (∀ M ∈ ctx.env, w ∉ M.body.freeVars) →
    (∀ σ ∈ schemes, w ∉ σ.body.freeVars) → w ∉ Expr.tyFreeVars.RecGroup.tyFreeVars bindings →
    (∀ p ∈ S, w ∉ p.2.freeVars) ∧ (∀ e ∈ bindingsOut, w ∉ e.tyFreeVars) := by
  cases h with
  | nil => intro w _ _ _ _; exact ⟨by simp, by simp⟩
  | cons hΦN hinfer huni _ _ hrest =>
    intro w hwΦ hctx hsch hbinds
    expose_names
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append, not_or] at hbinds
    have hσbody : w ∉ σ.body.freeVars := hsch σ List.mem_cons_self
    have hle1 := Infer.frontier_le hinfer
    have hwopen : w ∉ (e.openTyVars (freshVars N σ.paramCount)).tyFreeVars := by
      intro hc
      rcases Expr.tyFreeVars_openTyVars hc with h | h
      · exact hbinds.1 h
      · have := freshVars_ge w h; omega
    obtain ⟨heS, heτ, heo⟩ := Infer.eOut_avoid hinfer (w := w) (by omega) hctx hwopen
    have hσopen : w ∉ (σ.openVars (freshVars N σ.paramCount)).freeVars := by
      intro hc
      rcases Ty.freeVars_openVars_subset w hc with h | h
      · exact hσbody h
      · have := freshVars_ge w h; omega
    have hSchk : ∀ p ∈ Schk, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni p hp w hwp with h | h
      · exact heτ h
      · exact hσopen h
    have heo_closed : w ∉ ((eOut.substTyFvars (S₁ ++ Schk)).closeTyVars
        (freshVars N σ.paramCount)).tyFreeVars :=
      fun hc => Expr.notMem_tyFreeVars_substTyFvars heo
        (fun p hp => (List.mem_append.mp hp).elim (heS p) (hSchk p))
        (Expr.tyFreeVars_closeTyVars_subset hc)
    obtain ⟨hrS, hro⟩ := InferRecGroupAnn.eOut_avoid hrest (w := w) (by omega)
      (Subst.onCtx_avoid (Subst.onCtx_avoid hctx heS) hSchk)
      (fun σ' hσ' => hsch σ' (List.mem_cons_of_mem _ hσ'))
      hbinds.2
    refine ⟨?_, ?_⟩
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact heS p hp
      · exact hSchk p hp
      · exact hrS p hp
    · intro e' he'
      rcases List.mem_cons.mp he' with rfl | hmem
      · exact heo_closed
      · exact hro e' hmem
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end

/-! ### M3: composed idempotency (`Infer.eliminates`)

`S` is idempotent: a domain var never survives in any `S`-image, and `S` reduces
its own result type. Composed from the single-MGU `UnifyRel.eliminates` by the
cross-disjointness argument: for `S = A ++ B`, `dom(A) ∩ range(B) = ∅` (the later
`B` is inferred in a world avoiding `dom(A)`), discharged via `Infer.eOut_avoid`. -/

/-- Compose idempotency across a substitution append, given the earlier domain
    avoids the later range. -/
theorem Subst.eliminates_append {A B : Subst}
    (hA : ∀ p ∈ A, ∀ x : Ty, p.1 ∉ (A.onTy x).freeVars)
    (hB : ∀ p ∈ B, ∀ x : Ty, p.1 ∉ (B.onTy x).freeVars)
    (hcross : ∀ p ∈ A, ∀ q ∈ B, p.1 ∉ q.2.freeVars) :
    ∀ p ∈ A ++ B, ∀ x : Ty, p.1 ∉ ((A ++ B).onTy x).freeVars := by
  intro p hp x hc
  rw [Subst.onTy_append] at hc
  rcases List.mem_append.mp hp with hpA | hpB
  · rcases Subst.mem_freeVars_onTy hc with h | ⟨q, hq, hvq⟩
    · exact hA p hpA x h
    · exact hcross p hpA q hq hvq
  · exact hB p hpB (A.onTy x) hc

/-- An idempotent substitution's domain var avoids its own substituted context. -/
theorem Subst.eliminates_onCtx {S : Subst} {w : Nat} {ctx : Ctx}
    (hS : ∀ x : Ty, w ∉ (S.onTy x).freeVars) : ∀ M ∈ (S.onCtx ctx).env, w ∉ M.body.freeVars := by
  intro M hM
  simp only [Subst.onCtx, Subst.onEnv] at hM
  obtain ⟨M0, hM0, rfl⟩ := List.mem_map.mp hM
  simp only [Subst.onPolyTy]
  exact hS M0.body

mutual
theorem Infer.eliminates {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ)
    (hctx : CtxBelow Φ ctx) (hΦ : ∀ y ∈ e.tyFreeVars, y < Φ)
    (hSe : ∀ p ∈ S, p.1 ∉ e.tyFreeVars) :
    (∀ p ∈ S, ∀ x : Ty, p.1 ∉ (S.onTy x).freeVars) ∧ (∀ p ∈ S, p.1 ∉ τ.freeVars) := by
  cases h with
  | primLitUnit => exact ⟨by simp, by simp [Ty.freeVars]⟩
  | primLitInt => exact ⟨by simp, by simp [Ty.freeVars]⟩
  | primLitNat => exact ⟨by simp, by simp [Ty.freeVars]⟩
  | primLitStr => exact ⟨by simp, by simp [Ty.freeVars]⟩
  | var hlook => exact ⟨by simp, by simp⟩
  | ctor hlook => exact ⟨by simp, by simp⟩
  | lambda hseed hbody =>
    cases hseed with
    | none =>
      expose_names
      have hΦb : ∀ y ∈ body.tyFreeVars, y < Φ := fun y hy => hΦ y (by
        show y ∈ (Expr.lambda none body).tyFreeVars
        simpa only [Expr.tyFreeVars, Option.elim_none, List.nil_append] using hy)
      have hSb : ∀ p ∈ S, p.1 ∉ body.tyFreeVars := fun p hp hc => hSe p hp (by
        show p.1 ∈ (Expr.lambda none body).tyFreeVars
        simpa only [Expr.tyFreeVars, Option.elim_none, List.nil_append] using hc)
      obtain ⟨hbE, hbR⟩ := Infer.eliminates hbody
        (by intro M hM; rcases List.mem_cons.mp hM with rfl | hM
            · exact .fvar (by omega)
            · exact (hctx M hM).mono (by omega))
        (fun y hy => by have := hΦb y hy; omega) hSb
      refine ⟨hbE, ?_⟩
      intro p hp
      simp only [Ty.freeVars, List.mem_dedup, List.mem_append, not_or]
      exact ⟨hbE p hp (Ty.fvar Φ), hbR p hp⟩
    | some _ hT =>
      expose_names
      have hΦb : ∀ y ∈ body.tyFreeVars, y < Φ := fun y hy => hΦ y (by
        show y ∈ (Expr.lambda (some paramTy) body).tyFreeVars
        simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append]; exact Or.inr hy)
      have hSb : ∀ p ∈ S, p.1 ∉ body.tyFreeVars := fun p hp hc => hSe p hp (by
        show p.1 ∈ (Expr.lambda (some paramTy) body).tyFreeVars
        simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append]; exact Or.inr hc)
      obtain ⟨hbE, hbR⟩ := Infer.eliminates hbody
        (by intro M hM; rcases List.mem_cons.mp hM with rfl | hM
            · exact Ty.BelowFvars.of_freeVars_lt (fun v hv => hΦ v (by
                show v ∈ (Expr.lambda (some paramTy) body).tyFreeVars
                simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append]; exact Or.inl hv))
            · exact hctx M hM)
        hΦb hSb
      refine ⟨hbE, ?_⟩
      intro p hp
      simp only [Ty.freeVars, List.mem_dedup, List.mem_append, not_or]
      exact ⟨hbE p hp paramTy, hbR p hp⟩
  | app hf harg huni =>
    expose_names
    have hfle := Infer.frontier_le hf
    have hargle := Infer.frontier_le harg
    have hΦf : ∀ y ∈ f.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      show y ∈ (Expr.app f arg).tyFreeVars; simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inl hy)
    have hΦa : ∀ y ∈ arg.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      show y ∈ (Expr.app f arg).tyFreeVars; simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inr hy)
    have hSf : ∀ p ∈ S₁, p.1 ∉ f.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ (List.mem_append_left _ hp)) (by
        show p.1 ∈ (Expr.app f arg).tyFreeVars; simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inl hc)
    have hSa1 : ∀ p ∈ S₁, p.1 ∉ arg.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ (List.mem_append_left _ hp)) (by
        show p.1 ∈ (Expr.app f arg).tyFreeVars; simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inr hc)
    have hSa : ∀ p ∈ S₂, p.1 ∉ arg.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ (List.mem_append_right _ hp)) (by
        show p.1 ∈ (Expr.app f arg).tyFreeVars; simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inr hc)
    obtain ⟨hfE, hfR⟩ := Infer.eliminates hf hctx hΦf hSf
    obtain ⟨hf_τbel, hf_sbel⟩ := Infer.belowFvars hf hctx hΦf
    have hctx1 : CtxBelow Φ₁ (S₁.onCtx ctx) := Subst.onCtx_below hf_sbel hfle hctx
    have hf_dom := Infer.dom_below hf hctx hΦf
    obtain ⟨haE, haR⟩ := Infer.eliminates harg hctx1
      (fun y hy => lt_of_lt_of_le (hΦa y hy) hfle) hSa
    have ha_dom := Infer.dom_below harg hctx1 (fun y hy => lt_of_lt_of_le (hΦa y hy) hfle)
    have hcross12 : ∀ p ∈ S₁, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := fun p hp q hq =>
      (Infer.eOut_avoid harg (w := p.1) (hf_dom p hp)
        (Subst.eliminates_onCtx (hfE p hp)) (hSa1 p hp)).1 q hq
    have h12E := Subst.eliminates_append hfE haE hcross12
    have hcross123 : ∀ p ∈ S₁ ++ S₂, ∀ q ∈ S₃, p.1 ∉ q.2.freeVars := by
      intro p hp q hq hwq
      have hrm := UnifyRel.range_mem huni q hq p.1 hwq
      rcases List.mem_append.mp hp with hpS₁ | hpS₂
      · have havoid := Infer.eOut_avoid harg (w := p.1) (hf_dom p hpS₁)
          (Subst.eliminates_onCtx (hfE p hpS₁)) (hSa1 p hpS₁)
        rcases hrm with h | h
        · rcases Subst.mem_freeVars_onTy h with h' | ⟨r, hr, hvr⟩
          · exact hfR p hpS₁ h'
          · exact havoid.1 r hr hvr
        · simp only [Ty.freeVars, List.mem_dedup, List.mem_append, List.mem_singleton] at h
          rcases h with h | h
          · exact havoid.2.1 h
          · have := hf_dom p hpS₁; omega
      · rcases hrm with h | h
        · exact haE p hpS₂ τf h
        · simp only [Ty.freeVars, List.mem_dedup, List.mem_append, List.mem_singleton] at h
          rcases h with h | h
          · exact haR p hpS₂ h
          · have := ha_dom p hpS₂; omega
    refine ⟨Subst.eliminates_append h12E (UnifyRel.eliminates huni) hcross123, ?_⟩
    intro p hp hc
    rcases List.mem_append.mp hp with hp12 | hpS₃
    · rcases Subst.mem_freeVars_onTy hc with h | ⟨q, hq, hvq⟩
      · simp only [Ty.freeVars, List.mem_singleton] at h
        rcases List.mem_append.mp hp12 with hpS₁ | hpS₂
        · have := hf_dom p hpS₁; omega
        · have := ha_dom p hpS₂; omega
      · exact hcross123 p hp12 q hq hvq
    · exact UnifyRel.eliminates huni p hpS₃ (Ty.fvar Φ₂) hc
  | letIn hrhs hbody =>
    expose_names
    have hrle := Infer.frontier_le hrhs
    have hΦr : ∀ y ∈ rhs.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      show y ∈ (Expr.letIn none rhs body).tyFreeVars
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append]; exact Or.inl hy)
    have hΦb : ∀ y ∈ body.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      show y ∈ (Expr.letIn none rhs body).tyFreeVars
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append]; exact Or.inr hy)
    have hSr : ∀ p ∈ S₁, p.1 ∉ rhs.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ hp) (by
        show p.1 ∈ (Expr.letIn none rhs body).tyFreeVars
        simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append]; exact Or.inl hc)
    have hSb1 : ∀ p ∈ S₁, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ hp) (by
        show p.1 ∈ (Expr.letIn none rhs body).tyFreeVars
        simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append]; exact Or.inr hc)
    have hSb : ∀ p ∈ S₂, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_right _ hp) (by
        show p.1 ∈ (Expr.letIn none rhs body).tyFreeVars
        simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append]; exact Or.inr hc)
    obtain ⟨hrE, hrR⟩ := Infer.eliminates hrhs hctx hΦr hSr
    obtain ⟨hr_τbel, hr_sbel⟩ := Infer.belowFvars hrhs hctx hΦr
    have hr_dom := Infer.dom_below hrhs hctx hΦr
    have hctxb : CtxBelow Φ₁ { (S₁.onCtx ctx) with
        env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env } := by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact hr_τbel.closeOver
      · exact (Subst.onCtx_below hr_sbel hrle hctx) M hM
    obtain ⟨hbE, hbR⟩ := Infer.eliminates hbody hctxb
      (fun y hy => lt_of_lt_of_le (hΦb y hy) hrle) hSb
    have hbavoid : ∀ p ∈ S₁, ∀ M ∈ { (S₁.onCtx ctx) with
        env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env }.env,
        p.1 ∉ M.body.freeVars := by
      intro p hp M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact fun hc => hrR p hp (Ty.freeVars_closeOver_subset hc)
      · exact Subst.eliminates_onCtx (hrE p hp) M hM
    have hcross : ∀ p ∈ S₁, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := fun p hp q hq =>
      (Infer.eOut_avoid hbody (w := p.1) (hr_dom p hp) (hbavoid p hp) (hSb1 p hp)).1 q hq
    refine ⟨Subst.eliminates_append hrE hbE hcross, ?_⟩
    intro p hp
    rcases List.mem_append.mp hp with hpS₁ | hpS₂
    · exact (Infer.eOut_avoid hbody (w := p.1) (hr_dom p hpS₁) (hbavoid p hpS₁) (hSb1 p hpS₁)).2.1
    · exact hbR p hpS₂
  | letInAnn hσwf hΦN hrhs huni hesc1 _hesc2 hbody =>
    expose_names
    have hrle := Infer.frontier_le hrhs
    have hΦσ : ∀ y ∈ σ.body.freeVars, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append]; exact Or.inl (Or.inl hy))
    have hΦbody : ∀ y ∈ body.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append]; exact Or.inr hy)
    have hΦropen : ∀ y ∈ (rhs.openTyVars (freshVars N σ.paramCount)).tyFreeVars, y < N + σ.paramCount := by
      intro y hy
      rcases Expr.tyFreeVars_openTyVars hy with h | h
      · have := hΦ y (by simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append]
                         exact Or.inl (Or.inr h)); omega
      · have := freshVars_lt y h; omega
    have hSσ1 : ∀ p ∈ S₁, p.1 ∉ σ.body.freeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ (List.mem_append_left _ hp)) (by
        simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append]; exact Or.inl (Or.inl hc))
    have hSσall : ∀ p ∈ S₁ ++ Schk, p.1 ∉ σ.body.freeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ hp) (by
        simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append]; exact Or.inl (Or.inl hc))
    have hSbody : ∀ p ∈ S₁ ++ Schk, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ hp) (by
        simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append]; exact Or.inr hc)
    have hSb2 : ∀ p ∈ S₂, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_right _ hp) (by
        simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append]; exact Or.inr hc)
    have hSropen : ∀ p ∈ S₁, p.1 ∉ (rhs.openTyVars (freshVars N σ.paramCount)).tyFreeVars := by
      intro p hp hc
      rcases Expr.tyFreeVars_openTyVars hc with h | h
      · exact hSe p (List.mem_append_left _ (List.mem_append_left _ hp)) (by
          simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append]; exact Or.inl (Or.inr h))
      · exact hesc1 p.1 h (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩)
    have hctx_pc : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hctx M hM).mono (by omega)
    obtain ⟨hrE, hrR⟩ := Infer.eliminates hrhs hctx_pc hΦropen hSropen
    obtain ⟨hr_τbel, hr_sbel⟩ := Infer.belowFvars hrhs hctx_pc hΦropen
    have hr_dom := Infer.dom_below hrhs hctx_pc hΦropen
    have hσopenbel : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
      Ty.openVars_belowFvars ((Ty.BelowFvars.of_freeVars_lt hΦσ).mono (by omega))
        (fun x hx => by have := freshVars_lt x hx; omega)
    have hSchkbel : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hr_τbel hσopenbel
    have hSchkdom : ∀ p ∈ Schk, p.1 < Φ₁ := by
      intro p hp
      rcases UnifyRel.dom_mem huni p hp with h | h
      · exact hr_τbel.mem_lt p.1 h
      · exact hσopenbel.mem_lt p.1 h
    have hctxb : CtxBelow Φ₁ { (Schk.onCtx (S₁.onCtx ctx)) with
        env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env } := by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact (Ty.BelowFvars.of_freeVars_lt hΦσ).mono (by omega)
      · exact (Subst.onCtx_below hSchkbel (le_refl _) (Subst.onCtx_below hr_sbel hrle hctx_pc)) M hM
    obtain ⟨hbE, hbR⟩ := Infer.eliminates hbody hctxb
      (fun y hy => lt_of_lt_of_le (hΦbody y hy) (by omega)) hSb2
    have cross1 : ∀ p ∈ S₁, ∀ q ∈ Schk, p.1 ∉ q.2.freeVars := by
      intro p hp q hq hwq
      rcases UnifyRel.range_mem huni q hq p.1 hwq with h | h
      · exact hrR p hp h
      · rcases Ty.freeVars_openVars_subset p.1 h with h' | h'
        · exact hSσ1 p hp h'
        · exact hesc1 p.1 h' (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩)
    have hE1Schk := Subst.eliminates_append hrE (UnifyRel.eliminates huni) cross1
    have hdomall : ∀ p ∈ S₁ ++ Schk, p.1 < Φ₁ := by
      intro p hp; rcases List.mem_append.mp hp with h | h
      · exact hr_dom p h
      · exact hSchkdom p h
    have hbodyctx : ∀ p ∈ S₁ ++ Schk, ∀ M ∈ ({ (Schk.onCtx (S₁.onCtx ctx)) with
        env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env } : Ctx).env, p.1 ∉ M.body.freeVars := by
      intro p hp M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact hSσall p hp
      · rcases List.mem_append.mp hp with hpS₁ | hpSchk
        · exact Subst.onCtx_avoid (Subst.eliminates_onCtx (hrE p hpS₁)) (cross1 p hpS₁) M hM
        · exact Subst.eliminates_onCtx (UnifyRel.eliminates huni p hpSchk) M hM
    have cross2 : ∀ p ∈ S₁ ++ Schk, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := fun p hp q hq =>
      (Infer.eOut_avoid hbody (w := p.1) (hdomall p hp) (hbodyctx p hp) (hSbody p hp)).1 q hq
    refine ⟨Subst.eliminates_append hE1Schk hbE cross2, ?_⟩
    intro p hp
    rcases List.mem_append.mp hp with hp1Schk | hpS₂
    · exact (Infer.eOut_avoid hbody (w := p.1) (hdomall p hp1Schk)
        (hbodyctx p hp1Schk) (hSbody p hp1Schk)).2.1
    · exact hbR p hpS₂
  | match_ hscrut hne hbr =>
    expose_names
    have hle1 := Infer.frontier_le hscrut
    have hΦs : ∀ y ∈ scrut.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inl hy)
    have hΦbr : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars branches, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inr hy)
    have hSs : ∀ p ∈ S₁, p.1 ∉ scrut.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ hp) (by simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inl hc)
    have hSs1br : ∀ p ∈ S₁, p.1 ∉ Expr.tyFreeVars.BranchList.tyFreeVars branches := fun p hp hc =>
      hSe p (List.mem_append_left _ hp) (by simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inr hc)
    have hSbr : ∀ p ∈ S₂, p.1 ∉ Expr.tyFreeVars.BranchList.tyFreeVars branches := fun p hp hc =>
      hSe p (List.mem_append_right _ hp) (by simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inr hc)
    obtain ⟨hsE, hsR⟩ := Infer.eliminates hscrut hctx hΦs hSs
    obtain ⟨hs_τbel, hs_sbel⟩ := Infer.belowFvars hscrut hctx hΦs
    have hs_dom := Infer.dom_below hscrut hctx hΦs
    have hbrctx : CtxBelow (Φ₁ + 1) (S₁.onCtx ctx) :=
      Subst.onCtx_below (fun p hp => (hs_sbel p hp).mono (by omega)) (by omega) hctx
    have hbrE := InferBranches.eliminates hbr hbrctx (hs_τbel.mono (by omega)) (.fvar (by omega))
      (fun y hy => by have := hΦbr y hy; omega) hSbr
    have hcross : ∀ p ∈ S₁, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := fun p hp q hq =>
      (InferBranches.eOut_avoid hbr (w := p.1) (by have := hs_dom p hp; omega)
        (Subst.eliminates_onCtx (hsE p hp)) (hsR p hp)
        (by intro hc; simp only [Ty.freeVars, List.mem_singleton] at hc; have := hs_dom p hp; omega)
        (hSs1br p hp)).1 q hq
    refine ⟨Subst.eliminates_append hsE hbrE hcross, ?_⟩
    intro p hp
    rcases List.mem_append.mp hp with hpS₁ | hpS₂
    · exact (InferBranches.eOut_avoid hbr (w := p.1) (by have := hs_dom p hpS₁; omega)
        (Subst.eliminates_onCtx (hsE p hpS₁)) (hsR p hpS₁)
        (by intro hc; simp only [Ty.freeVars, List.mem_singleton] at hc; have := hs_dom p hpS₁; omega)
        (hSs1br p hpS₁)).2.1
    · exact hbrE p hpS₂ (Ty.fvar Φ₁)
  | letRec hgroup hbody =>
    expose_names
    have hgle := InferRecGroup.frontier_le hgroup
    have hΦbind : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inl hy)
    have hΦbody : ∀ y ∈ body.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inr hy)
    have hSbind : ∀ p ∈ S₁, p.1 ∉ Expr.tyFreeVars.RecGroup.tyFreeVars bindings := fun p hp hc =>
      hSe p (List.mem_append_left _ hp) (by simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inl hc)
    have hSbody1 : ∀ p ∈ S₁, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ hp) (by simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inr hc)
    have hSbody2 : ∀ p ∈ S₂, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_right _ hp) (by simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inr hc)
    have hctxβ : CtxBelow (Φ + bindings.length) { ctx with
        env := (freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i)) ++ ctx.env } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
        exact .fvar (by have := freshVars_lt x hx; omega)
      · exact (hctx M hM).mono (by omega)
    have htgβ : ∀ β ∈ (freshVars Φ bindings.length).map Ty.fvar, Ty.BelowFvars (Φ + bindings.length) β := by
      intro β hβ; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hβ
      exact .fvar (by have := freshVars_lt x hx; omega)
    have hgE := InferRecGroup.eliminates hgroup hctxβ htgβ
      (fun y hy => by have := hΦbind y hy; omega) hSbind
    have hg_dom := InferRecGroup.dom_below hgroup hctxβ htgβ (fun y hy => by have := hΦbind y hy; omega)
    have hS₁bel := InferRecGroup.belowFvars hgroup hctxβ htgβ (fun y hy => by have := hΦbind y hy; omega)
    have hctxbody : CtxBelow Φ₁ { (S₁.onCtx ctx) with
        env := genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
                 ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)))
               ++ (S₁.onCtx ctx).env } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · simp only [genGroupSchemes] at hM
        obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
        obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
        simp only [PolyTy.genGroup]
        exact (Subst.onTy_belowFvars hS₁bel (.fvar (by have := freshVars_lt x hx; omega))).closeOver
      · exact (Subst.onCtx_below hS₁bel (by omega) hctx) M hM
    obtain ⟨hbE, hbR⟩ := Infer.eliminates hbody hctxbody
      (fun y hy => lt_of_lt_of_le (hΦbody y hy) (by omega)) hSbody2
    have hbodyctx : ∀ p ∈ S₁, ∀ M ∈ ({ (S₁.onCtx ctx) with
        env := genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
                 ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)))
               ++ (S₁.onCtx ctx).env } : Ctx).env, p.1 ∉ M.body.freeVars := by
      intro p hp M hM; rcases List.mem_append.mp hM with hM | hM
      · simp only [genGroupSchemes] at hM
        obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
        obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
        simp only [PolyTy.genGroup]
        exact fun hc => hgE p hp (Ty.fvar x) (Ty.freeVars_closeOver_subset hc)
      · exact Subst.eliminates_onCtx (hgE p hp) M hM
    have hcross : ∀ p ∈ S₁, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := fun p hp q hq =>
      (Infer.eOut_avoid hbody (w := p.1) (hg_dom p hp) (hbodyctx p hp) (hSbody1 p hp)).1 q hq
    refine ⟨Subst.eliminates_append hgE hbE hcross, ?_⟩
    intro p hp
    rcases List.mem_append.mp hp with hpS₁ | hpS₂
    · exact (Infer.eOut_avoid hbody (w := p.1) (hg_dom p hpS₁) (hbodyctx p hpS₁) (hSbody1 p hpS₁)).2.1
    · exact hbR p hpS₂
  | letRecAnn hwf hlen hgroup hbody =>
    expose_names
    have hgle := InferRecGroupAnn.frontier_le hgroup
    have hΦsch : ∀ y ∈ Expr.tyFreeVars.SchemeList.tyFreeVars schemes, y < Φ := fun y hy =>
      hΦ y (by simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inl (Or.inl hy))
    have hΦbind : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ := fun y hy =>
      hΦ y (by simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inl (Or.inr hy))
    have hΦbody : ∀ y ∈ body.tyFreeVars, y < Φ := fun y hy =>
      hΦ y (by simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inr hy)
    have hsch : ∀ σ ∈ schemes, Ty.BelowFvars Φ σ.body := fun σ hσ =>
      Ty.BelowFvars.of_freeVars_lt
        (fun v hv => hΦsch v (Expr.scheme_body_mem_schemeList_tyFreeVars hσ hv))
    have hctxσ : CtxBelow Φ { ctx with env := schemes ++ ctx.env } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hsch M hM
      · exact hctx M hM
    have hSbind : ∀ p ∈ S₁, p.1 ∉ Expr.tyFreeVars.RecGroup.tyFreeVars bindings := fun p hp hc =>
      hSe p (List.mem_append_left _ hp) (by
        simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inl (Or.inr hc))
    have hSsch : ∀ p ∈ S₁, p.1 ∉ Expr.tyFreeVars.SchemeList.tyFreeVars schemes := fun p hp hc =>
      hSe p (List.mem_append_left _ hp) (by
        simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inl (Or.inl hc))
    have hSbody1 : ∀ p ∈ S₁, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ hp) (by
        simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inr hc)
    have hSbody2 : ∀ p ∈ S₂, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_right _ hp) (by
        simp only [Expr.tyFreeVars, List.mem_append]; exact Or.inr hc)
    have hgE := InferRecGroupAnn.eliminates hgroup hctxσ hsch hΦbind hSbind hSsch
    have hg_dom := InferRecGroupAnn.dom_below hgroup hctxσ hsch hΦbind
    have hS₁bel := InferRecGroupAnn.belowFvars hgroup hctxσ hsch hΦbind
    have hctxbody : CtxBelow Φ₁ (S₁.onCtx { ctx with env := schemes ++ ctx.env }) :=
      Subst.onCtx_below hS₁bel hgle hctxσ
    obtain ⟨hbE, hbR⟩ := Infer.eliminates hbody hctxbody
      (fun y hy => lt_of_lt_of_le (hΦbody y hy) hgle) hSbody2
    have hbodyctx : ∀ p ∈ S₁, ∀ M ∈ (S₁.onCtx { ctx with env := schemes ++ ctx.env }).env,
        p.1 ∉ M.body.freeVars := fun p hp => Subst.eliminates_onCtx (hgE p hp)
    have hcross : ∀ p ∈ S₁, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := fun p hp q hq =>
      (Infer.eOut_avoid hbody (w := p.1) (hg_dom p hp) (hbodyctx p hp) (hSbody1 p hp)).1 q hq
    refine ⟨Subst.eliminates_append hgE hbE hcross, ?_⟩
    intro p hp
    rcases List.mem_append.mp hp with hpS₁ | hpS₂
    · exact (Infer.eOut_avoid hbody (w := p.1) (hg_dom p hpS₁) (hbodyctx p hpS₁) (hSbody1 p hpS₁)).2.1
    · exact hbR p hpS₂
termination_by e.size
decreasing_by all_goals (try subst_vars; try simp only [Expr.size, Expr.size_openTyVars]; omega)
theorem InferBranches.eliminates {Φ ctx scrutTy ρ brs Φ' S brsOut}
    (h : InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut)
    (hctx : CtxBelow Φ ctx) (hsc : Ty.BelowFvars Φ scrutTy) (hρ : Ty.BelowFvars Φ ρ)
    (hΦ : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars brs, y < Φ)
    (hSe : ∀ p ∈ S, p.1 ∉ Expr.tyFreeVars.BranchList.tyFreeVars brs) :
    (∀ p ∈ S, ∀ x : Ty, p.1 ∉ (S.onTy x).freeVars) := by
  cases h with
  | nil => simp
  | cons hlook hn huni0 hbody huni hrest =>
    expose_names
    have hle0 : Φ + ctor.paramCount ≤ Φ₁ := Infer.frontier_le hbody
    have hΦhead : ∀ y ∈ body.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy)
    have hΦrest : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars rest, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hy)
    have hSbody0 : ∀ p ∈ S₁, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSe p (by simp only [List.mem_append]; tauto) (by
        simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc)
    have hSrestAll : ∀ p ∈ (S₀ ++ S₁) ++ S₂, p.1 ∉ Expr.tyFreeVars.BranchList.tyFreeVars rest :=
      fun p hp hc => hSe p (List.mem_append_left _ hp) (by
        simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hc)
    have hSrest3 : ∀ p ∈ S₃, p.1 ∉ Expr.tyFreeVars.BranchList.tyFreeVars rest := fun p hp hc =>
      hSe p (List.mem_append_right _ hp) (by
        simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hc)
    -- S₀
    have hS₀bel : ∀ p ∈ S₀, Ty.BelowFvars (Φ + ctor.paramCount) p.2 :=
      UnifyRel.belowFvars huni0 (hsc.mono (by omega))
        (.customTy (fun t ht => by obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
                                   exact .fvar (by have := freshVars_lt x hx; omega)))
    have hS₀dom : ∀ p ∈ S₀, p.1 < Φ + ctor.paramCount := by
      intro p hp; rcases UnifyRel.dom_mem huni0 p hp with h | h
      · exact (hsc.mono (show Φ ≤ Φ + ctor.paramCount by omega)).mem_lt p.1 h
      · simp only [Ty.freeVars] at h; rw [mem_TyList_freeVars] at h
        obtain ⟨t, ht, hgt⟩ := h; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
        simp only [Ty.freeVars, List.mem_singleton] at hgt; have := freshVars_lt x hx; omega
    have hbctx := branchBindings_below (ctorr := ctor)
      (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
      (Subst.onCtx_below hS₀bel (by omega) hctx)
      (fun t ht => by obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
                      obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv
                      exact Subst.onTy_belowFvars hS₀bel (.fvar (by have := freshVars_lt x hx; omega)))
    obtain ⟨hbE, hbR⟩ := Infer.eliminates hbody hbctx (fun y hy => by have := hΦhead y hy; omega) hSbody0
    obtain ⟨hb_τbel, hb_sbel⟩ := Infer.belowFvars hbody hbctx (fun y hy => by have := hΦhead y hy; omega)
    have hb_dom := Infer.dom_below hbody hbctx (fun y hy => by have := hΦhead y hy; omega)
    have hbranchAvoid : ∀ p ∈ S₀, ∀ M ∈ ({ (S₀.onCtx ctx) with
        env := (ctor.contents.map (Ty.openWith (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
          ++ (S₀.onCtx ctx).env } : Ctx).env, p.1 ∉ M.body.freeVars := by
      intro p hp M hM; rcases List.mem_append.mp hM with hM | hM
      · obtain ⟨t, ht, rfl⟩ := List.mem_map.mp hM
        obtain ⟨c, hc, rfl⟩ := List.mem_map.mp ht
        simp only [PolyTy.mkTrivial]
        refine Ty.not_mem_freeVars_openWith ?_ ((ctor.closed c hc).not_mem_freeVars p.1)
        intro v hv
        obtain ⟨v0, hv0, rfl⟩ := List.mem_map.mp hv
        obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv0
        exact UnifyRel.eliminates huni0 p hp (Ty.fvar x)
      · exact Subst.eliminates_onCtx (UnifyRel.eliminates huni0 p hp) M hM
    have heOutBody : ∀ p ∈ S₀, (∀ q ∈ S₁, p.1 ∉ q.2.freeVars) ∧ p.1 ∉ τb.freeVars ∧
        p.1 ∉ bodyOut.tyFreeVars := fun p hp =>
      Infer.eOut_avoid hbody (w := p.1) (hS₀dom p hp) (hbranchAvoid p hp) (by
        intro hc; exact hSe p (by simp only [List.mem_append]; tauto) (by
          simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc))
    -- E01
    have cross01 : ∀ p ∈ S₀, ∀ q ∈ S₁, p.1 ∉ q.2.freeVars := fun p hp q hq => (heOutBody p hp).1 q hq
    have hE01 := Subst.eliminates_append (UnifyRel.eliminates huni0) hbE cross01
    -- S₂
    have hρbel : Ty.BelowFvars (Φ + ctor.paramCount) ρ := hρ.mono (by omega)
    have hS₀ρbel : Ty.BelowFvars Φ₁ (S₀.onTy ρ) :=
      (Subst.onTy_belowFvars hS₀bel hρbel).mono hle0
    have hS₁S₀ρbel : Ty.BelowFvars Φ₁ (S₁.onTy (S₀.onTy ρ)) := Subst.onTy_belowFvars hb_sbel hS₀ρbel
    have hS₂bel : ∀ p ∈ S₂, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hb_τbel hS₁S₀ρbel
    have hS₂dom : ∀ p ∈ S₂, p.1 < Φ₁ := by
      intro p hp; rcases UnifyRel.dom_mem huni p hp with h | h
      · exact hb_τbel.mem_lt p.1 h
      · exact hS₁S₀ρbel.mem_lt p.1 h
    have cross012 : ∀ p ∈ S₀ ++ S₁, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := by
      intro p hp q hq hwq
      rcases UnifyRel.range_mem huni q hq p.1 hwq with h | h
      · rcases List.mem_append.mp hp with hpS₀ | hpS₁
        · exact (heOutBody p hpS₀).2.1 h
        · exact hbR p hpS₁ h
      · exact (Subst.onTy_append S₀ S₁ ρ ▸ hE01 p hp ρ) h
    have hE012 := Subst.eliminates_append hE01 (UnifyRel.eliminates huni) cross012
    -- rest
    have hle1 := InferBranches.frontier_le hrest
    have hscrut'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (S₀.onTy scrutTy))) :=
      Subst.onTy_belowFvars hS₂bel (Subst.onTy_belowFvars hb_sbel
        ((Subst.onTy_belowFvars hS₀bel (hsc.mono (by omega))).mono hle0))
    have hρ'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (S₀.onTy ρ))) :=
      Subst.onTy_belowFvars hS₂bel hS₁S₀ρbel
    have hctx1 : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx))) :=
      Subst.onCtx_below hS₂bel (le_refl _) (Subst.onCtx_below hb_sbel (le_refl _)
        (Subst.onCtx_below (fun p hp => (hS₀bel p hp).mono hle0) (by omega) hctx))
    have hrestE := InferBranches.eliminates hrest hctx1 hscrut'bel hρ'bel
      (fun y hy => by have := hΦrest y hy; omega) hSrest3
    have happ3 : ∀ x : Ty, ((S₀ ++ S₁) ++ S₂).onTy x = S₂.onTy (S₁.onTy (S₀.onTy x)) :=
      fun x => by rw [Subst.onTy_append, Subst.onTy_append]
    have honCtx3 : ((S₀ ++ S₁) ++ S₂).onCtx ctx = S₂.onCtx (S₁.onCtx (S₀.onCtx ctx)) := by
      rw [Subst.onCtx_append, Subst.onCtx_append]
    have cross0123 : ∀ p ∈ (S₀ ++ S₁) ++ S₂, ∀ q ∈ S₃, p.1 ∉ q.2.freeVars := by
      intro p hp q hq
      have hpdom : p.1 < Φ₁ := by
        rcases List.mem_append.mp hp with hp01 | hpS₂
        · rcases List.mem_append.mp hp01 with hpS₀ | hpS₁
          · have := hS₀dom p hpS₀; omega
          · exact hb_dom p hpS₁
        · exact hS₂dom p hpS₂
      refine (InferBranches.eOut_avoid hrest (w := p.1) hpdom
        (honCtx3 ▸ Subst.eliminates_onCtx (hE012 p hp))
        (happ3 scrutTy ▸ hE012 p hp scrutTy)
        (happ3 ρ ▸ hE012 p hp ρ) (hSrestAll p hp)).1 q hq
    exact Subst.eliminates_append hE012 hrestE cross0123
  | consWild hbody huni hrest =>
    expose_names
    have hle1 := Infer.frontier_le hbody
    have hΦhead : ∀ y ∈ body.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy)
    have hΦrest : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars rest, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hy)
    have hSbody0 : ∀ p ∈ S₁, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ (List.mem_append_left _ hp)) (by
        simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc)
    have hSrestAll : ∀ p ∈ S₁ ++ S₂, p.1 ∉ Expr.tyFreeVars.BranchList.tyFreeVars rest :=
      fun p hp hc => hSe p (List.mem_append_left _ hp) (by
        simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hc)
    have hSrest3 : ∀ p ∈ S₃, p.1 ∉ Expr.tyFreeVars.BranchList.tyFreeVars rest := fun p hp hc =>
      hSe p (List.mem_append_right _ hp) (by
        simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hc)
    obtain ⟨hbE, hbR⟩ := Infer.eliminates hbody hctx hΦhead hSbody0
    obtain ⟨hb_τbel, hb_sbel⟩ := Infer.belowFvars hbody hctx hΦhead
    have hb_dom := Infer.dom_below hbody hctx hΦhead
    have hS₁ρbel : Ty.BelowFvars Φ₁ (S₁.onTy ρ) := Subst.onTy_belowFvars hb_sbel (hρ.mono hle1)
    have hS₂bel : ∀ p ∈ S₂, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hb_τbel hS₁ρbel
    have hS₂dom : ∀ p ∈ S₂, p.1 < Φ₁ := by
      intro p hp; rcases UnifyRel.dom_mem huni p hp with h | h
      · exact hb_τbel.mem_lt p.1 h
      · exact hS₁ρbel.mem_lt p.1 h
    have cross1 : ∀ p ∈ S₁, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := by
      intro p hp q hq hwq
      rcases UnifyRel.range_mem huni q hq p.1 hwq with h | h
      · exact hbR p hp h
      · exact hbE p hp ρ h
    have hE12 := Subst.eliminates_append hbE (UnifyRel.eliminates huni) cross1
    have hctx1 : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx ctx)) :=
      Subst.onCtx_below hS₂bel (le_refl _) (Subst.onCtx_below hb_sbel hle1 hctx)
    have hrestE := InferBranches.eliminates hrest hctx1
      (Subst.onTy_belowFvars hS₂bel (Subst.onTy_belowFvars hb_sbel (hsc.mono hle1)))
      (Subst.onTy_belowFvars hS₂bel hS₁ρbel)
      (fun y hy => by have := hΦrest y hy; omega) hSrest3
    have happ2 : ∀ x : Ty, (S₁ ++ S₂).onTy x = S₂.onTy (S₁.onTy x) :=
      fun x => by rw [Subst.onTy_append]
    have honCtx2 : (S₁ ++ S₂).onCtx ctx = S₂.onCtx (S₁.onCtx ctx) := by rw [Subst.onCtx_append]
    have cross2 : ∀ p ∈ S₁ ++ S₂, ∀ q ∈ S₃, p.1 ∉ q.2.freeVars := by
      intro p hp q hq
      have hpdom : p.1 < Φ₁ := by
        rcases List.mem_append.mp hp with hpS₁ | hpS₂
        · exact hb_dom p hpS₁
        · exact hS₂dom p hpS₂
      refine (InferBranches.eOut_avoid hrest (w := p.1) hpdom
        (honCtx2 ▸ Subst.eliminates_onCtx (hE12 p hp))
        (happ2 scrutTy ▸ hE12 p hp scrutTy)
        (happ2 ρ ▸ hE12 p hp ρ) (hSrestAll p hp)).1 q hq
    exact Subst.eliminates_append hE12 hrestE cross2
termination_by Expr.sizeBranches brs
decreasing_by all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)
theorem InferRecGroup.eliminates {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut)
    (hctx : CtxBelow Φ ctx) (htg : ∀ β ∈ targets, Ty.BelowFvars Φ β)
    (hΦ : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ)
    (hSe : ∀ p ∈ S, p.1 ∉ Expr.tyFreeVars.RecGroup.tyFreeVars bindings) :
    (∀ p ∈ S, ∀ x : Ty, p.1 ∉ (S.onTy x).freeVars) := by
  cases h with
  | nil => simp
  | cons he huni hrest =>
    expose_names
    have hle1 := Infer.frontier_le he
    have hΦe : ∀ y ∈ e.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inl hy)
    have hΦrest : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars rest, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inr hy)
    have hSe1 : ∀ p ∈ S₁, p.1 ∉ e.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ (List.mem_append_left _ hp)) (by
        simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inl hc)
    have hSrestAll : ∀ p ∈ S₁ ++ S₂, p.1 ∉ Expr.tyFreeVars.RecGroup.tyFreeVars rest := fun p hp hc =>
      hSe p (List.mem_append_left _ hp) (by
        simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inr hc)
    have hSrest3 : ∀ p ∈ S₃, p.1 ∉ Expr.tyFreeVars.RecGroup.tyFreeVars rest := fun p hp hc =>
      hSe p (List.mem_append_right _ hp) (by
        simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inr hc)
    obtain ⟨heE, heR⟩ := Infer.eliminates he hctx hΦe hSe1
    obtain ⟨he_τbel, he_sbel⟩ := Infer.belowFvars he hctx hΦe
    have he_dom := Infer.dom_below he hctx hΦe
    have hβbel : Ty.BelowFvars Φ β := htg β (List.mem_cons_self ..)
    have hS₁βbel : Ty.BelowFvars Φ₁ (S₁.onTy β) := Subst.onTy_belowFvars he_sbel (hβbel.mono hle1)
    have hS₂bel : ∀ p ∈ S₂, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni he_τbel hS₁βbel
    have hS₂dom : ∀ p ∈ S₂, p.1 < Φ₁ := by
      intro p hp; rcases UnifyRel.dom_mem huni p hp with h | h
      · exact he_τbel.mem_lt p.1 h
      · exact hS₁βbel.mem_lt p.1 h
    have hctx1 : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx ctx)) :=
      Subst.onCtx_below hS₂bel (le_refl _) (Subst.onCtx_below he_sbel hle1 hctx)
    have htgrest : ∀ β' ∈ βs.map (fun b => S₂.onTy (S₁.onTy b)), Ty.BelowFvars Φ₁ β' := by
      intro β' hβ'; obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hβ'
      exact Subst.onTy_belowFvars hS₂bel
        (Subst.onTy_belowFvars he_sbel ((htg b (List.mem_cons_of_mem _ hb)).mono hle1))
    have hrestE := InferRecGroup.eliminates hrest hctx1 htgrest
      (fun y hy => by have := hΦrest y hy; omega) hSrest3
    have cross_e_S₂ : ∀ p ∈ S₁, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := by
      intro p hp q hq hwq
      rcases UnifyRel.range_mem huni q hq p.1 hwq with h | h
      · exact heR p hp h
      · exact heE p hp β h
    have hE1S₂ := Subst.eliminates_append heE (UnifyRel.eliminates huni) cross_e_S₂
    have cross_S₁S₂_S₃ : ∀ p ∈ S₁ ++ S₂, ∀ q ∈ S₃, p.1 ∉ q.2.freeVars := by
      intro p hp q hq
      have hpdom : p.1 < Φ₁ := by
        rcases List.mem_append.mp hp with h | h
        · exact he_dom p h
        · exact hS₂dom p h
      have hpctx : ∀ M ∈ (S₂.onCtx (S₁.onCtx ctx)).env, p.1 ∉ M.body.freeVars := by
        rcases List.mem_append.mp hp with h | h
        · exact Subst.onCtx_avoid (Subst.eliminates_onCtx (heE p h)) (cross_e_S₂ p h)
        · exact Subst.eliminates_onCtx (UnifyRel.eliminates huni p h)
      have hptg : ∀ β' ∈ βs.map (fun b => S₂.onTy (S₁.onTy b)), p.1 ∉ β'.freeVars := by
        intro β' hβ'; obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hβ'
        exact Subst.onTy_append S₁ S₂ b ▸ hE1S₂ p hp b
      exact (InferRecGroup.eOut_avoid hrest (w := p.1) hpdom hpctx hptg (hSrestAll p hp)).1 q hq
    exact Subst.eliminates_append hE1S₂ hrestE cross_S₁S₂_S₃
termination_by Expr.sizeRecGroup bindings
decreasing_by all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)
theorem InferRecGroupAnn.eliminates {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut)
    (hctx : CtxBelow Φ ctx) (hsch : ∀ σ ∈ schemes, Ty.BelowFvars Φ σ.body)
    (hΦ : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ)
    (hSe : ∀ p ∈ S, p.1 ∉ Expr.tyFreeVars.RecGroup.tyFreeVars bindings)
    (hSsch : ∀ p ∈ S, p.1 ∉ Expr.tyFreeVars.SchemeList.tyFreeVars schemes) :
    (∀ p ∈ S, ∀ x : Ty, p.1 ∉ (S.onTy x).freeVars) := by
  cases h with
  | nil => simp
  | cons hΦN hinfer huni hesc1 _ hrest =>
    expose_names
    have hrle := Infer.frontier_le hinfer
    have hσBel : Ty.BelowFvars Φ σ.body := hsch σ List.mem_cons_self
    have hΦe : ∀ y ∈ e.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inl hy)
    have hΦrest : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars rest, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inr hy)
    have hΦeopen : ∀ y ∈ (e.openTyVars (freshVars N σ.paramCount)).tyFreeVars, y < N + σ.paramCount := by
      intro y hy
      rcases Expr.tyFreeVars_openTyVars hy with h | h
      · have := hΦe y h; omega
      · have := freshVars_lt y h; omega
    have hSeopen : ∀ p ∈ S₁, p.1 ∉ (e.openTyVars (freshVars N σ.paramCount)).tyFreeVars := by
      intro p hp hc
      rcases Expr.tyFreeVars_openTyVars hc with h | h
      · exact hSe p (List.mem_append_left _ (List.mem_append_left _ hp)) (by
          simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inl h)
      · exact hesc1 p.1 h (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩)
    have hSσ1 : ∀ p ∈ S₁, p.1 ∉ σ.body.freeVars := fun p hp hc =>
      hSsch p (List.mem_append_left _ (List.mem_append_left _ hp))
        (Expr.scheme_body_mem_schemeList_tyFreeVars List.mem_cons_self hc)
    have hctx_pc : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hctx M hM).mono (by omega)
    obtain ⟨hrE, hrR⟩ := Infer.eliminates hinfer hctx_pc hΦeopen hSeopen
    obtain ⟨hr_τbel, hr_sbel⟩ := Infer.belowFvars hinfer hctx_pc hΦeopen
    have hr_dom := Infer.dom_below hinfer hctx_pc hΦeopen
    have hσopenbel : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
      Ty.openVars_belowFvars (hσBel.mono (by omega))
        (fun x hx => by have := freshVars_lt x hx; omega)
    have hSchkbel : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hr_τbel hσopenbel
    have hSchkdom : ∀ p ∈ Schk, p.1 < Φ₁ := by
      intro p hp; rcases UnifyRel.dom_mem huni p hp with h | h
      · exact hr_τbel.mem_lt p.1 h
      · exact hσopenbel.mem_lt p.1 h
    have cross1 : ∀ p ∈ S₁, ∀ q ∈ Schk, p.1 ∉ q.2.freeVars := by
      intro p hp q hq hwq
      rcases UnifyRel.range_mem huni q hq p.1 hwq with h | h
      · exact hrR p hp h
      · rcases Ty.freeVars_openVars_subset p.1 h with h' | h'
        · exact hSσ1 p hp h'
        · exact hesc1 p.1 h' (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩)
    have hE1Schk := Subst.eliminates_append hrE (UnifyRel.eliminates huni) cross1
    have hdomall : ∀ p ∈ S₁ ++ Schk, p.1 < Φ₁ := by
      intro p hp; rcases List.mem_append.mp hp with h | h
      · exact hr_dom p h
      · exact hSchkdom p h
    have hctx1 : CtxBelow Φ₁ (Schk.onCtx (S₁.onCtx ctx)) :=
      Subst.onCtx_below hSchkbel (le_refl _) (Subst.onCtx_below hr_sbel hrle hctx_pc)
    have hΦΦ₁ : Φ ≤ Φ₁ := by omega
    have hrestE := InferRecGroupAnn.eliminates hrest hctx1
      (fun σ' hσ' => (hsch σ' (List.mem_cons_of_mem _ hσ')).mono hΦΦ₁)
      (fun y hy => lt_of_lt_of_le (hΦrest y hy) hΦΦ₁)
      (fun p hp hc => hSe p (List.mem_append_right _ hp) (by
        simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inr hc))
      (fun p hp hc => hSsch p (List.mem_append_right _ hp) (by
        simp only [Expr.tyFreeVars.SchemeList.tyFreeVars, List.mem_append]; exact Or.inr hc))
    have cross2 : ∀ p ∈ S₁ ++ Schk, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := by
      intro p hp q hq
      have hpctx : ∀ M ∈ (Schk.onCtx (S₁.onCtx ctx)).env, p.1 ∉ M.body.freeVars := by
        rw [show Schk.onCtx (S₁.onCtx ctx) = (S₁ ++ Schk).onCtx ctx
              from (Subst.onCtx_append S₁ Schk ctx).symm]
        exact Subst.eliminates_onCtx (hE1Schk p hp)
      have hprest : p.1 ∉ Expr.tyFreeVars.RecGroup.tyFreeVars rest := fun h' =>
        hSe p (List.mem_append_left _ hp) (by
          simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inr h')
      exact (InferRecGroupAnn.eOut_avoid hrest (w := p.1) (hdomall p hp) hpctx
        (fun σ' hσ' h' => hSsch p (List.mem_append_left _ hp)
          (Expr.scheme_body_mem_schemeList_tyFreeVars (List.mem_cons_of_mem _ hσ') h'))
        hprest).1 q hq
    exact Subst.eliminates_append hE1Schk hrestE cross2
termination_by Expr.sizeRecGroup bindings
decreasing_by all_goals (try subst_vars; try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end

/-! ### M4: prefix-fix corollary

An earlier substitution `R` (domain below this derivation's input frontier `Φ`,
avoiding the context env and the skeleton's type variables) fixes the elaborated
output: `eOut.substTyFvars R = eOut`. Immediate from `Infer.eOut_avoid` (which
places `eOut.tyFreeVars` off `R`'s domain) and the syntactic
`Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars`. -/
theorem Infer.eOut_substTyFvars_eq {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ)
    {R : Subst} (hRΦ : ∀ p ∈ R, p.1 < Φ)
    (hRctx : ∀ p ∈ R, ∀ M ∈ ctx.env, p.1 ∉ M.body.freeVars)
    (hRe : ∀ p ∈ R, p.1 ∉ e.tyFreeVars) :
    eOut.substTyFvars R = eOut :=
  Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc =>
    (Infer.eOut_avoid h (w := p.1) (hRΦ p hp) (hRctx p hp) (hRe p hp)).2.2 hc)

/-! ### M5: the elaborated output is type-bvar-bounded (`TyBvarBounded 0`)

The Λ-outside encoding wraps each generalised binder in an annotated `letIn (some
Mⱼ)`, whose `TyBvarBounded` reading absorbs the `closeTyVars`-introduced scheme
`bvar`s — so the whole `eOut` has no *dangling* type `bvar`s. This is the second
hypothesis `Expr.openTyVars_closeTyVars_rename` needs (alongside `eOut.NoRecAnn`). -/

/-- `idxOf?` returns an in-range index (Core has this privately). -/
private theorem List.idxOf?_lt_length {α : Type*} [BEq α] {a : α} :
    ∀ {l : List α} {i : Nat}, l.idxOf? a = some i → i < l.length := by
  intro l
  induction l with
  | nil => intro i h; simp [List.idxOf?_nil] at h
  | cons x xs ih =>
    intro i h
    rw [List.idxOf?_cons] at h
    split at h
    · simp only [Option.some.injEq] at h; subst h; simp
    · obtain ⟨j, hj, rfl⟩ := Option.map_eq_some_iff.mp h
      have := ih hj; simp; omega

/-- Closing the names `Xs` at depth `d` raises the bvar bound by `Xs.length`. -/
theorem Ty.closeOverFrom_preserves_bvars {d : Nat} {Xs : List Nat} :
    ∀ {t : Ty}, ContainsBvarsUpTo d t → ContainsBvarsUpTo (d + Xs.length) (Ty.closeOverFrom d Xs t) := by
  intro t ht
  induction t using Ty.rec_strong with
  | prim p => exact .prim
  | bvar i => cases ht with | bvar hlt => exact .bvar (by omega)
  | fvar n =>
    simp only [Ty.closeOverFrom]
    cases hm : Xs.idxOf? n with
    | none => exact .fvar
    | some i => exact .bvar (by have := List.idxOf?_lt_length hm; omega)
  | arrow a b iha ihb => cases ht with | arrow ha hb => exact .arrow (iha ha) (ihb hb)
  | customTy nm tys ih =>
    cases ht with
    | customTy hall =>
      simp only [Ty.closeOverFrom, TyList.closeOverFrom_eq_map]
      exact .customTy (fun t' ht' => by
        obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht'
        exact ih t0 ht0 (hall t0 ht0))

/-- The annotated-group close raises each binding's shielded bvar bound by `Xs.length`
    (schemes thread unchanged). -/
private theorem RecGroupAnn.closeTyVarsAux_tyBvarBounded {Xs : List Nat} :
    ∀ (schemes : List PolyTy) (bindings : List Expr) (d : Nat),
      (∀ e ∈ bindings, ∀ (d' : Nat), e.TyBvarBounded d' →
          (e.closeTyVarsAux d' Xs).TyBvarBounded (d' + Xs.length)) →
      Expr.TyBvarBounded.RecGroupAnn d schemes bindings →
      Expr.TyBvarBounded.RecGroupAnn (d + Xs.length) schemes
        (RecGroupAnn.closeTyVarsAux d Xs schemes bindings) := by
  intro schemes bindings
  induction bindings generalizing schemes with
  | nil => intro d _ _; cases schemes <;> exact trivial
  | cons hd tl ih =>
    intro d ihB hbb
    cases schemes with
    | nil =>
      obtain ⟨hhd, hrest⟩ := hbb
      exact ⟨ihB hd List.mem_cons_self d hhd,
        ih [] d (fun e he => ihB e (List.mem_cons_of_mem _ he)) hrest⟩
    | cons σ ss =>
      obtain ⟨hhd, hrest⟩ := hbb
      refine ⟨?_, ih ss d (fun e he => ihB e (List.mem_cons_of_mem _ he)) hrest⟩
      have hh := ihB hd List.mem_cons_self (d + σ.paramCount) hhd
      rwa [show d + σ.paramCount + Xs.length = d + Xs.length + σ.paramCount by omega] at hh

/-- `closeTyVarsAux` raises the bvar bound by `Xs.length` (each leaf type is closed
    via `Ty.closeOverFrom`, binders advancing `d` in lockstep). (Re-based off `NoRecAnn`:
    `letRecAnn` schemes are bvar-closed independently and its bindings are shielded by
    `RecGroupAnn`, so the bound is raised uniformly.) -/
theorem Expr.closeTyVarsAux_tyBvarBounded {Xs : List Nat} :
    ∀ (e : Expr) (d : Nat), e.TyBvarBounded d →
      (e.closeTyVarsAux d Xs).TyBvarBounded (d + Xs.length) := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p => intro d _; exact trivial
  | ctor c => intro d _; exact trivial
  | var i tyArgs =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.closeTyVarsAux, Expr.TyBvarBounded]
    intro t ht
    obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
    exact Ty.closeOverFrom_preserves_bvars (hb t0 ht0)
  | lambda ann body ih =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.closeTyVarsAux, Expr.TyBvarBounded]
    refine ⟨?_, ih d hb.2⟩
    intro t ht
    obtain ⟨t', ht', rfl⟩ := Option.map_eq_some_iff.mp ht
    exact Ty.closeOverFrom_preserves_bvars (hb.1 t' ht')
  | app f arg ihf iharg =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.closeTyVarsAux, Expr.TyBvarBounded]
    exact ⟨ihf d hb.1, iharg d hb.2⟩
  | letIn ann rhs body ihr ihb =>
    intro d hb
    cases ann with
    | none =>
      simp only [Expr.TyBvarBounded] at hb
      simp only [Expr.closeTyVarsAux, Expr.TyBvarBounded]
      exact ⟨ihr d hb.1, ihb d hb.2⟩
    | some σ =>
      obtain ⟨pc, sb⟩ := σ
      simp only [Expr.TyBvarBounded] at hb
      simp only [Expr.closeTyVarsAux, Expr.TyBvarBounded]
      refine ⟨?_, ?_, ihb d hb.2.2⟩
      · have hh := Ty.closeOverFrom_preserves_bvars (d := d + pc) (Xs := Xs) hb.1
        rw [show d + Xs.length + pc = d + pc + Xs.length by omega]; exact hh
      · have hh := ihr (d + pc) hb.2.1
        rw [show d + Xs.length + pc = d + pc + Xs.length by omega]; exact hh
  | match_ scrut branches ihs ihbr =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.closeTyVarsAux, Expr.TyBvarBounded, BranchList.closeTyVarsAux_eq_map]
    refine ⟨ihs d hb.1, ?_⟩
    rw [Expr.TyBvarBounded.BranchList_iff] at hb ⊢
    intro p b hpb
    obtain ⟨⟨p', b'⟩, hmem, heq⟩ := List.mem_map.mp hpb
    simp only [Prod.mk.injEq] at heq
    obtain ⟨_, rfl⟩ := heq
    exact ihbr p' b' hmem d (hb.2 p' b' hmem)
  | letRec bindings body ihbs ihb =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.closeTyVarsAux, Expr.TyBvarBounded, RecGroup.closeTyVarsAux_eq_map]
    refine ⟨?_, ihb d hb.2⟩
    rw [Expr.TyBvarBounded.RecGroup_iff] at hb ⊢
    intro e he
    obtain ⟨e', hmem, rfl⟩ := List.mem_map.mp he
    exact ihbs e' hmem d (hb.1 e' hmem)
  | letRecAnn schemes bindings body ih_bindings ih_body =>
    intro d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.closeTyVarsAux, Expr.TyBvarBounded]
    obtain ⟨hsch, hrga, hbody⟩ := hb
    exact ⟨hsch, RecGroupAnn.closeTyVarsAux_tyBvarBounded schemes bindings d ih_bindings hrga,
      ih_body d hbody⟩

/-- `closeTyVars Xs` (top-level `d = 0`) raises the bvar bound to `Xs.length`. -/
theorem Expr.closeTyVars_tyBvarBounded {Xs : List Nat} {e : Expr}
    (he : e.TyBvarBounded 0) : (e.closeTyVars Xs).TyBvarBounded Xs.length := by
  have := Expr.closeTyVarsAux_tyBvarBounded (Xs := Xs) e 0 he
  simpa using this

/-- Term-var shifting preserves `TyBvarBounded` (it only renames term `bvar`s, never
    touching type annotations). (Re-based off `NoRecAnn`: shifting recurses through
    `letRecAnn` schemes/bindings without touching their type bvars.) -/
theorem Expr.shiftFrom_tyBvarBounded (n : Nat) {e : Expr} :
    ∀ (t d : Nat), e.TyBvarBounded d → (e.shiftFrom t n).TyBvarBounded d := by
  induction e using Expr.rec_strong with
  | primLit p => intro t d _; exact trivial
  | ctor c => intro t d _; exact trivial
  | var i tyArgs =>
    intro t d hb; simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.shiftFrom]; split <;> (simp only [Expr.TyBvarBounded]; exact hb)
  | lambda ann body ih =>
    intro t d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.shiftFrom, Expr.TyBvarBounded]
    exact ⟨hb.1, ih (t + 1) d hb.2⟩
  | app f arg ihf iharg =>
    intro t d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.shiftFrom, Expr.TyBvarBounded]
    exact ⟨ihf t d hb.1, iharg t d hb.2⟩
  | letIn ann rhs body ihr ihb =>
    intro t d hb
    cases ann with
    | none =>
      simp only [Expr.TyBvarBounded] at hb
      simp only [Expr.shiftFrom, Expr.TyBvarBounded]
      exact ⟨ihr t d hb.1, ihb (t + 1) d hb.2⟩
    | some σ =>
      simp only [Expr.TyBvarBounded] at hb
      simp only [Expr.shiftFrom, Expr.TyBvarBounded]
      exact ⟨hb.1, ihr t (d + σ.paramCount) hb.2.1, ihb (t + 1) d hb.2.2⟩
  | match_ scrut branches ihs ihbr =>
    intro t d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.shiftFrom, Expr.TyBvarBounded]
    refine ⟨ihs t d hb.1, ?_⟩
    obtain ⟨_, hbbr⟩ := hb
    induction branches with
    | nil => exact trivial
    | cons hd tl ihtl =>
      obtain ⟨p, b⟩ := hd
      exact ⟨ihbr p b List.mem_cons_self (t + p.bindCount) d hbbr.1,
             ihtl (fun p' b' hmem => ihbr p' b' (List.mem_cons_of_mem _ hmem)) hbbr.2⟩
  | letRec bindings body ihbs ihb =>
    intro t d hb
    simp only [Expr.TyBvarBounded] at hb
    simp only [Expr.shiftFrom, Expr.TyBvarBounded]
    refine ⟨?_, ihb (t + bindings.length) d hb.2⟩
    obtain ⟨hbbs, _⟩ := hb
    generalize t + bindings.length = thr
    induction bindings with
    | nil => exact trivial
    | cons hd tl ihtl =>
      exact ⟨ihbs hd List.mem_cons_self thr d hbbs.1,
             ihtl (fun e' hmem => ihbs e' (List.mem_cons_of_mem _ hmem)) hbbs.2⟩
  | letRecAnn schemes bindings body ihbs ihb =>
    intro t d hb
    simp only [Expr.TyBvarBounded] at hb
    obtain ⟨hsch, hrga, hbody⟩ := hb
    simp only [Expr.shiftFrom, Expr.TyBvarBounded]
    refine ⟨hsch, ?_, ihb (t + bindings.length) d hbody⟩
    generalize t + bindings.length = thr
    clear hbody hsch
    induction bindings generalizing schemes with
    | nil => exact trivial
    | cons hd tl ihtl =>
      cases schemes with
      | nil =>
        exact ⟨ihbs hd List.mem_cons_self thr d hrga.1,
               ihtl [] (fun e' hmem => ihbs e' (List.mem_cons_of_mem _ hmem)) hrga.2⟩
      | cons σ ss =>
        exact ⟨ihbs hd List.mem_cons_self thr (d + σ.paramCount) hrga.1,
               ihtl ss (fun e' hmem => ihbs e' (List.mem_cons_of_mem _ hmem)) hrga.2⟩

/-- The Λ-outside nest has no dangling type `bvar`s: each wrapper's annotated `letIn
    (some Mⱼ)` absorbs the `closeTyVars`-introduced scheme `bvar`s (`closeTyVars`
    bound = `gvⱼ.length = Mⱼ.paramCount`), and `Mⱼ.body = closeOver gvⱼ τⱼ` has
    bvars `< gvⱼ.length` since `τⱼ` is `IsLC`. -/
theorem Expr.letRecElabNest_tyBvarBounded {G : List Nat} {n : Nat} {rawBindings : List Expr}
    (hrawb : ∀ e ∈ rawBindings, e.TyBvarBounded 0) :
    ∀ (members : List (Nat × Ty)) {body : Expr}, (∀ p ∈ members, p.2.IsLC) →
      body.TyBvarBounded 0 → (Expr.letRecElabNest G n rawBindings members body).TyBvarBounded 0 := by
  intro members
  induction members with
  | nil => intro body _ hbody; exact hbody
  | cons hd tl ih =>
    obtain ⟨i, τ⟩ := hd
    intro body hlc hbody
    simp only [Expr.letRecElabNest, Expr.TyBvarBounded, PolyTy.genGroup, Nat.zero_add]
    refine ⟨Ty.closeOver_preserves_bvars (hlc (i, τ) List.mem_cons_self), ?_,
      ih (fun p hp => hlc p (List.mem_cons_of_mem _ hp)) hbody⟩
    have hbv : (Expr.letRec (rawBindings.map (·.shiftFrom n (n - 1 - i))) (Expr.var i [])).TyBvarBounded 0 := by
      refine ⟨?_, by intro t ht; simp at ht⟩
      rw [Expr.TyBvarBounded.RecGroup_iff]
      intro e he; obtain ⟨e', he', rfl⟩ := List.mem_map.mp he
      exact Expr.shiftFrom_tyBvarBounded (n - 1 - i) n 0 (hrawb e' he')
    exact Expr.closeTyVars_tyBvarBounded hbv

theorem Expr.letRecElab_tyBvarBounded {G : List Nat} {rawBindings : List Expr} {τs : List Ty}
    {body : Expr} (hrawb : ∀ e ∈ rawBindings, e.TyBvarBounded 0)
    (hτs : ∀ τ ∈ τs, τ.IsLC) (hbody : body.TyBvarBounded 0) :
    (Expr.letRecElab G rawBindings τs body).TyBvarBounded 0 :=
  Expr.letRecElabNest_tyBvarBounded hrawb _
    (fun p hp => hτs p.2 (List.of_mem_zip (List.mem_reverse.mp hp)).2) hbody

mutual
/-- The elaborated output has no dangling type `bvar`s (`TyBvarBounded 0`): every
    generalised binder is an annotated `letIn (some Mⱼ)` whose scheme depth absorbs
    the `closeTyVars`-introduced `bvar`s. -/
theorem Infer.eOut_tyBvarBounded {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ)
    (hctx : CtxWF ctx) : eOut.TyBvarBounded 0 := by
  cases h with
  | primLitUnit => exact trivial
  | primLitInt => exact trivial
  | primLitNat => exact trivial
  | primLitStr => exact trivial
  | var hlook =>
    simp only [Expr.TyBvarBounded]
    intro t ht; obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht; exact ContainsBvarsUpTo.fvar
  | ctor hlook => exact trivial
  | lambda hseed hbody =>
    cases hseed with
    | none =>
      refine ⟨fun t ht => absurd ht (by simp), Infer.eOut_tyBvarBounded hbody ?_⟩
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact ContainsBvarsUpTo.fvar
      · exact hctx M hM
    | some _ hT =>
      expose_names
      refine ⟨fun t ht => ?_, Infer.eOut_tyBvarBounded hbody ?_⟩
      · simp only [Option.some.injEq] at ht; subst ht; exact hT
      · intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact hT
        · exact hctx M hM
  | app hf harg _ =>
    exact ⟨Infer.eOut_tyBvarBounded hf hctx,
           Infer.eOut_tyBvarBounded harg (Subst.onCtx_wf (Infer.lc hf hctx).2 hctx)⟩
  | letIn hrhs hbody =>
    obtain ⟨hτ₁lc, hrhs_s⟩ := Infer.lc hrhs hctx
    simp only [Expr.TyBvarBounded, genScheme, Nat.zero_add]
    refine ⟨Ty.closeOver_preserves_bvars hτ₁lc,
      Expr.closeTyVars_tyBvarBounded
        (Expr.substTyFvars_tyBvarBounded hrhs_s (Infer.eOut_tyBvarBounded hrhs hctx)),
      Infer.eOut_tyBvarBounded hbody ?_⟩
    intro M hM; rcases List.mem_cons.mp hM with rfl | hM
    · exact genScheme_wf hτ₁lc
    · exact (Subst.onCtx_wf hrhs_s hctx) M hM
  | letInAnn hσwf hΦN hrhs huni _ _ hbody =>
    expose_names
    obtain ⟨hτ₁lc, hrhs_s⟩ := Infer.lc hrhs hctx
    have hSchk_lc : ∀ p ∈ Schk, p.2.IsLC :=
      UnifyRel.lc huni hτ₁lc (PolyTy.openVars_isLC hσwf (by simp))
    simp only [Expr.TyBvarBounded, Nat.zero_add]
    refine ⟨hσwf, ?_, Infer.eOut_tyBvarBounded hbody ?_⟩
    · have hh := Expr.closeTyVars_tyBvarBounded (Xs := freshVars N σ.paramCount)
        (Expr.substTyFvars_tyBvarBounded
          (fun p hp => (List.mem_append.mp hp).elim (hrhs_s p) (hSchk_lc p))
          (Infer.eOut_tyBvarBounded hrhs hctx))
      rw [freshVars_length] at hh; exact hh
    · intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact hσwf
      · exact (Subst.onCtx_wf hSchk_lc (Subst.onCtx_wf hrhs_s hctx)) M hM
  | match_ hscrut hne hbr =>
    obtain ⟨hτslc, hS₁⟩ := Infer.lc hscrut hctx
    exact ⟨Infer.eOut_tyBvarBounded hscrut hctx,
           InferBranches.eOut_tyBvarBounded hbr (Subst.onCtx_wf hS₁ hctx) hτslc ContainsBvarsUpTo.fvar⟩
  | letRec hgroup hbody =>
    expose_names
    have hctxβ : CtxWF ⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
        ++ ctx.env, ctx.ctors⟩ := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · obtain ⟨x, _, rfl⟩ := List.mem_map.mp hM; exact ContainsBvarsUpTo.fvar
      · exact hctx M hM
    have htgβ : ∀ β ∈ (freshVars Φ bindings.length).map Ty.fvar, β.IsLC := by
      intro β hβ; obtain ⟨x, _, rfl⟩ := List.mem_map.mp hβ; exact ContainsBvarsUpTo.fvar
    have hS₁lc := InferRecGroup.lc hgroup hctxβ htgβ
    have hτslc : ∀ τ ∈ (freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)), τ.IsLC := by
      intro τ hτ; obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ
      exact Subst.onTy_lc hS₁lc ContainsBvarsUpTo.fvar
    have hbodyWF : CtxWF ⟨genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
        ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i))) ++ (S₁.onCtx ctx).env,
        (S₁.onCtx ctx).ctors⟩ := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · simp only [genGroupSchemes] at hM
        obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
        exact PolyTy.genGroup_wf (hτslc τ hτ)
      · exact (Subst.onCtx_wf hS₁lc hctx) M hM
    exact Expr.letRecElab_tyBvarBounded
      (fun e he => by
        obtain ⟨e0, he0, rfl⟩ := List.mem_map.mp he
        exact Expr.substTyFvars_tyBvarBounded hS₁lc
          (InferRecGroup.eOut_tyBvarBounded hgroup hctxβ htgβ e0 he0))
      hτslc (Infer.eOut_tyBvarBounded hbody hbodyWF)
  | letRecAnn hwf hlen hgroup hbody =>
    expose_names
    have hctxσ : CtxWF { ctx with env := schemes ++ ctx.env } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hwf M hM
      · exact hctx M hM
    have hS₁lc := InferRecGroupAnn.lc hgroup hctxσ hwf
    have hbodyWF : CtxWF (S₁.onCtx { ctx with env := schemes ++ ctx.env }) :=
      Subst.onCtx_wf hS₁lc hctxσ
    simp only [Expr.TyBvarBounded]
    exact ⟨hwf, InferRecGroupAnn.eOut_tyBvarBounded hgroup hctxσ hwf,
      Infer.eOut_tyBvarBounded hbody hbodyWF⟩
termination_by e.size
decreasing_by all_goals (try subst_vars; try simp only [Expr.size, Expr.size_openTyVars]; omega)
theorem InferBranches.eOut_tyBvarBounded {Φ ctx scrutTy ρ brs Φ' S brsOut}
    (h : InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut)
    (hctx : CtxWF ctx) (hscrutTy : scrutTy.IsLC) (hρ : ρ.IsLC) :
    Expr.TyBvarBounded.BranchList 0 brsOut := by
  cases h with
  | nil => exact trivial
  | cons hlook hn huni0 hbody huni hrest =>
    expose_names
    have hS₀ := huni0.lc hscrutTy
      (.customTy (fun t ht => by obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht; exact ContainsBvarsUpTo.fvar))
    have hbodyWF : CtxWF ⟨(ctor.contents.map (Ty.openWith
          (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
          ++ (S₀.onCtx ctx).env, (S₀.onCtx ctx).ctors⟩ :=
      branchBindings_wf (Subst.onCtx_wf hS₀ hctx)
        (fun t ht => by
          obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
          obtain ⟨x, _, rfl⟩ := List.mem_map.mp hv
          exact Subst.onTy_lc hS₀ ContainsBvarsUpTo.fvar)
        (by simp)
    obtain ⟨hτb_lc, hS₁⟩ := Infer.lc hbody hbodyWF
    have hS₂ := huni.lc hτb_lc (Subst.onTy_lc hS₁ (Subst.onTy_lc hS₀ hρ))
    exact ⟨Infer.eOut_tyBvarBounded hbody hbodyWF,
           InferBranches.eOut_tyBvarBounded hrest
             (Subst.onCtx_wf hS₂ (Subst.onCtx_wf hS₁ (Subst.onCtx_wf hS₀ hctx)))
             (Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ (Subst.onTy_lc hS₀ hscrutTy)))
             (Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ (Subst.onTy_lc hS₀ hρ)))⟩
  | consWild hbody huni hrest =>
    obtain ⟨hτb_lc, hS₁⟩ := Infer.lc hbody hctx
    have hS₂ := huni.lc hτb_lc (Subst.onTy_lc hS₁ hρ)
    exact ⟨Infer.eOut_tyBvarBounded hbody hctx,
           InferBranches.eOut_tyBvarBounded hrest
             (Subst.onCtx_wf hS₂ (Subst.onCtx_wf hS₁ hctx))
             (Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ hscrutTy))
             (Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ hρ))⟩
termination_by Expr.sizeBranches brs
decreasing_by all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)
theorem InferRecGroup.eOut_tyBvarBounded {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut)
    (hctx : CtxWF ctx) (htargets : ∀ β ∈ targets, β.IsLC) :
    ∀ e ∈ bindingsOut, e.TyBvarBounded 0 := by
  cases h with
  | nil => intro e he; simp at he
  | cons he huni hrest =>
    expose_names
    intro e' he'
    rcases List.mem_cons.mp he' with rfl | hmem
    · exact Infer.eOut_tyBvarBounded he hctx
    · have hβ_lc : β.IsLC := htargets β List.mem_cons_self
      obtain ⟨hτ_lc, hS₁⟩ := Infer.lc he hctx
      have hS₂ := huni.lc hτ_lc (Subst.onTy_lc hS₁ hβ_lc)
      exact InferRecGroup.eOut_tyBvarBounded hrest
        (Subst.onCtx_wf hS₂ (Subst.onCtx_wf hS₁ hctx))
        (fun b' hb' => by obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hb'
                          exact Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ (htargets b (List.mem_cons_of_mem _ hb))))
        e' hmem
termination_by Expr.sizeRecGroup bindings
decreasing_by all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)
theorem InferRecGroupAnn.eOut_tyBvarBounded {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut)
    (hctx : CtxWF ctx) (hsch : ∀ σ ∈ schemes, σ.WF) :
    Expr.TyBvarBounded.RecGroupAnn 0 schemes bindingsOut := by
  cases h with
  | nil => exact trivial
  | cons hΦN hinfer huni _ _ hrest =>
    expose_names
    have hbb : eOut.TyBvarBounded 0 := Infer.eOut_tyBvarBounded hinfer hctx
    obtain ⟨hτlc, hS₁lc⟩ := Infer.lc hinfer hctx
    have hσopen_lc : (σ.openVars (freshVars N σ.paramCount)).IsLC :=
      PolyTy.openVars_isLC (hsch σ List.mem_cons_self) (by simp)
    have hSchk_lc : ∀ p ∈ Schk, p.2.IsLC := UnifyRel.lc huni hτlc hσopen_lc
    refine ⟨?_, ?_⟩
    · have hsub : (eOut.substTyFvars (S₁ ++ Schk)).TyBvarBounded 0 :=
        Expr.substTyFvars_tyBvarBounded
          (fun p hp => (List.mem_append.mp hp).elim (hS₁lc p) (hSchk_lc p)) hbb
      have hcl := Expr.closeTyVars_tyBvarBounded (Xs := freshVars N σ.paramCount) hsub
      rw [freshVars_length] at hcl
      rw [Nat.zero_add]
      exact hcl
    · exact InferRecGroupAnn.eOut_tyBvarBounded hrest
        (Subst.onCtx_wf hSchk_lc (Subst.onCtx_wf hS₁lc hctx))
        (fun σ' hσ' => hsch σ' (List.mem_cons_of_mem _ hσ'))
termination_by Expr.sizeRecGroup bindings
decreasing_by all_goals (try subst_vars; try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end

/-- `substTyFvars` distributes over substitution append (it is the left fold of
    `substTyFvar`). -/
theorem Expr.substTyFvars_append (A B : List (Nat × Ty)) (e : Expr) :
    e.substTyFvars (A ++ B) = (e.substTyFvars A).substTyFvars B := by
  induction A generalizing e with
  | nil => rfl
  | cons hd tl ih => obtain ⟨Z, U⟩ := hd; simp only [List.cons_append, Expr.substTyFvars, ih]

/-! ### M6 close-back plumbing: `substTyFvars` / `openTyVars` commutation

An LC substitution whose domain avoids the opening names `Xs` commutes with scoped
opening (its images carry no `Xs`, and it never substitutes the freshly-opened
`Xs`). Iterated lifts of Core's single-step `Expr.substTyFvar_openTyVarsAux` /
`Ty.substFvar_openVars`. -/

theorem Expr.substTyFvars_openTyVarsAux {S : List (Nat × Ty)} {Xs : List Nat}
    (hS_lc : ∀ p ∈ S, p.2.IsLC) (hS_Xs : ∀ p ∈ S, p.1 ∉ Xs) :
    ∀ (e : Expr) (d : Nat),
      (e.substTyFvars S).openTyVarsAux d Xs = (e.openTyVarsAux d Xs).substTyFvars S := by
  induction S with
  | nil => intro e d; rfl
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    intro e d
    simp only [Expr.substTyFvars]
    rw [ih (fun p hp => hS_lc p (List.mem_cons_of_mem _ hp))
          (fun p hp => hS_Xs p (List.mem_cons_of_mem _ hp)),
        ← Expr.substTyFvar_openTyVarsAux (hS_lc (Z, U) List.mem_cons_self)
            (hS_Xs (Z, U) List.mem_cons_self) e d]

/-- Top-level (`d = 0`) `substTyFvars`/`openTyVars` commutation. -/
theorem Expr.substTyFvars_openTyVars {S : List (Nat × Ty)} {Xs : List Nat} {e : Expr}
    (hS_lc : ∀ p ∈ S, p.2.IsLC) (hS_Xs : ∀ p ∈ S, p.1 ∉ Xs) :
    (e.substTyFvars S).openTyVars Xs = (e.openTyVars Xs).substTyFvars S :=
  Expr.substTyFvars_openTyVarsAux hS_lc hS_Xs e 0

theorem Ty.substFvars_openVars {S : List (Nat × Ty)} {Xs : List Nat} {t : Ty}
    (hS_lc : ∀ p ∈ S, p.2.IsLC) (hS_Xs : ∀ p ∈ S, p.1 ∉ Xs) :
    (t.substFvars S).openVars Xs = (t.openVars Xs).substFvars S := by
  induction S generalizing t with
  | nil => rfl
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    simp only [Ty.substFvars]
    rw [ih (fun p hp => hS_lc p (List.mem_cons_of_mem _ hp))
          (fun p hp => hS_Xs p (List.mem_cons_of_mem _ hp)),
        ← Ty.substFvar_openVars (hS_lc (Z, U) List.mem_cons_self)
            (hS_Xs (Z, U) List.mem_cons_self)]

/-! ### M6 close-back plumbing: `substTyFvars` / `closeTyVars` commutation

The elaboration `closeTyVars`-closes the (substituted) bound expression over the
generalised variables `genV`. Pushing the *enclosing* substitution `S` through
that close commutes when `S` avoids `genV` in **both** domain and range: `S`
never substitutes a freshly-closed `genV` (domain), and its images carry no
`genV` that would be spuriously captured by the close (range). Structural
mirror of `Expr.substTyFvars_openTyVars`, lifting the single-step
`Ty.substFvar_closeOverFrom_comm`. -/

/-- Closing at depth `d` over variables none of which occur free in `τ` is the
    identity (the offset analogue of `Ty.closeOver_eq_self_of_fresh`). -/
theorem Ty.closeOverFrom_eq_self_of_fresh {d : Nat} {gs : List Nat} {τ : Ty}
    (h : ∀ g ∈ gs, g ∉ τ.freeVars) : Ty.closeOverFrom d gs τ = τ := by
  induction τ using Ty.rec_strong with
  | prim p => simp [Ty.closeOverFrom]
  | bvar i => simp [Ty.closeOverFrom]
  | fvar n =>
    have hn : n ∉ gs := fun hmem => h n hmem (by simp [Ty.freeVars])
    simp only [Ty.closeOverFrom, List.idxOf?_eq_none_iff.mpr hn]
  | arrow a b iha ihb =>
    have ha : ∀ g ∈ gs, g ∉ a.freeVars := fun g hg hc =>
      h g hg (by simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inl hc)
    have hb : ∀ g ∈ gs, g ∉ b.freeVars := fun g hg hc =>
      h g hg (by simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inr hc)
    simp only [Ty.closeOverFrom, iha ha, ihb hb]
  | customTy nm tys ih =>
    simp only [Ty.closeOverFrom, TyList.closeOverFrom_eq_map]
    apply congrArg (Ty.customTy nm)
    conv_rhs => rw [← List.map_id tys]
    apply List.map_congr_left
    intro t ht
    exact ih t ht (fun g hg hc => h g hg (TyList.mem_freeVars_of_mem ht hc))

/-- `substFvar` commutes with offset closing (`Ty.closeOverFrom`) when the
    substituted variable `Z` and every free var of the replacement `U` avoid the
    closed-over pool `gs` (the offset analogue of `Ty.substFvar_closeOver_comm`). -/
theorem Ty.substFvar_closeOverFrom_comm {Z : Nat} {U : Ty} {gs : List Nat} {d : Nat} {τ : Ty}
    (hZ : Z ∉ gs) (hU : ∀ g ∈ gs, g ∉ U.freeVars) :
    Ty.substFvar Z U (Ty.closeOverFrom d gs τ) = Ty.closeOverFrom d gs (Ty.substFvar Z U τ) := by
  induction τ using Ty.rec_strong with
  | prim p => simp [Ty.closeOverFrom, Ty.substFvar]
  | bvar i => simp [Ty.closeOverFrom, Ty.substFvar]
  | fvar n =>
    cases h_idx : gs.idxOf? n with
    | none =>
      have hn : n ∉ gs := List.idxOf?_eq_none_iff.mp h_idx
      by_cases hnz : n = Z
      · subst hnz
        simp only [Ty.closeOverFrom, h_idx, Ty.substFvar, if_pos]
        exact (Ty.closeOverFrom_eq_self_of_fresh hU).symm
      · simp only [Ty.closeOverFrom, h_idx, Ty.substFvar, if_neg hnz]
    | some i =>
      have hn : n ∈ gs := List.mem_of_getElem? (List.getElem?_of_idxOf? h_idx)
      have hnz : ¬ n = Z := fun h => hZ (h ▸ hn)
      simp only [Ty.closeOverFrom, h_idx, Ty.substFvar, if_neg hnz]
  | arrow a b iha ihb =>
    simp only [Ty.closeOverFrom, Ty.substFvar, iha, ihb]
  | customTy nm tys ih =>
    simp only [Ty.closeOverFrom, Ty.substFvar, TyList.closeOverFrom_eq_map, TyList.substFvar_eq_map,
               List.map_map]
    apply congrArg (Ty.customTy nm)
    apply List.map_congr_left
    intro t ht
    simpa using ih t ht

/-- Iterated `substFvars` commutes with offset closing, lifting
    `Ty.substFvar_closeOverFrom_comm` over the substitution `S` (which must avoid
    the closed-over pool `Xs` in both domain and range). -/
theorem Ty.substFvars_closeOverFrom {S : Subst} {Xs : List Nat} {d : Nat}
    (hS_Xs : ∀ p ∈ S, p.1 ∉ Xs) (hS_ran : ∀ p ∈ S, ∀ x ∈ p.2.freeVars, x ∉ Xs) :
    ∀ {t : Ty}, Ty.substFvars S (Ty.closeOverFrom d Xs t) = Ty.closeOverFrom d Xs (Ty.substFvars S t) := by
  induction S with
  | nil => intro t; rfl
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    intro t
    simp only [Ty.substFvars]
    rw [Ty.substFvar_closeOverFrom_comm (hS_Xs (Z, U) List.mem_cons_self)
          (fun g hg hc => hS_ran (Z, U) List.mem_cons_self g hc hg),
        ih (fun p hp => hS_Xs p (List.mem_cons_of_mem _ hp))
          (fun p hp => hS_ran p (List.mem_cons_of_mem _ hp))]

/-- `substTyFvars` is the identity on `primLit` leaves. -/
private theorem Expr.substTyFvars_primLit {S : Subst} {p : PrimLitExpr} :
    (Expr.primLit p).substTyFvars S = .primLit p := by
  induction S with
  | nil => rfl
  | cons hd tl ih => obtain ⟨Z, U⟩ := hd; simp only [Expr.substTyFvars, Expr.substTyFvar, ih]

/-- `substTyFvars` is the identity on `ctor` leaves. -/
private theorem Expr.substTyFvars_ctor {S : Subst} {c : CtorName} :
    (Expr.ctor c).substTyFvars S = .ctor c := by
  induction S with
  | nil => rfl
  | cons hd tl ih => obtain ⟨Z, U⟩ := hd; simp only [Expr.substTyFvars, Expr.substTyFvar, ih]

/-- The annotated-group close/subst commutation (per binding, schemes thread by
    `PolyTy.substFvars`, depth-aligned via `paramCount_substFvars`). -/
private theorem RecGroupAnn.substTyFvars_closeTyVarsAux {S : Subst} {Xs : List Nat} :
    ∀ (schemes : List PolyTy) (bindings : List Expr) (d : Nat),
      (∀ e ∈ bindings, ∀ d',
          (e.closeTyVarsAux d' Xs).substTyFvars S = (e.substTyFvars S).closeTyVarsAux d' Xs) →
      (RecGroupAnn.closeTyVarsAux d Xs schemes bindings).map (·.substTyFvars S)
        = RecGroupAnn.closeTyVarsAux d Xs (schemes.map (PolyTy.substFvars S))
            (bindings.map (·.substTyFvars S)) := by
  intro schemes bindings
  induction bindings generalizing schemes with
  | nil => intro d _; cases schemes <;> rfl
  | cons hd tl ih =>
    intro d ihB
    cases schemes with
    | nil =>
      simp only [RecGroupAnn.closeTyVarsAux, List.map_cons, List.map_nil]
      rw [ihB hd List.mem_cons_self d, ih [] d (fun e he => ihB e (List.mem_cons_of_mem _ he)),
          List.map_nil]
    | cons σ ss =>
      simp only [RecGroupAnn.closeTyVarsAux, List.map_cons, PolyTy.paramCount_substFvars]
      rw [ihB hd List.mem_cons_self (d + σ.paramCount),
          ih ss d (fun e he => ihB e (List.mem_cons_of_mem _ he))]

/-- Iterated `substTyFvars` commutes with `closeTyVarsAux` when the substitution
    `S` avoids the closing names `Xs` in both domain and range. (Re-based off
    `NoRecAnn`: through `letRecAnn`, the schemes commute by `PolyTy.substFvars`
    keeping `paramCount`, and the bindings commute pointwise by `RecGroupAnn`.) -/
theorem Expr.substTyFvars_closeTyVarsAux {S : Subst} {Xs : List Nat}
    (hS_Xs : ∀ p ∈ S, p.1 ∉ Xs) (hS_ran : ∀ p ∈ S, ∀ x ∈ p.2.freeVars, x ∉ Xs) :
    ∀ (e : Expr) (d : Nat),
      (e.closeTyVarsAux d Xs).substTyFvars S = (e.substTyFvars S).closeTyVarsAux d Xs := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p => intro d; simp only [Expr.closeTyVarsAux, Expr.substTyFvars_primLit]
  | ctor nm => intro d; simp only [Expr.closeTyVarsAux, Expr.substTyFvars_ctor]
  | var n tyArgs =>
    intro d
    simp only [Expr.closeTyVarsAux, Expr.substTyFvars_var, List.map_map]
    refine congrArg (Expr.var n) ?_
    apply List.map_congr_left
    intro t _
    simp only [Function.comp_apply]
    exact Ty.substFvars_closeOverFrom hS_Xs hS_ran
  | app f inp ih_f ih_i =>
    intro d
    simp only [Expr.closeTyVarsAux, Expr.substTyFvars_app, Expr.app.injEq]
    exact ⟨ih_f d, ih_i d⟩
  | lambda ann body ih =>
    intro d
    simp only [Expr.closeTyVarsAux, Expr.substTyFvars_lambda, Expr.lambda.injEq]
    refine ⟨?_, ih d⟩
    cases ann with
    | none => rfl
    | some t =>
      simp only [Option.map_some, Option.some.injEq]
      exact Ty.substFvars_closeOverFrom hS_Xs hS_ran
  | letIn ann be body ih_be ih_body =>
    intro d
    cases ann with
    | none =>
      simp only [Expr.closeTyVarsAux, Expr.substTyFvars_letIn, Option.map_none]
      rw [ih_be d, ih_body d]
    | some σ =>
      simp only [Expr.closeTyVarsAux, Expr.substTyFvars_letIn, Option.map_some]
      rw [ih_be (d + σ.paramCount), ih_body d,
          Ty.substFvars_closeOverFrom hS_Xs hS_ran (t := σ.body)]
  | match_ scrut branches ih_scrut ih_branches =>
    intro d
    simp only [Expr.closeTyVarsAux, Expr.substTyFvars_match, BranchList.closeTyVarsAux_eq_map,
      List.map_map, Expr.match_.injEq]
    refine ⟨ih_scrut d, ?_⟩
    apply List.map_congr_left
    rintro ⟨p, b⟩ hpb
    simp only [Function.comp_apply, Prod.mk.injEq, true_and]
    exact ih_branches p b hpb d
  | letRec bindings body ih_bindings ih_body =>
    intro d
    simp only [Expr.closeTyVarsAux, Expr.substTyFvars_letRec, RecGroup.closeTyVarsAux_eq_map,
      List.map_map, Expr.letRec.injEq, Function.comp_def]
    refine ⟨?_, ih_body d⟩
    apply List.map_congr_left
    intro e he
    exact ih_bindings e he d
  | letRecAnn schemes bindings body ih_bindings ih_body =>
    intro d
    simp only [Expr.closeTyVarsAux, Expr.substTyFvars_letRecAnn, Expr.letRecAnn.injEq, true_and]
    exact ⟨RecGroupAnn.substTyFvars_closeTyVarsAux schemes bindings d
      (fun e he d' => ih_bindings e he d'), ih_body d⟩

/-- **Step A — the missing close/subst commutation.** Pushing a substitution `S`
    that avoids `Xs` (domain AND range) through a top-level `closeTyVars Xs`
    commutes: `(e.closeTyVars Xs).substTyFvars S = (e.substTyFvars S).closeTyVars Xs`.
    (Re-based off `NoRecAnn`; commutes through `letRecAnn` too.) This is the
    bound-expression `S`-distribution the honest `letIn`/`letInAnn` soundness
    cases need. -/
theorem Expr.substTyFvars_closeTyVars {S : Subst} {Xs : List Nat} {e : Expr}
    (hS_Xs : ∀ p ∈ S, p.1 ∉ Xs)
    (hS_ran : ∀ p ∈ S, ∀ x ∈ p.2.freeVars, x ∉ Xs) :
    (e.closeTyVars Xs).substTyFvars S = (e.substTyFvars S).closeTyVars Xs :=
  Expr.substTyFvars_closeTyVarsAux hS_Xs hS_ran e 0

/-- The **honest** `letIn` soundness case (close-after-`S₁` encoding). Given the
    *elaborated* bound expression `rhsElab` already typed at `τ₁`, the generalised
    `letIn` over `genV` (with bound expr `rhsElab.closeTyVars genV` and scheme
    `⟨genV.length, closeOver genV τ₁⟩`) types at `τ₂`. The cofinite premise is the
    close/open round-trip: `(rhsElab.closeTyVars genV).openTyVars Xs` is the
    `genV ↦ Xs` rename of `rhsElab` (`openTyVars_closeTyVars_rename`), which types
    at `(closeOver genV τ₁).openVars Xs` (`openVars_closeOver_rename`) by
    `typ_substs_preservation`. Because `genV` may include tyArg-mediated vars (now
    closed in `rhsElab` by the close-after-`S₁` fix), this generalises them soundly
    — every opening re-instantiates the stored tyArgs. -/
theorem Infer.sound_letIn {ctx : Ctx} {rhsElab bodyElab : Expr}
    {τ₁ τ₂ : Ty} {genV : List Nat}
    (hrhs_ty : TypeOfElabHM ctx rhsElab τ₁) (hrhs_lc : τ₁.IsLC)
    (hrhs_fresh : ∀ g ∈ genV, g ∉ rhsElab.recAnnSchemeFreeVars)
    (hgenV_nodup : genV.Nodup) (hgenV_env : ∀ g ∈ genV, g ∉ ctx.env.freeVars)
    (hbody_ty : TypeOfElabHM
      { ctx with env := ⟨genV.length, Ty.closeOver genV τ₁⟩ :: ctx.env } bodyElab τ₂) :
    TypeOfElabHM ctx
      (.letIn (some ⟨genV.length, Ty.closeOver genV τ₁⟩) (rhsElab.closeTyVars genV) bodyElab) τ₂ := by
  refine TypeOfElabHM.letIn (M := ⟨genV.length, Ty.closeOver genV τ₁⟩) (L := genV)
    (Ty.closeOver_preserves_bvars hrhs_lc) (fun σ' h => Option.some.inj h) ?_ rfl hbody_ty
  intro Xs hXfresh
  obtain ⟨hXlen, hXnodup, hXavoid⟩ := hXfresh
  have hgenV_Xs : ∀ g ∈ genV, g ∉ Xs := fun g hg hc => hXavoid g hc hg
  have hterm : Expr.openBoundTyVars (some ⟨genV.length, Ty.closeOver genV τ₁⟩) Xs
        (rhsElab.closeTyVars genV)
      = rhsElab.substTyFvars (genV.zip (Xs.map (Ty.fvar ·))) := by
    simp only [Expr.openBoundTyVars]
    exact Expr.openTyVars_closeTyVars_rename_of_fresh (TypeOfElabHM.tyBvarBounded hrhs_ty) hrhs_fresh
      hXlen.symm hgenV_nodup hgenV_Xs
  have htype : PolyTy.openVars Xs ⟨genV.length, Ty.closeOver genV τ₁⟩
      = Ty.substFvars (genV.zip (Xs.map (Ty.fvar ·))) τ₁ := by
    simp only [PolyTy.openVars]
    exact Ty.openVars_closeOver_rename hrhs_lc hgenV_nodup hXlen hgenV_Xs
  rw [hterm, htype]
  exact TypeOfElabHM.typ_substs_preservation (genV.zip (Xs.map (Ty.fvar ·)))
    (fun p hp => hgenV_env p.1 (List.of_mem_zip hp).1)
    (fun p hp => by
      obtain ⟨x, _, hx⟩ := List.mem_map.mp (List.of_mem_zip hp).2
      exact hx ▸ ContainsBvarsUpTo.fvar)
    hrhs_ty

/-- The **honest annotated** `letIn` soundness case (scoped-variable / D2 regime).

    Given the *elaborated* opened bound expr `rhsElab` typed at the single opening
    `σ.openVars Ys` (the `Schk`-unify resolution is done by the caller), the
    annotated `letIn` with bound expr `rhsElab.closeTyVars Ys` and scheme `σ` types
    at `τ₂`. The cofinite premise is the close/open `Ys ↦ Xs` rename
    (`openTyVars_closeTyVars_rename` on the term, `openWith_eq_substFvars_openVars`
    on the type) via `typ_substs_preservation`. The skolems `Ys` are closed exactly
    as before (the close-after-`S₁` fix leaves `letInAnn` untouched: `S` avoids `Ys`
    by the escape conditions). -/
theorem Infer.sound_letInAnn {ctx : Ctx} {σ : PolyTy} {rhsElab bodyElab : Expr}
    {Ys : List Nat} {τ₂ : Ty}
    (hrhs_at_Ys : TypeOfElabHM ctx rhsElab (σ.openVars Ys))
    (hσwf : σ.WF) (hYnodup : Ys.Nodup) (hYlen : Ys.length = σ.paramCount)
    (hrhs_fresh : ∀ y ∈ Ys, y ∉ rhsElab.recAnnSchemeFreeVars)
    (hYs_env : ∀ y ∈ Ys, y ∉ ctx.env.freeVars)
    (hYs_σ : ∀ y ∈ Ys, y ∉ σ.body.freeVars)
    (hbody_ty : TypeOfElabHM { ctx with env := σ :: ctx.env } bodyElab τ₂) :
    TypeOfElabHM ctx (.letIn (some σ) (rhsElab.closeTyVars Ys) bodyElab) τ₂ := by
  refine TypeOfElabHM.letIn (M := σ) (L := Ys) hσwf (fun σ' h => Option.some.inj h) ?_ rfl hbody_ty
  intro Xs hXfresh
  obtain ⟨hXlen, hXnodup, hXavoid⟩ := hXfresh
  have hYs_Xs : ∀ y ∈ Ys, y ∉ Xs := fun y hy hc => hXavoid y hc hy
  have hterm : Expr.openBoundTyVars (some σ) Xs (rhsElab.closeTyVars Ys)
      = rhsElab.substTyFvars (Ys.zip (Xs.map (Ty.fvar ·))) := by
    simp only [Expr.openBoundTyVars]
    exact Expr.openTyVars_closeTyVars_rename_of_fresh (TypeOfElabHM.tyBvarBounded hrhs_at_Ys) hrhs_fresh
      (by rw [hXlen, hYlen]) hYnodup hYs_Xs
  have key : PolyTy.openVars Xs σ = Ty.substFvars (Ys.zip (Xs.map (Ty.fvar ·))) (σ.openVars Ys) := by
    rw [show PolyTy.openVars Xs σ = Ty.openWith (Xs.map (Ty.fvar ·)) σ.body from Ty.openVars_eq_openWith]
    exact Ty.openWith_eq_substFvars_openVars
      ⟨by rw [List.length_map, hXlen, hYlen], fun V hV => by
        obtain ⟨x, _, rfl⟩ := List.mem_map.mp hV; exact ContainsBvarsUpTo.fvar⟩
      hYnodup hYs_σ
      (fun Y hY hc => hYs_Xs Y hY (Ty.mem_freeVarsList_map_fvar.mp hc))
  rw [hterm, key]
  exact TypeOfElabHM.typ_substs_preservation (Ys.zip (Xs.map (Ty.fvar ·)))
    (fun p hp => hYs_env p.1 (List.of_mem_zip hp).1)
    (fun p hp => by
      obtain ⟨x, _, hx⟩ := List.mem_map.mp (List.of_mem_zip hp).2
      exact hx ▸ ContainsBvarsUpTo.fvar)
    hrhs_at_Ys

/-- **Cofinite close-back** (the `?_` guts of `sound_letInAnn`, factored out for the
    annotated recursion group). Given that the elaborated RHS types at a *specific*
    skolem opening `σ.openVars Ys` (with `Ys` fresh for the term, the env, and `σ`),
    the closed-back `rhsElab.closeTyVars Ys`, re-opened at *any* sufficiently-fresh
    `Xs`, types at `σ.openVars Xs` — i.e. the cofinite premise of the scheme-relative
    typing rules. -/
theorem TypeOfElabHM.closeBack_cofinite {ctx : Ctx} {σ : PolyTy} {rhsElab : Expr} {Ys : List Nat}
    (hrhs_at_Ys : TypeOfElabHM ctx rhsElab (σ.openVars Ys))
    (hYnodup : Ys.Nodup) (hYlen : Ys.length = σ.paramCount)
    (hrhs_fresh : ∀ y ∈ Ys, y ∉ rhsElab.recAnnSchemeFreeVars)
    (hYs_env : ∀ y ∈ Ys, y ∉ ctx.env.freeVars)
    (hYs_σ : ∀ y ∈ Ys, y ∉ σ.body.freeVars) :
    ∀ Xs, FreshNames Ys σ.paramCount Xs →
      TypeOfElabHM ctx ((rhsElab.closeTyVars Ys).openTyVars Xs) (σ.openVars Xs) := by
  intro Xs hXfresh
  obtain ⟨hXlen, hXnodup, hXavoid⟩ := hXfresh
  have hYs_Xs : ∀ y ∈ Ys, y ∉ Xs := fun y hy hc => hXavoid y hc hy
  have hterm : (rhsElab.closeTyVars Ys).openTyVars Xs
      = rhsElab.substTyFvars (Ys.zip (Xs.map (Ty.fvar ·))) :=
    Expr.openTyVars_closeTyVars_rename_of_fresh (TypeOfElabHM.tyBvarBounded hrhs_at_Ys) hrhs_fresh
      (by rw [hXlen, hYlen]) hYnodup hYs_Xs
  have key : PolyTy.openVars Xs σ = Ty.substFvars (Ys.zip (Xs.map (Ty.fvar ·))) (σ.openVars Ys) := by
    rw [show PolyTy.openVars Xs σ = Ty.openWith (Xs.map (Ty.fvar ·)) σ.body from Ty.openVars_eq_openWith]
    exact Ty.openWith_eq_substFvars_openVars
      ⟨by rw [List.length_map, hXlen, hYlen], fun V hV => by
        obtain ⟨x, _, rfl⟩ := List.mem_map.mp hV; exact ContainsBvarsUpTo.fvar⟩
      hYnodup hYs_σ
      (fun Y hY hc => hYs_Xs Y hY (Ty.mem_freeVarsList_map_fvar.mp hc))
  rw [hterm, key]
  exact TypeOfElabHM.typ_substs_preservation (Ys.zip (Xs.map (Ty.fvar ·)))
    (fun p hp => hYs_env p.1 (List.of_mem_zip hp).1)
    (fun p hp => by
      obtain ⟨x, _, hx⟩ := List.mem_map.mp (List.of_mem_zip hp).2
      exact hx ▸ ContainsBvarsUpTo.fvar)
    hrhs_at_Ys

/-- Mapping a function over the left list of a `zip` distributes over the zip. -/
theorem List.zip_map_left_eq {α β γ : Type _} (f : α → γ) :
    ∀ (l : List α) (r : List β), (l.map f).zip r = (l.zip r).map (fun ab => (f ab.1, ab.2)) := by
  intro l
  induction l with
  | nil => intro r; simp
  | cons hd tl ih =>
    intro r; cases r with
    | nil => simp
    | cons rhd rtl => simp only [List.map_cons, List.zip_cons_cons, ih]

/-- A `letRecAnn` scheme's body free vars are among the scheme list's `tyFreeVars`. -/
theorem Expr.mem_tyFreeVars_schemeList {σ : PolyTy} {y : Nat} {ss : List PolyTy}
    (hmem : σ ∈ ss) (hy : y ∈ σ.body.freeVars) :
    y ∈ Expr.tyFreeVars.SchemeList.tyFreeVars ss := by
  induction ss with
  | nil => exact absurd hmem List.not_mem_nil
  | cons hd tl ih =>
    simp only [Expr.tyFreeVars.SchemeList.tyFreeVars, List.mem_append]
    rcases List.mem_cons.mp hmem with h | h
    · subst h; exact .inl hy
    · exact .inr (ih h)

/-! Structural simp lemmas for `openWith` (the analogues for `openVars` already
    exist above, but `openWith` lacks them). -/
@[simp] private theorem Ty.openWith_prim {Vs : List Ty} {p : PrimTy} :
    Ty.openWith Vs (.prim p) = .prim p := rfl
@[simp] private theorem Ty.openWith_fvar {Vs : List Ty} {n : Nat} :
    Ty.openWith Vs (.fvar n) = .fvar n := rfl
@[simp] private theorem Ty.openWith_arrow {Vs : List Ty} {a b : Ty} :
    Ty.openWith Vs (.arrow a b) = .arrow (Ty.openWith Vs a) (Ty.openWith Vs b) := rfl
@[simp] private theorem Ty.openWith_customTy {Vs : List Ty} {nm : TyName} {tys : List Ty} :
    Ty.openWith Vs (.customTy nm tys) = .customTy nm (tys.map (Ty.openWith Vs)) := by
  unfold Ty.openWith
  simp only [Ty.instantiate, TyList.instantiate_eq_map]

/-- Opening a scheme body with LC args is an instantiation by those args. -/
theorem InstantiatesBy.openWith {Vs : List Ty} {n : Nat} {ty : Ty}
    (hbv : ContainsBvarsUpTo n ty) (hn : n ≤ Vs.length) :
    InstantiatesBy Vs ty (Ty.openWith Vs ty) := by
  induction ty using Ty.rec_strong with
  | prim p => exact .prim
  | fvar m => exact .fvar
  | bvar i =>
    cases hbv with
    | bvar hlt =>
      have hi : i < Vs.length := by omega
      simp only [Ty.openWith, Ty.instantiate, List.getElem?_eq_getElem hi, Option.getD_some]
      exact .bvar (List.getElem?_eq_getElem hi)
  | arrow a b iha ihb => cases hbv with | arrow ha hb => exact .arrow (iha ha) (ihb hb)
  | customTy nm tys ih =>
    cases hbv with
    | customTy hball =>
      simp only [Ty.openWith_customTy]
      exact .customTy (List.forall₂_self_map (fun t ht => ih t ht (hball t ht)))

/-- Applying an LC substitution commutes with bvar-instantiation. -/
theorem Subst.onTy_instantiate {S : Subst} (hS : ∀ p ∈ S, p.2.IsLC) (σ : Nat → Ty) (X : Ty) :
    S.onTy (Ty.instantiate σ X) = Ty.instantiate (fun i => S.onTy (σ i)) (S.onTy X) := by
  induction X using Ty.rec_strong with
  | prim p => simp only [Ty.instantiate, Subst.onTy_prim]
  | bvar i => simp only [Ty.instantiate, Subst.onTy_bvar]
  | fvar n =>
    simp only [Ty.instantiate]
    rw [Ty.instantiate_eq_self_of_lc (Subst.onTy_lc hS ContainsBvarsUpTo.fvar)]
  | arrow a b iha ihb => simp only [Ty.instantiate, Subst.onTy_arrow, iha, ihb]
  | customTy nm tys ih =>
    simp only [Ty.instantiate, TyList.instantiate_eq_map, Subst.onTy_customTy, List.map_map]
    apply congrArg (Ty.customTy nm)
    apply List.map_congr_left
    intro t ht
    simp only [Function.comp_apply]
    exact ih t ht

/-- `onTy` (LC) commutes with `openWith`. -/
theorem Subst.onTy_openWith {S : Subst} (hS : ∀ p ∈ S, p.2.IsLC) (Vs : List Ty) (X : Ty) :
    S.onTy (Ty.openWith Vs X) = Ty.openWith (Vs.map S.onTy) (S.onTy X) := by
  unfold Ty.openWith
  rw [Subst.onTy_instantiate hS]
  have hfun : (fun i => S.onTy ((Vs[i]?).getD (.bvar i)))
      = (fun i => ((Vs.map S.onTy)[i]?).getD (.bvar i)) := by
    funext i
    rw [List.getElem?_map]
    cases Vs[i]? with
    | none => simp
    | some t => simp
  rw [hfun]

/-- Applying `S` to a branch-bindings-extended context commutes with mapping the
    type args by `S`: the constructor contents are closed, so `S` only renames
    inside the opened type args. -/
theorem Subst.onCtx_branchBindings {S : Subst} {ctorr : Ctor} {ta : List Ty} {ctx : Ctx}
    (hS : ∀ p ∈ S, p.2.IsLC) :
    S.onCtx { ctx with
        env := (ctorr.contents.map (Ty.openWith ta)).map PolyTy.mkTrivial ++ ctx.env }
      = { S.onCtx ctx with
        env := (ctorr.contents.map (Ty.openWith (ta.map S.onTy))).map PolyTy.mkTrivial
          ++ (S.onCtx ctx).env } := by
  have key : ∀ c ∈ ctorr.contents,
      S.onPolyTy (PolyTy.mkTrivial (Ty.openWith ta c))
        = PolyTy.mkTrivial (Ty.openWith (ta.map S.onTy) c) := by
    intro c hc
    have hcfix : S.onTy c = c :=
      Ty.substFvars_eq_self_of_no_key
        (fun p _ => NoFreeVars.not_mem_freeVars (ctorr.closed c hc) p.1)
    simp only [Subst.onPolyTy, PolyTy.mkTrivial, Subst.onTy_openWith hS, hcfix]
  have henv : ((ctorr.contents.map (Ty.openWith ta)).map PolyTy.mkTrivial).map S.onPolyTy
      = (ctorr.contents.map (Ty.openWith (ta.map S.onTy))).map PolyTy.mkTrivial := by
    simp only [List.map_map]
    apply List.map_congr_left
    intro c hc
    simp only [Function.comp_apply]
    exact key c hc
  simp only [Subst.onCtx, Subst.onEnv, List.map_append]
  rw [henv]

/-! ### Helper lemmas for the `letRec` soundness case -/

/-- Reverse of mapping the right component of a `zip`: a member of `l.zip (r.map g)`
    comes from a member of `l.zip r`. -/
private theorem List.mem_zip_map_snd {α β γ : Type _} {g : β → γ} :
    ∀ {l : List α} {r : List β} {p : α × γ},
      p ∈ l.zip (r.map g) → ∃ a b, (a, b) ∈ l.zip r ∧ p = (a, g b) := by
  intro l
  induction l with
  | nil => intro r p h; simp at h
  | cons hd tl ih =>
    intro r p h
    cases r with
    | nil => simp at h
    | cons rhd rtl =>
      simp only [List.map_cons, List.zip_cons_cons, List.mem_cons] at h
      cases h with
      | inl heq => exact ⟨hd, rhd, List.mem_cons_self, heq⟩
      | inr h' =>
        obtain ⟨a, b, hmem, heq⟩ := ih h'
        exact ⟨a, b, List.mem_cons_of_mem _ hmem, heq⟩

/-- An element of `freeVarsList` occurs in some member type's free vars. -/
private theorem Ty.mem_freeVarsList_exists {τs : List Ty} {g : Nat}
    (h : g ∈ Ty.freeVarsList τs) : ∃ t ∈ τs, g ∈ t.freeVars := by
  induction τs with
  | nil => simp [Ty.freeVarsList] at h
  | cons hd tl ih =>
    simp only [Ty.freeVarsList, List.mem_dedup, List.mem_append] at h
    rcases h with h | h
    · exact ⟨hd, List.mem_cons_self, h⟩
    · obtain ⟨t, ht, hg⟩ := ih h
      exact ⟨t, List.mem_cons_of_mem _ ht, hg⟩

/-- A free var of a member type is a member of the list's free vars. -/
private theorem Ty.mem_freeVarsList_of_mem {τs : List Ty} {τ : Ty} {w : Nat}
    (hτ : τ ∈ τs) (hw : w ∈ τ.freeVars) : w ∈ Ty.freeVarsList τs := by
  induction τs with
  | nil => simp at hτ
  | cons hd tl ih =>
    simp only [Ty.freeVarsList, List.mem_dedup, List.mem_append]
    rcases List.mem_cons.mp hτ with rfl | hτ'
    · exact .inl hw
    · exact .inr (ih hτ')

/-- The diagonal of `l.zip (l.map g)`: the second component is `g` of the first. -/
private theorem List.mem_zip_self_map' {α β : Type _} {g : α → β} {l : List α} {p : α × β}
    (h : p ∈ l.zip (l.map g)) : p.2 = g p.1 := by
  induction l with
  | nil => simp at h
  | cons hd tl ih =>
    simp only [List.map_cons, List.zip_cons_cons, List.mem_cons] at h
    cases h with
    | inl heq => subst heq; rfl
    | inr h' => exact ih h'

/-- `RecGroup.tyFreeVars` and `flatMap Expr.tyFreeVars` have the same members. -/
private theorem Expr.mem_recGroup_tyFreeVars {bindings : List Expr} {y : Nat} :
    y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings ↔ y ∈ bindings.flatMap Expr.tyFreeVars := by
  induction bindings with
  | nil => simp [Expr.tyFreeVars.RecGroup.tyFreeVars]
  | cons hd tl ih =>
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.flatMap_cons, List.mem_append, ih]

/-- A substitution whose keys avoid an env's free vars fixes that env. -/
theorem Subst.onEnv_eq_self_of_fresh {S : Subst} {env : Env}
    (h : ∀ p ∈ S, p.1 ∉ env.freeVars) : S.onEnv env = env := by
  show env.map S.onPolyTy = env
  conv_rhs => rw [← List.map_id env]
  apply List.map_congr_left
  intro M hM
  have hbody : S.onTy M.body = M.body :=
    Ty.substFvars_eq_self_of_no_key
      (fun p hp hc => h p hp (Env.mem_freeVars_iff.mpr ⟨M, hM, hc⟩))
  simp only [Subst.onPolyTy, hbody, id_eq]

/-- `genGroupVars` membership: the var occurs in the group's free vars, and is
    neither env-fixed nor rigid. -/
private theorem genGroupVars_spec {rigid : List Nat} {env : Env} {τs : List Ty} {g : Nat}
    (h : g ∈ genGroupVars rigid env τs) :
    g ∈ Ty.freeVarsList τs ∧ g ∉ env.freeVars ∧ g ∉ rigid := by
  simp only [genGroupVars, List.mem_filter, Bool.and_eq_true] at h
  exact ⟨h.1, by simpa using h.2.1, by simpa using h.2.2⟩

/-- Applying the renaming substitution `G ↦ Xs` is `Ty.renameG`. -/
private theorem Subst.onTy_zip_fvar_eq_renameG (G Xs : List Nat) (t : Ty) :
    Subst.onTy (G.zip (Xs.map (Ty.fvar ·))) t = Ty.renameG G Xs t := rfl

/-- Iterated `genGroup`/substitution commutation: a substitution whose domain and
    range both avoid the gen-var pool `G` commutes with `genGroup G`. -/
theorem Subst.onPolyTy_genGroup {G : List Nat} :
    ∀ {S : Subst}, (∀ p ∈ S, p.1 ∉ G) → (∀ p ∈ S, ∀ u ∈ p.2.freeVars, u ∉ G) →
    ∀ {τ : Ty}, Subst.onPolyTy S (PolyTy.genGroup G τ) = PolyTy.genGroup G (Subst.onTy S τ) := by
  intro S
  induction S with
  | nil => intro _ _ τ; simp only [Subst.onPolyTy_nil, Subst.onTy_nil]
  | cons hd S' ih =>
    intro hdom hran
    obtain ⟨Z, U⟩ := hd
    intro τ
    have hZG : Z ∉ G := hdom (Z, U) List.mem_cons_self
    have hUG : ∀ u ∈ U.freeVars, u ∉ G := hran (Z, U) List.mem_cons_self
    have hdom' : ∀ p ∈ S', p.1 ∉ G := fun p hp => hdom p (List.mem_cons_of_mem _ hp)
    have hran' : ∀ p ∈ S', ∀ u ∈ p.2.freeVars, u ∉ G :=
      fun p hp => hran p (List.mem_cons_of_mem _ hp)
    have e0 : Subst.onPolyTy ((Z, U) :: S') (PolyTy.genGroup G τ)
        = Subst.onPolyTy S' (PolyTy.substFvar Z U (PolyTy.genGroup G τ)) := by
      rw [show ((Z, U) :: S') = [(Z, U)] ++ S' from rfl, Subst.onPolyTy_append]; rfl
    have hτeq : Subst.onTy ((Z, U) :: S') τ = Subst.onTy S' (Ty.substFvar Z U τ) := by
      rw [show ((Z, U) :: S') = [(Z, U)] ++ S' from rfl, Subst.onTy_append]; rfl
    rw [e0, PolyTy.genGroup_substFvar hZG hUG, ih hdom' hran', hτeq]

/-- The shared-monotype `letRec` cofinite premise, built from the solved RHS
    typings by a single uniform substitution `S₂ ++ (G ↦ Xs)`: `S₂` lands the body
    context and `G ↦ Xs` is the shared opening of the gen-var pool (the gen-vars
    are renamed throughout; `S₂` avoids the bindings' annotation vars and `G` is
    disjoint from both the bindings' and the (substituted) env's free vars). -/
theorem TypeOfElabHM.letRec_cofinite_of_rhs
    {ctors : CtorEnv} {envCtx : Env} {bindings : List Expr} {τs : List Ty}
    {G Xs : List Nat} {S₂ : Subst}
    (hRHS : ∀ p ∈ bindings.zip τs,
      TypeOfElabHM ⟨τs.map PolyTy.mkTrivial ++ envCtx, ctors⟩ p.1 p.2)
    (hS₂lc : ∀ p ∈ S₂, p.2.IsLC)
    (hS₂_bind : ∀ e ∈ bindings, ∀ p ∈ S₂, p.1 ∉ e.tyFreeVars)
    (hG_bind : ∀ e ∈ bindings, ∀ g ∈ G, g ∉ e.tyFreeVars)
    (hG_env : ∀ g ∈ G, g ∉ (S₂.onEnv envCtx).freeVars) :
    ∀ p ∈ bindings.zip ((τs.map S₂.onTy).map (Ty.renameG G Xs)),
      TypeOfElabHM ⟨((τs.map S₂.onTy).map (Ty.renameG G Xs)).map PolyTy.mkTrivial
                  ++ S₂.onEnv envCtx, ctors⟩ p.1 p.2 := by
  intro p hp
  rw [List.map_map] at hp
  obtain ⟨e, τ, hmem, rfl⟩ := List.mem_zip_map_snd hp
  have he : e ∈ bindings := (List.of_mem_zip hmem).1
  have hLC : ∀ q ∈ S₂ ++ G.zip (Xs.map (Ty.fvar ·)), q.2.IsLC := by
    intro q hq
    rcases List.mem_append.mp hq with hq | hq
    · exact hS₂lc q hq
    · obtain ⟨x, _, hx⟩ := List.mem_map.mp (List.of_mem_zip hq).2
      rw [← hx]; exact ContainsBvarsUpTo.fvar
  have hsub := TypeOfElabHM.onSubst (S₂ ++ G.zip (Xs.map (Ty.fvar ·))) hLC (hRHS (e, τ) hmem)
  have hfix : e.substTyFvars (S₂ ++ G.zip (Xs.map (Ty.fvar ·))) = e := by
    refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun q hq hc => ?_)
    rcases List.mem_append.mp hq with hq | hq
    · exact hS₂_bind e he q hq hc
    · exact hG_bind e he q.1 (List.of_mem_zip hq).1 hc
  rw [hfix] at hsub
  have htype : (S₂ ++ G.zip (Xs.map (Ty.fvar ·))).onTy τ = Ty.renameG G Xs (S₂.onTy τ) := by
    rw [Subst.onTy_append, Subst.onTy_zip_fvar_eq_renameG]
  have henv : (S₂ ++ G.zip (Xs.map (Ty.fvar ·))).onCtx ⟨τs.map PolyTy.mkTrivial ++ envCtx, ctors⟩
      = ⟨((τs.map S₂.onTy).map (Ty.renameG G Xs)).map PolyTy.mkTrivial ++ S₂.onEnv envCtx, ctors⟩ := by
    simp only [Subst.onCtx]
    refine congrArg (fun E => (⟨E, ctors⟩ : Ctx)) ?_
    show (τs.map PolyTy.mkTrivial ++ envCtx).map (S₂ ++ G.zip (Xs.map (Ty.fvar ·))).onPolyTy
        = ((τs.map S₂.onTy).map (Ty.renameG G Xs)).map PolyTy.mkTrivial ++ S₂.onEnv envCtx
    rw [List.map_append]
    congr 1
    · simp only [List.map_map]
      apply List.map_congr_left
      intro t _
      simp only [Function.comp_apply, Subst.onPolyTy, PolyTy.mkTrivial, Subst.onTy_append,
        Subst.onTy_zip_fvar_eq_renameG]
    · rw [show envCtx.map (S₂ ++ G.zip (Xs.map (Ty.fvar ·))).onPolyTy
            = (S₂ ++ G.zip (Xs.map (Ty.fvar ·))).onEnv envCtx from rfl, Subst.onEnv_append]
      exact Subst.onEnv_eq_self_of_fresh (fun q hq => hG_env q.1 (List.of_mem_zip hq).1)
  rw [htype, henv] at hsub
  exact hsub

/-! ### Λ-outside `letRec` elaboration soundness (`letRecElab_sound`).

The elaborated `letRec` is a right-nested stack of annotated `letIn`s
(`Expr.letRecElab`): member `i` is wrapped in `letIn (some (genGroup G τᵢ)) …`
whose bound expr is the *monomorphic* inner group `letRec (raw shifted) (var i [])`
with its gen-vars `genFilter G τᵢ` closed back. We type it bottom-up: the inner
monomorphic group via `TypeOfElabHM.letRec` (shared pool `G' = []`) + `weaken_env`
for the `shiftFrom`, each outer wrapper via the committed `sound_letInAnn`
close-back pattern. These are standalone (the mutual feeds in the bindings'/body's
typings as hypotheses). -/

/-- The inner monomorphic group of the Λ-outside nest for member `i`: the shifted
    raw bindings type at the monotypes `τs` (shared pool `[]`), and projecting
    member `i` (`var i []`) lands its monotype `τ = τs[i]`. The `shiftFrom n …`
    de Bruijn shift is absorbed by `TypeOfElabHM.weaken_env`, inserting the
    `accEnv` outer-wrapper schemes between the group binders and the outer ctx. -/
private theorem innerMonoLetRec_sound {ctors : CtorEnv} {ctxEnv accEnv : Env}
    {bs : List Expr} {τs : List Ty} {n i : Nat} {τ : Ty}
    (hn : n = bs.length) (hlen : bs.length = τs.length)
    (hτs_lc : ∀ t ∈ τs, t.IsLC)
    (hτ : τs[i]? = some τ)
    (hshift : n - 1 - i = accEnv.length)
    (hRHS : ∀ p ∈ bs.zip τs, TypeOfElabHM ⟨τs.map PolyTy.mkTrivial ++ ctxEnv, ctors⟩ p.1 p.2) :
    TypeOfElabHM ⟨accEnv ++ ctxEnv, ctors⟩
      (.letRec (bs.map (·.shiftFrom n (n - 1 - i))) (.var i [])) τ := by
  have hi : i < τs.length := by rw [List.getElem?_eq_some_iff] at hτ; exact hτ.1
  have hτlc : τ.IsLC := hτs_lc τ (by rw [List.getElem?_eq_some_iff] at hτ; exact hτ.2 ▸ List.getElem_mem _)
  have hgnil : ∀ t : Ty, PolyTy.genGroup [] t = PolyTy.mkTrivial t := by
    intro t
    have hcl : Ty.closeOver [] t = t := Ty.closeOver_eq_self_of_fresh (by simp)
    simp only [PolyTy.genGroup, Ty.genFilter, List.filter_nil, List.length_nil, hcl,
      PolyTy.mkTrivial]
  have hmapren : ∀ (Xs : List Nat) (ts : List Ty), ts.map (Ty.renameG [] Xs) = ts := by
    intro Xs ts
    induction ts with
    | nil => rfl
    | cons hd tl ih =>
      have hhd : Ty.renameG [] Xs hd = hd := rfl
      rw [List.map_cons, ih, hhd]
  refine TypeOfElabHM.letRec (τs := τs) (Ms := τs.map PolyTy.mkTrivial) (G := []) (L := [])
    (by rw [List.length_map]; omega) (by rw [List.length_map]) hτs_lc List.nodup_nil
    (fun p hp => (List.mem_zip_self_map' hp).trans (hgnil p.1).symm)
    ?cofin rfl ?body
  case cofin =>
    intro Xs _ p hp
    rw [hmapren Xs τs] at hp ⊢
    rw [List.zip_map_left, List.mem_map] at hp
    obtain ⟨q, hq, rfl⟩ := hp
    have hw := TypeOfElabHM.weaken_env (env_pre := τs.map PolyTy.mkTrivial)
      (env_extra := accEnv) (env := ctxEnv) (hRHS q hq)
    have hlen' : (τs.map PolyTy.mkTrivial).length = n := by rw [List.length_map, ← hlen, ← hn]
    rw [hlen', ← hshift, List.append_assoc] at hw
    exact hw
  case body =>
    refine TypeOfElabHM.var (polyTy := PolyTy.mkTrivial τ) ?_ (by simp)
      (by simp [PolyTy.mkTrivial]) (InstantiatesBy.refl_of_closed hτlc)
    rw [List.getElem?_append_left (by rw [List.length_map]; exact hi), List.getElem?_map, hτ]
    rfl

/-- `((range τs.length).zip τs).map (f ∘ snd) = τs.map f` (the zip's second
    components are exactly `τs`). -/
private theorem map_snd_range_zip {β : Type _} (f : Ty → β) (τs : List Ty) :
    (((List.range τs.length).zip τs).map (fun p => f p.2)) = τs.map f := by
  apply List.ext_getElem
  · simp [List.length_zip, List.length_range]
  · intro k h1 h2
    simp only [List.getElem_map, List.getElem_zip]

/-- The Λ-outside nest types: each outer wrapper (member `i`) generalises
    `genFilter G τᵢ` over the inner monomorphic group via the committed
    `sound_letInAnn` close-back, and the body sees the per-binding schemes. Proved
    by induction on the member stack `ms`, accumulating the outer schemes `acc`
    (whose length equals the `shiftFrom` depth `n-1-i` for the current member). -/
private theorem letRecElabNest_sound {ctx : Ctx} {G : List Nat} {n : Nat}
    {bs : List Expr} {τs : List Ty} {body : Expr} {ρ : Ty}
    (hn : n = bs.length) (hlen : bs.length = τs.length)
    (hτs_lc : ∀ τ ∈ τs, τ.IsLC)
    (hG_nodup : G.Nodup)
    (hG_env : ∀ g ∈ G, g ∉ ctx.env.freeVars)
    (hbs_fresh : ∀ g ∈ G, ∀ e ∈ bs, g ∉ e.recAnnSchemeFreeVars)
    (hRHS : ∀ p ∈ bs.zip τs,
      TypeOfElabHM ⟨τs.map PolyTy.mkTrivial ++ ctx.env, ctx.ctors⟩ p.1 p.2) :
    ∀ (ms : List (Nat × Ty)) (acc : List PolyTy),
      (∀ M ∈ acc, ∀ g ∈ G, g ∉ M.body.freeVars) →
      acc.length + ms.length = n →
      (∀ k (hk : k < ms.length), (ms[k]'hk).1 = n - 1 - (acc.length + k) ∧
        τs[(ms[k]'hk).1]? = some (ms[k]'hk).2) →
      TypeOfElabHM ⟨(ms.map (fun p => PolyTy.genGroup G p.2)).reverse ++ acc ++ ctx.env,
          ctx.ctors⟩ body ρ →
      TypeOfElabHM ⟨acc ++ ctx.env, ctx.ctors⟩ (Expr.letRecElabNest G n bs ms body) ρ := by
  intro ms
  induction ms with
  | nil =>
    intro acc _ _ _ hbody
    simpa only [Expr.letRecElabNest, List.map_nil, List.reverse_nil, List.nil_append] using hbody
  | cons hd rest ih =>
    intro acc hacc_genG hcard hms hbody
    obtain ⟨i, τ⟩ := hd
    have hcard1 : acc.length + (rest.length + 1) = n := by simpa only [List.length_cons] using hcard
    obtain ⟨hi_eq, hτ_eq⟩ := hms 0 (by simp)
    simp only [List.getElem_cons_zero, Nat.add_zero] at hi_eq hτ_eq
    have hτlc : τ.IsLC := hτs_lc τ (List.mem_of_getElem? hτ_eq)
    have hshift : n - 1 - i = acc.length := by omega
    have hyG : ∀ y ∈ Ty.genFilter G τ, y ∈ G := fun y hy => by
      simp only [Ty.genFilter, List.mem_filter] at hy; exact hy.1
    simp only [Expr.letRecElabNest]
    refine Infer.sound_letInAnn
      (σ := PolyTy.genGroup G τ) (Ys := Ty.genFilter G τ)
      (rhsElab := .letRec (bs.map (·.shiftFrom n (n - 1 - i))) (.var i []))
      (bodyElab := Expr.letRecElabNest G n bs rest body)
      ?hrhs (PolyTy.genGroup_wf hτlc) (hG_nodup.filter _) rfl ?hnorec ?hYsenv ?hYsσ ?hbody'
    case hrhs =>
      have hopen : PolyTy.openVars (Ty.genFilter G τ) (PolyTy.genGroup G τ) = τ := by
        simp only [PolyTy.openVars, PolyTy.genGroup]; exact Ty.openVars_closeOver_self hτlc
      rw [hopen]
      exact innerMonoLetRec_sound hn hlen hτs_lc hτ_eq hshift hRHS
    case hnorec =>
      intro y hy hc
      simp only [Expr.recAnnSchemeFreeVars, List.append_nil] at hc
      rw [Expr.mem_recAnnSchemeFreeVars_RecGroup] at hc
      obtain ⟨e', he', hge'⟩ := hc
      obtain ⟨e₀, he₀, rfl⟩ := List.mem_map.mp he'
      rw [Expr.recAnnSchemeFreeVars_shiftFrom] at hge'
      exact hbs_fresh y (hyG y hy) e₀ he₀ hge'
    case hYsenv =>
      intro y hy hc
      rw [Env.mem_freeVars_iff] at hc
      obtain ⟨pt, hpt, hypt⟩ := hc
      rcases List.mem_append.mp hpt with hpt | hpt
      · exact hacc_genG pt hpt y (hyG y hy) hypt
      · exact hG_env y (hyG y hy) (Env.mem_freeVars_iff.mpr ⟨pt, hpt, hypt⟩)
    case hYsσ =>
      intro y hy
      exact Ty.not_mem_closeOver_freeVars hy
    case hbody' =>
      have hacc'_genG : ∀ M ∈ PolyTy.genGroup G τ :: acc, ∀ g ∈ G, g ∉ M.body.freeVars := by
        intro M hM g hg
        rcases List.mem_cons.mp hM with rfl | hM
        · intro hc
          by_cases hgτ : g ∈ τ.freeVars
          · exact Ty.not_mem_closeOver_freeVars
              (by simp only [Ty.genFilter, List.mem_filter, decide_eq_true_eq]; exact ⟨hg, hgτ⟩) hc
          · exact hgτ (Ty.freeVars_closeOver_subset hc)
        · exact hacc_genG M hM g hg
      have hcard' : (PolyTy.genGroup G τ :: acc).length + rest.length = n := by
        simp only [List.length_cons]; omega
      have hms' : ∀ k (hk : k < rest.length),
          (rest[k]'hk).1 = n - 1 - ((PolyTy.genGroup G τ :: acc).length + k) ∧
          τs[(rest[k]'hk).1]? = some (rest[k]'hk).2 := by
        intro k hk
        have := hms (k + 1) (by simp only [List.length_cons]; omega)
        simp only [List.getElem_cons_succ, List.length_cons] at this ⊢
        refine ⟨?_, this.2⟩
        rw [this.1]; congr 1; omega
      have hbody' : TypeOfElabHM
          ⟨(rest.map (fun p => PolyTy.genGroup G p.2)).reverse
              ++ (PolyTy.genGroup G τ :: acc) ++ ctx.env, ctx.ctors⟩ body ρ := by
        rw [List.map_cons, List.reverse_cons] at hbody
        simpa only [List.append_assoc, List.singleton_append, List.cons_append] using hbody
      have key := ih (PolyTy.genGroup G τ :: acc) hacc'_genG hcard' hms' hbody'
      simpa only [List.cons_append] using key

/-- **Λ-outside `letRec` elaboration soundness.** The fully-elaborated recursive
    group `Expr.letRecElab G bs τs body` types at `ρ` given the bindings' (bare,
    monomorphic) RHS typings and the body's typing under the per-binding gen
    schemes. (The mutual feeds these in; `S`-distribution is done at the call site.) -/
theorem Expr.letRecElab_sound {ctx : Ctx} {G : List Nat} {bs : List Expr}
    {τs : List Ty} {body : Expr} {ρ : Ty}
    (hlen : bs.length = τs.length)
    (hτs_lc : ∀ τ ∈ τs, τ.IsLC)
    (hG_nodup : G.Nodup)
    (hG_env : ∀ g ∈ G, g ∉ ctx.env.freeVars)
    (hbs_fresh : ∀ g ∈ G, ∀ e ∈ bs, g ∉ e.recAnnSchemeFreeVars)
    (hRHS : ∀ p ∈ bs.zip τs,
      TypeOfElabHM ⟨τs.map PolyTy.mkTrivial ++ ctx.env, ctx.ctors⟩ p.1 p.2)
    (hbody : TypeOfElabHM ⟨τs.map (PolyTy.genGroup G) ++ ctx.env, ctx.ctors⟩ body ρ) :
    TypeOfElabHM ctx (Expr.letRecElab G bs τs body) ρ := by
  have hmlen : bs.length = τs.length := hlen
  rw [Expr.letRecElab]
  have hcard : ([] : List PolyTy).length + (((List.range bs.length).zip τs).reverse).length
      = bs.length := by
    simp only [List.length_nil, List.length_reverse, List.length_zip, List.length_range,
      Nat.zero_add, ← hlen, Nat.min_self]
  have hbodyctx : TypeOfElabHM
      ⟨((((List.range bs.length).zip τs).reverse).map (fun p => PolyTy.genGroup G p.2)).reverse
          ++ ([] : List PolyTy) ++ ctx.env, ctx.ctors⟩ body ρ := by
    rw [List.map_reverse, List.reverse_reverse, List.append_nil]
    rw [show bs.length = τs.length from hlen, map_snd_range_zip]
    exact hbody
  have hvalid : ∀ k (hk : k < (((List.range bs.length).zip τs).reverse).length),
      ((((List.range bs.length).zip τs).reverse)[k]'hk).1
          = bs.length - 1 - (([] : List PolyTy).length + k) ∧
      τs[((((List.range bs.length).zip τs).reverse)[k]'hk).1]?
          = some ((((List.range bs.length).zip τs).reverse)[k]'hk).2 := by
    intro k hk
    have hlen_ms : (((List.range bs.length).zip τs).reverse).length = bs.length := by
      rw [List.length_reverse, List.length_zip, List.length_range, ← hlen, Nat.min_self]
    have hkn : k < bs.length := by rw [← hlen_ms]; exact hk
    have h1 : ((List.range bs.length).zip τs).length = bs.length := by
      rw [List.length_zip, List.length_range, ← hlen, Nat.min_self]
    have hidx : (((List.range bs.length).zip τs).reverse)[k]'hk
        = (bs.length - 1 - k, τs[bs.length - 1 - k]'(by omega)) := by
      simp only [List.getElem_reverse, List.getElem_zip, List.getElem_range, h1]
    rw [hidx]
    refine ⟨by simp only [List.length_nil, Nat.zero_add], ?_⟩
    simp only [List.getElem?_eq_getElem (by omega : bs.length - 1 - k < τs.length)]
  have key := letRecElabNest_sound (ctx := ctx) (G := G) (n := bs.length) (bs := bs) (τs := τs)
    (body := body) (ρ := ρ) rfl hlen hτs_lc hG_nodup hG_env hbs_fresh hRHS
    (((List.range bs.length).zip τs).reverse) [] (by simp) hcard hvalid hbodyctx
  simpa only [List.nil_append] using key

/-- Iterated `substFvars` commutes with `closeOver` when `S` avoids the closed-over
    pool `gs` in domain and range. Plural lift of `Ty.substFvar_closeOver_comm`
    (the `d = 0` companion of Step A's `Ty.substFvars_closeOverFrom`). Used by the
    honest `letIn` soundness case to push `S` through the generalised scheme. -/
theorem Ty.substFvars_closeOver {S : Subst} {gs : List Nat}
    (hS_gs : ∀ p ∈ S, p.1 ∉ gs) (hS_ran : ∀ p ∈ S, ∀ u ∈ p.2.freeVars, u ∉ gs) :
    ∀ {τ : Ty}, Ty.substFvars S (Ty.closeOver gs τ) = Ty.closeOver gs (Ty.substFvars S τ) := by
  induction S with
  | nil => intro τ; rfl
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    intro τ
    simp only [Ty.substFvars]
    rw [Ty.substFvar_closeOver_comm (hS_gs (Z, U) List.mem_cons_self)
          (fun g hg hc => hS_ran (Z, U) List.mem_cons_self g hc hg),
        ih (fun p hp => hS_gs p (List.mem_cons_of_mem _ hp))
          (fun p hp => hS_ran p (List.mem_cons_of_mem _ hp))]

/-- **Substitution removes its own domain from a term's free type vars.** If `w`
    avoids `S.onTy x` for every `x` (i.e. `w` is idempotently eliminated by `S`),
    then `w` is not free in `e.substTyFvars S` (for `letRecAnn`-free `e`). Direct
    structural twin of `Expr.mem_tyFreeVars_substTyFvars` that keeps the
    `(S.onTy t)` form for annotations instead of weakening to a disjunction — this
    is what makes `S₁` idempotent on the close-after-`S₁` `letIn` rhs. -/
theorem Expr.dom_notMem_substTyFvars {S : Subst} {w : Nat}
    (hidem : ∀ x : Ty, w ∉ (Ty.substFvars S x).freeVars) :
    ∀ {e : Expr}, w ∉ (e.substTyFvars S).tyFreeVars := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p => rw [Expr.substTyFvars_primLit]; simp [Expr.tyFreeVars]
  | ctor c => rw [Expr.substTyFvars_ctor]; simp [Expr.tyFreeVars]
  | var n tyArgs =>
    rw [Expr.substTyFvars_var]
    simp only [Expr.tyFreeVars, List.mem_flatMap, not_exists, not_and]
    intro t ht
    obtain ⟨t0, _, rfl⟩ := List.mem_map.mp ht
    exact hidem t0
  | lambda ann body ih =>
    rw [Expr.substTyFvars_lambda]
    cases ann with
    | none => simpa only [Expr.tyFreeVars, Option.map_none, Option.elim_none, List.nil_append] using ih
    | some t =>
      simp only [Expr.tyFreeVars, Option.map_some, Option.elim_some, List.mem_append, not_or]
      exact ⟨hidem t, ih⟩
  | app f arg ihf iharg =>
    rw [Expr.substTyFvars_app]
    simp only [Expr.tyFreeVars, List.mem_append, not_or]
    exact ⟨ihf, iharg⟩
  | letIn ann rhs body ihr ihb =>
    rw [Expr.substTyFvars_letIn]
    cases ann with
    | none =>
      simp only [Expr.tyFreeVars, Option.map_none, Option.elim_none, List.nil_append,
        List.mem_append, not_or]
      exact ⟨ihr, ihb⟩
    | some σ =>
      simp only [Expr.tyFreeVars, Option.map_some, Option.elim_some, List.mem_append, not_or]
      exact ⟨⟨hidem σ.body, ihr⟩, ihb⟩
  | match_ scrut branches ihs ihbr =>
    rw [Expr.substTyFvars_match]
    simp only [Expr.tyFreeVars, List.mem_append, not_or]
    refine ⟨ihs, ?_⟩
    intro hc
    obtain ⟨pb, hpb, hwpb⟩ := Expr.mem_branchListTyFreeVars hc
    obtain ⟨pb0, hpb0, rfl⟩ := List.mem_map.mp hpb
    obtain ⟨p0, b0⟩ := pb0
    exact ihbr p0 b0 hpb0 hwpb
  | letRec bindings body ihbs ihb =>
    rw [Expr.substTyFvars_letRec]
    simp only [Expr.tyFreeVars, List.mem_append, not_or]
    refine ⟨?_, ihb⟩
    intro hc
    obtain ⟨e', he', hwe'⟩ := Expr.mem_recGroupTyFreeVars hc
    obtain ⟨e0, he0, rfl⟩ := List.mem_map.mp he'
    exact ihbs e0 he0 hwe'
  | letRecAnn schemes bindings body ihbs ihb =>
    rw [Expr.substTyFvars_letRecAnn]
    simp only [Expr.tyFreeVars, List.mem_append, not_or]
    refine ⟨⟨?_, ?_⟩, ihb⟩
    · induction schemes with
      | nil => simp [Expr.tyFreeVars.SchemeList.tyFreeVars]
      | cons σ ss ihs =>
        simp only [List.map_cons, Expr.tyFreeVars.SchemeList.tyFreeVars, List.mem_append, not_or,
          PolyTy.body_substFvars]
        exact ⟨hidem σ.body, ihs⟩
    · intro hc
      obtain ⟨e', he', hwe'⟩ := Expr.mem_recGroupTyFreeVars hc
      obtain ⟨e0, he0, rfl⟩ := List.mem_map.mp he'
      exact ihbs e0 he0 hwe'

/-! ### Domain-locality (avoid form): `Infer.dom_avoid`

The substitution-**domain** twin of `Infer.eOut_avoid`: a var below the input
frontier that avoids the context env and the term's annotation free vars is not
**bound** by the inferred substitution. (`eOut_avoid` only handles the range;
`dom_below` only bounds the domain from above.) The honest soundness `let`/`letRec`
cases need this to show the body substitution leaves the *generalised* variables
untouched. Proof mirrors `eOut_avoid`, using `UnifyRel.dom_mem` (the unifier's
domain lies in the unified types' free vars) where `eOut_avoid` uses
`range_mem`. -/
mutual
theorem Infer.dom_avoid {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ) :
    ∀ {w : Nat}, w < Φ → (∀ M ∈ ctx.env, w ∉ M.body.freeVars) → w ∉ e.tyFreeVars →
    w ∉ S.map Prod.fst := by
  cases h with
  | primLitUnit => intro w _ _ _; simp
  | primLitInt => intro w _ _ _; simp
  | primLitNat => intro w _ _ _; simp
  | primLitStr => intro w _ _ _; simp
  | var hlook => intro w _ _ _; simp
  | ctor hlook => intro w _ _ _; simp
  | lambda hseed hbody =>
    intro w hwΦ hctx hwe
    cases hseed with
    | none =>
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append] at hwe
      exact Infer.dom_avoid hbody (by omega)
        (by intro M hM; rcases List.mem_cons.mp hM with rfl | hM
            · intro hc; simp only [PolyTy.mkTrivial, Ty.freeVars, List.mem_singleton] at hc; omega
            · exact hctx M hM)
        hwe
    | some T hT =>
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append, not_or] at hwe
      exact Infer.dom_avoid hbody hwΦ
        (by intro M hM; rcases List.mem_cons.mp hM with rfl | hM
            · simpa only [PolyTy.mkTrivial] using hwe.1
            · exact hctx M hM)
        hwe.2
  | app hf harg huni =>
    intro w hwΦ hctx hwe
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append, not_or] at hwe
    have hfle := Infer.frontier_le hf
    have hargle := Infer.frontier_le harg
    obtain ⟨hfS, hfτ, hfo⟩ := Infer.eOut_avoid hf (w := w) hwΦ hctx hwe.1
    have hfdom := Infer.dom_avoid hf hwΦ hctx hwe.1
    obtain ⟨haS, haτ, hao⟩ := Infer.eOut_avoid harg (w := w) (by omega)
      (Subst.onCtx_avoid hctx hfS) hwe.2
    have hadom := Infer.dom_avoid harg (by omega) (Subst.onCtx_avoid hctx hfS) hwe.2
    intro hc; simp only [List.map_append, List.mem_append] at hc
    rcases hc with (hc | hc) | hc
    · exact hfdom hc
    · exact hadom hc
    · obtain ⟨p, hp, hpw⟩ := List.mem_map.mp hc
      rcases UnifyRel.dom_mem huni p hp with h | h
      · rw [hpw] at h; exact Subst.notMemOnTy haS hfτ h
      · rw [hpw] at h
        simp only [Ty.freeVars, List.mem_dedup, List.mem_append, List.mem_singleton] at h
        rcases h with h | h
        · exact haτ h
        · omega
  | letIn hrhs hbody =>
    intro w hwΦ hctx hwe
    expose_names
    simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append, not_or] at hwe
    have hrle := Infer.frontier_le hrhs
    obtain ⟨hrS, hrτ, hro⟩ := Infer.eOut_avoid hrhs (w := w) hwΦ hctx hwe.1
    have hrdom := Infer.dom_avoid hrhs hwΦ hctx hwe.1
    have hbdom := Infer.dom_avoid hbody (by omega)
      (by intro M hM; rcases List.mem_cons.mp hM with rfl | hM
          · intro hc; exact hrτ (Ty.freeVars_closeOver_subset hc)
          · exact Subst.onCtx_avoid hctx hrS M hM)
      hwe.2
    intro hc; simp only [List.map_append, List.mem_append] at hc
    rcases hc with hc | hc
    · exact hrdom hc
    · exact hbdom hc
  | letInAnn hσwf hΦN hrhs huni hesc1 hesc2 hbody =>
    intro w hwΦ hctx hwe
    expose_names
    simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append, not_or] at hwe
    have hrle := Infer.frontier_le hrhs
    obtain ⟨hrS, hrτ, hro⟩ := Infer.eOut_avoid hrhs (w := w) (by omega) hctx (by
      intro hc; rcases Expr.tyFreeVars_openTyVars hc with h | h
      · exact hwe.1.2 h
      · have := freshVars_ge w h; omega)
    have hrdom := Infer.dom_avoid hrhs (by omega) hctx (by
      intro hc; rcases Expr.tyFreeVars_openTyVars hc with h | h
      · exact hwe.1.2 h
      · have := freshVars_ge w h; omega)
    have hσopen : w ∉ (σ.openVars (freshVars N σ.paramCount)).freeVars := by
      intro hc; rcases Ty.freeVars_openVars_subset w hc with h | h
      · exact hwe.1.1 h
      · have := freshVars_ge w h; omega
    have hSchk : ∀ p ∈ Schk, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni p hp w hwp with h | h
      · exact hrτ h
      · exact hσopen h
    have hSchkdom : w ∉ Schk.map Prod.fst := by
      intro hc; obtain ⟨p, hp, hpw⟩ := List.mem_map.mp hc
      rcases UnifyRel.dom_mem huni p hp with h | h
      · rw [hpw] at h; exact hrτ h
      · rw [hpw] at h; exact hσopen h
    have hbdom := Infer.dom_avoid hbody (by omega)
      (by intro M hM; rcases List.mem_cons.mp hM with rfl | hM
          · exact hwe.1.1
          · exact Subst.onCtx_avoid (Subst.onCtx_avoid hctx hrS) hSchk M hM)
      hwe.2
    intro hc; simp only [List.map_append, List.mem_append] at hc
    rcases hc with (hc | hc) | hc
    · exact hrdom hc
    · exact hSchkdom hc
    · exact hbdom hc
  | match_ hscrut hne hbr =>
    intro w hwΦ hctx hwe
    simp only [Expr.tyFreeVars, List.mem_append, not_or] at hwe
    obtain ⟨hsS, hsτ, hso⟩ := Infer.eOut_avoid hscrut (w := w) hwΦ hctx hwe.1
    have hsdom := Infer.dom_avoid hscrut hwΦ hctx hwe.1
    have hle1 := Infer.frontier_le hscrut
    have hbrdom := InferBranches.dom_avoid hbr (w := w) (by omega)
      (Subst.onCtx_avoid hctx hsS) hsτ
      (by intro hc; simp only [Ty.freeVars, List.mem_singleton] at hc; omega) hwe.2
    intro hc; simp only [List.map_append, List.mem_append] at hc
    rcases hc with hc | hc
    · exact hsdom hc
    · exact hbrdom hc
  | letRec hgroup hbody =>
    intro w hwΦ hctx hwe
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append, not_or] at hwe
    have hgle := InferRecGroup.frontier_le hgroup
    obtain ⟨hgS, hgo⟩ := InferRecGroup.eOut_avoid hgroup (w := w) (by omega)
      (by intro M hM; rcases List.mem_append.mp hM with hM | hM
          · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
            simp only [PolyTy.mkTrivial, Ty.freeVars, List.mem_singleton]
            have := freshVars_ge x hx; omega
          · exact hctx M hM)
      (by intro β hβ; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hβ
          simp only [Ty.freeVars, List.mem_singleton]; have := freshVars_ge x hx; omega)
      hwe.1
    have hgdom := InferRecGroup.dom_avoid hgroup (w := w) (by omega)
      (by intro M hM; rcases List.mem_append.mp hM with hM | hM
          · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
            simp only [PolyTy.mkTrivial, Ty.freeVars, List.mem_singleton]
            have := freshVars_ge x hx; omega
          · exact hctx M hM)
      (by intro β hβ; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hβ
          simp only [Ty.freeVars, List.mem_singleton]; have := freshVars_ge x hx; omega)
      hwe.1
    have hbdom := Infer.dom_avoid hbody (by omega)
      (by intro M hM; rcases List.mem_append.mp hM with hM | hM
          · simp only [genGroupSchemes] at hM
            obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
            obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
            simp only [PolyTy.genGroup]
            intro hc
            have hc' := Ty.freeVars_closeOver_subset hc
            rcases Subst.mem_freeVars_onTy hc' with h | ⟨p, hp, hvp⟩
            · simp only [Ty.freeVars, List.mem_singleton] at h; have := freshVars_ge x hx; omega
            · exact hgS p hp hvp
          · exact Subst.onCtx_avoid hctx hgS M hM)
      hwe.2
    intro hc; simp only [List.map_append, List.mem_append] at hc
    rcases hc with hc | hc
    · exact hgdom hc
    · exact hbdom hc
  | letRecAnn hwf hlen hgroup hbody =>
    intro w hwΦ hctx hwe
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append, not_or] at hwe
    have hsch : ∀ σ ∈ schemes, w ∉ σ.body.freeVars := fun σ hσ hc =>
      hwe.1.1 (Expr.scheme_body_mem_schemeList_tyFreeVars hσ hc)
    have hctxσ : ∀ M ∈ ({ ctx with env := schemes ++ ctx.env } : Ctx).env, w ∉ M.body.freeVars := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hsch M hM
      · exact hctx M hM
    have hgle := InferRecGroupAnn.frontier_le hgroup
    obtain ⟨hgS, hgo⟩ := InferRecGroupAnn.eOut_avoid hgroup (w := w) hwΦ hctxσ hsch hwe.1.2
    have hgdom := InferRecGroupAnn.dom_avoid hgroup (w := w) hwΦ hctxσ hsch hwe.1.2
    have hbdom := Infer.dom_avoid hbody (by omega)
      (Subst.onCtx_avoid hctxσ hgS) hwe.2
    intro hc; simp only [List.map_append, List.mem_append] at hc
    rcases hc with hc | hc
    · exact hgdom hc
    · exact hbdom hc
termination_by e.size
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.size, Expr.size_openTyVars]; omega)
theorem InferBranches.dom_avoid {Φ ctx scrutTy ρ brs Φ' S brsOut}
    (h : InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut) :
    ∀ {w : Nat}, w < Φ → (∀ M ∈ ctx.env, w ∉ M.body.freeVars) → w ∉ scrutTy.freeVars →
    w ∉ ρ.freeVars → w ∉ Expr.tyFreeVars.BranchList.tyFreeVars brs →
    w ∉ S.map Prod.fst := by
  cases h with
  | nil => intro w _ _ _ _ _; simp
  | cons hlook hn huni0 hbody huni hrest =>
    intro w hwΦ hctx hscrut hρ hbrs
    expose_names
    simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append, not_or] at hbrs
    have hS₀ : ∀ p ∈ S₀, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni0 p hp w hwp with h | h
      · exact hscrut h
      · simp only [Ty.freeVars] at h; rw [mem_TyList_freeVars] at h
        obtain ⟨t, ht, hgt⟩ := h; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
        simp only [Ty.freeVars, List.mem_singleton] at hgt; have := freshVars_ge x hx; omega
    have hS₀dom : w ∉ S₀.map Prod.fst := by
      intro hc; obtain ⟨p, hp, hpw⟩ := List.mem_map.mp hc
      rcases UnifyRel.dom_mem huni0 p hp with h | h
      · rw [hpw] at h; exact hscrut h
      · rw [hpw] at h; simp only [Ty.freeVars] at h; rw [mem_TyList_freeVars] at h
        obtain ⟨t, ht, hgt⟩ := h; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
        simp only [Ty.freeVars, List.mem_singleton] at hgt; have := freshVars_ge x hx; omega
    have hta : ∀ v ∈ (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy), w ∉ v.freeVars := by
      intro v hv hwv; obtain ⟨v0, hv0, rfl⟩ := List.mem_map.mp hv; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv0
      rcases Subst.mem_freeVars_onTy hwv with h | ⟨p, hp, hvp⟩
      · simp only [Ty.freeVars, List.mem_singleton] at h; have := freshVars_ge x hx; omega
      · exact hS₀ p hp hvp
    have hle0 := Infer.frontier_le hbody
    obtain ⟨hbS, hbτ, hbo⟩ := Infer.eOut_avoid hbody (w := w) (by omega)
      (by intro M hM; rcases List.mem_append.mp hM with hM | hM
          · obtain ⟨t, ht, rfl⟩ := List.mem_map.mp hM; obtain ⟨c, hc, rfl⟩ := List.mem_map.mp ht
            simpa only [PolyTy.mkTrivial] using Ty.not_mem_freeVars_openWith hta ((ctor.closed c hc).not_mem_freeVars w)
          · exact Subst.onCtx_avoid hctx hS₀ M hM)
      hbrs.1
    have hbdom := Infer.dom_avoid hbody (by omega)
      (by intro M hM; rcases List.mem_append.mp hM with hM | hM
          · obtain ⟨t, ht, rfl⟩ := List.mem_map.mp hM; obtain ⟨c, hc, rfl⟩ := List.mem_map.mp ht
            simpa only [PolyTy.mkTrivial] using Ty.not_mem_freeVars_openWith hta ((ctor.closed c hc).not_mem_freeVars w)
          · exact Subst.onCtx_avoid hctx hS₀ M hM)
      hbrs.1
    have hS₂ : ∀ p ∈ S₂, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni p hp w hwp with h | h
      · exact hbτ h
      · exact Subst.notMemOnTy hbS (Subst.notMemOnTy hS₀ hρ) h
    have hS₂dom : w ∉ S₂.map Prod.fst := by
      intro hc; obtain ⟨p, hp, hpw⟩ := List.mem_map.mp hc
      rcases UnifyRel.dom_mem huni p hp with h | h
      · rw [hpw] at h; exact hbτ h
      · rw [hpw] at h; exact Subst.notMemOnTy hbS (Subst.notMemOnTy hS₀ hρ) h
    have hrdom := InferBranches.dom_avoid hrest (w := w) (by omega)
      (Subst.onCtx_avoid (Subst.onCtx_avoid (Subst.onCtx_avoid hctx hS₀) hbS) hS₂)
      (Subst.notMemOnTy hS₂ (Subst.notMemOnTy hbS (Subst.notMemOnTy hS₀ hscrut)))
      (Subst.notMemOnTy hS₂ (Subst.notMemOnTy hbS (Subst.notMemOnTy hS₀ hρ)))
      hbrs.2
    intro hc; simp only [List.map_append, List.mem_append] at hc
    rcases hc with ((hc | hc) | hc) | hc
    · exact hS₀dom hc
    · exact hbdom hc
    · exact hS₂dom hc
    · exact hrdom hc
  | consWild hbody huni hrest =>
    intro w hwΦ hctx hscrut hρ hbrs
    expose_names
    simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append, not_or] at hbrs
    have hle1 := Infer.frontier_le hbody
    obtain ⟨hbS, hbτ, hbo⟩ := Infer.eOut_avoid hbody (w := w) hwΦ hctx hbrs.1
    have hbdom := Infer.dom_avoid hbody hwΦ hctx hbrs.1
    have hS₂ : ∀ p ∈ S₂, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni p hp w hwp with h | h
      · exact hbτ h
      · exact Subst.notMemOnTy hbS hρ h
    have hS₂dom : w ∉ S₂.map Prod.fst := by
      intro hc; obtain ⟨p, hp, hpw⟩ := List.mem_map.mp hc
      rcases UnifyRel.dom_mem huni p hp with h | h
      · rw [hpw] at h; exact hbτ h
      · rw [hpw] at h; exact Subst.notMemOnTy hbS hρ h
    have hrdom := InferBranches.dom_avoid hrest (w := w) (by omega)
      (Subst.onCtx_avoid (Subst.onCtx_avoid hctx hbS) hS₂)
      (Subst.notMemOnTy hS₂ (Subst.notMemOnTy hbS hscrut))
      (Subst.notMemOnTy hS₂ (Subst.notMemOnTy hbS hρ))
      hbrs.2
    intro hc; simp only [List.map_append, List.mem_append] at hc
    rcases hc with (hc | hc) | hc
    · exact hbdom hc
    · exact hS₂dom hc
    · exact hrdom hc
termination_by Expr.sizeBranches brs
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)
theorem InferRecGroup.dom_avoid {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut) :
    ∀ {w : Nat}, w < Φ → (∀ M ∈ ctx.env, w ∉ M.body.freeVars) →
    (∀ β ∈ targets, w ∉ β.freeVars) → w ∉ Expr.tyFreeVars.RecGroup.tyFreeVars bindings →
    w ∉ S.map Prod.fst := by
  cases h with
  | nil => intro w _ _ _ _; simp
  | cons he huni hrest =>
    intro w hwΦ hctx htargets hbinds
    expose_names
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append, not_or] at hbinds
    have hle1 := Infer.frontier_le he
    obtain ⟨heS, heτ, heo⟩ := Infer.eOut_avoid he (w := w) hwΦ hctx hbinds.1
    have hedom := Infer.dom_avoid he hwΦ hctx hbinds.1
    have hS₂ : ∀ p ∈ S₂, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni p hp w hwp with h | h
      · exact heτ h
      · exact Subst.notMemOnTy heS (htargets _ (List.mem_cons_self ..)) h
    have hS₂dom : w ∉ S₂.map Prod.fst := by
      intro hc; obtain ⟨p, hp, hpw⟩ := List.mem_map.mp hc
      rcases UnifyRel.dom_mem huni p hp with h | h
      · rw [hpw] at h; exact heτ h
      · rw [hpw] at h; exact Subst.notMemOnTy heS (htargets _ (List.mem_cons_self ..)) h
    have hrdom := InferRecGroup.dom_avoid hrest (w := w) (by omega)
      (Subst.onCtx_avoid (Subst.onCtx_avoid hctx heS) hS₂)
      (by intro β' hβ'; obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hβ'
          exact Subst.notMemOnTy hS₂ (Subst.notMemOnTy heS (htargets b (List.mem_cons_of_mem _ hb))))
      hbinds.2
    intro hc; simp only [List.map_append, List.mem_append] at hc
    rcases hc with (hc | hc) | hc
    · exact hedom hc
    · exact hS₂dom hc
    · exact hrdom hc
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)
theorem InferRecGroupAnn.dom_avoid {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut) :
    ∀ {w : Nat}, w < Φ → (∀ M ∈ ctx.env, w ∉ M.body.freeVars) →
    (∀ σ ∈ schemes, w ∉ σ.body.freeVars) → w ∉ Expr.tyFreeVars.RecGroup.tyFreeVars bindings →
    w ∉ S.map Prod.fst := by
  cases h with
  | nil => intro w _ _ _ _; simp
  | cons hΦN hinfer huni hesc1 _ hrest =>
    intro w hwΦ hctx hsch hbinds
    expose_names
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append, not_or] at hbinds
    have hle1 := Infer.frontier_le hinfer
    have hσbody : w ∉ σ.body.freeVars := hsch σ List.mem_cons_self
    have hwopen : w ∉ (e.openTyVars (freshVars N σ.paramCount)).tyFreeVars := by
      intro hc
      rcases Expr.tyFreeVars_openTyVars hc with h | h
      · exact hbinds.1 h
      · have := freshVars_ge w h; omega
    obtain ⟨heS, heτ, heo⟩ := Infer.eOut_avoid hinfer (w := w) (by omega) hctx hwopen
    have hedom := Infer.dom_avoid hinfer (by omega) hctx hwopen
    have hσopen : w ∉ (σ.openVars (freshVars N σ.paramCount)).freeVars := by
      intro hc
      rcases Ty.freeVars_openVars_subset w hc with h | h
      · exact hσbody h
      · have := freshVars_ge w h; omega
    have hSchk : ∀ p ∈ Schk, w ∉ p.2.freeVars := by
      intro p hp hwp
      rcases UnifyRel.range_mem huni p hp w hwp with h | h
      · exact heτ h
      · exact hσopen h
    have hSchkdom : w ∉ Schk.map Prod.fst := by
      intro hc; obtain ⟨p, hp, hpw⟩ := List.mem_map.mp hc
      rcases UnifyRel.dom_mem huni p hp with h | h
      · rw [hpw] at h; exact heτ h
      · rw [hpw] at h; exact hσopen h
    have hrdom := InferRecGroupAnn.dom_avoid hrest (w := w) (by omega)
      (Subst.onCtx_avoid (Subst.onCtx_avoid hctx heS) hSchk)
      (fun σ' hσ' => hsch σ' (List.mem_cons_of_mem _ hσ'))
      hbinds.2
    intro hc; simp only [List.map_append, List.mem_append] at hc
    rcases hc with (hc | hc) | hc
    · exact hedom hc
    · exact hSchkdom hc
    · exact hrdom hc
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end

/-- **Survival under substitution.** A free var not in `S`'s domain survives
    applying `S` (its occurrences are untouched). Used by the honest `letIn` case
    to show the generalised vars `genV` (which are not env-fixed) are `≥ Φ`. -/
theorem Ty.mem_freeVars_onTy_of_not_dom {S : Subst} {τ : Ty} {g : Nat}
    (hg : g ∈ τ.freeVars) (hdom : ∀ p ∈ S, p.1 ≠ g) : g ∈ (S.onTy τ).freeVars := by
  rw [Subst.onTy, Ty.mem_freeVars_substFvars_image]
  refine ⟨g, hg, ?_⟩
  rw [Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
    simp only [Ty.freeVars, List.mem_singleton] at hc; exact hdom p hp hc)]
  simp [Ty.freeVars]

/-! ### `substTyFvars`-over-`letRecElab` distribution (honest `letRec` soundness)

Type-substitution only rewrites type annotations/tyArgs while term-variable
shifting (`shiftFrom`) only renumbers term de Bruijn indices, so they commute;
combined with `Subst.onPolyTy_genGroup` (push `S` through the generalised scheme,
`S` avoiding the gen-pool `G`) and the Step-A close/subst commutation, a
substitution avoiding `G` distributes over the whole `Λ`-outside `letRecElab`. -/

theorem Expr.substTyFvars_shiftFrom {S : Subst} :
    ∀ (e : Expr) (t n : Nat),
      (e.shiftFrom t n).substTyFvars S = (e.substTyFvars S).shiftFrom t n := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p =>
    intro t n
    have h : (Expr.primLit p).substTyFvars S = Expr.primLit p :=
      Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun q _ hc => by simp [Expr.tyFreeVars] at hc)
    simp only [Expr.shiftFrom, h]
  | ctor c =>
    intro t n
    have h : (Expr.ctor c).substTyFvars S = Expr.ctor c :=
      Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun q _ hc => by simp [Expr.tyFreeVars] at hc)
    simp only [Expr.shiftFrom, h]
  | var i tyArgs =>
    intro t n
    rw [Expr.substTyFvars_var]
    simp only [Expr.shiftFrom]
    split <;> rw [Expr.substTyFvars_var]
  | lambda ann body ih =>
    intro t n
    simp only [Expr.shiftFrom, Expr.substTyFvars_lambda, Expr.lambda.injEq, true_and]
    exact ih (t + 1) n
  | app f arg ihf iharg =>
    intro t n
    simp only [Expr.shiftFrom, Expr.substTyFvars_app, Expr.app.injEq]
    exact ⟨ihf t n, iharg t n⟩
  | letIn ann rhs body ihr ihb =>
    intro t n
    simp only [Expr.shiftFrom, Expr.substTyFvars_letIn, Expr.letIn.injEq, true_and]
    exact ⟨ihr t n, ihb (t + 1) n⟩
  | match_ scrut branches ihs ihbr =>
    intro t n
    simp only [Expr.shiftFrom, Expr.substTyFvars_match, Expr.match_.injEq]
    refine ⟨ihs t n, ?_⟩
    induction branches with
    | nil => rfl
    | cons hd tl ihtl =>
      obtain ⟨p, b⟩ := hd
      show ((p, (b.shiftFrom (t + p.bindCount) n).substTyFvars S) :: _)
          = ((p, (b.substTyFvars S).shiftFrom (t + p.bindCount) n) :: _)
      rw [ihbr p b List.mem_cons_self (t + p.bindCount) n,
          ihtl (fun p' b' hm => ihbr p' b' (List.mem_cons_of_mem _ hm))]
  | letRec bindings body ihbs ihb =>
    intro t n
    simp only [Expr.shiftFrom, Expr.substTyFvars_letRec, List.length_map, Expr.letRec.injEq]
    refine ⟨?_, ihb (t + bindings.length) n⟩
    generalize t + bindings.length = thr
    induction bindings with
    | nil => rfl
    | cons hd tl ihtl =>
      show ((hd.shiftFrom thr n).substTyFvars S :: _)
          = ((hd.substTyFvars S).shiftFrom thr n :: _)
      rw [ihbs hd List.mem_cons_self thr n,
          ihtl (fun e' hm => ihbs e' (List.mem_cons_of_mem _ hm))]
  | letRecAnn schemes bindings body ihbs ihb =>
    intro t n
    simp only [Expr.shiftFrom, Expr.substTyFvars_letRecAnn, List.length_map, Expr.letRecAnn.injEq,
      true_and]
    refine ⟨?_, ihb (t + bindings.length) n⟩
    generalize t + bindings.length = thr
    induction bindings with
    | nil => rfl
    | cons hd tl ihtl =>
      show ((hd.shiftFrom thr n).substTyFvars S :: _)
          = ((hd.substTyFvars S).shiftFrom thr n :: _)
      rw [ihbs hd List.mem_cons_self thr n,
          ihtl (fun e' hm => ihbs e' (List.mem_cons_of_mem _ hm))]

/-- `genFilter` is invariant under a substitution avoiding the gen-pool `G`
    (`G`-members survive `S` iff they were already present). -/
theorem Ty.genFilter_onTy {S : Subst} {G : List Nat}
    (hSG : ∀ p ∈ S, p.1 ∉ G) (hSran : ∀ p ∈ S, ∀ u ∈ p.2.freeVars, u ∉ G) {τ : Ty} :
    Ty.genFilter G (S.onTy τ) = Ty.genFilter G τ := by
  simp only [Ty.genFilter]
  apply List.filter_congr
  intro g hg
  simp only [decide_eq_decide]
  constructor
  · intro h
    rcases Subst.mem_freeVars_onTy h with h | ⟨p, hp, hgp⟩
    · exact h
    · exact absurd hg (hSran p hp g hgp)
  · intro h
    exact Ty.mem_freeVars_onTy_of_not_dom h (by rintro p hp rfl; exact hSG p hp hg)

/-- Distribution of a `G`-avoiding substitution over one `letRecElabNest` level. -/
theorem Expr.substTyFvars_letRecElabNest {S : Subst} {G : List Nat} {n : Nat} {bs : List Expr}
    (hSG : ∀ p ∈ S, p.1 ∉ G) (hSran : ∀ p ∈ S, ∀ u ∈ p.2.freeVars, u ∉ G) :
    ∀ (L : List (Nat × Ty)) (body : Expr),
      (Expr.letRecElabNest G n bs L body).substTyFvars S
        = Expr.letRecElabNest G n (bs.map (·.substTyFvars S))
            (L.map (fun p => (p.1, S.onTy p.2))) (body.substTyFvars S) := by
  intro L
  induction L with
  | nil => intro body; rfl
  | cons hd tl ih =>
    obtain ⟨i, τ⟩ := hd
    intro body
    simp only [Expr.letRecElabNest, List.map_cons]
    rw [Expr.substTyFvars_letIn, ih body]
    have hSgf : ∀ p ∈ S, p.1 ∉ Ty.genFilter G τ :=
      fun p hp hc => hSG p hp (by simp only [Ty.genFilter, List.mem_filter] at hc; exact hc.1)
    have hSgfran : ∀ p ∈ S, ∀ u ∈ p.2.freeVars, u ∉ Ty.genFilter G τ :=
      fun p hp u hu hc => hSran p hp u hu (by
        simp only [Ty.genFilter, List.mem_filter] at hc; exact hc.1)
    congr 1
    · simp only [Option.map_some, Option.some.injEq]
      show Subst.onPolyTy S (PolyTy.genGroup G τ) = PolyTy.genGroup G (S.onTy τ)
      exact Subst.onPolyTy_genGroup hSG hSran
    · rw [Expr.substTyFvars_closeTyVars hSgf hSgfran, Expr.substTyFvars_letRec,
          Expr.substTyFvars_var, Ty.genFilter_onTy hSG hSran]
      congr 2
      rw [List.map_map, List.map_map]
      exact List.map_congr_left (fun e _ => Expr.substTyFvars_shiftFrom e n (n - 1 - i))

theorem Expr.substTyFvars_letRecElab {S : Subst} {G : List Nat} {bs : List Expr} {τs : List Ty}
    {body : Expr}
    (hSG : ∀ p ∈ S, p.1 ∉ G) (hSran : ∀ p ∈ S, ∀ u ∈ p.2.freeVars, u ∉ G) :
    (Expr.letRecElab G bs τs body).substTyFvars S
      = Expr.letRecElab G (bs.map (·.substTyFvars S)) (τs.map S.onTy) (body.substTyFvars S) := by
  simp only [Expr.letRecElab, List.length_map]
  rw [Expr.substTyFvars_letRecElabNest hSG hSran]
  congr 1
  rw [List.map_reverse]
  congr 1
  rw [List.zip_map_right]
  exact List.map_congr_left (fun p _ => rfl)

/-- An `InferRecGroup` derivation preserves the group length in its output. -/
theorem InferRecGroup.bindingsOut_length {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut) :
    bindingsOut.length = bindings.length := by
  induction bindings generalizing Φ ctx targets Φ' S bindingsOut with
  | nil => cases h with | nil => rfl
  | cons e rest ih =>
    cases h with
    | cons _ _ hrest => simp only [List.length_cons]; exact congrArg (· + 1) (ih hrest)

/-- An `InferRecGroupAnn` derivation preserves the group length in its output. -/
theorem InferRecGroupAnn.bindingsOut_length {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut) :
    bindingsOut.length = bindings.length := by
  induction bindings generalizing Φ ctx schemes Φ' S bindingsOut with
  | nil => cases h with | nil => rfl
  | cons e rest ih =>
    cases h with
    | cons _ _ _ _ _ hrest => simp only [List.length_cons]; exact congrArg (· + 1) (ih hrest)

/-- An `InferRecGroupAnn` derivation checks exactly as many bindings as schemes. -/
theorem InferRecGroupAnn.length_eq {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut) :
    bindings.length = schemes.length := by
  induction bindings generalizing Φ ctx schemes Φ' S bindingsOut with
  | nil => cases h with | nil => rfl
  | cons e rest ih =>
    cases h with
    | cons _ _ _ _ _ hrest => simp only [List.length_cons]; exact congrArg (· + 1) (ih hrest)

/-- A `substFvars`-mapped scheme list's scheme-body free var traces back to a source
    scheme-body var, mapped by `S`. -/
private theorem SchemeList.mem_recAnnSchemeFreeVars_substFvars {S : List (Nat × Ty)} {g : Nat} :
    ∀ (schemes : List PolyTy),
      g ∈ Expr.tyFreeVars.SchemeList.tyFreeVars (schemes.map (PolyTy.substFvars S)) →
      ∃ g₀ ∈ Expr.tyFreeVars.SchemeList.tyFreeVars schemes,
        g ∈ (Ty.substFvars S (Ty.fvar g₀)).freeVars := by
  intro schemes
  induction schemes with
  | nil => intro h; simp [Expr.tyFreeVars.SchemeList.tyFreeVars] at h
  | cons σ ss ih =>
    intro h
    simp only [List.map_cons, Expr.tyFreeVars.SchemeList.tyFreeVars, List.mem_append,
      PolyTy.body_substFvars] at h
    rcases h with h | h
    · obtain ⟨g₀, hg₀, hg⟩ := Ty.mem_freeVars_substFvars_image.mp h
      exact ⟨g₀, by simp only [Expr.tyFreeVars.SchemeList.tyFreeVars, List.mem_append]
                    exact Or.inl hg₀, hg⟩
    · obtain ⟨g₀, hg₀, hg⟩ := ih h
      exact ⟨g₀, by simp only [Expr.tyFreeVars.SchemeList.tyFreeVars, List.mem_append]
                    exact Or.inr hg₀, hg⟩

/-- A scheme-body free var of `e.substTyFvars S` traces back to a source scheme-body var
    `g₀ ∈ e.recAnnSchemeFreeVars`, mapped by `S` (the sharp form needed to keep `S`'s key
    in view). -/
theorem Expr.mem_recAnnSchemeFreeVars_substTyFvars {S : List (Nat × Ty)} {g : Nat} :
    ∀ {e : Expr}, g ∈ (e.substTyFvars S).recAnnSchemeFreeVars →
      ∃ g₀ ∈ e.recAnnSchemeFreeVars, g ∈ (Ty.substFvars S (Ty.fvar g₀)).freeVars := by
  intro e
  induction e using Expr.rec_strong with
  | primLit p => intro h; rw [Expr.substTyFvars_primLit] at h; simp [Expr.recAnnSchemeFreeVars] at h
  | ctor c => intro h; rw [Expr.substTyFvars_ctor] at h; simp [Expr.recAnnSchemeFreeVars] at h
  | var i tyArgs => intro h; rw [Expr.substTyFvars_var] at h; simp [Expr.recAnnSchemeFreeVars] at h
  | lambda ann body ih =>
    intro h; rw [Expr.substTyFvars_lambda] at h
    simp only [Expr.recAnnSchemeFreeVars] at h ⊢; exact ih h
  | app f arg ihf iharg =>
    intro h; rw [Expr.substTyFvars_app] at h
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at h ⊢
    rcases h with h | h
    · obtain ⟨g₀, hg₀, hg⟩ := ihf h; exact ⟨g₀, Or.inl hg₀, hg⟩
    · obtain ⟨g₀, hg₀, hg⟩ := iharg h; exact ⟨g₀, Or.inr hg₀, hg⟩
  | letIn ann rhs body ihr ihb =>
    intro h; rw [Expr.substTyFvars_letIn] at h
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at h ⊢
    rcases h with h | h
    · obtain ⟨g₀, hg₀, hg⟩ := ihr h; exact ⟨g₀, Or.inl hg₀, hg⟩
    · obtain ⟨g₀, hg₀, hg⟩ := ihb h; exact ⟨g₀, Or.inr hg₀, hg⟩
  | match_ scrut branches ihs ihbr =>
    intro h; rw [Expr.substTyFvars_match] at h
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at h ⊢
    rcases h with h | h
    · obtain ⟨g₀, hg₀, hg⟩ := ihs h; exact ⟨g₀, Or.inl hg₀, hg⟩
    · rw [Expr.mem_recAnnSchemeFreeVars_BranchList] at h
      obtain ⟨pb, hpb, hgb⟩ := h
      obtain ⟨⟨p₀, b₀⟩, hpb₀, rfl⟩ := List.mem_map.mp hpb
      obtain ⟨g₀, hg₀, hg⟩ := ihbr p₀ b₀ hpb₀ hgb
      exact ⟨g₀, Or.inr (Expr.mem_recAnnSchemeFreeVars_BranchList.mpr ⟨(p₀, b₀), hpb₀, hg₀⟩), hg⟩
  | letRec bindings body ihbs ihb =>
    intro h; rw [Expr.substTyFvars_letRec] at h
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at h ⊢
    rcases h with h | h
    · rw [Expr.mem_recAnnSchemeFreeVars_RecGroup] at h
      obtain ⟨e', he', hge'⟩ := h
      obtain ⟨e₀, he₀, rfl⟩ := List.mem_map.mp he'
      obtain ⟨g₀, hg₀, hg⟩ := ihbs e₀ he₀ hge'
      exact ⟨g₀, Or.inl (Expr.mem_recAnnSchemeFreeVars_RecGroup.mpr ⟨e₀, he₀, hg₀⟩), hg⟩
    · obtain ⟨g₀, hg₀, hg⟩ := ihb h; exact ⟨g₀, Or.inr hg₀, hg⟩
  | letRecAnn schemes bindings body ihbs ihb =>
    intro h; rw [Expr.substTyFvars_letRecAnn] at h
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at h ⊢
    rcases h with (h | h) | h
    · obtain ⟨g₀, hg₀, hg⟩ := SchemeList.mem_recAnnSchemeFreeVars_substFvars schemes h
      exact ⟨g₀, Or.inl (Or.inl hg₀), hg⟩
    · rw [Expr.mem_recAnnSchemeFreeVars_RecGroup] at h
      obtain ⟨e', he', hge'⟩ := h
      obtain ⟨e₀, he₀, rfl⟩ := List.mem_map.mp he'
      obtain ⟨g₀, hg₀, hg⟩ := ihbs e₀ he₀ hge'
      exact ⟨g₀, Or.inl (Or.inr (Expr.mem_recAnnSchemeFreeVars_RecGroup.mpr ⟨e₀, he₀, hg₀⟩)), hg⟩
    · obtain ⟨g₀, hg₀, hg⟩ := ihb h; exact ⟨g₀, Or.inr hg₀, hg⟩

/-- A scheme-body free var of the Λ-outside `letRecElab` encoding comes from one of the
    raw bindings or the body (the wrapper `letIn`/`letRec`/`closeTyVars`/`shiftFrom`
    layers introduce none, and leave `letRecAnn` scheme bodies untouched). -/
theorem Expr.recAnnSchemeFreeVars_letRecElabNest {G : List Nat} {n : Nat} {bs : List Expr} {g : Nat} :
    ∀ {L : List (Nat × Ty)} {body : Expr},
      g ∈ (Expr.letRecElabNest G n bs L body).recAnnSchemeFreeVars →
      (∃ e ∈ bs, g ∈ e.recAnnSchemeFreeVars) ∨ g ∈ body.recAnnSchemeFreeVars := by
  intro L
  induction L with
  | nil => intro body h; exact Or.inr h
  | cons hd tl ih =>
    obtain ⟨i, τ⟩ := hd
    intro body h
    simp only [Expr.letRecElabNest, Expr.recAnnSchemeFreeVars, List.mem_append] at h
    rcases h with h | h
    · rw [Expr.recAnnSchemeFreeVars_closeTyVars] at h
      simp only [Expr.recAnnSchemeFreeVars, List.append_nil] at h
      rw [Expr.mem_recAnnSchemeFreeVars_RecGroup] at h
      obtain ⟨e', he', hge'⟩ := h
      obtain ⟨e₀, he₀, rfl⟩ := List.mem_map.mp he'
      rw [Expr.recAnnSchemeFreeVars_shiftFrom] at hge'
      exact Or.inl ⟨e₀, he₀, hge'⟩
    · rcases ih h with hh | hh
      · exact Or.inl hh
      · exact Or.inr hh

theorem Expr.recAnnSchemeFreeVars_letRecElab {G : List Nat} {bs : List Expr} {τs : List Ty}
    {body : Expr} {g : Nat}
    (h : g ∈ (Expr.letRecElab G bs τs body).recAnnSchemeFreeVars) :
    (∃ e ∈ bs, g ∈ e.recAnnSchemeFreeVars) ∨ g ∈ body.recAnnSchemeFreeVars := by
  simp only [Expr.letRecElab] at h
  exact Expr.recAnnSchemeFreeVars_letRecElabNest h

/-! **The `K`-locality invariant.** The elaborated output's `letRecAnn` scheme-body free
    vars stay within the ambient skolem set `K`: source scheme bodies are `⊆ K` and the
    derivation's substitution avoids `K`, so substitution never moves them out of `K`. -/
mutual
theorem Infer.eOut_recAnnScheme_in_K {Φ ctx e Φ' S eOut τ} {K : List Nat}
    (h : Infer Φ ctx e Φ' S eOut τ)
    (hKe : ∀ y ∈ e.recAnnSchemeFreeVars, y ∈ K) (hSK : ∀ p ∈ S, p.1 ∉ K) :
    ∀ g ∈ eOut.recAnnSchemeFreeVars, g ∈ K := by
  cases h with
  | primLitUnit => intro g hg; simp [Expr.recAnnSchemeFreeVars] at hg
  | primLitInt => intro g hg; simp [Expr.recAnnSchemeFreeVars] at hg
  | primLitNat => intro g hg; simp [Expr.recAnnSchemeFreeVars] at hg
  | primLitStr => intro g hg; simp [Expr.recAnnSchemeFreeVars] at hg
  | var hlook => intro g hg; simp [Expr.recAnnSchemeFreeVars] at hg
  | ctor hlook => intro g hg; simp [Expr.recAnnSchemeFreeVars] at hg
  | lambda hseed hbody =>
    intro g hg
    simp only [Expr.recAnnSchemeFreeVars] at hg ⊢
    exact Infer.eOut_recAnnScheme_in_K hbody hKe hSK g hg
  | app hf harg huni =>
    intro g hg
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at hg
    rcases hg with hg | hg
    · exact Infer.eOut_recAnnScheme_in_K hf
        (fun y hy => hKe y (by simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inl hy))
        (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ hp))) g hg
    · exact Infer.eOut_recAnnScheme_in_K harg
        (fun y hy => hKe y (by simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inr hy))
        (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_right _ hp))) g hg
  | letIn hrhs hbody =>
    intro g hg
    expose_names
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at hg
    rcases hg with hg | hg
    · rw [Expr.recAnnSchemeFreeVars_closeTyVars] at hg
      obtain ⟨g₀, hg₀, hgg₀⟩ := Expr.mem_recAnnSchemeFreeVars_substTyFvars hg
      have hg₀K : g₀ ∈ K := Infer.eOut_recAnnScheme_in_K hrhs
        (fun y hy => hKe y (by simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inl hy))
        (fun p hp => hSK p (List.mem_append_left _ hp)) g₀ hg₀
      rw [Ty.substFvars_eq_self_of_no_key (fun p hp hcc => by
        simp only [Ty.freeVars, List.mem_singleton] at hcc
        exact hSK p (List.mem_append_left _ hp) (hcc ▸ hg₀K))] at hgg₀
      simp only [Ty.freeVars, List.mem_singleton] at hgg₀
      exact hgg₀ ▸ hg₀K
    · exact Infer.eOut_recAnnScheme_in_K hbody
        (fun y hy => hKe y (by simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inr hy))
        (fun p hp => hSK p (List.mem_append_right _ hp)) g hg
  | letInAnn hσwf hΦN hrhs huni hesc1 hesc2 hbody =>
    intro g hg
    expose_names
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at hg
    rcases hg with hg | hg
    · rw [Expr.recAnnSchemeFreeVars_closeTyVars] at hg
      obtain ⟨g₀, hg₀, hgg₀⟩ := Expr.mem_recAnnSchemeFreeVars_substTyFvars hg
      have hg₀K : g₀ ∈ K := Infer.eOut_recAnnScheme_in_K hrhs
        (fun y hy => hKe y (by
          rw [Expr.recAnnSchemeFreeVars_openTyVars] at hy
          simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inl hy))
        (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ hp))) g₀ hg₀
      rw [Ty.substFvars_eq_self_of_no_key (fun p hp hcc => by
        simp only [Ty.freeVars, List.mem_singleton] at hcc
        exact hSK p (List.mem_append_left _ hp) (hcc ▸ hg₀K))] at hgg₀
      simp only [Ty.freeVars, List.mem_singleton] at hgg₀
      exact hgg₀ ▸ hg₀K
    · exact Infer.eOut_recAnnScheme_in_K hbody
        (fun y hy => hKe y (by simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inr hy))
        (fun p hp => hSK p (List.mem_append_right _ hp)) g hg
  | match_ hscrut hne hbr =>
    intro g hg
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at hg
    rcases hg with hg | hg
    · exact Infer.eOut_recAnnScheme_in_K hscrut
        (fun y hy => hKe y (by simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inl hy))
        (fun p hp => hSK p (List.mem_append_left _ hp)) g hg
    · exact InferBranches.eOut_recAnnScheme_in_K hbr
        (fun y hy => hKe y (by simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inr hy))
        (fun p hp => hSK p (List.mem_append_right _ hp)) g hg
  | letRec hgroup hbody =>
    intro g hg
    expose_names
    rcases Expr.recAnnSchemeFreeVars_letRecElab hg with ⟨e', he', hge'⟩ | hgb
    · obtain ⟨e₀, he₀, rfl⟩ := List.mem_map.mp he'
      obtain ⟨g₀, hg₀, hgg₀⟩ := Expr.mem_recAnnSchemeFreeVars_substTyFvars hge'
      have hg₀K : g₀ ∈ K := InferRecGroup.eOut_recAnnScheme_in_K hgroup
        (fun y hy => hKe y (by simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inl hy))
        (fun p hp => hSK p (List.mem_append_left _ hp)) g₀
        (Expr.mem_recAnnSchemeFreeVars_RecGroup.mpr ⟨e₀, he₀, hg₀⟩)
      rw [Ty.substFvars_eq_self_of_no_key (fun p hp hcc => by
        simp only [Ty.freeVars, List.mem_singleton] at hcc
        exact hSK p (List.mem_append_left _ hp) (hcc ▸ hg₀K))] at hgg₀
      simp only [Ty.freeVars, List.mem_singleton] at hgg₀
      exact hgg₀ ▸ hg₀K
    · exact Infer.eOut_recAnnScheme_in_K hbody
        (fun y hy => hKe y (by simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inr hy))
        (fun p hp => hSK p (List.mem_append_right _ hp)) g hgb
  | letRecAnn hwf hlen hgroup hbody =>
    intro g hg
    expose_names
    simp only [Expr.recAnnSchemeFreeVars, List.mem_append] at hg
    rcases hg with (hg | hg) | hg
    · exact hKe g (by simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inl (Or.inl hg))
    · exact InferRecGroupAnn.eOut_recAnnScheme_in_K hgroup
        (fun y hy => hKe y (by
          simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inl (Or.inr hy)))
        (fun p hp => hSK p (List.mem_append_left _ hp)) g hg
    · exact Infer.eOut_recAnnScheme_in_K hbody
        (fun y hy => hKe y (by simp only [Expr.recAnnSchemeFreeVars, List.mem_append]; exact Or.inr hy))
        (fun p hp => hSK p (List.mem_append_right _ hp)) g hg
termination_by e.size
decreasing_by all_goals (try subst_vars; try simp only [Expr.size, Expr.size_openTyVars]; omega)
theorem InferBranches.eOut_recAnnScheme_in_K {Φ ctx scrutTy ρ brs Φ' S brsOut} {K : List Nat}
    (h : InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut)
    (hKe : ∀ y ∈ Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars brs, y ∈ K)
    (hSK : ∀ p ∈ S, p.1 ∉ K) :
    ∀ g ∈ Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars brsOut, g ∈ K := by
  cases h with
  | nil => intro g hg; simp [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars] at hg
  | cons hlook hn huni0 hbody huni hrest =>
    intro g hg
    expose_names
    simp only [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars, List.mem_append] at hg ⊢
    rcases hg with hg | hg
    · exact Infer.eOut_recAnnScheme_in_K hbody
        (fun y hy => hKe y (by
          simp only [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars, List.mem_append]
          exact Or.inl hy))
        (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_right _ hp)))) g hg
    · exact InferBranches.eOut_recAnnScheme_in_K hrest
        (fun y hy => hKe y (by
          simp only [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars, List.mem_append]
          exact Or.inr hy))
        (fun p hp => hSK p (List.mem_append_right _ hp)) g hg
  | consWild hbody huni hrest =>
    intro g hg
    expose_names
    simp only [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars, List.mem_append] at hg ⊢
    rcases hg with hg | hg
    · exact Infer.eOut_recAnnScheme_in_K hbody
        (fun y hy => hKe y (by
          simp only [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars, List.mem_append]
          exact Or.inl hy))
        (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ hp))) g hg
    · exact InferBranches.eOut_recAnnScheme_in_K hrest
        (fun y hy => hKe y (by
          simp only [Expr.recAnnSchemeFreeVars.BranchList.recAnnSchemeFreeVars, List.mem_append]
          exact Or.inr hy))
        (fun p hp => hSK p (List.mem_append_right _ hp)) g hg
termination_by Expr.sizeBranches brs
decreasing_by all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)
theorem InferRecGroup.eOut_recAnnScheme_in_K {Φ ctx bindings targets Φ' S bindingsOut} {K : List Nat}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut)
    (hKe : ∀ y ∈ Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars bindings, y ∈ K)
    (hSK : ∀ p ∈ S, p.1 ∉ K) :
    ∀ g ∈ Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars bindingsOut, g ∈ K := by
  cases h with
  | nil => intro g hg; simp [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars] at hg
  | cons he huni hrest =>
    intro g hg
    expose_names
    simp only [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars, List.mem_append] at hg ⊢
    rcases hg with hg | hg
    · exact Infer.eOut_recAnnScheme_in_K he
        (fun y hy => hKe y (by
          simp only [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars, List.mem_append]
          exact Or.inl hy))
        (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ hp))) g hg
    · exact InferRecGroup.eOut_recAnnScheme_in_K hrest
        (fun y hy => hKe y (by
          simp only [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars, List.mem_append]
          exact Or.inr hy))
        (fun p hp => hSK p (List.mem_append_right _ hp)) g hg
termination_by Expr.sizeRecGroup bindings
decreasing_by all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)
theorem InferRecGroupAnn.eOut_recAnnScheme_in_K {Φ ctx bindings schemes Φ' S bindingsOut} {K : List Nat}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut)
    (hKe : ∀ y ∈ Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars bindings, y ∈ K)
    (hSK : ∀ p ∈ S, p.1 ∉ K) :
    ∀ g ∈ Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars bindingsOut, g ∈ K := by
  cases h with
  | nil => intro g hg; simp [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars] at hg
  | cons hΦN hinfer huni hesc1 hesc2 hrest =>
    intro g hg
    expose_names
    simp only [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars, List.mem_append] at hg ⊢
    rcases hg with hg | hg
    · rw [Expr.recAnnSchemeFreeVars_closeTyVars] at hg
      obtain ⟨g₀, hg₀, hgg₀⟩ := Expr.mem_recAnnSchemeFreeVars_substTyFvars hg
      have hg₀K : g₀ ∈ K := Infer.eOut_recAnnScheme_in_K hinfer
        (fun y hy => hKe y (by
          rw [Expr.recAnnSchemeFreeVars_openTyVars] at hy
          simp only [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars, List.mem_append]
          exact Or.inl hy))
        (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ hp))) g₀ hg₀
      rw [Ty.substFvars_eq_self_of_no_key (fun p hp hcc => by
        simp only [Ty.freeVars, List.mem_singleton] at hcc
        exact hSK p (List.mem_append_left _ hp) (hcc ▸ hg₀K))] at hgg₀
      simp only [Ty.freeVars, List.mem_singleton] at hgg₀
      exact hgg₀ ▸ hg₀K
    · exact InferRecGroupAnn.eOut_recAnnScheme_in_K hrest
        (fun y hy => hKe y (by
          simp only [Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars, List.mem_append]
          exact Or.inr hy))
        (fun p hp => hSK p (List.mem_append_right _ hp)) g hg
termination_by Expr.sizeRecGroup bindings
decreasing_by all_goals (try subst_vars; try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end

/-- The substituted elaborated output's scheme-body free vars stay within the ambient
    skolem set `K`: combine the `eOut.recAnnSchemeFreeVars ⊆ K` invariant with the sharp
    substitution-membership lemma; `S` avoids `K` so it fixes the in-`K` source var. -/
theorem Infer.recAnnSchemeFreeVars_substTyFvars_in_K {Φ ctx e Φ' S₀ eOut τ}
    {S : Subst} {K : List Nat} {g : Nat}
    (h : Infer Φ ctx e Φ' S₀ eOut τ)
    (hKe : ∀ y ∈ e.recAnnSchemeFreeVars, y ∈ K)
    (hSK₀ : ∀ p ∈ S₀, p.1 ∉ K) (hSK : ∀ p ∈ S, p.1 ∉ K)
    (hc : g ∈ (eOut.substTyFvars S).recAnnSchemeFreeVars) : g ∈ K := by
  obtain ⟨g₀, hg₀, hgg₀⟩ := Expr.mem_recAnnSchemeFreeVars_substTyFvars hc
  have hg₀K : g₀ ∈ K := Infer.eOut_recAnnScheme_in_K h hKe hSK₀ g₀ hg₀
  rw [Ty.substFvars_eq_self_of_no_key (fun p hp hcc => by
    simp only [Ty.freeVars, List.mem_singleton] at hcc
    exact hSK p hp (hcc ▸ hg₀K))] at hgg₀
  simp only [Ty.freeVars, List.mem_singleton] at hgg₀
  exact hgg₀ ▸ hg₀K

/-- Group form of `recAnnSchemeFreeVars_substTyFvars_in_K`: a scheme-body free var of a
    `substTyFvars`-mapped elaborated binding stays within `K`. -/
theorem InferRecGroup.recAnnSchemeFreeVars_substTyFvars_in_K
    {Φ ctx bindings targets Φ' S₀ bindingsOut} {S : Subst} {K : List Nat} {g : Nat} {e : Expr}
    (h : InferRecGroup Φ ctx bindings targets Φ' S₀ bindingsOut)
    (hKe : ∀ y ∈ Expr.recAnnSchemeFreeVars.RecGroup.recAnnSchemeFreeVars bindings, y ∈ K)
    (hSK₀ : ∀ p ∈ S₀, p.1 ∉ K) (hSK : ∀ p ∈ S, p.1 ∉ K)
    (he : e ∈ bindingsOut.map (·.substTyFvars S))
    (hc : g ∈ e.recAnnSchemeFreeVars) : g ∈ K := by
  obtain ⟨e₀, he₀, rfl⟩ := List.mem_map.mp he
  obtain ⟨g₀, hg₀, hgg₀⟩ := Expr.mem_recAnnSchemeFreeVars_substTyFvars hc
  have hg₀K : g₀ ∈ K := InferRecGroup.eOut_recAnnScheme_in_K h hKe hSK₀ g₀
    (Expr.mem_recAnnSchemeFreeVars_RecGroup.mpr ⟨e₀, he₀, hg₀⟩)
  rw [Ty.substFvars_eq_self_of_no_key (fun p hp hcc => by
    simp only [Ty.freeVars, List.mem_singleton] at hcc
    exact hSK p hp (hcc ▸ hg₀K))] at hgg₀
  simp only [Ty.freeVars, List.mem_singleton] at hgg₀
  exact hgg₀ ▸ hg₀K

set_option maxRecDepth 8000 in
mutual
/-- Soundness of `Infer` against the declarative `TypeOfElabHM`: applying the
    inferred substitution to the context yields a declarative typing. -/
theorem Infer.sound {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ) :
    CtxWF ctx → CtxBelow Φ ctx → (K : List Nat) → (∀ k ∈ K, k < Φ) →
    (∀ y ∈ e.tyFreeVars, y ∈ K) → (∀ p ∈ S, p.1 ∉ K) →
    TypeOfElabHM (S.onCtx ctx) (eOut.substTyFvars S) τ := by
  cases h with
  | primLitUnit => intro _ _ _ _ _ _; simp only [Subst.onCtx_nil, Expr.substTyFvars]; exact .primLitUnit
  | primLitInt => intro _ _ _ _ _ _; simp only [Subst.onCtx_nil, Expr.substTyFvars]; exact .primLitInt
  | primLitNat => intro _ _ _ _ _ _; simp only [Subst.onCtx_nil, Expr.substTyFvars]; exact .primLitNat
  -- | primLitBool => intro _ _ _ _ _ _; simp only [Subst.onCtx_nil, Expr.substTyFvars]; exact .primLitBool
  | primLitStr => intro _ _ _ _ _ _; simp only [Subst.onCtx_nil, Expr.substTyFvars]; exact .primLitStr
  | lambda hseed hbody =>
    intro hctx hbelow K hKΦ hKe hSK
    rw [Expr.substTyFvars_lambda]
    cases hseed
    case none =>
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append] at hKe
      have hbodyWF : CtxWF { ctx with env := PolyTy.mkTrivial (.fvar Φ) :: ctx.env } := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact ContainsBvarsUpTo.fvar
        · exact hctx M hM
      have hbodyBelow : CtxBelow (Φ + 1) { ctx with env := PolyTy.mkTrivial (.fvar Φ) :: ctx.env } := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact .fvar (by omega)
        · exact (hbelow M hM).mono (by omega)
      refine TypeOfElabHM.lambda
        (Subst.onTy_lc (Infer.lc hbody hbodyWF).2 ContainsBvarsUpTo.fvar)
        (fun T hT => absurd hT (by simp)) rfl
        (Infer.sound hbody hbodyWF hbodyBelow K (fun k hk => by have := hKΦ k hk; omega) hKe hSK)
    case some hcl =>
      expose_names
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at hKe
      have hbodyWF : CtxWF { ctx with env := PolyTy.mkTrivial paramTy :: ctx.env } := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact hcl
        · exact hctx M hM
      have hbodyBelow : CtxBelow Φ { ctx with env := PolyTy.mkTrivial paramTy :: ctx.env } := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hKe v (.inl hv)))
        · exact hbelow M hM
      refine TypeOfElabHM.lambda
        (Subst.onTy_lc (Infer.lc hbody hbodyWF).2 hcl)
        (fun T hT => by simp only [Option.map_some, Option.some.injEq] at hT; exact hT) rfl
        (Infer.sound hbody hbodyWF hbodyBelow K (fun k hk => by have := hKΦ k hk; omega)
          (fun y hy => hKe y (.inr hy)) hSK)
  | app hf harg huni =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at hKe
    obtain ⟨hf_lc, hf_s⟩ := Infer.lc hf hctx
    have hctx1 := Subst.onCtx_wf hf_s hctx
    obtain ⟨harg_lc, harg_s⟩ := Infer.lc harg hctx1
    have hf_below := Infer.belowFvars hf hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))
    have hbelow1 := Subst.onCtx_below hf_below.2 (Infer.frontier_le hf) hbelow
    have hs3 := huni.lc (Subst.onTy_lc harg_s hf_lc) (.arrow harg_lc ContainsBvarsUpTo.fvar)
    have helim := Infer.eliminates hf hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))
      (fun p hp hc => hSK p (List.mem_append_left _ (List.mem_append_left _ hp)) (hKe p.1 (.inl hc)))
    have hargfix : argOut.substTyFvars S₁ = argOut := Infer.eOut_substTyFvars_eq harg
      (fun p hp => Infer.dom_below hf hbelow (fun y hy => hKΦ y (hKe y (.inl hy))) p hp)
      (fun p hp M hM => by
        obtain ⟨M₀, _, rfl⟩ := List.mem_map.mp hM
        exact helim.1 p hp M₀.body)
      (fun p hp hc => hSK p (List.mem_append_left _ (List.mem_append_left _ hp)) (hKe p.1 (.inr hc)))
    have hf_sound := Infer.sound hf hctx hbelow K hKΦ (fun y hy => hKe y (.inl hy))
      (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ hp)))
    have harg_sound := Infer.sound harg hctx1 hbelow1 K
      (fun k hk => lt_of_lt_of_le (hKΦ k hk) (Infer.frontier_le hf))
      (fun y hy => hKe y (.inr hy))
      (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_right _ hp)))
    have hueq := huni.unifies
    simp only [Unifies, Subst.onTy_arrow] at hueq
    have f2 := TypeOfElabHM.onSubst S₃ hs3 (TypeOfElabHM.onSubst S₂ harg_s hf_sound)
    rw [← Expr.substTyFvars_append, ← Expr.substTyFvars_append, ← List.append_assoc, hueq] at f2
    have a1 := TypeOfElabHM.onSubst S₃ hs3 harg_sound
    have hargterm : argOut.substTyFvars (S₁ ++ S₂ ++ S₃) = (argOut.substTyFvars S₂).substTyFvars S₃ := by
      rw [Expr.substTyFvars_append, Expr.substTyFvars_append, hargfix]
    rw [Expr.substTyFvars_app, Subst.onCtx_append, Subst.onCtx_append, hargterm]
    exact .app f2 a1
  | var hlook =>
    intro hctx _ _ _ _ _
    simp only [Subst.onCtx_nil, Expr.substTyFvars]
    refine TypeOfElabHM.var hlook (fun tyArg ht => ?_) (by simp [freshVars_length])
      (InstantiatesBy.openVars (hctx _ (List.mem_of_getElem? hlook)) (by simp [freshVars_length]))
    obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht
    exact .fvar
  | ctor hlook =>
    intro _ _ _ _ _ _
    simp only [Subst.onCtx_nil, Expr.substTyFvars]
    refine TypeOfElabHM.ctor hlook (fun tyArg ht => ?_)
      (InstantiatesBy.openVars (Ctor.toTy_wf _) (by simp [Ctor.toTy]))
    obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht
    exact .fvar
  | letIn hrhs hbody =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append] at hKe
    obtain ⟨hrhs_lc, hrhs_s⟩ := Infer.lc hrhs hctx
    have hrhs_below := Infer.belowFvars hrhs hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))
    have hbelow1 := Subst.onCtx_below hrhs_below.2 (Infer.frontier_le hrhs) hbelow
    have helimR := Infer.eliminates hrhs hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))
      (fun p hp hc => hSK p (List.mem_append_left _ hp) (hKe p.1 (.inl hc)))
    have hS₁τ₁ : S₁.onTy τ₁ = τ₁ := Ty.substFvars_eq_self_of_no_key (fun p hp => helimR.2 p hp)
    have hbodyWF : CtxWF { (S₁.onCtx ctx) with
        env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env } := by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact genScheme_wf hrhs_lc
      · exact (Subst.onCtx_wf hrhs_s hctx) M hM
    have hbodyBelow : CtxBelow Φ₁ { (S₁.onCtx ctx) with
        env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env } := by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact hrhs_below.1.closeOver
      · exact hbelow1 M hM
    have hbody_s := (Infer.lc hbody hbodyWF).2
    -- genV facts (genV := genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)
    have hgenV_τ₁ : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁, g ∈ τ₁.freeVars :=
      fun g hg => by simp only [genVars, List.mem_filter] at hg; exact hg.1
    have hgenV_env : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁,
        g ∉ (S₁.onCtx ctx).env.freeVars := fun g hg => genVars_not_mem hg
    have hgenV_lt : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁, g < Φ₁ :=
      fun g hg => hrhs_below.1.mem_lt g (hgenV_τ₁ g hg)
    have hgenV_ge : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁, Φ ≤ g := by
      intro g hg
      by_contra hlt
      push_neg at hlt
      have hg_S₁dom : ∀ p ∈ S₁, p.1 ≠ g := fun p hp hpeq => helimR.2 p hp (hpeq ▸ hgenV_τ₁ g hg)
      have hg_ctxenv : ∀ M ∈ ctx.env, g ∉ M.body.freeVars := by
        intro M₀ hM₀ hgM
        exact hgenV_env g hg (Env.mem_freeVars_iff.mpr ⟨S₁.onPolyTy M₀,
          List.mem_map.mpr ⟨M₀, hM₀, rfl⟩, Ty.mem_freeVars_onTy_of_not_dom hgM hg_S₁dom⟩)
      exact (Infer.eOut_avoid hrhs (w := g) hlt hg_ctxenv (genVars_not_mem_rigid hg)).2.1
        (hgenV_τ₁ g hg)
    have hgenV_body : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁, g ∉ body.tyFreeVars :=
      fun g hg hc => by have := hKΦ g (hKe g (.inr hc)); have := hgenV_ge g hg; omega
    have hbodyCtxAvoid : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁,
        ∀ M ∈ (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env),
          g ∉ M.body.freeVars := by
      intro g hg M hM
      rcases List.mem_cons.mp hM with rfl | hM
      · exact Ty.not_mem_closeOver_freeVars hg
      · exact fun hc2 => hgenV_env g hg (Env.mem_freeVars_iff.mpr ⟨M, hM, hc2⟩)
    have hS₂genV : ∀ p ∈ S₂, p.1 ∉ genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ := by
      intro p hp hc
      exact Infer.dom_avoid hbody (hgenV_lt p.1 hc) (hbodyCtxAvoid p.1 hc)
        (fun hc2 => hSK p (List.mem_append_right _ hp) (hKe p.1 (.inr hc2)))
        (List.mem_map.mpr ⟨p, hp, rfl⟩)
    have hS₂genVran : ∀ p ∈ S₂, ∀ u ∈ p.2.freeVars, u ∉ genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ := by
      intro p hp u hu hc
      exact (Infer.eOut_avoid hbody (w := u) (hgenV_lt u hc) (hbodyCtxAvoid u hc)
        (hgenV_body u hc)).1 p hp hu
    have hS₁fixE : ∀ p ∈ S₁, p.1 ∉ (rhsOut.substTyFvars S₁).tyFreeVars :=
      fun p hp => Expr.dom_notMem_substTyFvars (fun x => helimR.1 p hp x)
    have hbodyfix : bodyOut.substTyFvars S₁ = bodyOut :=
      Infer.eOut_substTyFvars_eq hbody
        (fun p hp => Infer.dom_below hrhs hbelow (fun y hy => hKΦ y (hKe y (.inl hy))) p hp)
        (fun p hp M hM => by
          rcases List.mem_cons.mp hM with rfl | hM
          · exact fun hc2 => helimR.2 p hp (Ty.freeVars_closeOver_subset hc2)
          · obtain ⟨M₀, _, rfl⟩ := List.mem_map.mp hM; exact helimR.1 p hp M₀.body)
        (fun p hp hc => hSK p (List.mem_append_left _ hp) (hKe p.1 (.inr hc)))
    have hbound : ((rhsOut.substTyFvars S₁).closeTyVars (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)).substTyFvars (S₁ ++ S₂)
        = (rhsOut.substTyFvars (S₁ ++ S₂)).closeTyVars (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) := by
      rw [Expr.substTyFvars_append,
          Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars
            (fun p hp hc => hS₁fixE p hp (Expr.tyFreeVars_closeTyVars_subset hc)),
          Expr.substTyFvars_closeTyVars hS₂genV hS₂genVran,
          ← Expr.substTyFvars_append]
    have hSτ₁ : (S₁ ++ S₂).onTy τ₁ = S₂.onTy τ₁ := by rw [Subst.onTy_append, hS₁τ₁]
    have hschemebody : S₂.onTy (Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) τ₁)
        = Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) ((S₁ ++ S₂).onTy τ₁) := by
      have h1 : S₂.onTy (Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) τ₁)
          = Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) (S₂.onTy τ₁) :=
        Ty.substFvars_closeOver hS₂genV hS₂genVran
      rw [h1, ← hSτ₁]
    have hann : Ty.substFvars (S₁ ++ S₂) (Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) τ₁)
        = Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) ((S₁ ++ S₂).onTy τ₁) := by
      rw [show Ty.substFvars (S₁ ++ S₂) (Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) τ₁)
            = (S₁ ++ S₂).onTy (Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) τ₁) from rfl,
          Subst.onTy_append,
          show S₁.onTy (Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) τ₁)
            = Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) τ₁ from
            Ty.substFvars_eq_self_of_no_key (fun p hp hc => helimR.2 p hp (Ty.freeVars_closeOver_subset hc)),
          hschemebody]
    have hgenV_env' : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁,
        g ∉ ((S₁ ++ S₂).onCtx ctx).env.freeVars := by
      intro g hg hc
      rw [Subst.onCtx_append, Env.mem_freeVars_iff] at hc
      obtain ⟨M, hM, hgM⟩ := hc
      simp only [Subst.onCtx, Subst.onEnv] at hM
      obtain ⟨M₀, hM₀, rfl⟩ := List.mem_map.mp hM
      exact Subst.notMemOnTy (fun p hp hgp => hS₂genVran p hp g hgp hg)
        (fun hc2 => hgenV_env g hg (Env.mem_freeVars_iff.mpr ⟨M₀, hM₀, hc2⟩)) hgM
    -- typings
    have hrhs_sound := Infer.sound hrhs hctx hbelow K hKΦ (fun y hy => hKe y (.inl hy))
      (fun p hp => hSK p (List.mem_append_left _ hp))
    have hr2 := TypeOfElabHM.onSubst S₂ hbody_s hrhs_sound
    rw [← Expr.substTyFvars_append, ← Subst.onCtx_append, ← hSτ₁] at hr2
    have hbodyterm : bodyOut.substTyFvars (S₁ ++ S₂) = bodyOut.substTyFvars S₂ := by
      rw [Expr.substTyFvars_append, hbodyfix]
    have hschemeeq : Subst.onPolyTy S₂ (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)
        = ⟨(genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁).length,
            Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) ((S₁ ++ S₂).onTy τ₁)⟩ := by
      simp only [Subst.onPolyTy, genScheme]
      rw [hschemebody]
    have heqbodyctx : Subst.onCtx S₂ { (S₁.onCtx ctx) with
          env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env }
        = { (S₁ ++ S₂).onCtx ctx with
          env := Subst.onPolyTy S₂ (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)
            :: ((S₁ ++ S₂).onCtx ctx).env } := by
      rw [Subst.onCtx_append]
      simp only [Subst.onCtx, Subst.onEnv, List.map_cons]
    have hbody_sound := Infer.sound hbody hbodyWF hbodyBelow K
      (fun k hk => lt_of_lt_of_le (hKΦ k hk) (Infer.frontier_le hrhs))
      (fun y hy => hKe y (.inr hy)) (fun p hp => hSK p (List.mem_append_right _ hp))
    rw [heqbodyctx, hschemeeq, ← hbodyterm] at hbody_sound
    -- assemble
    rw [Expr.substTyFvars_letIn]
    simp only [Option.map_some, genScheme]
    rw [hann, hbound]
    exact Infer.sound_letIn hr2 (Subst.onTy_lc (fun p hp => by
        rcases List.mem_append.mp hp with h | h
        · exact hrhs_s p h
        · exact hbody_s p h) hrhs_lc)
      (by
        intro g hg hc
        have hgK : g ∈ K :=
          Infer.recAnnSchemeFreeVars_substTyFvars_in_K hrhs (S := S₁ ++ S₂)
            (fun y hy => hKe y (.inl (Expr.recAnnSchemeFreeVars_subset_tyFreeVars hy)))
            (fun p hp => hSK p (List.mem_append_left _ hp)) hSK hc
        have := hgenV_ge g hg; have := hKΦ g hgK; omega)
      genVars_nodup hgenV_env' hbody_sound
  | letInAnn hσwf hΦN hrhs huni hesc1 hesc2 hbody =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at hKe
    have hrle := Infer.frontier_le hrhs
    have hctx_pc : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hbelow M hM).mono (by omega)
    have hσbody : Ty.BelowFvars Φ σ.body :=
      Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hKe v (.inl (.inl hv))))
    obtain ⟨hrhs_lc, hrhs_s⟩ := Infer.lc hrhs hctx
    have hΦ_rhs : ∀ y ∈ (rhs.openTyVars (freshVars N σ.paramCount)).tyFreeVars, y < N + σ.paramCount := by
      intro y hy
      rcases Expr.tyFreeVars_openTyVars hy with h | h
      · have := hKΦ y (hKe y (.inl (.inr h))); have := hΦN; omega
      · have := freshVars_lt y h; omega
    have hSe_rhs : ∀ p ∈ S₁, p.1 ∉ (rhs.openTyVars (freshVars N σ.paramCount)).tyFreeVars := by
      intro p hp hc
      rcases Expr.tyFreeVars_openTyVars hc with h | h
      · exact hSK p (List.mem_append_left _ (List.mem_append_left _ hp)) (hKe p.1 (.inl (.inr h)))
      · exact hesc1 p.1 h (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩)
    obtain ⟨hr_τ, hr_s⟩ := Infer.belowFvars hrhs hctx_pc hΦ_rhs
    have hσopen : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
      Ty.openVars_belowFvars (hσbody.mono (by omega)) (fun x hx => by have := freshVars_lt x hx; omega)
    have hSchk_below : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hr_τ hσopen
    have hSchk_lc : ∀ p ∈ Schk, p.2.IsLC :=
      UnifyRel.lc huni hrhs_lc (PolyTy.openVars_isLC hσwf (by simp))
    have hbodyWF : CtxWF { (Schk.onCtx (S₁.onCtx ctx)) with
        env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env } := by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact hσwf
      · exact (Subst.onCtx_wf hSchk_lc (Subst.onCtx_wf hrhs_s hctx)) M hM
    have hbodyBelow : CtxBelow Φ₁ { (Schk.onCtx (S₁.onCtx ctx)) with
        env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env } := by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact hσbody.mono (by omega)
      · exact (Subst.onCtx_below hSchk_below (le_refl _) (Subst.onCtx_below hr_s hrle hctx_pc)) M hM
    have hbody_s := (Infer.lc hbody hbodyWF).2
    have hSlc : ∀ p ∈ S₁ ++ Schk ++ S₂, p.2.IsLC := by
      intro p hp
      rcases List.mem_append.mp hp with hp | hp
      · rcases List.mem_append.mp hp with hp | hp
        · exact hrhs_s p hp
        · exact hSchk_lc p hp
      · exact hbody_s p hp
    have helimR := Infer.eliminates hrhs hctx_pc hΦ_rhs hSe_rhs
    have hcross : ∀ p ∈ S₁, ∀ q ∈ Schk, p.1 ∉ q.2.freeVars := by
      intro p hp q hq hwq
      rcases UnifyRel.range_mem huni q hq p.1 hwq with h | h
      · exact helimR.2 p hp h
      · rcases Ty.freeVars_openVars_subset p.1 h with h | h
        · exact hSK p (List.mem_append_left _ (List.mem_append_left _ hp)) (hKe p.1 (.inl (.inl h)))
        · exact hesc1 p.1 h (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩)
    have hcombelim := Subst.eliminates_append helimR.1 (UnifyRel.eliminates huni) hcross
    -- Ys = freshVars N σ.paramCount facts
    have hYs_lt : ∀ y ∈ freshVars N σ.paramCount, y < Φ₁ :=
      fun y hy => by have := freshVars_lt y hy; have := hrle; omega
    have hYs_bodyCtx : ∀ y ∈ freshVars N σ.paramCount,
        ∀ M ∈ (σ :: (Schk.onCtx (S₁.onCtx ctx)).env), y ∉ M.body.freeVars := by
      intro y hy M hM
      rcases List.mem_cons.mp hM with rfl | hM
      · exact fun hc => by have := hKΦ y (hKe y (.inl (.inl hc))); have := freshVars_ge y hy; omega
      · exact fun hc => hesc2 y hy (Env.mem_freeVars_iff.mpr ⟨M, hM, hc⟩)
    have hYs_body : ∀ y ∈ freshVars N σ.paramCount, y ∉ body.tyFreeVars :=
      fun y hy hc => by have := hKΦ y (hKe y (.inr hc)); have := freshVars_ge y hy; omega
    have hS₂Ys : ∀ p ∈ S₂, p.1 ∉ freshVars N σ.paramCount := by
      intro p hp hc
      exact Infer.dom_avoid hbody (hYs_lt p.1 hc) (hYs_bodyCtx p.1 hc) (hYs_body p.1 hc)
        (List.mem_map.mpr ⟨p, hp, rfl⟩)
    have hS₂Ysran : ∀ p ∈ S₂, ∀ u ∈ p.2.freeVars, u ∉ freshVars N σ.paramCount := by
      intro p hp u hu hc
      exact (Infer.eOut_avoid hbody (w := u) (hYs_lt u hc) (hYs_bodyCtx u hc) (hYs_body u hc)).1 p hp hu
    have hSYs : ∀ p ∈ S₁ ++ Schk ++ S₂, p.1 ∉ freshVars N σ.paramCount := by
      intro p hp
      rcases List.mem_append.mp hp with hp | hp
      · exact fun hc => hesc1 p.1 hc (List.mem_map.mpr ⟨p, hp, rfl⟩)
      · exact hS₂Ys p hp
    -- S₁++Schk fixes the rhs output / S fixes σ.body and σ.openVars Ys
    have hRfixE : ∀ p ∈ S₁ ++ Schk, p.1 ∉ (rhsOut.substTyFvars (S₁ ++ Schk)).tyFreeVars :=
      fun p hp => Expr.dom_notMem_substTyFvars (fun x => hcombelim p hp x)
    have hSσbody : (S₁ ++ Schk ++ S₂).onTy σ.body = σ.body :=
      Ty.substFvars_eq_self_of_no_key (fun p hp hc => hSK p hp (hKe p.1 (.inl (.inl hc))))
    have hSfix_σopen : ∀ p ∈ S₁ ++ Schk ++ S₂,
        p.1 ∉ (σ.openVars (freshVars N σ.paramCount)).freeVars := by
      intro p hp hc
      rcases Ty.freeVars_openVars_subset p.1 hc with h | h
      · exact hSK p hp (hKe p.1 (.inl (.inl h)))
      · exact hSYs p hp h
    have hSchk_fix : Schk.onTy (σ.openVars (freshVars N σ.paramCount))
        = σ.openVars (freshVars N σ.paramCount) :=
      Ty.substFvars_eq_self_of_no_key
        (fun p hp => hSfix_σopen p (List.mem_append_left _ (List.mem_append_right _ hp)))
    have hS₂fix : S₂.onTy (σ.openVars (freshVars N σ.paramCount))
        = σ.openVars (freshVars N σ.paramCount) :=
      Ty.substFvars_eq_self_of_no_key (fun p hp => hSfix_σopen p (List.mem_append_right _ hp))
    have hopeneq2 : (Subst.onPolyTy (S₁ ++ Schk ++ S₂) σ).openVars (freshVars N σ.paramCount)
        = σ.openVars (freshVars N σ.paramCount) := by
      unfold PolyTy.openVars
      rw [show (Subst.onPolyTy (S₁ ++ Schk ++ S₂) σ).body = (S₁ ++ Schk ++ S₂).onTy σ.body from rfl,
          hSσbody]
    -- the bound expression equality (re-applying S₁++Schk is a no-op; Step A with S₂)
    have hbound : ((rhsOut.substTyFvars (S₁ ++ Schk)).closeTyVars (freshVars N σ.paramCount)).substTyFvars (S₁ ++ Schk ++ S₂)
        = (rhsOut.substTyFvars (S₁ ++ Schk ++ S₂)).closeTyVars (freshVars N σ.paramCount) := by
      rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Expr.substTyFvars_append,
          Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars
            (fun p hp hc => hRfixE p hp (Expr.tyFreeVars_closeTyVars_subset hc)),
          Expr.substTyFvars_closeTyVars hS₂Ys hS₂Ysran,
          ← Expr.substTyFvars_append]
    -- the rhs typing at the opened scheme
    have hrhs_sound := Infer.sound hrhs hctx hctx_pc (K ++ freshVars N σ.paramCount)
      (fun k hk => by rcases List.mem_append.mp hk with h | h
                      · have := hKΦ k h; have := hΦN; omega
                      · have := freshVars_lt k h; omega)
      (fun y hy => by rcases Expr.tyFreeVars_openTyVars hy with h | h
                      · exact List.mem_append_left _ (hKe y (.inl (.inr h)))
                      · exact List.mem_append_right _ h)
      (fun p hp hc => by rcases List.mem_append.mp hc with h | h
                         · exact hSK p (List.mem_append_left _ (List.mem_append_left _ hp)) h
                         · exact hesc1 p.1 h (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩))
    have hr1 := TypeOfElabHM.onSubst Schk hSchk_lc hrhs_sound
    rw [← Expr.substTyFvars_append, ← Subst.onCtx_append] at hr1
    have hr2 := TypeOfElabHM.onSubst S₂ hbody_s hr1
    rw [← Expr.substTyFvars_append, ← Subst.onCtx_append] at hr2
    have hu := huni.unifies
    simp only [Unifies] at hu
    rw [hu, hSchk_fix, hS₂fix, ← hopeneq2] at hr2
    -- the body typing under the substituted scheme
    have hbodyfix : bodyOut.substTyFvars (S₁ ++ Schk) = bodyOut :=
      Infer.eOut_substTyFvars_eq hbody
        (fun p hp => by
          rcases List.mem_append.mp hp with hp | hp
          · exact Infer.dom_below hrhs hctx_pc hΦ_rhs p hp
          · rcases UnifyRel.dom_mem huni p hp with h | h
            · exact hr_τ.mem_lt p.1 h
            · exact hσopen.mem_lt p.1 h)
        (fun p hp M hM => by
          rcases List.mem_cons.mp hM with rfl | hM
          · exact fun hc => hSK p (List.mem_append_left _ hp) (hKe p.1 (.inl (.inl hc)))
          · rw [show (Schk.onCtx (S₁.onCtx ctx)) = (S₁ ++ Schk).onCtx ctx
                  from (Subst.onCtx_append S₁ Schk ctx).symm] at hM
            exact Subst.eliminates_onCtx (hcombelim p hp) M hM)
        (fun p hp hc => hSK p (List.mem_append_left _ hp) (hKe p.1 (.inr hc)))
    have hbodyterm : bodyOut.substTyFvars (S₁ ++ Schk ++ S₂) = bodyOut.substTyFvars S₂ := by
      rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Expr.substTyFvars_append, hbodyfix]
    have hS₁Schk_σbody : (S₁ ++ Schk).onTy σ.body = σ.body :=
      Ty.substFvars_eq_self_of_no_key (fun p hp hc => hSK p (List.mem_append_left _ hp)
        (hKe p.1 (.inl (.inl hc))))
    have hSσ : Subst.onPolyTy (S₁ ++ Schk) σ = σ := by
      simp only [Subst.onPolyTy, hS₁Schk_σbody]
    have hschemeeq : Subst.onPolyTy S₂ σ = Subst.onPolyTy (S₁ ++ Schk ++ S₂) σ := by
      rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Subst.onPolyTy_append, hSσ]
    have heqbodyctx : Subst.onCtx S₂ { (Schk.onCtx (S₁.onCtx ctx)) with
          env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env }
        = { (S₁ ++ Schk ++ S₂).onCtx ctx with
          env := Subst.onPolyTy S₂ σ :: ((S₁ ++ Schk ++ S₂).onCtx ctx).env } := by
      rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Subst.onCtx_append, Subst.onCtx_append]
      simp only [Subst.onCtx, Subst.onEnv, List.map_cons]
    have hbody_sound := Infer.sound hbody hbodyWF hbodyBelow K
      (fun k hk => by have := hKΦ k hk; have := hΦN; have := hrle; omega)
      (fun y hy => hKe y (.inr hy)) (fun p hp => hSK p (List.mem_append_right _ hp))
    rw [heqbodyctx, hschemeeq, ← hbodyterm] at hbody_sound
    -- Ys avoid the substituted context env / scheme body
    have hYs_σ : ∀ y ∈ freshVars N σ.paramCount,
        y ∉ (Subst.onPolyTy (S₁ ++ Schk ++ S₂) σ).body.freeVars := by
      intro y hy hc
      rw [show (Subst.onPolyTy (S₁ ++ Schk ++ S₂) σ).body = (S₁ ++ Schk ++ S₂).onTy σ.body from rfl,
          hSσbody] at hc
      have := hKΦ y (hKe y (.inl (.inl hc))); have := freshVars_ge y hy; omega
    have hYs_env : ∀ y ∈ freshVars N σ.paramCount,
        y ∉ ((S₁ ++ Schk ++ S₂).onCtx ctx).env.freeVars := by
      intro y hy hc
      rw [show (S₁ ++ Schk ++ S₂).onCtx ctx = S₂.onCtx ((S₁ ++ Schk).onCtx ctx) from by
            rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Subst.onCtx_append],
          Env.mem_freeVars_iff] at hc
      obtain ⟨M, hM, hyM⟩ := hc
      rw [show (S₂.onCtx ((S₁ ++ Schk).onCtx ctx)).env
            = ((S₁ ++ Schk).onCtx ctx).env.map (Subst.onPolyTy S₂) from rfl, List.mem_map] at hM
      obtain ⟨M₀, hM₀, rfl⟩ := hM
      refine Subst.notMemOnTy (fun p hp hyp => hS₂Ysran p hp y hyp hy) (fun hc2 => ?_) hyM
      rw [Subst.onCtx_append] at hM₀
      exact hesc2 y hy (Env.mem_freeVars_iff.mpr ⟨M₀, hM₀, hc2⟩)
    -- assemble
    rw [Expr.substTyFvars_letIn]
    simp only [Option.map_some]
    rw [hbound]
    exact Infer.sound_letInAnn hr2 (Subst.onPolyTy_wf hSlc hσwf) freshVars_nodup
      (by rw [freshVars_length])
      (by
        intro y hy hc
        have hyK : y ∈ K :=
          Infer.recAnnSchemeFreeVars_substTyFvars_in_K hrhs (S := S₁ ++ Schk ++ S₂)
            (fun g hg => hKe g (.inl (.inr (Expr.recAnnSchemeFreeVars_subset_tyFreeVars
              (Expr.recAnnSchemeFreeVars_openTyVars ▸ hg)))))
            (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ hp))) hSK hc
        have := freshVars_ge y hy; have := hKΦ y hyK; omega)
      hYs_env hYs_σ hbody_sound
  | match_ hscrut hne hbr =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at hKe
    obtain ⟨hτs_lc, hS₁⟩ := Infer.lc hscrut hctx
    have hle1 := Infer.frontier_le hscrut
    have hscrut_below := Infer.belowFvars hscrut hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))
    have hctx1 := Subst.onCtx_wf hS₁ hctx
    have hbelow1 := Subst.onCtx_below hscrut_below.2 hle1 hbelow
    have helimS := Infer.eliminates hscrut hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))
      (fun p hp hc => hSK p (List.mem_append_left _ hp) (hKe p.1 (.inl hc)))
    have hS₂lc := (InferBranches.lc hbr hctx1 hτs_lc ContainsBvarsUpTo.fvar).2
    -- S₁ prefix-fixes the branch outputs
    have hbrfix : ∀ b ∈ branchesOut, b.2.substTyFvars S₁ = b.2 := by
      intro b hb
      refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => ?_)
      refine (InferBranches.eOut_avoid hbr (w := p.1)
        (by have := Infer.dom_below hscrut hbelow (fun y hy => hKΦ y (hKe y (.inl hy))) p hp; omega)
        (Subst.eliminates_onCtx (helimS.1 p hp)) (helimS.2 p hp)
        (by have := Infer.dom_below hscrut hbelow (fun y hy => hKΦ y (hKe y (.inl hy))) p hp
            simp only [Ty.freeVars, List.mem_singleton]; omega)
        (fun hcc => hSK p (List.mem_append_left _ hp) (hKe p.1 (.inr hcc)))).2.2
        (Expr.mem_branchListTyFreeVars_of hb hc)
    -- scrutinee typing
    have hscrut_sound := Infer.sound hscrut hctx hbelow K hKΦ (fun y hy => hKe y (.inl hy))
      (fun p hp => hSK p (List.mem_append_left _ hp))
    have hscrut_decl := TypeOfElabHM.onSubst S₂ hS₂lc hscrut_sound
    rw [← Expr.substTyFvars_append, ← Subst.onCtx_append] at hscrut_decl
    -- branch typings
    have hbr_sound := InferBranches.sound hbr hctx1 (fun M hM => (hbelow1 M hM).mono (by omega))
      hτs_lc ContainsBvarsUpTo.fvar
      (hscrut_below.1.mono (by omega)) (.fvar (by omega)) K
      (fun k hk => by have := hKΦ k hk; omega)
      (fun y hy => hKe y (.inr hy)) (fun p hp => hSK p (List.mem_append_right _ hp))
    have hbrne : branchesOut ≠ [] := by
      cases hbr with
      | nil => exact absurd rfl hne
      | cons => simp
      | consWild => simp
    rw [Expr.substTyFvars_match]
    refine TypeOfElabHM.match_ hscrut_decl
      (by simp only [ne_eq, List.map_eq_nil_iff]; exact hbrne) ?_
    intro br hbr_mem
    obtain ⟨pb, hpb, rfl⟩ := List.mem_map.mp hbr_mem
    rw [Subst.onCtx_append,
        show pb.2.substTyFvars (S₁ ++ S₂) = pb.2.substTyFvars S₂ from by
          rw [Expr.substTyFvars_append, hbrfix pb hpb]]
    exact hbr_sound pb hpb
  | letRec hgroup hbody =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at hKe
    obtain ⟨τs, hτs⟩ :
        ∃ t, t = (freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)) := ⟨_, rfl⟩
    obtain ⟨G, hG⟩ :
        ∃ g, g = genGroupVars (bindings.flatMap Expr.tyFreeVars) (Subst.onCtx S₁ ctx).env τs := ⟨_, rfl⟩
    rw [← hτs, ← hG]
    rw [← hτs] at hbody
    have hctxβ : CtxWF { ctx with env :=
        (freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i)) ++ ctx.env } := by
      intro M hM
      rcases List.mem_append.mp hM with hM | hM
      · obtain ⟨x, _, rfl⟩ := List.mem_map.mp hM; exact ContainsBvarsUpTo.fvar
      · exact hctx M hM
    have hctxβbelow : CtxBelow (Φ + bindings.length)
        { ctx with env := (freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i)) ++ ctx.env } := by
      intro M hM
      rcases List.mem_append.mp hM with hM | hM
      · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
        exact Ty.BelowFvars.of_freeVars_lt (fun v hv => by
          simp only [PolyTy.mkTrivial, Ty.freeVars, List.mem_singleton] at hv
          have := freshVars_lt x hx; omega)
      · exact (hbelow M hM).mono (by omega)
    have hS₁lc : ∀ p ∈ S₁, p.2.IsLC :=
      InferRecGroup.lc hgroup hctxβ
        (fun τ hτ => by obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ; exact ContainsBvarsUpTo.fvar)
    have hτs_lc : ∀ τ ∈ τs, τ.IsLC := by
      intro τ hτ; rw [hτs] at hτ
      obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ
      exact Subst.onTy_lc hS₁lc ContainsBvarsUpTo.fvar
    have hgrle := InferRecGroup.frontier_le hgroup
    have hgroupSound := InferRecGroup.sound hgroup hctxβ hctxβbelow
      (fun τ hτ => by obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ; exact ContainsBvarsUpTo.fvar)
      (fun τ hτ => by obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
                      exact Ty.BelowFvars.of_freeVars_lt (fun v hv => by
                        simp only [Ty.freeVars, List.mem_singleton] at hv
                        have := freshVars_lt x hx; omega))
      K (fun k hk => by have := hKΦ k hk; omega)
      (fun y hy => hKe y (.inl hy)) (fun p hp => hSK p (List.mem_append_left _ hp))
    have ha : ((freshVars Φ bindings.length).map Ty.fvar).map (Subst.onTy S₁) = τs := by
      rw [hτs, List.map_map]; rfl
    have hb : Subst.onCtx S₁ { ctx with env :=
          (freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i)) ++ ctx.env }
        = ⟨τs.map PolyTy.mkTrivial ++ (Subst.onCtx S₁ ctx).env, ctx.ctors⟩ := by
      rw [hτs]
      simp only [Subst.onCtx, Subst.onEnv, List.map_append, List.map_map, Function.comp_def,
        Subst.onPolyTy, PolyTy.mkTrivial]
    rw [ha, hb] at hgroupSound
    have hbodyWF : CtxWF { (Subst.onCtx S₁ ctx) with
        env := genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (Subst.onCtx S₁ ctx).env τs
                 ++ (Subst.onCtx S₁ ctx).env } := by
      intro M hM
      rcases List.mem_append.mp hM with hM | hM
      · simp only [genGroupSchemes] at hM
        obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
        exact PolyTy.genGroup_wf (hτs_lc τ hτ)
      · exact (Subst.onCtx_wf hS₁lc hctx) M hM
    have hS₂lc : ∀ p ∈ S₂, p.2.IsLC := (Infer.lc hbody hbodyWF).2
    have hG_nodup : G.Nodup := by rw [hG]; exact genGroupVars_nodup
    -- gen-pool facts (survival `G ≥ Φ` ⇒ `S₂` and the body avoid `G`)
    have hG_τs : ∀ g ∈ G, g ∈ Ty.freeVarsList τs := fun g hg => (genGroupVars_spec (hG ▸ hg)).1
    have hG_envS₁ : ∀ g ∈ G, g ∉ (Subst.onCtx S₁ ctx).env.freeVars :=
      fun g hg => (genGroupVars_spec (hG ▸ hg)).2.1
    have hG_rigid : ∀ g ∈ G, g ∉ bindings.flatMap Expr.tyFreeVars :=
      fun g hg => (genGroupVars_spec (hG ▸ hg)).2.2
    have htg_below : ∀ β ∈ (freshVars Φ bindings.length).map Ty.fvar,
        Ty.BelowFvars (Φ + bindings.length) β :=
      fun β hβ => by
        obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hβ
        exact Ty.BelowFvars.of_freeVars_lt (fun v hv => by
          simp only [Ty.freeVars, List.mem_singleton] at hv; have := freshVars_lt x hx; omega)
    have htfv_below : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ + bindings.length :=
      fun y hy => by have := hKΦ y (hKe y (.inl hy)); omega
    have hS₁below : ∀ p ∈ S₁, Ty.BelowFvars Φ₁ p.2 :=
      InferRecGroup.belowFvars hgroup hctxβbelow htg_below htfv_below
    have helimG : ∀ p ∈ S₁, ∀ x : Ty, p.1 ∉ (S₁.onTy x).freeVars :=
      InferRecGroup.eliminates hgroup hctxβbelow htg_below htfv_below
        (fun p hp hc => hSK p (List.mem_append_left _ hp) (hKe p.1 (.inl hc)))
    have hτs_below : ∀ τ ∈ τs, Ty.BelowFvars Φ₁ τ := by
      intro τ hτ; rw [hτs] at hτ
      obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
      exact Ty.BelowFvars.of_freeVars_lt (fun v hv => by
        rcases Subst.mem_freeVars_onTy hv with h | ⟨p, hp, hvp⟩
        · simp only [Ty.freeVars, List.mem_singleton] at h
          have := freshVars_lt x hx; omega
        · exact (hS₁below p hp).mem_lt v hvp)
    have hG_lt : ∀ g ∈ G, g < Φ₁ := fun g hg => by
      obtain ⟨τ, hτ, hgτ⟩ := Ty.mem_freeVarsList_exists (hG_τs g hg)
      exact (hτs_below τ hτ).mem_lt g hgτ
    have hG_S₁dom : ∀ g ∈ G, ∀ p ∈ S₁, p.1 ≠ g := by
      intro g hg p hp hpeq
      obtain ⟨τ, hτ, hgτ⟩ := Ty.mem_freeVarsList_exists (hG_τs g hg)
      rw [hτs] at hτ; obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ
      exact helimG p hp (Ty.fvar x) (hpeq ▸ hgτ)
    have hG_ctxenv : ∀ g ∈ G, ∀ M ∈ ctx.env, g ∉ M.body.freeVars := by
      intro g hg M hM hc
      exact hG_envS₁ g hg (by
        rw [Env.mem_freeVars_iff]
        exact ⟨S₁.onPolyTy M, List.mem_map.mpr ⟨M, hM, rfl⟩,
          Ty.mem_freeVars_onTy_of_not_dom hc (fun p hp hpeq => hG_S₁dom g hg p hp hpeq)⟩)
    have hG_ge : ∀ g ∈ G, Φ ≤ g := by
      intro g hg
      by_contra hlt; push_neg at hlt
      obtain ⟨τ, hτ, hgτ⟩ := Ty.mem_freeVarsList_exists (hG_τs g hg)
      rw [hτs] at hτ; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
      have hgrange := (InferRecGroup.eOut_avoid hgroup (w := g) (by omega)
        (by intro M hM
            rcases List.mem_append.mp hM with hM | hM
            · obtain ⟨y, hy, rfl⟩ := List.mem_map.mp hM
              simp only [PolyTy.mkTrivial, Ty.freeVars, List.mem_singleton]
              intro h; have := freshVars_ge y hy; omega
            · exact hG_ctxenv g hg M hM)
        (by intro β hβ; obtain ⟨y, hy, rfl⟩ := List.mem_map.mp hβ
            simp only [Ty.freeVars, List.mem_singleton]
            intro h; have := freshVars_ge y hy; omega)
        (fun hc => hG_rigid g hg (Expr.mem_recGroup_tyFreeVars.mp hc))).1
      rcases Subst.mem_freeVars_onTy hgτ with h | ⟨p, hp, hgp⟩
      · simp only [Ty.freeVars, List.mem_singleton] at h
        have := freshVars_ge x hx; omega
      · exact hgrange p hp hgp
    have hG_body : ∀ g ∈ G, g ∉ body.tyFreeVars :=
      fun g hg hc => by have := hKΦ g (hKe g (.inr hc)); have := hG_ge g hg; omega
    have hG_bodyCtx : ∀ g ∈ G, ∀ M ∈ (genGroupSchemes (bindings.flatMap Expr.tyFreeVars)
        (Subst.onCtx S₁ ctx).env τs ++ (Subst.onCtx S₁ ctx).env), g ∉ M.body.freeVars := by
      intro g hg M hM hc
      rcases List.mem_append.mp hM with hM | hM
      · simp only [genGroupSchemes] at hM
        obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
        simp only [PolyTy.genGroup] at hc
        rw [← hG] at hc
        by_cases hgτ : g ∈ τ.freeVars
        · exact Ty.not_mem_closeOver_freeVars
            (by simp only [Ty.genFilter, List.mem_filter, decide_eq_true_eq]; exact ⟨hg, hgτ⟩) hc
        · exact hgτ (Ty.freeVars_closeOver_subset hc)
      · exact hG_envS₁ g hg (Env.mem_freeVars_iff.mpr ⟨M, hM, hc⟩)
    have hS₂G : ∀ p ∈ S₂, p.1 ∉ G := fun p hp hc =>
      Infer.dom_avoid hbody (hG_lt p.1 hc) (hG_bodyCtx p.1 hc) (hG_body p.1 hc)
        (List.mem_map.mpr ⟨p, hp, rfl⟩)
    have hS₂Gran : ∀ p ∈ S₂, ∀ u ∈ p.2.freeVars, u ∉ G := fun p hp u hu hc =>
      (Infer.eOut_avoid hbody (w := u) (hG_lt u hc) (hG_bodyCtx u hc) (hG_body u hc)).1 p hp hu
    have hS₁dom_below : ∀ p ∈ S₁, p.1 < Φ₁ :=
      InferRecGroup.dom_below hgroup hctxβbelow htg_below htfv_below
    have hS₁bc : ∀ p ∈ S₁, ∀ M ∈ (genGroupSchemes (bindings.flatMap Expr.tyFreeVars)
        (Subst.onCtx S₁ ctx).env τs ++ (Subst.onCtx S₁ ctx).env), p.1 ∉ M.body.freeVars := by
      intro p hp M hM hc
      rcases List.mem_append.mp hM with hM | hM
      · simp only [genGroupSchemes] at hM
        obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
        rw [hτs] at hτ; obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ
        simp only [PolyTy.genGroup] at hc
        exact helimG p hp (Ty.fvar x) (Ty.freeVars_closeOver_subset hc)
      · simp only [Subst.onCtx, Subst.onEnv] at hM
        obtain ⟨M₀, hM₀, rfl⟩ := List.mem_map.mp hM
        exact helimG p hp M₀.body hc
    -- `S₁` fixes the elaborated output (its free type vars are `S₁`-reduced)
    have hEoutS₁ : (Expr.letRecElab G (bindingsOut.map (·.substTyFvars S₁)) τs bodyOut).substTyFvars S₁
        = Expr.letRecElab G (bindingsOut.map (·.substTyFvars S₁)) τs bodyOut := by
      apply Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars
      intro p hp hc
      rcases Expr.mem_tyFreeVars_letRecElab hc with ⟨τ, hτ, hpτ⟩ | ⟨e, he, hpe⟩ | hpbody
      · rw [hτs] at hτ; obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ
        exact helimG p hp (Ty.fvar x) hpτ
      · obtain ⟨e0, he0, rfl⟩ := List.mem_map.mp he
        exact Expr.dom_notMem_substTyFvars (fun u => helimG p hp u) hpe
      · exact (Infer.eOut_avoid hbody (w := p.1) (hS₁dom_below p hp) (hS₁bc p hp)
          (fun hcb => hSK p (List.mem_append_left _ hp) (hKe p.1 (.inr hcb)))).2.2 hpbody
    have hEoutS : (Expr.letRecElab G (bindingsOut.map (·.substTyFvars S₁)) τs bodyOut).substTyFvars (S₁ ++ S₂)
        = Expr.letRecElab G (bindingsOut.map (·.substTyFvars (S₁ ++ S₂))) (τs.map S₂.onTy)
            (bodyOut.substTyFvars S₂) := by
      rw [Expr.substTyFvars_append, hEoutS₁,
          Expr.substTyFvars_letRecElab hS₂G hS₂Gran]
      congr 1
      rw [List.map_map]
      exact List.map_congr_left (fun e _ => (Expr.substTyFvars_append S₁ S₂ e).symm)
    rw [hEoutS]
    refine Expr.letRecElab_sound ?_ ?_ hG_nodup ?_ ?_ ?_ ?_
    · rw [List.length_map, List.length_map, hτs, List.length_map, freshVars_length]
      exact InferRecGroup.bindingsOut_length hgroup
    · intro τ hτ; obtain ⟨τ0, hτ0, rfl⟩ := List.mem_map.mp hτ
      exact Subst.onTy_lc hS₂lc (hτs_lc τ0 hτ0)
    · intro g hg hc
      rw [Subst.onCtx_append, Env.mem_freeVars_iff] at hc
      obtain ⟨M, hM, hgM⟩ := hc
      rw [show (S₂.onCtx (S₁.onCtx ctx)).env = (S₁.onCtx ctx).env.map (Subst.onPolyTy S₂) from rfl,
          List.mem_map] at hM
      obtain ⟨M₀, hM₀, rfl⟩ := hM
      exact Subst.notMemOnTy (fun p hp hgp => hS₂Gran p hp g hgp hg)
        (fun hc2 => hG_envS₁ g hg (Env.mem_freeVars_iff.mpr ⟨M₀, hM₀, hc2⟩)) hgM
    · intro g hg e he hc
      have hgK : g ∈ K := InferRecGroup.recAnnSchemeFreeVars_substTyFvars_in_K hgroup
        (fun y hy => by
          obtain ⟨e₁, he₁, hye₁⟩ := Expr.mem_recAnnSchemeFreeVars_RecGroup.mp hy
          exact hKe y (Or.inl (Expr.mem_recGroupTyFreeVars_of he₁
            (Expr.recAnnSchemeFreeVars_subset_tyFreeVars hye₁))))
        (fun p hp => hSK p (List.mem_append_left _ hp)) hSK he hc
      have h1 := hG_ge g hg
      have h2 := hKΦ g hgK
      omega
    · intro p hp
      rw [List.zip_map] at hp
      obtain ⟨q, hq, rfl⟩ := List.mem_map.mp hp
      have honsubst := TypeOfElabHM.onSubst S₂ hS₂lc (hgroupSound q hq)
      rw [← Expr.substTyFvars_append] at honsubst
      have hctxeq : Subst.onCtx S₂ ⟨τs.map PolyTy.mkTrivial ++ (S₁.onCtx ctx).env, ctx.ctors⟩
          = ⟨(τs.map S₂.onTy).map PolyTy.mkTrivial ++ ((S₁ ++ S₂).onCtx ctx).env,
              ((S₁ ++ S₂).onCtx ctx).ctors⟩ := by
        rw [Subst.onCtx_append]
        simp only [Subst.onCtx, Subst.onEnv, List.map_append, List.map_map, Function.comp_def,
          Subst.onPolyTy, PolyTy.mkTrivial]
      rw [hctxeq] at honsubst
      exact honsubst
    · have hbodyBelow : CtxBelow Φ₁ ⟨genGroupSchemes (bindings.flatMap Expr.tyFreeVars)
          (S₁.onCtx ctx).env τs ++ (S₁.onCtx ctx).env, (S₁.onCtx ctx).ctors⟩ := by
        intro M hM
        rcases List.mem_append.mp hM with hM | hM
        · simp only [genGroupSchemes] at hM
          obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
          exact (hτs_below τ hτ).closeOver
        · exact Subst.onCtx_below hS₁below (by omega) hbelow M hM
      have hbodysound := Infer.sound hbody hbodyWF hbodyBelow K
        (fun k hk => by have := hKΦ k hk; have := hgrle; omega)
        (fun y hy => hKe y (.inr hy)) (fun p hp => hSK p (List.mem_append_right _ hp))
      have hbctxeq : Subst.onCtx S₂ ⟨genGroupSchemes (bindings.flatMap Expr.tyFreeVars)
            (S₁.onCtx ctx).env τs ++ (S₁.onCtx ctx).env, (S₁.onCtx ctx).ctors⟩
          = ⟨(τs.map S₂.onTy).map (PolyTy.genGroup G) ++ ((S₁ ++ S₂).onCtx ctx).env,
              ((S₁ ++ S₂).onCtx ctx).ctors⟩ := by
        rw [Subst.onCtx_append]
        show (⟨Subst.onEnv S₂ (genGroupSchemes (bindings.flatMap Expr.tyFreeVars)
              (S₁.onCtx ctx).env τs ++ (S₁.onCtx ctx).env), (S₁.onCtx ctx).ctors⟩ : Ctx) = _
        rw [Subst.onEnv, List.map_append]
        refine congrArg (fun E => (⟨E ++ (S₂.onCtx (S₁.onCtx ctx)).env, _⟩ : Ctx)) ?_
        simp only [genGroupSchemes, List.map_map]
        rw [← hG]
        apply List.map_congr_left
        intro τ hτ
        simp only [Function.comp_apply]
        exact Subst.onPolyTy_genGroup hS₂G hS₂Gran
      rw [hbctxeq] at hbodysound
      exact hbodysound
  | letRecAnn hwf hlen hgroup hbody =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at hKe
    -- derived `K`-bounds for the schemes and the bindings
    have hKsch : ∀ σ ∈ schemes, ∀ y ∈ σ.body.freeVars, y ∈ K := fun σ hσ y hy =>
      hKe y (.inl (.inl (Expr.mem_tyFreeVars_schemeList hσ hy)))
    have hKbr : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K := fun y hy =>
      hKe y (.inl (.inr hy))
    have hKbrΦ : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ := fun y hy => hKΦ y (hKbr y hy)
    have hschB : ∀ σ ∈ schemes, Ty.BelowFvars Φ σ.body := fun σ hσ =>
      Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hKsch σ hσ v hv))
    -- the schemes-extended group context, well-formed and frontier-bounded
    have hgWF : CtxWF { env := schemes ++ ctx.env, ctors := ctx.ctors } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hwf M hM
      · exact hctx M hM
    have hgbelow : CtxBelow Φ { env := schemes ++ ctx.env, ctors := ctx.ctors } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hschB M hM
      · exact hbelow M hM
    have hgrle : Φ ≤ Φ₁ := InferRecGroupAnn.frontier_le hgroup
    have hS₁K : ∀ p ∈ S₁, p.1 ∉ K := fun p hp => hSK p (List.mem_append_left _ hp)
    have hS₂K : ∀ p ∈ S₂, p.1 ∉ K := fun p hp => hSK p (List.mem_append_right _ hp)
    have hS₁lc : ∀ p ∈ S₁, p.2.IsLC := InferRecGroupAnn.lc hgroup hgWF hwf
    have hS₁below : ∀ p ∈ S₁, Ty.BelowFvars Φ₁ p.2 := InferRecGroupAnn.belowFvars hgroup hgbelow hschB hKbrΦ
    have hS₁dom : ∀ p ∈ S₁, p.1 < Φ₁ := InferRecGroupAnn.dom_below hgroup hgbelow hschB hKbrΦ
    have helimS₁ : ∀ p ∈ S₁, ∀ x : Ty, p.1 ∉ (S₁.onTy x).freeVars :=
      InferRecGroupAnn.eliminates hgroup hgbelow hschB hKbrΦ
        (fun p hp hc => hS₁K p hp (hKbr p.1 hc))
        (fun p hp hc => hS₁K p hp (hKe p.1 (.inl (.inl hc))))
    have hgS₁WF : CtxWF (S₁.onCtx { env := schemes ++ ctx.env, ctors := ctx.ctors }) :=
      Subst.onCtx_wf hS₁lc hgWF
    have hgS₁below : CtxBelow Φ₁ (S₁.onCtx { env := schemes ++ ctx.env, ctors := ctx.ctors }) :=
      Subst.onCtx_below hS₁below hgrle hgbelow
    have hS₂lc : ∀ p ∈ S₂, p.2.IsLC := (Infer.lc hbody hgS₁WF).2
    -- the schemes are *rigid* under `S₁ ++ S₂` (their free vars are `⊆ K`, which `S` avoids)
    have hσfix : ∀ σ ∈ schemes, PolyTy.substFvars (S₁ ++ S₂) σ = σ := fun σ hσ => by
      have hb : Ty.substFvars (S₁ ++ S₂) σ.body = σ.body :=
        Ty.substFvars_eq_self_of_no_key (fun p hp hc => hSK p hp (hKsch σ hσ p.1 hc))
      calc PolyTy.substFvars (S₁ ++ S₂) σ
          = ⟨(PolyTy.substFvars (S₁ ++ S₂) σ).paramCount, (PolyTy.substFvars (S₁ ++ S₂) σ).body⟩ := rfl
        _ = σ := by rw [PolyTy.paramCount_substFvars, PolyTy.body_substFvars, hb]
    have hschemesmap : schemes.map (PolyTy.substFvars (S₁ ++ S₂)) = schemes := by
      conv_rhs => rw [← List.map_id schemes]
      exact List.map_congr_left hσfix
    have hschemes_onEnv : ∀ (T : Subst), (∀ p ∈ T, p.1 ∉ K) → Subst.onEnv T schemes = schemes :=
      fun T hT => Subst.onEnv_eq_self_of_fresh (fun p hp hc => by
        obtain ⟨M, hM, hMc⟩ := Env.mem_freeVars_iff.mp hc
        exact hT p hp (hKsch M hM p.1 hMc))
    -- pushing `S₂` past `S₁` over the schemes-extended context (schemes are `S`-rigid)
    have hctxbridge : S₂.onCtx (S₁.onCtx { env := schemes ++ ctx.env, ctors := ctx.ctors })
        = { env := schemes ++ ((S₁ ++ S₂).onCtx ctx).env, ctors := ((S₁ ++ S₂).onCtx ctx).ctors } := by
      rw [← Subst.onCtx_append]
      simp only [Subst.onCtx, Subst.onEnv, List.map_append]
      rw [show List.map (Subst.onPolyTy (S₁ ++ S₂)) schemes = schemes from hschemes_onEnv (S₁ ++ S₂) hSK]
    -- the group's cofinite soundness
    obtain ⟨L, hcof⟩ := InferRecGroupAnn.sound hgroup hgWF hgbelow hwf K hKΦ hKbr hKsch hS₁K
    rw [Expr.substTyFvars_letRecAnn, hschemesmap]
    refine TypeOfElabHM.letRecAnn (L := L ++ S₂.map Prod.fst) ?_ hwf ?_ rfl ?_
    · rw [List.length_map]; exact (InferRecGroupAnn.bindingsOut_length hgroup).trans hlen
    · -- the cofinite premise: bridge `S₁`-typings to `S₁ ++ S₂` by pushing `S₂`
      intro p hp Xs hXfresh
      rw [List.zip_map_left_eq] at hp
      obtain ⟨⟨bo, σ⟩, hbozip, rfl⟩ := List.mem_map.mp hp
      have hXfresh' : FreshNames L σ.paramCount Xs :=
        ⟨hXfresh.length, hXfresh.nodup, fun x hx hc => hXfresh.avoid x hx (List.mem_append_left _ hc)⟩
      have hXs_S₂ : ∀ q ∈ S₂, q.1 ∉ Xs := fun q hq hc =>
        hXfresh.avoid q.1 hc (List.mem_append_right _ (List.mem_map.mpr ⟨q, hq, rfl⟩))
      have hzipS₁ : (bo.substTyFvars S₁, σ) ∈ (bindingsOut.map (·.substTyFvars S₁)).zip schemes := by
        rw [List.zip_map_left_eq]; exact List.mem_map.mpr ⟨(bo, σ), hbozip, rfl⟩
      have hcof_p := hcof (bo.substTyFvars S₁, σ) hzipS₁ Xs hXfresh'
      have hpushed := TypeOfElabHM.onSubst S₂ hS₂lc hcof_p
      rw [hctxbridge] at hpushed
      rw [show ((bo.substTyFvars S₁).openTyVars Xs).substTyFvars S₂
            = (bo.substTyFvars (S₁ ++ S₂)).openTyVars Xs from by
            rw [← Expr.substTyFvars_openTyVars hS₂lc hXs_S₂, ← Expr.substTyFvars_append]] at hpushed
      rw [show S₂.onTy (σ.openVars Xs) = σ.openVars Xs from
            Ty.substFvars_eq_self_of_no_key (fun q hq hc => by
              rcases Ty.freeVars_openVars_subset q.1 hc with h | h
              · exact hS₂K q hq (hKsch σ (List.of_mem_zip hbozip).2 q.1 h)
              · exact hXs_S₂ q hq h)] at hpushed
      exact hpushed
    · -- the body typing, threaded through `S₂`
      have hbodyfix : bodyOut.substTyFvars S₁ = bodyOut :=
        Infer.eOut_substTyFvars_eq hbody hS₁dom
          (fun p hp M hM => by
            obtain ⟨M₀, hM₀, rfl⟩ := List.mem_map.mp hM
            exact fun hc => helimS₁ p hp M₀.body hc)
          (fun p hp hc => hS₁K p hp (hKe p.1 (.inr hc)))
      have hbodyterm : bodyOut.substTyFvars (S₁ ++ S₂) = bodyOut.substTyFvars S₂ := by
        rw [Expr.substTyFvars_append, hbodyfix]
      have hbodysound := Infer.sound hbody hgS₁WF hgS₁below K
        (fun k hk => by have := hKΦ k hk; have := hgrle; omega)
        (fun y hy => hKe y (.inr hy)) hS₂K
      rw [hctxbridge, ← hbodyterm] at hbodysound
      exact hbodysound
termination_by e.size
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.size, Expr.size_openTyVars]; omega)
theorem InferBranches.sound {Φ ctx scrutTy ρ brs Φ' S brsOut}
    (h : InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut)
    (hctx : CtxWF ctx) (hbelow : CtxBelow Φ ctx)
    (hscrutTy : scrutTy.IsLC) (hρ : ρ.IsLC)
    (hscrutB : Ty.BelowFvars Φ scrutTy) (hρB : Ty.BelowFvars Φ ρ) (K : List Nat)
    (hKΦ : ∀ k ∈ K, k < Φ)
    (hKbr : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars brs, y ∈ K)
    (hSK : ∀ p ∈ S, p.1 ∉ K) :
    ∀ p ∈ brsOut, TypeOfElabMatchBranch (S.onCtx ctx) (p.1, p.2.substTyFvars S)
      (S.onTy scrutTy) (S.onTy ρ) := by
  cases h with
  | nil => intro p hp; simp at hp
  | cons hlook hn huni0 hbody huni hrest =>
    expose_names
    have hΦ : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars ((MatchPattern.named c n, body) :: rest), y < Φ :=
      fun y hy => hKΦ y (hKbr y hy)
    have hSe : ∀ p ∈ S₀ ++ S₁ ++ S₂ ++ S₃,
        p.1 ∉ Expr.tyFreeVars.BranchList.tyFreeVars ((MatchPattern.named c n, body) :: rest) :=
      fun p hp hc => hSK p hp (hKbr p.1 hc)
    have hle0 : Φ + ctor.paramCount ≤ Φ₁ := Infer.frontier_le hbody
    have hΦhead : ∀ y ∈ body.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy)
    -- LC facts
    have hS₀lc := huni0.lc hscrutTy
      (.customTy (fun t ht => by obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht; exact ContainsBvarsUpTo.fvar))
    have hbodyWF := branchBindings_wf (ctorr := ctor)
      (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
      (Subst.onCtx_wf hS₀lc hctx)
      (fun t ht => by
        obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
        obtain ⟨x, _, rfl⟩ := List.mem_map.mp hv
        exact Subst.onTy_lc hS₀lc ContainsBvarsUpTo.fvar)
      (by simp)
    obtain ⟨hτb_lc, hS₁lc⟩ := Infer.lc hbody hbodyWF
    have hS₂lc := huni.lc hτb_lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc hρ))
    have hctx1WF := Subst.onCtx_wf hS₂lc (Subst.onCtx_wf hS₁lc (Subst.onCtx_wf hS₀lc hctx))
    have hS₃lc := (InferBranches.lc hrest hctx1WF
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc hscrutTy)))
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc hρ)))).2
    -- belowFvars + eliminates (mirrors InferBranches.eliminates)
    have hS₀bel : ∀ p ∈ S₀, Ty.BelowFvars (Φ + ctor.paramCount) p.2 :=
      UnifyRel.belowFvars huni0 (hscrutB.mono (by omega))
        (.customTy (fun t ht => by obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
                                   exact .fvar (by have := freshVars_lt x hx; omega)))
    have hS₀dom : ∀ p ∈ S₀, p.1 < Φ + ctor.paramCount := by
      intro p hp; rcases UnifyRel.dom_mem huni0 p hp with h | h
      · exact (hscrutB.mono (show Φ ≤ Φ + ctor.paramCount by omega)).mem_lt p.1 h
      · simp only [Ty.freeVars] at h; rw [mem_TyList_freeVars] at h
        obtain ⟨t, ht, hgt⟩ := h; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
        simp only [Ty.freeVars, List.mem_singleton] at hgt; have := freshVars_lt x hx; omega
    have hbodyBelow := branchBindings_below (ctorr := ctor)
      (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
      (Subst.onCtx_below hS₀bel (by omega) hbelow)
      (fun t ht => by obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
                      obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv
                      exact Subst.onTy_belowFvars hS₀bel (.fvar (by have := freshVars_lt x hx; omega)))
    have hSbody : ∀ p ∈ S₁, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_right _ hp)))
        (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc)
    obtain ⟨hbE, hbR⟩ := Infer.eliminates hbody hbodyBelow (fun y hy => by have := hΦhead y hy; omega) hSbody
    obtain ⟨hb_τbel, hb_sbel⟩ := Infer.belowFvars hbody hbodyBelow (fun y hy => by have := hΦhead y hy; omega)
    have hb_dom := Infer.dom_below hbody hbodyBelow (fun y hy => by have := hΦhead y hy; omega)
    have hbranchAvoid : ∀ p ∈ S₀, ∀ M ∈ ({ (S₀.onCtx ctx) with
        env := (ctor.contents.map (Ty.openWith (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
          ++ (S₀.onCtx ctx).env } : Ctx).env, p.1 ∉ M.body.freeVars := by
      intro p hp M hM; rcases List.mem_append.mp hM with hM | hM
      · obtain ⟨t, ht, rfl⟩ := List.mem_map.mp hM
        obtain ⟨c0, hc0, rfl⟩ := List.mem_map.mp ht
        simp only [PolyTy.mkTrivial]
        refine Ty.not_mem_freeVars_openWith ?_ ((ctor.closed c0 hc0).not_mem_freeVars p.1)
        intro v hv
        obtain ⟨v0, hv0, rfl⟩ := List.mem_map.mp hv
        obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv0
        exact UnifyRel.eliminates huni0 p hp (Ty.fvar x)
      · exact Subst.eliminates_onCtx (UnifyRel.eliminates huni0 p hp) M hM
    have heOutBody : ∀ p ∈ S₀, (∀ q ∈ S₁, p.1 ∉ q.2.freeVars) ∧ p.1 ∉ τb.freeVars ∧
        p.1 ∉ bodyOut.tyFreeVars := fun p hp =>
      Infer.eOut_avoid hbody (w := p.1) (hS₀dom p hp) (hbranchAvoid p hp)
        (fun hc => hSe p (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_left _ hp)))
          (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc))
    have cross01 : ∀ p ∈ S₀, ∀ q ∈ S₁, p.1 ∉ q.2.freeVars := fun p hp q hq => (heOutBody p hp).1 q hq
    have hE01 := Subst.eliminates_append (UnifyRel.eliminates huni0) hbE cross01
    have hS₀ρbel : Ty.BelowFvars Φ₁ (S₀.onTy ρ) :=
      (Subst.onTy_belowFvars hS₀bel (hρB.mono (by omega))).mono hle0
    have hS₀scrutbel : Ty.BelowFvars Φ₁ (S₀.onTy scrutTy) :=
      (Subst.onTy_belowFvars hS₀bel (hscrutB.mono (by omega))).mono hle0
    have hS₁S₀ρbel : Ty.BelowFvars Φ₁ (S₁.onTy (S₀.onTy ρ)) := Subst.onTy_belowFvars hb_sbel hS₀ρbel
    have hS₂bel : ∀ p ∈ S₂, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hb_τbel hS₁S₀ρbel
    have hS₂dom : ∀ p ∈ S₂, p.1 < Φ₁ := by
      intro p hp; rcases UnifyRel.dom_mem huni p hp with h | h
      · exact hb_τbel.mem_lt p.1 h
      · exact hS₁S₀ρbel.mem_lt p.1 h
    have cross012 : ∀ p ∈ S₀ ++ S₁, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := by
      intro p hp q hq hwq
      rcases UnifyRel.range_mem huni q hq p.1 hwq with h | h
      · rcases List.mem_append.mp hp with hpS₀ | hpS₁
        · exact (heOutBody p hpS₀).2.1 h
        · exact hbR p hpS₁ h
      · exact (Subst.onTy_append S₀ S₁ ρ ▸ hE01 p hp ρ) h
    have hE012 := Subst.eliminates_append hE01 (UnifyRel.eliminates huni) cross012
    have happ3 : ∀ x : Ty, ((S₀ ++ S₁) ++ S₂).onTy x = S₂.onTy (S₁.onTy (S₀.onTy x)) :=
      fun x => by rw [Subst.onTy_append, Subst.onTy_append]
    have honCtx3 : ((S₀ ++ S₁) ++ S₂).onCtx ctx = S₂.onCtx (S₁.onCtx (S₀.onCtx ctx)) := by
      rw [Subst.onCtx_append, Subst.onCtx_append]
    have hctx1bel : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx))) :=
      Subst.onCtx_below hS₂bel (le_refl _) (Subst.onCtx_below hb_sbel (le_refl _)
        (Subst.onCtx_below (fun p hp => (hS₀bel p hp).mono hle0) (by omega) hbelow))
    have hscrut'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (S₀.onTy scrutTy))) :=
      Subst.onTy_belowFvars hS₂bel (Subst.onTy_belowFvars hb_sbel hS₀scrutbel)
    have hρ'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (S₀.onTy ρ))) :=
      Subst.onTy_belowFvars hS₂bel hS₁S₀ρbel
    -- prefix-fixes
    have hbodyfix : bodyOut.substTyFvars S₀ = bodyOut :=
      Infer.eOut_substTyFvars_eq hbody hS₀dom hbranchAvoid
        (fun p hp hc => hSe p (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_left _ hp)))
          (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc))
    have hrestfix : ∀ q ∈ restOut, q.2.substTyFvars ((S₀ ++ S₁) ++ S₂) = q.2 := by
      intro q hq
      refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => ?_)
      refine (InferBranches.eOut_avoid hrest (w := p.1)
        (by rcases List.mem_append.mp hp with hp01 | hp2
            · rcases List.mem_append.mp hp01 with hp0 | hp1
              · have := hS₀dom p hp0; omega
              · exact hb_dom p hp1
            · exact hS₂dom p hp2)
        (honCtx3 ▸ Subst.eliminates_onCtx (hE012 p hp))
        (happ3 scrutTy ▸ hE012 p hp scrutTy)
        (happ3 ρ ▸ hE012 p hp ρ)
        (fun hcc => hSe p (List.mem_append_left _ hp)
          (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hcc))).2.2
        (Expr.mem_branchListTyFreeVars_of hq hc)
    have hrest_sound := InferBranches.sound hrest hctx1WF hctx1bel
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc hscrutTy)))
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc hρ)))
      hscrut'bel hρ'bel K (fun k hk => by have := hKΦ k hk; omega)
      (fun y hy => hKbr y (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hy))
      (fun p hp => hSK p (List.mem_append_right _ hp))
    intro p hp
    rcases List.mem_cons.mp hp with rfl | hp_rest
    · simp only [Subst.onCtx_append, Subst.onTy_append]
      rw [show bodyOut.substTyFvars (S₀ ++ S₁ ++ S₂ ++ S₃)
            = ((bodyOut.substTyFvars S₁).substTyFvars S₂).substTyFvars S₃ from by
            rw [Expr.substTyFvars_append, Expr.substTyFvars_append, Expr.substTyFvars_append, hbodyfix]]
      have h0 := Infer.sound hbody hbodyWF hbodyBelow K (fun k hk => by have := hKΦ k hk; have := hle0; omega)
        (fun y hy => hKbr y (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy))
        (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_right _ hp))))
      have h1 := TypeOfElabHM.onSubst S₃ hS₃lc (TypeOfElabHM.onSubst S₂ hS₂lc h0)
      rw [Subst.onCtx_branchBindings hS₁lc, Subst.onCtx_branchBindings hS₂lc,
          Subst.onCtx_branchBindings hS₃lc] at h1
      have huni_eq := huni.unifies
      simp only [Unifies] at huni_eq
      rw [huni_eq] at h1
      have hscrutEq : S₃.onTy (S₂.onTy (S₁.onTy (S₀.onTy scrutTy)))
          = .customTy ctor.tyName
              ((((((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy).map S₁.onTy).map S₂.onTy).map S₃.onTy) := by
        have hu := huni0.unifies
        simp only [Unifies, Subst.onTy_customTy] at hu
        rw [hu]
        simp only [Subst.onTy_customTy]
      refine TypeOfElabMatchBranch.mk hlook hscrutEq (by simp) hn
        (List.forall₂_self_map (fun c0 hc0 => InstantiatesBy.openWith (ctor.bound c0 hc0) (by simp)))
        rfl rfl ?_
      exact h1
    · simp only [Subst.onCtx_append, Subst.onTy_append]
      rw [show p.2.substTyFvars (S₀ ++ S₁ ++ S₂ ++ S₃) = p.2.substTyFvars S₃ from by
            rw [Expr.substTyFvars_append, hrestfix p hp_rest]]
      exact hrest_sound p hp_rest
  | consWild hbody huni hrest =>
    expose_names
    have hΦ : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars ((MatchPattern.wildcard, body) :: rest), y < Φ :=
      fun y hy => hKΦ y (hKbr y hy)
    have hSe : ∀ p ∈ S₁ ++ S₂ ++ S₃,
        p.1 ∉ Expr.tyFreeVars.BranchList.tyFreeVars ((MatchPattern.wildcard, body) :: rest) :=
      fun p hp hc => hSK p hp (hKbr p.1 hc)
    have hle1 := Infer.frontier_le hbody
    have hΦhead : ∀ y ∈ body.tyFreeVars, y < Φ := fun y hy => hΦ y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy)
    obtain ⟨hτb_lc, hS₁lc⟩ := Infer.lc hbody hctx
    have hS₂lc := huni.lc hτb_lc (Subst.onTy_lc hS₁lc hρ)
    have hctx1WF := Subst.onCtx_wf hS₂lc (Subst.onCtx_wf hS₁lc hctx)
    have hS₃lc := (InferBranches.lc hrest hctx1WF
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc hscrutTy))
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc hρ))).2
    have hSbody : ∀ p ∈ S₁, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSe p (List.mem_append_left _ (List.mem_append_left _ hp))
        (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc)
    obtain ⟨hbE, hbR⟩ := Infer.eliminates hbody hbelow hΦhead hSbody
    obtain ⟨hb_τbel, hb_sbel⟩ := Infer.belowFvars hbody hbelow hΦhead
    have hb_dom := Infer.dom_below hbody hbelow hΦhead
    have hS₁ρbel : Ty.BelowFvars Φ₁ (S₁.onTy ρ) := Subst.onTy_belowFvars hb_sbel (hρB.mono hle1)
    have hS₂bel : ∀ p ∈ S₂, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hb_τbel hS₁ρbel
    have hS₂dom : ∀ p ∈ S₂, p.1 < Φ₁ := by
      intro p hp; rcases UnifyRel.dom_mem huni p hp with h | h
      · exact hb_τbel.mem_lt p.1 h
      · exact hS₁ρbel.mem_lt p.1 h
    have cross1 : ∀ p ∈ S₁, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := by
      intro p hp q hq hwq
      rcases UnifyRel.range_mem huni q hq p.1 hwq with h | h
      · exact hbR p hp h
      · exact hbE p hp ρ h
    have hE12 := Subst.eliminates_append hbE (UnifyRel.eliminates huni) cross1
    have happ2 : ∀ x : Ty, (S₁ ++ S₂).onTy x = S₂.onTy (S₁.onTy x) :=
      fun x => by rw [Subst.onTy_append]
    have honCtx2 : (S₁ ++ S₂).onCtx ctx = S₂.onCtx (S₁.onCtx ctx) := by rw [Subst.onCtx_append]
    have hctx1bel : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx ctx)) :=
      Subst.onCtx_below hS₂bel (le_refl _) (Subst.onCtx_below hb_sbel hle1 hbelow)
    have hscrut'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy scrutTy)) :=
      Subst.onTy_belowFvars hS₂bel (Subst.onTy_belowFvars hb_sbel (hscrutB.mono hle1))
    have hρ'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy ρ)) :=
      Subst.onTy_belowFvars hS₂bel hS₁ρbel
    have hrestfix : ∀ q ∈ restOut, q.2.substTyFvars (S₁ ++ S₂) = q.2 := by
      intro q hq
      refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => ?_)
      refine (InferBranches.eOut_avoid hrest (w := p.1)
        (by rcases List.mem_append.mp hp with hp1 | hp2
            · exact hb_dom p hp1
            · exact hS₂dom p hp2)
        (honCtx2 ▸ Subst.eliminates_onCtx (hE12 p hp))
        (happ2 scrutTy ▸ hE12 p hp scrutTy)
        (happ2 ρ ▸ hE12 p hp ρ)
        (fun hcc => hSe p (List.mem_append_left _ hp)
          (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hcc))).2.2
        (Expr.mem_branchListTyFreeVars_of hq hc)
    have hrest_sound := InferBranches.sound hrest hctx1WF hctx1bel
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc hscrutTy))
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc hρ))
      hscrut'bel hρ'bel K (fun k hk => by have := hKΦ k hk; omega)
      (fun y hy => hKbr y (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hy))
      (fun p hp => hSK p (List.mem_append_right _ hp))
    intro p hp
    rcases List.mem_cons.mp hp with rfl | hp_rest
    · simp only [Subst.onCtx_append, Subst.onTy_append]
      rw [show bodyOut.substTyFvars (S₁ ++ S₂ ++ S₃)
            = ((bodyOut.substTyFvars S₁).substTyFvars S₂).substTyFvars S₃ from by
            rw [Expr.substTyFvars_append, Expr.substTyFvars_append]]
      have h0 := Infer.sound hbody hctx hbelow K hKΦ
        (fun y hy => hKbr y (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy))
        (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ hp)))
      have h1 := TypeOfElabHM.onSubst S₃ hS₃lc (TypeOfElabHM.onSubst S₂ hS₂lc h0)
      have huni_eq := huni.unifies
      simp only [Unifies] at huni_eq
      rw [huni_eq] at h1
      exact TypeOfElabMatchBranch.wildcard h1
    · simp only [Subst.onCtx_append, Subst.onTy_append]
      rw [show p.2.substTyFvars (S₁ ++ S₂ ++ S₃) = p.2.substTyFvars S₃ from by
            rw [Expr.substTyFvars_append, hrestfix p hp_rest]]
      exact hrest_sound p hp_rest
termination_by Expr.sizeBranches brs
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)
theorem InferRecGroup.sound {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut)
    (hctx : CtxWF ctx) (hbelow : CtxBelow Φ ctx)
    (htargets : ∀ τ ∈ targets, τ.IsLC) (htargetsB : ∀ τ ∈ targets, Ty.BelowFvars Φ τ)
    (K : List Nat) (hKΦ : ∀ k ∈ K, k < Φ)
    (hKbr : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K)
    (hSK : ∀ p ∈ S, p.1 ∉ K) :
    ∀ p ∈ bindingsOut.zip (targets.map S.onTy),
      TypeOfElabHM (S.onCtx ctx) (p.1.substTyFvars S) p.2 := by
  cases h with
  | nil => intro p hp; simp at hp
  | cons he huni hrest =>
    expose_names
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at hKbr
    obtain ⟨hτ_lc, hS₁⟩ := Infer.lc he hctx
    have hβ_lc : β.IsLC := htargets β List.mem_cons_self
    have hS₂ := huni.lc hτ_lc (Subst.onTy_lc hS₁ hβ_lc)
    have hle1 := Infer.frontier_le he
    have he_below := Infer.belowFvars he hbelow (fun y hy => hKΦ y (hKbr y (.inl hy)))
    have he_dom := Infer.dom_below he hbelow (fun y hy => hKΦ y (hKbr y (.inl hy)))
    have hβ_below : Ty.BelowFvars Φ β := htargetsB β List.mem_cons_self
    have hS₁β := Subst.onTy_belowFvars he_below.2 (hβ_below.mono hle1)
    have hS₂below := UnifyRel.belowFvars huni he_below.1 hS₁β
    have hS₂dom : ∀ p ∈ S₂, p.1 < Φ₁ := by
      intro p hp
      rcases UnifyRel.dom_mem huni p hp with h | h
      · exact he_below.1.mem_lt p.1 h
      · exact hS₁β.mem_lt p.1 h
    have hbelow2 := Subst.onCtx_below hS₂below (le_refl _) (Subst.onCtx_below he_below.2 hle1 hbelow)
    have hctx2 := Subst.onCtx_wf hS₂ (Subst.onCtx_wf hS₁ hctx)
    have hS₃' : ∀ p ∈ S₃, p.2.IsLC := InferRecGroup.lc hrest hctx2
      (fun b' hb' => by obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hb'
                        exact Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ (htargets b (List.mem_cons_of_mem _ hb))))
    have helimE := Infer.eliminates he hbelow (fun y hy => hKΦ y (hKbr y (.inl hy)))
      (fun p hp hc => hSK p (List.mem_append_left _ (List.mem_append_left _ hp)) (hKbr p.1 (.inl hc)))
    -- combined S₁ ++ S₂ idempotency
    have hcross : ∀ p ∈ S₁, ∀ q ∈ S₂, p.1 ∉ q.2.freeVars := by
      intro p hp q hq hvq
      rcases UnifyRel.range_mem huni q hq p.1 hvq with h | h
      · exact helimE.2 p hp h
      · exact helimE.1 p hp β h
    have hS₁₂elim := Subst.eliminates_append helimE.1 (UnifyRel.eliminates huni) hcross
    -- S₁ ++ S₂ fixes the rest's outputs (prefix-fix)
    have hfix : ∀ b ∈ restOut, b.substTyFvars (S₁ ++ S₂) = b := by
      intro b hb
      refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => ?_)
      refine (InferRecGroup.eOut_avoid hrest (w := p.1) ?_ ?_ ?_ ?_).2 b hb hc
      · rcases List.mem_append.mp hp with hp | hp
        · exact he_dom p hp
        · exact hS₂dom p hp
      · intro M hM
        rw [show (S₂.onCtx (S₁.onCtx ctx)) = (S₁ ++ S₂).onCtx ctx
              from (Subst.onCtx_append S₁ S₂ ctx).symm] at hM
        exact Subst.eliminates_onCtx (hS₁₂elim p hp) M hM
      · intro β' hβ'; obtain ⟨b0, _, rfl⟩ := List.mem_map.mp hβ'
        rw [show S₂.onTy (S₁.onTy b0) = (S₁ ++ S₂).onTy b0 from (Subst.onTy_append S₁ S₂ b0).symm]
        exact hS₁₂elim p hp b0
      · rcases List.mem_append.mp hp with hp | hp
        · exact fun hcc => hSK p (List.mem_append_left _ (List.mem_append_left _ hp))
            (hKbr p.1 (.inr hcc))
        · exact fun hcc => hSK p (List.mem_append_left _ (List.mem_append_right _ hp))
            (hKbr p.1 (.inr hcc))
    -- the recursive group typing
    have hrest_sound := InferRecGroup.sound hrest hctx2 hbelow2
      (fun b' hb' => by obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hb'
                        exact Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ (htargets b (List.mem_cons_of_mem _ hb))))
      (fun b' hb' => by obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hb'
                        exact Subst.onTy_belowFvars hS₂below
                          (Subst.onTy_belowFvars he_below.2 ((htargetsB b (List.mem_cons_of_mem _ hb)).mono hle1)))
      K (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle1)
      (fun y hy => hKbr y (.inr hy)) (fun p hp => hSK p (List.mem_append_right _ hp))
    intro p hp
    simp only [List.map_cons, List.zip_cons_cons, List.mem_cons] at hp
    rcases hp with rfl | hp_rest
    · rw [Subst.onCtx_append, Subst.onCtx_append, Subst.onTy_append, Subst.onTy_append]
      have h0 := Infer.sound he hctx hbelow K hKΦ (fun y hy => hKbr y (.inl hy))
        (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ hp)))
      have hhead := TypeOfElabHM.onSubst S₃ hS₃' (TypeOfElabHM.onSubst S₂ hS₂ h0)
      rw [← Expr.substTyFvars_append, ← Expr.substTyFvars_append, ← List.append_assoc] at hhead
      have huni_eq := huni.unifies
      simp only [Unifies] at huni_eq
      rw [huni_eq] at hhead
      exact hhead
    · have hmap : List.map (Subst.onTy (S₁ ++ S₂ ++ S₃)) βs
          = List.map (Subst.onTy S₃) (List.map (fun b => Subst.onTy S₂ (Subst.onTy S₁ b)) βs) := by
        rw [List.map_map]
        apply List.map_congr_left
        intro b _
        simp only [Function.comp_apply, Subst.onTy_append]
      rw [hmap] at hp_rest
      have hb_in : p.1 ∈ restOut := (List.of_mem_zip hp_rest).1
      rw [Subst.onCtx_append, Subst.onCtx_append,
          show p.1.substTyFvars (S₁ ++ S₂ ++ S₃) = p.1.substTyFvars S₃ from by
            rw [Expr.substTyFvars_append, hfix p.1 hb_in]]
      exact hrest_sound p hp_rest
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)
/-- **Soundness of an annotated recursion group**, cofinite at the (rigid) schemes.
    Each elaborated binding (substituted by the group's `S`), opened at any
    sufficiently-fresh `Xs`, types at its scheme's opening — exactly the cofinite
    premise of `TypeOfElabHM.letRecAnn`. -/
theorem InferRecGroupAnn.sound {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut)
    (hctx : CtxWF ctx) (hbelow : CtxBelow Φ ctx)
    (hschemesWF : ∀ σ ∈ schemes, σ.WF)
    (K : List Nat) (hKΦ : ∀ k ∈ K, k < Φ)
    (hKbr : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K)
    (hKsch : ∀ σ ∈ schemes, ∀ y ∈ σ.body.freeVars, y ∈ K)
    (hSK : ∀ p ∈ S, p.1 ∉ K) :
    ∃ L : List Nat, ∀ p ∈ (bindingsOut.map (·.substTyFvars S)).zip schemes,
      ∀ Xs, FreshNames L p.2.paramCount Xs →
        TypeOfElabHM (S.onCtx ctx) (p.1.openTyVars Xs) (p.2.openVars Xs) := by
  cases h with
  | nil => exact ⟨[], by intro p hp; simp at hp⟩
  | cons hΦN hinfer huni hesc1 hesc2 hrest =>
    expose_names
    have hσwf : σ.WF := hschemesWF σ List.mem_cons_self
    have hrle : N + σ.paramCount ≤ Φ₁ := Infer.frontier_le hinfer
    have hΦN' : Φ ≤ Φ₁ := le_trans hΦN (le_trans (Nat.le_add_right _ _) hrle)
    have hctx_pc : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hbelow M hM).mono (by omega)
    have hσbody : Ty.BelowFvars Φ σ.body :=
      Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hKsch σ List.mem_cons_self v hv))
    obtain ⟨hrhs_lc, hrhs_s⟩ := Infer.lc hinfer hctx
    have hΦ_rhs : ∀ y ∈ (Expr.openTyVars (freshVars N σ.paramCount) e).tyFreeVars, y < N + σ.paramCount := by
      intro y hy
      rcases Expr.tyFreeVars_openTyVars hy with h | h
      · have := hKΦ y (hKbr y (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self h)); omega
      · have := freshVars_lt y h; omega
    have hSe_rhs : ∀ p ∈ S₁, p.1 ∉ (Expr.openTyVars (freshVars N σ.paramCount) e).tyFreeVars := by
      intro p hp hc
      rcases Expr.tyFreeVars_openTyVars hc with h | h
      · exact hSK p (List.mem_append_left _ (List.mem_append_left _ hp))
          (hKbr p.1 (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self h))
      · exact hesc1 p.1 h (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩)
    obtain ⟨hr_τ, hr_s⟩ := Infer.belowFvars hinfer hctx_pc hΦ_rhs
    have hσopen : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
      Ty.openVars_belowFvars (hσbody.mono hΦN') (fun x hx => by have := freshVars_lt x hx; omega)
    have hSchk_below : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hr_τ hσopen
    have hSchk_lc : ∀ p ∈ Schk, p.2.IsLC :=
      UnifyRel.lc huni hrhs_lc (PolyTy.openVars_isLC hσwf (by simp))
    have hctx' : CtxWF (Schk.onCtx (S₁.onCtx ctx)) := Subst.onCtx_wf hSchk_lc (Subst.onCtx_wf hrhs_s hctx)
    have hbelow' : CtxBelow Φ₁ (Schk.onCtx (S₁.onCtx ctx)) :=
      Subst.onCtx_below hSchk_below (le_refl _) (Subst.onCtx_below hr_s hrle hctx_pc)
    have hschemesWF' : ∀ σ' ∈ schemes, σ'.WF := fun σ' hσ' => hschemesWF σ' (List.mem_cons_of_mem _ hσ')
    have hKbr' : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars rest, y ∈ K := fun y hy =>
      hKbr y (by simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact .inr hy)
    have hKsch' : ∀ σ' ∈ schemes, ∀ y ∈ σ'.body.freeVars, y ∈ K := fun σ' hσ' y hy =>
      hKsch σ' (List.mem_cons_of_mem _ hσ') y hy
    have hS₂K : ∀ p ∈ S₂, p.1 ∉ K := fun p hp => hSK p (List.mem_append_right _ hp)
    have hS₂lc : ∀ p ∈ S₂, p.2.IsLC := InferRecGroupAnn.lc hrest hctx' hschemesWF'
    have helimR := Infer.eliminates hinfer hctx_pc hΦ_rhs hSe_rhs
    have hcross : ∀ p ∈ S₁, ∀ q ∈ Schk, p.1 ∉ q.2.freeVars := by
      intro p hp q hq hwq
      rcases UnifyRel.range_mem huni q hq p.1 hwq with h | h
      · exact helimR.2 p hp h
      · rcases Ty.freeVars_openVars_subset p.1 h with h | h
        · exact hSK p (List.mem_append_left _ (List.mem_append_left _ hp)) (hKsch σ List.mem_cons_self p.1 h)
        · exact hesc1 p.1 h (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩)
    have hcombelim := Subst.eliminates_append helimR.1 (UnifyRel.eliminates huni) hcross
    have hYs_lt : ∀ y ∈ freshVars N σ.paramCount, y < Φ₁ := fun y hy => by have := freshVars_lt y hy; omega
    have hYs_ctx : ∀ y ∈ freshVars N σ.paramCount,
        ∀ M ∈ (Schk.onCtx (S₁.onCtx ctx)).env, y ∉ M.body.freeVars :=
      fun y hy M hM hc => hesc2 y hy (Env.mem_freeVars_iff.mpr ⟨M, hM, hc⟩)
    have hYs_sch : ∀ y ∈ freshVars N σ.paramCount, ∀ σ' ∈ schemes, y ∉ σ'.body.freeVars :=
      fun y hy σ' hσ' hc => by have := hKΦ y (hKsch' σ' hσ' y hc); have := freshVars_ge y hy; omega
    have hYs_rest : ∀ y ∈ freshVars N σ.paramCount, y ∉ Expr.tyFreeVars.RecGroup.tyFreeVars rest :=
      fun y hy hc => by have := hKΦ y (hKbr' y hc); have := freshVars_ge y hy; omega
    have hS₂Ys : ∀ p ∈ S₂, p.1 ∉ freshVars N σ.paramCount := fun p hp hc =>
      InferRecGroupAnn.dom_avoid hrest (hYs_lt p.1 hc) (hYs_ctx p.1 hc) (hYs_sch p.1 hc) (hYs_rest p.1 hc)
        (List.mem_map.mpr ⟨p, hp, rfl⟩)
    have hS₂Ysran : ∀ p ∈ S₂, ∀ u ∈ p.2.freeVars, u ∉ freshVars N σ.paramCount := fun p hp u hu hc =>
      (InferRecGroupAnn.eOut_avoid hrest (w := u) (hYs_lt u hc) (hYs_ctx u hc) (hYs_sch u hc)
        (hYs_rest u hc)).1 p hp hu
    -- head rhs types at the skolem opening `σ.openVars Ys`
    have hrhs_sound := Infer.sound hinfer hctx hctx_pc (K ++ freshVars N σ.paramCount)
      (fun k hk => by rcases List.mem_append.mp hk with h | h
                      · have := hKΦ k h; omega
                      · have := freshVars_lt k h; omega)
      (fun y hy => by rcases Expr.tyFreeVars_openTyVars hy with h | h
                      · exact List.mem_append_left _ (hKbr y (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self h))
                      · exact List.mem_append_right _ h)
      (fun p hp hc => by rcases List.mem_append.mp hc with h | h
                         · exact hSK p (List.mem_append_left _ (List.mem_append_left _ hp)) h
                         · exact hesc1 p.1 h (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩))
    have hr1 := TypeOfElabHM.onSubst Schk hSchk_lc hrhs_sound
    rw [← Expr.substTyFvars_append, ← Subst.onCtx_append] at hr1
    have hr2 := TypeOfElabHM.onSubst S₂ hS₂lc hr1
    rw [← Expr.substTyFvars_append, ← Subst.onCtx_append] at hr2
    have hu := huni.unifies
    simp only [Unifies] at hu
    have hSchk_fix : Schk.onTy (σ.openVars (freshVars N σ.paramCount))
        = σ.openVars (freshVars N σ.paramCount) :=
      Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
        rcases Ty.freeVars_openVars_subset p.1 hc with h | h
        · exact hSK p (List.mem_append_left _ (List.mem_append_right _ hp)) (hKsch σ List.mem_cons_self p.1 h)
        · exact hesc1 p.1 h (List.mem_map.mpr ⟨p, List.mem_append_right _ hp, rfl⟩))
    have hS₂fix : S₂.onTy (σ.openVars (freshVars N σ.paramCount))
        = σ.openVars (freshVars N σ.paramCount) :=
      Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
        rcases Ty.freeVars_openVars_subset p.1 hc with h | h
        · exact hS₂K p hp (hKsch σ List.mem_cons_self p.1 h)
        · exact hS₂Ys p hp h)
    rw [hu, hSchk_fix, hS₂fix] at hr2
    -- the head close-back equality (re-applying `S₁ ++ Schk` is a no-op; push `S₂`)
    have hRfixE : ∀ p ∈ S₁ ++ Schk, p.1 ∉ (eOut.substTyFvars (S₁ ++ Schk)).tyFreeVars :=
      fun p hp => Expr.dom_notMem_substTyFvars (fun x => hcombelim p hp x)
    have hbound : ((eOut.substTyFvars (S₁ ++ Schk)).closeTyVars (freshVars N σ.paramCount)).substTyFvars (S₁ ++ Schk ++ S₂)
        = (eOut.substTyFvars (S₁ ++ Schk ++ S₂)).closeTyVars (freshVars N σ.paramCount) := by
      rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Expr.substTyFvars_append,
          Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars
            (fun p hp hc => hRfixE p hp (Expr.tyFreeVars_closeTyVars_subset hc)),
          Expr.substTyFvars_closeTyVars hS₂Ys hS₂Ysran, ← Expr.substTyFvars_append]
    -- head freshness facts (for `closeBack_cofinite`)
    have hrhs_fresh : ∀ y ∈ freshVars N σ.paramCount,
        y ∉ (eOut.substTyFvars (S₁ ++ Schk ++ S₂)).recAnnSchemeFreeVars := by
      intro y hy hc
      have hyK : y ∈ K := Infer.recAnnSchemeFreeVars_substTyFvars_in_K hinfer (S := S₁ ++ Schk ++ S₂)
        (fun g hg => hKbr g (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self
          (Expr.recAnnSchemeFreeVars_subset_tyFreeVars (Expr.recAnnSchemeFreeVars_openTyVars ▸ hg))))
        (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ hp))) hSK hc
      have := freshVars_ge y hy; have := hKΦ y hyK; omega
    have hYs_env : ∀ y ∈ freshVars N σ.paramCount,
        y ∉ ((S₁ ++ Schk ++ S₂).onCtx ctx).env.freeVars := by
      intro y hy hc
      rw [show (S₁ ++ Schk ++ S₂).onCtx ctx = S₂.onCtx ((S₁ ++ Schk).onCtx ctx) from by
            rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Subst.onCtx_append],
          Env.mem_freeVars_iff] at hc
      obtain ⟨M, hM, hyM⟩ := hc
      rw [show (S₂.onCtx ((S₁ ++ Schk).onCtx ctx)).env
            = ((S₁ ++ Schk).onCtx ctx).env.map (Subst.onPolyTy S₂) from rfl, List.mem_map] at hM
      obtain ⟨M₀, hM₀, rfl⟩ := hM
      refine Subst.notMemOnTy (fun p hp hyp => hS₂Ysran p hp y hyp hy) (fun hc2 => ?_) hyM
      rw [Subst.onCtx_append] at hM₀
      exact hesc2 y hy (Env.mem_freeVars_iff.mpr ⟨M₀, hM₀, hc2⟩)
    have hYs_σ : ∀ y ∈ freshVars N σ.paramCount, y ∉ σ.body.freeVars :=
      fun y hy hc => by have := hKΦ y (hKsch σ List.mem_cons_self y hc); have := freshVars_ge y hy; omega
    have head_cof := TypeOfElabHM.closeBack_cofinite hr2 freshVars_nodup (by rw [freshVars_length])
      hrhs_fresh hYs_env hYs_σ
    -- the tail group's cofinite soundness
    obtain ⟨L_tail, tail_cof⟩ := InferRecGroupAnn.sound hrest hctx' hbelow' hschemesWF'
      K (fun k hk => by have := hKΦ k hk; omega) hKbr' hKsch' hS₂K
    -- `S₁ ++ Schk` fixes the tail outputs
    have hfix : ∀ b ∈ restOut, b.substTyFvars (S₁ ++ Schk ++ S₂) = b.substTyFvars S₂ := by
      intro b hb
      rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Expr.substTyFvars_append]
      congr 1
      refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => ?_)
      refine (InferRecGroupAnn.eOut_avoid hrest (w := p.1) ?_ ?_ ?_ ?_).2 b hb hc
      · rcases List.mem_append.mp hp with hp | hp
        · exact Infer.dom_below hinfer hctx_pc hΦ_rhs p hp
        · rcases UnifyRel.dom_mem huni p hp with h | h
          · exact hr_τ.mem_lt p.1 h
          · exact hσopen.mem_lt p.1 h
      · intro M hM
        rw [show (Schk.onCtx (S₁.onCtx ctx)) = (S₁ ++ Schk).onCtx ctx
              from (Subst.onCtx_append S₁ Schk ctx).symm] at hM
        exact Subst.eliminates_onCtx (hcombelim p hp) M hM
      · exact fun σ' hσ' hcc => hSK p (List.mem_append_left _ hp) (hKsch σ' (List.mem_cons_of_mem _ hσ') p.1 hcc)
      · exact fun hcc => hSK p (List.mem_append_left _ hp) (hKbr' p.1 hcc)
    have hctxbridge : S₂.onCtx (Schk.onCtx (S₁.onCtx ctx)) = (S₁ ++ Schk ++ S₂).onCtx ctx := by
      rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Subst.onCtx_append, Subst.onCtx_append]
    -- combine head + tail
    refine ⟨freshVars N σ.paramCount ++ L_tail, ?_⟩
    intro p hp Xs hXfresh
    simp only [List.map_cons, List.zip_cons_cons, List.mem_cons] at hp
    rcases hp with rfl | hp_tail
    · rw [hbound]
      exact head_cof Xs ⟨hXfresh.length, hXfresh.nodup,
        fun x hx hc => hXfresh.avoid x hx (List.mem_append_left _ hc)⟩
    · rw [List.zip_map_left_eq] at hp_tail
      obtain ⟨⟨bo, σ'⟩, hbozip, rfl⟩ := List.mem_map.mp hp_tail
      have hbo_in : bo ∈ restOut := (List.of_mem_zip hbozip).1
      have hzipS₂ : (bo.substTyFvars S₂, σ') ∈ (restOut.map (·.substTyFvars S₂)).zip schemes := by
        rw [List.zip_map_left_eq]; exact List.mem_map.mpr ⟨(bo, σ'), hbozip, rfl⟩
      have htail := tail_cof (bo.substTyFvars S₂, σ') hzipS₂ Xs
        ⟨hXfresh.length, hXfresh.nodup, fun x hx hc => hXfresh.avoid x hx (List.mem_append_right _ hc)⟩
      rw [hctxbridge, ← hfix bo hbo_in] at htail
      exact htail
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end

/-- **Headline soundness for a closed program.** A program with no free scoped
    type variables (`e.tyFreeVars = []`, the top-level case) instantiates the
    ambient skolem set `K := []`: the inferred substitution then trivially avoids
    it, recovering the plain `TypeOfElabHM (S.onCtx ctx) (eOut.substTyFvars S) τ`. -/
theorem Infer.sound_closed {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ)
    (hctx : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hclosed : e.tyFreeVars = []) :
    TypeOfElabHM (S.onCtx ctx) (eOut.substTyFvars S) τ :=
  Infer.sound h hctx hbelow [] (by simp) (by simp [hclosed]) (by simp)


/-! ## Algorithmic phase, step 2b: completeness (principality) scaffolding

Foundations for `Infer.complete`, independent of how the residual-substitution
obstruction is resolved. -/

/-! ### Completeness foundations: substitution agreement

`Infer` covers the full language, so principality is stated for all
expressions. The agreement lemmas let us swap one substitution for another that
agrees on the in-scope (`< Φ`) variables. -/

/-- Two substitutions agreeing on all vars `< Φ` act identically on a
    below-`Φ` type. -/
theorem Subst.onTy_congr {Φ : Nat} {S T : Subst}
    (hag : ∀ v, v < Φ → S.onTy (.fvar v) = T.onTy (.fvar v)) :
    ∀ {τ : Ty}, Ty.BelowFvars Φ τ → S.onTy τ = T.onTy τ := by
  intro τ hτ
  induction τ using Ty.rec_strong with
  | prim p => simp only [Subst.onTy_prim]
  | bvar i => simp only [Subst.onTy_bvar]
  | fvar n => cases hτ with | fvar hlt => exact hag n hlt
  | arrow a b iha ihb => cases hτ with | arrow ha hb => simp only [Subst.onTy_arrow, iha ha, ihb hb]
  | customTy nm tys ih =>
    cases hτ with
    | customTy hall =>
      simp only [Subst.onTy_customTy]
      apply congrArg (Ty.customTy nm)
      apply List.map_congr_left
      intro t ht
      exact ih t ht (hall t ht)

/-- Agreeing substitutions act identically on a below-`Φ` context. -/
theorem Subst.onCtx_congr {Φ : Nat} {S T : Subst} {ctx : Ctx}
    (hag : ∀ v, v < Φ → S.onTy (.fvar v) = T.onTy (.fvar v)) (hb : CtxBelow Φ ctx) :
    S.onCtx ctx = T.onCtx ctx := by
  simp only [Subst.onCtx, Subst.onEnv]
  congr 1
  apply List.map_congr_left
  intro M hM
  simp only [Subst.onPolyTy, Subst.onTy_congr hag (hb M hM)]

/-- `S` and `T` agree (act identically on every `fvar`) below frontier `Φ`. This
    is the "`S₀ = R ∘ S` below `Φ`" agreement clause threaded through the
    principality proofs; a reducible abbreviation so it is defeq to the raw `∀`. -/
abbrev Subst.AgreesBelow (Φ : Nat) (S T : Subst) : Prop :=
  ∀ v, v < Φ → S.onTy (.fvar v) = T.onTy (.fvar v)

/-- The recurring agreement-threading step: if `S₀` agrees with `S₁ ++ R₁` below
    `Φ`, and the residual `R₁` agrees with `S₂ ++ R₂` below the larger frontier
    `Φ₁` (which bounds `S₁`'s replacements), then `S₀` agrees with the composed
    `(S₁ ++ S₂) ++ R₂` below `Φ`. Collapses the `calc` repeated verbatim across the
    `pair`/`app`/`match` completeness cases. -/
theorem Subst.AgreesBelow.trans_append {Φ Φ₁ : Nat} {S₀ S₁ R₁ S₂ R₂ : Subst}
    (hle : Φ ≤ Φ₁)
    (hag1 : Subst.AgreesBelow Φ S₀ (S₁ ++ R₁))
    (hbelowS₁ : ∀ p ∈ S₁, Ty.BelowFvars Φ₁ p.2)
    (hag2 : Subst.AgreesBelow Φ₁ R₁ (S₂ ++ R₂)) :
    Subst.AgreesBelow Φ S₀ ((S₁ ++ S₂) ++ R₂) := by
  intro v hv
  have hbv : Ty.BelowFvars Φ₁ (S₁.onTy (.fvar v)) :=
    Subst.onTy_belowFvars hbelowS₁ (.fvar (by omega))
  calc S₀.onTy (.fvar v)
      = (S₁ ++ R₁).onTy (.fvar v) := hag1 v hv
    _ = R₁.onTy (S₁.onTy (.fvar v)) := by rw [Subst.onTy_append]
    _ = (S₂ ++ R₂).onTy (S₁.onTy (.fvar v)) := Subst.onTy_congr hag2 hbv
    _ = ((S₁ ++ S₂) ++ R₂).onTy (.fvar v) := by
          rw [List.append_assoc, Subst.onTy_append S₁ (S₂ ++ R₂)]


/-! ### Principality (completeness) — per-expression statement + case lemmas

`Infer.CompleteAt e` packages the principality property at a single expression,
abstracted over the frontier `Φ`, context `ctx`, input specialization `S₀`, and
declarative type `τ₀`. Each syntactic form gets its own case lemma (taking the
sub-expressions' `CompleteAt` as hypotheses, exactly the shape produced by
inducting on `e` with `Expr.rec_strong`); `Infer.completeAt`/`Infer.complete`
then just compose them. Keeping the universally-quantified `Φ ctx S₀ τ₀` inside
the predicate means each case lemma is independently stated and verifiable. -/

/-! ### `TypeOfHM` metatheory (direct induction port from `TypeOfElabHM`)

The forward "sandwich" bridge is dead (elaboration changes term structure for
polymorphic `let`, so no same-skeleton `TypeOfElabHM` typing exists — `toElab` is
false). Instead we port the standard HM metatheory directly. Mirrors the
`TypeOfElabHM` proofs; the `var` case is *simpler* (existential instantiation
witness, no `length = paramCount` premise). -/

/-- Branch disjunction motive for `TypeOfHM.rec_strong` (mirrors
    `TypeOfElabHM.BranchMotive`). -/
abbrev TypeOfHM.BranchMotive
    (motive : (ctx : Ctx) → (e : Expr) → (τ : Ty) → TypeOfHM ctx e τ → Prop)
    (ctx : Ctx) (branch : MatchPattern × Expr) (scrutTy : Ty)
    (resultTy : Ty) : Prop :=
  (∃ (ctor : Ctor) (c : CtorName) (n : Nat) (tyArgs : List Ty) (instContents : List Ty),
    branch.1 = .named c n ∧
    LookupList.get? ctx.ctors c = some ctor ∧
    scrutTy = .customTy ctor.tyName tyArgs ∧
    ctor.paramCount = tyArgs.length ∧
    n = ctor.contents.length ∧
    List.Forall₂ (InstantiatesBy tyArgs) ctor.contents instContents ∧
    ∃ hbody : TypeOfHM ⟨instContents.map PolyTy.mkTrivial ++ ctx.env, ctx.ctors⟩ branch.2 resultTy,
      motive ⟨instContents.map PolyTy.mkTrivial ++ ctx.env, ctx.ctors⟩ branch.2 resultTy hbody)
  ∨
  (branch.1 = .wildcard ∧
    ∃ hbody : TypeOfHM ctx branch.2 resultTy,
      motive ctx branch.2 resultTy hbody)

/-- Strong induction principle for `TypeOfHM` (mirrors `TypeOfElabHM.rec_strong`),
    packaged with a single motive so the metatheory never touches the mutual
    recursor / `motive_2` directly. -/
@[elab_as_elim]
theorem TypeOfHM.rec_strong
    {motive : (ctx : Ctx) → (e : Expr) → (τ : Ty) → TypeOfHM ctx e τ → Prop}
    (primLitUnit : ∀ {ctx : Ctx}, motive ctx (.primLit .unit) (.prim .unit) .primLitUnit)
    (primLitInt : ∀ {ctx : Ctx} {n : ℤ}, motive ctx (.primLit (.int n)) (.prim .int) .primLitInt)
    (primLitNat : ∀ {ctx : Ctx} {n : ℕ}, motive ctx (.primLit (.nat n)) (.prim .nat) .primLitNat)
    (primLitStr : ∀ {ctx : Ctx} {s : String}, motive ctx (.primLit (.str s)) (.prim .str) .primLitStr)
    (lambda : ∀ {paramTy : Ty} {ann : Option Ty} {bodyCtx ctx : Ctx} {body : Expr} {bodyTy : Ty}
      (hpc : ContainsBvarsUpTo 0 paramTy) (hann : ∀ T, ann = some T → paramTy = T)
      (heq : bodyCtx = { env := PolyTy.mkTrivial paramTy :: ctx.env, ctors := ctx.ctors })
      (hbody : TypeOfHM bodyCtx body bodyTy),
      motive bodyCtx body bodyTy hbody →
      motive ctx (.lambda ann body) (.arrow paramTy bodyTy) (.lambda hpc hann heq hbody))
    (app : ∀ {ctx : Ctx} {f : Expr} {argTy retTy : Ty} {input : Expr}
      (hf : TypeOfHM ctx f (.arrow argTy retTy)) (hinput : TypeOfHM ctx input argTy),
      motive ctx f (.arrow argTy retTy) hf → motive ctx input argTy hinput →
      motive ctx (.app f input) retTy (.app hf hinput))
    (letIn : ∀ {ann : Option PolyTy} {ctx : Ctx} {boundExpr : Expr} {bodyCtx : Ctx} {body : Expr}
      {bodyTy : Ty} {M : PolyTy} {L : List Nat}
      (hwf : M.WF) (hann : ∀ σ, ann = some σ → M = σ)
      (hcofin : ∀ Xs, FreshNames L M.paramCount Xs →
        TypeOfHM ctx (Expr.openBoundTyVars ann Xs boundExpr) (M.openVars Xs))
      (heq : bodyCtx = { env := M :: ctx.env, ctors := ctx.ctors })
      (hbody : TypeOfHM bodyCtx body bodyTy),
      (∀ Xs (hf : FreshNames L M.paramCount Xs),
        motive ctx (Expr.openBoundTyVars ann Xs boundExpr) (M.openVars Xs) (hcofin Xs hf)) →
      motive bodyCtx body bodyTy hbody →
      motive ctx (.letIn ann boundExpr body) bodyTy (.letIn hwf hann hcofin heq hbody))
    (var : ∀ {dbl : Nat} {polyTy : PolyTy} {instArgs tyArgs : List Ty} {ty : Ty} {ctx : Ctx}
      (hlook : ctx.env[dbl]? = some polyTy)
      (htyargs : ∀ tyArg ∈ instArgs, ContainsBvarsUpTo 0 tyArg)
      (hinst : InstantiatesBy instArgs polyTy.body ty),
      motive ctx (.var dbl tyArgs) ty (.var hlook htyargs hinst))
    (ctor : ∀ {name : CtorName} {ctorr : Ctor} {tyArgs : List Ty} {ty : Ty} {ctx : Ctx}
      (hlook : LookupList.get? ctx.ctors name = some ctorr)
      (htyargs : ∀ tyArg ∈ tyArgs, ContainsBvarsUpTo 0 tyArg)
      (hinst : InstantiatesBy tyArgs ctorr.toTy.body ty),
      motive ctx (.ctor name) ty (.ctor hlook htyargs hinst))
    (match_ : ∀ {ctx : Ctx} {scrutinee : Expr} {scrutTy : Ty}
      {branches : List (MatchPattern × Expr)} {resultTy : Ty}
      (hscrut : TypeOfHM ctx scrutinee scrutTy) (hne : branches ≠ [])
      (hbrs : ∀ branch ∈ branches, TypeOfMatchBranch ctx branch scrutTy resultTy),
      motive ctx scrutinee scrutTy hscrut →
      (∀ branch ∈ branches, TypeOfHM.BranchMotive motive ctx branch scrutTy resultTy) →
      motive ctx (.match_ scrutinee branches) resultTy (.match_ hscrut hne hbrs))
    (letRec : ∀ {ctx bodyCtx : Ctx} {bindings : List Expr} {L G : List Nat} {τs : List Ty}
      {Ms : List PolyTy} {body : Expr} {ρ : Ty}
      (hlen : bindings.length = τs.length)
      (hlen2 : τs.length = Ms.length)
      (hlc : ∀ τ ∈ τs, τ.IsLC)
      (hG : G.Nodup)
      (hgen : ∀ p ∈ τs.zip Ms, p.2 = PolyTy.genGroup G p.1)
      (hcofin : ∀ Xs, FreshNames L G.length Xs →
        ∀ p ∈ bindings.zip (τs.map (Ty.renameG G Xs)),
          TypeOfHM { ctx with env := (τs.map (Ty.renameG G Xs)).map PolyTy.mkTrivial ++ ctx.env }
            p.1 p.2)
      (heq : bodyCtx = { ctx with env := Ms ++ ctx.env })
      (hbody : TypeOfHM bodyCtx body ρ),
      (∀ Xs (hf : FreshNames L G.length Xs)
          p (hp : p ∈ bindings.zip (τs.map (Ty.renameG G Xs))),
        motive { ctx with env := (τs.map (Ty.renameG G Xs)).map PolyTy.mkTrivial ++ ctx.env }
          p.1 p.2 (hcofin Xs hf p hp)) →
      motive bodyCtx body ρ hbody →
      motive ctx (.letRec bindings body) ρ (.letRec hlen hlen2 hlc hG hgen hcofin heq hbody))
    (letRecAnn : ∀ {ctx bodyCtx : Ctx} {schemes : List PolyTy} {bindings : List Expr}
      {L : List Nat} {body : Expr} {ρ : Ty}
      (hlen : bindings.length = schemes.length)
      (hwf : ∀ σ ∈ schemes, σ.WF)
      (hcofin : ∀ p ∈ bindings.zip schemes,
        ∀ Xs, FreshNames L p.2.paramCount Xs →
          TypeOfHM { ctx with env := schemes ++ ctx.env }
            (p.1.openTyVars Xs) (p.2.openVars Xs))
      (heq : bodyCtx = { ctx with env := schemes ++ ctx.env })
      (hbody : TypeOfHM bodyCtx body ρ),
      (∀ p (hp : p ∈ bindings.zip schemes)
          Xs (hf : FreshNames L p.2.paramCount Xs),
        motive { ctx with env := schemes ++ ctx.env }
          (p.1.openTyVars Xs) (p.2.openVars Xs) (hcofin p hp Xs hf)) →
      motive bodyCtx body ρ hbody →
      motive ctx (.letRecAnn schemes bindings body) ρ (.letRecAnn hlen hwf hcofin heq hbody))
    {ctx : Ctx} {e : Expr} {τ : Ty} (h : TypeOfHM ctx e τ) : motive ctx e τ h := by
  induction h using TypeOfHM.rec
    (motive_2 := fun ctx br scrutTy resultTy _ =>
      TypeOfHM.BranchMotive motive ctx br scrutTy resultTy) with
  | primLitUnit => exact primLitUnit
  | primLitInt => exact primLitInt
  | primLitNat => exact primLitNat
  | primLitStr => exact primLitStr
  | lambda hpc hann heq hbody ihbody => exact lambda hpc hann heq hbody ihbody
  | app hf hinput ihf ihinput => exact app hf hinput ihf ihinput
  | letIn hwf hann hcofin heq hbody ihcofin ihbody =>
      exact letIn hwf hann hcofin heq hbody ihcofin ihbody
  | var hlook htyargs hinst => exact var hlook htyargs hinst
  | ctor hlook htyargs hinst => exact ctor hlook htyargs hinst
  | match_ hscrut hne hbrs ihscrut ihbrs => exact match_ hscrut hne hbrs ihscrut ihbrs
  | letRec hlen hlen2 hlc hG hgen hcofin heq hbody ihcofin ihbody =>
      exact letRec hlen hlen2 hlc hG hgen hcofin heq hbody ihcofin ihbody
  | letRecAnn hlen hwf hcofin heq hbody ihcofin ihbody =>
      exact letRecAnn hlen hwf hcofin heq hbody ihcofin ihbody
  | mk hlook hscrutEq hpc hn hinstC hpb heq hbodyT ih =>
      subst hpb; subst heq
      exact Or.inl ⟨_, _, _, _, _, rfl, hlook, hscrutEq, hpc, hn, hinstC, hbodyT, ih⟩
  | wildcard hbodyT ih =>
      exact Or.inr ⟨rfl, hbodyT, ih⟩

/-! Local copies of Core-private auxiliary lemmas (needed by the `letRec`/`letRecAnn`
    cases of `typ_subst_preservation_uniform`; cf. the `SpikeLetRecAnn` precedent of
    copying Core-private helpers into the consuming file). -/

private theorem Ty.freeVars_subset_freeVarsList {V : Ty} {Vs : List Ty}
    (h : V ∈ Vs) : ∀ x ∈ V.freeVars, x ∈ Ty.freeVarsList Vs := by
  induction Vs with
  | nil => exact absurd h List.not_mem_nil
  | cons hd tl ih =>
    intro x hx
    simp only [Ty.freeVarsList, List.mem_dedup, List.mem_append]
    cases h with
    | head _ => exact .inl hx
    | tail _ h' => exact .inr (ih h' x hx)

private theorem Ty.IsLC.substFvars {s : List (Nat × Ty)} {τ : Ty}
    (hs : ∀ p ∈ s, p.2.IsLC) (hτ : τ.IsLC) : (Ty.substFvars s τ).IsLC := by
  induction s generalizing τ with
  | nil => exact hτ
  | cons hd tl ih =>
    obtain ⟨Z, U⟩ := hd
    simp only [Ty.substFvars]
    exact ih (fun p hp => hs p (List.mem_cons_of_mem _ hp))
      (Ty.IsLC.substFvar (hs (Z, U) List.mem_cons_self) hτ)

private theorem Ty.renameG_isLC {G Xs : List Nat} {τ : Ty}
    (hτ : τ.IsLC) : (Ty.renameG G Xs τ).IsLC := by
  unfold Ty.renameG
  refine Ty.IsLC.substFvars ?_ hτ
  intro p hp
  obtain ⟨x, _, hx⟩ := List.mem_map.mp (List.of_mem_zip hp).2
  rw [← hx]; exact .fvar

private theorem List.mem_zip_map {α β γ δ : Type _} {f : α → γ} {g : β → δ} :
    ∀ {l : List α} {r : List β} {p : γ × δ},
      p ∈ (l.map f).zip (r.map g) → ∃ a b, (a, b) ∈ l.zip r ∧ p = (f a, g b) := by
  intro l
  induction l with
  | nil => intro r p h; simp at h
  | cons hd tl ih =>
    intro r p h
    cases r with
    | nil => simp at h
    | cons rhd rtl =>
      simp only [List.map_cons, List.zip_cons_cons, List.mem_cons] at h
      cases h with
      | inl heq => exact ⟨hd, rhd, List.mem_cons_self, heq⟩
      | inr h' =>
        obtain ⟨a, b, hmem, heq⟩ := ih h'
        exact ⟨a, b, List.mem_cons_of_mem _ hmem, heq⟩

private theorem List.mem_zip_map_right {α β γ : Type _} {g : β → γ}
    {l : List α} {r : List β} {a : α} {b : β}
    (h : (a, b) ∈ l.zip r) : (a, g b) ∈ l.zip (r.map g) := by
  induction l generalizing r with
  | nil => simp at h
  | cons hd tl ih =>
    cases r with
    | nil => simp at h
    | cons rhd rtl =>
      simp only [List.map_cons, List.zip_cons_cons, List.mem_cons] at h ⊢
      cases h with
      | inl heq =>
        rw [Prod.mk.injEq] at heq
        obtain ⟨rfl, rfl⟩ := heq
        exact Or.inl rfl
      | inr h' => exact Or.inr (ih h')

/-- Single-fvar `substTyFvar` is the one-element iterated `substTyFvars` (defeq).
    Lets the `match_`/`letRec`/`letRecAnn` cases use the *public*
    `Expr.substTyFvars_*` structural lemmas instead of the Core-private single-fvar
    list helpers. -/
private theorem Expr.substTyFvar_eq_substTyFvars_single {Z : Nat} {U : Ty} {e : Expr} :
    Expr.substTyFvar Z U e = Expr.substTyFvars [(Z, U)] e := rfl

/-- **Single-fvar substitution preservation for `TypeOfHM`** (uniform over the whole
    env). Direct induction port of `TypeOfElabHM.typ_subst_preservation_uniform`; the
    `var`/`ctor` cases are simpler (existential instantiation witness, no length
    premise). All auxiliary lemmas (`Ty.renameG_*`, `PolyTy.genGroup_*`,
    `InstantiatesBy.substFvar`, …) are relation-agnostic and reused verbatim.
    The `match_`/`letRec`/`letRecAnn` cases bridge the Core-private single-fvar
    list helpers via `Expr.substTyFvar Z U e ≡ Expr.substTyFvars [(Z, U)] e` (defeq)
    + the public `Expr.substTyFvars_match/_letRec/_letRecAnn`. -/
theorem TypeOfHM.typ_subst_preservation_uniform {Z : Nat} {U : Ty} (h_U_lc : U.IsLC)
    {ctx : Ctx} {e : Expr} {τ : Ty} (h : TypeOfHM ctx e τ) :
    TypeOfHM ⟨ctx.env.substFvar Z U, ctx.ctors⟩ (e.substTyFvar Z U) (Ty.substFvar Z U τ) := by
  induction h using TypeOfHM.rec_strong with
  | primLitUnit => exact .primLitUnit
  | primLitInt => exact .primLitInt
  | primLitNat => exact .primLitNat
  | primLitStr => exact .primLitStr
  | app _ _ ihf ihinput =>
    simp only [Expr.substTyFvar]
    simp only [Ty.substFvar] at ihf
    exact .app ihf ihinput
  | lambda hpc hann heq hbody ihbody =>
    subst heq
    expose_names
    simp only [Ty.substFvar, Expr.substTyFvar]
    refine TypeOfHM.lambda (Ty.IsLC.substFvar h_U_lc hpc) ?_ rfl ?_
    · intro T hT
      rcases ann with _ | T₀
      · simp at hT
      · simp only [Option.map_some, Option.some.injEq] at hT
        subst hT
        rw [hann T₀ rfl]
    · simpa only [Env.substFvar, List.map_cons, PolyTy.substFvar, PolyTy.mkTrivial] using ihbody
  | var hlook htyargs hinst =>
    simp only [Expr.substTyFvar]
    have hlook' := congrArg (Option.map (PolyTy.substFvar Z U)) hlook
    simp only [Option.map_some] at hlook'
    rw [← List.getElem?_map] at hlook'
    refine TypeOfHM.var hlook' ?_ (InstantiatesBy.substFvar h_U_lc hinst)
    intro tyArg hmem
    obtain ⟨t, ht, rfl⟩ := List.mem_map.mp hmem
    exact Ty.IsLC.substFvar h_U_lc (htyargs t ht)
  | ctor hlook htyargs hinst =>
    simp only [Expr.substTyFvar]
    have hbody := InstantiatesBy.substFvar (Z := Z) (U := U) h_U_lc hinst
    rw [Ty.substFvar_fresh (NoFreeVars.not_mem_freeVars (Ctor.toTy_body_noFreeVars _) Z)] at hbody
    refine TypeOfHM.ctor hlook ?_ hbody
    intro tyArg hmem
    obtain ⟨t, ht, rfl⟩ := List.mem_map.mp hmem
    exact Ty.IsLC.substFvar h_U_lc (htyargs t ht)
  | letIn hwf hann hcofin heq hbody ihcofin ihbody =>
    subst heq
    expose_names
    simp only [Expr.substTyFvar]
    refine TypeOfHM.letIn (M := PolyTy.substFvar Z U M) (L := Z :: L)
      (PolyTy.WF.substFvar h_U_lc hwf) ?_ ?_ rfl ?_
    · intro σ hσ
      rcases ann with _ | σ₀
      · simp at hσ
      · simp only [Option.map_some, Option.some.injEq] at hσ
        subst hσ
        rw [hann σ₀ rfl]
    · intro Xs hfresh
      have hZ_notin : Z ∉ Xs := fun hc => hfresh.avoid Z hc List.mem_cons_self
      have hXs_freshL : FreshNames L M.paramCount Xs :=
        ⟨by simpa using hfresh.length, hfresh.nodup,
         fun x hx hc => hfresh.avoid x hx (List.mem_cons_of_mem _ hc)⟩
      have hbe := ihcofin Xs hXs_freshL
      rw [Expr.substTyFvar_openBoundTyVars h_U_lc hZ_notin] at hbe
      have hopen : (M.substFvar Z U).openVars Xs = Ty.substFvar Z U (M.openVars Xs) := by
        unfold PolyTy.openVars PolyTy.substFvar
        exact (Ty.substFvar_openVars h_U_lc hZ_notin).symm
      rw [hopen]
      exact hbe
    · simpa only [Env.substFvar, List.map_cons] using ihbody
  | match_ hscrut hne hbrs ihscrut ihbrs =>
    rw [Expr.substTyFvar_eq_substTyFvars_single, Expr.substTyFvars_match]
    refine TypeOfHM.match_ ihscrut ?_ ?_
    · simpa using hne
    · intro branch' hmem'
      obtain ⟨⟨pat, body⟩, hmem, rfl⟩ := List.mem_map.mp hmem'
      rcases ihbrs (pat, body) hmem with
        ⟨ct, c, n, tyArgs, instContents, hpat, hlook, hScrutEq, hpc, hcontents, hinstC, _, hbodyIH⟩ |
        ⟨hpat, _, hbodyIH⟩
      · subst hpat
        have hcc : ct.contents.map (Ty.substFvar Z U) = ct.contents := by
          have hpt : ∀ c ∈ ct.contents, Ty.substFvar Z U c = id c := fun c hc =>
            Ty.substFvar_fresh ((ct.closed c hc).not_mem_freeVars Z)
          rw [List.map_congr_left hpt, List.map_id]
        have hinstC' := InstantiatesBy.forall2_substFvar (Z := Z) (U := U) h_U_lc hinstC
        rw [hcc] at hinstC'
        rw [Env.substFvar_append, Env.substFvar_map_mkTrivial] at hbodyIH
        refine TypeOfMatchBranch.mk hlook ?_ (by simpa using hpc) hcontents hinstC' rfl rfl hbodyIH
        rw [hScrutEq]; simp [Ty.substFvar, TyList.substFvar_eq_map]
      · subst hpat
        exact TypeOfMatchBranch.wildcard hbodyIH
  | letRec hlen hlen2 hlc hG hgen hcofin heq hbody ihcofin ihbody =>
    subst heq
    expose_names
    rw [Expr.substTyFvar_eq_substTyFvars_single, Expr.substTyFvars_letRec]
    obtain ⟨W, hWlen0, hWnodup, hWavoid⟩ :=
      exists_fresh_names (G ++ [Z] ++ U.freeVars ++ Ty.freeVarsList τs) G.length
    have hWG : ∀ w ∈ W, w ∉ G := fun w hw hc =>
      hWavoid w hw (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_left _ hc)))
    have hGW : ∀ g ∈ G, g ∉ W := fun g hg hc => hWG g hc hg
    have hZW : Z ∉ W := fun hc =>
      hWavoid Z hc (List.mem_append_left _ (List.mem_append_left _
        (List.mem_append_right _ (List.mem_singleton.2 rfl))))
    have hUW : ∀ u ∈ U.freeVars, u ∉ W := fun u hu hc =>
      hWavoid u hc (List.mem_append_left _ (List.mem_append_right _ hu))
    have hW_τs : ∀ w ∈ W, w ∉ Ty.freeVarsList τs := fun w hw hc =>
      hWavoid w hw (List.mem_append_right _ hc)
    have hWfree : ∀ t ∈ τs, ∀ w ∈ W, w ∉ t.freeVars := fun t ht w hw hc =>
      hW_τs w hw (Ty.freeVars_subset_freeVarsList ht w hc)
    refine TypeOfHM.letRec
      (τs := τs.map (fun t => Ty.substFvar Z U (Ty.renameG G W t)))
      (Ms := Ms.map (PolyTy.substFvar Z U)) (G := W) (L := Z :: (G ++ W ++ L))
      ?_ ?_ ?_ ?_ ?_ ?_ rfl ?_
    · simp only [List.length_map]; exact hlen
    · simp only [List.length_map]; exact hlen2
    · intro τ' hτ'
      obtain ⟨t, ht, rfl⟩ := List.mem_map.mp hτ'
      exact Ty.IsLC.substFvar h_U_lc (Ty.renameG_isLC (hlc t ht))
    · exact hWnodup
    · intro p hp
      obtain ⟨a, b, hab, rfl⟩ := List.mem_zip_map hp
      have ha : a ∈ τs := (List.of_mem_zip hab).1
      have hbgen : b = PolyTy.genGroup G a := hgen (a, b) hab
      rw [hbgen, PolyTy.genGroup_renameG (hlc a ha) hWlen0 hG hWnodup hGW (hWfree a ha)]
      exact PolyTy.genGroup_substFvar hZW hUW
    · intro Xs hfresh p hp
      have hXlen : Xs.length = G.length := hfresh.length.trans hWlen0
      have hZXs : Z ∉ Xs := fun hc => hfresh.avoid Z hc List.mem_cons_self
      have hGXs : ∀ g ∈ G, g ∉ Xs := fun g hg hc =>
        hfresh.avoid g hc (List.mem_cons_of_mem _ (List.mem_append_left _ (List.mem_append_left _ hg)))
      have hWXs : ∀ w ∈ W, w ∉ Xs := fun w hw hc =>
        hfresh.avoid w hc (List.mem_cons_of_mem _ (List.mem_append_left _ (List.mem_append_right _ hw)))
      have hXsL : FreshNames L G.length Xs :=
        ⟨hXlen, hfresh.nodup, fun x hx hc =>
          hfresh.avoid x hx (List.mem_cons_of_mem _ (List.mem_append_right _ hc))⟩
      have key : ∀ t ∈ τs,
          Ty.renameG W Xs (Ty.substFvar Z U (Ty.renameG G W t))
            = Ty.substFvar Z U (Ty.renameG G Xs t) := by
        intro t ht
        rw [Ty.renameG_substFvar_comm h_U_lc hZW hUW hZXs
              (Ty.renameG_isLC (hlc t ht)) hWnodup hfresh.length hWXs,
            Ty.renameG_renameG (hlc t ht) hG hWnodup hWlen0 hXlen hGW (hWfree t ht) hWXs hGXs]
      have hlist : List.map (Ty.renameG W Xs)
            (List.map (fun t => Ty.substFvar Z U (Ty.renameG G W t)) τs)
          = List.map (fun t => Ty.substFvar Z U (Ty.renameG G Xs t)) τs := by
        rw [List.map_map]
        apply List.map_congr_left
        intro t ht
        simpa only [Function.comp_apply] using key t ht
      rw [hlist] at hp ⊢
      obtain ⟨a, b, hab, rfl⟩ := List.mem_zip_map hp
      have hIH := ihcofin Xs hXsL (a, Ty.renameG G Xs b) (List.mem_zip_map_right hab)
      rw [Env.substFvar_append, Env.substFvar_map_mkTrivial,
          show List.map (Ty.substFvar Z U) (List.map (Ty.renameG G Xs) τs)
             = List.map (fun t => Ty.substFvar Z U (Ty.renameG G Xs t)) τs from List.map_map] at hIH
      exact hIH
    · simpa only [Env.substFvar, List.map_append] using ihbody
  | letRecAnn hlen hwf hcofin heq hbody ihcofin ihbody =>
    subst heq
    expose_names
    rw [Expr.substTyFvar_eq_substTyFvars_single, Expr.substTyFvars_letRecAnn,
        show PolyTy.substFvars [(Z, U)] = PolyTy.substFvar Z U from rfl]
    refine TypeOfHM.letRecAnn (L := Z :: L) ?_ ?_ ?_ rfl ?_
    · simp only [List.length_map]; exact hlen
    · intro σ' hσ'
      obtain ⟨σ, hσ, rfl⟩ := List.mem_map.mp hσ'
      exact PolyTy.WF.substFvar h_U_lc (hwf σ hσ)
    · intro p hp Xs hfresh
      obtain ⟨a, b, hab, rfl⟩ := List.mem_zip_map hp
      have hZXs : Z ∉ Xs := fun hc => hfresh.avoid Z hc List.mem_cons_self
      have hfreshL : FreshNames L b.paramCount Xs :=
        ⟨hfresh.length, hfresh.nodup, fun x hx hc => hfresh.avoid x hx (List.mem_cons_of_mem _ hc)⟩
      have hIH := ihcofin (a, b) hab Xs hfreshL
      rw [Expr.substTyFvar_openTyVars h_U_lc hZXs,
          ← PolyTy.substFvar_openVars h_U_lc hZXs] at hIH
      simpa only [Env.substFvar, List.map_append] using hIH
    · rw [← Expr.substTyFvar_eq_substTyFvars_single]
      simpa only [Env.substFvar, List.map_append] using ihbody

/-- Single-variable substitution preserves `TypeOfHM` across the whole context. -/
theorem TypeOfHM.onSubstFvar {ctx : Ctx} {e : Expr} {τ : Ty} (Z : Nat) (U : Ty)
    (hU : U.IsLC) (h : TypeOfHM ctx e τ) :
    TypeOfHM (Subst.onCtx [(Z, U)] ctx) (e.substTyFvar Z U) (Subst.onTy [(Z, U)] τ) :=
  TypeOfHM.typ_subst_preservation_uniform hU h

/-- **Substitution preservation for `TypeOfHM`** (whole substitution). Direct
    induction port (via `onSubstFvar` + `induction S`), mirroring
    `TypeOfElabHM.onSubst`. No `CtxWF` needed. -/
theorem TypeOfHM.onSubst {ctx : Ctx} {e : Expr} {τ : Ty} (S : Subst)
    (h_lc : ∀ p ∈ S, p.2.IsLC) (h : TypeOfHM ctx e τ) :
    TypeOfHM (S.onCtx ctx) (e.substTyFvars S) (S.onTy τ) := by
  induction S generalizing ctx e τ with
  | nil => simpa [Expr.substTyFvars] using h
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    have hU : U.IsLC := h_lc (Z, U) (List.mem_cons_self ..)
    have hS' : ∀ p ∈ S', p.2.IsLC := fun p hp => h_lc p (List.mem_cons_of_mem _ hp)
    have step := TypeOfHM.onSubstFvar Z U hU h
    have rest := ih hS' step
    rw [show ((Z, U) :: S') = [(Z, U)] ++ S' from rfl, Subst.onCtx_append, Subst.onTy_append]
    exact rest

/-- **Fixed-term corollary of `TypeOfHM.onSubst`.** When `S` fixes `e`'s annotation
    free vars (`e.substTyFvars S = e`), preservation keeps the term fixed. -/
theorem TypeOfHM.onSubst_fixed {ctx : Ctx} {e : Expr} {τ : Ty} (S : Subst)
    (h_lc : ∀ p ∈ S, p.2.IsLC) (h_fix : e.substTyFvars S = e) (h : TypeOfHM ctx e τ) :
    TypeOfHM (S.onCtx ctx) e (S.onTy τ) := by
  have key := TypeOfHM.onSubst S h_lc h
  rwa [h_fix] at key

/-- **Iterated fixed-env substitution preservation for `TypeOfHM`.** When the
    substitution keys avoid the context env's free vars, the env is unchanged;
    only the term and type are substituted. Direct port of
    `TypeOfElabHM.typ_substs_preservation` (via the single-fvar uniform lemma). -/
theorem TypeOfHM.typ_substs_preservation {ctx : Ctx} {e : Expr}
    (pairs : List (Nat × Ty))
    (h_fresh : ∀ p ∈ pairs, p.1 ∉ ctx.env.freeVars)
    (h_lc : ∀ p ∈ pairs, Ty.IsLC p.2)
    {τ : Ty} (h : TypeOfHM ctx e τ) :
    TypeOfHM ctx (e.substTyFvars pairs) (Ty.substFvars pairs τ) := by
  induction pairs generalizing e τ with
  | nil => exact h
  | cons hd tl ih =>
    obtain ⟨Z, U⟩ := hd
    simp only [Expr.substTyFvars, Ty.substFvars]
    have hZ : Z ∉ ctx.env.freeVars := h_fresh (Z, U) List.mem_cons_self
    have hU : Ty.IsLC U := h_lc (Z, U) List.mem_cons_self
    have hstep := TypeOfHM.typ_subst_preservation_uniform (Z := Z) (U := U) hU h
    rw [Env.substFvar_fresh hZ] at hstep
    exact ih (fun p hp => h_fresh p (List.mem_cons_of_mem _ hp))
             (fun p hp => h_lc p (List.mem_cons_of_mem _ hp)) hstep

/-! Regularity for `TypeOfHM`: a declaratively-typed term has a locally-closed type.
    Direct port of `TypeOfElabHM.regular`; `var`/`ctor` via `InstantiatesBy.preserves_bvars`
    on the (existential) LC instantiation witness. -/
mutual
theorem TypeOfHM.regular : {ctx : Ctx} → {e : Expr} → {τ : Ty} →
    TypeOfHM ctx e τ → τ.IsLC
  | _, _, _, .primLitUnit => .prim
  | _, _, _, .primLitInt => .prim
  | _, _, _, .primLitNat => .prim
  | _, _, _, .primLitStr => .prim
  | _, _, _, .lambda hpc _ _ hbody => .arrow hpc (TypeOfHM.regular hbody)
  | _, _, _, .app hf _ => by
    have := TypeOfHM.regular hf; cases this with | arrow _ hret => exact hret
  | _, _, _, .letIn _ _ _ _ hbody => TypeOfHM.regular hbody
  | _, _, _, .var _ htyargs hinst => InstantiatesBy.preserves_bvars htyargs hinst
  | _, _, _, .ctor _ htyargs hinst => InstantiatesBy.preserves_bvars htyargs hinst
  | _, _, _, @TypeOfHM.match_ _ _ _ branches _ hscrut hne hbrs => by
    obtain ⟨hd, tl, rfl⟩ := List.exists_cons_of_ne_nil hne
    exact TypeOfMatchBranch.regular (hbrs hd (List.mem_cons_self ..))
  | _, _, _, .letRec _ _ _ _ _ _ _ hbody => TypeOfHM.regular hbody
  | _, _, _, .letRecAnn _ _ _ _ hbody => TypeOfHM.regular hbody

theorem TypeOfMatchBranch.regular : {ctx : Ctx} → {br : MatchPattern × Expr} →
    {scrutTy : Ty} → {rt : Ty} →
    TypeOfMatchBranch ctx br scrutTy rt → rt.IsLC
  | _, _, _, _, .mk _ _ _ _ _ _ _ hbody => TypeOfHM.regular hbody
  | _, _, _, _, .wildcard hbody => TypeOfHM.regular hbody
end

/-- **Strong 4-motive mutual eliminator for `Infer`** (mirrors `TypeOfHM.rec_strong`).
    A thin repackaging of `Infer.rec` with distinctly-named minor premises (so the
    sibling `nil`/`cons`/`consWild` tag collisions disappear) that hands each case
    genuine induction hypotheses. Lets the backward-soundness / metatheory proofs do
    `induction h using Infer.rec_strong with | lambda … => …` and get kernel-valid
    terms (unlike the `cases h` + self-call fixpoint, which the kernel rejects when
    the conclusion mentions the recursion subject `e`). -/
@[elab_as_elim]
theorem Infer.rec_strong
    {motive : (Φ : ℕ) → (ctx : Ctx) → (e : Expr) → (Φ' : ℕ) → (S : Subst) → (eOut : Expr) →
      (τ : Ty) → Infer Φ ctx e Φ' S eOut τ → Prop}
    {motiveBr : (Φ : ℕ) → (ctx : Ctx) → (scrutTy ρ : Ty) → (brs : List (MatchPattern × Expr)) →
      (Φ' : ℕ) → (S : Subst) → (brsOut : List (MatchPattern × Expr)) →
      InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut → Prop}
    {motiveGrp : (Φ : ℕ) → (ctx : Ctx) → (bindings : List Expr) → (targets : List Ty) →
      (Φ' : ℕ) → (S : Subst) → (bindingsOut : List Expr) →
      InferRecGroup Φ ctx bindings targets Φ' S bindingsOut → Prop}
    {motiveGrpAnn : (Φ : ℕ) → (ctx : Ctx) → (bindings : List Expr) → (schemes : List PolyTy) →
      (Φ' : ℕ) → (S : Subst) → (bindingsOut : List Expr) →
      InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut → Prop}
    (primLitUnit : ∀ {Φ ctx},
      motive Φ ctx (.primLit .unit) Φ [] (.primLit .unit) (.prim .unit) .primLitUnit)
    (primLitInt : ∀ {Φ ctx n},
      motive Φ ctx (.primLit (.int n)) Φ [] (.primLit (.int n)) (.prim .int) .primLitInt)
    (primLitNat : ∀ {Φ ctx n},
      motive Φ ctx (.primLit (.nat n)) Φ [] (.primLit (.nat n)) (.prim .nat) .primLitNat)
    (primLitStr : ∀ {Φ ctx s},
      motive Φ ctx (.primLit (.str s)) Φ [] (.primLit (.str s)) (.prim .str) .primLitStr)
    (lambda : ∀ {Φ ctx ann paramTy body Φ₀ Φ' S bodyOut τb}
      (hseed : LamSeed Φ ann paramTy Φ₀)
      (hbody : Infer Φ₀ { ctx with env := PolyTy.mkTrivial paramTy :: ctx.env }
        body Φ' S bodyOut τb),
      motive Φ₀ { ctx with env := PolyTy.mkTrivial paramTy :: ctx.env }
          body Φ' S bodyOut τb hbody →
        motive Φ ctx (.lambda ann body) Φ' S (.lambda ann bodyOut) ((S.onTy paramTy).arrow τb)
          (.lambda hseed hbody))
    (app : ∀ {Φ ctx f arg Φ₁ Φ₂ S₁ S₂ S₃ fOut argOut τf τa}
      (hf : Infer Φ ctx f Φ₁ S₁ fOut τf) (harg : Infer Φ₁ (S₁.onCtx ctx) arg Φ₂ S₂ argOut τa)
      (huni : UnifyRel (S₂.onTy τf) (τa.arrow (Ty.fvar Φ₂)) S₃),
      motive Φ ctx f Φ₁ S₁ fOut τf hf →
        motive Φ₁ (S₁.onCtx ctx) arg Φ₂ S₂ argOut τa harg →
          motive Φ ctx (f.app arg) (Φ₂ + 1) (S₁ ++ S₂ ++ S₃) (fOut.app argOut) (S₃.onTy (Ty.fvar Φ₂))
            (.app hf harg huni))
    (var : ∀ {Φ ctx i tyArgsIn polyTy} (hlook : ctx.env[i]? = some polyTy),
      motive Φ ctx (.var i tyArgsIn) (Φ + polyTy.paramCount) []
        (.var i ((freshVars Φ polyTy.paramCount).map Ty.fvar))
        (polyTy.openVars (freshVars Φ polyTy.paramCount)) (.var hlook))
    (ctor : ∀ {Φ ctx name ctorr} (hlook : LookupList.get? ctx.ctors name = some ctorr),
      motive Φ ctx (.ctor name) (Φ + ctorr.paramCount) [] (.ctor name)
        (ctorr.toTy.openVars (freshVars Φ ctorr.paramCount)) (.ctor hlook))
    (letIn : ∀ {Φ ctx rhs body Φ₁ Φ₂ S₁ S₂ rhsOut bodyOut τ₁ τ₂}
      (hrhs : Infer Φ ctx rhs Φ₁ S₁ rhsOut τ₁)
      (hbody : Infer Φ₁ { (S₁.onCtx ctx) with
          env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env }
          body Φ₂ S₂ bodyOut τ₂),
      motive Φ ctx rhs Φ₁ S₁ rhsOut τ₁ hrhs →
        motive Φ₁ { (S₁.onCtx ctx) with
            env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env }
            body Φ₂ S₂ bodyOut τ₂ hbody →
          motive Φ ctx (.letIn none rhs body) Φ₂ (S₁ ++ S₂)
            (.letIn (some (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁))
              ((rhsOut.substTyFvars S₁).closeTyVars (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)) bodyOut)
            τ₂ (.letIn hrhs hbody))
    (letInAnn : ∀ {Φ N ctx σ rhs body Φ₁ Φ₂ S₁ Schk S₂ rhsOut bodyOut τ₁ τ₂}
      (hσwf : σ.WF) (hΦN : Φ ≤ N)
      (hrhs : Infer (N + σ.paramCount) ctx (rhs.openTyVars (freshVars N σ.paramCount)) Φ₁ S₁ rhsOut τ₁)
      (huni : UnifyRel τ₁ (σ.openVars (freshVars N σ.paramCount)) Schk)
      (hesc1 : ∀ y ∈ freshVars N σ.paramCount, y ∉ (S₁ ++ Schk).map Prod.fst)
      (hesc2 : ∀ y ∈ freshVars N σ.paramCount, y ∉ (Schk.onCtx (S₁.onCtx ctx)).env.freeVars)
      (hbody : Infer Φ₁ { (Schk.onCtx (S₁.onCtx ctx)) with
          env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env } body Φ₂ S₂ bodyOut τ₂),
      motive (N + σ.paramCount) ctx (rhs.openTyVars (freshVars N σ.paramCount)) Φ₁ S₁ rhsOut τ₁ hrhs →
        motive Φ₁ { (Schk.onCtx (S₁.onCtx ctx)) with
            env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env } body Φ₂ S₂ bodyOut τ₂ hbody →
          motive Φ ctx (.letIn (some σ) rhs body) Φ₂ (S₁ ++ Schk ++ S₂)
            (.letIn (some σ) ((rhsOut.substTyFvars (S₁ ++ Schk)).closeTyVars
              (freshVars N σ.paramCount)) bodyOut) τ₂ (.letInAnn hσwf hΦN hrhs huni hesc1 hesc2 hbody))
    (match_ : ∀ {Φ ctx scrut branches Φ₁ Φ₂ S₁ S₂ scrutOut branchesOut τs}
      (hscrut : Infer Φ ctx scrut Φ₁ S₁ scrutOut τs) (hne : branches ≠ [])
      (hbranches : InferBranches (Φ₁ + 1) (S₁.onCtx ctx) τs (.fvar Φ₁) branches Φ₂ S₂ branchesOut),
      motive Φ ctx scrut Φ₁ S₁ scrutOut τs hscrut →
        motiveBr (Φ₁ + 1) (S₁.onCtx ctx) τs (.fvar Φ₁) branches Φ₂ S₂ branchesOut hbranches →
          motive Φ ctx (.match_ scrut branches) Φ₂ (S₁ ++ S₂) (.match_ scrutOut branchesOut)
            (S₂.onTy (.fvar Φ₁)) (.match_ hscrut hne hbranches))
    (letRec : ∀ {Φ ctx bindings body Φ₁ Φ₂ S₁ S₂ bindingsOut bodyOut τ₂}
      (hgroup : InferRecGroup (Φ + bindings.length)
        { ctx with env := (freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i)) ++ ctx.env }
        bindings ((freshVars Φ bindings.length).map Ty.fvar) Φ₁ S₁ bindingsOut)
      (hbody : Infer Φ₁ { (S₁.onCtx ctx) with
          env := genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
            ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i))) ++ (S₁.onCtx ctx).env }
          body Φ₂ S₂ bodyOut τ₂),
      motiveGrp (Φ + bindings.length)
          { ctx with env := (freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i)) ++ ctx.env }
          bindings ((freshVars Φ bindings.length).map Ty.fvar) Φ₁ S₁ bindingsOut hgroup →
        motive Φ₁ { (S₁.onCtx ctx) with
            env := genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
              ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i))) ++ (S₁.onCtx ctx).env }
            body Φ₂ S₂ bodyOut τ₂ hbody →
          motive Φ ctx (.letRec bindings body) Φ₂ (S₁ ++ S₂)
            (Expr.letRecElab
              (genGroupVars (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
                ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i))))
              (bindingsOut.map (·.substTyFvars S₁))
              ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i))) bodyOut)
            τ₂ (.letRec hgroup hbody))
    (letRecAnn : ∀ {Φ ctx schemes bindings body Φ₁ Φ₂ S₁ S₂ bindingsOut bodyOut τ₂}
      (hwf : ∀ σ ∈ schemes, σ.WF) (hlen : bindings.length = schemes.length)
      (hgroup : InferRecGroupAnn Φ { ctx with env := schemes ++ ctx.env }
        bindings schemes Φ₁ S₁ bindingsOut)
      (hbody : Infer Φ₁ (S₁.onCtx { ctx with env := schemes ++ ctx.env })
        body Φ₂ S₂ bodyOut τ₂),
      motiveGrpAnn Φ { ctx with env := schemes ++ ctx.env }
          bindings schemes Φ₁ S₁ bindingsOut hgroup →
        motive Φ₁ (S₁.onCtx { ctx with env := schemes ++ ctx.env })
            body Φ₂ S₂ bodyOut τ₂ hbody →
          motive Φ ctx (.letRecAnn schemes bindings body) Φ₂ (S₁ ++ S₂)
            (.letRecAnn schemes bindingsOut bodyOut) τ₂ (.letRecAnn hwf hlen hgroup hbody))
    (brNil : ∀ {Φ ctx scrutTy ρ}, motiveBr Φ ctx scrutTy ρ [] Φ [] [] .nil)
    (brCons : ∀ {Φ ctx scrutTy ρ c n body rest ctorr Φ₁ Φ₂ S₀ S₁ S₂ S₃ bodyOut restOut τb}
      (hlook : LookupList.get? ctx.ctors c = some ctorr) (hn : n = ctorr.contents.length)
      (huni0 : UnifyRel scrutTy
        (.customTy ctorr.tyName ((freshVars Φ ctorr.paramCount).map (Ty.fvar ·))) S₀)
      (hbody : Infer (Φ + ctorr.paramCount)
        { (S₀.onCtx ctx) with env := (ctorr.contents.map (Ty.openWith
            (((freshVars Φ ctorr.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
          ++ (S₀.onCtx ctx).env } body Φ₁ S₁ bodyOut τb)
      (huni : UnifyRel τb (S₁.onTy (S₀.onTy ρ)) S₂)
      (hrest : InferBranches Φ₁ (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx)))
        (S₂.onTy (S₁.onTy (S₀.onTy scrutTy))) (S₂.onTy (S₁.onTy (S₀.onTy ρ))) rest Φ₂ S₃ restOut),
      motive (Φ + ctorr.paramCount)
          { (S₀.onCtx ctx) with env := (ctorr.contents.map (Ty.openWith
              (((freshVars Φ ctorr.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
            ++ (S₀.onCtx ctx).env } body Φ₁ S₁ bodyOut τb hbody →
        motiveBr Φ₁ (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx)))
            (S₂.onTy (S₁.onTy (S₀.onTy scrutTy))) (S₂.onTy (S₁.onTy (S₀.onTy ρ))) rest Φ₂ S₃ restOut hrest →
          motiveBr Φ ctx scrutTy ρ ((.named c n, body) :: rest) Φ₂ (S₀ ++ S₁ ++ S₂ ++ S₃)
            ((.named c n, bodyOut) :: restOut) (.cons hlook hn huni0 hbody huni hrest))
    (brWild : ∀ {Φ ctx scrutTy ρ body rest Φ₁ Φ₂ S₁ S₂ S₃ bodyOut restOut τb}
      (hbody : Infer Φ ctx body Φ₁ S₁ bodyOut τb) (huni : UnifyRel τb (S₁.onTy ρ) S₂)
      (hrest : InferBranches Φ₁ (S₂.onCtx (S₁.onCtx ctx)) (S₂.onTy (S₁.onTy scrutTy))
        (S₂.onTy (S₁.onTy ρ)) rest Φ₂ S₃ restOut),
      motive Φ ctx body Φ₁ S₁ bodyOut τb hbody →
        motiveBr Φ₁ (S₂.onCtx (S₁.onCtx ctx)) (S₂.onTy (S₁.onTy scrutTy)) (S₂.onTy (S₁.onTy ρ))
            rest Φ₂ S₃ restOut hrest →
          motiveBr Φ ctx scrutTy ρ ((.wildcard, body) :: rest) Φ₂ (S₁ ++ S₂ ++ S₃)
            ((.wildcard, bodyOut) :: restOut) (.consWild hbody huni hrest))
    (grpNil : ∀ {Φ ctx}, motiveGrp Φ ctx [] [] Φ [] [] .nil)
    (grpCons : ∀ {Φ ctx e rest β βs Φ₁ Φ₂ S₁ S₂ S₃ eOut restOut τ}
      (he : Infer Φ ctx e Φ₁ S₁ eOut τ) (huni : UnifyRel τ (S₁.onTy β) S₂)
      (hrest : InferRecGroup Φ₁ (S₂.onCtx (S₁.onCtx ctx)) rest
        (βs.map (fun b => S₂.onTy (S₁.onTy b))) Φ₂ S₃ restOut),
      motive Φ ctx e Φ₁ S₁ eOut τ he →
        motiveGrp Φ₁ (S₂.onCtx (S₁.onCtx ctx)) rest (βs.map (fun b => S₂.onTy (S₁.onTy b))) Φ₂ S₃ restOut hrest →
          motiveGrp Φ ctx (e :: rest) (β :: βs) Φ₂ (S₁ ++ S₂ ++ S₃) (eOut :: restOut)
            (.cons he huni hrest))
    (grpAnnNil : ∀ {Φ ctx}, motiveGrpAnn Φ ctx [] [] Φ [] [] .nil)
    (grpAnnCons : ∀ {Φ N ctx σ schemes e rest Φ₁ Φ₂ S₁ Schk S₂ eOut restOut τ}
      (hΦN : Φ ≤ N)
      (he : Infer (N + σ.paramCount) ctx (e.openTyVars (freshVars N σ.paramCount)) Φ₁ S₁ eOut τ)
      (huni : UnifyRel τ (σ.openVars (freshVars N σ.paramCount)) Schk)
      (hesc1 : ∀ y ∈ freshVars N σ.paramCount, y ∉ (S₁ ++ Schk).map Prod.fst)
      (hesc2 : ∀ y ∈ freshVars N σ.paramCount, y ∉ (Schk.onCtx (S₁.onCtx ctx)).env.freeVars)
      (hrest : InferRecGroupAnn Φ₁ (Schk.onCtx (S₁.onCtx ctx)) rest schemes Φ₂ S₂ restOut),
      motive (N + σ.paramCount) ctx (e.openTyVars (freshVars N σ.paramCount)) Φ₁ S₁ eOut τ he →
        motiveGrpAnn Φ₁ (Schk.onCtx (S₁.onCtx ctx)) rest schemes Φ₂ S₂ restOut hrest →
          motiveGrpAnn Φ ctx (e :: rest) (σ :: schemes) Φ₂ (S₁ ++ Schk ++ S₂)
            ((eOut.substTyFvars (S₁ ++ Schk)).closeTyVars (freshVars N σ.paramCount) :: restOut)
            (.cons hΦN he huni hesc1 hesc2 hrest))
    {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ) : motive Φ ctx e Φ' S eOut τ h :=
  Infer.rec (motive_1 := motive) (motive_2 := motiveBr) (motive_3 := motiveGrp)
    (motive_4 := motiveGrpAnn)
    primLitUnit primLitInt primLitNat primLitStr lambda app var ctor letIn letInAnn
    match_ letRec letRecAnn brNil brCons brWild grpNil grpCons grpAnnNil grpAnnCons h

/-! **Backward soundness against the source skeleton (C2).** `Infer` on source `e`
    yields a declarative `TypeOfHM` typing of `e` *itself* (not the elaborated
    output) under the solved context `S.onCtx ctx`. Mirrors `Infer.sound`'s
    hypotheses but is simpler (source terms are un-elaborated — no close-back /
    `letRecElab`). The `var` case uses `TypeOfHM.var`'s existential instantiation
    witness `(freshVars Φ pc).map (Ty.fvar ·)`.

    NOTE: the cofinite/recursive arms (`lambda`/`app`/`letIn`/`letInAnn`/`match_`/
    `letRec`/`letRecAnn`) mirror the corresponding (substantial) `Infer.sound` arms
    and are currently `sorry`-scaffolded pending the backward-soundness port. -/
theorem Infer.sourceSound {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ) :
    CtxWF ctx → CtxBelow Φ ctx → (K : List Nat) → (∀ k ∈ K, k < Φ) →
    (∀ y ∈ e.tyFreeVars, y ∈ K) → (∀ p ∈ S, p.1 ∉ K) →
    TypeOfHM (S.onCtx ctx) e τ := by
  induction h using Infer.rec_strong
    (motiveBr := fun Φb ctxb scrutTy ρ brs _ Sb _ _ =>
      CtxWF ctxb → CtxBelow Φb ctxb → scrutTy.IsLC → ρ.IsLC →
      Ty.BelowFvars Φb scrutTy → Ty.BelowFvars Φb ρ → ∀ (K : List Nat), (∀ k ∈ K, k < Φb) →
      (∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars brs, y ∈ K) → (∀ p ∈ Sb, p.1 ∉ K) →
      ∀ p ∈ brs, TypeOfMatchBranch (Sb.onCtx ctxb) p (Sb.onTy scrutTy) (Sb.onTy ρ))
    (motiveGrp := fun Φb ctxb bindings targets _ Sb _ _ =>
      CtxWF ctxb → CtxBelow Φb ctxb → (∀ τ ∈ targets, τ.IsLC) →
      (∀ τ ∈ targets, Ty.BelowFvars Φb τ) → ∀ (K : List Nat), (∀ k ∈ K, k < Φb) →
      (∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K) → (∀ p ∈ Sb, p.1 ∉ K) →
      ∀ p ∈ bindings.zip (targets.map Sb.onTy), TypeOfHM (Sb.onCtx ctxb) p.1 p.2)
    (motiveGrpAnn := fun Φb ctxb bindings schemes _ Sb _ _ =>
      CtxWF ctxb → CtxBelow Φb ctxb → (∀ σ ∈ schemes, σ.WF) →
      ∀ (K : List Nat), (∀ k ∈ K, k < Φb) →
      (∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K) →
      (∀ σ ∈ schemes, ∀ y ∈ σ.body.freeVars, y ∈ K) → (∀ p ∈ Sb, p.1 ∉ K) →
      ∃ L : List Nat, ∀ p ∈ bindings.zip schemes, ∀ Xs, FreshNames L p.2.paramCount Xs →
        TypeOfHM (Sb.onCtx ctxb) (p.1.openTyVars Xs) (p.2.openVars Xs))
    with
  | primLitUnit => intro _ _ _ _ _ _; simp only [Subst.onCtx_nil]; exact .primLitUnit
  | primLitInt => intro _ _ _ _ _ _; simp only [Subst.onCtx_nil]; exact .primLitInt
  | primLitNat => intro _ _ _ _ _ _; simp only [Subst.onCtx_nil]; exact .primLitNat
  | primLitStr => intro _ _ _ _ _ _; simp only [Subst.onCtx_nil]; exact .primLitStr
  | var hlook =>
    intro hctx _ _ _ _ _
    simp only [Subst.onCtx_nil]
    refine TypeOfHM.var hlook (fun tyArg ht => ?_)
      (InstantiatesBy.openVars (hctx _ (List.mem_of_getElem? hlook)) (by simp [freshVars_length]))
    obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht
    exact .fvar
  | ctor hlook =>
    intro _ _ _ _ _ _
    simp only [Subst.onCtx_nil]
    refine TypeOfHM.ctor hlook (fun tyArg ht => ?_)
      (InstantiatesBy.openVars (Ctor.toTy_wf _) (by simp [Ctor.toTy]))
    obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht
    exact .fvar
  | lambda hseed hbody ihbody =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    cases hseed
    case none =>
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append] at hKe
      have hbodyWF : CtxWF { ctx_1 with env := PolyTy.mkTrivial (.fvar Φ_1) :: ctx_1.env } := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact ContainsBvarsUpTo.fvar
        · exact hctx M hM
      have hbodyBelow : CtxBelow (Φ_1 + 1) { ctx_1 with env := PolyTy.mkTrivial (.fvar Φ_1) :: ctx_1.env } := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact .fvar (by omega)
        · exact (hbelow M hM).mono (by omega)
      have hbodyTy := ihbody hbodyWF hbodyBelow K (fun k hk => by have := hKΦ k hk; omega) hKe hSK
      refine TypeOfHM.lambda
        (Subst.onTy_lc (Infer.lc hbody hbodyWF).2 ContainsBvarsUpTo.fvar)
        (fun T hT => absurd hT (by simp)) ?_ hbodyTy
      simp only [Subst.onCtx, Subst.onEnv, List.map_cons, Subst.onPolyTy, PolyTy.mkTrivial]
    case some hcl =>
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at hKe
      have hbodyWF : CtxWF { ctx_1 with env := PolyTy.mkTrivial paramTy :: ctx_1.env } := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact hcl
        · exact hctx M hM
      have hbodyBelow : CtxBelow Φ_1 { ctx_1 with env := PolyTy.mkTrivial paramTy :: ctx_1.env } := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hKe v (.inl hv)))
        · exact hbelow M hM
      have hbodyTy := ihbody hbodyWF hbodyBelow K (fun k hk => by have := hKΦ k hk; omega)
        (fun y hy => hKe y (.inr hy)) hSK
      refine TypeOfHM.lambda
        (Subst.onTy_lc (Infer.lc hbody hbodyWF).2 hcl)
        (fun T hT => by
          have h1 : paramTy = T := Option.some.inj hT
          have h2 : S_1.onTy paramTy = paramTy :=
            Ty.substFvars_eq_self_of_no_key (fun p hp hc => hSK p hp (hKe p.1 (.inl hc)))
          rw [h2, h1]) ?_ hbodyTy
      simp only [Subst.onCtx, Subst.onEnv, List.map_cons, Subst.onPolyTy, PolyTy.mkTrivial]
  | app hf harg huni ihf iharg =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at hKe
    obtain ⟨hf_lc, hf_s⟩ := Infer.lc hf hctx
    have hctx1 := Subst.onCtx_wf hf_s hctx
    obtain ⟨harg_lc, harg_s⟩ := Infer.lc harg hctx1
    have hf_below := Infer.belowFvars hf hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))
    have hbelow1 := Subst.onCtx_below hf_below.2 (Infer.frontier_le hf) hbelow
    have hs3 := huni.lc (Subst.onTy_lc harg_s hf_lc) (.arrow harg_lc ContainsBvarsUpTo.fvar)
    have hf_sound := ihf hctx hbelow K hKΦ (fun y hy => hKe y (.inl hy))
      (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ hp)))
    have harg_sound := iharg hctx1 hbelow1 K
      (fun k hk => lt_of_lt_of_le (hKΦ k hk) (Infer.frontier_le hf))
      (fun y hy => hKe y (.inr hy))
      (fun p hp => hSK p (List.mem_append_left _ (List.mem_append_right _ hp)))
    have hueq := huni.unifies
    simp only [Unifies, Subst.onTy_arrow] at hueq
    have hffix : (f.substTyFvars S₂).substTyFvars S₃ = f := by
      rw [← Expr.substTyFvars_append]
      refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => ?_)
      rcases List.mem_append.mp hp with h | h
      · exact hSK p (List.mem_append_left _ (List.mem_append_right _ h)) (hKe p.1 (.inl hc))
      · exact hSK p (List.mem_append_right _ h) (hKe p.1 (.inl hc))
    have hargfix : arg.substTyFvars S₃ = arg :=
      Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc =>
        hSK p (List.mem_append_right _ hp) (hKe p.1 (.inr hc)))
    have f2 := TypeOfHM.onSubst S₃ hs3 (TypeOfHM.onSubst S₂ harg_s hf_sound)
    rw [hffix, hueq] at f2
    have a1 := TypeOfHM.onSubst S₃ hs3 harg_sound
    rw [hargfix] at a1
    rw [Subst.onCtx_append, Subst.onCtx_append]
    exact .app f2 a1
  | letIn hrhs hbody ihrhs ihbody =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append] at hKe
    obtain ⟨hrhs_lc, hrhs_s⟩ := Infer.lc hrhs hctx
    have hrhs_below := Infer.belowFvars hrhs hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))
    have hbelow1 := Subst.onCtx_below hrhs_below.2 (Infer.frontier_le hrhs) hbelow
    have helimR := Infer.eliminates hrhs hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))
      (fun p hp hc => hSK p (List.mem_append_left _ hp) (hKe p.1 (.inl hc)))
    have hS₁τ₁ : S₁.onTy τ₁ = τ₁ := Ty.substFvars_eq_self_of_no_key (fun p hp => helimR.2 p hp)
    have hbodyWF : CtxWF { (S₁.onCtx ctx_1) with
        env := genScheme rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁ :: (S₁.onCtx ctx_1).env } := by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact genScheme_wf hrhs_lc
      · exact (Subst.onCtx_wf hrhs_s hctx) M hM
    have hbodyBelow : CtxBelow Φ₁ { (S₁.onCtx ctx_1) with
        env := genScheme rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁ :: (S₁.onCtx ctx_1).env } := by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact hrhs_below.1.closeOver
      · exact hbelow1 M hM
    have hbody_s := (Infer.lc hbody hbodyWF).2
    have hS₁S₂lc : ∀ p ∈ S₁ ++ S₂, p.2.IsLC :=
      fun p hp => (List.mem_append.mp hp).elim (fun h => hrhs_s p h) (fun h => hbody_s p h)
    have hgenV_τ₁ : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁, g ∈ τ₁.freeVars :=
      fun g hg => by simp only [genVars, List.mem_filter] at hg; exact hg.1
    have hgenV_env : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁,
        g ∉ (S₁.onCtx ctx_1).env.freeVars := fun g hg => genVars_not_mem hg
    have hgenV_lt : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁, g < Φ₁ :=
      fun g hg => hrhs_below.1.mem_lt g (hgenV_τ₁ g hg)
    have hgenV_ge : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁, Φ_1 ≤ g := by
      intro g hg
      by_contra hlt
      push_neg at hlt
      have hg_S₁dom : ∀ p ∈ S₁, p.1 ≠ g := fun p hp hpeq => helimR.2 p hp (hpeq ▸ hgenV_τ₁ g hg)
      have hg_ctxenv : ∀ M ∈ ctx_1.env, g ∉ M.body.freeVars := by
        intro M₀ hM₀ hgM
        exact hgenV_env g hg (Env.mem_freeVars_iff.mpr ⟨S₁.onPolyTy M₀,
          List.mem_map.mpr ⟨M₀, hM₀, rfl⟩, Ty.mem_freeVars_onTy_of_not_dom hgM hg_S₁dom⟩)
      exact (Infer.eOut_avoid hrhs (w := g) hlt hg_ctxenv (genVars_not_mem_rigid hg)).2.1
        (hgenV_τ₁ g hg)
    have hgenV_body : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁, g ∉ body.tyFreeVars :=
      fun g hg hc => by have := hKΦ g (hKe g (.inr hc)); have := hgenV_ge g hg; omega
    have hbodyCtxAvoid : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁,
        ∀ M ∈ (genScheme rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁ :: (S₁.onCtx ctx_1).env),
          g ∉ M.body.freeVars := by
      intro g hg M hM
      rcases List.mem_cons.mp hM with rfl | hM
      · exact Ty.not_mem_closeOver_freeVars hg
      · exact fun hc2 => hgenV_env g hg (Env.mem_freeVars_iff.mpr ⟨M, hM, hc2⟩)
    have hS₂genV : ∀ p ∈ S₂, p.1 ∉ genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁ := by
      intro p hp hc
      exact Infer.dom_avoid hbody (hgenV_lt p.1 hc) (hbodyCtxAvoid p.1 hc)
        (fun hc2 => hSK p (List.mem_append_right _ hp) (hKe p.1 (.inr hc2)))
        (List.mem_map.mpr ⟨p, hp, rfl⟩)
    have hS₂genVran : ∀ p ∈ S₂, ∀ u ∈ p.2.freeVars,
        u ∉ genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁ := by
      intro p hp u hu hc
      exact (Infer.eOut_avoid hbody (w := u) (hgenV_lt u hc) (hbodyCtxAvoid u hc)
        (hgenV_body u hc)).1 p hp hu
    have hSτ₁ : (S₁ ++ S₂).onTy τ₁ = S₂.onTy τ₁ := by rw [Subst.onTy_append, hS₁τ₁]
    have hschemebody : S₂.onTy (Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁) τ₁)
        = Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁) ((S₁ ++ S₂).onTy τ₁) := by
      have h1 : S₂.onTy (Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁) τ₁)
          = Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁) (S₂.onTy τ₁) :=
        Ty.substFvars_closeOver hS₂genV hS₂genVran
      rw [h1, ← hSτ₁]
    have hgenV_env' : ∀ g ∈ genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁,
        g ∉ ((S₁ ++ S₂).onCtx ctx_1).env.freeVars := by
      intro g hg hc
      rw [Subst.onCtx_append, Env.mem_freeVars_iff] at hc
      obtain ⟨M, hM, hgM⟩ := hc
      simp only [Subst.onCtx, Subst.onEnv] at hM
      obtain ⟨M₀, hM₀, rfl⟩ := List.mem_map.mp hM
      exact Subst.notMemOnTy (fun p hp hgp => hS₂genVran p hp g hgp hg)
        (fun hc2 => hgenV_env g hg (Env.mem_freeVars_iff.mpr ⟨M₀, hM₀, hc2⟩)) hgM
    have heqbodyctx : Subst.onCtx S₂ { (S₁.onCtx ctx_1) with
          env := genScheme rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁ :: (S₁.onCtx ctx_1).env }
        = { (S₁ ++ S₂).onCtx ctx_1 with
          env := Subst.onPolyTy S₂ (genScheme rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁)
            :: ((S₁ ++ S₂).onCtx ctx_1).env } := by
      rw [Subst.onCtx_append]
      simp only [Subst.onCtx, Subst.onEnv, List.map_cons]
    -- source typings
    have hrhs_sound := ihrhs hctx hbelow K hKΦ (fun y hy => hKe y (.inl hy))
      (fun p hp => hSK p (List.mem_append_left _ hp))
    have hr2 := TypeOfHM.onSubst S₂ hbody_s hrhs_sound
    have hrhsfixS2 : rhs.substTyFvars S₂ = rhs :=
      Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars
        (fun p hp hc => hSK p (List.mem_append_right _ hp) (hKe p.1 (.inl hc)))
    rw [hrhsfixS2, ← Subst.onCtx_append] at hr2
    have hbase : TypeOfHM ((S₁ ++ S₂).onCtx ctx_1) rhs ((S₁ ++ S₂).onTy τ₁) := by rw [hSτ₁]; exact hr2
    have hbody_sound := ihbody hbodyWF hbodyBelow K
      (fun k hk => lt_of_lt_of_le (hKΦ k hk) (Infer.frontier_le hrhs))
      (fun y hy => hKe y (.inr hy)) (fun p hp => hSK p (List.mem_append_right _ hp))
    rw [heqbodyctx] at hbody_sound
    refine TypeOfHM.letIn (M := Subst.onPolyTy S₂ (genScheme rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁))
      (L := genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁)
      (Subst.onPolyTy_wf hbody_s (genScheme_wf hrhs_lc)) (fun σ h => by simp at h) ?_ rfl hbody_sound
    intro Xs hXfresh
    show TypeOfHM ((S₁ ++ S₂).onCtx ctx_1) rhs
      ((Subst.onPolyTy S₂ (genScheme rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁)).openVars Xs)
    have hMbody : (Subst.onPolyTy S₂ (genScheme rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁)).openVars Xs
        = Ty.substFvars ((genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁).zip (Xs.map (Ty.fvar ·)))
            ((S₁ ++ S₂).onTy τ₁) := by
      simp only [PolyTy.openVars, Subst.onPolyTy, genScheme]
      rw [hschemebody]
      exact Ty.openVars_closeOver_rename (Subst.onTy_lc hS₁S₂lc hrhs_lc) genVars_nodup
        hXfresh.length (fun g hg hgX => hXfresh.avoid g hgX hg)
    rw [hMbody]
    have hren := TypeOfHM.onSubst ((genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁).zip (Xs.map (Ty.fvar ·)))
      (fun p hp => by
        obtain ⟨_, hp2⟩ := List.of_mem_zip hp
        obtain ⟨x, _, hxeq⟩ := List.mem_map.mp hp2
        rw [← hxeq]; exact ContainsBvarsUpTo.fvar) hbase
    have hctxfix : Subst.onCtx ((genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁).zip (Xs.map (Ty.fvar ·)))
        ((S₁ ++ S₂).onCtx ctx_1) = (S₁ ++ S₂).onCtx ctx_1 := by
      conv_lhs => rw [Subst.onCtx]
      rw [Subst.onEnv_eq_self_of_fresh (fun p hp hc => hgenV_env' p.1 (List.of_mem_zip hp).1 hc)]
    have hrhsfixRen : rhs.substTyFvars
        ((genVars rhs.tyFreeVars (S₁.onCtx ctx_1).env τ₁).zip (Xs.map (Ty.fvar ·))) = rhs :=
      Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => by
        have h1 := hgenV_ge p.1 (List.of_mem_zip hp).1
        have h2 := hKΦ p.1 (hKe p.1 (.inl hc))
        omega)
    rw [hctxfix, hrhsfixRen] at hren
    exact hren
  | letInAnn hσwf hΦN hrhs huni hesc1 hesc2 hbody ihrhs ihbody =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at hKe
    have hrle := Infer.frontier_le hrhs
    have hctx_pc : CtxBelow (N + σ.paramCount) ctx_1 := fun M hM => (hbelow M hM).mono (by omega)
    have hσbody : Ty.BelowFvars Φ_1 σ.body :=
      Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hKe v (.inl (.inl hv))))
    obtain ⟨hrhs_lc, hrhs_s⟩ := Infer.lc hrhs hctx
    have hΦ_rhs : ∀ y ∈ (rhs.openTyVars (freshVars N σ.paramCount)).tyFreeVars, y < N + σ.paramCount := by
      intro y hy
      rcases Expr.tyFreeVars_openTyVars hy with h | h
      · have := hKΦ y (hKe y (.inl (.inr h))); have := hΦN; omega
      · have := freshVars_lt y h; omega
    have hSe_rhs : ∀ p ∈ S₁, p.1 ∉ (rhs.openTyVars (freshVars N σ.paramCount)).tyFreeVars := by
      intro p hp hc
      rcases Expr.tyFreeVars_openTyVars hc with h | h
      · exact hSK p (List.mem_append_left _ (List.mem_append_left _ hp)) (hKe p.1 (.inl (.inr h)))
      · exact hesc1 p.1 h (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩)
    obtain ⟨hr_τ, hr_s⟩ := Infer.belowFvars hrhs hctx_pc hΦ_rhs
    have hσopen : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
      Ty.openVars_belowFvars (hσbody.mono (by omega)) (fun x hx => by have := freshVars_lt x hx; omega)
    have hSchk_below : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hr_τ hσopen
    have hSchk_lc : ∀ p ∈ Schk, p.2.IsLC :=
      UnifyRel.lc huni hrhs_lc (PolyTy.openVars_isLC hσwf (by simp))
    have hbodyWF : CtxWF { (Schk.onCtx (S₁.onCtx ctx_1)) with
        env := σ :: (Schk.onCtx (S₁.onCtx ctx_1)).env } := by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact hσwf
      · exact (Subst.onCtx_wf hSchk_lc (Subst.onCtx_wf hrhs_s hctx)) M hM
    have hbodyBelow : CtxBelow Φ₁ { (Schk.onCtx (S₁.onCtx ctx_1)) with
        env := σ :: (Schk.onCtx (S₁.onCtx ctx_1)).env } := by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact hσbody.mono (by omega)
      · exact (Subst.onCtx_below hSchk_below (le_refl _) (Subst.onCtx_below hr_s hrle hctx_pc)) M hM
    have hbody_s := (Infer.lc hbody hbodyWF).2
    have hYs_lt : ∀ y ∈ freshVars N σ.paramCount, y < Φ₁ :=
      fun y hy => by have := freshVars_lt y hy; have := hrle; omega
    have hYs_bodyCtx : ∀ y ∈ freshVars N σ.paramCount,
        ∀ M ∈ (σ :: (Schk.onCtx (S₁.onCtx ctx_1)).env), y ∉ M.body.freeVars := by
      intro y hy M hM
      rcases List.mem_cons.mp hM with rfl | hM
      · exact fun hc => by have := hKΦ y (hKe y (.inl (.inl hc))); have := freshVars_ge y hy; omega
      · exact fun hc => hesc2 y hy (Env.mem_freeVars_iff.mpr ⟨M, hM, hc⟩)
    have hYs_body : ∀ y ∈ freshVars N σ.paramCount, y ∉ body.tyFreeVars :=
      fun y hy hc => by have := hKΦ y (hKe y (.inr hc)); have := freshVars_ge y hy; omega
    have hS₂Ys : ∀ p ∈ S₂, p.1 ∉ freshVars N σ.paramCount := by
      intro p hp hc
      exact Infer.dom_avoid hbody (hYs_lt p.1 hc) (hYs_bodyCtx p.1 hc) (hYs_body p.1 hc)
        (List.mem_map.mpr ⟨p, hp, rfl⟩)
    have hS₂Ysran : ∀ p ∈ S₂, ∀ u ∈ p.2.freeVars, u ∉ freshVars N σ.paramCount := by
      intro p hp u hu hc
      exact (Infer.eOut_avoid hbody (w := u) (hYs_lt u hc) (hYs_bodyCtx u hc) (hYs_body u hc)).1 p hp hu
    have hSYs : ∀ p ∈ S₁ ++ Schk ++ S₂, p.1 ∉ freshVars N σ.paramCount := by
      intro p hp
      rcases List.mem_append.mp hp with hp | hp
      · exact fun hc => hesc1 p.1 hc (List.mem_map.mpr ⟨p, hp, rfl⟩)
      · exact hS₂Ys p hp
    have hSσbody : (S₁ ++ Schk ++ S₂).onTy σ.body = σ.body :=
      Ty.substFvars_eq_self_of_no_key (fun p hp hc => hSK p hp (hKe p.1 (.inl (.inl hc))))
    have hSfix_σopen : ∀ p ∈ S₁ ++ Schk ++ S₂,
        p.1 ∉ (σ.openVars (freshVars N σ.paramCount)).freeVars := by
      intro p hp hc
      rcases Ty.freeVars_openVars_subset p.1 hc with h | h
      · exact hSK p hp (hKe p.1 (.inl (.inl h)))
      · exact hSYs p hp h
    have hSchk_fix : Schk.onTy (σ.openVars (freshVars N σ.paramCount))
        = σ.openVars (freshVars N σ.paramCount) :=
      Ty.substFvars_eq_self_of_no_key
        (fun p hp => hSfix_σopen p (List.mem_append_left _ (List.mem_append_right _ hp)))
    have hS₂fix : S₂.onTy (σ.openVars (freshVars N σ.paramCount))
        = σ.openVars (freshVars N σ.paramCount) :=
      Ty.substFvars_eq_self_of_no_key (fun p hp => hSfix_σopen p (List.mem_append_right _ hp))
    have hrhsfix : (rhs.openTyVars (freshVars N σ.paramCount)).substTyFvars (Schk ++ S₂)
        = rhs.openTyVars (freshVars N σ.paramCount) := by
      refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => ?_)
      have hpS : p ∈ S₁ ++ Schk ++ S₂ := by
        rcases List.mem_append.mp hp with h | h
        · exact List.mem_append_left _ (List.mem_append_right _ h)
        · exact List.mem_append_right _ h
      rcases Expr.tyFreeVars_openTyVars hc with h | h
      · exact hSK p hpS (hKe p.1 (.inl (.inr h)))
      · exact hSYs p hpS h
    have hrhs_sound := ihrhs hctx hctx_pc (K ++ freshVars N σ.paramCount)
      (fun k hk => by rcases List.mem_append.mp hk with h | h
                      · have := hKΦ k h; have := hΦN; omega
                      · have := freshVars_lt k h; omega)
      (fun y hy => by rcases Expr.tyFreeVars_openTyVars hy with h | h
                      · exact List.mem_append_left _ (hKe y (.inl (.inr h)))
                      · exact List.mem_append_right _ h)
      (fun p hp hc => by rcases List.mem_append.mp hc with h | h
                         · exact hSK p (List.mem_append_left _ (List.mem_append_left _ hp)) h
                         · exact hesc1 p.1 h (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩))
    have hr1 := TypeOfHM.onSubst Schk hSchk_lc hrhs_sound
    rw [← Subst.onCtx_append] at hr1
    have hr2 := TypeOfHM.onSubst S₂ hbody_s hr1
    rw [← Subst.onCtx_append] at hr2
    have hu := huni.unifies
    simp only [Unifies] at hu
    rw [show (((rhs.openTyVars (freshVars N σ.paramCount)).substTyFvars Schk).substTyFvars S₂)
          = rhs.openTyVars (freshVars N σ.paramCount) from by
            rw [← Expr.substTyFvars_append]; exact hrhsfix,
        hu, hSchk_fix, hS₂fix] at hr2
    -- hr2 : TypeOfHM ((S₁++Schk++S₂).onCtx ctx_1) (rhs.openTyVars Ys) (σ.openVars Ys)
    have hYs_env : ∀ y ∈ freshVars N σ.paramCount,
        y ∉ ((S₁ ++ Schk ++ S₂).onCtx ctx_1).env.freeVars := by
      intro y hy hc
      rw [show (S₁ ++ Schk ++ S₂).onCtx ctx_1 = S₂.onCtx ((S₁ ++ Schk).onCtx ctx_1) from by
            rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Subst.onCtx_append],
          Env.mem_freeVars_iff] at hc
      obtain ⟨M, hM, hyM⟩ := hc
      rw [show (S₂.onCtx ((S₁ ++ Schk).onCtx ctx_1)).env
            = ((S₁ ++ Schk).onCtx ctx_1).env.map (Subst.onPolyTy S₂) from rfl, List.mem_map] at hM
      obtain ⟨M₀, hM₀, rfl⟩ := hM
      refine Subst.notMemOnTy (fun p hp hyp => hS₂Ysran p hp y hyp hy) (fun hc2 => ?_) hyM
      rw [Subst.onCtx_append] at hM₀
      exact hesc2 y hy (Env.mem_freeVars_iff.mpr ⟨M₀, hM₀, hc2⟩)
    -- body typing
    have hS₂σ : Subst.onPolyTy S₂ σ = σ := by
      simp only [Subst.onPolyTy]
      rw [show S₂.onTy σ.body = σ.body from
            Ty.substFvars_eq_self_of_no_key (fun p hp hc =>
              hSK p (List.mem_append_right _ hp) (hKe p.1 (.inl (.inl hc))))]
    have heqbodyctx : Subst.onCtx S₂ { (Schk.onCtx (S₁.onCtx ctx_1)) with
          env := σ :: (Schk.onCtx (S₁.onCtx ctx_1)).env }
        = { (S₁ ++ Schk ++ S₂).onCtx ctx_1 with
          env := σ :: ((S₁ ++ Schk ++ S₂).onCtx ctx_1).env } := by
      rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Subst.onCtx_append, Subst.onCtx_append]
      simp only [Subst.onCtx, Subst.onEnv, List.map_cons]
      rw [show Subst.onPolyTy S₂ σ = σ from hS₂σ]
    have hbody_sound := ihbody hbodyWF hbodyBelow K
      (fun k hk => by have := hKΦ k hk; have := hΦN; have := hrle; omega)
      (fun y hy => hKe y (.inr hy)) (fun p hp => hSK p (List.mem_append_right _ hp))
    rw [heqbodyctx] at hbody_sound
    refine TypeOfHM.letIn (M := σ) (L := freshVars N σ.paramCount)
      hσwf (fun σ' h => Option.some.inj h) ?_ rfl hbody_sound
    intro Xs hXfresh
    show TypeOfHM ((S₁ ++ Schk ++ S₂).onCtx ctx_1) (rhs.openTyVars Xs) (σ.openVars Xs)
    have hYsX : ∀ y ∈ freshVars N σ.paramCount, y ∉ Xs :=
      fun y hy hc => hXfresh.avoid y hc hy
    have hXlen : Xs.length = (freshVars N σ.paramCount).length := by
      rw [hXfresh.length, freshVars_length]
    have htermeq : (rhs.openTyVars (freshVars N σ.paramCount)).substTyFvars
          ((freshVars N σ.paramCount).zip (Xs.map (Ty.fvar ·))) = rhs.openTyVars Xs :=
      Expr.substTyFvars_zip_openTyVars hXlen.symm freshVars_nodup
        (fun y hy hc => by have := hKΦ y (hKe y (.inl (.inr hc))); have := freshVars_ge y hy; omega)
        hYsX
    have htypeeq : Subst.onTy ((freshVars N σ.paramCount).zip (Xs.map (Ty.fvar ·)))
          (σ.openVars (freshVars N σ.paramCount)) = σ.openVars Xs := by
      have h := Ty.openWith_eq_substFvars_openVars (ty := σ.body) (Vs := Xs.map (Ty.fvar ·))
        (Xs := freshVars N σ.paramCount)
        ⟨by rw [List.length_map, hXlen], fun V hV => by
              obtain ⟨x, _, rfl⟩ := List.mem_map.mp hV; exact ContainsBvarsUpTo.fvar⟩
        freshVars_nodup
        (fun y hy hc => by have := hKΦ y (hKe y (.inl (.inl hc))); have := freshVars_ge y hy; omega)
        (fun y hy hc => hYsX y hy (Ty.mem_freeVarsList_map_fvar.mp hc))
      rw [← Ty.openVars_eq_openWith] at h
      exact h.symm
    have hren := TypeOfHM.onSubst ((freshVars N σ.paramCount).zip (Xs.map (Ty.fvar ·)))
      (fun s hs => by
        obtain ⟨_, hs2⟩ := List.of_mem_zip hs
        obtain ⟨x, _, hxeq⟩ := List.mem_map.mp hs2
        rw [← hxeq]; exact ContainsBvarsUpTo.fvar) hr2
    have hctxfix : Subst.onCtx ((freshVars N σ.paramCount).zip (Xs.map (Ty.fvar ·)))
          ((S₁ ++ Schk ++ S₂).onCtx ctx_1) = (S₁ ++ Schk ++ S₂).onCtx ctx_1 := by
      conv_lhs => rw [Subst.onCtx]
      rw [Subst.onEnv_eq_self_of_fresh (fun s hs hc => hYs_env s.1 (List.of_mem_zip hs).1 hc)]
    rw [htermeq, htypeeq, hctxfix] at hren
    exact hren
  | match_ hscrut hne hbranches ihscrut ihbranches =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at hKe
    obtain ⟨hτs_lc, hS₁⟩ := Infer.lc hscrut hctx
    have hle1 := Infer.frontier_le hscrut
    have hscrut_below := Infer.belowFvars hscrut hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))
    have hctx1 := Subst.onCtx_wf hS₁ hctx
    have hbelow1 := Subst.onCtx_below hscrut_below.2 hle1 hbelow
    have hS₂lc := (InferBranches.lc hbranches hctx1 hτs_lc ContainsBvarsUpTo.fvar).2
    have hscrut_sound := ihscrut hctx hbelow K hKΦ (fun y hy => hKe y (.inl hy))
      (fun p hp => hSK p (List.mem_append_left _ hp))
    have hscrut_decl := TypeOfHM.onSubst S₂ hS₂lc hscrut_sound
    have hscrutfix : scrut.substTyFvars S₂ = scrut :=
      Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars
        (fun p hp hc => hSK p (List.mem_append_right _ hp) (hKe p.1 (.inl hc)))
    rw [hscrutfix] at hscrut_decl
    have hbr_sound := ihbranches hctx1 (fun M hM => (hbelow1 M hM).mono (by omega))
      hτs_lc ContainsBvarsUpTo.fvar
      (hscrut_below.1.mono (by omega)) (.fvar (by omega)) K
      (fun k hk => by have := hKΦ k hk; omega)
      (fun y hy => hKe y (.inr hy)) (fun p hp => hSK p (List.mem_append_right _ hp))
    rw [Subst.onCtx_append]
    exact TypeOfHM.match_ hscrut_decl hne hbr_sound
  | letRec hgroup hbody ihgroup ihbody =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at hKe
    obtain ⟨τs, hτs⟩ :
        ∃ t, t = (freshVars Φ_1 bindings.length).map (fun i => S₁.onTy (Ty.fvar i)) := ⟨_, rfl⟩
    obtain ⟨G, hG⟩ :
        ∃ g, g = genGroupVars (bindings.flatMap Expr.tyFreeVars) (Subst.onCtx S₁ ctx_1).env τs := ⟨_, rfl⟩
    rw [← hτs] at hbody ihbody
    have hctxβ : CtxWF { ctx_1 with env :=
        (freshVars Φ_1 bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i)) ++ ctx_1.env } := by
      intro M hM
      rcases List.mem_append.mp hM with hM | hM
      · obtain ⟨x, _, rfl⟩ := List.mem_map.mp hM; exact ContainsBvarsUpTo.fvar
      · exact hctx M hM
    have hctxβbelow : CtxBelow (Φ_1 + bindings.length)
        { ctx_1 with env := (freshVars Φ_1 bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i)) ++ ctx_1.env } := by
      intro M hM
      rcases List.mem_append.mp hM with hM | hM
      · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
        exact Ty.BelowFvars.of_freeVars_lt (fun v hv => by
          simp only [PolyTy.mkTrivial, Ty.freeVars, List.mem_singleton] at hv
          have := freshVars_lt x hx; omega)
      · exact (hbelow M hM).mono (by omega)
    have hS₁lc : ∀ p ∈ S₁, p.2.IsLC :=
      InferRecGroup.lc hgroup hctxβ
        (fun τ hτ => by obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ; exact ContainsBvarsUpTo.fvar)
    have hτs_lc : ∀ τ ∈ τs, τ.IsLC := by
      intro τ hτ; rw [hτs] at hτ
      obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ
      exact Subst.onTy_lc hS₁lc ContainsBvarsUpTo.fvar
    have hgrle := InferRecGroup.frontier_le hgroup
    have hgroupSound := ihgroup hctxβ hctxβbelow
      (fun τ hτ => by obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ; exact ContainsBvarsUpTo.fvar)
      (fun τ hτ => by obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
                      exact Ty.BelowFvars.of_freeVars_lt (fun v hv => by
                        simp only [Ty.freeVars, List.mem_singleton] at hv
                        have := freshVars_lt x hx; omega))
      K (fun k hk => by have := hKΦ k hk; omega)
      (fun y hy => hKe y (.inl hy)) (fun p hp => hSK p (List.mem_append_left _ hp))
    have ha : ((freshVars Φ_1 bindings.length).map Ty.fvar).map (Subst.onTy S₁) = τs := by
      rw [hτs, List.map_map]; rfl
    have hb : Subst.onCtx S₁ { ctx_1 with env :=
          (freshVars Φ_1 bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i)) ++ ctx_1.env }
        = ⟨τs.map PolyTy.mkTrivial ++ (Subst.onCtx S₁ ctx_1).env, ctx_1.ctors⟩ := by
      rw [hτs]
      simp only [Subst.onCtx, Subst.onEnv, List.map_append, List.map_map, Function.comp_def,
        Subst.onPolyTy, PolyTy.mkTrivial]
    rw [ha, hb] at hgroupSound
    have hbodyWF : CtxWF { (Subst.onCtx S₁ ctx_1) with
        env := genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (Subst.onCtx S₁ ctx_1).env τs
                 ++ (Subst.onCtx S₁ ctx_1).env } := by
      intro M hM
      rcases List.mem_append.mp hM with hM | hM
      · simp only [genGroupSchemes] at hM
        obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
        exact PolyTy.genGroup_wf (hτs_lc τ hτ)
      · exact (Subst.onCtx_wf hS₁lc hctx) M hM
    have hS₂lc : ∀ p ∈ S₂, p.2.IsLC := (Infer.lc hbody hbodyWF).2
    have hG_nodup : G.Nodup := by rw [hG]; exact genGroupVars_nodup
    have hG_τs : ∀ g ∈ G, g ∈ Ty.freeVarsList τs := fun g hg => (genGroupVars_spec (hG ▸ hg)).1
    have hG_envS₁ : ∀ g ∈ G, g ∉ (Subst.onCtx S₁ ctx_1).env.freeVars :=
      fun g hg => (genGroupVars_spec (hG ▸ hg)).2.1
    have hG_rigid : ∀ g ∈ G, g ∉ bindings.flatMap Expr.tyFreeVars :=
      fun g hg => (genGroupVars_spec (hG ▸ hg)).2.2
    have htg_below : ∀ β ∈ (freshVars Φ_1 bindings.length).map Ty.fvar,
        Ty.BelowFvars (Φ_1 + bindings.length) β :=
      fun β hβ => by
        obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hβ
        exact Ty.BelowFvars.of_freeVars_lt (fun v hv => by
          simp only [Ty.freeVars, List.mem_singleton] at hv; have := freshVars_lt x hx; omega)
    have htfv_below : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ_1 + bindings.length :=
      fun y hy => by have := hKΦ y (hKe y (.inl hy)); omega
    have hS₁below : ∀ p ∈ S₁, Ty.BelowFvars Φ₁ p.2 :=
      InferRecGroup.belowFvars hgroup hctxβbelow htg_below htfv_below
    have helimG : ∀ p ∈ S₁, ∀ x : Ty, p.1 ∉ (S₁.onTy x).freeVars :=
      InferRecGroup.eliminates hgroup hctxβbelow htg_below htfv_below
        (fun p hp hc => hSK p (List.mem_append_left _ hp) (hKe p.1 (.inl hc)))
    have hτs_below : ∀ τ ∈ τs, Ty.BelowFvars Φ₁ τ := by
      intro τ hτ; rw [hτs] at hτ
      obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
      exact Ty.BelowFvars.of_freeVars_lt (fun v hv => by
        rcases Subst.mem_freeVars_onTy hv with h | ⟨p, hp, hvp⟩
        · simp only [Ty.freeVars, List.mem_singleton] at h
          have := freshVars_lt x hx; omega
        · exact (hS₁below p hp).mem_lt v hvp)
    have hG_lt : ∀ g ∈ G, g < Φ₁ := fun g hg => by
      obtain ⟨τ, hτ, hgτ⟩ := Ty.mem_freeVarsList_exists (hG_τs g hg)
      exact (hτs_below τ hτ).mem_lt g hgτ
    have hG_S₁dom : ∀ g ∈ G, ∀ p ∈ S₁, p.1 ≠ g := by
      intro g hg p hp hpeq
      obtain ⟨τ, hτ, hgτ⟩ := Ty.mem_freeVarsList_exists (hG_τs g hg)
      rw [hτs] at hτ; obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ
      exact helimG p hp (Ty.fvar x) (hpeq ▸ hgτ)
    have hG_ctxenv : ∀ g ∈ G, ∀ M ∈ ctx_1.env, g ∉ M.body.freeVars := by
      intro g hg M hM hc
      exact hG_envS₁ g hg (by
        rw [Env.mem_freeVars_iff]
        exact ⟨S₁.onPolyTy M, List.mem_map.mpr ⟨M, hM, rfl⟩,
          Ty.mem_freeVars_onTy_of_not_dom hc (fun p hp hpeq => hG_S₁dom g hg p hp hpeq)⟩)
    have hG_ge : ∀ g ∈ G, Φ_1 ≤ g := by
      intro g hg
      by_contra hlt; push_neg at hlt
      obtain ⟨τ, hτ, hgτ⟩ := Ty.mem_freeVarsList_exists (hG_τs g hg)
      rw [hτs] at hτ; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
      have hgrange := (InferRecGroup.eOut_avoid hgroup (w := g) (by omega)
        (by intro M hM
            rcases List.mem_append.mp hM with hM | hM
            · obtain ⟨y, hy, rfl⟩ := List.mem_map.mp hM
              simp only [PolyTy.mkTrivial, Ty.freeVars, List.mem_singleton]
              intro h; have := freshVars_ge y hy; omega
            · exact hG_ctxenv g hg M hM)
        (by intro β hβ; obtain ⟨y, hy, rfl⟩ := List.mem_map.mp hβ
            simp only [Ty.freeVars, List.mem_singleton]
            intro h; have := freshVars_ge y hy; omega)
        (fun hc => hG_rigid g hg (Expr.mem_recGroup_tyFreeVars.mp hc))).1
      rcases Subst.mem_freeVars_onTy hgτ with h | ⟨p, hp, hgp⟩
      · simp only [Ty.freeVars, List.mem_singleton] at h
        have := freshVars_ge x hx; omega
      · exact hgrange p hp hgp
    have hG_body : ∀ g ∈ G, g ∉ body.tyFreeVars :=
      fun g hg hc => by have := hKΦ g (hKe g (.inr hc)); have := hG_ge g hg; omega
    have hG_bodyCtx : ∀ g ∈ G, ∀ M ∈ (genGroupSchemes (bindings.flatMap Expr.tyFreeVars)
        (Subst.onCtx S₁ ctx_1).env τs ++ (Subst.onCtx S₁ ctx_1).env), g ∉ M.body.freeVars := by
      intro g hg M hM hc
      rcases List.mem_append.mp hM with hM | hM
      · simp only [genGroupSchemes] at hM
        obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
        simp only [PolyTy.genGroup] at hc
        rw [← hG] at hc
        by_cases hgτ : g ∈ τ.freeVars
        · exact Ty.not_mem_closeOver_freeVars
            (by simp only [Ty.genFilter, List.mem_filter, decide_eq_true_eq]; exact ⟨hg, hgτ⟩) hc
        · exact hgτ (Ty.freeVars_closeOver_subset hc)
      · exact hG_envS₁ g hg (Env.mem_freeVars_iff.mpr ⟨M, hM, hc⟩)
    have hS₂G : ∀ p ∈ S₂, p.1 ∉ G := fun p hp hc =>
      Infer.dom_avoid hbody (hG_lt p.1 hc) (hG_bodyCtx p.1 hc) (hG_body p.1 hc)
        (List.mem_map.mpr ⟨p, hp, rfl⟩)
    have hS₂Gran : ∀ p ∈ S₂, ∀ u ∈ p.2.freeVars, u ∉ G := fun p hp u hu hc =>
      (Infer.eOut_avoid hbody (w := u) (hG_lt u hc) (hG_bodyCtx u hc) (hG_body u hc)).1 p hp hu
    have hG_envS : ∀ g ∈ G, g ∉ ((S₁ ++ S₂).onCtx ctx_1).env.freeVars := by
      intro g hg hc
      rw [Subst.onCtx_append, Env.mem_freeVars_iff] at hc
      obtain ⟨M, hM, hgM⟩ := hc
      rw [show (S₂.onCtx (S₁.onCtx ctx_1)).env = (S₁.onCtx ctx_1).env.map (Subst.onPolyTy S₂) from rfl,
          List.mem_map] at hM
      obtain ⟨M₀, hM₀, rfl⟩ := hM
      exact Subst.notMemOnTy (fun p hp hgp => hS₂Gran p hp g hgp hg)
        (fun hc2 => hG_envS₁ g hg (Env.mem_freeVars_iff.mpr ⟨M₀, hM₀, hc2⟩)) hgM
    have hbctxeq : Subst.onCtx S₂ ⟨genGroupSchemes (bindings.flatMap Expr.tyFreeVars)
          (S₁.onCtx ctx_1).env τs ++ (S₁.onCtx ctx_1).env, (S₁.onCtx ctx_1).ctors⟩
        = ⟨(τs.map S₂.onTy).map (PolyTy.genGroup G) ++ ((S₁ ++ S₂).onCtx ctx_1).env,
            ((S₁ ++ S₂).onCtx ctx_1).ctors⟩ := by
      rw [Subst.onCtx_append]
      show (⟨Subst.onEnv S₂ (genGroupSchemes (bindings.flatMap Expr.tyFreeVars)
            (S₁.onCtx ctx_1).env τs ++ (S₁.onCtx ctx_1).env), (S₁.onCtx ctx_1).ctors⟩ : Ctx) = _
      rw [Subst.onEnv, List.map_append]
      refine congrArg (fun E => (⟨E ++ (S₂.onCtx (S₁.onCtx ctx_1)).env, _⟩ : Ctx)) ?_
      simp only [genGroupSchemes, List.map_map]
      rw [← hG]
      apply List.map_congr_left
      intro τ hτ
      simp only [Function.comp_apply]
      exact Subst.onPolyTy_genGroup hS₂G hS₂Gran
    -- monomorphic base at (τs.map S₂.onTy)
    have hbase : ∀ q ∈ bindings.zip (τs.map S₂.onTy),
        TypeOfHM ⟨(τs.map S₂.onTy).map PolyTy.mkTrivial ++ ((S₁ ++ S₂).onCtx ctx_1).env,
            ((S₁ ++ S₂).onCtx ctx_1).ctors⟩ q.1 q.2 := by
      intro q hq
      rw [List.zip_map_right] at hq
      obtain ⟨r, hr, rfl⟩ := List.mem_map.mp hq
      have hmono := hgroupSound r hr
      have hpush := TypeOfHM.onSubst S₂ hS₂lc hmono
      have hrfix : r.1.substTyFvars S₂ = r.1 :=
        Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc =>
          hSK p (List.mem_append_right _ hp)
            (hKe p.1 (.inl (Expr.mem_recGroupTyFreeVars_of (List.of_mem_zip hr).1 hc))))
      have hctxeq : Subst.onCtx S₂ ⟨τs.map PolyTy.mkTrivial ++ (S₁.onCtx ctx_1).env, ctx_1.ctors⟩
          = ⟨(τs.map S₂.onTy).map PolyTy.mkTrivial ++ ((S₁ ++ S₂).onCtx ctx_1).env,
              ((S₁ ++ S₂).onCtx ctx_1).ctors⟩ := by
        rw [Subst.onCtx_append]
        simp only [Subst.onCtx, Subst.onEnv, List.map_append, List.map_map, Function.comp_def,
          Subst.onPolyTy, PolyTy.mkTrivial]
      rw [hrfix, hctxeq] at hpush
      exact hpush
    refine TypeOfHM.letRec (τs := τs.map S₂.onTy)
      (Ms := (τs.map S₂.onTy).map (PolyTy.genGroup G)) (G := G) (L := G)
      ?_ (by simp only [List.length_map]) ?_ hG_nodup ?_ ?_ rfl ?_
    · simp only [List.length_map, hτs, freshVars_length]
    · intro τ hτ; obtain ⟨τ0, hτ0, rfl⟩ := List.mem_map.mp hτ
      exact Subst.onTy_lc hS₂lc (hτs_lc τ0 hτ0)
    · intro p hp
      exact List.mem_zip_self_map' hp
    · -- cofinite
      intro Xs hXfresh p hp
      rw [show (τs.map S₂.onTy).map (Ty.renameG G Xs)
            = (τs.map S₂.onTy).map (Subst.onTy (G.zip (Xs.map (Ty.fvar ·)))) from rfl,
          List.zip_map_right] at hp
      obtain ⟨q, hq, rfl⟩ := List.mem_map.mp hp
      have hren := TypeOfHM.onSubst (G.zip (Xs.map (Ty.fvar ·)))
        (fun s hs => by
          obtain ⟨_, hs2⟩ := List.of_mem_zip hs
          obtain ⟨x, _, hxeq⟩ := List.mem_map.mp hs2
          rw [← hxeq]; exact ContainsBvarsUpTo.fvar) (hbase q hq)
      have hqbind : q.1 ∈ bindings := (List.of_mem_zip hq).1
      have hqfix : q.1.substTyFvars (G.zip (Xs.map (Ty.fvar ·))) = q.1 :=
        Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun s hs hc => by
          have h1 := hG_ge s.1 (List.of_mem_zip hs).1
          have h2 := hKΦ s.1 (hKe s.1 (.inl (Expr.mem_recGroupTyFreeVars_of hqbind hc)))
          omega)
      have henvfix : Subst.onEnv (G.zip (Xs.map (Ty.fvar ·))) ((S₁ ++ S₂).onCtx ctx_1).env
          = ((S₁ ++ S₂).onCtx ctx_1).env :=
        Subst.onEnv_eq_self_of_fresh (fun s hs hc => hG_envS s.1 (List.of_mem_zip hs).1 hc)
      have hbindeq : Subst.onEnv (G.zip (Xs.map (Ty.fvar ·))) ((τs.map S₂.onTy).map PolyTy.mkTrivial)
          = ((τs.map S₂.onTy).map (Ty.renameG G Xs)).map PolyTy.mkTrivial := by
        simp only [Subst.onEnv, List.map_map, Function.comp_def, Subst.onPolyTy, PolyTy.mkTrivial,
          Ty.renameG, Subst.onTy]
      have hctxfix : Subst.onCtx (G.zip (Xs.map (Ty.fvar ·)))
            ⟨(τs.map S₂.onTy).map PolyTy.mkTrivial ++ ((S₁ ++ S₂).onCtx ctx_1).env,
              ((S₁ ++ S₂).onCtx ctx_1).ctors⟩
          = ⟨((τs.map S₂.onTy).map (Ty.renameG G Xs)).map PolyTy.mkTrivial
              ++ ((S₁ ++ S₂).onCtx ctx_1).env, ((S₁ ++ S₂).onCtx ctx_1).ctors⟩ := by
        show (⟨Subst.onEnv (G.zip (Xs.map (Ty.fvar ·)))
              ((τs.map S₂.onTy).map PolyTy.mkTrivial ++ ((S₁ ++ S₂).onCtx ctx_1).env), _⟩ : Ctx) = _
        rw [show Subst.onEnv (G.zip (Xs.map (Ty.fvar ·)))
                ((τs.map S₂.onTy).map PolyTy.mkTrivial ++ ((S₁ ++ S₂).onCtx ctx_1).env)
              = Subst.onEnv (G.zip (Xs.map (Ty.fvar ·))) ((τs.map S₂.onTy).map PolyTy.mkTrivial)
                ++ Subst.onEnv (G.zip (Xs.map (Ty.fvar ·))) ((S₁ ++ S₂).onCtx ctx_1).env from by
              rw [Subst.onEnv, List.map_append]; rfl,
            hbindeq, henvfix]
      rw [hqfix, hctxfix] at hren
      exact hren
    · have hbodysound := ihbody hbodyWF (by
        intro M hM
        rcases List.mem_append.mp hM with hM | hM
        · simp only [genGroupSchemes] at hM
          obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
          exact (hτs_below τ hτ).closeOver
        · exact Subst.onCtx_below hS₁below (by omega) hbelow M hM) K
        (fun k hk => by have := hKΦ k hk; have := hgrle; omega)
        (fun y hy => hKe y (.inr hy)) (fun p hp => hSK p (List.mem_append_right _ hp))
      rw [hbctxeq] at hbodysound
      exact hbodysound
  | letRecAnn hwf hlen hgroup hbody ihgroup ihbody =>
    intro hctx hbelow K hKΦ hKe hSK
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at hKe
    have hKsch : ∀ σ ∈ schemes, ∀ y ∈ σ.body.freeVars, y ∈ K := fun σ hσ y hy =>
      hKe y (.inl (.inl (Expr.mem_tyFreeVars_schemeList hσ hy)))
    have hKbr : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K := fun y hy =>
      hKe y (.inl (.inr hy))
    have hKbrΦ : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ_1 := fun y hy => hKΦ y (hKbr y hy)
    have hschB : ∀ σ ∈ schemes, Ty.BelowFvars Φ_1 σ.body := fun σ hσ =>
      Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hKsch σ hσ v hv))
    have hgWF : CtxWF { env := schemes ++ ctx_1.env, ctors := ctx_1.ctors } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hwf M hM
      · exact hctx M hM
    have hgbelow : CtxBelow Φ_1 { env := schemes ++ ctx_1.env, ctors := ctx_1.ctors } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hschB M hM
      · exact hbelow M hM
    have hgrle : Φ_1 ≤ Φ₁ := InferRecGroupAnn.frontier_le hgroup
    have hS₁K : ∀ p ∈ S₁, p.1 ∉ K := fun p hp => hSK p (List.mem_append_left _ hp)
    have hS₂K : ∀ p ∈ S₂, p.1 ∉ K := fun p hp => hSK p (List.mem_append_right _ hp)
    have hS₁lc : ∀ p ∈ S₁, p.2.IsLC := InferRecGroupAnn.lc hgroup hgWF hwf
    have hS₁below : ∀ p ∈ S₁, Ty.BelowFvars Φ₁ p.2 := InferRecGroupAnn.belowFvars hgroup hgbelow hschB hKbrΦ
    have hgS₁WF : CtxWF (S₁.onCtx { env := schemes ++ ctx_1.env, ctors := ctx_1.ctors }) :=
      Subst.onCtx_wf hS₁lc hgWF
    have hgS₁below : CtxBelow Φ₁ (S₁.onCtx { env := schemes ++ ctx_1.env, ctors := ctx_1.ctors }) :=
      Subst.onCtx_below hS₁below hgrle hgbelow
    have hS₂lc : ∀ p ∈ S₂, p.2.IsLC := (Infer.lc hbody hgS₁WF).2
    have hschemes_onEnv : ∀ (T : Subst), (∀ p ∈ T, p.1 ∉ K) → Subst.onEnv T schemes = schemes :=
      fun T hT => Subst.onEnv_eq_self_of_fresh (fun p hp hc => by
        obtain ⟨M, hM, hMc⟩ := Env.mem_freeVars_iff.mp hc
        exact hT p hp (hKsch M hM p.1 hMc))
    have hctxbridge : S₂.onCtx (S₁.onCtx { env := schemes ++ ctx_1.env, ctors := ctx_1.ctors })
        = { env := schemes ++ ((S₁ ++ S₂).onCtx ctx_1).env, ctors := ((S₁ ++ S₂).onCtx ctx_1).ctors } := by
      rw [← Subst.onCtx_append]
      simp only [Subst.onCtx, Subst.onEnv, List.map_append]
      rw [show List.map (Subst.onPolyTy (S₁ ++ S₂)) schemes = schemes from hschemes_onEnv (S₁ ++ S₂) hSK]
    obtain ⟨L, hcof⟩ := ihgroup hgWF hgbelow hwf K hKΦ hKbr hKsch hS₁K
    refine TypeOfHM.letRecAnn (schemes := schemes) (L := L ++ S₂.map Prod.fst) hlen hwf ?_ rfl ?_
    · intro p hp Xs hXfresh
      have hXfresh' : FreshNames L p.2.paramCount Xs :=
        ⟨hXfresh.length, hXfresh.nodup, fun x hx hc => hXfresh.avoid x hx (List.mem_append_left _ hc)⟩
      have hXs_S₂ : ∀ q ∈ S₂, q.1 ∉ Xs := fun q hq hc =>
        hXfresh.avoid q.1 hc (List.mem_append_right _ (List.mem_map.mpr ⟨q, hq, rfl⟩))
      have hcof_p := hcof p hp Xs hXfresh'
      have hpushed := TypeOfHM.onSubst S₂ hS₂lc hcof_p
      rw [hctxbridge] at hpushed
      rw [show (p.1.openTyVars Xs).substTyFvars S₂ = p.1.openTyVars Xs from by
            rw [← Expr.substTyFvars_openTyVars hS₂lc hXs_S₂,
                show p.1.substTyFvars S₂ = p.1 from
                  Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun q hq hc =>
                    hS₂K q hq (hKbr q.1 (Expr.mem_recGroupTyFreeVars_of (List.of_mem_zip hp).1 hc)))]] at hpushed
      rw [show S₂.onTy (p.2.openVars Xs) = p.2.openVars Xs from
            Ty.substFvars_eq_self_of_no_key (fun q hq hc => by
              rcases Ty.freeVars_openVars_subset q.1 hc with h | h
              · exact hS₂K q hq (hKsch p.2 (List.of_mem_zip hp).2 q.1 h)
              · exact hXs_S₂ q hq h)] at hpushed
      exact hpushed
    · have hbodysound := ihbody hgS₁WF hgS₁below K
        (fun k hk => by have := hKΦ k hk; have := hgrle; omega)
        (fun y hy => hKe y (.inr hy)) hS₂K
      rw [hctxbridge] at hbodysound
      exact hbodysound
  | brNil => intros; exact absurd (by assumption) (by simp)
  | brCons hlook hn huni0 hbody huni hrest ihbody ihrest =>
    expose_names
    have hΦhead : ∀ y ∈ body.tyFreeVars, y < Φ_1 := fun y hy => h_6 y (h_7 y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy))
    have hle0 : Φ_1 + ctorr.paramCount ≤ Φ₁ := Infer.frontier_le hbody
    have hS₀lc := huni0.lc h_2
      (.customTy (fun t ht => by obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht; exact ContainsBvarsUpTo.fvar))
    have hbodyWF := branchBindings_wf (ctorr := ctorr)
      (ta := ((freshVars Φ_1 ctorr.paramCount).map (Ty.fvar ·)).map S₀.onTy)
      (Subst.onCtx_wf hS₀lc h)
      (fun t ht => by
        obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
        obtain ⟨x, _, rfl⟩ := List.mem_map.mp hv
        exact Subst.onTy_lc hS₀lc ContainsBvarsUpTo.fvar)
      (by simp)
    obtain ⟨hτb_lc, hS₁lc⟩ := Infer.lc hbody hbodyWF
    have hS₂lc := huni.lc hτb_lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc h_3))
    have hctx1WF := Subst.onCtx_wf hS₂lc (Subst.onCtx_wf hS₁lc (Subst.onCtx_wf hS₀lc h))
    have hS₃lc := (InferBranches.lc hrest hctx1WF
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc h_2)))
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc h_3)))).2
    have hS₀bel : ∀ p ∈ S₀, Ty.BelowFvars (Φ_1 + ctorr.paramCount) p.2 :=
      UnifyRel.belowFvars huni0 (h_4.mono (by omega))
        (.customTy (fun t ht => by obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
                                   exact .fvar (by have := freshVars_lt x hx; omega)))
    have hbodyBelow := branchBindings_below (ctorr := ctorr)
      (ta := ((freshVars Φ_1 ctorr.paramCount).map (Ty.fvar ·)).map S₀.onTy)
      (Subst.onCtx_below hS₀bel (by omega) h_1)
      (fun t ht => by obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
                      obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv
                      exact Subst.onTy_belowFvars hS₀bel (.fvar (by have := freshVars_lt x hx; omega)))
    obtain ⟨hb_τbel, hb_sbel⟩ := Infer.belowFvars hbody hbodyBelow (fun y hy => by have := hΦhead y hy; omega)
    have hS₀ρbel : Ty.BelowFvars Φ₁ (S₀.onTy ρ) :=
      (Subst.onTy_belowFvars hS₀bel (h_5.mono (by omega))).mono hle0
    have hS₀scrutbel : Ty.BelowFvars Φ₁ (S₀.onTy scrutTy) :=
      (Subst.onTy_belowFvars hS₀bel (h_4.mono (by omega))).mono hle0
    have hS₁S₀ρbel : Ty.BelowFvars Φ₁ (S₁.onTy (S₀.onTy ρ)) := Subst.onTy_belowFvars hb_sbel hS₀ρbel
    have hS₂bel : ∀ p ∈ S₂, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hb_τbel hS₁S₀ρbel
    have hctx1bel : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx_1))) :=
      Subst.onCtx_below hS₂bel (le_refl _) (Subst.onCtx_below hb_sbel (le_refl _)
        (Subst.onCtx_below (fun p hp => (hS₀bel p hp).mono hle0) (by omega) h_1))
    have hscrut'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (S₀.onTy scrutTy))) :=
      Subst.onTy_belowFvars hS₂bel (Subst.onTy_belowFvars hb_sbel hS₀scrutbel)
    have hρ'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (S₀.onTy ρ))) :=
      Subst.onTy_belowFvars hS₂bel hS₁S₀ρbel
    have hrest_sound := ihrest hctx1WF hctx1bel
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc h_2)))
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc h_3)))
      hscrut'bel hρ'bel K (fun k hk => by have := h_6 k hk; omega)
      (fun y hy => h_7 y (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hy))
      (fun p hp => h_8 p (List.mem_append_right _ hp))
    rcases List.mem_cons.mp h_9 with rfl | hp_rest
    · simp only [Subst.onCtx_append, Subst.onTy_append]
      have h0 := ihbody hbodyWF hbodyBelow K (fun k hk => by have := h_6 k hk; omega)
        (fun y hy => h_7 y (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy))
        (fun q hq => h_8 q (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_right _ hq))))
      have h1 := TypeOfHM.onSubst S₃ hS₃lc (TypeOfHM.onSubst S₂ hS₂lc h0)
      rw [Subst.onCtx_branchBindings hS₁lc, Subst.onCtx_branchBindings hS₂lc,
          Subst.onCtx_branchBindings hS₃lc] at h1
      have huni_eq := huni.unifies
      simp only [Unifies] at huni_eq
      rw [huni_eq] at h1
      have hbodyfix_src : (body.substTyFvars S₂).substTyFvars S₃ = body := by
        rw [← Expr.substTyFvars_append]
        refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun q hq hc => ?_)
        have hmem : q.1 ∈ Expr.tyFreeVars.BranchList.tyFreeVars ((MatchPattern.named c n, body) :: rest) := by
          simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc
        rcases List.mem_append.mp hq with hh | hh
        · exact h_8 q (List.mem_append_left _ (List.mem_append_right _ hh)) (h_7 q.1 hmem)
        · exact h_8 q (List.mem_append_right _ hh) (h_7 q.1 hmem)
      rw [hbodyfix_src] at h1
      have hscrutEq : S₃.onTy (S₂.onTy (S₁.onTy (S₀.onTy scrutTy)))
          = .customTy ctorr.tyName
              ((((((freshVars Φ_1 ctorr.paramCount).map (Ty.fvar ·)).map S₀.onTy).map S₁.onTy).map S₂.onTy).map S₃.onTy) := by
        have hu := huni0.unifies
        simp only [Unifies, Subst.onTy_customTy] at hu
        rw [hu]
        simp only [Subst.onTy_customTy]
      refine TypeOfMatchBranch.mk hlook hscrutEq (by simp) hn
        (List.forall₂_self_map (fun c0 hc0 => InstantiatesBy.openWith (ctorr.bound c0 hc0) (by simp)))
        rfl rfl ?_
      exact h1
    · simp only [Subst.onCtx_append, Subst.onTy_append]
      exact hrest_sound p hp_rest
  | brWild hbody huni hrest ihbody ihrest =>
    expose_names
    have hle1 := Infer.frontier_le hbody
    have hΦhead : ∀ y ∈ body.tyFreeVars, y < Φ_1 := fun y hy => h_6 y (h_7 y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy))
    obtain ⟨hτb_lc, hS₁lc⟩ := Infer.lc hbody h
    have hS₂lc := huni.lc hτb_lc (Subst.onTy_lc hS₁lc h_3)
    have hctx1WF := Subst.onCtx_wf hS₂lc (Subst.onCtx_wf hS₁lc h)
    have hS₃lc := (InferBranches.lc hrest hctx1WF
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc h_2))
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc h_3))).2
    have hSbody : ∀ p ∈ S₁, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      h_8 p (List.mem_append_left _ (List.mem_append_left _ hp))
        (h_7 p.1 (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc))
    obtain ⟨hbE, hbR⟩ := Infer.eliminates hbody h_1 hΦhead hSbody
    obtain ⟨hb_τbel, hb_sbel⟩ := Infer.belowFvars hbody h_1 hΦhead
    have hS₁ρbel : Ty.BelowFvars Φ₁ (S₁.onTy ρ) := Subst.onTy_belowFvars hb_sbel (h_5.mono hle1)
    have hS₂bel : ∀ p ∈ S₂, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hb_τbel hS₁ρbel
    have hctx1bel : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx ctx_1)) :=
      Subst.onCtx_below hS₂bel (le_refl _) (Subst.onCtx_below hb_sbel hle1 h_1)
    have hscrut'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy scrutTy)) :=
      Subst.onTy_belowFvars hS₂bel (Subst.onTy_belowFvars hb_sbel (h_4.mono hle1))
    have hρ'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy ρ)) :=
      Subst.onTy_belowFvars hS₂bel hS₁ρbel
    have hrest_sound := ihrest hctx1WF hctx1bel
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc h_2))
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc h_3))
      hscrut'bel hρ'bel K (fun k hk => by have := h_6 k hk; omega)
      (fun y hy => h_7 y (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hy))
      (fun p hp => h_8 p (List.mem_append_right _ hp))
    rcases List.mem_cons.mp h_9 with rfl | hp_rest
    · simp only [Subst.onCtx_append, Subst.onTy_append]
      have h0 := ihbody h h_1 K h_6
        (fun y hy => h_7 y (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy))
        (fun q hq => h_8 q (List.mem_append_left _ (List.mem_append_left _ hq)))
      have h1 := TypeOfHM.onSubst S₃ hS₃lc (TypeOfHM.onSubst S₂ hS₂lc h0)
      have hbodyfix : (body.substTyFvars S₂).substTyFvars S₃ = body := by
        rw [← Expr.substTyFvars_append]
        refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun q hq hc => ?_)
        have hmem : q.1 ∈ Expr.tyFreeVars.BranchList.tyFreeVars ((MatchPattern.wildcard, body) :: rest) := by
          simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc
        rcases List.mem_append.mp hq with hh | hh
        · exact h_8 q (List.mem_append_left _ (List.mem_append_right _ hh)) (h_7 q.1 hmem)
        · exact h_8 q (List.mem_append_right _ hh) (h_7 q.1 hmem)
      rw [hbodyfix] at h1
      have huni_eq := huni.unifies; simp only [Unifies] at huni_eq
      rw [huni_eq] at h1
      exact TypeOfMatchBranch.wildcard h1
    · simp only [Subst.onCtx_append, Subst.onTy_append]
      exact hrest_sound p hp_rest
  | grpNil => intros; exact absurd (by assumption) (by simp)
  | grpCons he huni hrest ihe ihrest =>
    expose_names
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at h_5
    obtain ⟨hτ_lc, hS₁⟩ := Infer.lc he h
    have hβ_lc : β.IsLC := h_2 β List.mem_cons_self
    have hS₂ := huni.lc hτ_lc (Subst.onTy_lc hS₁ hβ_lc)
    have hle1 := Infer.frontier_le he
    have he_below := Infer.belowFvars he h_1 (fun y hy => h_4 y (h_5 y (.inl hy)))
    have hβ_below : Ty.BelowFvars Φ_1 β := h_3 β List.mem_cons_self
    have hS₁β := Subst.onTy_belowFvars he_below.2 (hβ_below.mono hle1)
    have hS₂below := UnifyRel.belowFvars huni he_below.1 hS₁β
    have hbelow2 := Subst.onCtx_below hS₂below (le_refl _) (Subst.onCtx_below he_below.2 hle1 h_1)
    have hctx2 := Subst.onCtx_wf hS₂ (Subst.onCtx_wf hS₁ h)
    have hS₃' : ∀ p ∈ S₃, p.2.IsLC := InferRecGroup.lc hrest hctx2
      (fun b' hb' => by obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hb'
                        exact Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ (h_2 b (List.mem_cons_of_mem _ hb))))
    have hrest_sound := ihrest hctx2 hbelow2
      (fun b' hb' => by obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hb'
                        exact Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ (h_2 b (List.mem_cons_of_mem _ hb))))
      (fun b' hb' => by obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hb'
                        exact Subst.onTy_belowFvars hS₂below
                          (Subst.onTy_belowFvars he_below.2 ((h_3 b (List.mem_cons_of_mem _ hb)).mono hle1)))
      K (fun k hk => lt_of_lt_of_le (h_4 k hk) hle1)
      (fun y hy => h_5 y (.inr hy)) (fun p hp => h_6 p (List.mem_append_right _ hp))
    simp only [List.map_cons, List.zip_cons_cons, List.mem_cons] at h_7
    rcases h_7 with rfl | hp_rest
    · rw [Subst.onCtx_append, Subst.onCtx_append, Subst.onTy_append, Subst.onTy_append]
      have h0 := ihe h h_1 K h_4 (fun y hy => h_5 y (.inl hy))
        (fun q hq => h_6 q (List.mem_append_left _ (List.mem_append_left _ hq)))
      have hhead := TypeOfHM.onSubst S₃ hS₃' (TypeOfHM.onSubst S₂ hS₂ h0)
      have hefix : (e_1.substTyFvars S₂).substTyFvars S₃ = e_1 := by
        rw [← Expr.substTyFvars_append]
        refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun q hq hc => ?_)
        rcases List.mem_append.mp hq with hh | hh
        · exact h_6 q (List.mem_append_left _ (List.mem_append_right _ hh)) (h_5 q.1 (Or.inl hc))
        · exact h_6 q (List.mem_append_right _ hh) (h_5 q.1 (Or.inl hc))
      rw [hefix] at hhead
      have huni_eq := huni.unifies
      simp only [Unifies] at huni_eq
      rw [huni_eq] at hhead
      exact hhead
    · have hmap : List.map (Subst.onTy (S₁ ++ S₂ ++ S₃)) βs
          = List.map (Subst.onTy S₃) (List.map (fun b => Subst.onTy S₂ (Subst.onTy S₁ b)) βs) := by
        rw [List.map_map]
        apply List.map_congr_left
        intro b _
        simp only [Function.comp_apply, Subst.onTy_append]
      rw [hmap] at hp_rest
      rw [Subst.onCtx_append, Subst.onCtx_append]
      exact hrest_sound p hp_rest
  | grpAnnNil => intros; exact ⟨[], fun p hp => absurd hp (by simp)⟩
  | grpAnnCons hΦN he huni hesc1 hesc2 hrest ihe ihrest =>
    expose_names
    have hσwf : σ.WF := h_2 σ List.mem_cons_self
    have hrle : N + σ.paramCount ≤ Φ₁ := Infer.frontier_le he
    have hΦN' : Φ_1 ≤ Φ₁ := le_trans hΦN (le_trans (Nat.le_add_right _ _) hrle)
    have hctx_pc : CtxBelow (N + σ.paramCount) ctx_1 := fun M hM => (h_1 M hM).mono (by omega)
    have hσbody : Ty.BelowFvars Φ_1 σ.body :=
      Ty.BelowFvars.of_freeVars_lt (fun v hv => h_3 v (h_5 σ List.mem_cons_self v hv))
    obtain ⟨hrhs_lc, hrhs_s⟩ := Infer.lc he h
    have hΦ_rhs : ∀ y ∈ (Expr.openTyVars (freshVars N σ.paramCount) e_1).tyFreeVars, y < N + σ.paramCount := by
      intro y hy
      rcases Expr.tyFreeVars_openTyVars hy with hh | hh
      · have := h_3 y (h_4 y (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self hh)); omega
      · have := freshVars_lt y hh; omega
    have hSe_rhs : ∀ p ∈ S₁, p.1 ∉ (Expr.openTyVars (freshVars N σ.paramCount) e_1).tyFreeVars := by
      intro p hp hc
      rcases Expr.tyFreeVars_openTyVars hc with hh | hh
      · exact h_6 p (List.mem_append_left _ (List.mem_append_left _ hp))
          (h_4 p.1 (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self hh))
      · exact hesc1 p.1 hh (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩)
    obtain ⟨hr_τ, hr_s⟩ := Infer.belowFvars he hctx_pc hΦ_rhs
    have hσopen : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
      Ty.openVars_belowFvars (hσbody.mono hΦN') (fun x hx => by have := freshVars_lt x hx; omega)
    have hSchk_below : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hr_τ hσopen
    have hSchk_lc : ∀ p ∈ Schk, p.2.IsLC :=
      UnifyRel.lc huni hrhs_lc (PolyTy.openVars_isLC hσwf (by simp))
    have hctx' : CtxWF (Schk.onCtx (S₁.onCtx ctx_1)) := Subst.onCtx_wf hSchk_lc (Subst.onCtx_wf hrhs_s h)
    have hbelow' : CtxBelow Φ₁ (Schk.onCtx (S₁.onCtx ctx_1)) :=
      Subst.onCtx_below hSchk_below (le_refl _) (Subst.onCtx_below hr_s hrle hctx_pc)
    have hschemesWF' : ∀ σ' ∈ schemes, σ'.WF := fun σ' hσ' => h_2 σ' (List.mem_cons_of_mem _ hσ')
    have hKbr' : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars rest, y ∈ K := fun y hy =>
      h_4 y (by simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact .inr hy)
    have hKsch' : ∀ σ' ∈ schemes, ∀ y ∈ σ'.body.freeVars, y ∈ K := fun σ' hσ' y hy =>
      h_5 σ' (List.mem_cons_of_mem _ hσ') y hy
    have hS₂K : ∀ p ∈ S₂, p.1 ∉ K := fun p hp => h_6 p (List.mem_append_right _ hp)
    have hS₂lc : ∀ p ∈ S₂, p.2.IsLC := InferRecGroupAnn.lc hrest hctx' hschemesWF'
    have hbody_s := hS₂lc
    have hYs_lt : ∀ y ∈ freshVars N σ.paramCount, y < Φ₁ := fun y hy => by have := freshVars_lt y hy; omega
    have hYs_ctx : ∀ y ∈ freshVars N σ.paramCount,
        ∀ M ∈ (Schk.onCtx (S₁.onCtx ctx_1)).env, y ∉ M.body.freeVars :=
      fun y hy M hM hc => hesc2 y hy (Env.mem_freeVars_iff.mpr ⟨M, hM, hc⟩)
    have hYs_sch : ∀ y ∈ freshVars N σ.paramCount, ∀ σ' ∈ schemes, y ∉ σ'.body.freeVars :=
      fun y hy σ' hσ' hc => by have := h_3 y (hKsch' σ' hσ' y hc); have := freshVars_ge y hy; omega
    have hYs_rest : ∀ y ∈ freshVars N σ.paramCount, y ∉ Expr.tyFreeVars.RecGroup.tyFreeVars rest :=
      fun y hy hc => by have := h_3 y (hKbr' y hc); have := freshVars_ge y hy; omega
    have hS₂Ys : ∀ p ∈ S₂, p.1 ∉ freshVars N σ.paramCount := fun p hp hc =>
      InferRecGroupAnn.dom_avoid hrest (hYs_lt p.1 hc) (hYs_ctx p.1 hc) (hYs_sch p.1 hc) (hYs_rest p.1 hc)
        (List.mem_map.mpr ⟨p, hp, rfl⟩)
    have hS₂Ysran : ∀ p ∈ S₂, ∀ u ∈ p.2.freeVars, u ∉ freshVars N σ.paramCount := fun p hp u hu hc =>
      (InferRecGroupAnn.eOut_avoid hrest (w := u) (hYs_lt u hc) (hYs_ctx u hc) (hYs_sch u hc)
        (hYs_rest u hc)).1 p hp hu
    have hSYs : ∀ p ∈ S₁ ++ Schk ++ S₂, p.1 ∉ freshVars N σ.paramCount := by
      intro p hp
      rcases List.mem_append.mp hp with hp | hp
      · exact fun hc => hesc1 p.1 hc (List.mem_map.mpr ⟨p, hp, rfl⟩)
      · exact hS₂Ys p hp
    have hSfix_σopen : ∀ p ∈ S₁ ++ Schk ++ S₂,
        p.1 ∉ (σ.openVars (freshVars N σ.paramCount)).freeVars := by
      intro p hp hc
      rcases Ty.freeVars_openVars_subset p.1 hc with hh | hh
      · exact h_6 p hp (h_5 σ List.mem_cons_self p.1 hh)
      · exact hSYs p hp hh
    have hSchk_fix : Schk.onTy (σ.openVars (freshVars N σ.paramCount))
        = σ.openVars (freshVars N σ.paramCount) :=
      Ty.substFvars_eq_self_of_no_key
        (fun p hp => hSfix_σopen p (List.mem_append_left _ (List.mem_append_right _ hp)))
    have hS₂fix : S₂.onTy (σ.openVars (freshVars N σ.paramCount))
        = σ.openVars (freshVars N σ.paramCount) :=
      Ty.substFvars_eq_self_of_no_key (fun p hp => hSfix_σopen p (List.mem_append_right _ hp))
    have hrhsfix : (e_1.openTyVars (freshVars N σ.paramCount)).substTyFvars (Schk ++ S₂)
        = e_1.openTyVars (freshVars N σ.paramCount) := by
      refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => ?_)
      have hpS : p ∈ S₁ ++ Schk ++ S₂ := by
        rcases List.mem_append.mp hp with hh | hh
        · exact List.mem_append_left _ (List.mem_append_right _ hh)
        · exact List.mem_append_right _ hh
      rcases Expr.tyFreeVars_openTyVars hc with hh | hh
      · exact h_6 p hpS (h_4 p.1 (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self hh))
      · exact hSYs p hpS hh
    have hrhs_sound := ihe h hctx_pc (K ++ freshVars N σ.paramCount)
      (fun k hk => by rcases List.mem_append.mp hk with hh | hh
                      · have := h_3 k hh; have := hΦN; omega
                      · have := freshVars_lt k hh; omega)
      (fun y hy => by rcases Expr.tyFreeVars_openTyVars hy with hh | hh
                      · exact List.mem_append_left _ (h_4 y (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self hh))
                      · exact List.mem_append_right _ hh)
      (fun p hp hc => by rcases List.mem_append.mp hc with hh | hh
                         · exact h_6 p (List.mem_append_left _ (List.mem_append_left _ hp)) hh
                         · exact hesc1 p.1 hh (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩))
    have hr1 := TypeOfHM.onSubst Schk hSchk_lc hrhs_sound
    rw [← Subst.onCtx_append] at hr1
    have hr2 := TypeOfHM.onSubst S₂ hbody_s hr1
    rw [← Subst.onCtx_append] at hr2
    have hu := huni.unifies
    simp only [Unifies] at hu
    rw [show (((e_1.openTyVars (freshVars N σ.paramCount)).substTyFvars Schk).substTyFvars S₂)
          = e_1.openTyVars (freshVars N σ.paramCount) from by
            rw [← Expr.substTyFvars_append]; exact hrhsfix,
        hu, hSchk_fix, hS₂fix] at hr2
    have hYs_env : ∀ y ∈ freshVars N σ.paramCount,
        y ∉ ((S₁ ++ Schk ++ S₂).onCtx ctx_1).env.freeVars := by
      intro y hy hc
      rw [show (S₁ ++ Schk ++ S₂).onCtx ctx_1 = S₂.onCtx ((S₁ ++ Schk).onCtx ctx_1) from by
            rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Subst.onCtx_append],
          Env.mem_freeVars_iff] at hc
      obtain ⟨M, hM, hyM⟩ := hc
      rw [show (S₂.onCtx ((S₁ ++ Schk).onCtx ctx_1)).env
            = ((S₁ ++ Schk).onCtx ctx_1).env.map (Subst.onPolyTy S₂) from rfl, List.mem_map] at hM
      obtain ⟨M₀, hM₀, rfl⟩ := hM
      refine Subst.notMemOnTy (fun p hp hyp => hS₂Ysran p hp y hyp hy) (fun hc2 => ?_) hyM
      rw [Subst.onCtx_append] at hM₀
      exact hesc2 y hy (Env.mem_freeVars_iff.mpr ⟨M₀, hM₀, hc2⟩)
    have head_cof : ∀ Xs, FreshNames (freshVars N σ.paramCount) σ.paramCount Xs →
        TypeOfHM ((S₁ ++ Schk ++ S₂).onCtx ctx_1) (e_1.openTyVars Xs) (σ.openVars Xs) := by
      intro Xs hXfresh
      have hYsX : ∀ y ∈ freshVars N σ.paramCount, y ∉ Xs :=
        fun y hy hc => hXfresh.avoid y hc hy
      have hXlen : Xs.length = (freshVars N σ.paramCount).length := by
        rw [hXfresh.length, freshVars_length]
      have htermeq : (e_1.openTyVars (freshVars N σ.paramCount)).substTyFvars
            ((freshVars N σ.paramCount).zip (Xs.map (Ty.fvar ·))) = e_1.openTyVars Xs :=
        Expr.substTyFvars_zip_openTyVars hXlen.symm freshVars_nodup
          (fun y hy hc => by have := h_3 y (h_4 y (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self hc))
                             have := freshVars_ge y hy; omega)
          hYsX
      have htypeeq : Subst.onTy ((freshVars N σ.paramCount).zip (Xs.map (Ty.fvar ·)))
            (σ.openVars (freshVars N σ.paramCount)) = σ.openVars Xs := by
        have hh := Ty.openWith_eq_substFvars_openVars (ty := σ.body) (Vs := Xs.map (Ty.fvar ·))
          (Xs := freshVars N σ.paramCount)
          ⟨by rw [List.length_map, hXlen], fun V hV => by
                obtain ⟨x, _, rfl⟩ := List.mem_map.mp hV; exact ContainsBvarsUpTo.fvar⟩
          freshVars_nodup
          (fun y hy hc => by have := h_3 y (h_5 σ List.mem_cons_self y hc); have := freshVars_ge y hy; omega)
          (fun y hy hc => hYsX y hy (Ty.mem_freeVarsList_map_fvar.mp hc))
        rw [← Ty.openVars_eq_openWith] at hh
        exact hh.symm
      have hren := TypeOfHM.onSubst ((freshVars N σ.paramCount).zip (Xs.map (Ty.fvar ·)))
        (fun s hs => by
          obtain ⟨_, hs2⟩ := List.of_mem_zip hs
          obtain ⟨x, _, hxeq⟩ := List.mem_map.mp hs2
          rw [← hxeq]; exact ContainsBvarsUpTo.fvar) hr2
      have hctxfix : Subst.onCtx ((freshVars N σ.paramCount).zip (Xs.map (Ty.fvar ·)))
            ((S₁ ++ Schk ++ S₂).onCtx ctx_1) = (S₁ ++ Schk ++ S₂).onCtx ctx_1 := by
        conv_lhs => rw [Subst.onCtx]
        rw [Subst.onEnv_eq_self_of_fresh (fun s hs hc => hYs_env s.1 (List.of_mem_zip hs).1 hc)]
      rw [htermeq, htypeeq, hctxfix] at hren
      exact hren
    obtain ⟨L_tail, tail_cof⟩ := ihrest hctx' hbelow' hschemesWF'
      K (fun k hk => by have := h_3 k hk; omega) hKbr' hKsch' hS₂K
    refine ⟨freshVars N σ.paramCount ++ L_tail, ?_⟩
    intro p hp Xs hXfresh
    rw [List.zip_cons_cons, List.mem_cons] at hp
    rcases hp with rfl | hp_rest
    · exact head_cof Xs ⟨hXfresh.length, hXfresh.nodup,
        fun x hx hc => hXfresh.avoid x hx (List.mem_append_left _ hc)⟩
    · rw [show (S₁ ++ Schk ++ S₂).onCtx ctx_1 = S₂.onCtx (Schk.onCtx (S₁.onCtx ctx_1)) from by
            rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Subst.onCtx_append,
                Subst.onCtx_append]]
      exact tail_cof p hp_rest Xs ⟨hXfresh.length, hXfresh.nodup,
        fun x hx hc => hXfresh.avoid x hx (List.mem_append_right _ hc)⟩

/-- Source branch-typings from an `InferBranches` derivation (the `TypeOfHM`
    analogue of `InferBranches.sound`; drives each body via `Infer.sourceSound`). -/
theorem InferBranches.sourceSound {Φ ctx scrutTy ρ brs Φ' S brsOut}
    (h : InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut)
    (hctx : CtxWF ctx) (hbelow : CtxBelow Φ ctx)
    (hscrutTy : scrutTy.IsLC) (hρ : ρ.IsLC)
    (hscrutB : Ty.BelowFvars Φ scrutTy) (hρB : Ty.BelowFvars Φ ρ) (K : List Nat)
    (hKΦ : ∀ k ∈ K, k < Φ)
    (hKbr : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars brs, y ∈ K)
    (hSK : ∀ p ∈ S, p.1 ∉ K) :
    ∀ p ∈ brs, TypeOfMatchBranch (S.onCtx ctx) p (S.onTy scrutTy) (S.onTy ρ) := by
  cases h with
  | nil => intro p hp; simp at hp
  | cons hlook hn huni0 hbody huni hrest =>
    expose_names
    have hΦhead : ∀ y ∈ body.tyFreeVars, y < Φ := fun y hy => hKΦ y (hKbr y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy))
    have hle0 : Φ + ctor.paramCount ≤ Φ₁ := Infer.frontier_le hbody
    have hS₀lc := huni0.lc hscrutTy
      (.customTy (fun t ht => by obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht; exact ContainsBvarsUpTo.fvar))
    have hbodyWF := branchBindings_wf (ctorr := ctor)
      (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
      (Subst.onCtx_wf hS₀lc hctx)
      (fun t ht => by
        obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
        obtain ⟨x, _, rfl⟩ := List.mem_map.mp hv
        exact Subst.onTy_lc hS₀lc ContainsBvarsUpTo.fvar)
      (by simp)
    obtain ⟨hτb_lc, hS₁lc⟩ := Infer.lc hbody hbodyWF
    have hS₂lc := huni.lc hτb_lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc hρ))
    have hctx1WF := Subst.onCtx_wf hS₂lc (Subst.onCtx_wf hS₁lc (Subst.onCtx_wf hS₀lc hctx))
    have hS₃lc := (InferBranches.lc hrest hctx1WF
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc hscrutTy)))
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc hρ)))).2
    have hS₀bel : ∀ p ∈ S₀, Ty.BelowFvars (Φ + ctor.paramCount) p.2 :=
      UnifyRel.belowFvars huni0 (hscrutB.mono (by omega))
        (.customTy (fun t ht => by obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
                                   exact .fvar (by have := freshVars_lt x hx; omega)))
    have hbodyBelow := branchBindings_below (ctorr := ctor)
      (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
      (Subst.onCtx_below hS₀bel (by omega) hbelow)
      (fun t ht => by obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
                      obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv
                      exact Subst.onTy_belowFvars hS₀bel (.fvar (by have := freshVars_lt x hx; omega)))
    obtain ⟨hb_τbel, hb_sbel⟩ := Infer.belowFvars hbody hbodyBelow (fun y hy => by have := hΦhead y hy; omega)
    have hS₀ρbel : Ty.BelowFvars Φ₁ (S₀.onTy ρ) :=
      (Subst.onTy_belowFvars hS₀bel (hρB.mono (by omega))).mono hle0
    have hS₀scrutbel : Ty.BelowFvars Φ₁ (S₀.onTy scrutTy) :=
      (Subst.onTy_belowFvars hS₀bel (hscrutB.mono (by omega))).mono hle0
    have hS₁S₀ρbel : Ty.BelowFvars Φ₁ (S₁.onTy (S₀.onTy ρ)) := Subst.onTy_belowFvars hb_sbel hS₀ρbel
    have hS₂bel : ∀ p ∈ S₂, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hb_τbel hS₁S₀ρbel
    have hctx1bel : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx))) :=
      Subst.onCtx_below hS₂bel (le_refl _) (Subst.onCtx_below hb_sbel (le_refl _)
        (Subst.onCtx_below (fun p hp => (hS₀bel p hp).mono hle0) (by omega) hbelow))
    have hscrut'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (S₀.onTy scrutTy))) :=
      Subst.onTy_belowFvars hS₂bel (Subst.onTy_belowFvars hb_sbel hS₀scrutbel)
    have hρ'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (S₀.onTy ρ))) :=
      Subst.onTy_belowFvars hS₂bel hS₁S₀ρbel
    have hrest_sound := InferBranches.sourceSound hrest hctx1WF hctx1bel
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc hscrutTy)))
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc (Subst.onTy_lc hS₀lc hρ)))
      hscrut'bel hρ'bel K (fun k hk => by have := hKΦ k hk; omega)
      (fun y hy => hKbr y (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hy))
      (fun p hp => hSK p (List.mem_append_right _ hp))
    intro p hp
    rcases List.mem_cons.mp hp with rfl | hp_rest
    · simp only [Subst.onCtx_append, Subst.onTy_append]
      have h0 := Infer.sourceSound hbody hbodyWF hbodyBelow K (fun k hk => by have := hKΦ k hk; omega)
        (fun y hy => hKbr y (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy))
        (fun q hq => hSK q (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_right _ hq))))
      have h1 := TypeOfHM.onSubst S₃ hS₃lc (TypeOfHM.onSubst S₂ hS₂lc h0)
      rw [Subst.onCtx_branchBindings hS₁lc, Subst.onCtx_branchBindings hS₂lc,
          Subst.onCtx_branchBindings hS₃lc] at h1
      have huni_eq := huni.unifies
      simp only [Unifies] at huni_eq
      rw [huni_eq] at h1
      have hbodyfix_src : (body.substTyFvars S₂).substTyFvars S₃ = body := by
        rw [← Expr.substTyFvars_append]
        refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun q hq hc => ?_)
        have hmem : q.1 ∈ Expr.tyFreeVars.BranchList.tyFreeVars ((MatchPattern.named c n, body) :: rest) := by
          simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc
        rcases List.mem_append.mp hq with hh | hh
        · exact hSK q (List.mem_append_left _ (List.mem_append_right _ hh)) (hKbr q.1 hmem)
        · exact hSK q (List.mem_append_right _ hh) (hKbr q.1 hmem)
      rw [hbodyfix_src] at h1
      have hscrutEq : S₃.onTy (S₂.onTy (S₁.onTy (S₀.onTy scrutTy)))
          = .customTy ctor.tyName
              ((((((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy).map S₁.onTy).map S₂.onTy).map S₃.onTy) := by
        have hu := huni0.unifies
        simp only [Unifies, Subst.onTy_customTy] at hu
        rw [hu]
        simp only [Subst.onTy_customTy]
      refine TypeOfMatchBranch.mk hlook hscrutEq (by simp) hn
        (List.forall₂_self_map (fun c0 hc0 => InstantiatesBy.openWith (ctor.bound c0 hc0) (by simp)))
        rfl rfl ?_
      exact h1
    · simp only [Subst.onCtx_append, Subst.onTy_append]
      exact hrest_sound p hp_rest
  | consWild hbody huni hrest =>
    expose_names
    have hle1 := Infer.frontier_le hbody
    have hΦhead : ∀ y ∈ body.tyFreeVars, y < Φ := fun y hy => hKΦ y (hKbr y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy))
    obtain ⟨hτb_lc, hS₁lc⟩ := Infer.lc hbody hctx
    have hS₂lc := huni.lc hτb_lc (Subst.onTy_lc hS₁lc hρ)
    have hctx1WF := Subst.onCtx_wf hS₂lc (Subst.onCtx_wf hS₁lc hctx)
    have hS₃lc := (InferBranches.lc hrest hctx1WF
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc hscrutTy))
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc hρ))).2
    have hSbody : ∀ p ∈ S₁, p.1 ∉ body.tyFreeVars := fun p hp hc =>
      hSK p (List.mem_append_left _ (List.mem_append_left _ hp))
        (hKbr p.1 (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc))
    obtain ⟨hbE, hbR⟩ := Infer.eliminates hbody hbelow hΦhead hSbody
    obtain ⟨hb_τbel, hb_sbel⟩ := Infer.belowFvars hbody hbelow hΦhead
    have hS₁ρbel : Ty.BelowFvars Φ₁ (S₁.onTy ρ) := Subst.onTy_belowFvars hb_sbel (hρB.mono hle1)
    have hS₂bel : ∀ p ∈ S₂, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hb_τbel hS₁ρbel
    have hctx1bel : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx ctx)) :=
      Subst.onCtx_below hS₂bel (le_refl _) (Subst.onCtx_below hb_sbel hle1 hbelow)
    have hscrut'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy scrutTy)) :=
      Subst.onTy_belowFvars hS₂bel (Subst.onTy_belowFvars hb_sbel (hscrutB.mono hle1))
    have hρ'bel : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy ρ)) :=
      Subst.onTy_belowFvars hS₂bel hS₁ρbel
    have hrest_sound := InferBranches.sourceSound hrest hctx1WF hctx1bel
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc hscrutTy))
      (Subst.onTy_lc hS₂lc (Subst.onTy_lc hS₁lc hρ))
      hscrut'bel hρ'bel K (fun k hk => by have := hKΦ k hk; omega)
      (fun y hy => hKbr y (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hy))
      (fun p hp => hSK p (List.mem_append_right _ hp))
    intro p hp
    rcases List.mem_cons.mp hp with rfl | hp_rest
    · simp only [Subst.onCtx_append, Subst.onTy_append]
      have h0 := Infer.sourceSound hbody hctx hbelow K hKΦ
        (fun y hy => hKbr y (by simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy))
        (fun q hq => hSK q (List.mem_append_left _ (List.mem_append_left _ hq)))
      have h1 := TypeOfHM.onSubst S₃ hS₃lc (TypeOfHM.onSubst S₂ hS₂lc h0)
      have hbodyfix : (body.substTyFvars S₂).substTyFvars S₃ = body := by
        rw [← Expr.substTyFvars_append]
        refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun q hq hc => ?_)
        have hmem : q.1 ∈ Expr.tyFreeVars.BranchList.tyFreeVars ((MatchPattern.wildcard, body) :: rest) := by
          simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hc
        rcases List.mem_append.mp hq with hh | hh
        · exact hSK q (List.mem_append_left _ (List.mem_append_right _ hh)) (hKbr q.1 hmem)
        · exact hSK q (List.mem_append_right _ hh) (hKbr q.1 hmem)
      rw [hbodyfix] at h1
      have huni_eq := huni.unifies; simp only [Unifies] at huni_eq
      rw [huni_eq] at h1
      exact TypeOfMatchBranch.wildcard h1
    · simp only [Subst.onCtx_append, Subst.onTy_append]
      exact hrest_sound p hp_rest
termination_by Expr.sizeBranches brs
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)

/-- Source binding-typings from an `InferRecGroup` derivation (the `TypeOfHM`
    analogue of `InferRecGroup.sound`; drives each binding via `Infer.sourceSound`). -/
theorem InferRecGroup.sourceSound {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut)
    (hctx : CtxWF ctx) (hbelow : CtxBelow Φ ctx)
    (htargets : ∀ τ ∈ targets, τ.IsLC) (htargetsB : ∀ τ ∈ targets, Ty.BelowFvars Φ τ)
    (K : List Nat) (hKΦ : ∀ k ∈ K, k < Φ)
    (hKbr : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K)
    (hSK : ∀ p ∈ S, p.1 ∉ K) :
    ∀ p ∈ bindings.zip (targets.map S.onTy), TypeOfHM (S.onCtx ctx) p.1 p.2 := by
  cases h with
  | nil => intro p hp; simp at hp
  | cons he huni hrest =>
    expose_names
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at hKbr
    obtain ⟨hτ_lc, hS₁⟩ := Infer.lc he hctx
    have hβ_lc : β.IsLC := htargets β List.mem_cons_self
    have hS₂ := huni.lc hτ_lc (Subst.onTy_lc hS₁ hβ_lc)
    have hle1 := Infer.frontier_le he
    have he_below := Infer.belowFvars he hbelow (fun y hy => hKΦ y (hKbr y (.inl hy)))
    have hβ_below : Ty.BelowFvars Φ β := htargetsB β List.mem_cons_self
    have hS₁β := Subst.onTy_belowFvars he_below.2 (hβ_below.mono hle1)
    have hS₂below := UnifyRel.belowFvars huni he_below.1 hS₁β
    have hbelow2 := Subst.onCtx_below hS₂below (le_refl _) (Subst.onCtx_below he_below.2 hle1 hbelow)
    have hctx2 := Subst.onCtx_wf hS₂ (Subst.onCtx_wf hS₁ hctx)
    have hS₃' : ∀ p ∈ S₃, p.2.IsLC := InferRecGroup.lc hrest hctx2
      (fun b' hb' => by obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hb'
                        exact Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ (htargets b (List.mem_cons_of_mem _ hb))))
    have hrest_sound := InferRecGroup.sourceSound hrest hctx2 hbelow2
      (fun b' hb' => by obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hb'
                        exact Subst.onTy_lc hS₂ (Subst.onTy_lc hS₁ (htargets b (List.mem_cons_of_mem _ hb))))
      (fun b' hb' => by obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hb'
                        exact Subst.onTy_belowFvars hS₂below
                          (Subst.onTy_belowFvars he_below.2 ((htargetsB b (List.mem_cons_of_mem _ hb)).mono hle1)))
      K (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle1)
      (fun y hy => hKbr y (.inr hy)) (fun p hp => hSK p (List.mem_append_right _ hp))
    intro p hp
    simp only [List.map_cons, List.zip_cons_cons, List.mem_cons] at hp
    rcases hp with rfl | hp_rest
    · rw [Subst.onCtx_append, Subst.onCtx_append, Subst.onTy_append, Subst.onTy_append]
      have h0 := Infer.sourceSound he hctx hbelow K hKΦ (fun y hy => hKbr y (.inl hy))
        (fun q hq => hSK q (List.mem_append_left _ (List.mem_append_left _ hq)))
      have hhead := TypeOfHM.onSubst S₃ hS₃' (TypeOfHM.onSubst S₂ hS₂ h0)
      have hefix : (e.substTyFvars S₂).substTyFvars S₃ = e := by
        rw [← Expr.substTyFvars_append]
        refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun q hq hc => ?_)
        rcases List.mem_append.mp hq with hh | hh
        · exact hSK q (List.mem_append_left _ (List.mem_append_right _ hh)) (hKbr q.1 (Or.inl hc))
        · exact hSK q (List.mem_append_right _ hh) (hKbr q.1 (Or.inl hc))
      rw [hefix] at hhead
      have huni_eq := huni.unifies
      simp only [Unifies] at huni_eq
      rw [huni_eq] at hhead
      exact hhead
    · have hmap : List.map (Subst.onTy (S₁ ++ S₂ ++ S₃)) βs
          = List.map (Subst.onTy S₃) (List.map (fun b => Subst.onTy S₂ (Subst.onTy S₁ b)) βs) := by
        rw [List.map_map]
        apply List.map_congr_left
        intro b _
        simp only [Function.comp_apply, Subst.onTy_append]
      rw [hmap] at hp_rest
      rw [Subst.onCtx_append, Subst.onCtx_append]
      exact hrest_sound p hp_rest
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)

/-- Source per-binding cofinite typings from an annotated recursion-group
    derivation (the `TypeOfHM` analogue of `InferRecGroupAnn.sound`; drives each
    skolem-opened binding via `Infer.sourceSound`, and produces the cofinite by a
    skolem-renaming — no close-back needed since the bindings are un-elaborated). -/
theorem InferRecGroupAnn.sourceSound {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut)
    (hctx : CtxWF ctx) (hbelow : CtxBelow Φ ctx)
    (hschemesWF : ∀ σ ∈ schemes, σ.WF)
    (K : List Nat) (hKΦ : ∀ k ∈ K, k < Φ)
    (hKbr : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K)
    (hKsch : ∀ σ ∈ schemes, ∀ y ∈ σ.body.freeVars, y ∈ K)
    (hSK : ∀ p ∈ S, p.1 ∉ K) :
    ∃ L : List Nat, ∀ p ∈ bindings.zip schemes, ∀ Xs, FreshNames L p.2.paramCount Xs →
      TypeOfHM (S.onCtx ctx) (p.1.openTyVars Xs) (p.2.openVars Xs) := by
  cases h with
  | nil => exact ⟨[], fun p hp => absurd hp (by simp)⟩
  | @cons _ N _ σ schtl e rest Φ₁ Φ₂ S₁ Schk S₂ eOut restOut τ hΦN he huni hesc1 hesc2 hrest =>
    have hσwf : σ.WF := hschemesWF σ List.mem_cons_self
    have hrle : N + σ.paramCount ≤ Φ₁ := Infer.frontier_le he
    have hΦN' : Φ ≤ Φ₁ := le_trans hΦN (le_trans (Nat.le_add_right _ _) hrle)
    have hctx_pc : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hbelow M hM).mono (by omega)
    have hσbody : Ty.BelowFvars Φ σ.body :=
      Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hKsch σ List.mem_cons_self v hv))
    obtain ⟨hrhs_lc, hrhs_s⟩ := Infer.lc he hctx
    have hΦ_rhs : ∀ y ∈ (Expr.openTyVars (freshVars N σ.paramCount) e).tyFreeVars, y < N + σ.paramCount := by
      intro y hy
      rcases Expr.tyFreeVars_openTyVars hy with hh | hh
      · have := hKΦ y (hKbr y (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self hh)); omega
      · have := freshVars_lt y hh; omega
    have hSe_rhs : ∀ p ∈ S₁, p.1 ∉ (Expr.openTyVars (freshVars N σ.paramCount) e).tyFreeVars := by
      intro p hp hc
      rcases Expr.tyFreeVars_openTyVars hc with hh | hh
      · exact hSK p (List.mem_append_left _ (List.mem_append_left _ hp))
          (hKbr p.1 (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self hh))
      · exact hesc1 p.1 hh (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩)
    obtain ⟨hr_τ, hr_s⟩ := Infer.belowFvars he hctx_pc hΦ_rhs
    have hσopen : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
      Ty.openVars_belowFvars (hσbody.mono hΦN') (fun x hx => by have := freshVars_lt x hx; omega)
    have hSchk_below : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 := UnifyRel.belowFvars huni hr_τ hσopen
    have hSchk_lc : ∀ p ∈ Schk, p.2.IsLC :=
      UnifyRel.lc huni hrhs_lc (PolyTy.openVars_isLC hσwf (by simp))
    have hctx' : CtxWF (Schk.onCtx (S₁.onCtx ctx)) := Subst.onCtx_wf hSchk_lc (Subst.onCtx_wf hrhs_s hctx)
    have hbelow' : CtxBelow Φ₁ (Schk.onCtx (S₁.onCtx ctx)) :=
      Subst.onCtx_below hSchk_below (le_refl _) (Subst.onCtx_below hr_s hrle hctx_pc)
    have hschemesWF' : ∀ σ' ∈ schtl, σ'.WF := fun σ' hσ' => hschemesWF σ' (List.mem_cons_of_mem _ hσ')
    have hKbr' : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars rest, y ∈ K := fun y hy =>
      hKbr y (by simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact .inr hy)
    have hKsch' : ∀ σ' ∈ schtl, ∀ y ∈ σ'.body.freeVars, y ∈ K := fun σ' hσ' y hy =>
      hKsch σ' (List.mem_cons_of_mem _ hσ') y hy
    have hS₂K : ∀ p ∈ S₂, p.1 ∉ K := fun p hp => hSK p (List.mem_append_right _ hp)
    have hS₂lc : ∀ p ∈ S₂, p.2.IsLC := InferRecGroupAnn.lc hrest hctx' hschemesWF'
    have hYs_lt : ∀ y ∈ freshVars N σ.paramCount, y < Φ₁ := fun y hy => by have := freshVars_lt y hy; omega
    have hYs_ctx : ∀ y ∈ freshVars N σ.paramCount,
        ∀ M ∈ (Schk.onCtx (S₁.onCtx ctx)).env, y ∉ M.body.freeVars :=
      fun y hy M hM hc => hesc2 y hy (Env.mem_freeVars_iff.mpr ⟨M, hM, hc⟩)
    have hYs_sch : ∀ y ∈ freshVars N σ.paramCount, ∀ σ' ∈ schtl, y ∉ σ'.body.freeVars :=
      fun y hy σ' hσ' hc => by have := hKΦ y (hKsch' σ' hσ' y hc); have := freshVars_ge y hy; omega
    have hYs_rest : ∀ y ∈ freshVars N σ.paramCount, y ∉ Expr.tyFreeVars.RecGroup.tyFreeVars rest :=
      fun y hy hc => by have := hKΦ y (hKbr' y hc); have := freshVars_ge y hy; omega
    have hS₂Ys : ∀ p ∈ S₂, p.1 ∉ freshVars N σ.paramCount := fun p hp hc =>
      InferRecGroupAnn.dom_avoid hrest (hYs_lt p.1 hc) (hYs_ctx p.1 hc) (hYs_sch p.1 hc) (hYs_rest p.1 hc)
        (List.mem_map.mpr ⟨p, hp, rfl⟩)
    have hS₂Ysran : ∀ p ∈ S₂, ∀ u ∈ p.2.freeVars, u ∉ freshVars N σ.paramCount := fun p hp u hu hc =>
      (InferRecGroupAnn.eOut_avoid hrest (w := u) (hYs_lt u hc) (hYs_ctx u hc) (hYs_sch u hc)
        (hYs_rest u hc)).1 p hp hu
    have hSYs : ∀ p ∈ S₁ ++ Schk ++ S₂, p.1 ∉ freshVars N σ.paramCount := by
      intro p hp
      rcases List.mem_append.mp hp with hp | hp
      · exact fun hc => hesc1 p.1 hc (List.mem_map.mpr ⟨p, hp, rfl⟩)
      · exact hS₂Ys p hp
    have hSfix_σopen : ∀ p ∈ S₁ ++ Schk ++ S₂,
        p.1 ∉ (σ.openVars (freshVars N σ.paramCount)).freeVars := by
      intro p hp hc
      rcases Ty.freeVars_openVars_subset p.1 hc with hh | hh
      · exact hSK p hp (hKsch σ List.mem_cons_self p.1 hh)
      · exact hSYs p hp hh
    have hSchk_fix : Schk.onTy (σ.openVars (freshVars N σ.paramCount))
        = σ.openVars (freshVars N σ.paramCount) :=
      Ty.substFvars_eq_self_of_no_key
        (fun p hp => hSfix_σopen p (List.mem_append_left _ (List.mem_append_right _ hp)))
    have hS₂fix : S₂.onTy (σ.openVars (freshVars N σ.paramCount))
        = σ.openVars (freshVars N σ.paramCount) :=
      Ty.substFvars_eq_self_of_no_key (fun p hp => hSfix_σopen p (List.mem_append_right _ hp))
    have hrhsfix : (e.openTyVars (freshVars N σ.paramCount)).substTyFvars (Schk ++ S₂)
        = e.openTyVars (freshVars N σ.paramCount) := by
      refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => ?_)
      have hpS : p ∈ S₁ ++ Schk ++ S₂ := by
        rcases List.mem_append.mp hp with hh | hh
        · exact List.mem_append_left _ (List.mem_append_right _ hh)
        · exact List.mem_append_right _ hh
      rcases Expr.tyFreeVars_openTyVars hc with hh | hh
      · exact hSK p hpS (hKbr p.1 (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self hh))
      · exact hSYs p hpS hh
    have hrhs_sound := Infer.sourceSound he hctx hctx_pc (K ++ freshVars N σ.paramCount)
      (fun k hk => by rcases List.mem_append.mp hk with hh | hh
                      · have := hKΦ k hh; have := hΦN; omega
                      · have := freshVars_lt k hh; omega)
      (fun y hy => by rcases Expr.tyFreeVars_openTyVars hy with hh | hh
                      · exact List.mem_append_left _ (hKbr y (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self hh))
                      · exact List.mem_append_right _ hh)
      (fun p hp hc => by rcases List.mem_append.mp hc with hh | hh
                         · exact hSK p (List.mem_append_left _ (List.mem_append_left _ hp)) hh
                         · exact hesc1 p.1 hh (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, rfl⟩))
    have hr1 := TypeOfHM.onSubst Schk hSchk_lc hrhs_sound
    rw [← Subst.onCtx_append] at hr1
    have hr2 := TypeOfHM.onSubst S₂ hS₂lc hr1
    rw [← Subst.onCtx_append] at hr2
    have hu := huni.unifies
    simp only [Unifies] at hu
    rw [show (((e.openTyVars (freshVars N σ.paramCount)).substTyFvars Schk).substTyFvars S₂)
          = e.openTyVars (freshVars N σ.paramCount) from by
            rw [← Expr.substTyFvars_append]; exact hrhsfix,
        hu, hSchk_fix, hS₂fix] at hr2
    have hYs_env : ∀ y ∈ freshVars N σ.paramCount,
        y ∉ ((S₁ ++ Schk ++ S₂).onCtx ctx).env.freeVars := by
      intro y hy hc
      rw [show (S₁ ++ Schk ++ S₂).onCtx ctx = S₂.onCtx ((S₁ ++ Schk).onCtx ctx) from by
            rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Subst.onCtx_append],
          Env.mem_freeVars_iff] at hc
      obtain ⟨M, hM, hyM⟩ := hc
      rw [show (S₂.onCtx ((S₁ ++ Schk).onCtx ctx)).env
            = ((S₁ ++ Schk).onCtx ctx).env.map (Subst.onPolyTy S₂) from rfl, List.mem_map] at hM
      obtain ⟨M₀, hM₀, rfl⟩ := hM
      refine Subst.notMemOnTy (fun p hp hyp => hS₂Ysran p hp y hyp hy) (fun hc2 => ?_) hyM
      rw [Subst.onCtx_append] at hM₀
      exact hesc2 y hy (Env.mem_freeVars_iff.mpr ⟨M₀, hM₀, hc2⟩)
    have head_cof : ∀ Xs, FreshNames (freshVars N σ.paramCount) σ.paramCount Xs →
        TypeOfHM ((S₁ ++ Schk ++ S₂).onCtx ctx) (e.openTyVars Xs) (σ.openVars Xs) := by
      intro Xs hXfresh
      have hYsX : ∀ y ∈ freshVars N σ.paramCount, y ∉ Xs :=
        fun y hy hc => hXfresh.avoid y hc hy
      have hXlen : Xs.length = (freshVars N σ.paramCount).length := by
        rw [hXfresh.length, freshVars_length]
      have htermeq : (e.openTyVars (freshVars N σ.paramCount)).substTyFvars
            ((freshVars N σ.paramCount).zip (Xs.map (Ty.fvar ·))) = e.openTyVars Xs :=
        Expr.substTyFvars_zip_openTyVars hXlen.symm freshVars_nodup
          (fun y hy hc => by have := hKΦ y (hKbr y (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self hc))
                             have := freshVars_ge y hy; omega)
          hYsX
      have htypeeq : Subst.onTy ((freshVars N σ.paramCount).zip (Xs.map (Ty.fvar ·)))
            (σ.openVars (freshVars N σ.paramCount)) = σ.openVars Xs := by
        have hh := Ty.openWith_eq_substFvars_openVars (ty := σ.body) (Vs := Xs.map (Ty.fvar ·))
          (Xs := freshVars N σ.paramCount)
          ⟨by rw [List.length_map, hXlen], fun V hV => by
                obtain ⟨x, _, rfl⟩ := List.mem_map.mp hV; exact ContainsBvarsUpTo.fvar⟩
          freshVars_nodup
          (fun y hy hc => by have := hKΦ y (hKsch σ List.mem_cons_self y hc); have := freshVars_ge y hy; omega)
          (fun y hy hc => hYsX y hy (Ty.mem_freeVarsList_map_fvar.mp hc))
        rw [← Ty.openVars_eq_openWith] at hh
        exact hh.symm
      have hren := TypeOfHM.onSubst ((freshVars N σ.paramCount).zip (Xs.map (Ty.fvar ·)))
        (fun s hs => by
          obtain ⟨_, hs2⟩ := List.of_mem_zip hs
          obtain ⟨x, _, hxeq⟩ := List.mem_map.mp hs2
          rw [← hxeq]; exact ContainsBvarsUpTo.fvar) hr2
      have hctxfix : Subst.onCtx ((freshVars N σ.paramCount).zip (Xs.map (Ty.fvar ·)))
            ((S₁ ++ Schk ++ S₂).onCtx ctx) = (S₁ ++ Schk ++ S₂).onCtx ctx := by
        conv_lhs => rw [Subst.onCtx]
        rw [Subst.onEnv_eq_self_of_fresh (fun s hs hc => hYs_env s.1 (List.of_mem_zip hs).1 hc)]
      rw [htermeq, htypeeq, hctxfix] at hren
      exact hren
    obtain ⟨L_tail, tail_cof⟩ := InferRecGroupAnn.sourceSound hrest hctx' hbelow' hschemesWF'
      K (fun k hk => by have := hKΦ k hk; omega) hKbr' hKsch' hS₂K
    refine ⟨freshVars N σ.paramCount ++ L_tail, ?_⟩
    intro p hp Xs hXfresh
    rw [List.zip_cons_cons, List.mem_cons] at hp
    rcases hp with rfl | hp_rest
    · exact head_cof Xs ⟨hXfresh.length, hXfresh.nodup,
        fun x hx hc => hXfresh.avoid x hx (List.mem_append_left _ hc)⟩
    · rw [show (S₁ ++ Schk ++ S₂).onCtx ctx = S₂.onCtx (Schk.onCtx (S₁.onCtx ctx)) from by
            rw [show (S₁ ++ Schk ++ S₂) = (S₁ ++ Schk) ++ S₂ from rfl, Subst.onCtx_append,
                Subst.onCtx_append]]
      exact tail_cof p hp_rest Xs ⟨hXfresh.length, hXfresh.nodup,
        fun x hx hc => hXfresh.avoid x hx (List.mem_append_right _ hc)⟩
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)

/-- The principality property at `e`: for *any* declarative typing of `e` under
    an LC specialization `S₀` of a WF, frontier-bounded context, `Infer`
    succeeds with `(S, τ)` and the typing factors through it via an LC residual
    `R` (`S₀ = R ∘ S` below the frontier, `τ₀ = R.onTy τ`). -/
def Infer.CompleteAt (e : Expr) : Prop :=
  ∀ {Φ : Nat} {ctx : Ctx} {S₀ : Subst} {τ₀ : Ty} (K : List Nat),
    CtxWF ctx → CtxBelow Φ ctx → (∀ p ∈ S₀, p.2.IsLC) →
    (∀ k ∈ K, k < Φ) → (∀ y ∈ e.tyFreeVars, y ∈ K) →
    (∀ k ∈ K, S₀.onTy (.fvar k) = .fvar k) →
    TypeOfHM (S₀.onCtx ctx) e τ₀ →
    ∃ Φ' S eOut τ R,
      Infer Φ ctx e Φ' S eOut τ ∧
      Subst.AgreesBelow Φ S₀ (S ++ R) ∧
      τ₀ = R.onTy τ ∧
      (∀ p ∈ R, p.2.IsLC) ∧
      (∀ k ∈ K, R.onTy (.fvar k) = .fvar k) ∧
      (∀ p ∈ S, p.1 ∉ K)

/-- Principality, primitive-literal case: `Infer` returns the literal's type
    with the empty substitution; the residual is `S₀` unchanged. -/
theorem Infer.complete_prim {p : PrimLitExpr} : Infer.CompleteAt (.primLit p) := by
  intro Φ ctx S₀ τ₀ K _ _ hS₀ _ _ hKfix hty
  cases hty with
  | primLitUnit => exact ⟨Φ, [], _, _, S₀, .primLitUnit, fun v _ => by rw [List.nil_append], by simp, hS₀, hKfix, by simp⟩
  | primLitInt  => exact ⟨Φ, [], _, _, S₀, .primLitInt,  fun v _ => by rw [List.nil_append], by simp, hS₀, hKfix, by simp⟩
  | primLitNat  => exact ⟨Φ, [], _, _, S₀, .primLitNat,  fun v _ => by rw [List.nil_append], by simp, hS₀, hKfix, by simp⟩
  -- | primLitBool => exact ⟨Φ, [], _, S₀, .primLitBool, fun v _ => by rw [List.nil_append], by simp, hS₀, hKfix, by simp⟩
  | primLitStr  => exact ⟨Φ, [], _, _, S₀, .primLitStr,  fun v _ => by rw [List.nil_append], by simp, hS₀, hKfix, by simp⟩

/-! ### Injective renaming of free type variables (principality binder cases)

The list-substitution residual hits an obstruction at fresh binders: a clean
override of `S₀` mapping the fresh var to the declarative type is unrealisable as
a `List` once the fresh var occurs in `S₀`'s range (or the declarative
instantiation types). We dodge it by α-renaming the declarative derivation with a
*swap* `Φ ↔ W` (`W` fresh) so the clash disappears, then mapping the residual
back. `Ty.rename` applies a variable relabelling; the swap is realised as the
3-element list `swapSubst` (with a fresh intermediate `c`) so the existing
`TypeOfHM.onSubst` carries the renaming through the derivation. -/

mutual
/-- Relabel every free type variable by `f`. An α-renaming when `f` is injective. -/
def Ty.rename (f : Nat → Nat) : Ty → Ty
  | .prim p          => .prim p
  | .arrow a b       => .arrow (a.rename f) (b.rename f)
  | .bvar i          => .bvar i
  | .fvar n          => .fvar (f n)
  | .customTy nm tys => .customTy nm (TyList.rename f tys)

private def TyList.rename (f : Nat → Nat) : List Ty → List Ty
  | []       => []
  | hd :: tl => hd.rename f :: TyList.rename f tl
end

theorem TyList.rename_eq_map (f : Nat → Nat) (tys : List Ty) :
    TyList.rename f tys = tys.map (Ty.rename f) := by
  induction tys with
  | nil => rfl
  | cons hd tl ih => simp only [TyList.rename, List.map_cons, ih]

@[simp] theorem Ty.rename_prim {f : Nat → Nat} {p : PrimTy} :
    Ty.rename f (.prim p) = .prim p := rfl
@[simp] theorem Ty.rename_bvar {f : Nat → Nat} {i : Nat} :
    Ty.rename f (.bvar i) = .bvar i := rfl
@[simp] theorem Ty.rename_fvar {f : Nat → Nat} {n : Nat} :
    Ty.rename f (.fvar n) = .fvar (f n) := rfl
@[simp] theorem Ty.rename_arrow {f : Nat → Nat} {a b : Ty} :
    Ty.rename f (.arrow a b) = .arrow (Ty.rename f a) (Ty.rename f b) := rfl
@[simp] theorem Ty.rename_customTy {f : Nat → Nat} {nm : TyName} {tys : List Ty} :
    Ty.rename f (.customTy nm tys) = .customTy nm (tys.map (Ty.rename f)) := by
  simp [Ty.rename, TyList.rename_eq_map]

/-- Renaming by a function that fixes `τ`'s free vars is the identity. -/
theorem Ty.rename_eq_self {f : Nat → Nat} {τ : Ty}
    (h : ∀ v ∈ τ.freeVars, f v = v) : Ty.rename f τ = τ := by
  induction τ using Ty.rec_strong with
  | prim _ => rfl
  | arrow a b ih_a ih_b =>
    simp only [Ty.rename_arrow, Ty.arrow.injEq]
    refine ⟨ih_a (fun v hv => h v ?_), ih_b (fun v hv => h v ?_)⟩
    · simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inl hv
    · simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inr hv
  | bvar _ => rfl
  | fvar n =>
    have hn := h n (by simp [Ty.freeVars])
    simp only [Ty.rename_fvar, hn]
  | customTy nm tys ih =>
    simp only [Ty.rename_customTy, Ty.customTy.injEq, true_and]
    conv_rhs => rw [← List.map_id tys]
    apply List.map_congr_left
    intro t ht
    exact ih t ht (fun v hv => h v (TyList.mem_freeVars_of_mem ht hv))

/-- Renaming preserves the bvar bound (it only touches `fvar`s). -/
theorem Ty.rename_containsBvars {f : Nat → Nat} {n : Nat} {τ : Ty}
    (h : ContainsBvarsUpTo n τ) : ContainsBvarsUpTo n (Ty.rename f τ) := by
  induction τ using Ty.rec_strong with
  | prim p => exact .prim
  | bvar i => cases h with | bvar hlt => exact .bvar hlt
  | fvar m => exact .fvar
  | arrow a b iha ihb => cases h with | arrow ha hb => exact .arrow (iha ha) (ihb hb)
  | customTy nm tys ih =>
    cases h with
    | customTy hall =>
      simp only [Ty.rename_customTy]
      apply ContainsBvarsUpTo.customTy
      intro t ht
      obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
      exact ih t0 ht0 (hall t0 ht0)

/-- Renaming preserves local-closedness. -/
theorem Ty.rename_isLC {f : Nat → Nat} {τ : Ty} (h : τ.IsLC) :
    (Ty.rename f τ).IsLC := Ty.rename_containsBvars h

/-- Renaming by an injective `f` commutes with single-var substitution. -/
theorem Ty.rename_substFvar {f : Nat → Nat} (hf : Function.Injective f)
    (z : Nat) (u t : Ty) :
    Ty.rename f (Ty.substFvar z u t)
      = Ty.substFvar (f z) (Ty.rename f u) (Ty.rename f t) := by
  induction t using Ty.rec_strong with
  | prim p => rfl
  | arrow a b iha ihb => simp only [Ty.substFvar, Ty.rename_arrow, iha, ihb]
  | bvar i => rfl
  | fvar m =>
    simp only [Ty.substFvar, Ty.rename_fvar]
    by_cases hm : m = z
    · rw [if_pos hm, if_pos (congrArg f hm)]
    · rw [if_neg hm, if_neg (fun heq => hm (hf heq)), Ty.rename_fvar]
  | customTy nm tys ih =>
    simp only [Ty.substFvar, TyList.substFvar_eq_map, Ty.rename_customTy, List.map_map,
               Ty.customTy.injEq, true_and]
    apply List.map_congr_left
    intro t0 ht0
    exact ih t0 ht0

/-- Swap two naturals. -/
def swapNat (a b n : Nat) : Nat := if n = a then b else if n = b then a else n

@[simp] theorem swapNat_left (a b : Nat) : swapNat a b a = b := by simp [swapNat]
theorem swapNat_right (a b : Nat) : swapNat a b b = a := by
  simp only [swapNat]; split <;> simp_all
theorem swapNat_other {a b n : Nat} (ha : n ≠ a) (hb : n ≠ b) : swapNat a b n = n := by
  simp [swapNat, ha, hb]
theorem swapNat_involutive (a b n : Nat) : swapNat a b (swapNat a b n) = n := by
  by_cases hna : n = a
  · subst hna; rw [swapNat_left, swapNat_right]
  · by_cases hnb : n = b
    · subst hnb; rw [swapNat_right, swapNat_left]
    · rw [swapNat_other hna hnb, swapNat_other hna hnb]
theorem swapNat_injective (a b : Nat) : Function.Injective (swapNat a b) :=
  Function.Involutive.injective (swapNat_involutive a b)

/-- Conjugate a substitution by a renaming: relabel both keys and values. -/
def Subst.conj (f : Nat → Nat) (S : Subst) : Subst :=
  S.map (fun p => (f p.1, Ty.rename f p.2))

/-- Conjugation preserves LC of the replacement types. -/
theorem Subst.conj_lc {f : Nat → Nat} {S : Subst} (hS : ∀ p ∈ S, p.2.IsLC) :
    ∀ p ∈ Subst.conj f S, p.2.IsLC := by
  intro p hp
  simp only [Subst.conj, List.mem_map] at hp
  obtain ⟨p0, hp0, rfl⟩ := hp
  exact Ty.rename_isLC (hS p0 hp0)

/-- The defining property of conjugation: it intertwines `onTy` with the
    renaming (for injective `f`). -/
theorem Subst.onTy_conj {f : Nat → Nat} (hf : Function.Injective f) (S : Subst) (τ : Ty) :
    (Subst.conj f S).onTy (Ty.rename f τ) = Ty.rename f (S.onTy τ) := by
  induction S generalizing τ with
  | nil => simp only [Subst.conj, List.map_nil, Subst.onTy_nil]
  | cons hd S' ih =>
    obtain ⟨z, u⟩ := hd
    show Subst.onTy (Subst.conj f S') (Ty.substFvar (f z) (Ty.rename f u) (Ty.rename f τ))
        = Ty.rename f (Subst.onTy S' (Ty.substFvar z u τ))
    rw [← Ty.rename_substFvar hf, ih]

/-- The 3-element list realising the swap `a ↔ b` (with a fresh intermediate `c`),
    usable with `TypeOfHM.onSubst`. -/
def swapSubst (a b c : Nat) : Subst := [(a, .fvar c), (b, .fvar a), (c, .fvar b)]

theorem swapSubst_lc (a b c : Nat) : ∀ p ∈ swapSubst a b c, p.2.IsLC := by
  intro p hp
  simp only [swapSubst, List.mem_cons, List.not_mem_nil, or_false] at hp
  obtain rfl | rfl | rfl := hp <;> exact ContainsBvarsUpTo.fvar

/-- The swap list acts as `rename (swapNat a b)` on types avoiding the fresh `c`. -/
theorem swapSubst_onTy {a b c : Nat} (hab : a ≠ b) (hac : a ≠ c) (hbc : b ≠ c)
    {τ : Ty} (hc : c ∉ τ.freeVars) :
    (swapSubst a b c).onTy τ = Ty.rename (swapNat a b) τ := by
  induction τ using Ty.rec_strong with
  | prim p => simp only [Subst.onTy_prim, Ty.rename_prim]
  | bvar i => simp only [Subst.onTy_bvar, Ty.rename_bvar]
  | arrow a' b' iha ihb =>
    simp only [Ty.freeVars, List.mem_dedup, List.mem_append, not_or] at hc
    simp only [Subst.onTy_arrow, Ty.rename_arrow]
    exact congrArg₂ Ty.arrow (iha hc.1) (ihb hc.2)
  | fvar n =>
    simp only [Ty.freeVars, List.mem_singleton] at hc
    have hnc : n ≠ c := fun h => hc h.symm
    by_cases hna : n = a
    · subst hna
      simp [swapSubst, Subst.onTy, Ty.substFvars, Ty.substFvar, hbc.symm]
    · by_cases hnb : n = b
      · subst hnb
        simp [swapSubst, Subst.onTy, Ty.substFvars, Ty.substFvar, swapNat_right,
              hab.symm, hac]
      · simp [swapSubst, Subst.onTy, Ty.substFvars, Ty.substFvar,
              swapNat_other hna hnb, hna, hnb, hnc]
  | customTy nm tys ih =>
    simp only [Ty.freeVars] at hc
    simp only [Subst.onTy_customTy, Ty.rename_customTy, Ty.customTy.injEq, true_and]
    apply List.map_congr_left
    intro t ht
    exact ih t ht (fun hct => hc (TyList.mem_freeVars_of_mem ht hct))

/-- After swapping `Φ ↔ W`, the var `Φ` is absent provided `W` was absent
    (the only source of `Φ` would have been a pre-existing `W`). -/
theorem Ty.rename_swap_not_mem_left {Φ W : Nat} {Y : Ty} (h : W ∉ Y.freeVars) :
    Φ ∉ (Ty.rename (swapNat Φ W) Y).freeVars := by
  induction Y using Ty.rec_strong with
  | prim p => simp [Ty.freeVars]
  | bvar i => simp [Ty.freeVars]
  | fvar n =>
    simp only [Ty.freeVars, List.mem_singleton] at h
    simp only [Ty.rename_fvar, Ty.freeVars, List.mem_singleton, swapNat]
    split_ifs <;> omega
  | arrow a b iha ihb =>
    simp only [Ty.freeVars, List.mem_dedup, List.mem_append, not_or] at h
    simp only [Ty.rename_arrow, Ty.freeVars, List.mem_dedup, List.mem_append, not_or]
    exact ⟨iha h.1, ihb h.2⟩
  | customTy nm tys ih =>
    simp only [Ty.freeVars] at h
    simp only [Ty.rename_customTy, Ty.freeVars]
    rw [TyList.not_mem_freeVars_iff]
    intro t' ht'
    obtain ⟨t, ht, rfl⟩ := List.mem_map.mp ht'
    exact ih t ht (fun hct => h (TyList.mem_freeVars_of_mem ht hct))

/-- Map-back: substituting `W ↦ Φ` undoes the swap `Φ ↔ W` on a `W`-free type. -/
theorem Ty.substFvar_rename_swap {Φ W : Nat} {X : Ty} (h : W ∉ X.freeVars) :
    Ty.substFvar W (.fvar Φ) (Ty.rename (swapNat Φ W) X) = X := by
  induction X using Ty.rec_strong with
  | prim p => rfl
  | bvar i => rfl
  | fvar n =>
    simp only [Ty.freeVars, List.mem_singleton] at h
    simp only [Ty.rename_fvar, Ty.substFvar]
    by_cases hn : n = Φ
    · subst hn; simp [swapNat]
    · rw [swapNat_other hn (fun he => h he.symm), if_neg (fun he => h he.symm)]
  | arrow a b iha ihb =>
    simp only [Ty.freeVars, List.mem_dedup, List.mem_append, not_or] at h
    simp only [Ty.rename_arrow, Ty.substFvar, iha h.1, ihb h.2]
  | customTy nm tys ih =>
    simp only [Ty.freeVars] at h
    simp only [Ty.rename_customTy, Ty.substFvar, TyList.substFvar_eq_map, List.map_map]
    refine congrArg (Ty.customTy nm) ?_
    conv_rhs => rw [← List.map_id tys]
    apply List.map_congr_left
    intro t ht
    exact ih t ht (fun hct => h (TyList.mem_freeVars_of_mem ht hct))

/-- Two distinct fresh names, both `≥ Φ` and avoiding a given finite set. -/
theorem exists_fresh_two_ge (Φ : Nat) (avoid : List Nat) :
    ∃ W c, Φ ≤ W ∧ Φ ≤ c ∧ W ≠ c ∧ W ∉ avoid ∧ c ∉ avoid := by
  obtain ⟨Xs, hlen, hnodup, hav⟩ := exists_fresh_names (List.range Φ ++ avoid) 2
  obtain ⟨W, c, rfl⟩ : ∃ W c, Xs = [W, c] := by
    match Xs, hlen with
    | [W, c], _ => exact ⟨W, c, rfl⟩
  have hWmem : W ∈ [W, c] := by simp
  have hcmem : c ∈ [W, c] := by simp
  have hWav := hav W hWmem
  have hcav := hav c hcmem
  simp only [List.mem_append, not_or] at hWav hcav
  refine ⟨W, c, ?_, ?_, ?_, hWav.2, hcav.2⟩
  · have := hWav.1; simp only [List.mem_range, not_lt] at this; omega
  · have := hcav.1; simp only [List.mem_range, not_lt] at this; omega
  · simp only [List.nodup_cons, List.mem_singleton, List.not_mem_nil, not_false_eq_true,
      List.nodup_nil, and_true] at hnodup
    exact hnodup

/-- `substFvar` keeps `W` fresh when `W` is fresh for the input and the replacement. -/
theorem Ty.not_mem_freeVars_substFvar {Z W : Nat} {U τ : Ty}
    (hτ : W ∉ τ.freeVars) (hU : W ∉ U.freeVars) :
    W ∉ (Ty.substFvar Z U τ).freeVars := by
  induction τ using Ty.rec_strong with
  | prim p => simp [Ty.substFvar, Ty.freeVars]
  | bvar i => simp [Ty.substFvar, Ty.freeVars]
  | fvar n =>
    simp only [Ty.freeVars, List.mem_singleton] at hτ
    simp only [Ty.substFvar]
    by_cases hn : n = Z
    · simp only [if_pos hn]; exact hU
    · simp only [if_neg hn, Ty.freeVars, List.mem_singleton]; exact hτ
  | arrow a b iha ihb =>
    simp only [Ty.freeVars, List.mem_dedup, List.mem_append, not_or] at hτ
    simp only [Ty.substFvar, Ty.freeVars, List.mem_dedup, List.mem_append, not_or]
    exact ⟨iha hτ.1, ihb hτ.2⟩
  | customTy nm tys ih =>
    simp only [Ty.freeVars] at hτ
    simp only [Ty.substFvar, Ty.freeVars, TyList.substFvar_eq_map]
    rw [TyList.not_mem_freeVars_iff]
    intro t' ht'
    obtain ⟨t, ht, rfl⟩ := List.mem_map.mp ht'
    exact ih t ht (fun hct => hτ (TyList.mem_freeVars_of_mem ht hct))

/-- A whole substitution keeps `W` fresh when `W` avoids its range and the input. -/
theorem Subst.not_mem_onTy_freeVars {S : Subst} {W : Nat} {τ : Ty}
    (hS : ∀ p ∈ S, W ∉ p.2.freeVars) (hτ : W ∉ τ.freeVars) :
    W ∉ (S.onTy τ).freeVars := by
  induction S generalizing τ with
  | nil => simpa using hτ
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    rw [show ((Z, U) :: S') = [(Z, U)] ++ S' from rfl, Subst.onTy_append]
    refine ih (fun p hp => hS p (List.mem_cons_of_mem _ hp)) ?_
    exact Ty.not_mem_freeVars_substFvar hτ (hS (Z, U) List.mem_cons_self)

/-- A substitution that fixes every free variable of a type fixes the whole
    type. (Unlike `substFvars_eq_self_of_no_key`, the keys *may* occur, as long
    as the composite acts as the identity on them.) -/
theorem Subst.onTy_eq_self_of_fixes {S : Subst} :
    ∀ {t : Ty}, (∀ v ∈ t.freeVars, S.onTy (.fvar v) = .fvar v) → S.onTy t = t := by
  intro t
  induction t using Ty.rec_strong with
  | prim p => intro _; simp only [Subst.onTy_prim]
  | bvar i => intro _; simp only [Subst.onTy_bvar]
  | fvar n => intro h; exact h n (by simp [Ty.freeVars])
  | arrow a b iha ihb =>
    intro h
    rw [Subst.onTy_arrow,
        iha (fun v hv => h v (by
          simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inl hv)),
        ihb (fun v hv => h v (by
          simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inr hv))]
  | customTy nm tys ih =>
    intro h
    rw [Subst.onTy_customTy]
    refine congrArg (Ty.customTy nm) ?_
    conv_rhs => rw [← List.map_id tys]
    apply List.map_congr_left
    intro t ht
    exact ih t ht (fun v hv => h v (by
      simp only [Ty.freeVars]; exact TyList.mem_freeVars_of_mem ht hv))

theorem Infer.complete_lambda_aux {body : Expr} {Φ : Nat} {ctx : Ctx}
    {S₀ : Subst} {paramTy bodyTy : Ty} {K : List Nat}
    (ih : Infer.CompleteAt body)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hS₀ : ∀ p ∈ S₀, p.2.IsLC)
    (hparamLC : paramTy.IsLC) (hKΦ : ∀ k ∈ K, k < Φ)
    (hbodyK : ∀ y ∈ body.tyFreeVars, y ∈ K)
    (hKfix : ∀ k ∈ K, S₀.onTy (.fvar k) = .fvar k)
    (hbodyty : TypeOfHM { (S₀.onCtx ctx) with
        env := PolyTy.mkTrivial paramTy :: (S₀.onCtx ctx).env } body bodyTy) :
    ∃ Φ' S eOut τ R,
      Infer Φ ctx (.lambda none body) Φ' S eOut τ ∧
      Subst.AgreesBelow Φ S₀ (S ++ R) ∧
      Ty.arrow paramTy bodyTy = R.onTy τ ∧
      (∀ p ∈ R, p.2.IsLC) ∧
      (∀ k ∈ K, R.onTy (.fvar k) = .fvar k) ∧
      (∀ p ∈ S, p.1 ∉ K) := by
  -- STEP 0: fresh names
  obtain ⟨W, c, hΦW, hΦc, hWc, hWav, hcav⟩ := exists_fresh_two_ge Φ
    ([Φ] ++ S₀.map Prod.fst ++ S₀.flatMap (fun p => p.2.freeVars)
      ++ paramTy.freeVars ++ bodyTy.freeVars)
  simp only [List.mem_append, List.mem_singleton, List.mem_map, List.mem_flatMap] at hWav hcav
  push_neg at hWav hcav
  obtain ⟨⟨⟨⟨hWΦ, hWkey⟩, hWrange⟩, hWparam⟩, hWbody⟩ := hWav
  obtain ⟨⟨⟨⟨hcΦ, hckey⟩, hcrange⟩, hcparam⟩, hcbody⟩ := hcav
  have finj : Function.Injective (swapNat Φ W) := swapNat_injective Φ W
  have hfix : ∀ v, v < Φ → swapNat Φ W v = v := fun v hv =>
    swapNat_other (by omega) (by omega)
  have hWonTy : ∀ {τ : Ty}, W ∉ τ.freeVars → W ∉ (S₀.onTy τ).freeVars :=
    fun h => Subst.not_mem_onTy_freeVars hWrange h
  have hconΦ : ∀ p ∈ Subst.conj (swapNat Φ W) S₀, p.1 ≠ Φ := by
    intro p hp
    simp only [Subst.conj, List.mem_map] at hp
    obtain ⟨q, hq, rfl⟩ := hp
    intro hc
    apply hWkey q hq
    have hc' : swapNat Φ W q.1 = Φ := hc
    simp only [swapNat] at hc'
    split_ifs at hc' <;> omega
  have hSconjΦ : (Subst.conj (swapNat Φ W) S₀).onTy (.fvar Φ) = .fvar Φ := by
    apply Ty.substFvars_eq_self_of_no_key
    intro p hp hc
    simp only [Ty.freeVars, List.mem_singleton] at hc
    exact hconΦ p hp hc
  -- STEP 1: rename the body derivation and reinterpret its context
  have hctxeq : (swapSubst Φ W c).onCtx
        { (S₀.onCtx ctx) with env := PolyTy.mkTrivial paramTy :: (S₀.onCtx ctx).env }
      = (Subst.conj (swapNat Φ W) S₀ ++ [(Φ, Ty.rename (swapNat Φ W) paramTy)]).onCtx
        { ctx with env := PolyTy.mkTrivial (.fvar Φ) :: ctx.env } := by
    simp only [Subst.onCtx, Subst.onEnv, List.map_cons, List.map_map]
    congr 1
    congr 1
    · -- head
      simp only [Subst.onPolyTy, PolyTy.mkTrivial, PolyTy.mk.injEq, true_and]
      rw [swapSubst_onTy (Ne.symm hWΦ) (Ne.symm hcΦ) hWc hcparam,
          Subst.onTy_append (Subst.conj (swapNat Φ W) S₀)
            [(Φ, Ty.rename (swapNat Φ W) paramTy)] (.fvar Φ),
          hSconjΦ]
      simp only [Subst.onTy, Ty.substFvars, Ty.substFvar, if_pos]
    · -- tail
      apply List.map_congr_left
      intro M hM
      have hMbelow : ∀ v ∈ M.body.freeVars, v < Φ := (hbelow M hM).mem_lt
      have hrenM : Ty.rename (swapNat Φ W) M.body = M.body :=
        Ty.rename_eq_self (fun v hv => hfix v (hMbelow v hv))
      have hΦnotin : Φ ∉ (Ty.rename (swapNat Φ W) (S₀.onTy M.body)).freeVars :=
        Ty.rename_swap_not_mem_left (hWonTy (τ := M.body) (fun hv => by have := hMbelow _ hv; omega))
      simp only [Function.comp, Subst.onPolyTy, PolyTy.mk.injEq, true_and]
      rw [swapSubst_onTy (Ne.symm hWΦ) (Ne.symm hcΦ) hWc
            (Subst.not_mem_onTy_freeVars hcrange (fun hv => by have := hMbelow _ hv; omega)),
          Subst.onTy_append (Subst.conj (swapNat Φ W) S₀)
            [(Φ, Ty.rename (swapNat Φ W) paramTy)] M.body]
      conv_rhs => rw [← hrenM, Subst.onTy_conj finj]
      exact (Ty.substFvar_fresh hΦnotin).symm
  have hbodyTyeq : (swapSubst Φ W c).onTy bodyTy = Ty.rename (swapNat Φ W) bodyTy :=
    swapSubst_onTy (Ne.symm hWΦ) (Ne.symm hcΦ) hWc hcbody
  have hren := TypeOfHM.onSubst (swapSubst Φ W c) (swapSubst_lc Φ W c) hbodyty
  rw [hctxeq, hbodyTyeq] at hren
  -- The swap acts only on `≥ Φ` names; `body`'s scoped annotation vars are `< Φ`
  -- (in `K`), so the swap leaves the body term itself unchanged.
  have hbodyfix : body.substTyFvars (swapSubst Φ W c) = body := by
    refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => ?_)
    have hpΦ : Φ ≤ p.1 := by
      simp only [swapSubst, List.mem_cons, List.not_mem_nil, or_false] at hp
      obtain rfl | rfl | rfl := hp <;> omega
    have := hKΦ p.1 (hbodyK p.1 hc); omega
  rw [hbodyfix] at hren
  -- STEP 2: apply the IH to the body under the conjugated specialization
  have hwf_b : CtxWF { ctx with env := PolyTy.mkTrivial (.fvar Φ) :: ctx.env } := by
    intro M hM
    rcases List.mem_cons.mp hM with rfl | hM
    · exact ContainsBvarsUpTo.fvar
    · exact hwf M hM
  have hbelow_b : CtxBelow (Φ + 1) { ctx with env := PolyTy.mkTrivial (.fvar Φ) :: ctx.env } := by
    intro M hM
    rcases List.mem_cons.mp hM with rfl | hM
    · exact .fvar (by omega)
    · exact (hbelow M hM).mono (by omega)
  have hT_lc : ∀ p ∈ Subst.conj (swapNat Φ W) S₀ ++ [(Φ, Ty.rename (swapNat Φ W) paramTy)],
      p.2.IsLC := by
    intro p hp
    rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact Subst.conj_lc hS₀ p hp
    · rw [List.mem_singleton] at hp
      subst hp
      exact Ty.rename_isLC hparamLC
  have hSconjK : ∀ k ∈ K, (Subst.conj (swapNat Φ W) S₀
      ++ [(Φ, Ty.rename (swapNat Φ W) paramTy)]).onTy (.fvar k) = .fvar k := by
    intro k hk
    have hklt := hKΦ k hk
    rw [Subst.onTy_append]
    have hconj : (Subst.conj (swapNat Φ W) S₀).onTy (.fvar k) = .fvar k := by
      have h := Subst.onTy_conj finj S₀ (.fvar k)
      rw [Ty.rename_fvar, hfix k hklt] at h
      rw [h, hKfix k hk, Ty.rename_fvar, hfix k hklt]
    rw [hconj]
    exact Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
      rw [List.mem_singleton] at hp; subst hp
      simp only [Ty.freeVars, List.mem_singleton] at hc; omega)
  obtain ⟨Φ', S, eOutb, τb, R_b, hinfb, hagb, htyb, hR_b, hR_bfix, hSK⟩ :=
    ih K hwf_b hbelow_b hT_lc (fun k hk => by have := hKΦ k hk; omega) hbodyK hSconjK hren
  -- STEP 3: assemble the conclusion
  refine ⟨Φ', S, .lambda none eOutb, .arrow (S.onTy (.fvar Φ)) τb, R_b ++ [(W, .fvar Φ)], ?_, ?_, ?_, ?_, ?_, hSK⟩
  · exact Infer.lambda .none hinfb
  · -- agreement below Φ
    intro v hv
    have hWv : W ∉ (Ty.fvar v).freeVars := by
      simp only [Ty.freeVars, List.mem_singleton]; omega
    have hconjv : (Subst.conj (swapNat Φ W) S₀).onTy (.fvar v)
        = Ty.rename (swapNat Φ W) (S₀.onTy (.fvar v)) := by
      have h := Subst.onTy_conj finj S₀ (.fvar v)
      rw [Ty.rename_fvar, hfix v hv] at h
      exact h
    have hTv : (Subst.conj (swapNat Φ W) S₀ ++ [(Φ, Ty.rename (swapNat Φ W) paramTy)]).onTy (.fvar v)
        = Ty.rename (swapNat Φ W) (S₀.onTy (.fvar v)) := by
      rw [Subst.onTy_append (Subst.conj (swapNat Φ W) S₀)
            [(Φ, Ty.rename (swapNat Φ W) paramTy)] (.fvar v), hconjv]
      exact Ty.substFvar_fresh (Ty.rename_swap_not_mem_left (hWonTy (τ := .fvar v) hWv))
    rw [← List.append_assoc, Subst.onTy_append (S ++ R_b) [(W, .fvar Φ)] (.fvar v),
        ← hagb v (by omega), hTv]
    exact (Ty.substFvar_rename_swap (hWonTy (τ := .fvar v) hWv)).symm
  · -- the arrow type is recovered
    have hTΦ : (Subst.conj (swapNat Φ W) S₀ ++ [(Φ, Ty.rename (swapNat Φ W) paramTy)]).onTy (.fvar Φ)
        = Ty.rename (swapNat Φ W) paramTy := by
      rw [Subst.onTy_append (Subst.conj (swapNat Φ W) S₀)
            [(Φ, Ty.rename (swapNat Φ W) paramTy)] (.fvar Φ), hSconjΦ]
      simp only [Subst.onTy, Ty.substFvars, Ty.substFvar, if_pos]
    rw [Subst.onTy_arrow]
    refine congrArg₂ Ty.arrow ?_ ?_
    · rw [Subst.onTy_append R_b [(W, .fvar Φ)] (S.onTy (.fvar Φ)),
          ← Subst.onTy_append S R_b (.fvar Φ),
          ← hagb Φ (by omega), hTΦ]
      exact (Ty.substFvar_rename_swap hWparam).symm
    · rw [Subst.onTy_append R_b [(W, .fvar Φ)] τb, ← htyb]
      exact (Ty.substFvar_rename_swap hWbody).symm
  · -- residual is LC
    intro p hp
    rw [List.mem_append] at hp
    rcases hp with hp | hp
    · exact hR_b p hp
    · rw [List.mem_singleton] at hp
      subst hp
      exact ContainsBvarsUpTo.fvar
  · -- residual fixes the rigid set `K`
    intro k hk
    rw [Subst.onTy_append, hR_bfix k hk]
    exact Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
      rw [List.mem_singleton] at hp; subst hp
      simp only [Ty.freeVars, List.mem_singleton] at hc
      have := hKΦ k hk; omega)

/-- Principality, annotated lambda case (named binders). The annotation `T` may
    now carry free *scoped* type variables; they are rigid — members of the
    ambient set `K`, fixed by `S₀` — so `S₀.onTy T = T`, and the residual
    obligation `R.onTy (S.onTy T) = T` is discharged via the agreement clause
    (`S ++ R` agrees with `S₀` below `Φ`) plus `S₀` fixing `K`. -/
theorem Infer.complete_lambda_ann_aux {body : Expr} {Φ : Nat} {ctx : Ctx}
    {S₀ : Subst} {T bodyTy : Ty} {K : List Nat}
    (ih : Infer.CompleteAt body)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hS₀ : ∀ p ∈ S₀, p.2.IsLC)
    (hTlc : T.IsLC) (hKΦ : ∀ k ∈ K, k < Φ)
    (hTK : ∀ y ∈ T.freeVars, y ∈ K) (hbodyK : ∀ y ∈ body.tyFreeVars, y ∈ K)
    (hKfix : ∀ k ∈ K, S₀.onTy (.fvar k) = .fvar k)
    (hbodyty : TypeOfHM { (S₀.onCtx ctx) with
        env := PolyTy.mkTrivial T :: (S₀.onCtx ctx).env } body bodyTy) :
    ∃ Φ' S eOut τ R,
      Infer Φ ctx (.lambda (some T) body) Φ' S eOut τ ∧
      Subst.AgreesBelow Φ S₀ (S ++ R) ∧
      Ty.arrow T bodyTy = R.onTy τ ∧
      (∀ p ∈ R, p.2.IsLC) ∧
      (∀ k ∈ K, R.onTy (.fvar k) = .fvar k) ∧
      (∀ p ∈ S, p.1 ∉ K) := by
  have hTbelow : Ty.BelowFvars Φ T := Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hTK v hv))
  have hself : S₀.onTy T = T := Subst.onTy_eq_self_of_fixes (fun v hv => hKfix v (hTK v hv))
  have hwf' : CtxWF { ctx with env := PolyTy.mkTrivial T :: ctx.env } := by
    intro M hM; rcases List.mem_cons.mp hM with rfl | hM
    · exact hTlc
    · exact hwf M hM
  have hbelow' : CtxBelow Φ { ctx with env := PolyTy.mkTrivial T :: ctx.env } := by
    intro M hM; rcases List.mem_cons.mp hM with rfl | hM
    · exact hTbelow
    · exact hbelow M hM
  have hbody' : TypeOfHM (S₀.onCtx { ctx with env := PolyTy.mkTrivial T :: ctx.env }) body bodyTy := by
    have heq2 : S₀.onCtx { ctx with env := PolyTy.mkTrivial T :: ctx.env }
        = { (S₀.onCtx ctx) with env := PolyTy.mkTrivial T :: (S₀.onCtx ctx).env } := by
      simp only [Subst.onCtx, Subst.onEnv, List.map_cons, Subst.onPolyTy, PolyTy.mkTrivial, hself]
    rw [heq2]; exact hbodyty
  obtain ⟨Φ', S, eOutb, τ, R, hinfb, hag, htyb, hRlc, hRfix, hSK⟩ :=
    ih K hwf' hbelow' hS₀ hKΦ hbodyK hKfix hbody'
  refine ⟨Φ', S, _, .arrow (S.onTy T) τ, R, Infer.lambda (.some T hTlc) hinfb, hag, ?_, hRlc, hRfix, hSK⟩
  rw [Subst.onTy_arrow]
  refine congrArg₂ Ty.arrow ?_ htyb
  rw [← Subst.onTy_append, ← Subst.onTy_congr hag hTbelow, hself]

/-- Principality, lambda case. -/
theorem Infer.complete_lambda {ann : Option Ty} {body : Expr}
    (ih : Infer.CompleteAt body) : Infer.CompleteAt (.lambda ann body) := by
  intro Φ ctx S₀ τ₀ K hwf hbelow hS₀ hKΦ hKe hKfix hty
  cases hty with
  | lambda hparamLC hann heq hbodyty =>
    subst heq
    cases ann with
    | none =>
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append] at hKe
      exact Infer.complete_lambda_aux ih hwf hbelow hS₀ hparamLC hKΦ hKe hKfix hbodyty
    | some T =>
      have hpeq : _ = T := hann T rfl
      subst hpeq
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at hKe
      exact Infer.complete_lambda_ann_aux ih hwf hbelow hS₀ hparamLC hKΦ
        (fun y hy => hKe y (.inl hy)) (fun y hy => hKe y (.inr hy)) hKfix hbodyty


/-! ### Block renaming (for the `var`/`letIn` cases, which allocate a block of
    fresh vars at once). Generalises the single-var swap: `blockSwap Φ W k`
    transposes `[Φ,Φ+k)` with `[W,W+k)` (disjoint when `Φ+k ≤ W`). On types that
    avoid the `W`-block, the forward list `blockList` realises it, and the
    backward list `blockListBack` is the map-back. -/

/-- Transpose the blocks `[Φ,Φ+k)` and `[W,W+k)` (disjoint when `Φ+k ≤ W`). -/
def blockSwap (Φ W k n : Nat) : Nat :=
  if Φ ≤ n ∧ n < Φ + k then n + (W - Φ)
  else if W ≤ n ∧ n < W + k then n - (W - Φ)
  else n

/-- Forward renaming list: `Φ+i ↦ .fvar (W+i)`. -/
def blockList (Φ W k : Nat) : Subst := (List.range k).map (fun i => (Φ + i, Ty.fvar (W + i)))

/-- Backward renaming list: `W+i ↦ .fvar (Φ+i)`. -/
def blockListBack (Φ W k : Nat) : Subst := (List.range k).map (fun i => (W + i, Ty.fvar (Φ + i)))

theorem blockSwap_lt {Φ W k n : Nat} (hle : Φ ≤ W) (h : n < Φ) :
    blockSwap Φ W k n = n := by
  simp only [blockSwap]; split_ifs <;> omega

theorem blockSwap_involutive {Φ W k : Nat} (hd : Φ + k ≤ W) (n : Nat) :
    blockSwap Φ W k (blockSwap Φ W k n) = n := by
  simp only [blockSwap]; split_ifs <;> omega

theorem blockSwap_injective {Φ W k : Nat} (hd : Φ + k ≤ W) :
    Function.Injective (blockSwap Φ W k) :=
  Function.Involutive.injective (blockSwap_involutive hd)

theorem blockList_lc (Φ W k : Nat) : ∀ p ∈ blockList Φ W k, p.2.IsLC := by
  intro p hp
  simp only [blockList, List.mem_map] at hp
  obtain ⟨i, _, rfl⟩ := hp
  exact ContainsBvarsUpTo.fvar

theorem blockListBack_lc (Φ W k : Nat) : ∀ p ∈ blockListBack Φ W k, p.2.IsLC := by
  intro p hp
  simp only [blockListBack, List.mem_map] at hp
  obtain ⟨i, _, rfl⟩ := hp
  exact ContainsBvarsUpTo.fvar

/-- A `range`-indexed list of single-var substitutions `a+i ↦ .fvar (b+i)`
    acts on a free variable `n` exactly like the block transposition: if `n`
    is in `[a, a+k)` it becomes `n - a + b`, otherwise it is unchanged. The
    disjointness premise prevents a relabelled var from being touched again. -/
private theorem rangeMapList_onTy_fvar (a b : Nat) (k : Nat)
    (hdisj : a + k ≤ b ∨ b + k ≤ a) (n : Nat) :
    Subst.onTy ((List.range k).map (fun i => (a + i, Ty.fvar (b + i)))) (Ty.fvar n)
      = Ty.fvar (if a ≤ n ∧ n < a + k then n - a + b else n) := by
  induction k with
  | zero =>
    simp only [List.range_zero, List.map_nil, Subst.onTy_nil, Nat.add_zero]
    split_ifs <;> first | rfl | omega
  | succ k ih =>
    have hdisj' : a + k ≤ b ∨ b + k ≤ a := by omega
    simp only [List.range_succ, List.map_append, List.map_cons, List.map_nil,
               Subst.onTy_append]
    rw [ih hdisj']
    simp only [Subst.onTy, Ty.substFvars, Ty.substFvar]
    split_ifs <;> first | rfl | omega | (rw [Ty.fvar.injEq]; omega)

/-- The forward list realises `blockSwap` on `W`-block-avoiding types. -/
theorem blockList_onTy {Φ W k : Nat} (hd : Φ + k ≤ W) {τ : Ty}
    (hτ : ∀ v ∈ τ.freeVars, ¬ (W ≤ v ∧ v < W + k)) :
    (blockList Φ W k).onTy τ = Ty.rename (blockSwap Φ W k) τ := by
  induction τ using Ty.rec_strong with
  | prim p => simp only [Subst.onTy_prim, Ty.rename_prim]
  | bvar i => simp only [Subst.onTy_bvar, Ty.rename_bvar]
  | arrow a b iha ihb =>
    simp only [Subst.onTy_arrow, Ty.rename_arrow]
    refine congrArg₂ Ty.arrow (iha (fun v hv => hτ v ?_)) (ihb (fun v hv => hτ v ?_))
    · simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inl hv
    · simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inr hv
  | fvar n =>
    have hWn : ¬ (W ≤ n ∧ n < W + k) :=
      hτ n (by simp only [Ty.freeVars, List.mem_singleton])
    simp only [blockList]
    rw [rangeMapList_onTy_fvar Φ W k (Or.inl hd) n, Ty.rename_fvar]
    simp only [blockSwap]
    split_ifs <;> first | rfl | omega | (rw [Ty.fvar.injEq]; omega)
  | customTy nm tys ih =>
    simp only [Subst.onTy_customTy, Ty.rename_customTy, Ty.customTy.injEq, true_and]
    apply List.map_congr_left
    intro t ht
    exact ih t ht (fun v hv => hτ v (TyList.mem_freeVars_of_mem ht hv))

/-- Map-back: the backward list undoes `blockSwap` on `W`-block-avoiding types. -/
theorem blockListBack_onTy_rename {Φ W k : Nat} (hd : Φ + k ≤ W) {X : Ty}
    (hX : ∀ v ∈ X.freeVars, ¬ (W ≤ v ∧ v < W + k)) :
    (blockListBack Φ W k).onTy (Ty.rename (blockSwap Φ W k) X) = X := by
  induction X using Ty.rec_strong with
  | prim p => simp only [Ty.rename_prim, Subst.onTy_prim]
  | bvar i => simp only [Ty.rename_bvar, Subst.onTy_bvar]
  | arrow a b iha ihb =>
    simp only [Ty.rename_arrow, Subst.onTy_arrow]
    refine congrArg₂ Ty.arrow (iha (fun v hv => hX v ?_)) (ihb (fun v hv => hX v ?_))
    · simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inl hv
    · simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inr hv
  | fvar n =>
    have hWn : ¬ (W ≤ n ∧ n < W + k) :=
      hX n (by simp only [Ty.freeVars, List.mem_singleton])
    simp only [Ty.rename_fvar, blockListBack]
    rw [rangeMapList_onTy_fvar W Φ k (Or.inr hd) (blockSwap Φ W k n)]
    simp only [blockSwap]
    split_ifs <;> first | rfl | omega | (rw [Ty.fvar.injEq]; omega)
  | customTy nm tys ih =>
    simp only [Ty.rename_customTy, Subst.onTy_customTy, List.map_map, Ty.customTy.injEq,
               true_and]
    conv_rhs => rw [← List.map_id tys]
    apply List.map_congr_left
    intro t ht
    exact ih t ht (fun v hv => hX v (TyList.mem_freeVars_of_mem ht hv))

/-- The `Φ`-block is absent after renaming a `W`-block-avoiding type. -/
theorem blockSwap_rename_not_mem {Φ W k : Nat} (hd : Φ + k ≤ W) {Y : Ty}
    (hY : ∀ v ∈ Y.freeVars, ¬ (W ≤ v ∧ v < W + k)) :
    ∀ v, Φ ≤ v → v < Φ + k → v ∉ (Ty.rename (blockSwap Φ W k) Y).freeVars := by
  induction Y using Ty.rec_strong with
  | prim p => simp [Ty.freeVars]
  | bvar i => simp [Ty.freeVars]
  | fvar n =>
    intro v hv1 hv2
    have hWn : ¬ (W ≤ n ∧ n < W + k) :=
      hY n (by simp only [Ty.freeVars, List.mem_singleton])
    simp only [Ty.rename_fvar, Ty.freeVars, List.mem_singleton, blockSwap]
    split_ifs <;> omega
  | arrow a b iha ihb =>
    intro v hv1 hv2
    simp only [Ty.rename_arrow, Ty.freeVars, List.mem_dedup, List.mem_append, not_or]
    refine ⟨iha (fun w hw => hY w ?_) v hv1 hv2, ihb (fun w hw => hY w ?_) v hv1 hv2⟩
    · simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inl hw
    · simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact Or.inr hw
  | customTy nm tys ih =>
    intro v hv1 hv2
    simp only [Ty.rename_customTy, Ty.freeVars]
    rw [TyList.not_mem_freeVars_iff]
    intro t' ht'
    obtain ⟨t, ht, rfl⟩ := List.mem_map.mp ht'
    exact ih t ht (fun w hw => hY w (TyList.mem_freeVars_of_mem ht hw)) v hv1 hv2

/-- Refinement of `Ty.substFvars_zip_fvar_eq` needing freshness only of the
    *selected* value `v` (not all of `Vs`), and no length condition. Substituting
    along `Xs.zip Vs` sends `.fvar Xs[i]` to `Vs[i]`. -/
theorem Ty.substFvars_zip_fvar_eq' {Xs : List Nat} {Vs : List Ty} {i : Nat} {x : Nat} {v : Ty}
    (h_nodup : Xs.Nodup) (hx : Xs[i]? = some x) (hv : Vs[i]? = some v)
    (h_fresh : ∀ X ∈ Xs, X ∉ v.freeVars) :
    Ty.substFvars (Xs.zip Vs) (.fvar x) = v := by
  induction Xs generalizing Vs i x v with
  | nil => simp at hx
  | cons X0 Xs' ih =>
    cases Vs with
    | nil => simp at hv
    | cons V0 Vs' =>
      cases i with
      | zero =>
        simp only [List.getElem?_cons_zero, Option.some.injEq] at hx hv
        simp only [List.zip_cons_cons, Ty.substFvars]
        rw [← hx, show Ty.substFvar X0 V0 (.fvar X0) = V0 by simp [Ty.substFvar], ← hv]
        apply Ty.substFvars_eq_self_of_no_key
        intro p hp hcontra
        have hp1 : p.1 ∈ Xs' := (List.of_mem_zip hp).1
        exact h_fresh p.1 (List.mem_cons_of_mem _ hp1) (hv ▸ hcontra)
      | succ k =>
        simp only [List.getElem?_cons_succ] at hx hv
        have h_X0_notin : X0 ∉ Xs' := (List.nodup_cons.mp h_nodup).1
        have h_ne : x ≠ X0 := fun h => h_X0_notin (h ▸ List.mem_of_getElem? hx)
        simp only [List.zip_cons_cons, Ty.substFvars]
        rw [show Ty.substFvar X0 V0 (.fvar x) = .fvar x by simp [Ty.substFvar, h_ne]]
        exact ih (List.nodup_cons.mp h_nodup).2 hx hv
          (fun X hX => h_fresh X (List.mem_cons_of_mem _ hX))

/-- Bridge for the `var` completeness case: if `ty` instantiates to `τ` under
    `tyArgs`, then opening `ty` with fresh names `Xs` (nodup, fresh for `τ`) and
    substituting `Xs ↦ tyArgs` recovers `τ`. Only the result `τ`'s freshness is
    needed (used `tyArgs` are subterms of `τ`); unused `tyArgs` never matter, as
    the induction only visits `ty`'s actual bound vars. -/
theorem InstantiatesBy.onTy_openVars_zip {Xs : List Nat} {ty τ : Ty} {tyArgs : List Ty}
    (hinst : InstantiatesBy tyArgs ty τ)
    (hbv : ContainsBvarsUpTo Xs.length ty)
    (hnodup : Xs.Nodup)
    (hXfresh : ∀ x ∈ Xs, x ∉ τ.freeVars) :
    Subst.onTy (Xs.zip tyArgs) (Ty.openVars Xs ty) = τ := by
  induction ty using Ty.rec_strong generalizing τ with
  | prim p => cases hinst; simp only [Ty.openVars_prim, Subst.onTy_prim]
  | fvar n =>
    cases hinst
    simp only [Ty.openVars, Ty.instantiate]
    apply Ty.substFvars_eq_self_of_no_key
    intro p hp hc
    simp only [Ty.freeVars, List.mem_singleton] at hc
    subst hc
    exact hXfresh p.1 (List.of_mem_zip hp).1 (by simp [Ty.freeVars])
  | bvar i =>
    cases hinst with
    | bvar hsome =>
      cases hbv with
      | bvar hlt =>
        have hxi : Xs[i]? = some Xs[i] := List.getElem?_eq_getElem hlt
        simp only [Ty.openVars, Ty.instantiate, hxi, Option.elim_some]
        exact Ty.substFvars_zip_fvar_eq' hnodup hxi hsome hXfresh
  | arrow a b iha ihb =>
    cases hinst with
    | arrow ha hb =>
      cases hbv with
      | arrow hba hbb =>
        simp only [Ty.openVars_arrow, Subst.onTy_arrow]
        refine congrArg₂ Ty.arrow (iha ha hba ?_) (ihb hb hbb ?_)
        · intro x hx hc; exact hXfresh x hx (by
            simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inl hc)
        · intro x hx hc; exact hXfresh x hx (by
            simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inr hc)
  | customTy nm tys ih =>
    cases hinst with
    | customTy hforall =>
      cases hbv with
      | customTy hball =>
        simp only [Ty.openVars_customTy, Subst.onTy_customTy]
        refine congrArg (Ty.customTy nm) ?_
        induction hforall with
        | nil => rfl
        | cons hhd htl ihtl =>
          rename_i a instA tys' instTys'
          simp only [List.map_cons, List.cons.injEq]
          refine ⟨ih a List.mem_cons_self hhd (hball a List.mem_cons_self) ?_, ?_⟩
          · intro x hx hc
            apply hXfresh x hx
            simp only [Ty.freeVars, TyList.freeVars, List.mem_dedup, List.mem_append]
            exact Or.inl hc
          · refine ihtl (fun t ht => ih t (List.mem_cons_of_mem _ ht)) ?_
              (fun t ht => hball t (List.mem_cons_of_mem _ ht))
            intro x hx hc
            apply hXfresh x hx
            simp only [Ty.freeVars, TyList.freeVars, List.mem_dedup, List.mem_append] at hc ⊢
            exact Or.inr hc

/-- A single fresh name `W` starting a block `[W,W+k)` disjoint from `[Φ,Φ+k)`
    and above a finite `avoid` set. -/
theorem exists_fresh_block (avoid : List Nat) (Φ k : Nat) :
    ∃ W, Φ + k ≤ W ∧ ∀ v ∈ avoid, v < W := by
  refine ⟨avoid.foldr max 0 + Φ + k + 1, by omega, ?_⟩
  intro v hv
  have := List.le_foldr_max hv
  omega

/-- Principality, variable case. The algorithm opens the looked-up scheme with a
    fresh block `[Φ,Φ+pc)`; these may clash with `S₀`'s range or the declarative
    instantiation `tyArgs`, so we α-rename the derivation by the block-swap
    `[Φ,Φ+pc) ↔ [W,W+pc)` (`W` fresh), read off the residual from the clean
    (block-avoiding) renamed instantiation via `InstantiatesBy.onTy_openVars_zip`,
    then map back with `blockListBack`. -/
theorem Infer.complete_var {i : Nat} {tyArgs : List Ty} : Infer.CompleteAt (.var i tyArgs) := by
  intro Φ ctx S₀ τ₀ K hwf hbelow hS₀ hKΦ hKe hKfix hty
  -- STEP 0: looked-up scheme from `ctx`, without consuming `hty`.
  obtain ⟨polyTy, hlook_orig⟩ : ∃ pt, ctx.env[i]? = some pt := by
    cases hty with
    | var hlook _ _ =>
      have hmap : (S₀.onCtx ctx).env[i]? = (ctx.env[i]?).map S₀.onPolyTy := by
        show (ctx.env.map S₀.onPolyTy)[i]? = _
        rw [List.getElem?_map]
      rw [hmap] at hlook
      obtain ⟨pt, h, _⟩ := Option.map_eq_some_iff.mp hlook
      exact ⟨pt, h⟩
  have hmem : polyTy ∈ ctx.env := List.mem_of_getElem? hlook_orig
  set pc := polyTy.paramCount with hpc
  have hwfpoly : ContainsBvarsUpTo pc polyTy.body := hwf polyTy hmem
  -- STEP 1: fresh block start `W`.
  obtain ⟨W, hd, hWfresh⟩ := exists_fresh_block
    (S₀.map Prod.fst ++ S₀.flatMap (fun p => p.2.freeVars) ++ τ₀.freeVars) Φ pc
  have hτ₀W : ∀ v ∈ τ₀.freeVars, ¬ (W ≤ v ∧ v < W + pc) := by
    intro v hv hc
    have := hWfresh v (List.mem_append_right _ hv)
    omega
  have hSrange_lt : ∀ p ∈ S₀, ∀ v ∈ p.2.freeVars, v < W := by
    intro p hp v hv
    exact hWfresh v (List.mem_append_left _
      (List.mem_append_right _ (List.mem_flatMap.mpr ⟨p, hp, hv⟩)))
  have hSkey_lt : ∀ p ∈ S₀, p.1 < W := by
    intro p hp
    exact hWfresh p.1 (List.mem_append_left _
      (List.mem_append_left _ (List.mem_map.mpr ⟨p, hp, rfl⟩)))
  have hS₀belowW : ∀ p ∈ S₀, Ty.BelowFvars W p.2 :=
    fun p hp => Ty.BelowFvars.of_freeVars_lt (fun v hv => hSrange_lt p hp v hv)
  have hWblock_of_belowW : ∀ {t : Ty}, Ty.BelowFvars W t →
      ∀ v ∈ t.freeVars, ¬ (W ≤ v ∧ v < W + pc) :=
    fun {t} ht v hv => by have := ht.mem_lt v hv; omega
  have finj : Function.Injective (blockSwap Φ W pc) := blockSwap_injective hd
  have hffix : ∀ v, v < Φ → blockSwap Φ W pc v = v := fun v hv => blockSwap_lt (by omega) hv
  -- STEP 2: rename `hty` by the block-swap, reinterpret as a `conj`.
  have hren := TypeOfHM.onSubst_fixed (blockList Φ W pc) (blockList_lc Φ W pc)
    (Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => by
      simp only [blockList, List.mem_map, List.mem_range] at hp
      obtain ⟨j, _, rfl⟩ := hp
      have := hKΦ _ (hKe _ hc); omega)) hty
  have hctxeq : (blockList Φ W pc).onCtx (S₀.onCtx ctx)
      = (Subst.conj (blockSwap Φ W pc) S₀).onCtx ctx := by
    simp only [Subst.onCtx, Subst.onEnv, List.map_map]
    congr 1
    apply List.map_congr_left
    intro M hM
    simp only [Function.comp_apply, Subst.onPolyTy]
    congr 1
    rw [blockList_onTy hd
        (hWblock_of_belowW (Subst.onTy_belowFvars hS₀belowW ((hbelow M hM).mono (by omega))))]
    conv_rhs => rw [← Ty.rename_eq_self (f := blockSwap Φ W pc) (τ := M.body)
      (fun v hv => hffix v ((hbelow M hM).mem_lt v hv))]
    rw [Subst.onTy_conj finj]
  have htyeq : (blockList Φ W pc).onTy τ₀ = Ty.rename (blockSwap Φ W pc) τ₀ :=
    blockList_onTy hd hτ₀W
  have hren2 : TypeOfHM ((Subst.conj (blockSwap Φ W pc) S₀).onCtx ctx) (.var i tyArgs)
      (Ty.rename (blockSwap Φ W pc) τ₀) := by
    rw [hctxeq, htyeq] at hren
    exact hren
  -- STEP 3: invert renamed derivation. (`TypeOfHM.var` gives an *existential*
  -- instantiation witness `instArgs2`; the node `tyArgs` decoration is irrelevant.)
  cases hren2 with
  | @var _ polyTy2 instArgs2 _ _ _ hlook2 hbvars2 hinst2 =>
    have hlook2' : ((Subst.conj (blockSwap Φ W pc) S₀).onCtx ctx).env[i]?
        = some ((Subst.conj (blockSwap Φ W pc) S₀).onPolyTy polyTy) := by
      show (ctx.env.map (Subst.conj (blockSwap Φ W pc) S₀).onPolyTy)[i]? = _
      simp only [List.getElem?_map, hlook_orig, Option.map_some]
    have hpolyTy2 : polyTy2 = (Subst.conj (blockSwap Φ W pc) S₀).onPolyTy polyTy :=
      Option.some.inj (hlook2.symm.trans hlook2')
    subst hpolyTy2
    simp only [Subst.onPolyTy] at hinst2
    -- STEP 4: assemble.
    have hdomfresh : ∀ p ∈ Subst.conj (blockSwap Φ W pc) S₀, p.1 ∉ freshVars Φ pc := by
      intro p hp hmemf
      simp only [Subst.conj, List.mem_map] at hp
      obtain ⟨q, hq, rfl⟩ := hp
      have hq1 : q.1 < W := hSkey_lt q hq
      have hge := freshVars_ge _ hmemf
      have hlt := freshVars_lt _ hmemf
      simp only [blockSwap] at hge hlt
      split_ifs at hge hlt <;> omega
    have hbv2 : ContainsBvarsUpTo (freshVars Φ pc).length
        ((Subst.conj (blockSwap Φ W pc) S₀).onTy polyTy.body) := by
      rw [freshVars_length]
      exact Subst.onTy_containsBvars (Subst.conj_lc hS₀) hwfpoly
    have hXfresh2 : ∀ x ∈ freshVars Φ pc, x ∉ (Ty.rename (blockSwap Φ W pc) τ₀).freeVars :=
      fun x hx => blockSwap_rename_not_mem hd hτ₀W x (freshVars_ge x hx) (freshVars_lt x hx)
    have hR'eq : Subst.onTy (Subst.conj (blockSwap Φ W pc) S₀ ++ (freshVars Φ pc).zip instArgs2)
        (polyTy.openVars (freshVars Φ pc)) = Ty.rename (blockSwap Φ W pc) τ₀ := by
      rw [Subst.onTy_append]
      simp only [PolyTy.openVars]
      rw [Subst.onTy_openVars (Subst.conj_lc hS₀) hdomfresh]
      exact InstantiatesBy.onTy_openVars_zip hinst2 hbv2 freshVars_nodup hXfresh2
    have hagree : Subst.AgreesBelow Φ S₀
        (([] : Subst) ++ ((Subst.conj (blockSwap Φ W pc) S₀ ++ (freshVars Φ pc).zip instArgs2)
          ++ blockListBack Φ W pc)) := by
      intro v hv
      have hbelowfv : Ty.BelowFvars W (S₀.onTy (.fvar v)) :=
        Subst.onTy_belowFvars hS₀belowW (Ty.BelowFvars.fvar (show v < W by omega))
      have hconjv : Subst.onTy (Subst.conj (blockSwap Φ W pc) S₀) (.fvar v)
          = Ty.rename (blockSwap Φ W pc) (S₀.onTy (.fvar v)) := by
        conv_lhs => rw [show (Ty.fvar v) = Ty.rename (blockSwap Φ W pc) (Ty.fvar v) by
          rw [Ty.rename_fvar, hffix v hv]]
        rw [Subst.onTy_conj finj]
      have hzipnoop : Subst.onTy ((freshVars Φ pc).zip instArgs2)
          (Ty.rename (blockSwap Φ W pc) (S₀.onTy (.fvar v)))
          = Ty.rename (blockSwap Φ W pc) (S₀.onTy (.fvar v)) :=
        Ty.substFvars_eq_self_of_no_key (fun p hp =>
          blockSwap_rename_not_mem hd (hWblock_of_belowW hbelowfv) p.1
            (freshVars_ge p.1 (List.of_mem_zip hp).1) (freshVars_lt p.1 (List.of_mem_zip hp).1))
      have hback : Subst.onTy (blockListBack Φ W pc)
          (Ty.rename (blockSwap Φ W pc) (S₀.onTy (.fvar v))) = S₀.onTy (.fvar v) :=
        blockListBack_onTy_rename hd (hWblock_of_belowW hbelowfv)
      rw [List.nil_append, Subst.onTy_append, Subst.onTy_append, hconjv, hzipnoop, hback]
    refine ⟨Φ + pc, [], _, polyTy.openVars (freshVars Φ pc),
      (Subst.conj (blockSwap Φ W pc) S₀ ++ (freshVars Φ pc).zip instArgs2) ++ blockListBack Φ W pc,
      Infer.var hlook_orig, hagree, ?_, ?_,
      fun k hk => (hagree k (hKΦ k hk)).symm.trans (hKfix k hk), by simp⟩
    · -- declarative type factors through the residual
      rw [Subst.onTy_append, hR'eq, blockListBack_onTy_rename hd hτ₀W]
    · -- residual is locally closed
      intro p hp
      rw [List.mem_append] at hp
      rcases hp with hp | hp
      · rw [List.mem_append] at hp
        rcases hp with hp | hp
        · exact Subst.conj_lc hS₀ p hp
        · exact hbvars2 p.2 (List.of_mem_zip hp).2
      · exact blockListBack_lc Φ W pc p hp

/-- Principality, constructor case. Structurally identical to `complete_var`,
    but the looked-up scheme `ctor.toTy` lives in `ctx.ctors` (untouched by
    `S₀.onCtx`) and its body is closed (`Ctor.toTy_body_noFreeVars`), so the
    substitution `S₀`/`conj` never fires on it. -/
theorem Infer.complete_ctor {name : CtorName} : Infer.CompleteAt (.ctor name) := by
  intro Φ ctx S₀ τ₀ K hwf hbelow hS₀ hKΦ _ hKfix hty
  -- STEP 0: looked-up ctor from `ctx.ctors` (unchanged by `S₀.onCtx`), without consuming `hty`.
  obtain ⟨ctor, hlook_orig⟩ : ∃ c, LookupList.get? ctx.ctors name = some c := by
    cases hty with
    | ctor hlook _ _ => exact ⟨_, hlook⟩
  set pc := ctor.paramCount with hpc
  have hwfpoly : ContainsBvarsUpTo pc ctor.toTy.body := Ctor.toTy_wf ctor
  -- STEP 1: fresh block start `W`.
  obtain ⟨W, hd, hWfresh⟩ := exists_fresh_block
    (S₀.map Prod.fst ++ S₀.flatMap (fun p => p.2.freeVars) ++ τ₀.freeVars) Φ pc
  have hτ₀W : ∀ v ∈ τ₀.freeVars, ¬ (W ≤ v ∧ v < W + pc) := by
    intro v hv hc
    have := hWfresh v (List.mem_append_right _ hv)
    omega
  have hSrange_lt : ∀ p ∈ S₀, ∀ v ∈ p.2.freeVars, v < W := by
    intro p hp v hv
    exact hWfresh v (List.mem_append_left _
      (List.mem_append_right _ (List.mem_flatMap.mpr ⟨p, hp, hv⟩)))
  have hSkey_lt : ∀ p ∈ S₀, p.1 < W := by
    intro p hp
    exact hWfresh p.1 (List.mem_append_left _
      (List.mem_append_left _ (List.mem_map.mpr ⟨p, hp, rfl⟩)))
  have hS₀belowW : ∀ p ∈ S₀, Ty.BelowFvars W p.2 :=
    fun p hp => Ty.BelowFvars.of_freeVars_lt (fun v hv => hSrange_lt p hp v hv)
  have hWblock_of_belowW : ∀ {t : Ty}, Ty.BelowFvars W t →
      ∀ v ∈ t.freeVars, ¬ (W ≤ v ∧ v < W + pc) :=
    fun {t} ht v hv => by have := ht.mem_lt v hv; omega
  have finj : Function.Injective (blockSwap Φ W pc) := blockSwap_injective hd
  have hffix : ∀ v, v < Φ → blockSwap Φ W pc v = v := fun v hv => blockSwap_lt (by omega) hv
  -- STEP 2: rename `hty` by the block-swap, reinterpret as a `conj`.
  have hren := TypeOfHM.onSubst_fixed (blockList Φ W pc) (blockList_lc Φ W pc)
    (Expr.substTyFvars_eq_self_of_tyFreeVars_nil _ rfl) hty
  have hctxeq : (blockList Φ W pc).onCtx (S₀.onCtx ctx)
      = (Subst.conj (blockSwap Φ W pc) S₀).onCtx ctx := by
    simp only [Subst.onCtx, Subst.onEnv, List.map_map]
    congr 1
    apply List.map_congr_left
    intro M hM
    simp only [Function.comp_apply, Subst.onPolyTy]
    congr 1
    rw [blockList_onTy hd
        (hWblock_of_belowW (Subst.onTy_belowFvars hS₀belowW ((hbelow M hM).mono (by omega))))]
    conv_rhs => rw [← Ty.rename_eq_self (f := blockSwap Φ W pc) (τ := M.body)
      (fun v hv => hffix v ((hbelow M hM).mem_lt v hv))]
    rw [Subst.onTy_conj finj]
  have htyeq : (blockList Φ W pc).onTy τ₀ = Ty.rename (blockSwap Φ W pc) τ₀ :=
    blockList_onTy hd hτ₀W
  have hren2 : TypeOfHM ((Subst.conj (blockSwap Φ W pc) S₀).onCtx ctx) (.ctor name)
      (Ty.rename (blockSwap Φ W pc) τ₀) := by
    rw [hctxeq, htyeq] at hren
    exact hren
  -- STEP 3: invert renamed derivation.
  cases hren2 with
  | ctor hlook2 htyargs2 hinst2 =>
    rename_i ctor2 tyArgs2
    have hctor2 : ctor = ctor2 := by
      have hlook2' : LookupList.get? ctx.ctors name = some ctor2 := hlook2
      exact Option.some.inj (hlook_orig.symm.trans hlook2')
    subst hctor2
    -- STEP 4: assemble.
    have hdomfresh : ∀ p ∈ Subst.conj (blockSwap Φ W pc) S₀, p.1 ∉ freshVars Φ pc := by
      intro p hp hmemf
      simp only [Subst.conj, List.mem_map] at hp
      obtain ⟨q, hq, rfl⟩ := hp
      have hq1 : q.1 < W := hSkey_lt q hq
      have hge := freshVars_ge _ hmemf
      have hlt := freshVars_lt _ hmemf
      simp only [blockSwap] at hge hlt
      split_ifs at hge hlt <;> omega
    have hbv2 : ContainsBvarsUpTo (freshVars Φ pc).length ctor.toTy.body := by
      rw [freshVars_length]; exact hwfpoly
    have hXfresh2 : ∀ x ∈ freshVars Φ pc, x ∉ (Ty.rename (blockSwap Φ W pc) τ₀).freeVars :=
      fun x hx => blockSwap_rename_not_mem hd hτ₀W x (freshVars_ge x hx) (freshVars_lt x hx)
    have hR'eq : Subst.onTy (Subst.conj (blockSwap Φ W pc) S₀ ++ (freshVars Φ pc).zip tyArgs2)
        (ctor.toTy.openVars (freshVars Φ pc)) = Ty.rename (blockSwap Φ W pc) τ₀ := by
      rw [Subst.onTy_append]
      simp only [PolyTy.openVars]
      rw [Subst.onTy_openVars (Subst.conj_lc hS₀) hdomfresh]
      rw [show (Subst.conj (blockSwap Φ W pc) S₀).onTy ctor.toTy.body = ctor.toTy.body from
        Ty.substFvars_eq_self_of_no_key
          (fun p _ => NoFreeVars.not_mem_freeVars (Ctor.toTy_body_noFreeVars ctor) p.1)]
      exact InstantiatesBy.onTy_openVars_zip hinst2 hbv2 freshVars_nodup hXfresh2
    have hagree : Subst.AgreesBelow Φ S₀
        (([] : Subst) ++ ((Subst.conj (blockSwap Φ W pc) S₀ ++ (freshVars Φ pc).zip tyArgs2)
          ++ blockListBack Φ W pc)) := by
      intro v hv
      have hbelowfv : Ty.BelowFvars W (S₀.onTy (.fvar v)) :=
        Subst.onTy_belowFvars hS₀belowW (Ty.BelowFvars.fvar (show v < W by omega))
      have hconjv : Subst.onTy (Subst.conj (blockSwap Φ W pc) S₀) (.fvar v)
          = Ty.rename (blockSwap Φ W pc) (S₀.onTy (.fvar v)) := by
        conv_lhs => rw [show (Ty.fvar v) = Ty.rename (blockSwap Φ W pc) (Ty.fvar v) by
          rw [Ty.rename_fvar, hffix v hv]]
        rw [Subst.onTy_conj finj]
      have hzipnoop : Subst.onTy ((freshVars Φ pc).zip tyArgs2)
          (Ty.rename (blockSwap Φ W pc) (S₀.onTy (.fvar v)))
          = Ty.rename (blockSwap Φ W pc) (S₀.onTy (.fvar v)) :=
        Ty.substFvars_eq_self_of_no_key (fun p hp =>
          blockSwap_rename_not_mem hd (hWblock_of_belowW hbelowfv) p.1
            (freshVars_ge p.1 (List.of_mem_zip hp).1) (freshVars_lt p.1 (List.of_mem_zip hp).1))
      have hback : Subst.onTy (blockListBack Φ W pc)
          (Ty.rename (blockSwap Φ W pc) (S₀.onTy (.fvar v))) = S₀.onTy (.fvar v) :=
        blockListBack_onTy_rename hd (hWblock_of_belowW hbelowfv)
      rw [List.nil_append, Subst.onTy_append, Subst.onTy_append, hconjv, hzipnoop, hback]
    refine ⟨Φ + pc, [], _, ctor.toTy.openVars (freshVars Φ pc),
      (Subst.conj (blockSwap Φ W pc) S₀ ++ (freshVars Φ pc).zip tyArgs2) ++ blockListBack Φ W pc,
      Infer.ctor hlook_orig, hagree, ?_, ?_,
      fun k hk => (hagree k (hKΦ k hk)).symm.trans (hKfix k hk), by simp⟩
    · -- declarative type factors through the residual
      rw [Subst.onTy_append, hR'eq, blockListBack_onTy_rename hd hτ₀W]
    · -- residual is locally closed
      intro p hp
      rw [List.mem_append] at hp
      rcases hp with hp | hp
      · rw [List.mem_append] at hp
        rcases hp with hp | hp
        · exact Subst.conj_lc hS₀ p hp
        · exact htyargs2 p.2 (List.of_mem_zip hp).2
      · exact blockListBack_lc Φ W pc p hp


/-! ### Unification: MGU residual is LC, and unification completeness

For the `app` completeness case we need: (1) the residual `R` factoring a unifier
`S'` through the MGU `S` is locally-closed when `S'` is (so it can serve as the
next `S₀`); (2) unifiability implies a `UnifyRel` derivation exists. -/

mutual
/-- Like `UnifyRel.greatest`, but the residual `R` is also LC when `S'` is. -/
theorem UnifyRel.greatest_lc : {τ₁ τ₂ : Ty} → {S : Subst} → UnifyRel τ₁ τ₂ S →
    ∀ S' : Subst, (∀ p ∈ S', p.2.IsLC) → Unifies S' τ₁ τ₂ →
    ∃ R : Subst, (∀ τ, S'.onTy τ = R.onTy (S.onTy τ)) ∧ (∀ p ∈ R, p.2.IsLC)
  | _, _, _, .prim, S', hlc, _ => ⟨S', fun τ => by simp only [Subst.onTy_nil], hlc⟩
  | _, _, _, .fvarRefl, S', hlc, _ => ⟨S', fun τ => by simp only [Subst.onTy_nil], hlc⟩
  | _, _, _, .fvarL _ _, S', hlc, hS' => ⟨S', fun τ => (Subst.onTy_substFvar hS' τ).symm, hlc⟩
  | _, _, _, .fvarR _ _, S', hlc, hS' => ⟨S', fun τ => (Subst.onTy_substFvar (Eq.symm hS') τ).symm, hlc⟩
  | _, _, _, @UnifyRel.arrow a b c d S₁ S₂ h₁ h₂, S', hlc, hS' => by
    simp only [Unifies, Subst.onTy_arrow, Ty.arrow.injEq] at hS'
    obtain ⟨hac, hbd⟩ := hS'
    obtain ⟨R₁, hR₁, hR₁lc⟩ := UnifyRel.greatest_lc h₁ S' hlc hac
    have hR₁bd : Unifies R₁ (S₁.onTy b) (S₁.onTy d) := by
      show R₁.onTy (S₁.onTy b) = R₁.onTy (S₁.onTy d)
      rw [← hR₁ b, ← hR₁ d]; exact hbd
    obtain ⟨R₂, hR₂, hR₂lc⟩ := UnifyRel.greatest_lc h₂ R₁ hR₁lc hR₁bd
    exact ⟨R₂, fun τ => by rw [Subst.onTy_append, ← hR₂ (S₁.onTy τ), hR₁ τ], hR₂lc⟩
  | _, _, _, .customTy hl, S', hlc, hS' => by
    simp only [Unifies, Subst.onTy_customTy, Ty.customTy.injEq, true_and] at hS'
    exact UnifyRelList.greatest_lc hl S' hlc hS'

theorem UnifyRelList.greatest_lc : {ts₁ ts₂ : List Ty} → {S : Subst} →
    UnifyRelList ts₁ ts₂ S → ∀ S' : Subst, (∀ p ∈ S', p.2.IsLC) →
      ts₁.map S'.onTy = ts₂.map S'.onTy →
      ∃ R : Subst, (∀ τ, S'.onTy τ = R.onTy (S.onTy τ)) ∧ (∀ p ∈ R, p.2.IsLC)
  | _, _, _, .nil, S', hlc, _ => ⟨S', fun τ => by simp only [Subst.onTy_nil], hlc⟩
  | _, _, _, @UnifyRelList.cons t₁ t₂ ts₁ ts₂ S₁ S₂ h₁ ht, S', hlc, hS' => by
    simp only [List.map_cons, List.cons.injEq] at hS'
    obtain ⟨ht1t2, htail⟩ := hS'
    obtain ⟨R₁, hR₁, hR₁lc⟩ := UnifyRel.greatest_lc h₁ S' hlc ht1t2
    have key : ∀ (l : List Ty), l.map (R₁.onTy ∘ S₁.onTy) = l.map S'.onTy := by
      intro l; apply List.map_congr_left; intro t _; exact (hR₁ t).symm
    have hlist : (ts₁.map S₁.onTy).map R₁.onTy = (ts₂.map S₁.onTy).map R₁.onTy := by
      rw [List.map_map, List.map_map, key, key]; exact htail
    obtain ⟨R₂, hR₂, hR₂lc⟩ := UnifyRelList.greatest_lc ht R₁ hR₁lc hlist
    exact ⟨R₂, fun τ => by rw [Subst.onTy_append, ← hR₂ (S₁.onTy τ), hR₁ τ], hR₂lc⟩
end

/-! ### MGU residual preserves a rigid set `K`

For the unification-based principality cases (`app`, `fst`/`snd`, `match`) the
residual `R` that factors a witness unifier `S'` through the MGU `S` must keep
every rigid skolem `k ∈ K` fixed (so it can serve as the next `S₀` and ultimately
feed the enclosing `letInAnn`'s extended `K`). This is *not* the statement that
the MGU `S` avoids `K` — it generally does not (the left-leaning MGU can bind a
skolem facing a fresh var). Rather, `greatest` builds `R` by **threading the
witness**: at every leaf the residual *is* `S'`, and the recursive cases feed the
intermediate residual as the next witness. So "`R` fixes `K`" follows from "`S'`
fixes `K`" by the very same induction as `greatest_lc`. -/

mutual
/-- `greatest_lc` augmented: the residual `R` also fixes every `k ∈ K` whenever
    the witness `S'` does. -/
theorem UnifyRel.greatest_K {K : List Nat} :
    {τ₁ τ₂ : Ty} → {S : Subst} → UnifyRel τ₁ τ₂ S →
    ∀ S' : Subst, (∀ p ∈ S', p.2.IsLC) → Unifies S' τ₁ τ₂ →
    (∀ k ∈ K, S'.onTy (.fvar k) = .fvar k) →
    ∃ R : Subst, (∀ τ, S'.onTy τ = R.onTy (S.onTy τ)) ∧ (∀ p ∈ R, p.2.IsLC) ∧
      (∀ k ∈ K, R.onTy (.fvar k) = .fvar k)
  | _, _, _, .prim, S', hlc, _, hK => ⟨S', fun τ => by simp only [Subst.onTy_nil], hlc, hK⟩
  | _, _, _, .fvarRefl, S', hlc, _, hK => ⟨S', fun τ => by simp only [Subst.onTy_nil], hlc, hK⟩
  | _, _, _, .fvarL _ _, S', hlc, hS', hK =>
    ⟨S', fun τ => (Subst.onTy_substFvar hS' τ).symm, hlc, hK⟩
  | _, _, _, .fvarR _ _, S', hlc, hS', hK =>
    ⟨S', fun τ => (Subst.onTy_substFvar (Eq.symm hS') τ).symm, hlc, hK⟩
  | _, _, _, @UnifyRel.arrow a b c d S₁ S₂ h₁ h₂, S', hlc, hS', hK => by
    simp only [Unifies, Subst.onTy_arrow, Ty.arrow.injEq] at hS'
    obtain ⟨hac, hbd⟩ := hS'
    obtain ⟨R₁, hR₁, hR₁lc, hR₁K⟩ := UnifyRel.greatest_K h₁ S' hlc hac hK
    have hR₁bd : Unifies R₁ (S₁.onTy b) (S₁.onTy d) := by
      show R₁.onTy (S₁.onTy b) = R₁.onTy (S₁.onTy d)
      rw [← hR₁ b, ← hR₁ d]; exact hbd
    obtain ⟨R₂, hR₂, hR₂lc, hR₂K⟩ := UnifyRel.greatest_K h₂ R₁ hR₁lc hR₁bd hR₁K
    exact ⟨R₂, fun τ => by rw [Subst.onTy_append, ← hR₂ (S₁.onTy τ), hR₁ τ], hR₂lc, hR₂K⟩
  | _, _, _, .customTy hl, S', hlc, hS', hK => by
    simp only [Unifies, Subst.onTy_customTy, Ty.customTy.injEq, true_and] at hS'
    exact UnifyRelList.greatest_K hl S' hlc hS' hK
theorem UnifyRelList.greatest_K {K : List Nat} :
    {ts₁ ts₂ : List Ty} → {S : Subst} → UnifyRelList ts₁ ts₂ S →
    ∀ S' : Subst, (∀ p ∈ S', p.2.IsLC) → ts₁.map S'.onTy = ts₂.map S'.onTy →
    (∀ k ∈ K, S'.onTy (.fvar k) = .fvar k) →
    ∃ R : Subst, (∀ τ, S'.onTy τ = R.onTy (S.onTy τ)) ∧ (∀ p ∈ R, p.2.IsLC) ∧
      (∀ k ∈ K, R.onTy (.fvar k) = .fvar k)
  | _, _, _, .nil, S', hlc, _, hK => ⟨S', fun τ => by simp only [Subst.onTy_nil], hlc, hK⟩
  | _, _, _, @UnifyRelList.cons t₁ t₂ ts₁ ts₂ S₁ S₂ h₁ ht, S', hlc, hS', hK => by
    simp only [List.map_cons, List.cons.injEq] at hS'
    obtain ⟨ht1t2, htail⟩ := hS'
    obtain ⟨R₁, hR₁, hR₁lc, hR₁K⟩ := UnifyRel.greatest_K h₁ S' hlc ht1t2 hK
    have key : ∀ (l : List Ty), l.map (R₁.onTy ∘ S₁.onTy) = l.map S'.onTy := by
      intro l; apply List.map_congr_left; intro t _; exact (hR₁ t).symm
    have hlist : (ts₁.map S₁.onTy).map R₁.onTy = (ts₂.map S₁.onTy).map R₁.onTy := by
      rw [List.map_map, List.map_map, key, key]; exact htail
    obtain ⟨R₂, hR₂, hR₂lc, hR₂K⟩ := UnifyRelList.greatest_K ht R₁ hR₁lc hlist hR₁K
    exact ⟨R₂, fun τ => by rw [Subst.onTy_append, ← hR₂ (S₁.onTy τ), hR₁ τ], hR₂lc, hR₂K⟩
end

/-! A structural size on types (own measure; cleaner than `sizeOf` for the
    unification termination argument). -/
mutual
def Ty.size : Ty → Nat
  | .prim _ => 1
  | .bvar _ => 1
  | .fvar _ => 1
  | .arrow a b => 1 + a.size + b.size
  | .customTy _ tys => 1 + TyList.size tys
def TyList.size : List Ty → Nat
  | [] => 0
  | hd :: tl => hd.size + TyList.size tl
end

theorem TyList.size_mem_le {t : Ty} {tys : List Ty} (h : t ∈ tys) :
    t.size ≤ TyList.size tys := by
  induction tys with
  | nil => exact absurd h List.not_mem_nil
  | cons hd tl ih =>
    rcases List.mem_cons.mp h with rfl | h
    · simp only [TyList.size]; omega
    · have := ih h; simp only [TyList.size]; omega

@[simp] theorem Ty.size_pos : ∀ {τ : Ty}, 0 < τ.size
  | .prim _ => by simp only [Ty.size]; omega
  | .bvar _ => by simp only [Ty.size]; omega
  | .fvar _ => by simp only [Ty.size]; omega
  | .arrow _ _ => by simp only [Ty.size]; omega
  | .customTy _ _ => by simp only [Ty.size]; omega

/-- Occurs-check via size: applying any substitution, a variable's image is no
    bigger than the image of any type containing it. -/
theorem Ty.size_onTy_fvar_le {S : Subst} {n : Nat} :
    ∀ {b : Ty}, n ∈ b.freeVars → (S.onTy (.fvar n)).size ≤ (S.onTy b).size := by
  intro b
  induction b using Ty.rec_strong with
  | prim p => simp [Ty.freeVars]
  | bvar i => simp [Ty.freeVars]
  | fvar m => intro h; simp only [Ty.freeVars, List.mem_singleton] at h; subst h; exact le_refl _
  | arrow a b iha ihb =>
    intro h
    simp only [Ty.freeVars, List.mem_dedup, List.mem_append] at h
    simp only [Subst.onTy_arrow, Ty.size]
    rcases h with h | h
    · have := iha h; omega
    · have := ihb h; omega
  | customTy nm tys ih =>
    intro h
    simp only [Ty.freeVars] at h
    simp only [Subst.onTy_customTy, Ty.size]
    obtain ⟨t, ht, hnt⟩ : ∃ t ∈ tys, n ∈ t.freeVars := by
      by_contra hc; push_neg at hc
      exact (TyList.not_mem_freeVars_iff.mpr hc) h
    have h1 := ih t ht hnt
    have h2 : (S.onTy t).size ≤ TyList.size (tys.map S.onTy) :=
      TyList.size_mem_le (List.mem_map.mpr ⟨t, ht, rfl⟩)
    omega

/-- The strict occurs-check: a variable's image is strictly smaller than the
    image of a *compound* type containing it. -/
theorem Ty.size_onTy_fvar_lt {S : Subst} {n : Nat} {b : Ty}
    (hmem : n ∈ b.freeVars) (hne : b ≠ .fvar n) :
    (S.onTy (.fvar n)).size < (S.onTy b).size := by
  cases b with
  | prim p => simp [Ty.freeVars] at hmem
  | bvar i => simp [Ty.freeVars] at hmem
  | fvar m => simp only [Ty.freeVars, List.mem_singleton] at hmem; subst hmem; exact absurd rfl hne
  | arrow a b =>
    simp only [Ty.freeVars, List.mem_dedup, List.mem_append] at hmem
    simp only [Subst.onTy_arrow, Ty.size]
    rcases hmem with h | h
    · have := Ty.size_onTy_fvar_le (S := S) (n := n) h; have := @Ty.size_pos (S.onTy b); omega
    · have := Ty.size_onTy_fvar_le (S := S) (n := n) h; have := @Ty.size_pos (S.onTy a); omega
  | customTy nm tys =>
    simp only [Ty.freeVars] at hmem
    simp only [Subst.onTy_customTy, Ty.size]
    obtain ⟨t, ht, hnt⟩ : ∃ t ∈ tys, n ∈ t.freeVars := by
      by_contra hc; push_neg at hc
      exact (TyList.not_mem_freeVars_iff.mpr hc) hmem
    have h1 := Ty.size_onTy_fvar_le (S := S) (n := n) hnt
    have h2 : (S.onTy t).size ≤ TyList.size (tys.map S.onTy) :=
      TyList.size_mem_le (List.mem_map.mpr ⟨t, ht, rfl⟩)
    omega

/-- Unification completeness (+ the list version), bounded by the measure
    `2 * (size of the unified result) + flag` so a single strong induction on the
    bound `N` covers all recursive calls (`flag = 0` for `UnifyRel`, `1` for the
    list — the offset makes the singleton-list ↔ element step strictly decrease). -/
theorem UnifyRel.complete_aux : ∀ (N : Nat),
    (∀ {a b : Ty} {U : Subst}, 2 * (U.onTy a).size < N → a.IsLC → b.IsLC →
        Unifies U a b → ∃ S, UnifyRel a b S) ∧
    (∀ {as bs : List Ty} {U : Subst}, 2 * TyList.size (as.map U.onTy) + 1 < N →
        (∀ t ∈ as, t.IsLC) → (∀ t ∈ bs, t.IsLC) → as.length = bs.length →
        as.map U.onTy = bs.map U.onTy → ∃ S, UnifyRelList as bs S) := by
  intro N
  induction N with
  | zero => exact ⟨fun h => absurd h (by omega), fun h => absurd h (by omega)⟩
  | succ N ih =>
    obtain ⟨ihU, ihL⟩ := ih
    refine ⟨?_, ?_⟩
    · -- UnifyRel
      intro a b U hsz ha hb hU
      cases a with
      | bvar i => cases ha with | bvar h => omega
      | prim p =>
        cases b with
        | bvar i => cases hb with | bvar h => omega
        | prim q =>
          simp only [Unifies, Subst.onTy_prim, Ty.prim.injEq] at hU
          subst hU; exact ⟨[], .prim⟩
        | fvar m => exact ⟨[(m, .prim p)], .fvarR (by simp) (by simp [Ty.freeVars])⟩
        | arrow b₁ b₂ => simp [Unifies] at hU
        | customTy nm bs => simp [Unifies] at hU
      | fvar n =>
        cases b with
        | bvar i => cases hb with | bvar h => omega
        | fvar m =>
          by_cases hnm : n = m
          · subst hnm; exact ⟨[], .fvarRefl⟩
          · exact ⟨[(n, .fvar m)], .fvarL (by simp only [ne_eq, Ty.fvar.injEq]; omega)
              (by simp only [Ty.freeVars, List.mem_singleton]; omega)⟩
        | prim q =>
          by_cases hocc : n ∈ (Ty.prim q).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · exact ⟨[(n, .prim q)], .fvarL (by simp) hocc⟩
        | arrow b₁ b₂ =>
          by_cases hocc : n ∈ (Ty.arrow b₁ b₂).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · exact ⟨[(n, .arrow b₁ b₂)], .fvarL (by simp) hocc⟩
        | customTy nm bs =>
          by_cases hocc : n ∈ (Ty.customTy nm bs).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · exact ⟨[(n, .customTy nm bs)], .fvarL (by simp) hocc⟩
      | arrow a₁ a₂ =>
        cases ha with
        | arrow ha₁ ha₂ =>
        cases b with
        | bvar i => cases hb with | bvar h => omega
        | fvar m =>
          by_cases hocc : m ∈ (Ty.arrow a₁ a₂).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · exact ⟨[(m, .arrow a₁ a₂)], .fvarR (by simp) hocc⟩
        | prim q => simp [Unifies] at hU
        | customTy nm bs => simp [Unifies] at hU
        | arrow b₁ b₂ =>
          cases hb with
          | arrow hb₁ hb₂ =>
          have hpsz : (U.onTy (.arrow a₁ a₂)).size
              = 1 + (U.onTy a₁).size + (U.onTy a₂).size := by simp [Subst.onTy_arrow, Ty.size]
          simp only [Unifies, Subst.onTy_arrow, Ty.arrow.injEq] at hU
          obtain ⟨S₁, h₁⟩ := ihU (a := a₁) (b := b₁) (U := U)
            (by rw [hpsz] at hsz; omega) ha₁ hb₁ hU.1
          obtain ⟨R, hR⟩ := UnifyRel.greatest h₁ U hU.1
          have hS₁lc := UnifyRel.lc h₁ ha₁ hb₁
          obtain ⟨S₂, h₂⟩ := ihU (a := S₁.onTy a₂) (b := S₁.onTy b₂) (U := R)
            (by rw [← hR a₂]; rw [hpsz] at hsz; omega)
            (Subst.onTy_lc hS₁lc ha₂) (Subst.onTy_lc hS₁lc hb₂)
            (by show R.onTy (S₁.onTy a₂) = R.onTy (S₁.onTy b₂); rw [← hR a₂, ← hR b₂]; exact hU.2)
          exact ⟨S₁ ++ S₂, .arrow h₁ h₂⟩
      | customTy nm tys₁ =>
        cases b with
        | bvar i => cases hb with | bvar h => omega
        | fvar m =>
          by_cases hocc : m ∈ (Ty.customTy nm tys₁).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · exact ⟨[(m, .customTy nm tys₁)], .fvarR (by simp) hocc⟩
        | prim q => simp [Unifies] at hU
        | arrow b₁ b₂ => simp [Unifies] at hU
        | customTy nm' tys₂ =>
          have hcsz : (U.onTy (.customTy nm tys₁)).size
              = 1 + TyList.size (tys₁.map U.onTy) := by simp [Subst.onTy_customTy, Ty.size]
          simp only [Unifies, Subst.onTy_customTy, Ty.customTy.injEq] at hU
          obtain ⟨rfl, hmapeq⟩ := hU
          have hlen : tys₁.length = tys₂.length := by
            have := congrArg List.length hmapeq; simpa using this
          obtain ⟨S, hS⟩ := ihL (as := tys₁) (bs := tys₂) (U := U)
            (by rw [hcsz] at hsz; omega)
            (fun t ht => by cases ha with | customTy h => exact h t ht)
            (fun t ht => by cases hb with | customTy h => exact h t ht)
            hlen hmapeq
          exact ⟨S, .customTy hS⟩
    · -- UnifyRelList
      intro as bs U hsz has hbs hlen hmap
      cases as with
      | nil =>
        cases bs with
        | nil => exact ⟨[], .nil⟩
        | cons t₂ ts₂ => simp at hlen
      | cons t₁ ts₁ =>
        cases bs with
        | nil => simp at hlen
        | cons t₂ ts₂ =>
          simp only [List.map_cons, List.cons.injEq] at hmap
          have htsz : TyList.size ((t₁ :: ts₁).map U.onTy)
              = (U.onTy t₁).size + TyList.size (ts₁.map U.onTy) := by
            simp [List.map_cons, TyList.size]
          have ht1pos := @Ty.size_pos (U.onTy t₁)
          obtain ⟨S₁, h₁⟩ := ihU (a := t₁) (b := t₂) (U := U)
            (by rw [htsz] at hsz; omega) (has t₁ List.mem_cons_self)
            (hbs t₂ List.mem_cons_self) hmap.1
          obtain ⟨R, hR⟩ := UnifyRel.greatest h₁ U hmap.1
          have hS₁lc := UnifyRel.lc h₁ (has t₁ List.mem_cons_self) (hbs t₂ List.mem_cons_self)
          have key : ∀ l : List Ty, l.map (R.onTy ∘ S₁.onTy) = l.map U.onTy := by
            intro l; apply List.map_congr_left; intro t _; exact (hR t).symm
          have hkey1 : (ts₁.map S₁.onTy).map R.onTy = ts₁.map U.onTy := by
            rw [List.map_map]; exact key ts₁
          have hmaptail : (ts₁.map S₁.onTy).map R.onTy = (ts₂.map S₁.onTy).map R.onTy := by
            rw [List.map_map, List.map_map, key, key]; exact hmap.2
          obtain ⟨S₂, h₂⟩ := ihL (as := ts₁.map S₁.onTy) (bs := ts₂.map S₁.onTy) (U := R)
            (by rw [hkey1]; rw [htsz] at hsz; omega)
            (by
              intro t ht; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
              exact Subst.onTy_lc hS₁lc (has t0 (List.mem_cons_of_mem _ ht0)))
            (by
              intro t ht; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
              exact Subst.onTy_lc hS₁lc (hbs t0 (List.mem_cons_of_mem _ ht0)))
            (by simp only [List.length_map]; have := hlen; simpa using this)
            hmaptail
          exact ⟨S₁ ++ S₂, .cons h₁ h₂⟩

/-- Unification completeness: if `a` and `b` (both LC) have a unifier `U`, then a
    `UnifyRel` derivation exists. -/
theorem UnifyRel.complete {a b : Ty} {U : Subst}
    (ha : a.IsLC) (hb : b.IsLC) (hU : Unifies U a b) : ∃ S, UnifyRel a b S :=
  (UnifyRel.complete_aux (2 * (U.onTy a).size + 1)).1 (by omega) ha hb hU

/-! ### Rigidity-aware unification completeness

The **symmetric** `UnifyRel` can always be oriented to **avoid a rigid set `K`**,
given only that some LC unifier `U` keeps every `k ∈ K` fixed — crucially with NO
restriction that one operand's free vars lie in `K`. The structural recursion
threads the `K`-fixing witness via `greatest_K`: var–var is oriented away from
`K`, and a rigid var meeting a non-variable is *vacuous* (`U` fixes it, so it
can't equal a non-var image). The produced `S` avoids `K` by construction, so the
no-skolem-escape obligation (`hesc1`) is structural rather than a property to
re-establish — this is what replaced the old post-hoc escape-check approach. -/
theorem UnifyRel.complete_K_aux {K : List Nat} : ∀ (N : Nat),
    (∀ {a b : Ty} {U : Subst}, 2 * (U.onTy a).size < N → a.IsLC → b.IsLC →
        (∀ p ∈ U, p.2.IsLC) → Unifies U a b → (∀ k ∈ K, U.onTy (.fvar k) = .fvar k) →
        ∃ S, UnifyRel a b S ∧ (∀ p ∈ S, p.1 ∉ K)) ∧
    (∀ {as bs : List Ty} {U : Subst}, 2 * TyList.size (as.map U.onTy) + 1 < N →
        (∀ t ∈ as, t.IsLC) → (∀ t ∈ bs, t.IsLC) → (∀ p ∈ U, p.2.IsLC) →
        as.length = bs.length → as.map U.onTy = bs.map U.onTy →
        (∀ k ∈ K, U.onTy (.fvar k) = .fvar k) →
        ∃ S, UnifyRelList as bs S ∧ (∀ p ∈ S, p.1 ∉ K)) := by
  intro N
  induction N with
  | zero => exact ⟨fun h => absurd h (by omega), fun h => absurd h (by omega)⟩
  | succ N ih =>
    obtain ⟨ihU, ihL⟩ := ih
    refine ⟨?_, ?_⟩
    · intro a b U hsz ha hb hUlc hU hUK
      cases a with
      | bvar i => cases ha with | bvar h => omega
      | prim p =>
        cases b with
        | bvar i => cases hb with | bvar h => omega
        | prim q =>
          simp only [Unifies, Subst.onTy_prim, Ty.prim.injEq] at hU
          subst hU; exact ⟨[], .prim, by simp⟩
        | fvar m =>
          have hmK : m ∉ K := fun hmK => by
            have h1 : U.onTy (Ty.prim p) = U.onTy (Ty.fvar m) := hU
            rw [hUK m hmK, Subst.onTy_prim] at h1; exact absurd h1 (by simp)
          exact ⟨[(m, .prim p)], .fvarR (by simp) (by simp [Ty.freeVars]),
            by intro p' hp'; rw [List.mem_singleton] at hp'; subst hp'; exact hmK⟩
        | arrow b₁ b₂ => simp [Unifies] at hU
        | customTy nm bs => simp [Unifies] at hU
      | fvar n =>
        cases b with
        | bvar i => cases hb with | bvar h => omega
        | fvar m =>
          by_cases hnm : n = m
          · subst hnm; exact ⟨[], .fvarRefl, by simp⟩
          · by_cases hnK : n ∈ K
            · have hmK : m ∉ K := fun hmK => by
                have h1 : U.onTy (Ty.fvar n) = U.onTy (Ty.fvar m) := hU
                rw [hUK n hnK, hUK m hmK] at h1
                simp only [Ty.fvar.injEq] at h1; exact hnm h1
              exact ⟨[(m, .fvar n)], .fvarR (by simp only [ne_eq, Ty.fvar.injEq]; omega)
                (by simp only [Ty.freeVars, List.mem_singleton]; omega),
                by intro p' hp'; rw [List.mem_singleton] at hp'; subst hp'; exact hmK⟩
            · exact ⟨[(n, .fvar m)], .fvarL (by simp only [ne_eq, Ty.fvar.injEq]; omega)
                (by simp only [Ty.freeVars, List.mem_singleton]; omega),
                by intro p' hp'; rw [List.mem_singleton] at hp'; subst hp'; exact hnK⟩
        | prim q =>
          by_cases hocc : n ∈ (Ty.prim q).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · by_cases hnK : n ∈ K
            · exfalso
              have h1 : U.onTy (Ty.fvar n) = U.onTy (Ty.prim q) := hU
              rw [hUK n hnK, Subst.onTy_prim] at h1; exact absurd h1 (by simp)
            · exact ⟨[(n, .prim q)], .fvarL (by simp) hocc,
                by intro p' hp'; rw [List.mem_singleton] at hp'; subst hp'; exact hnK⟩
        | arrow b₁ b₂ =>
          by_cases hocc : n ∈ (Ty.arrow b₁ b₂).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · by_cases hnK : n ∈ K
            · exfalso
              have h1 : U.onTy (Ty.fvar n) = U.onTy (Ty.arrow b₁ b₂) := hU
              rw [hUK n hnK, Subst.onTy_arrow] at h1; exact absurd h1 (by simp)
            · exact ⟨[(n, .arrow b₁ b₂)], .fvarL (by simp) hocc,
                by intro p' hp'; rw [List.mem_singleton] at hp'; subst hp'; exact hnK⟩
        | customTy nm bs =>
          by_cases hocc : n ∈ (Ty.customTy nm bs).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · by_cases hnK : n ∈ K
            · exfalso
              have h1 : U.onTy (Ty.fvar n) = U.onTy (Ty.customTy nm bs) := hU
              rw [hUK n hnK, Subst.onTy_customTy] at h1; exact absurd h1 (by simp)
            · exact ⟨[(n, .customTy nm bs)], .fvarL (by simp) hocc,
                by intro p' hp'; rw [List.mem_singleton] at hp'; subst hp'; exact hnK⟩
      | arrow a₁ a₂ =>
        cases ha with
        | arrow ha₁ ha₂ =>
        cases b with
        | bvar i => cases hb with | bvar h => omega
        | fvar m =>
          by_cases hocc : m ∈ (Ty.arrow a₁ a₂).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · by_cases hmK : m ∈ K
            · exfalso
              have h1 : U.onTy (Ty.arrow a₁ a₂) = U.onTy (Ty.fvar m) := hU
              rw [hUK m hmK, Subst.onTy_arrow] at h1; exact absurd h1 (by simp)
            · exact ⟨[(m, .arrow a₁ a₂)], .fvarR (by simp) hocc,
                by intro p' hp'; rw [List.mem_singleton] at hp'; subst hp'; exact hmK⟩
        | prim q => simp [Unifies] at hU
        | customTy nm bs => simp [Unifies] at hU
        | arrow b₁ b₂ =>
          cases hb with
          | arrow hb₁ hb₂ =>
          have hpsz : (U.onTy (.arrow a₁ a₂)).size = 1 + (U.onTy a₁).size + (U.onTy a₂).size := by
            simp [Subst.onTy_arrow, Ty.size]
          simp only [Unifies, Subst.onTy_arrow, Ty.arrow.injEq] at hU
          obtain ⟨S₁, h₁, hS₁K⟩ := ihU (a := a₁) (b := b₁) (U := U)
            (by rw [hpsz] at hsz; omega) ha₁ hb₁ hUlc hU.1 hUK
          have hS₁lc := UnifyRel.lc h₁ ha₁ hb₁
          obtain ⟨R, hR, hRlc, hRK⟩ := UnifyRel.greatest_K h₁ U hUlc hU.1 hUK
          obtain ⟨S₂, h₂, hS₂K⟩ := ihU (a := S₁.onTy a₂) (b := S₁.onTy b₂) (U := R)
            (by rw [← hR a₂]; rw [hpsz] at hsz; omega)
            (Subst.onTy_lc hS₁lc ha₂) (Subst.onTy_lc hS₁lc hb₂) hRlc
            (by show R.onTy (S₁.onTy a₂) = R.onTy (S₁.onTy b₂); rw [← hR a₂, ← hR b₂]; exact hU.2) hRK
          exact ⟨S₁ ++ S₂, .arrow h₁ h₂, by
            intro p hp; rcases List.mem_append.mp hp with h | h
            · exact hS₁K p h
            · exact hS₂K p h⟩
      | customTy nm tys₁ =>
        cases b with
        | bvar i => cases hb with | bvar h => omega
        | fvar m =>
          by_cases hocc : m ∈ (Ty.customTy nm tys₁).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · by_cases hmK : m ∈ K
            · exfalso
              have h1 : U.onTy (Ty.customTy nm tys₁) = U.onTy (Ty.fvar m) := hU
              rw [hUK m hmK, Subst.onTy_customTy] at h1; exact absurd h1 (by simp)
            · exact ⟨[(m, .customTy nm tys₁)], .fvarR (by simp) hocc,
                by intro p' hp'; rw [List.mem_singleton] at hp'; subst hp'; exact hmK⟩
        | prim q => simp [Unifies] at hU
        | arrow b₁ b₂ => simp [Unifies] at hU
        | customTy nm' tys₂ =>
          have hcsz : (U.onTy (.customTy nm tys₁)).size = 1 + TyList.size (tys₁.map U.onTy) := by
            simp [Subst.onTy_customTy, Ty.size]
          simp only [Unifies, Subst.onTy_customTy, Ty.customTy.injEq] at hU
          obtain ⟨rfl, hmapeq⟩ := hU
          have hlen : tys₁.length = tys₂.length := by
            have := congrArg List.length hmapeq; simpa using this
          obtain ⟨S, hS, hSK⟩ := ihL (as := tys₁) (bs := tys₂) (U := U)
            (by rw [hcsz] at hsz; omega)
            (fun t ht => by cases ha with | customTy h => exact h t ht)
            (fun t ht => by cases hb with | customTy h => exact h t ht)
            hUlc hlen hmapeq hUK
          exact ⟨S, .customTy hS, hSK⟩
    · intro as bs U hsz has hbs hUlc hlen hmap hUK
      cases as with
      | nil =>
        cases bs with
        | nil => exact ⟨[], .nil, by simp⟩
        | cons t₂ ts₂ => simp at hlen
      | cons t₁ ts₁ =>
        cases bs with
        | nil => simp at hlen
        | cons t₂ ts₂ =>
          simp only [List.map_cons, List.cons.injEq] at hmap
          have htsz : TyList.size ((t₁ :: ts₁).map U.onTy)
              = (U.onTy t₁).size + TyList.size (ts₁.map U.onTy) := by simp [List.map_cons, TyList.size]
          have ht1pos := @Ty.size_pos (U.onTy t₁)
          obtain ⟨S₁, h₁, hS₁K⟩ := ihU (a := t₁) (b := t₂) (U := U)
            (by rw [htsz] at hsz; omega) (has t₁ List.mem_cons_self) (hbs t₂ List.mem_cons_self)
            hUlc hmap.1 hUK
          have hS₁lc := UnifyRel.lc h₁ (has t₁ List.mem_cons_self) (hbs t₂ List.mem_cons_self)
          obtain ⟨R, hR, hRlc, hRK⟩ := UnifyRel.greatest_K h₁ U hUlc hmap.1 hUK
          have key : ∀ l : List Ty, l.map (R.onTy ∘ S₁.onTy) = l.map U.onTy := by
            intro l; apply List.map_congr_left; intro t _; exact (hR t).symm
          have hkey1 : (ts₁.map S₁.onTy).map R.onTy = ts₁.map U.onTy := by
            rw [List.map_map]; exact key ts₁
          have hmaptail : (ts₁.map S₁.onTy).map R.onTy = (ts₂.map S₁.onTy).map R.onTy := by
            rw [List.map_map, List.map_map, key, key]; exact hmap.2
          obtain ⟨S₂, h₂, hS₂K⟩ := ihL (as := ts₁.map S₁.onTy) (bs := ts₂.map S₁.onTy) (U := R)
            (by rw [hkey1]; rw [htsz] at hsz; omega)
            (by intro t ht; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
                exact Subst.onTy_lc hS₁lc (has t0 (List.mem_cons_of_mem _ ht0)))
            (by intro t ht; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
                exact Subst.onTy_lc hS₁lc (hbs t0 (List.mem_cons_of_mem _ ht0)))
            hRlc
            (by simp only [List.length_map]; have := hlen; simpa using this)
            hmaptail hRK
          exact ⟨S₁ ++ S₂, .cons h₁ h₂, by
            intro p hp; rcases List.mem_append.mp hp with h | h
            · exact hS₁K p h
            · exact hS₂K p h⟩

/-- A `K`-fixing LC unifier of two LC types yields a `UnifyRel` derivation that
    **avoids `K`** — with no side-condition on the operands' free variables. The
    rigidity-aware orientation that makes annotated-`let` completeness's `hesc1`
    structural. -/
theorem UnifyRel.complete_K {K : List Nat} {a b : Ty} {U : Subst}
    (ha : a.IsLC) (hb : b.IsLC) (hUlc : ∀ p ∈ U, p.2.IsLC)
    (hU : Unifies U a b) (hUK : ∀ k ∈ K, U.onTy (.fvar k) = .fvar k) :
    ∃ S, UnifyRel a b S ∧ (∀ p ∈ S, p.1 ∉ K) :=
  (UnifyRel.complete_K_aux (2 * (U.onTy a).size + 1)).1 (by omega) ha hb hUlc hU hUK

/-- Principality, application case (factored with named binders). Recurse on `f`
    then `arg`; the declarative typing yields a unifier of `S₂.onTy τf` and
    `arrow τa (.fvar Φ₂)` — exhibited explicitly as `[(Φ₂,.fvar W)] ++ R₂ ++ [(W,τ₀)]`
    (`W` fresh) so no derivation renaming is needed — which `UnifyRel.complete`
    realises as a derivation `S₃`, and `greatest_lc` factors through to the
    residual. -/
theorem Infer.complete_app_aux {f arg : Expr} {Φ : Nat} {ctx : Ctx} {S₀ : Subst}
    {argTy τ₀ : Ty} {K : List Nat}
    (ihf : Infer.CompleteAt f) (iharg : Infer.CompleteAt arg)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hS₀ : ∀ p ∈ S₀, p.2.IsLC)
    (hKΦ : ∀ k ∈ K, k < Φ)
    (hKf : ∀ y ∈ f.tyFreeVars, y ∈ K) (hKa : ∀ y ∈ arg.tyFreeVars, y ∈ K)
    (hKfix : ∀ k ∈ K, S₀.onTy (.fvar k) = .fvar k)
    (hf : TypeOfHM (S₀.onCtx ctx) f (.arrow argTy τ₀))
    (harg : TypeOfHM (S₀.onCtx ctx) arg argTy) :
    ∃ Φ' S eOut τ R,
      Infer Φ ctx (.app f arg) Φ' S eOut τ ∧
      Subst.AgreesBelow Φ S₀ (S ++ R) ∧
      τ₀ = R.onTy τ ∧
      (∀ p ∈ R, p.2.IsLC) ∧
      (∀ k ∈ K, R.onTy (.fvar k) = .fvar k) ∧
      (∀ p ∈ S, p.1 ∉ K) := by
  -- STEP 1: recurse on `f`.
  obtain ⟨Φ₁, S₁, eOutf, τf, R₁, hinff, hagf, htyf, hR₁, hR₁K, hS₁K⟩ :=
    ihf K hwf hbelow hS₀ hKΦ hKf hKfix hf
  have hS₁ := (Infer.lc hinff hwf).2
  have hbf := Infer.belowFvars hinff hbelow (fun y hy => hKΦ y (hKf y hy))
  have hle1 := Infer.frontier_le hinff
  have hwf₁ := Subst.onCtx_wf hS₁ hwf
  have hbelow₁ := Subst.onCtx_below hbf.2 hle1 hbelow
  -- STEP 2: recurse on `arg`.
  have hctxeq : R₁.onCtx (S₁.onCtx ctx) = S₀.onCtx ctx := by
    rw [← Subst.onCtx_append]
    exact Subst.onCtx_congr (fun v hv => (hagf v hv).symm) hbelow
  have harg' : TypeOfHM (R₁.onCtx (S₁.onCtx ctx)) arg argTy := by
    rw [hctxeq]; exact harg
  obtain ⟨Φ₂, S₂, eOuta, τa, R₂, hinfa, haga, htya, hR₂, hR₂K, hS₂K⟩ :=
    iharg K hwf₁ hbelow₁ hR₁ (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle1) hKa hR₁K harg'
  have hS₂ := (Infer.lc hinfa hwf₁).2
  have hba := Infer.belowFvars hinfa hbelow₁ (fun y hy => lt_of_lt_of_le (hKΦ y (hKa y hy)) hle1)
  have hle2 := Infer.frontier_le hinfa
  have hτfLC := (Infer.lc hinff hwf).1
  have hτaLC := (Infer.lc hinfa hwf₁).1
  -- STEP 3: key equalities.
  have hcongr_f : R₁.onTy τf = (S₂ ++ R₂).onTy τf := Subst.onTy_congr haga hbf.1
  have hP : R₂.onTy (S₂.onTy τf) = Ty.arrow argTy τ₀ := by
    rw [← Subst.onTy_append, ← hcongr_f]; exact htyf.symm
  have hΦ₂τf : Φ₂ ∉ (S₂.onTy τf).freeVars := fun hm => by
    have := (Subst.onTy_belowFvars hba.2 (hbf.1.mono hle2)).mem_lt _ hm
    omega
  have hΦ₂τa : Φ₂ ∉ τa.freeVars := fun hm => by
    have := hba.1.mem_lt _ hm
    omega
  have hτ₀LC : τ₀.IsLC := by
    have hreg := TypeOfHM.regular hf
    cases hreg with
    | arrow _ hT => exact hT
  -- STEP 4: a fresh variable `W`.
  obtain ⟨W, hWge, hWfresh⟩ := exists_fresh_block
    (R₂.map Prod.fst ++ R₂.flatMap (fun p => p.2.freeVars) ++ argTy.freeVars ++ τ₀.freeVars) Φ₂ 1
  have hWdom : ∀ p ∈ R₂, p.1 ≠ W := by
    intro p hp he
    have := hWfresh p.1 (List.mem_append_left _ (List.mem_append_left _
      (List.mem_append_left _ (List.mem_map.mpr ⟨p, hp, rfl⟩))))
    omega
  have hWrange : ∀ p ∈ R₂, W ∉ p.2.freeVars := by
    intro p hp hc
    have := hWfresh W (List.mem_append_left _ (List.mem_append_left _
      (List.mem_append_right _ (List.mem_flatMap.mpr ⟨p, hp, hc⟩))))
    omega
  have hWargTy : W ∉ argTy.freeVars := fun hc => by
    have := hWfresh W (List.mem_append_left _ (List.mem_append_right _ hc)); omega
  have hWτ₀ : W ∉ τ₀.freeVars := fun hc => by
    have := hWfresh W (List.mem_append_right _ hc); omega
  have hR₂Wfvar : R₂.onTy (Ty.fvar W) = Ty.fvar W := by
    apply Ty.substFvars_eq_self_of_no_key
    intro p hp hc
    simp only [Ty.freeVars, List.mem_singleton] at hc
    exact hWdom p hp hc
  -- STEP 5: the explicit unifier `U`.
  obtain ⟨U, hUdef⟩ : ∃ U : Subst, U = [(Φ₂, Ty.fvar W)] ++ R₂ ++ [(W, τ₀)] := ⟨_, rfl⟩
  have hsingle : ∀ (Z : Nat) (V y : Ty), Subst.onTy [(Z, V)] y = Ty.substFvar Z V y :=
    fun _ _ _ => rfl
  have hsubArrow : ∀ (Z : Nat) (V a b : Ty),
      Ty.substFvar Z V (Ty.arrow a b) = Ty.arrow (Ty.substFvar Z V a) (Ty.substFvar Z V b) :=
    fun _ _ _ _ => rfl
  have hUonTy : ∀ x, U.onTy x = Ty.substFvar W τ₀ (R₂.onTy (Ty.substFvar Φ₂ (Ty.fvar W) x)) := by
    intro x
    rw [hUdef, Subst.onTy_append, Subst.onTy_append, hsingle, hsingle]
  have e1 : Ty.substFvar Φ₂ (Ty.fvar W) (Ty.fvar Φ₂) = Ty.fvar W := by simp [Ty.substFvar]
  have e2 : Ty.substFvar W τ₀ (Ty.fvar W) = τ₀ := by simp [Ty.substFvar]
  have hUniL : U.onTy (S₂.onTy τf) = Ty.arrow argTy τ₀ := by
    rw [hUonTy, Ty.substFvar_fresh hΦ₂τf, hP, hsubArrow,
        Ty.substFvar_fresh hWargTy, Ty.substFvar_fresh hWτ₀]
  have hUniR : U.onTy (Ty.arrow τa (Ty.fvar Φ₂)) = Ty.arrow argTy τ₀ := by
    rw [hUonTy, hsubArrow, Ty.substFvar_fresh hΦ₂τa, e1, Subst.onTy_arrow,
        hR₂Wfvar, ← htya, hsubArrow, Ty.substFvar_fresh hWargTy, e2]
  have hUni : Unifies U (S₂.onTy τf) (Ty.arrow τa (Ty.fvar Φ₂)) := by
    show U.onTy (S₂.onTy τf) = U.onTy (Ty.arrow τa (Ty.fvar Φ₂))
    rw [hUniL, hUniR]
  -- STEP 6/7: factor `U` through the `K`-avoiding derivation `S₃`.
  have hUlc : ∀ p ∈ U, p.2.IsLC := by
    rw [hUdef]
    intro p hp
    rcases List.mem_append.mp hp with hp' | hp'
    · rcases List.mem_append.mp hp' with hp'' | hp''
      · obtain rfl := List.mem_singleton.mp hp''
        exact ContainsBvarsUpTo.fvar
      · exact hR₂ p hp''
    · obtain rfl := List.mem_singleton.mp hp'
      exact hτ₀LC
  have hUK : ∀ k ∈ K, U.onTy (Ty.fvar k) = Ty.fvar k := by
    intro k hk
    have hklt : k < Φ := hKΦ k hk
    have hkΦ₂ : Φ₂ ∉ (Ty.fvar k).freeVars := by
      simp only [Ty.freeVars, List.mem_singleton]; omega
    have hkW : W ∉ (Ty.fvar k).freeVars := by
      simp only [Ty.freeVars, List.mem_singleton]; omega
    rw [hUonTy, Ty.substFvar_fresh hkΦ₂, hR₂K k hk, Ty.substFvar_fresh hkW]
  obtain ⟨S₃, h₃, hS₃K⟩ := UnifyRel.complete_K (Subst.onTy_lc hS₂ hτfLC)
    (.arrow hτaLC ContainsBvarsUpTo.fvar) hUlc hUni hUK
  obtain ⟨R₃, hR₃, hR₃lc, hR₃K⟩ := UnifyRel.greatest_K h₃ U hUlc hUni hUK
  -- STEP 8: assemble.
  refine ⟨Φ₂ + 1, S₁ ++ S₂ ++ S₃, _, S₃.onTy (Ty.fvar Φ₂), R₃,
    Infer.app hinff hinfa h₃, ?_, ?_, hR₃lc, hR₃K, ?_⟩
  · intro v hv
    have ht1 : Ty.BelowFvars Φ₁ (S₁.onTy (Ty.fvar v)) :=
      Subst.onTy_belowFvars hbf.2 (Ty.BelowFvars.fvar (by omega))
    have ht : Ty.BelowFvars Φ₂ (S₂.onTy (S₁.onTy (Ty.fvar v))) :=
      Subst.onTy_belowFvars hba.2 (ht1.mono hle2)
    have hΦ₂t : Φ₂ ∉ (S₂.onTy (S₁.onTy (Ty.fvar v))).freeVars := fun hm => by
      have := ht.mem_lt _ hm; omega
    have hWt : W ∉ (S₂.onTy (S₁.onTy (Ty.fvar v))).freeVars := fun hm => by
      have := ht.mem_lt _ hm; omega
    have hWR₂t : W ∉ (R₂.onTy (S₂.onTy (S₁.onTy (Ty.fvar v)))).freeVars :=
      Subst.not_mem_onTy_freeVars hWrange hWt
    simp only [Subst.onTy_append]
    rw [← hR₃ (S₂.onTy (S₁.onTy (Ty.fvar v))), hUonTy,
        Ty.substFvar_fresh hΦ₂t, Ty.substFvar_fresh hWR₂t,
        hagf v hv, Subst.onTy_append, Subst.onTy_congr haga ht1, Subst.onTy_append]
  · rw [← hR₃ (Ty.fvar Φ₂), hUonTy, e1, hR₂Wfvar, e2]
  · intro p hp
    rcases List.mem_append.mp hp with hp' | hp'
    · rcases List.mem_append.mp hp' with hp'' | hp''
      · exact hS₁K p hp''
      · exact hS₂K p hp''
    · exact hS₃K p hp'

/-- Principality, application case. -/
theorem Infer.complete_app {f arg : Expr}
    (ihf : Infer.CompleteAt f) (iharg : Infer.CompleteAt arg) :
    Infer.CompleteAt (.app f arg) := by
  intro Φ ctx S₀ τ₀ K hwf hbelow hS₀ hKΦ hKe hKfix hty
  simp only [Expr.tyFreeVars, List.mem_append] at hKe
  cases hty with
  | app hf harg =>
    exact Infer.complete_app_aux ihf iharg hwf hbelow hS₀ hKΦ
      (fun y hy => hKe y (.inl hy)) (fun y hy => hKe y (.inr hy)) hKfix hf harg


/-! ### Scheme weakening (for the `letIn` principality case)

The algorithm generalises the rhs's principal type with `genScheme`, the
*maximal* generalisation; the declarative scheme `M` is less general. Giving the
let-bound variable a more general scheme preserves typing: every instance the
body used is still available. -/

/-- `M'` is at least as general as `M`: every instantiation of `M` is also an
    instantiation of `M'`. -/
def PolyTy.Generalizes (M' M : PolyTy) : Prop :=
  ∀ tyArgs ty, (∀ t ∈ tyArgs, t.IsLC) → InstantiatesBy tyArgs M.body ty →
    ∃ tyArgs', (∀ t ∈ tyArgs', t.IsLC) ∧ InstantiatesBy tyArgs' M'.body ty

/-- Replacing a context scheme `M` by a more general `M'` preserves `TypeOfHM`.
    Direct port of `TypeOfElabHM.weaken_scheme`; the weakened-position `var` case
    (sorry'd for `TypeOfElabHM` since the stored `tyArgs` instantiate the OLD scheme)
    **dissolves** here: `TypeOfHM.var`'s instantiation witness is existential, so
    `hgen` supplies a fresh witness for `M'` while the term keeps its decoration. -/
theorem TypeOfHM.weaken_scheme {ctors : CtorEnv} {env_post env : Env} {M M' : PolyTy}
    {e : Expr} {τ : Ty}
    (hgen : M'.Generalizes M)
    (h : TypeOfHM ⟨env_post ++ [M] ++ env, ctors⟩ e τ) :
    TypeOfHM ⟨env_post ++ [M'] ++ env, ctors⟩ e τ := by
  have H : ∀ {ctx : Ctx} {e₀ : Expr} {τ₀ : Ty}, TypeOfHM ctx e₀ τ₀ →
      ∀ ep : Env, ctx.env = ep ++ [M] ++ env →
      TypeOfHM ⟨ep ++ [M'] ++ env, ctx.ctors⟩ e₀ τ₀ := by
    intro ctx e₀ τ₀ hd
    induction hd using TypeOfHM.rec_strong with
    | primLitUnit => intro ep _; exact .primLitUnit
    | primLitInt => intro ep _; exact .primLitInt
    | primLitNat => intro ep _; exact .primLitNat
    | primLitStr => intro ep _; exact .primLitStr
    | app hf hinput ihf ihinput => intro ep heq; exact .app (ihf ep heq) (ihinput ep heq)
    | @lambda paramTy ann bodyCtx ctx body bodyTy hpc hann heqctx hbody ihbody =>
      intro ep heq
      refine TypeOfHM.lambda hpc hann rfl ?_
      have hbc := ihbody (PolyTy.mkTrivial paramTy :: ep) (by simp only [heqctx, heq, List.cons_append])
      simpa only [heqctx, List.cons_append] using hbc
    | @letIn ann ctx boundExpr bodyCtx body bodyTy Msch L hwf hann hcofin heqctx hbody ihcofin ihbody =>
      intro ep heq
      refine TypeOfHM.letIn hwf hann (fun Xs hfresh => ihcofin Xs hfresh ep heq) rfl ?_
      have hbc := ihbody (Msch :: ep) (by simp only [heqctx, heq, List.cons_append])
      simpa only [heqctx, List.cons_append] using hbc
    | @var dbl polyTy instArgs tyArgs ty ctx hlook hbvars hinst =>
      intro ep heq
      rw [heq] at hlook
      rcases lt_trichotomy dbl ep.length with hlt | heqd | hgt
      · refine TypeOfHM.var ?_ hbvars hinst
        show (ep ++ [M'] ++ env)[dbl]? = _
        rw [List.append_assoc, List.getElem?_append_left hlt]
        rw [List.append_assoc, List.getElem?_append_left hlt] at hlook
        exact hlook
      · subst heqd
        have hpoly : polyTy = M := by
          rw [List.append_assoc, List.getElem?_append_right (le_refl ep.length)] at hlook
          simpa only [Nat.sub_self, List.singleton_append, List.getElem?_cons_zero,
            Option.some.injEq] using hlook.symm
        subst hpoly
        obtain ⟨instArgs', hbvars', hinst'⟩ := hgen instArgs ty hbvars hinst
        refine TypeOfHM.var ?_ hbvars' hinst'
        show (ep ++ [M'] ++ env)[ep.length]? = some M'
        rw [List.append_assoc, List.getElem?_append_right (le_refl ep.length)]
        simp only [Nat.sub_self, List.singleton_append, List.getElem?_cons_zero]
      · refine TypeOfHM.var ?_ hbvars hinst
        show (ep ++ [M'] ++ env)[dbl]? = _
        have hle : ep.length ≤ dbl := by omega
        rw [List.append_assoc, List.getElem?_append_right hle] at hlook
        rw [List.append_assoc, List.getElem?_append_right hle]
        rw [show ([M] ++ env) = M :: env from rfl] at hlook
        rw [show ([M'] ++ env) = M' :: env from rfl]
        rw [show (dbl - ep.length) = (dbl - ep.length - 1) + 1 from by omega] at hlook ⊢
        simp only [List.getElem?_cons_succ] at hlook ⊢
        exact hlook
    | ctor hlook htyargs hinst => intro ep _; exact .ctor hlook htyargs hinst
    | @match_ ctx scrut scrutTy branches resultTy hscrut hne hbrs ihscrut ihbrs =>
      intro ep heq
      refine TypeOfHM.match_ (ihscrut ep heq) hne ?_
      intro branch hmem
      obtain ⟨pat, body⟩ := branch
      rcases ihbrs (pat, body) hmem with
        ⟨ctorr, c, n, tyArgs, instContents, hpat, hlook, hscrutEq, hpc, hn, hinstC, _, ihbody⟩ |
        ⟨hpat, _, ihbody⟩
      · subst hpat
        refine TypeOfMatchBranch.mk hlook hscrutEq hpc hn hinstC rfl rfl ?_
        have hbc := ihbody (instContents.map PolyTy.mkTrivial ++ ep)
          (by simp only [heq, List.append_assoc])
        simpa only [List.append_assoc] using hbc
      · subst hpat
        exact TypeOfMatchBranch.wildcard (ihbody ep heq)
    | letRec hlen hlen2 hlc hG hgen hcofin heq hbody ihcofin ihbody =>
      intro ep hep
      subst heq
      expose_names
      refine TypeOfHM.letRec (τs := τs) (Ms := Ms) (G := G) (L := L)
        hlen hlen2 hlc hG hgen ?_ rfl ?_
      · intro Xs hfresh p hp
        have hc := ihcofin Xs hfresh p hp
          ((τs.map (Ty.renameG G Xs)).map PolyTy.mkTrivial ++ ep)
          (by rw [hep]; simp only [List.append_assoc])
        simp only [List.append_assoc] at hc ⊢
        exact hc
      · have hb := ihbody (Ms ++ ep) (by rw [hep]; simp only [List.append_assoc])
        simp only [List.append_assoc] at hb ⊢
        exact hb
    | letRecAnn hlen hwf hcofin heq hbody ihcofin ihbody =>
      intro ep hep
      subst heq
      expose_names
      refine TypeOfHM.letRecAnn (schemes := schemes) (L := L) hlen hwf ?_ rfl ?_
      · intro p hp Xs hfresh
        have hc := ihcofin p hp Xs hfresh (schemes ++ ep)
          (by rw [hep]; simp only [List.append_assoc])
        simp only [List.append_assoc] at hc ⊢
        exact hc
      · have hb := ihbody (schemes ++ ep) (by rw [hep]; simp only [List.append_assoc])
        simp only [List.append_assoc] at hb ⊢
        exact hb
  exact H h env_post rfl

/-- Replacing a *list* of context schemes by pointwise more-general schemes preserves
    `TypeOfHM`. Direct port of `TypeOfHM.weaken_schemes` (iterates `weaken_scheme`). -/
theorem TypeOfHM.weaken_schemes {ctors : CtorEnv} {env : Env} {e : Expr} {τ : Ty}
    {Ms Ms' : List PolyTy}
    (hgen : List.Forall₂ PolyTy.Generalizes Ms' Ms)
    (h : TypeOfHM ⟨Ms ++ env, ctors⟩ e τ) :
    TypeOfHM ⟨Ms' ++ env, ctors⟩ e τ := by
  have H : ∀ {Ms Ms' : List PolyTy}, List.Forall₂ PolyTy.Generalizes Ms' Ms →
      ∀ (ep : Env), TypeOfHM ⟨ep ++ Ms ++ env, ctors⟩ e τ →
        TypeOfHM ⟨ep ++ Ms' ++ env, ctors⟩ e τ := by
    intro Ms Ms' hgen
    induction hgen with
    | nil => intro ep h; simpa using h
    | @cons M' M Mt' Mt hM htail ih =>
      intro ep h
      have h1 : TypeOfHM ⟨(ep ++ [M]) ++ Mt ++ env, ctors⟩ e τ := by
        simpa only [List.append_assoc, List.cons_append, List.nil_append,
          List.singleton_append] using h
      have h2 := ih (ep ++ [M]) h1
      have h3 := TypeOfHM.weaken_scheme (env_post := ep) (env := Mt' ++ env) hM
        (by simpa only [List.append_assoc, List.singleton_append] using h2)
      simpa only [List.append_assoc, List.cons_append, List.nil_append,
        List.singleton_append] using h3
  have hfin := H hgen [] (by simpa using h)
  simpa using hfin

/-- Build a `Forall₂` from equal lengths and a pointwise (index-indexed) relation. -/
private theorem List.forall₂_of_getElem {α β : Type*} {R : α → β → Prop}
    {l₁ : List α} {l₂ : List β} (hlen : l₁.length = l₂.length)
    (h : ∀ i (h₁ : i < l₁.length) (h₂ : i < l₂.length), R l₁[i] l₂[i]) :
    List.Forall₂ R l₁ l₂ := by
  induction l₁ generalizing l₂ with
  | nil => cases l₂ with | nil => exact .nil | cons => simp at hlen
  | cons a as ih =>
    cases l₂ with
    | nil => simp at hlen
    | cons b bs =>
      refine .cons (h 0 (by simp) (by simp)) (ih (by simpa using hlen) ?_)
      intro i h₁ h₂
      exact h (i + 1) (by simpa using h₁) (by simpa using h₂)


/-! ### Generalisation/substitution commutation lemmas (for `letIn` principality) -/

/-- For a nodup list, `idxOf?` of the element at index `i` is `some i`. -/
private theorem List.idxOf?_getElem_self {α : Type*} [BEq α] [LawfulBEq α]
    {l : List α} (hnd : l.Nodup) {i : Nat} (hi : i < l.length) :
    l.idxOf? l[i] = some i := by
  induction l generalizing i with
  | nil => simp at hi
  | cons x xs ih =>
    rw [List.nodup_cons] at hnd
    cases i with
    | zero => simp [List.idxOf?_cons]
    | succ j =>
      have hj : j < xs.length := by simpa using hi
      have hmem : xs[j] ∈ xs := List.getElem_mem hj
      have hxne : (x == xs[j]) = false := by
        simp only [beq_eq_false_iff_ne, ne_eq]
        intro h; exact hnd.1 (h ▸ hmem)
      rw [List.getElem_cons_succ, List.idxOf?_cons, hxne]
      simp [ih hnd.2 hj]

/-- Closing names fresh for `X` commutes into an `openWith`. -/
theorem Ty.closeOver_openWith_comm {Xs : List Nat} {Vs : List Ty} {X : Ty}
    (hfresh : ∀ x ∈ Xs, x ∉ X.freeVars) :
    Ty.closeOver Xs (Ty.openWith Vs X) = Ty.openWith (Vs.map (Ty.closeOver Xs)) X := by
  induction X using Ty.rec_strong with
  | prim p => rfl
  | bvar i =>
    simp only [Ty.openWith, Ty.instantiate]
    rw [List.getElem?_map]
    cases Vs[i]? with
    | none => simp [Ty.closeOver]
    | some t => simp
  | fvar n =>
    have hn : n ∉ Xs := fun h => hfresh n h (by simp [Ty.freeVars])
    simp only [Ty.openWith_fvar]
    rw [Ty.closeOver.eq_5, List.idxOf?_eq_none_iff.mpr hn]
  | arrow a b iha ihb =>
    have hfa : ∀ x ∈ Xs, x ∉ a.freeVars := fun x hx hc =>
      hfresh x hx (List.mem_dedup.mpr (List.mem_append.mpr (Or.inl hc)))
    have hfb : ∀ x ∈ Xs, x ∉ b.freeVars := fun x hx hc =>
      hfresh x hx (List.mem_dedup.mpr (List.mem_append.mpr (Or.inr hc)))
    simp only [Ty.openWith_arrow, Ty.closeOver, iha hfa, ihb hfb]
  | customTy nm tys ih =>
    simp only [Ty.openWith_customTy, Ty.closeOver, TyList.closeOver_eq_map, List.map_map]
    apply congrArg (Ty.customTy nm)
    apply List.map_congr_left
    intro t ht
    simp only [Function.comp_apply]
    exact ih t ht (fun x hx hc => hfresh x hx (TyList.mem_freeVars_of_mem ht hc))

/-- Composition of openings (when `X`'s bvars are covered by the inner args). -/
theorem Ty.openWith_openWith {Vs Ws : List Ty} {X : Ty}
    (hbv : ContainsBvarsUpTo Ws.length X) :
    Ty.openWith Vs (Ty.openWith Ws X) = Ty.openWith (Ws.map (Ty.openWith Vs)) X := by
  induction X using Ty.rec_strong with
  | prim p => rfl
  | fvar n => rfl
  | bvar i =>
    cases hbv with
    | bvar hlt =>
      have hi : Ws[i]? = some Ws[i] := List.getElem?_eq_getElem hlt
      have e1 : Ty.openWith Ws (.bvar i) = Ws[i] := by
        simp only [Ty.openWith, Ty.instantiate, hi, Option.getD_some]
      have e2 : Ty.openWith (Ws.map (Ty.openWith Vs)) (.bvar i) = Ty.openWith Vs (Ws[i]) := by
        simp only [Ty.openWith, Ty.instantiate, List.getElem?_map, hi, Option.map_some,
          Option.getD_some]
      rw [e1, e2]
  | arrow a b iha ihb =>
    cases hbv with
    | arrow hba hbb =>
      simp only [Ty.openWith_arrow, iha hba, ihb hbb]
  | customTy nm tys ih =>
    cases hbv with
    | customTy hball =>
      simp only [Ty.openWith_customTy, List.map_map]
      apply congrArg (Ty.customTy nm)
      apply List.map_congr_left
      intro t ht
      simp only [Function.comp_apply]
      exact ih t ht (hball t ht)

/-! ### Free-var bounds (for the `letIn` principality freshness obligation) -/

/-- Every free var of `S.onTy Z` traces back to a free var of `Z`. -/
theorem Ty.mem_freeVars_onTy_iff {S : Subst} {x : Nat} {Z : Ty} :
    x ∈ (S.onTy Z).freeVars ↔ ∃ v ∈ Z.freeVars, x ∈ (S.onTy (.fvar v)).freeVars := by
  induction Z using Ty.rec_strong with
  | prim p => simp [Subst.onTy_prim, Ty.freeVars]
  | bvar i => simp [Subst.onTy_bvar, Ty.freeVars]
  | fvar n => simp only [Ty.freeVars, List.mem_singleton, exists_eq_left]
  | arrow a b iha ihb =>
    simp only [Subst.onTy_arrow, Ty.freeVars, List.mem_dedup, List.mem_append]
    rw [iha, ihb]
    constructor
    · rintro (⟨v, hv, hx⟩ | ⟨v, hv, hx⟩)
      · exact ⟨v, .inl hv, hx⟩
      · exact ⟨v, .inr hv, hx⟩
    · rintro ⟨v, (hv | hv), hx⟩
      · exact .inl ⟨v, hv, hx⟩
      · exact .inr ⟨v, hv, hx⟩
  | customTy nm tys ih =>
    simp only [Subst.onTy_customTy, Ty.freeVars]
    rw [TyList.mem_freeVars_iff]
    constructor
    · rintro ⟨t', ht', hx⟩
      obtain ⟨t, ht, rfl⟩ := List.mem_map.mp ht'
      obtain ⟨v, hv, hxv⟩ := (ih t ht).mp hx
      exact ⟨v, TyList.mem_freeVars_iff.mpr ⟨t, ht, hv⟩, hxv⟩
    · rintro ⟨v, hv, hx⟩
      obtain ⟨t, ht, hvt⟩ := TyList.mem_freeVars_iff.mp hv
      exact ⟨S.onTy t, List.mem_map.mpr ⟨t, ht, rfl⟩, (ih t ht).mpr ⟨v, hvt, hx⟩⟩

/-- Closing over vars only removes free vars. -/
theorem Ty.closeOver_freeVars_subset {gs : List Nat} {τ : Ty} :
    (Ty.closeOver gs τ).freeVars ⊆ τ.freeVars := by
  induction τ using Ty.rec_strong with
  | prim p => simp [Ty.closeOver, Ty.freeVars]
  | bvar i => simp [Ty.closeOver, Ty.freeVars]
  | fvar n =>
    rw [Ty.closeOver.eq_5]
    cases gs.idxOf? n with
    | none => exact fun x hx => hx
    | some i => simp [Ty.freeVars]
  | arrow a b iha ihb =>
    intro x hx
    simp only [Ty.closeOver, Ty.freeVars, List.mem_dedup, List.mem_append] at hx ⊢
    rcases hx with h | h
    · exact .inl (iha h)
    · exact .inr (ihb h)
  | customTy nm tys ih =>
    intro x hx
    simp only [Ty.closeOver, Ty.freeVars, TyList.closeOver_eq_map] at hx ⊢
    obtain ⟨t', ht', hxt⟩ := TyList.mem_freeVars_iff.mp hx
    obtain ⟨t, ht, rfl⟩ := List.mem_map.mp ht'
    exact TyList.mem_freeVars_iff.mpr ⟨t, ht, ih t ht hxt⟩


/-! ### Principal generalization generalizes the declarative scheme -/

/-- General form: closing `τ₁` over *any* variable set `g` (transported by the
    residual `R`) yields a scheme at least as general as a declarative scheme `M`
    whose fresh opening factors as `R.onTy τ₁`. The crux of `let`/`letPair`
    principality: the body, declaratively typed under `M`, can be retyped under
    the more general scheme the algorithm produces. (The proof barely uses what
    `g` is, so `genScheme` and the pair schemes are all instances.) -/
theorem closeOver_generalizes {g : List Nat} {τ₁ : Ty} {R : Subst} {M : PolyTy} {Xs : List Nat}
    (hτ₁ : τ₁.IsLC) (hR : ∀ p ∈ R, p.2.IsLC) (hMwf : M.WF)
    (hXnodup : Xs.Nodup) (hXlen : Xs.length = M.paramCount)
    (hXMbody : ∀ x ∈ Xs, x ∉ M.body.freeVars)
    (htyr : Ty.openVars Xs M.body = R.onTy τ₁)
    (hXM'' : ∀ x ∈ Xs, x ∉ (R.onPolyTy ⟨g.length, Ty.closeOver g τ₁⟩).body.freeVars) :
    (R.onPolyTy ⟨g.length, Ty.closeOver g τ₁⟩).Generalizes M := by
  intro tyArgs ty htyargs_lc hinst
  -- `ty = openWith Vs M.body` for the length-`paramCount` prefix `Vs` of `tyArgs`.
  have hty_eq : ty = Ty.openWith
      ((List.range M.paramCount).map (fun i => (tyArgs[i]?).getD (.prim .unit))) M.body :=
    hinst.eq_openWith_range hMwf
  set Vs := (List.range M.paramCount).map (fun i => (tyArgs[i]?).getD (.prim .unit)) with hVsdef
  have hVs_len : Vs.length = M.paramCount := by rw [hVsdef]; simp
  have hVs_lc : ∀ v ∈ Vs, v.IsLC := by
    intro v hv
    rw [hVsdef] at hv
    obtain ⟨i, _, rfl⟩ := List.mem_map.mp hv
    cases hh : tyArgs[i]? with
    | none => simp only [Option.getD_none]; exact ContainsBvarsUpTo.prim
    | some t => simp only [Option.getD_some]; exact htyargs_lc t (List.mem_of_getElem? hh)
  -- `M.body = closeOver Xs (R.onTy τ₁)` (close the just-opened fresh names).
  have hMbody : M.body = Ty.closeOver Xs (R.onTy τ₁) := by
    have hbv : ContainsBvarsUpTo Xs.length M.body := by rw [hXlen]; exact hMwf
    have hrt := Ty.closeOver_openVars_self hXnodup hbv hXMbody
    rw [htyr] at hrt
    exact hrt.symm
  -- `R.onTy τ₁ = openWith Wg M'.body` where `Wg = R` applied to each gen var.
  have hRτ₁ : R.onTy τ₁ = Ty.openWith
      (g.map (fun gj => R.onTy (Ty.fvar gj)))
      ((R.onPolyTy ⟨g.length, Ty.closeOver g τ₁⟩).body) := by
    show R.onTy τ₁ = Ty.openWith (g.map (fun gj => R.onTy (Ty.fvar gj)))
      (R.onTy (Ty.closeOver g τ₁))
    conv_lhs => rw [← Ty.openVars_closeOver_self (gs := g) hτ₁]
    rw [Ty.openVars_eq_openWith, Subst.onTy_openWith hR, List.map_map]
    simp only [Function.comp_def]
  -- Round-trip to `M'.body`, then re-open with the composed args.
  have hbv1 : ContainsBvarsUpTo
      ((g.map (fun gj => R.onTy (Ty.fvar gj))).map (Ty.closeOver Xs)).length
      ((R.onPolyTy ⟨g.length, Ty.closeOver g τ₁⟩).body) := by
    have hwf := Subst.onPolyTy_wf hR (M := ⟨g.length, Ty.closeOver g τ₁⟩)
      (Ty.closeOver_preserves_bvars hτ₁)
    have hlen : ((g.map (fun gj => R.onTy (Ty.fvar gj))).map (Ty.closeOver Xs)).length
        = (R.onPolyTy ⟨g.length, Ty.closeOver g τ₁⟩).paramCount := by
      simp only [List.length_map]; rfl
    rw [hlen]; exact hwf
  have hty_final : ty = Ty.openWith
      (((g.map (fun gj => R.onTy (Ty.fvar gj))).map (Ty.closeOver Xs)).map (Ty.openWith Vs))
      ((R.onPolyTy ⟨g.length, Ty.closeOver g τ₁⟩).body) :=
    calc ty
        = Ty.openWith Vs M.body := hty_eq
      _ = Ty.openWith Vs (Ty.closeOver Xs (R.onTy τ₁)) := by rw [hMbody]
      _ = Ty.openWith Vs (Ty.closeOver Xs (Ty.openWith
            (g.map (fun gj => R.onTy (Ty.fvar gj)))
            ((R.onPolyTy ⟨g.length, Ty.closeOver g τ₁⟩).body))) := by rw [hRτ₁]
      _ = Ty.openWith Vs (Ty.openWith
            ((g.map (fun gj => R.onTy (Ty.fvar gj))).map (Ty.closeOver Xs))
            ((R.onPolyTy ⟨g.length, Ty.closeOver g τ₁⟩).body)) := by
              rw [Ty.closeOver_openWith_comm hXM'']
      _ = Ty.openWith
            (((g.map (fun gj => R.onTy (Ty.fvar gj))).map (Ty.closeOver Xs)).map
              (Ty.openWith Vs))
            ((R.onPolyTy ⟨g.length, Ty.closeOver g τ₁⟩).body) := by rw [Ty.openWith_openWith hbv1]
  -- The composed args are LC and witness the instantiation.
  refine ⟨((g.map (fun gj => R.onTy (Ty.fvar gj))).map (Ty.closeOver Xs)).map
      (Ty.openWith Vs), ?_, ?_⟩
  · intro v hv
    obtain ⟨z, hz, rfl⟩ := List.mem_map.mp hv
    obtain ⟨w, hw, rfl⟩ := List.mem_map.mp hz
    obtain ⟨gj, _, rfl⟩ := List.mem_map.mp hw
    have hwlc : (R.onTy (Ty.fvar gj)).IsLC := Subst.onTy_lc hR ContainsBvarsUpTo.fvar
    have hzbv : ContainsBvarsUpTo Xs.length (Ty.closeOver Xs (R.onTy (Ty.fvar gj))) :=
      Ty.closeOver_preserves_bvars hwlc
    exact Ty.openWith_isLC hVs_lc hzbv (by rw [hVs_len]; exact hXlen.le)
  · have hwf := Subst.onPolyTy_wf hR (M := ⟨g.length, Ty.closeOver g τ₁⟩)
      (Ty.closeOver_preserves_bvars hτ₁)
    have hVs'len : (((g.map (fun gj => R.onTy (Ty.fvar gj))).map
        (Ty.closeOver Xs)).map (Ty.openWith Vs)).length
        = (R.onPolyTy ⟨g.length, Ty.closeOver g τ₁⟩).paramCount := by
      simp only [List.length_map]; rfl
    have hinstW := InstantiatesBy.openWith hwf (le_of_eq hVs'len.symm)
    rw [hty_final]
    exact hinstW

/-- `genScheme env τ₁` (transported by `R`) generalizes any `M` it factors. The
    `g := genVars env τ₁` instance of `closeOver_generalizes`. -/
theorem genScheme_generalizes {rigid : List Nat} {env : Env} {τ₁ : Ty} {R : Subst} {M : PolyTy}
    {Xs : List Nat}
    (hτ₁ : τ₁.IsLC) (hR : ∀ p ∈ R, p.2.IsLC) (hMwf : M.WF)
    (hXnodup : Xs.Nodup) (hXlen : Xs.length = M.paramCount)
    (hXMbody : ∀ x ∈ Xs, x ∉ M.body.freeVars)
    (htyr : Ty.openVars Xs M.body = R.onTy τ₁)
    (hXM'' : ∀ x ∈ Xs, x ∉ (R.onPolyTy (genScheme rigid env τ₁)).body.freeVars) :
    (R.onPolyTy (genScheme rigid env τ₁)).Generalizes M :=
  closeOver_generalizes (g := genVars rigid env τ₁) hτ₁ hR hMwf hXnodup hXlen hXMbody htyr hXM''

/-- `PolyTy.genGroup Ginf τ₁` (transported by `R`) generalizes any `M` it factors.
    The `g := Ty.genFilter Ginf τ₁` instance of `closeOver_generalizes` — the
    `letRec` per-binding analogue of `genScheme_generalizes`. -/
theorem genGroup_generalizes {Ginf : List Nat} {τ₁ : Ty} {R : Subst} {M : PolyTy}
    {Xs : List Nat}
    (hτ₁ : τ₁.IsLC) (hR : ∀ p ∈ R, p.2.IsLC) (hMwf : M.WF)
    (hXnodup : Xs.Nodup) (hXlen : Xs.length = M.paramCount)
    (hXMbody : ∀ x ∈ Xs, x ∉ M.body.freeVars)
    (htyr : Ty.openVars Xs M.body = R.onTy τ₁)
    (hXM'' : ∀ x ∈ Xs, x ∉ (R.onPolyTy (PolyTy.genGroup Ginf τ₁)).body.freeVars) :
    (R.onPolyTy (PolyTy.genGroup Ginf τ₁)).Generalizes M :=
  closeOver_generalizes (g := Ty.genFilter Ginf τ₁) hτ₁ hR hMwf hXnodup hXlen hXMbody htyr hXM''

/-- A `zip`-renaming `Ws ↦ vs` (the `Ws` distinct, all fresh for the `vs`) sends
    the key `fvar w` (with `(w, v)` in the zip) to its partner `v`. The
    block-residual computation behind `exists_recgroup_residual`. -/
private theorem Subst.onTy_zip_fvar_get : ∀ {Ws : List Nat} {vs : List Ty} {w : Nat} {v : Ty},
    Ws.Nodup → (∀ k ∈ Ws, ∀ t ∈ vs, k ∉ t.freeVars) → (w, v) ∈ Ws.zip vs →
    Subst.onTy (Ws.zip vs) (Ty.fvar w) = v
  | [], _, _, _, _, _, hmem => by simp at hmem
  | _ :: _, [], _, _, _, _, hmem => by simp at hmem
  | w0 :: Ws', v0 :: vs', w, v, hnd, hfresh, hmem => by
    rw [List.zip_cons_cons, show ((w0, v0) :: Ws'.zip vs') = [(w0, v0)] ++ Ws'.zip vs' from rfl,
        Subst.onTy_append]
    rw [List.nodup_cons] at hnd
    have hstep : Subst.onTy [(w0, v0)] (Ty.fvar w) = Ty.substFvar w0 v0 (Ty.fvar w) := rfl
    rw [hstep]
    rcases List.mem_cons.mp hmem with heq | hmemrest
    · obtain ⟨rfl, rfl⟩ := Prod.mk.inj heq
      rw [show Ty.substFvar w v (Ty.fvar w) = v from by simp [Ty.substFvar]]
      exact Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
        have hp1 : p.1 ∈ Ws' := (List.of_mem_zip hp).1
        exact hfresh p.1 (List.mem_cons_of_mem _ hp1) v (List.mem_cons_self ..) hc)
    · have hw : w ∈ Ws' := (List.of_mem_zip hmemrest).1
      have hne : w0 ≠ w := fun h => hnd.1 (h ▸ hw)
      rw [Ty.substFvar_fresh (by simp only [Ty.freeVars, List.mem_singleton]; exact hne)]
      exact Subst.onTy_zip_fvar_get hnd.2
        (fun k hk t ht => hfresh k (List.mem_cons_of_mem _ hk) t (List.mem_cons_of_mem _ ht)) hmemrest

/-- The `renameG`-flavoured group generalisation: the inferred per-binding scheme
    `R.onPolyTy (genGroup Ginf τinf)` is at least as general as the declarative
    `genGroup G τdecl`, given the connection `R.onTy τinf = renameG G Xsfull τdecl`
    on a fresh shared opening `Xsfull` of the declarative pool `G`. The
    per-binding fresh opening for `genGroup_generalizes` is the `genFilter` slice
    of `Xsfull` (cf. `TypeOfHM.rewrap_hasScheme`). -/
theorem genGroup_generalizes_renameG {Ginf G Xsfull : List Nat} {τinf τdecl : Ty} {R : Subst}
    (hτinf : τinf.IsLC) (hτdecl : τdecl.IsLC) (hR : ∀ p ∈ R, p.2.IsLC)
    (hG : G.Nodup) (hXlen : Xsfull.length = G.length) (hXnodup : Xsfull.Nodup)
    (hXG : ∀ g ∈ G, g ∉ Xsfull) (hXτ : ∀ x ∈ Xsfull, x ∉ τdecl.freeVars)
    (hconn : R.onTy τinf = Ty.renameG G Xsfull τdecl)
    (hXinf : ∀ x ∈ Xsfull, x ∉ (R.onPolyTy (PolyTy.genGroup Ginf τinf)).body.freeVars) :
    (R.onPolyTy (PolyTy.genGroup Ginf τinf)).Generalizes (PolyTy.genGroup G τdecl) := by
  set Xs := Ty.genFilter Xsfull (Ty.renameG G Xsfull τdecl) with hXsdef
  have hgg : PolyTy.genGroup G τdecl = PolyTy.genGroup Xsfull (Ty.renameG G Xsfull τdecl) :=
    PolyTy.genGroup_renameG hτdecl hXlen hG hXnodup hXG hXτ
  have hXlen_filter : Xs.length = (Ty.genFilter G τdecl).length := by
    have h := congrArg PolyTy.paramCount hgg
    simp only [PolyTy.genGroup] at h
    rw [hXsdef]; exact h.symm
  have hXnodup' : Xs.Nodup := by rw [hXsdef]; unfold Ty.genFilter; exact hXnodup.filter _
  have hXsub : ∀ x ∈ Xs, x ∈ Xsfull := by
    rw [hXsdef]; intro x hx; exact Ty.mem_of_mem_genFilter hx
  have hGFnodup : (Ty.genFilter G τdecl).Nodup := by unfold Ty.genFilter; exact hG.filter _
  have hGFdisj : ∀ g ∈ Ty.genFilter G τdecl, g ∉ Xs :=
    fun g hg hc => hXG g (Ty.mem_of_mem_genFilter hg) (hXsub g hc)
  refine genGroup_generalizes (Ginf := Ginf) (τ₁ := τinf) (M := PolyTy.genGroup G τdecl) (Xs := Xs)
    hτinf hR (PolyTy.genGroup_wf hτdecl) hXnodup'
    (by rw [hXlen_filter]; rfl) ?_ ?_ ?_
  · -- Xs avoids the declarative scheme body's free vars (⊆ τdecl's)
    intro x hx hc
    exact hXτ x (hXsub x hx) (Ty.freeVars_closeOver_subset hc)
  · -- htyr : openVars Xs (genGroup G τdecl).body = R.onTy τinf
    show Ty.openVars Xs (Ty.closeOver (Ty.genFilter G τdecl) τdecl) = R.onTy τinf
    rw [Ty.openVars_closeOver_rename hτdecl hGFnodup hXlen_filter hGFdisj, hconn]
    exact (Ty.renameG_eq_genFilter hXlen hG hXnodup hXG hXτ).symm
  · intro x hx; exact hXinf x (hXsub x hx)

/-- **The `letRec` group residual.** Construct an LC residual `R₀` (fixing `K`,
    agreeing with `S₀` below the frontier `Φ`) that sends the group's fresh
    monotype-var block `[Φ, Φ+n)` to the chosen declarative opened monotypes `vs`.
    A proxy-block `[Φ,Φ+n) ↦ [W,W+n)` (fresh `W`) shields the block from `S₀`,
    then a `[W,W+n) ↦ vs` block realises the targets. -/
theorem exists_recgroup_residual {Φ n : Nat} {S₀ : Subst} {vs : List Ty} {K : List Nat}
    (hn : vs.length = n)
    (hS₀ : ∀ p ∈ S₀, p.2.IsLC) (hvs : ∀ t ∈ vs, t.IsLC)
    (hKΦ : ∀ k ∈ K, k < Φ)
    (hKfix : ∀ k ∈ K, S₀.onTy (Ty.fvar k) = Ty.fvar k) :
    ∃ R₀ : Subst, (∀ p ∈ R₀, p.2.IsLC) ∧ (∀ k ∈ K, R₀.onTy (Ty.fvar k) = Ty.fvar k) ∧
      (∀ v, v < Φ → R₀.onTy (Ty.fvar v) = S₀.onTy (Ty.fvar v)) ∧
      (((freshVars Φ n).map Ty.fvar).map R₀.onTy = vs) := by
  obtain ⟨W, hWge, hWfresh⟩ := exists_fresh_block
    (S₀.map Prod.fst ++ S₀.flatMap (fun p => p.2.freeVars) ++ Ty.freeVarsList vs) Φ n
  have hW_S₀key : ∀ p ∈ S₀, p.1 < W := fun p hp =>
    hWfresh p.1 (List.mem_append_left _ (List.mem_append_left _ (List.mem_map.mpr ⟨p, hp, rfl⟩)))
  have hW_S₀ran : ∀ p ∈ S₀, ∀ u ∈ p.2.freeVars, u < W := fun p hp u hu =>
    hWfresh u (List.mem_append_left _ (List.mem_append_right _ (List.mem_flatMap.mpr ⟨p, hp, hu⟩)))
  have hW_vs : ∀ t ∈ vs, ∀ u ∈ t.freeVars, u < W := fun t ht u hu =>
    hWfresh u (List.mem_append_right _ (Ty.mem_freeVarsList_of_mem ht hu))
  set proxy : Subst := (List.range n).map (fun i => (Φ + i, Ty.fvar (W + i))) with hproxydef
  set breal : Subst := (freshVars W n).zip vs with hbrealdef
  have hproxy_onTy : ∀ m, proxy.onTy (Ty.fvar m)
      = Ty.fvar (if Φ ≤ m ∧ m < Φ + n then m - Φ + W else m) := by
    intro m; rw [hproxydef]; exact rangeMapList_onTy_fvar Φ W n (Or.inl (by omega)) m
  have hbreal_key : ∀ p ∈ breal, W ≤ p.1 := by
    intro p hp; rw [hbrealdef] at hp; exact freshVars_ge p.1 (List.of_mem_zip hp).1
  have hcomp : ∀ m, (proxy ++ S₀ ++ breal).onTy (Ty.fvar m)
      = breal.onTy (S₀.onTy (proxy.onTy (Ty.fvar m))) := by
    intro m; rw [Subst.onTy_append, Subst.onTy_append]
  refine ⟨proxy ++ S₀ ++ breal, ?_, ?_, ?_, ?_⟩
  · intro p hp
    rcases List.mem_append.mp hp with hp' | hp'
    · rcases List.mem_append.mp hp' with hp'' | hp''
      · rw [hproxydef] at hp''; obtain ⟨i, _, rfl⟩ := List.mem_map.mp hp''
        exact ContainsBvarsUpTo.fvar
      · exact hS₀ p hp''
    · rw [hbrealdef] at hp'; exact hvs p.2 (List.of_mem_zip hp').2
  · intro k hk
    have hklt := hKΦ k hk
    rw [hcomp, hproxy_onTy, if_neg (by omega), hKfix k hk]
    exact Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
      simp only [Ty.freeVars, List.mem_singleton] at hc
      have := hbreal_key p hp; omega)
  · intro v hv
    rw [hcomp, hproxy_onTy, if_neg (by omega)]
    exact Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
      have hpW : W ≤ p.1 := hbreal_key p hp
      have hp_notmem : p.1 ∉ (S₀.onTy (Ty.fvar v)).freeVars :=
        Subst.not_mem_onTy_freeVars
          (fun q hq hcq => by have := hW_S₀ran q hq p.1 hcq; omega)
          (by simp only [Ty.freeVars, List.mem_singleton]; omega)
      exact hp_notmem hc)
  · apply List.ext_getElem
    · simp [hn]
    · intro j h1 h2
      simp only [List.getElem_map]
      have hj : j < n := by simpa using h1
      have hfj : (freshVars Φ n)[j]'(by simp [hj]) = Φ + j := by
        simp only [freshVars, List.getElem_map, List.getElem_range]
      rw [hfj, hcomp, hproxy_onTy, if_pos (by omega)]
      have hWj : Φ + j - Φ + W = W + j := by omega
      rw [hWj]
      have hS₀fix : S₀.onTy (Ty.fvar (W + j)) = Ty.fvar (W + j) :=
        Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
          simp only [Ty.freeVars, List.mem_singleton] at hc
          have := hW_S₀key p hp; omega)
      rw [hS₀fix, hbrealdef]
      apply Subst.onTy_zip_fvar_get freshVars_nodup
        (fun k hk t ht hc => by
          have hkW : W ≤ k := freshVars_ge k hk
          have := hW_vs t ht k hc; omega)
      have hfWj : (freshVars W n)[j]'(by simp [hj]) = W + j := by
        simp only [freshVars, List.getElem_map, List.getElem_range]
      have hmem : ((freshVars W n).zip vs)[j]'(by simp [hn, hj]) = (W + j, vs[j]) := by
        rw [List.getElem_zip, hfWj]
      rw [← hmem]
      exact List.getElem_mem _

/-- **The `letRec` body retyping.** The body, declaratively typed under the group
    schemes `Ms` (pool `G`, monotypes `τs`), retypes under the algorithm's
    `genGroupSchemes` (transported by the group residual `R₁`): each inferred
    scheme `R₁.onPolyTy (genGroup Ginf τs_inf[j])` generalizes the declarative
    `Ms[j] = genGroup G τs[j]` (via `genGroup_generalizes_renameG`), so
    `weaken_schemes` retypes the body. Shared by the `complete'` and producing
    `completeAt` `letRec` cases. -/
theorem letRec_body_retype
    {Φ : Nat} {ctx : Ctx} {S₁ R₁ S₀ : Subst} {bindings : List Expr}
    {body : Expr} {τs : List Ty} {Ms : List PolyTy} {G Xs : List Nat} {τ₀ : Ty} {K : List Nat}
    (hlen : bindings.length = τs.length) (hlen2 : τs.length = Ms.length)
    (hlc : ∀ τ ∈ τs, τ.IsLC) (hG : G.Nodup)
    (hgen : ∀ p ∈ τs.zip Ms, p.2 = PolyTy.genGroup G p.1)
    (hXlen : Xs.length = G.length) (hXnodup : Xs.Nodup)
    (hXG : ∀ g ∈ G, g ∉ Xs)
    (hXτs : ∀ x ∈ Xs, x ∉ Ty.freeVarsList τs)
    (hXenv : ∀ x ∈ Xs, x ∉ (S₀.onCtx ctx).env.freeVars)
    (hXrigid : ∀ x ∈ Xs, x ∉ bindings.flatMap Expr.tyFreeVars)
    (hS₁lc : ∀ p ∈ S₁, p.2.IsLC) (hR₁lc : ∀ p ∈ R₁, p.2.IsLC)
    (hctxS₁ : R₁.onCtx (S₁.onCtx ctx) = S₀.onCtx ctx)
    (hKrigid : ∀ y ∈ bindings.flatMap Expr.tyFreeVars, y ∈ K)
    (hR₁K : ∀ k ∈ K, R₁.onTy (Ty.fvar k) = Ty.fvar k)
    (hconn : ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i))).map R₁.onTy
              = τs.map (Ty.renameG G Xs))
    (hbodydecl : TypeOfHM ⟨Ms ++ (S₀.onCtx ctx).env, (S₀.onCtx ctx).ctors⟩ body τ₀) :
    TypeOfHM (R₁.onCtx ⟨genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
          ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i))) ++ (S₁.onCtx ctx).env,
        (S₁.onCtx ctx).ctors⟩)
      body τ₀ := by
  set rigid := bindings.flatMap Expr.tyFreeVars with hrigid_def
  set envS₁ := (S₁.onCtx ctx).env with henvS₁_def
  set τs_inf := (freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)) with hτsinf_def
  have hτsinf_len : τs_inf.length = τs.length := by
    rw [hτsinf_def, List.length_map, freshVars_length, hlen]
  have hτsinf_lc : ∀ t ∈ τs_inf, t.IsLC := by
    intro t ht; rw [hτsinf_def] at ht; obtain ⟨i, _, rfl⟩ := List.mem_map.mp ht
    exact Subst.onTy_lc hS₁lc ContainsBvarsUpTo.fvar
  -- the algorithm's group schemes pointwise generalize the declarative `Ms`
  have hforall : List.Forall₂ PolyTy.Generalizes
      (R₁.onEnv (genGroupSchemes rigid envS₁ τs_inf)) Ms := by
    apply List.forall₂_of_getElem
    · simp only [Subst.onEnv, genGroupSchemes, List.length_map, hτsinf_len, hlen2]
    · intro i h1 h2
      simp only [Subst.onEnv, genGroupSchemes, List.getElem_map]
      have hi_τs : i < τs.length := by
        simp only [Subst.onEnv, genGroupSchemes, List.length_map, hτsinf_len] at h1; exact h1
      have hi_Ms : i < Ms.length := by rw [← hlen2]; exact hi_τs
      have hi_inf : i < τs_inf.length := by rw [hτsinf_len]; exact hi_τs
      have hzipmem : (τs[i]'hi_τs, Ms[i]'hi_Ms) ∈ τs.zip Ms := by
        have hz : (τs.zip Ms)[i]'(by rw [List.length_zip]; omega) = (τs[i], Ms[i]) :=
          List.getElem_zip
        rw [← hz]; exact List.getElem_mem _
      have hMsi : Ms[i]'hi_Ms = PolyTy.genGroup G (τs[i]'hi_τs) := hgen _ hzipmem
      have hci : R₁.onTy (τs_inf[i]'hi_inf) = Ty.renameG G Xs (τs[i]'hi_τs) := by
        have h := congrArg (fun l => l[i]?) hconn
        simp only [List.getElem?_map] at h
        rw [List.getElem?_eq_getElem hi_inf, List.getElem?_eq_getElem hi_τs] at h
        simpa using h
      rw [hMsi]
      refine genGroup_generalizes_renameG (hτsinf_lc _ (List.getElem_mem _))
        (hlc _ (List.getElem_mem _)) hR₁lc hG hXlen hXnodup hXG
        (fun x hx hc => hXτs x hx (Ty.mem_freeVarsList_of_mem (List.getElem_mem _) hc)) hci ?_
      -- hXinf : the structural rigid/env-bound argument
      intro x hx hmem
      change x ∈ (R₁.onTy (Ty.closeOver (Ty.genFilter (genGroupVars rigid envS₁ τs_inf)
        (τs_inf[i]'hi_inf)) (τs_inf[i]'hi_inf))).freeVars at hmem
      obtain ⟨v, hv, hxv⟩ := Ty.mem_freeVars_onTy_iff.mp hmem
      have hvτinf : v ∈ (τs_inf[i]'hi_inf).freeVars := Ty.freeVars_closeOver_subset hv
      have hvGinf : v ∉ genGroupVars rigid envS₁ τs_inf := by
        intro hgi
        exact Ty.not_mem_closeOver_freeVars
          (by simp only [Ty.genFilter, List.mem_filter, decide_eq_true_eq]; exact ⟨hgi, hvτinf⟩) hv
      have hv_flist : v ∈ Ty.freeVarsList τs_inf :=
        Ty.mem_freeVarsList_of_mem (List.getElem_mem hi_inf) hvτinf
      have hcase : v ∈ envS₁.freeVars ∨ v ∈ rigid := by
        by_contra hcon
        push_neg at hcon
        apply hvGinf
        simp only [genGroupVars, List.mem_filter, Bool.and_eq_true, Bool.not_eq_eq_eq_not,
          Bool.not_true, List.contains_eq_mem, decide_eq_false_iff_not]
        exact ⟨hv_flist, hcon.1, hcon.2⟩
      rcases hcase with henv | hrig
      · obtain ⟨pt, hpt, hvpt⟩ := Env.mem_freeVars_iff.mp henv
        have hx_onTy : x ∈ (R₁.onTy pt.body).freeVars := Ty.mem_freeVars_onTy_iff.mpr ⟨v, hvpt, hxv⟩
        have hpt_mem : R₁.onPolyTy pt ∈ (S₀.onCtx ctx).env := by
          have hm2 : R₁.onPolyTy pt ∈ (R₁.onCtx (S₁.onCtx ctx)).env := by
            simp only [Subst.onCtx, Subst.onEnv]; exact List.mem_map.mpr ⟨pt, hpt, rfl⟩
          rw [hctxS₁] at hm2; exact hm2
        exact hXenv x hx (Env.mem_freeVars_iff.mpr ⟨R₁.onPolyTy pt, hpt_mem, hx_onTy⟩)
      · have hfix : R₁.onTy (Ty.fvar v) = Ty.fvar v := hR₁K v (hKrigid v hrig)
        rw [hfix] at hxv
        simp only [Ty.freeVars, List.mem_singleton] at hxv
        exact hXrigid x hx (by rw [hxv]; exact hrig)
  -- rewrite the goal context and apply `weaken_schemes`
  have hctxeq : R₁.onCtx ⟨genGroupSchemes rigid envS₁ τs_inf ++ envS₁, (S₁.onCtx ctx).ctors⟩
      = ⟨R₁.onEnv (genGroupSchemes rigid envS₁ τs_inf) ++ (S₀.onCtx ctx).env, (S₀.onCtx ctx).ctors⟩ := by
    have he : R₁.onEnv envS₁ = (S₀.onCtx ctx).env := by rw [← hctxS₁]; rfl
    have hc : (S₁.onCtx ctx).ctors = (S₀.onCtx ctx).ctors := by rw [← hctxS₁]; rfl
    show (⟨R₁.onEnv (genGroupSchemes rigid envS₁ τs_inf ++ envS₁), (S₁.onCtx ctx).ctors⟩ : Ctx) = _
    rw [Subst.onEnv, List.map_append, ← Subst.onEnv, ← Subst.onEnv, he, hc]
  rw [hctxeq]
  exact TypeOfHM.weaken_schemes hforall hbodydecl

/-! ### Principality, `letIn` case -/

/-- The `letIn` principality core (inverted form, with the declarative scheme `M`
    and cofinite-fresh-set `L` named). Mirrors `Infer.sound_letIn`'s factoring to
    sidestep inaccessible binders. The algorithm infers the rhs to `(S₁, τ₁)` and
    generalizes with the principal `genScheme`; `genScheme_generalizes` shows that
    scheme weakens the declarative `M`, so the body — declaratively typed under
    `M` — retypes (`weaken_scheme`) under the algorithm's scheme, ready for the
    body IH. Assembly then mirrors `complete_pair`. -/
theorem Infer.complete_letIn_aux {Φ : Nat} {ctx : Ctx} {S₀ : Subst}
    {rhs body : Expr} {M : PolyTy} {L : List Nat} {τ₀ : Ty} {K : List Nat}
    (iha : Infer.CompleteAt rhs) (ihb : Infer.CompleteAt body)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hS₀ : ∀ p ∈ S₀, p.2.IsLC)
    (hKΦ : ∀ k ∈ K, k < Φ) (hKrhs : ∀ y ∈ rhs.tyFreeVars, y ∈ K)
    (hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K)
    (hKfix : ∀ k ∈ K, S₀.onTy (.fvar k) = .fvar k)
    (hMwf : M.WF)
    (hcofin : ∀ Xs : List Nat, FreshNames L M.paramCount Xs →
      TypeOfHM (S₀.onCtx ctx) rhs (M.openVars Xs))
    (hbody : TypeOfHM { (S₀.onCtx ctx) with env := M :: (S₀.onCtx ctx).env } body τ₀) :
    ∃ Φ' S eOut τ R,
      Infer Φ ctx (.letIn none rhs body) Φ' S eOut τ ∧
      Subst.AgreesBelow Φ S₀ (S ++ R) ∧
      τ₀ = R.onTy τ ∧
      (∀ p ∈ R, p.2.IsLC) ∧
      (∀ k ∈ K, R.onTy (.fvar k) = .fvar k) ∧
      (∀ p ∈ S, p.1 ∉ K) := by
  -- A fresh opening avoiding `L`, `M.body`, the (substituted) env, and the rigid
  -- scoped vars `rhs.tyFreeVars` (which `genScheme` keeps free).
  obtain ⟨Xs, hXlen, hXnodup, hXavoid⟩ :=
    exists_fresh_names
      (L ++ M.body.freeVars ++ (S₀.onCtx ctx).env.freeVars ++ rhs.tyFreeVars) M.paramCount
  have hXfreshL : FreshNames L M.paramCount Xs :=
    ⟨hXlen, hXnodup, fun x hx hc =>
      hXavoid x hx (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_left _ hc)))⟩
  have hXMbody : ∀ x ∈ Xs, x ∉ M.body.freeVars :=
    fun x hx hc =>
      hXavoid x hx (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_right _ hc)))
  have hXenv : ∀ x ∈ Xs, x ∉ (S₀.onCtx ctx).env.freeVars :=
    fun x hx hc => hXavoid x hx (List.mem_append_left _ (List.mem_append_right _ hc))
  have hXrhs : ∀ x ∈ Xs, x ∉ rhs.tyFreeVars :=
    fun x hx hc => hXavoid x hx (List.mem_append_right _ hc)
  -- Apply the rhs IH at the fresh opening.
  obtain ⟨Φ₁, S₁, eOuta, τ₁, R₁, hinfa, haga, htya, hR₁, hR₁K, hS₁K⟩ :=
    iha K hwf hbelow hS₀ hKΦ hKrhs hKfix (hcofin Xs hXfreshL)
  have hS₁ : ∀ p ∈ S₁, p.2.IsLC := (Infer.lc hinfa hwf).2
  have hτ₁lc : τ₁.IsLC := (Infer.lc hinfa hwf).1
  have hle : Φ ≤ Φ₁ := Infer.frontier_le hinfa
  have hbfa := Infer.belowFvars hinfa hbelow (fun y hy => hKΦ y (hKrhs y hy))
  have hbelowτ₁ : Ty.BelowFvars Φ₁ τ₁ := hbfa.1
  have hbelowS₁ : ∀ p ∈ S₁, Ty.BelowFvars Φ₁ p.2 := hbfa.2
  have hwf₁ : CtxWF (S₁.onCtx ctx) := Subst.onCtx_wf hS₁ hwf
  have hbelow₁ : CtxBelow Φ₁ (S₁.onCtx ctx) := Subst.onCtx_below hbelowS₁ hle hbelow
  have hctxeq : R₁.onCtx (S₁.onCtx ctx) = S₀.onCtx ctx := by
    rw [← Subst.onCtx_append]
    exact Subst.onCtx_congr (fun v hv => (haga v hv).symm) hbelow
  -- Well-formedness / below-frontier for the algorithm's body context.
  have hwfBody : CtxWF { (S₁.onCtx ctx) with
      env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env } := by
    intro N hN
    rcases List.mem_cons.mp hN with rfl | hN
    · exact genScheme_wf hτ₁lc
    · exact hwf₁ N hN
  have hbelowBody : CtxBelow Φ₁ { (S₁.onCtx ctx) with
      env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env } := by
    intro N hN
    rcases List.mem_cons.mp hN with rfl | hN
    · show Ty.BelowFvars Φ₁ (Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) τ₁)
      exact hbelowτ₁.closeOver
    · exact hbelow₁ N hN
  -- The freshness obligation for `genScheme_generalizes`: the fresh `Xs` don't
  -- occur free in the (substituted) principal scheme body. A free var `v` of the
  -- closed-over body is in `τ₁.freeVars` but excluded from `genVars`: either it is
  -- env-bound (original argument) or it is a rigid scoped var (`rhs.tyFreeVars`),
  -- in which case `R₁` fixes it and the image is `v` itself, fresh-avoided by `Xs`.
  have hXM'' : ∀ x ∈ Xs,
      x ∉ (R₁.onPolyTy (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)).body.freeVars := by
    intro x hx hmem
    change x ∈ (R₁.onTy (Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) τ₁)).freeVars
      at hmem
    obtain ⟨v, hv, hxv⟩ := Ty.mem_freeVars_onTy_iff.mp hmem
    have hvτ₁ : v ∈ τ₁.freeVars := Ty.closeOver_freeVars_subset hv
    have hvg : v ∉ genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :=
      fun hg => Ty.not_mem_closeOver_freeVars hg hv
    by_cases hvrhs : v ∈ rhs.tyFreeVars
    · -- rigid scoped var: `R₁` fixes it, so `x = v`, contradicting `Xs`-freshness.
      have hfix : R₁.onTy (Ty.fvar v) = Ty.fvar v := hR₁K v (hKrhs v hvrhs)
      rw [hfix] at hxv
      simp only [Ty.freeVars, List.mem_singleton] at hxv
      exact hXrhs x hx (hxv ▸ hvrhs)
    · -- env-bound var: original argument via the (substituted) env.
      have hvenv : v ∈ (S₁.onCtx ctx).env.freeVars := by
        by_contra hc
        apply hvg
        simp only [genVars, List.mem_filter, Bool.and_eq_true]
        exact ⟨hvτ₁, by simpa using hc, by simpa using hvrhs⟩
      obtain ⟨pt, hpt, hvpt⟩ := Env.mem_freeVars_iff.mp hvenv
      have hx_onTy : x ∈ (R₁.onTy pt.body).freeVars :=
        Ty.mem_freeVars_onTy_iff.mpr ⟨v, hvpt, hxv⟩
      have hpt_mem : R₁.onPolyTy pt ∈ (S₀.onCtx ctx).env := by
        have hmem2 : R₁.onPolyTy pt ∈ (R₁.onCtx (S₁.onCtx ctx)).env := by
          simp only [Subst.onCtx, Subst.onEnv]
          exact List.mem_map.mpr ⟨pt, hpt, rfl⟩
        rw [hctxeq] at hmem2; exact hmem2
      exact hXenv x hx (Env.mem_freeVars_iff.mpr ⟨R₁.onPolyTy pt, hpt_mem, hx_onTy⟩)
  -- Weaken the declarative body typing to the algorithm's (more general) scheme.
  have hgen : (R₁.onPolyTy (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)).Generalizes M :=
    genScheme_generalizes hτ₁lc hR₁ hMwf hXnodup hXlen hXMbody htya hXM''
  have hbody' : TypeOfHM
      ⟨R₁.onPolyTy (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) :: (S₀.onCtx ctx).env,
        (S₀.onCtx ctx).ctors⟩ body τ₀ :=
    TypeOfHM.weaken_scheme (env_post := []) (env := (S₀.onCtx ctx).env) (M := M) hgen hbody
  have hbody'' : TypeOfHM (R₁.onCtx { (S₁.onCtx ctx) with
      env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env }) body τ₀ := by
    show TypeOfHM ⟨R₁.onPolyTy (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)
        :: R₁.onEnv (S₁.onCtx ctx).env, (S₁.onCtx ctx).ctors⟩ body τ₀
    rw [show R₁.onEnv (S₁.onCtx ctx).env = (S₀.onCtx ctx).env from congrArg Ctx.env hctxeq]
    exact hbody'
  -- Apply the body IH (specialization is `R₁`).
  obtain ⟨Φ₂, S₂, eOutb, τ₂, R₂, hinfb, hagb, htyb, hR₂, hR₂K, hS₂K⟩ :=
    ihb K hwfBody hbelowBody hR₁ (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle) hKbody hR₁K hbody''
  refine ⟨Φ₂, S₁ ++ S₂, _, τ₂, R₂, .letIn hinfa hinfb, ?_, htyb, hR₂, hR₂K, ?_⟩
  · exact Subst.AgreesBelow.trans_append hle haga hbelowS₁ hagb
  · intro p hp
    rcases List.mem_append.mp hp with hp | hp
    · exact hS₁K p hp
    · exact hS₂K p hp

/-! ### Kernel for annotated-`let` completeness

The annotated `let` rule skolemizes `σ` with fresh rigid names `Ys`, infers the
*opened* rhs at the rigid set `K ∪ Ys`, and unifies the rhs's principal type `τ₁`
with the skolem opening `σ.openVars Ys`. Rigidity is structural: the rhs residual
`R₁` fixes `K ∪ Ys`, so `UnifyRel.complete_K` yields an MGU `Schk` that avoids
`Ys` (`hesc1`) by construction. The helper below extracts a typing at one
*specific* opening `Ys` from the cofinite premise (`typeOfHM_at_block`). -/

/-- From the cofinite "types at every fresh opening of `σ`" premise, extract a
    typing at one *specific* opening `Ys` (any list of the right length): pick a
    generic fresh `Xs`, type at `σ.openVars Xs`, then rename `Xs → Ys`. -/
theorem typeOfHM_at_block {ctx : Ctx} {rhs : Expr} {σ : PolyTy} {L Ys : List Nat}
    (hYlen : Ys.length = σ.paramCount)
    (hcofin : ∀ Xs : List Nat, FreshNames L σ.paramCount Xs →
      TypeOfHM ctx (rhs.openTyVars Xs) (σ.openVars Xs)) :
    TypeOfHM ctx (rhs.openTyVars Ys) (σ.openVars Ys) := by
  -- Type at a generic fresh `Xs`, then rename `Xs → Ys` (term via the Core bridge
  -- `substTyFvars_zip_openTyVars`, type via `openWith_eq_substFvars_openVars`).
  obtain ⟨Xs, hXlen, hXnodup, hXavoid⟩ :=
    exists_fresh_names (L ++ ctx.env.freeVars ++ Ys ++ rhs.tyFreeVars ++ σ.body.freeVars)
      σ.paramCount
  have hXL : ∀ x ∈ Xs, x ∉ L := fun x hx hc => hXavoid x hx (by simp only [List.mem_append]; tauto)
  have hXenv : ∀ x ∈ Xs, x ∉ ctx.env.freeVars := fun x hx hc =>
    hXavoid x hx (by simp only [List.mem_append]; tauto)
  have hXYs : ∀ x ∈ Xs, x ∉ Ys := fun x hx hc => hXavoid x hx (by simp only [List.mem_append]; tauto)
  have hXrhs : ∀ x ∈ Xs, x ∉ rhs.tyFreeVars := fun x hx hc =>
    hXavoid x hx (by simp only [List.mem_append]; tauto)
  have hXσ : ∀ x ∈ Xs, x ∉ σ.body.freeVars := fun x hx hc =>
    hXavoid x hx (by simp only [List.mem_append]; tauto)
  have hwit := hcofin Xs ⟨hXlen, hXnodup, hXL⟩
  have hrename := TypeOfHM.typ_substs_preservation (Xs.zip (Ys.map (Ty.fvar ·)))
    (fun p hp => hXenv p.1 (List.of_mem_zip hp).1)
    (fun p hp => by
      obtain ⟨y, _, hy⟩ := List.mem_map.mp (List.of_mem_zip hp).2
      exact hy ▸ ContainsBvarsUpTo.fvar)
    hwit
  rw [Expr.substTyFvars_zip_openTyVars (by rw [hXlen, hYlen]) hXnodup hXrhs hXYs] at hrename
  have hkey : σ.openVars Ys
      = Ty.substFvars (Xs.zip (Ys.map (Ty.fvar ·))) (σ.openVars Xs) := by
    rw [show σ.openVars Ys = Ty.openWith (Ys.map (Ty.fvar ·)) σ.body from
          Ty.openVars_eq_openWith]
    exact Ty.openWith_eq_substFvars_openVars
      ⟨by rw [List.length_map, hYlen, hXlen], fun V hV => by
        obtain ⟨y, _, rfl⟩ := List.mem_map.mp hV; exact ContainsBvarsUpTo.fvar⟩
      hXnodup hXσ
      (fun X hX hc => hXYs X hX (Ty.mem_freeVarsList_map_fvar.mp hc))
  rw [hkey]
  exact hrename

/-! ### Principality, `fst`/`snd` cases -/

/-- Reconstruct a list from the length-indexed lookups into it. The map of
    `Vs[i]?.getD d` over `range Vs.length` is just `Vs`. -/
private theorem range_length_map_getD {Vs : List Ty} {d : Ty} :
    (List.range Vs.length).map (fun i => (Vs[i]?).getD d) = Vs := by
  apply List.ext_getElem
  · simp
  · intro i h1 h2
    simp only [List.getElem_map, List.getElem_range]
    rw [List.getElem?_eq_getElem h2]
    rfl

/-- The instantiation list of a `Forall₂ (InstantiatesBy Vs)` (when every source
    type's bvars are within `Vs.length`) is exactly the element-wise `openWith Vs`. -/
private theorem instContents_eq_openWith {Vs : List Ty} {cs insts : List Ty}
    (hforall : List.Forall₂ (InstantiatesBy Vs) cs insts)
    (hbv : ∀ c ∈ cs, ContainsBvarsUpTo Vs.length c) :
    insts = cs.map (Ty.openWith Vs) := by
  induction hforall with
  | nil => rfl
  | @cons c inst cs' insts' hhead _ ih =>
    simp only [List.map_cons]
    have hc_bv : ContainsBvarsUpTo Vs.length c := hbv c List.mem_cons_self
    have heq := InstantiatesBy.eq_openWith_range hhead hc_bv
    rw [range_length_map_getD] at heq
    rw [heq]
    congr 1
    exact ih (fun c' hc' => hbv c' (List.mem_cons_of_mem _ hc'))

/-- A composition law for `Subst.onCtx`: if two substitutions compose on every
    monotype, they compose on whole contexts. -/
private theorem Subst.onCtx_comp_of_onTy_eq {A B C : Subst} {ctx : Ctx}
    (h : ∀ τ, A.onTy τ = B.onTy (C.onTy τ)) :
    A.onCtx ctx = B.onCtx (C.onCtx ctx) := by
  simp only [Subst.onCtx, Subst.onEnv, List.map_map]
  congr 1
  apply List.map_congr_left
  intro M _
  simp only [Subst.onPolyTy, Function.comp_apply, h M.body]


/-- The `i`-th allocated fresh name (`i < k`) is `Φ + i`. -/
private theorem freshVars_getElem? {Φ k i : Nat} (hi : i < k) :
    (freshVars Φ k)[i]? = some (Φ + i) := by
  simp only [freshVars, List.getElem?_map, List.getElem?_range hi, Option.map_some]


/-- **The named-branch `customTy` unification dodge.** Given a residual `R`
    (LC, fixing `K`, below-`Φ`-friendly) under which `scrutTy` types as
    `customTy ctor.tyName tyArgs`, the algorithm's per-branch unification of
    `scrutTy` against `customTy ctor.tyName (freshVars Φ ctor.paramCount)`
    succeeds, and `R` factors through the resulting MGU `S₀` via a fresh residual
    `R₀` (sending the fresh block to `tyArgs`). Mirrors the old up-front match
    dodge, now relocated to each named branch. -/
private theorem customTy_dodge_unifier {Φ : Nat} {scrutTy : Ty} {R : Subst}
    {K : List Nat} {ctor : Ctor} {tyArgs : List Ty}
    (hbscrut : Ty.BelowFvars Φ scrutTy)
    (hR : ∀ p ∈ R, p.2.IsLC) (hKΦ : ∀ k ∈ K, k < Φ)
    (hKfix : ∀ k ∈ K, R.onTy (.fvar k) = .fvar k)
    (htyArgs_lc : ∀ t ∈ tyArgs, t.IsLC) (hpc : ctor.paramCount = tyArgs.length)
    (hscrutEq : R.onTy scrutTy = .customTy ctor.tyName tyArgs) :
    ∃ U : Subst,
      Unifies U scrutTy
        (.customTy ctor.tyName ((freshVars Φ ctor.paramCount).map (Ty.fvar ·))) ∧
      (∀ p ∈ U, p.2.IsLC) ∧
      (∀ k ∈ K, U.onTy (.fvar k) = .fvar k) ∧
      (∀ v, v < Φ → U.onTy (.fvar v) = R.onTy (.fvar v)) ∧
      ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map U.onTy = tyArgs := by
  obtain ⟨W₀, hW₀ge, hW₀fresh⟩ := exists_fresh_block
    (R.map Prod.fst ++ R.flatMap (fun p => p.2.freeVars) ++ tyArgs.flatMap Ty.freeVars)
    Φ ctor.paramCount
  obtain ⟨U, hUdef⟩ : ∃ U : Subst,
    U = (freshVars Φ ctor.paramCount).zip ((freshVars W₀ ctor.paramCount).map (Ty.fvar ·))
        ++ R ++ (freshVars W₀ ctor.paramCount).zip tyArgs := ⟨_, rfl⟩
  have hUonTy : ∀ x, U.onTy x =
      Subst.onTy ((freshVars W₀ ctor.paramCount).zip tyArgs)
        (R.onTy (Subst.onTy ((freshVars Φ ctor.paramCount).zip
          ((freshVars W₀ ctor.paramCount).map (Ty.fvar ·))) x)) := by
    intro x; rw [hUdef, Subst.onTy_append, Subst.onTy_append]
  have hWi_mem : ∀ {i : Nat}, i < ctor.paramCount →
      W₀ + i ∈ freshVars W₀ ctor.paramCount := by
    intro i hi; simp only [freshVars, List.mem_map, List.mem_range]; exact ⟨i, hi, rfl⟩
  have hWs_notin_Rkeys : ∀ w ∈ freshVars W₀ ctor.paramCount, w ∉ R.map Prod.fst := by
    intro w hw hc
    have hwge := freshVars_ge w hw
    have := hW₀fresh w (List.mem_append_left _ (List.mem_append_left _ hc)); omega
  have hWs_Rrange : ∀ w ∈ freshVars W₀ ctor.paramCount, ∀ q ∈ R, w ∉ q.2.freeVars := by
    intro w hw q hq hc
    have hwge := freshVars_ge w hw
    have := hW₀fresh w (List.mem_append_left _ (List.mem_append_right _
      (List.mem_flatMap.mpr ⟨q, hq, hc⟩))); omega
  have htyArgs_belowW₀ : ∀ t ∈ tyArgs, Ty.BelowFvars W₀ t := by
    intro t ht; apply Ty.BelowFvars.of_freeVars_lt
    intro v hv
    exact hW₀fresh v (List.mem_append_right _ (List.mem_flatMap.mpr ⟨t, ht, hv⟩))
  have hU_index : ∀ (i : Nat) (v : Ty), i < ctor.paramCount →
      tyArgs[i]? = some v → U.onTy (Ty.fvar (Φ + i)) = v := by
    intro i v hi hvi
    rw [hUonTy]
    have hL1 : Subst.onTy ((freshVars Φ ctor.paramCount).zip
        ((freshVars W₀ ctor.paramCount).map (Ty.fvar ·))) (Ty.fvar (Φ + i))
        = Ty.fvar (W₀ + i) := by
      apply Ty.substFvars_zip_fvar_eq' freshVars_nodup (freshVars_getElem? hi)
      · rw [List.getElem?_map, freshVars_getElem? hi]; rfl
      · intro X hX hc
        simp only [Ty.freeVars, List.mem_singleton] at hc
        have hXlt := freshVars_lt X hX; omega
    rw [hL1]
    have hL2 : R.onTy (Ty.fvar (W₀ + i)) = Ty.fvar (W₀ + i) := by
      apply Ty.substFvars_eq_self_of_no_key
      intro p hp hc
      simp only [Ty.freeVars, List.mem_singleton] at hc
      have hkey : p.1 ∈ R.map Prod.fst := List.mem_map.mpr ⟨p, hp, rfl⟩
      rw [hc] at hkey
      exact hWs_notin_Rkeys (W₀ + i) (hWi_mem hi) hkey
    rw [hL2]
    apply Ty.substFvars_zip_fvar_eq' freshVars_nodup (freshVars_getElem? hi) hvi
    intro w hw hc
    have hwge := freshVars_ge w hw
    have hvmem : v ∈ tyArgs := List.mem_of_getElem? hvi
    have := (htyArgs_belowW₀ v hvmem).mem_lt w hc; omega
  have hmap_eq : ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map U.onTy = tyArgs := by
    apply List.ext_getElem?
    intro i
    rcases Nat.lt_or_ge i ctor.paramCount with hi | hi
    · rw [List.getElem?_map, List.getElem?_map, freshVars_getElem? hi]
      simp only [Option.map_some]
      have hi' : i < tyArgs.length := by omega
      rw [List.getElem?_eq_getElem hi']
      congr 1
      exact hU_index i (tyArgs[i]'hi') hi (by rw [List.getElem?_eq_getElem hi'])
    · rw [List.getElem?_eq_none (by simp only [List.length_map, freshVars_length]; exact hi),
        List.getElem?_eq_none (by omega)]
  have hA_id_scrut : Subst.onTy ((freshVars Φ ctor.paramCount).zip
      ((freshVars W₀ ctor.paramCount).map (Ty.fvar ·))) scrutTy = scrutTy := by
    apply Ty.substFvars_eq_self_of_no_key
    intro p hp hc
    have hp1 : p.1 ∈ freshVars Φ ctor.paramCount := (List.of_mem_zip hp).1
    have hge := freshVars_ge p.1 hp1
    have hlt := hbscrut.mem_lt p.1 hc; omega
  have hC_id_custom : Subst.onTy ((freshVars W₀ ctor.paramCount).zip tyArgs)
      (Ty.customTy ctor.tyName tyArgs) = Ty.customTy ctor.tyName tyArgs := by
    apply Ty.substFvars_eq_self_of_no_key
    intro p hp hc
    have hp1 : p.1 ∈ freshVars W₀ ctor.paramCount := (List.of_mem_zip hp).1
    have hge := freshVars_ge p.1 hp1
    have hcb : Ty.BelowFvars W₀ (Ty.customTy ctor.tyName tyArgs) := .customTy htyArgs_belowW₀
    have hlt := hcb.mem_lt p.1 hc; omega
  have hUL : U.onTy scrutTy = Ty.customTy ctor.tyName tyArgs := by
    rw [hUonTy, hA_id_scrut, hscrutEq]; exact hC_id_custom
  have hUR : U.onTy (Ty.customTy ctor.tyName ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)))
      = Ty.customTy ctor.tyName tyArgs := by
    rw [Subst.onTy_customTy, hmap_eq]
  have hUni : Unifies U scrutTy
      (Ty.customTy ctor.tyName ((freshVars Φ ctor.paramCount).map (Ty.fvar ·))) := by
    show U.onTy scrutTy = U.onTy _
    rw [hUL, hUR]
  have hUlc : ∀ p ∈ U, p.2.IsLC := by
    rw [hUdef]
    intro p hp
    rcases List.mem_append.mp hp with hp' | hp'
    · rcases List.mem_append.mp hp' with hp'' | hp''
      · have hmem := (List.of_mem_zip hp'').2
        obtain ⟨x, _, hxeq⟩ := List.mem_map.mp hmem
        rw [← hxeq]; exact ContainsBvarsUpTo.fvar
      · exact hR p hp''
    · have hmem := (List.of_mem_zip hp').2
      exact htyArgs_lc p.2 hmem
  have hUeqR : ∀ v, v < Φ → U.onTy (Ty.fvar v) = R.onTy (Ty.fvar v) := by
    intro v hv
    rw [hUonTy]
    have hA_id : Subst.onTy ((freshVars Φ ctor.paramCount).zip
        ((freshVars W₀ ctor.paramCount).map (Ty.fvar ·))) (Ty.fvar v) = Ty.fvar v := by
      apply Ty.substFvars_eq_self_of_no_key
      intro p hp hc
      simp only [Ty.freeVars, List.mem_singleton] at hc
      have hp1 : p.1 ∈ freshVars Φ ctor.paramCount := (List.of_mem_zip hp).1
      have := freshVars_ge p.1 hp1; omega
    rw [hA_id]
    apply Ty.substFvars_eq_self_of_no_key
    intro p hp hc
    have hp1 : p.1 ∈ freshVars W₀ ctor.paramCount := (List.of_mem_zip hp).1
    have hge := freshVars_ge p.1 hp1
    exact Subst.not_mem_onTy_freeVars (hWs_Rrange p.1 hp1)
      (by simp only [Ty.freeVars, List.mem_singleton]; omega) hc
  have hUK : ∀ k ∈ K, U.onTy (Ty.fvar k) = Ty.fvar k := fun k hk =>
    (hUeqR k (hKΦ k hk)).trans (hKfix k hk)
  exact ⟨U, hUni, hUlc, hUK, hUeqR, hmap_eq⟩

/-- **The named-branch `customTy` unification dodge (producing form).** -/
private theorem customTy_unify_dodge {Φ : Nat} {scrutTy : Ty} {R : Subst}
    {K : List Nat} {ctor : Ctor} {tyArgs : List Ty}
    (hscrutLC : scrutTy.IsLC) (hbscrut : Ty.BelowFvars Φ scrutTy)
    (hR : ∀ p ∈ R, p.2.IsLC) (hKΦ : ∀ k ∈ K, k < Φ)
    (hKfix : ∀ k ∈ K, R.onTy (.fvar k) = .fvar k)
    (htyArgs_lc : ∀ t ∈ tyArgs, t.IsLC) (hpc : ctor.paramCount = tyArgs.length)
    (hscrutEq : R.onTy scrutTy = .customTy ctor.tyName tyArgs) :
    ∃ (S₀ R₀ : Subst),
      UnifyRel scrutTy
        (.customTy ctor.tyName ((freshVars Φ ctor.paramCount).map (Ty.fvar ·))) S₀ ∧
      (∀ p ∈ S₀, p.2.IsLC) ∧
      (∀ p ∈ S₀, Ty.BelowFvars (Φ + ctor.paramCount) p.2) ∧
      (∀ p ∈ S₀, p.1 ∉ K) ∧
      (∀ p ∈ R₀, p.2.IsLC) ∧
      (∀ k ∈ K, R₀.onTy (.fvar k) = .fvar k) ∧
      (∀ v, v < Φ → R.onTy (Ty.fvar v) = R₀.onTy (S₀.onTy (Ty.fvar v))) ∧
      (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy).map R₀.onTy = tyArgs := by
  obtain ⟨U, hUni, hUlc, hUK, hUeqR, hmap_eq⟩ :=
    customTy_dodge_unifier hbscrut hR hKΦ hKfix htyArgs_lc hpc hscrutEq
  have hcustomTy_lc : (Ty.customTy ctor.tyName
      ((freshVars Φ ctor.paramCount).map (Ty.fvar ·))).IsLC :=
    ContainsBvarsUpTo.customTy (fun t ht => by
      obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht; exact ContainsBvarsUpTo.fvar)
  obtain ⟨S₀, h₀, hS₀K⟩ := UnifyRel.complete_K hscrutLC hcustomTy_lc hUlc hUni hUK
  obtain ⟨R₀, hR₀eq, hR₀lc, hR₀K⟩ := UnifyRel.greatest_K h₀ U hUlc hUni hUK
  have hS₀ : ∀ p ∈ S₀, p.2.IsLC := UnifyRel.lc h₀ hscrutLC hcustomTy_lc
  have hS₀below : ∀ p ∈ S₀, Ty.BelowFvars (Φ + ctor.paramCount) p.2 := by
    apply UnifyRel.belowFvars h₀ (hbscrut.mono (by omega))
    apply Ty.BelowFvars.customTy
    intro t ht
    obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
    exact Ty.BelowFvars.fvar (by have := freshVars_lt x hx; omega)
  refine ⟨S₀, R₀, h₀, hS₀, hS₀below, hS₀K, hR₀lc, hR₀K, ?_, ?_⟩
  · intro v hv; rw [← hR₀eq, hUeqR v hv]
  · rw [List.map_map]
    exact Eq.trans (List.map_congr_left (fun t _ => (hR₀eq t).symm)) hmap_eq

/-- **The named-branch `customTy` factoring dodge (given-MGU form).** Given the
    branch's own MGU `S₀` (from a *given* `InferBranches.cons` derivation), the
    declarative residual `R` factors through it via `R₀`. -/
private theorem customTy_factor_dodge {Φ : Nat} {scrutTy : Ty} {R S₀ : Subst}
    {K : List Nat} {ctor : Ctor} {tyArgs : List Ty}
    (h₀ : UnifyRel scrutTy
      (.customTy ctor.tyName ((freshVars Φ ctor.paramCount).map (Ty.fvar ·))) S₀)
    (hbscrut : Ty.BelowFvars Φ scrutTy)
    (hR : ∀ p ∈ R, p.2.IsLC) (hKΦ : ∀ k ∈ K, k < Φ)
    (hKfix : ∀ k ∈ K, R.onTy (.fvar k) = .fvar k)
    (htyArgs_lc : ∀ t ∈ tyArgs, t.IsLC) (hpc : ctor.paramCount = tyArgs.length)
    (hscrutEq : R.onTy scrutTy = .customTy ctor.tyName tyArgs) :
    ∃ R₀ : Subst,
      (∀ p ∈ R₀, p.2.IsLC) ∧
      (∀ k ∈ K, R₀.onTy (.fvar k) = .fvar k) ∧
      (∀ v, v < Φ → R.onTy (Ty.fvar v) = R₀.onTy (S₀.onTy (Ty.fvar v))) ∧
      (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy).map R₀.onTy = tyArgs := by
  obtain ⟨U, hUni, hUlc, hUK, hUeqR, hmap_eq⟩ :=
    customTy_dodge_unifier hbscrut hR hKΦ hKfix htyArgs_lc hpc hscrutEq
  obtain ⟨R₀, hR₀eq, hR₀lc, hR₀K⟩ := UnifyRel.greatest_K h₀ U hUlc hUni hUK
  refine ⟨R₀, hR₀lc, hR₀K, ?_, ?_⟩
  · intro v hv; rw [← hR₀eq, hUeqR v hv]
  · rw [List.map_map]
    exact Eq.trans (List.map_congr_left (fun t _ => (hR₀eq t).symm)) hmap_eq


/-- **Branch-list completeness** companion of `Infer.complete`: if each branch
    body is principal (`CompleteAt`), and every branch types declaratively under
    a residual `R` (an LC specialization, below the frontier `Φ`), then
    `InferBranches` succeeds and the declarative typing factors through it via an
    LC residual `R'` (`R = R' ∘ S` below `Φ`). The companion to `match_`
    principality. With Core v2 the named-branch `customTy` unification is realised
    *inside* each `cons` via `customTy_unify_dodge`; the wildcard case leaves
    `scrutTy` free. -/
theorem InferBranches.complete {branches : List (MatchPattern × Expr)} :
    ∀ {Φ : Nat} {ctx : Ctx} {scrutTy : Ty} {ρ : Ty} {R : Subst} (K : List Nat),
    (∀ br ∈ branches, Infer.CompleteAt br.2) →
    CtxWF ctx → CtxBelow Φ ctx →
    scrutTy.IsLC → Ty.BelowFvars Φ scrutTy →
    ρ.IsLC → Ty.BelowFvars Φ ρ →
    (∀ p ∈ R, p.2.IsLC) →
    (∀ k ∈ K, k < Φ) →
    (∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars branches, y ∈ K) →
    (∀ k ∈ K, R.onTy (.fvar k) = .fvar k) →
    (∀ br ∈ branches, TypeOfMatchBranch (R.onCtx ctx) br (R.onTy scrutTy) (R.onTy ρ)) →
    ∃ Φ' S brsOut R',
      InferBranches Φ ctx scrutTy ρ branches Φ' S brsOut ∧
      Subst.AgreesBelow Φ R (S ++ R') ∧
      (∀ p ∈ R', p.2.IsLC) ∧
      (∀ k ∈ K, R'.onTy (.fvar k) = .fvar k) ∧
      (∀ p ∈ S, p.1 ∉ K) := by
  induction branches with
  | nil =>
    intro Φ ctx scrutTy ρ R K _ihbr _hwf _hbelow _hscrutLC _hbscrut _hρ _hbρ hR _hKΦ _hKbr hKfix _hdecl
    exact ⟨Φ, [], [], R, InferBranches.nil, fun v _ => by rw [List.nil_append], hR, hKfix, by simp⟩
  | cons head rest ih =>
    intro Φ ctx scrutTy ρ R K ihbr hwf hbelow hscrutLC hbscrut hρ hbρ hR hKΦ hKbr hKfix hdecl
    obtain ⟨pat, body⟩ := head
    have hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKbr y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy)
    have hKrest : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars rest, y ∈ K := fun y hy => hKbr y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hy)
    have ihbody : Infer.CompleteAt body := ihbr (pat, body) (List.mem_cons_self ..)
    have ihrest : ∀ br ∈ rest, Infer.CompleteAt br.2 :=
      fun br hbr => ihbr br (List.mem_cons_of_mem _ hbr)
    have hdhead := hdecl (pat, body) (List.mem_cons_self ..)
    cases hdhead with
    | mk hlook hscrutEq hpc hn hforall hpb hbctx hbodyty =>
      expose_names
      -- the type args are LC (regularity of the scrutinee's customTy under `R`)
      have htyArgs_lc : ∀ t ∈ tyArgs, t.IsLC := by
        have hreg : (Ty.customTy ctor.tyName tyArgs).IsLC := hscrutEq ▸ Subst.onTy_lc hR hscrutLC
        cases hreg with | customTy h => exact h
      -- STEP 0: realise the per-branch `customTy` unifier `S₀`, factor `R` through `R₀`.
      obtain ⟨S₀, R₀, h₀, hS₀, hS₀below, hS₀K, hR₀lc, hR₀K, hR₀ag, hfresh_R₀⟩ :=
        customTy_unify_dodge hscrutLC hbscrut hR hKΦ hKfix htyArgs_lc hpc hscrutEq
      -- `R₀` reconciles `S₀ ∘ ·` back to `R` below `Φ`.
      have key_t : ∀ {t : Ty}, Ty.BelowFvars Φ t → R₀.onTy (S₀.onTy t) = R.onTy t := by
        intro t ht
        rw [← Subst.onTy_append]
        exact Subst.onTy_congr (fun v hv => by rw [Subst.onTy_append]; exact (hR₀ag v hv).symm) ht
      have hctx0 : R₀.onCtx (S₀.onCtx ctx) = R.onCtx ctx := by
        rw [← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => by rw [Subst.onTy_append]; exact (hR₀ag v hv).symm) hbelow
      -- the declarative instantiations are `openWith` of `tyArgs`
      have hinsts : instContents = ctor.contents.map (Ty.openWith tyArgs) :=
        instContents_eq_openWith hforall (fun c hc => hpc ▸ ctor.bound c hc)
      -- recast `hbodyty` over the algorithmic body context
      rw [hbctx, hpb, hinsts] at hbodyty
      -- algorithmic body context and its invariants
      have hbodyWF : CtxWF { S₀.onCtx ctx with
          env := (ctor.contents.map (Ty.openWith
              (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
            ++ (S₀.onCtx ctx).env } :=
        branchBindings_wf (ctorr := ctor)
          (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
          (Subst.onCtx_wf hS₀ hwf)
          (fun t ht => by
            obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
            obtain ⟨x, _, rfl⟩ := List.mem_map.mp hv
            exact Subst.onTy_lc hS₀ ContainsBvarsUpTo.fvar)
          (by simp)
      have hbodyBelow : CtxBelow (Φ + ctor.paramCount) { S₀.onCtx ctx with
          env := (ctor.contents.map (Ty.openWith
              (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
            ++ (S₀.onCtx ctx).env } :=
        branchBindings_below (ctorr := ctor)
          (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
          (Subst.onCtx_below hS₀below (by omega) hbelow)
          (fun t ht => by
            obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
            obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv
            exact Subst.onTy_belowFvars hS₀below (.fvar (by have := freshVars_lt x hx; omega)))
      -- the declarative body typing over the algorithmic context (via `R₀`)
      have hbodyty2 : TypeOfHM (R₀.onCtx { S₀.onCtx ctx with
          env := (ctor.contents.map (Ty.openWith
              (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
            ++ (S₀.onCtx ctx).env }) body (R₀.onTy (S₀.onTy ρ)) := by
        rw [Subst.onCtx_branchBindings hR₀lc, hctx0, hfresh_R₀, key_t hbρ]
        exact hbodyty
      -- STEP: apply the head branch's IH (principality of `body`)
      obtain ⟨Φ_b, S_b, eOut_b, τ_b, R_b, hinfbody, hagbody, htybody, hR_b, hR_bK, hS_bK⟩ :=
        ihbody K hbodyWF hbodyBelow hR₀lc
          (fun k hk => by have := hKΦ k hk; omega) hKbody hR₀K hbodyty2
      have hbodyLC := Infer.lc hinfbody hbodyWF
      have hτb_lc : τ_b.IsLC := hbodyLC.1
      have hS_b : ∀ p ∈ S_b, p.2.IsLC := hbodyLC.2
      have hbb := Infer.belowFvars hinfbody hbodyBelow (fun y hy => by have := hKΦ y (hKbody y hy); omega)
      have hle_b : Φ + ctor.paramCount ≤ Φ_b := Infer.frontier_le hinfbody
      have hbS₀ρ : Ty.BelowFvars (Φ + ctor.paramCount) (S₀.onTy ρ) :=
        Subst.onTy_belowFvars hS₀below (hbρ.mono (by omega))
      -- STEP: the result unifier `S_u` (= `R_b` realised) and its `greatest` factoring
      have hUni : Unifies R_b τ_b (S_b.onTy (S₀.onTy ρ)) := by
        show R_b.onTy τ_b = R_b.onTy (S_b.onTy (S₀.onTy ρ))
        rw [← htybody]
        exact (Subst.onTy_congr hagbody hbS₀ρ).trans (Subst.onTy_append S_b R_b (S₀.onTy ρ))
      obtain ⟨S_u, h_u, hS_uK⟩ :=
        UnifyRel.complete_K hτb_lc (Subst.onTy_lc hS_b (Subst.onTy_lc hS₀ hρ)) hR_b hUni hR_bK
      obtain ⟨R_u, hR_u_eq, hR_u, hR_uK⟩ := UnifyRel.greatest_K h_u R_b hR_b hUni hR_bK
      have hS_u : ∀ p ∈ S_u, p.2.IsLC :=
        UnifyRel.lc h_u hτb_lc (Subst.onTy_lc hS_b (Subst.onTy_lc hS₀ hρ))
      have hS_u_below : ∀ p ∈ S_u, Ty.BelowFvars Φ_b p.2 :=
        UnifyRel.belowFvars h_u hbb.1 (Subst.onTy_belowFvars hbb.2 (hbS₀ρ.mono hle_b))
      -- STEP: recursion hypotheses on `rest`
      have hbelow' : CtxBelow Φ_b (S_u.onCtx (S_b.onCtx (S₀.onCtx ctx))) :=
        Subst.onCtx_below hS_u_below (le_refl _)
          (Subst.onCtx_below hbb.2 (le_refl _)
            (Subst.onCtx_below (fun p hp => (hS₀below p hp).mono hle_b) (by omega) hbelow))
      have hwf' : CtxWF (S_u.onCtx (S_b.onCtx (S₀.onCtx ctx))) :=
        Subst.onCtx_wf hS_u (Subst.onCtx_wf hS_b (Subst.onCtx_wf hS₀ hwf))
      have hbS₀scrut : Ty.BelowFvars (Φ + ctor.paramCount) (S₀.onTy scrutTy) :=
        Subst.onTy_belowFvars hS₀below (hbscrut.mono (by omega))
      have hscrutLC' : (S_u.onTy (S_b.onTy (S₀.onTy scrutTy))).IsLC :=
        Subst.onTy_lc hS_u (Subst.onTy_lc hS_b (Subst.onTy_lc hS₀ hscrutLC))
      have hbscrut' : Ty.BelowFvars Φ_b (S_u.onTy (S_b.onTy (S₀.onTy scrutTy))) :=
        Subst.onTy_belowFvars hS_u_below (Subst.onTy_belowFvars hbb.2 (hbS₀scrut.mono hle_b))
      have hρ' : (S_u.onTy (S_b.onTy (S₀.onTy ρ))).IsLC :=
        Subst.onTy_lc hS_u (Subst.onTy_lc hS_b (Subst.onTy_lc hS₀ hρ))
      have hbρ' : Ty.BelowFvars Φ_b (S_u.onTy (S_b.onTy (S₀.onTy ρ))) :=
        Subst.onTy_belowFvars hS_u_below (Subst.onTy_belowFvars hbb.2 (hbS₀ρ.mono hle_b))
      have key_full : ∀ {t : Ty}, Ty.BelowFvars Φ t →
          R_u.onTy (S_u.onTy (S_b.onTy (S₀.onTy t))) = R.onTy t := by
        intro t ht
        have hbS₀t : Ty.BelowFvars (Φ + ctor.paramCount) (S₀.onTy t) :=
          Subst.onTy_belowFvars hS₀below (ht.mono (by omega))
        rw [← hR_u_eq, ← Subst.onTy_append, Subst.onTy_congr hagbody hbS₀t |>.symm]
        exact key_t ht
      have hctx_full : R_u.onCtx (S_u.onCtx (S_b.onCtx (S₀.onCtx ctx))) = R.onCtx ctx := by
        rw [← Subst.onCtx_append, ← Subst.onCtx_append, ← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => by
          simp only [Subst.onTy_append]; exact key_full (Ty.BelowFvars.fvar hv)) hbelow
      have hdecl' : ∀ br ∈ rest, TypeOfMatchBranch (R_u.onCtx (S_u.onCtx (S_b.onCtx (S₀.onCtx ctx))))
          br (R_u.onTy (S_u.onTy (S_b.onTy (S₀.onTy scrutTy))))
          (R_u.onTy (S_u.onTy (S_b.onTy (S₀.onTy ρ)))) := by
        intro br hbr
        rw [hctx_full, key_full hbscrut, key_full hbρ]
        exact hdecl br (List.mem_cons_of_mem _ hbr)
      obtain ⟨Φ', S_r, brsOut_r, R_r, hinfrest, hagrest, hR_r, hR_rK, hS_rK⟩ :=
        ih K ihrest hwf' hbelow' hscrutLC' hbscrut' hρ' hbρ' hR_u
          (fun k hk => lt_of_lt_of_le (hKΦ k hk) (le_trans (Nat.le_add_right _ _) hle_b))
          hKrest hR_uK hdecl'
      refine ⟨Φ', S₀ ++ S_b ++ S_u ++ S_r, _, R_r,
        InferBranches.cons hlook hn h₀ hinfbody h_u hinfrest, ?_, hR_r, hR_rK, ?_⟩
      · intro v hv
        have hb0 : Ty.BelowFvars (Φ + ctor.paramCount) (S₀.onTy (.fvar v)) :=
          Subst.onTy_belowFvars hS₀below (.fvar (by omega))
        have hb1 : Ty.BelowFvars Φ_b (S_b.onTy (S₀.onTy (.fvar v))) :=
          Subst.onTy_belowFvars hbb.2 (hb0.mono hle_b)
        have hb2 : Ty.BelowFvars Φ_b (S_u.onTy (S_b.onTy (S₀.onTy (.fvar v)))) :=
          Subst.onTy_belowFvars hS_u_below hb1
        calc R.onTy (.fvar v)
            = R₀.onTy (S₀.onTy (.fvar v)) := hR₀ag v hv
          _ = (S_b ++ R_b).onTy (S₀.onTy (.fvar v)) := Subst.onTy_congr hagbody hb0
          _ = R_b.onTy (S_b.onTy (S₀.onTy (.fvar v))) := by rw [Subst.onTy_append]
          _ = R_u.onTy (S_u.onTy (S_b.onTy (S₀.onTy (.fvar v)))) := hR_u_eq _
          _ = (S_r ++ R_r).onTy (S_u.onTy (S_b.onTy (S₀.onTy (.fvar v)))) :=
                Subst.onTy_congr hagrest hb2
          _ = ((S₀ ++ S_b ++ S_u ++ S_r) ++ R_r).onTy (.fvar v) := by simp only [Subst.onTy_append]
      · intro p hp
        rcases List.mem_append.mp hp with hp' | hp'
        · rcases List.mem_append.mp hp' with hp'' | hp''
          · rcases List.mem_append.mp hp'' with hp3 | hp3
            · exact hS₀K p hp3
            · exact hS_bK p hp3
          · exact hS_uK p hp''
        · exact hS_rK p hp'
    | wildcard hbodyty =>
      obtain ⟨Φ_b, S_b, eOut_b, τ_b, R_b, hinfbody, hagbody, htybody, hR_b, hR_bK, hS_bK⟩ :=
        ihbody K hwf hbelow hR hKΦ hKbody hKfix hbodyty
      have hbodyLC := Infer.lc hinfbody hwf
      have hτb_lc : τ_b.IsLC := hbodyLC.1
      have hS_b : ∀ p ∈ S_b, p.2.IsLC := hbodyLC.2
      have hbb := Infer.belowFvars hinfbody hbelow (fun y hy => hKΦ y (hKbody y hy))
      have hle_b : Φ ≤ Φ_b := Infer.frontier_le hinfbody
      have hUni : Unifies R_b τ_b (S_b.onTy ρ) := by
        show R_b.onTy τ_b = R_b.onTy (S_b.onTy ρ)
        rw [← htybody, ← Subst.onTy_append]
        exact Subst.onTy_congr hagbody hbρ
      obtain ⟨S_u, h_u, hS_uK⟩ :=
        UnifyRel.complete_K hτb_lc (Subst.onTy_lc hS_b hρ) hR_b hUni hR_bK
      obtain ⟨R_u, hR_u_eq, hR_u, hR_uK⟩ := UnifyRel.greatest_K h_u R_b hR_b hUni hR_bK
      have hS_u : ∀ p ∈ S_u, p.2.IsLC := UnifyRel.lc h_u hτb_lc (Subst.onTy_lc hS_b hρ)
      have hS_u_below : ∀ p ∈ S_u, Ty.BelowFvars Φ_b p.2 :=
        UnifyRel.belowFvars h_u hbb.1 (Subst.onTy_belowFvars hbb.2 (hbρ.mono hle_b))
      have hwf' : CtxWF (S_u.onCtx (S_b.onCtx ctx)) :=
        Subst.onCtx_wf hS_u (Subst.onCtx_wf hS_b hwf)
      have hbelow_Sb : CtxBelow Φ_b (S_b.onCtx ctx) := Subst.onCtx_below hbb.2 hle_b hbelow
      have hbelow' : CtxBelow Φ_b (S_u.onCtx (S_b.onCtx ctx)) :=
        Subst.onCtx_below hS_u_below (le_refl _) hbelow_Sb
      have key_t : ∀ {t : Ty}, Ty.BelowFvars Φ t →
          R_u.onTy (S_u.onTy (S_b.onTy t)) = R.onTy t := by
        intro t ht
        rw [← hR_u_eq (S_b.onTy t), ← Subst.onTy_append]
        exact (Subst.onTy_congr hagbody ht).symm
      have hscrutLC' : (S_u.onTy (S_b.onTy scrutTy)).IsLC :=
        Subst.onTy_lc hS_u (Subst.onTy_lc hS_b hscrutLC)
      have hbscrut' : Ty.BelowFvars Φ_b (S_u.onTy (S_b.onTy scrutTy)) :=
        Subst.onTy_belowFvars hS_u_below (Subst.onTy_belowFvars hbb.2 (hbscrut.mono hle_b))
      have hρ' : (S_u.onTy (S_b.onTy ρ)).IsLC := Subst.onTy_lc hS_u (Subst.onTy_lc hS_b hρ)
      have hbρ' : Ty.BelowFvars Φ_b (S_u.onTy (S_b.onTy ρ)) :=
        Subst.onTy_belowFvars hS_u_below (Subst.onTy_belowFvars hbb.2 (hbρ.mono hle_b))
      have hctx_eq : R_u.onCtx (S_u.onCtx (S_b.onCtx ctx)) = R.onCtx ctx := by
        rw [← Subst.onCtx_comp_of_onTy_eq hR_u_eq, ← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => (hagbody v hv).symm) hbelow
      have hdecl' : ∀ br ∈ rest, TypeOfMatchBranch (R_u.onCtx (S_u.onCtx (S_b.onCtx ctx)))
          br (R_u.onTy (S_u.onTy (S_b.onTy scrutTy))) (R_u.onTy (S_u.onTy (S_b.onTy ρ))) := by
        intro br hbr
        rw [hctx_eq, key_t hbscrut, key_t hbρ]
        exact hdecl br (List.mem_cons_of_mem _ hbr)
      obtain ⟨Φ', S_r, brsOut_r, R_r, hinfrest, hagrest, hR_r, hR_rK, hS_rK⟩ :=
        ih K ihrest hwf' hbelow' hscrutLC' hbscrut' hρ' hbρ' hR_u
          (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle_b) hKrest hR_uK hdecl'
      refine ⟨Φ', S_b ++ S_u ++ S_r, _, R_r,
        InferBranches.consWild hinfbody h_u hinfrest, ?_, hR_r, hR_rK, ?_⟩
      · intro v hv
        have hbv1 : Ty.BelowFvars Φ_b (S_b.onTy (.fvar v)) :=
          Subst.onTy_belowFvars hbb.2 (.fvar (by omega))
        have hbv2 : Ty.BelowFvars Φ_b (S_u.onTy (S_b.onTy (.fvar v))) :=
          Subst.onTy_belowFvars hS_u_below hbv1
        calc R.onTy (.fvar v)
            = (S_b ++ R_b).onTy (.fvar v) := hagbody v hv
          _ = R_b.onTy (S_b.onTy (.fvar v)) := by rw [Subst.onTy_append]
          _ = R_u.onTy (S_u.onTy (S_b.onTy (.fvar v))) := hR_u_eq (S_b.onTy (.fvar v))
          _ = (S_r ++ R_r).onTy (S_u.onTy (S_b.onTy (.fvar v))) := Subst.onTy_congr hagrest hbv2
          _ = ((S_b ++ S_u ++ S_r) ++ R_r).onTy (.fvar v) := by simp only [Subst.onTy_append]
      · intro p hp
        rcases List.mem_append.mp hp with hp' | hp'
        · rcases List.mem_append.mp hp' with hp'' | hp''
          · exact hS_bK p hp''
          · exact hS_uK p hp''
        · exact hS_rK p hp'

/-- **Recursive-group completeness** companion of `Infer.complete` (the producer
    direction): if each binding is principal (`CompleteAt`) and every binding types
    declaratively at its target under a residual `R`, then `InferRecGroup` succeeds
    and the typing factors through it. The `letRec` analogue of
    `InferBranches.complete`; threads like the `consWild` case against per-binding
    targets. -/
theorem InferRecGroup.complete {bindings : List Expr} :
    ∀ {Φ : Nat} {ctx : Ctx} {targets : List Ty} {R : Subst} (K : List Nat),
    bindings.length = targets.length →
    (∀ e ∈ bindings, Infer.CompleteAt e) →
    CtxWF ctx → CtxBelow Φ ctx →
    (∀ τ ∈ targets, τ.IsLC) → (∀ τ ∈ targets, Ty.BelowFvars Φ τ) →
    (∀ p ∈ R, p.2.IsLC) →
    (∀ k ∈ K, k < Φ) →
    (∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K) →
    (∀ k ∈ K, R.onTy (.fvar k) = .fvar k) →
    (∀ p ∈ bindings.zip (targets.map R.onTy), TypeOfHM (R.onCtx ctx) p.1 p.2) →
    ∃ Φ' S bindingsOut R',
      InferRecGroup Φ ctx bindings targets Φ' S bindingsOut ∧
      Subst.AgreesBelow Φ R (S ++ R') ∧
      (∀ p ∈ R', p.2.IsLC) ∧
      (∀ k ∈ K, R'.onTy (.fvar k) = .fvar k) ∧
      (∀ p ∈ S, p.1 ∉ K) := by
  induction bindings with
  | nil =>
    intro Φ ctx targets R K hlen _ _ _ _ _ hR _ _ hKfix _
    cases targets with
    | nil =>
      exact ⟨Φ, [], [], R, InferRecGroup.nil, fun v _ => by rw [List.nil_append], hR, hKfix, by simp⟩
    | cons => simp at hlen
  | cons e rest ih =>
    intro Φ ctx targets R K hlen ihbr hwf hbelow htlc htbelow hR hKΦ hKbr hKfix hdecl
    cases targets with
    | nil => simp at hlen
    | cons β βs =>
      have hKbody : ∀ y ∈ e.tyFreeVars, y ∈ K := fun y hy => hKbr y (by
        simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inl hy)
      have hKrest : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars rest, y ∈ K := fun y hy => hKbr y (by
        simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inr hy)
      have ihe : Infer.CompleteAt e := ihbr e List.mem_cons_self
      have ihrest : ∀ e' ∈ rest, Infer.CompleteAt e' :=
        fun e' he' => ihbr e' (List.mem_cons_of_mem _ he')
      have hβlc : β.IsLC := htlc β List.mem_cons_self
      have hbβ : Ty.BelowFvars Φ β := htbelow β List.mem_cons_self
      have hbodyty : TypeOfHM (R.onCtx ctx) e (R.onTy β) :=
        hdecl (e, R.onTy β) (by rw [List.map_cons, List.zip_cons_cons]; exact List.mem_cons_self)
      obtain ⟨Φ_b, S_b, eOut_b, τ_b, R_b, hinfbody, hagbody, htybody, hR_b, hR_bK, hS_bK⟩ :=
        ihe K hwf hbelow hR hKΦ hKbody hKfix hbodyty
      have hbodyLC := Infer.lc hinfbody hwf
      have hτb_lc := hbodyLC.1
      have hS_b := hbodyLC.2
      have hbb := Infer.belowFvars hinfbody hbelow (fun y hy => hKΦ y (hKbody y hy))
      have hle_b := Infer.frontier_le hinfbody
      have hUni : Unifies R_b τ_b (S_b.onTy β) := by
        show R_b.onTy τ_b = R_b.onTy (S_b.onTy β)
        rw [← htybody, ← Subst.onTy_append]
        exact Subst.onTy_congr hagbody hbβ
      obtain ⟨S_u, h_u, hS_uK⟩ :=
        UnifyRel.complete_K hτb_lc (Subst.onTy_lc hS_b hβlc) hR_b hUni hR_bK
      obtain ⟨R_u, hR_u_eq, hR_u, hR_uK⟩ := UnifyRel.greatest_K h_u R_b hR_b hUni hR_bK
      have hS_u := UnifyRel.lc h_u hτb_lc (Subst.onTy_lc hS_b hβlc)
      have hS_u_below := UnifyRel.belowFvars h_u hbb.1 (Subst.onTy_belowFvars hbb.2 (hbβ.mono hle_b))
      have hwf' : CtxWF (S_u.onCtx (S_b.onCtx ctx)) := Subst.onCtx_wf hS_u (Subst.onCtx_wf hS_b hwf)
      have hbelow_Sb : CtxBelow Φ_b (S_b.onCtx ctx) := Subst.onCtx_below hbb.2 hle_b hbelow
      have hbelow' : CtxBelow Φ_b (S_u.onCtx (S_b.onCtx ctx)) :=
        Subst.onCtx_below hS_u_below (le_refl _) hbelow_Sb
      have key_t : ∀ {t : Ty}, Ty.BelowFvars Φ t → R_u.onTy (S_u.onTy (S_b.onTy t)) = R.onTy t := by
        intro t ht
        rw [← hR_u_eq (S_b.onTy t), ← Subst.onTy_append]
        exact (Subst.onTy_congr hagbody ht).symm
      have htlc' : ∀ τ ∈ βs.map (fun b => S_u.onTy (S_b.onTy b)), τ.IsLC := by
        intro τ hτ; obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hτ
        exact Subst.onTy_lc hS_u (Subst.onTy_lc hS_b (htlc b (List.mem_cons_of_mem _ hb)))
      have htbelow' : ∀ τ ∈ βs.map (fun b => S_u.onTy (S_b.onTy b)), Ty.BelowFvars Φ_b τ := by
        intro τ hτ; obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hτ
        exact Subst.onTy_belowFvars hS_u_below
          (Subst.onTy_belowFvars hbb.2 ((htbelow b (List.mem_cons_of_mem _ hb)).mono hle_b))
      have hctx_eq : R_u.onCtx (S_u.onCtx (S_b.onCtx ctx)) = R.onCtx ctx := by
        rw [← Subst.onCtx_comp_of_onTy_eq hR_u_eq, ← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => (hagbody v hv).symm) hbelow
      have hmapeq : (βs.map (fun b => S_u.onTy (S_b.onTy b))).map R_u.onTy = βs.map R.onTy := by
        rw [List.map_map]; apply List.map_congr_left; intro b hb
        exact key_t (htbelow b (List.mem_cons_of_mem _ hb))
      have hdecl' : ∀ p ∈ rest.zip ((βs.map (fun b => S_u.onTy (S_b.onTy b))).map R_u.onTy),
          TypeOfHM (R_u.onCtx (S_u.onCtx (S_b.onCtx ctx))) p.1 p.2 := by
        rw [hmapeq, hctx_eq]
        intro p hp
        exact hdecl p (by rw [List.map_cons, List.zip_cons_cons]; exact List.mem_cons_of_mem _ hp)
      obtain ⟨Φ', S_r, bindingsOut_r, R_r, hinfrest, hagrest, hR_r, hR_rK, hS_rK⟩ :=
        ih K (by simpa using hlen) ihrest hwf' hbelow' htlc' htbelow' hR_u
          (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle_b) hKrest hR_uK hdecl'
      refine ⟨Φ', S_b ++ S_u ++ S_r, _, R_r, InferRecGroup.cons hinfbody h_u hinfrest, ?_, hR_r, hR_rK, ?_⟩
      · intro v hv
        have hbv1 : Ty.BelowFvars Φ_b (S_b.onTy (.fvar v)) :=
          Subst.onTy_belowFvars hbb.2 (.fvar (by omega))
        have hbv2 : Ty.BelowFvars Φ_b (S_u.onTy (S_b.onTy (.fvar v))) :=
          Subst.onTy_belowFvars hS_u_below hbv1
        calc R.onTy (.fvar v)
            = (S_b ++ R_b).onTy (.fvar v) := hagbody v hv
          _ = R_b.onTy (S_b.onTy (.fvar v)) := by rw [Subst.onTy_append]
          _ = R_u.onTy (S_u.onTy (S_b.onTy (.fvar v))) := hR_u_eq (S_b.onTy (.fvar v))
          _ = (S_r ++ R_r).onTy (S_u.onTy (S_b.onTy (.fvar v))) := Subst.onTy_congr hagrest hbv2
          _ = ((S_b ++ S_u ++ S_r) ++ R_r).onTy (.fvar v) := by simp only [Subst.onTy_append]
      · intro p hp
        rcases List.mem_append.mp hp with hp' | hp'
        · rcases List.mem_append.mp hp' with hp'' | hp''
          · exact hS_bK p hp''
          · exact hS_uK p hp''
        · exact hS_rK p hp'


/-- Principality, `match_` case (factored with named binders). Recurse on the
    scrutinee, allocate the single result variable `Φ₁`, realise the residual
    `Φ₁ ↦ τ₀` via a fresh-variable dodge, thread the branch list with
    `InferBranches.complete` (which now owns the per-branch `customTy`
    unification), and assemble the agreement chain. The scrutinee type stays free
    (it is constrained only inside named branches), so an all-wildcard `match`
    has a principal type. -/
theorem Infer.complete_match_aux {scrut : Expr} {branches : List (MatchPattern × Expr)}
    {Φ : Nat} {ctx : Ctx} {S₀ : Subst} {scrutTy : Ty} {τ₀ : Ty}
    {K : List Nat}
    (ihscrut : Infer.CompleteAt scrut)
    (ihbranches : ∀ br ∈ branches, Infer.CompleteAt br.2)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hS₀ : ∀ p ∈ S₀, p.2.IsLC)
    (hKΦ : ∀ k ∈ K, k < Φ) (hKscrut : ∀ y ∈ scrut.tyFreeVars, y ∈ K)
    (hKbr : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars branches, y ∈ K)
    (hKfix : ∀ k ∈ K, S₀.onTy (.fvar k) = .fvar k)
    (hscrut_decl : TypeOfHM (S₀.onCtx ctx) scrut scrutTy)
    (hne : branches ≠ [])
    (hbranches_decl : ∀ br ∈ branches, TypeOfMatchBranch (S₀.onCtx ctx) br scrutTy τ₀) :
    ∃ Φ' S eOut τ R,
      Infer Φ ctx (.match_ scrut branches) Φ' S eOut τ ∧
      Subst.AgreesBelow Φ S₀ (S ++ R) ∧
      τ₀ = R.onTy τ ∧ (∀ p ∈ R, p.2.IsLC) ∧
      (∀ k ∈ K, R.onTy (.fvar k) = .fvar k) ∧
      (∀ p ∈ S, p.1 ∉ K) := by
  -- STEP 1: scrutinee IH.
  obtain ⟨Φ₁, S₁, eOuts, τs, R₁, hinfs, hags, htys, hR₁, hR₁K, hS₁K⟩ :=
    ihscrut K hwf hbelow hS₀ hKΦ hKscrut hKfix hscrut_decl
  have hS₁ := (Infer.lc hinfs hwf).2
  have hτsLC := (Infer.lc hinfs hwf).1
  have hbs := Infer.belowFvars hinfs hbelow (fun y hy => hKΦ y (hKscrut y hy))
  have hle1 := Infer.frontier_le hinfs
  have hwf₁ := Subst.onCtx_wf hS₁ hwf
  have hbelow₁ := Subst.onCtx_below hbs.2 hle1 hbelow
  have hctxeq : R₁.onCtx (S₁.onCtx ctx) = S₀.onCtx ctx := by
    rw [← Subst.onCtx_append]
    exact Subst.onCtx_congr (fun v hv => (hags v hv).symm) hbelow
  have hτ₀_lc : τ₀.IsLC := by
    obtain ⟨hd, tl, hcons⟩ := List.exists_cons_of_ne_nil hne
    exact TypeOfMatchBranch.regular (hbranches_decl hd (by rw [hcons]; exact List.mem_cons_self ..))
  -- STEP 2: fresh result-target variable `W` and the explicit residual `U` (= `Φ₁ ↦ τ₀`).
  obtain ⟨W, hWge, hWfresh⟩ := exists_fresh_block
    (R₁.map Prod.fst ++ R₁.flatMap (fun p => p.2.freeVars) ++ τ₀.freeVars) Φ₁ 1
  have hWdom : ∀ p ∈ R₁, p.1 ≠ W := by
    intro p hp he
    have := hWfresh p.1 (List.mem_append_left _ (List.mem_append_left _ (List.mem_map.mpr ⟨p, hp, rfl⟩)))
    omega
  have hWrange : ∀ p ∈ R₁, W ∉ p.2.freeVars := by
    intro p hp hc
    have := hWfresh W (List.mem_append_left _ (List.mem_append_right _ (List.mem_flatMap.mpr ⟨p, hp, hc⟩)))
    omega
  have hWτ₀ : W ∉ τ₀.freeVars := fun hc => by
    have := hWfresh W (List.mem_append_right _ hc); omega
  have hR₁Wfvar : R₁.onTy (Ty.fvar W) = Ty.fvar W := by
    apply Ty.substFvars_eq_self_of_no_key
    intro p hp hc
    simp only [Ty.freeVars, List.mem_singleton] at hc
    exact hWdom p hp hc
  obtain ⟨U, hUdef⟩ : ∃ U : Subst, U = [(Φ₁, Ty.fvar W)] ++ R₁ ++ [(W, τ₀)] := ⟨_, rfl⟩
  have hsingle : ∀ (Z : Nat) (V y : Ty), Subst.onTy [(Z, V)] y = Ty.substFvar Z V y :=
    fun _ _ _ => rfl
  have hUonTy : ∀ x, U.onTy x = Ty.substFvar W τ₀ (R₁.onTy (Ty.substFvar Φ₁ (Ty.fvar W) x)) := by
    intro x; rw [hUdef, Subst.onTy_append, Subst.onTy_append, hsingle, hsingle]
  have hUlc : ∀ p ∈ U, p.2.IsLC := by
    rw [hUdef]
    intro p hp
    rcases List.mem_append.mp hp with hp' | hp'
    · rcases List.mem_append.mp hp' with hp'' | hp''
      · obtain rfl := List.mem_singleton.mp hp''; exact ContainsBvarsUpTo.fvar
      · exact hR₁ p hp''
    · obtain rfl := List.mem_singleton.mp hp'; exact hτ₀_lc
  have hUeqR₁ : ∀ v, v < Φ₁ → U.onTy (Ty.fvar v) = R₁.onTy (Ty.fvar v) := by
    intro v hv
    have hvΦ₁ : Φ₁ ∉ (Ty.fvar v).freeVars := by simp only [Ty.freeVars, List.mem_singleton]; omega
    have hvW : W ∉ (R₁.onTy (Ty.fvar v)).freeVars := by
      apply Subst.not_mem_onTy_freeVars hWrange
      simp only [Ty.freeVars, List.mem_singleton]; omega
    rw [hUonTy, Ty.substFvar_fresh hvΦ₁, Ty.substFvar_fresh hvW]
  have hUΦ₁ : U.onTy (Ty.fvar Φ₁) = τ₀ := by
    rw [hUonTy, show Ty.substFvar Φ₁ (Ty.fvar W) (Ty.fvar Φ₁) = Ty.fvar W from by simp [Ty.substFvar],
        hR₁Wfvar, show Ty.substFvar W τ₀ (Ty.fvar W) = τ₀ from by simp [Ty.substFvar]]
  have hUK : ∀ k ∈ K, U.onTy (Ty.fvar k) = Ty.fvar k := fun k hk =>
    (hUeqR₁ k (lt_of_lt_of_le (hKΦ k hk) hle1)).trans (hR₁K k hk)
  -- STEP 3: recast the branch declarative typings under `U`.
  have hbr' : ∀ br ∈ branches,
      TypeOfMatchBranch (U.onCtx (S₁.onCtx ctx)) br (U.onTy τs) (U.onTy (Ty.fvar Φ₁)) := by
    intro br hbr
    rw [Subst.onCtx_congr hUeqR₁ hbelow₁, hctxeq, Subst.onTy_congr hUeqR₁ hbs.1, htys.symm, hUΦ₁]
    exact hbranches_decl br hbr
  -- STEP 4: thread the branch list (which owns the per-branch `customTy` unification).
  obtain ⟨Φ₂, S₂, brsOut, R₂, hinfbr, hagbr, hR₂lc, hR₂K, hS₂K⟩ :=
    InferBranches.complete (Φ := Φ₁ + 1) (ρ := Ty.fvar Φ₁) (R := U) K ihbranches hwf₁
      (Subst.onCtx_below (fun p hp => (hbs.2 p hp).mono (by omega)) (by omega) hbelow)
      hτsLC (hbs.1.mono (by omega)) ContainsBvarsUpTo.fvar (.fvar (by omega))
      hUlc (fun k hk => by have := hKΦ k hk; have := hle1; omega) hKbr hUK hbr'
  -- STEP 5: assemble.
  refine ⟨Φ₂, S₁ ++ S₂, _, S₂.onTy (Ty.fvar Φ₁), R₂,
    Infer.match_ hinfs hne hinfbr, ?_, ?_, hR₂lc, hR₂K, ?_⟩
  · intro v hv
    have ht1 : Ty.BelowFvars Φ₁ (S₁.onTy (Ty.fvar v)) :=
      Subst.onTy_belowFvars hbs.2 (Ty.BelowFvars.fvar (by omega))
    calc S₀.onTy (Ty.fvar v)
        = (S₁ ++ R₁).onTy (Ty.fvar v) := hags v hv
      _ = R₁.onTy (S₁.onTy (Ty.fvar v)) := by rw [Subst.onTy_append]
      _ = U.onTy (S₁.onTy (Ty.fvar v)) := (Subst.onTy_congr hUeqR₁ ht1).symm
      _ = (S₂ ++ R₂).onTy (S₁.onTy (Ty.fvar v)) := Subst.onTy_congr hagbr (ht1.mono (by omega))
      _ = ((S₁ ++ S₂) ++ R₂).onTy (Ty.fvar v) := by simp only [Subst.onTy_append]
  · calc τ₀ = U.onTy (Ty.fvar Φ₁) := hUΦ₁.symm
      _ = (S₂ ++ R₂).onTy (Ty.fvar Φ₁) := Subst.onTy_congr hagbr (Ty.BelowFvars.fvar (by omega))
      _ = R₂.onTy (S₂.onTy (Ty.fvar Φ₁)) := by rw [Subst.onTy_append]
  · intro p hp
    rcases List.mem_append.mp hp with hp' | hp'
    · exact hS₁K p hp'
    · exact hS₂K p hp'

/-- Principality, `match_` case. -/
theorem Infer.complete_match {scrut : Expr} {branches : List (MatchPattern × Expr)}
    (ihscrut : Infer.CompleteAt scrut)
    (ihbranches : ∀ br ∈ branches, Infer.CompleteAt br.2) :
    Infer.CompleteAt (.match_ scrut branches) := by
  intro Φ ctx S₀ τ₀ K hwf hbelow hS₀ hKΦ hKe hKfix hty
  simp only [Expr.tyFreeVars, List.mem_append] at hKe
  cases hty with
  | match_ hscrut_decl hne hbranches_decl =>
    exact Infer.complete_match_aux ihscrut ihbranches hwf hbelow hS₀ hKΦ
      (fun y hy => hKe y (.inl hy)) (fun y hy => hKe y (.inr hy)) hKfix
      hscrut_decl hne hbranches_decl

/-! ### Principality of a given inference (`complete'`) → output-uniqueness + `IsPrincipal`

`Infer.complete'` is `Infer.complete` reoriented: rather than *producing* a
derivation from a typing, it takes a *given* derivation and shows every typing
factors through *its* output. Pulling the typing back through one derivation
avoids the diverging-substituted-context problem that an `output_unique`-by-
two-derivations attack would hit. -/

-- `Infer.complete'` is a large mutual block (~1700 lines, now incl. the `letRec`
-- case + `InferRecGroup.complete'`): its elaboration needs a raised `maxRecDepth`
-- (the default 512 is exceeded by the equation-compiler / well-founded-recursion
-- machinery for a mutual this size) and a raised `maxHeartbeats` (the `letRec`
-- case's context manipulations push it past the default 200000 budget).
set_option maxRecDepth 4000 in
set_option maxHeartbeats 1000000 in
mutual
/-- Principality of a *given* derivation: if `Infer` derived `(S, τ)` for `e`,
    then any declarative typing `τ₀` of `e` (under an LC specialization `S₀` of a
    WF, frontier-bounded `ctx`) factors through `τ` via an LC residual `R`, with
    `S₀ = R ∘ S` below `Φ`. -/
theorem Infer.complete' {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) {S₀ : Subst} {τ₀ : Ty}
    (hS₀ : ∀ p ∈ S₀, p.2.IsLC) (K : List Nat)
    (hKΦ : ∀ k ∈ K, k < Φ) (hKe : ∀ y ∈ e.tyFreeVars, y ∈ K)
    (hKfix : ∀ k ∈ K, S₀.onTy (.fvar k) = .fvar k)
    (hty : TypeOfHM (S₀.onCtx ctx) e τ₀) :
    ∃ R, Subst.AgreesBelow Φ S₀ (S ++ R) ∧ τ₀ = R.onTy τ ∧ (∀ p ∈ R, p.2.IsLC) ∧
      (∀ k ∈ K, R.onTy (.fvar k) = .fvar k) := by
  cases h with
  | primLitUnit => cases hty; exact ⟨S₀, fun v _ => by rw [List.nil_append], by simp, hS₀, hKfix⟩
  | primLitInt  => cases hty; exact ⟨S₀, fun v _ => by rw [List.nil_append], by simp, hS₀, hKfix⟩
  | primLitNat  => cases hty; exact ⟨S₀, fun v _ => by rw [List.nil_append], by simp, hS₀, hKfix⟩
  -- | primLitBool => cases hty; exact ⟨S₀, fun v _ => by rw [List.nil_append], by simp, hS₀, hKfix⟩
  | primLitStr  => cases hty; exact ⟨S₀, fun v _ => by rw [List.nil_append], by simp, hS₀, hKfix⟩
  | var hlook =>
    obtain ⟨_, _, _, _, R, hinf, hag, hfac, hRlc, hRfix, _⟩ :=
      Infer.complete_var K hwf hbelow hS₀ hKΦ hKe hKfix hty
    cases hinf with
    | var hlook' =>
      obtain rfl := Option.some.inj (hlook.symm.trans hlook')
      exact ⟨R, hag, hfac, hRlc, hRfix⟩
  | ctor hlook =>
    obtain ⟨_, _, _, _, R, hinf, hag, hfac, hRlc, hRfix, _⟩ :=
      Infer.complete_ctor K hwf hbelow hS₀ hKΦ hKe hKfix hty
    cases hinf with
    | ctor hlook' =>
      obtain rfl := Option.some.inj (hlook.symm.trans hlook')
      exact ⟨R, hag, hfac, hRlc, hRfix⟩
  | lambda hseed Dbody =>
    cases hty with
    | lambda hparamLC hann heq hbodyty =>
      subst heq
      cases hseed
      case some hcl =>
        expose_names
        have hpeq := hann paramTy rfl
        rw [hpeq] at hbodyty
        simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at hKe
        have hTK : ∀ y ∈ paramTy.freeVars, y ∈ K := fun y hy => hKe y (.inl hy)
        have hbodyK : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKe y (.inr hy)
        have hTbelow : Ty.BelowFvars Φ paramTy :=
          Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hTK v hv))
        have hself : S₀.onTy paramTy = paramTy :=
          Subst.onTy_eq_self_of_fixes (fun v hv => hKfix v (hTK v hv))
        have hwf_b : CtxWF { ctx with env := PolyTy.mkTrivial paramTy :: ctx.env } := by
          intro M hM; rcases List.mem_cons.mp hM with rfl | hM
          · exact hcl
          · exact hwf M hM
        have hbelow_b : CtxBelow Φ { ctx with env := PolyTy.mkTrivial paramTy :: ctx.env } := by
          intro M hM; rcases List.mem_cons.mp hM with rfl | hM
          · exact hTbelow
          · exact hbelow M hM
        have hbody' : TypeOfHM (S₀.onCtx { ctx with env := PolyTy.mkTrivial paramTy :: ctx.env }) body bodyTy := by
          have heq2 : S₀.onCtx { ctx with env := PolyTy.mkTrivial paramTy :: ctx.env }
              = { (S₀.onCtx ctx) with env := PolyTy.mkTrivial paramTy :: (S₀.onCtx ctx).env } := by
            simp only [Subst.onCtx, Subst.onEnv, List.map_cons, Subst.onPolyTy, PolyTy.mkTrivial, hself]
          rw [heq2]; exact hbodyty
        obtain ⟨R, hag, htyb, hRlc, hRfix⟩ :=
          Infer.complete' Dbody hwf_b hbelow_b hS₀ K hKΦ hbodyK hKfix hbody'
        refine ⟨R, hag, ?_, hRlc, hRfix⟩
        rw [hpeq, Subst.onTy_arrow]
        refine congrArg₂ Ty.arrow ?_ htyb
        rw [← Subst.onTy_append, ← Subst.onTy_congr hag hTbelow, hself]
      expose_names
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append] at hKe
      have hbodyK : ∀ y ∈ body.tyFreeVars, y ∈ K := hKe
      -- STEP 0: fresh names
      obtain ⟨W, c, hΦW, hΦc, hWc, hWav, hcav⟩ := exists_fresh_two_ge Φ
        ([Φ] ++ S₀.map Prod.fst ++ S₀.flatMap (fun p => p.2.freeVars)
          ++ paramTy.freeVars ++ bodyTy.freeVars)
      simp only [List.mem_append, List.mem_singleton, List.mem_map, List.mem_flatMap] at hWav hcav
      push_neg at hWav hcav
      obtain ⟨⟨⟨⟨hWΦ, hWkey⟩, hWrange⟩, hWparam⟩, hWbody⟩ := hWav
      obtain ⟨⟨⟨⟨hcΦ, hckey⟩, hcrange⟩, hcparam⟩, hcbody⟩ := hcav
      have finj : Function.Injective (swapNat Φ W) := swapNat_injective Φ W
      have hfix : ∀ v, v < Φ → swapNat Φ W v = v := fun v hv =>
        swapNat_other (by omega) (by omega)
      have hWonTy : ∀ {τ : Ty}, W ∉ τ.freeVars → W ∉ (S₀.onTy τ).freeVars :=
        fun h => Subst.not_mem_onTy_freeVars hWrange h
      have hconΦ : ∀ p ∈ Subst.conj (swapNat Φ W) S₀, p.1 ≠ Φ := by
        intro p hp
        simp only [Subst.conj, List.mem_map] at hp
        obtain ⟨q, hq, rfl⟩ := hp
        intro hc
        apply hWkey q hq
        have hc' : swapNat Φ W q.1 = Φ := hc
        simp only [swapNat] at hc'
        split_ifs at hc' <;> omega
      have hSconjΦ : (Subst.conj (swapNat Φ W) S₀).onTy (.fvar Φ) = .fvar Φ := by
        apply Ty.substFvars_eq_self_of_no_key
        intro p hp hc
        simp only [Ty.freeVars, List.mem_singleton] at hc
        exact hconΦ p hp hc
      -- STEP 1: rename the body derivation and reinterpret its context
      have hctxeq : (swapSubst Φ W c).onCtx
            { (S₀.onCtx ctx) with env := PolyTy.mkTrivial paramTy :: (S₀.onCtx ctx).env }
          = (Subst.conj (swapNat Φ W) S₀ ++ [(Φ, Ty.rename (swapNat Φ W) paramTy)]).onCtx
            { ctx with env := PolyTy.mkTrivial (.fvar Φ) :: ctx.env } := by
        simp only [Subst.onCtx, Subst.onEnv, List.map_cons, List.map_map]
        congr 1
        congr 1
        · -- head
          simp only [Subst.onPolyTy, PolyTy.mkTrivial, PolyTy.mk.injEq, true_and]
          rw [swapSubst_onTy (Ne.symm hWΦ) (Ne.symm hcΦ) hWc hcparam,
              Subst.onTy_append (Subst.conj (swapNat Φ W) S₀)
                [(Φ, Ty.rename (swapNat Φ W) paramTy)] (.fvar Φ),
              hSconjΦ]
          simp only [Subst.onTy, Ty.substFvars, Ty.substFvar, if_pos]
        · -- tail
          apply List.map_congr_left
          intro M hM
          have hMbelow : ∀ v ∈ M.body.freeVars, v < Φ := (hbelow M hM).mem_lt
          have hrenM : Ty.rename (swapNat Φ W) M.body = M.body :=
            Ty.rename_eq_self (fun v hv => hfix v (hMbelow v hv))
          have hΦnotin : Φ ∉ (Ty.rename (swapNat Φ W) (S₀.onTy M.body)).freeVars :=
            Ty.rename_swap_not_mem_left (hWonTy (τ := M.body) (fun hv => by have := hMbelow _ hv; omega))
          simp only [Function.comp, Subst.onPolyTy, PolyTy.mk.injEq, true_and]
          rw [swapSubst_onTy (Ne.symm hWΦ) (Ne.symm hcΦ) hWc
                (Subst.not_mem_onTy_freeVars hcrange (fun hv => by have := hMbelow _ hv; omega)),
              Subst.onTy_append (Subst.conj (swapNat Φ W) S₀)
                [(Φ, Ty.rename (swapNat Φ W) paramTy)] M.body]
          conv_rhs => rw [← hrenM, Subst.onTy_conj finj]
          exact (Ty.substFvar_fresh hΦnotin).symm
      have hbodyTyeq : (swapSubst Φ W c).onTy bodyTy = Ty.rename (swapNat Φ W) bodyTy :=
        swapSubst_onTy (Ne.symm hWΦ) (Ne.symm hcΦ) hWc hcbody
      have hren := TypeOfHM.onSubst (swapSubst Φ W c) (swapSubst_lc Φ W c) hbodyty
      rw [hctxeq, hbodyTyeq] at hren
      have hbodyfix : body.substTyFvars (swapSubst Φ W c) = body := by
        refine Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars (fun p hp hc => ?_)
        have hpΦ : Φ ≤ p.1 := by
          simp only [swapSubst, List.mem_cons, List.not_mem_nil, or_false] at hp
          obtain rfl | rfl | rfl := hp <;> omega
        have := hKΦ p.1 (hbodyK p.1 hc); omega
      rw [hbodyfix] at hren
      -- STEP 2: apply the body IH under the conjugated specialization
      have hwf_b : CtxWF { ctx with env := PolyTy.mkTrivial (.fvar Φ) :: ctx.env } := by
        intro M hM
        rcases List.mem_cons.mp hM with rfl | hM
        · exact ContainsBvarsUpTo.fvar
        · exact hwf M hM
      have hbelow_b : CtxBelow (Φ + 1) { ctx with env := PolyTy.mkTrivial (.fvar Φ) :: ctx.env } := by
        intro M hM
        rcases List.mem_cons.mp hM with rfl | hM
        · exact .fvar (by omega)
        · exact (hbelow M hM).mono (by omega)
      have hT_lc : ∀ p ∈ Subst.conj (swapNat Φ W) S₀ ++ [(Φ, Ty.rename (swapNat Φ W) paramTy)],
          p.2.IsLC := by
        intro p hp
        rw [List.mem_append] at hp
        rcases hp with hp | hp
        · exact Subst.conj_lc hS₀ p hp
        · rw [List.mem_singleton] at hp
          subst hp
          exact Ty.rename_isLC hparamLC
      have hSconjK : ∀ k ∈ K, (Subst.conj (swapNat Φ W) S₀
          ++ [(Φ, Ty.rename (swapNat Φ W) paramTy)]).onTy (.fvar k) = .fvar k := by
        intro k hk
        have hklt := hKΦ k hk
        rw [Subst.onTy_append]
        have hconj : (Subst.conj (swapNat Φ W) S₀).onTy (.fvar k) = .fvar k := by
          have h := Subst.onTy_conj finj S₀ (.fvar k)
          rw [Ty.rename_fvar, hfix k hklt] at h
          rw [h, hKfix k hk, Ty.rename_fvar, hfix k hklt]
        rw [hconj]
        exact Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
          rw [List.mem_singleton] at hp; subst hp
          simp only [Ty.freeVars, List.mem_singleton] at hc; omega)
      obtain ⟨R_b, hagb, htyb, hR_b, hR_bfix⟩ :=
        Infer.complete' Dbody hwf_b hbelow_b hT_lc K (fun k hk => by have := hKΦ k hk; omega)
          hbodyK hSconjK hren
      -- STEP 3: assemble the conclusion
      refine ⟨R_b ++ [(W, .fvar Φ)], ?_, ?_, ?_, ?_⟩
      · -- agreement below Φ
        intro v hv
        have hWv : W ∉ (Ty.fvar v).freeVars := by
          simp only [Ty.freeVars, List.mem_singleton]; omega
        have hconjv : (Subst.conj (swapNat Φ W) S₀).onTy (.fvar v)
            = Ty.rename (swapNat Φ W) (S₀.onTy (.fvar v)) := by
          have h := Subst.onTy_conj finj S₀ (.fvar v)
          rw [Ty.rename_fvar, hfix v hv] at h
          exact h
        have hTv : (Subst.conj (swapNat Φ W) S₀ ++ [(Φ, Ty.rename (swapNat Φ W) paramTy)]).onTy (.fvar v)
            = Ty.rename (swapNat Φ W) (S₀.onTy (.fvar v)) := by
          rw [Subst.onTy_append (Subst.conj (swapNat Φ W) S₀)
                [(Φ, Ty.rename (swapNat Φ W) paramTy)] (.fvar v), hconjv]
          exact Ty.substFvar_fresh (Ty.rename_swap_not_mem_left (hWonTy (τ := .fvar v) hWv))
        rw [← List.append_assoc, Subst.onTy_append (S ++ R_b) [(W, .fvar Φ)] (.fvar v),
            ← hagb v (by omega), hTv]
        exact (Ty.substFvar_rename_swap (hWonTy (τ := .fvar v) hWv)).symm
      · -- the arrow type is recovered
        have hTΦ : (Subst.conj (swapNat Φ W) S₀ ++ [(Φ, Ty.rename (swapNat Φ W) paramTy)]).onTy (.fvar Φ)
            = Ty.rename (swapNat Φ W) paramTy := by
          rw [Subst.onTy_append (Subst.conj (swapNat Φ W) S₀)
                [(Φ, Ty.rename (swapNat Φ W) paramTy)] (.fvar Φ), hSconjΦ]
          simp only [Subst.onTy, Ty.substFvars, Ty.substFvar, if_pos]
        rw [Subst.onTy_arrow]
        refine congrArg₂ Ty.arrow ?_ ?_
        · rw [Subst.onTy_append R_b [(W, .fvar Φ)] (S.onTy (.fvar Φ)),
              ← Subst.onTy_append S R_b (.fvar Φ),
              ← hagb Φ (by omega), hTΦ]
          exact (Ty.substFvar_rename_swap hWparam).symm
        · rw [Subst.onTy_append R_b [(W, .fvar Φ)] τb, ← htyb]
          exact (Ty.substFvar_rename_swap hWbody).symm
      · -- residual is LC
        intro p hp
        rw [List.mem_append] at hp
        rcases hp with hp | hp
        · exact hR_b p hp
        · rw [List.mem_singleton] at hp
          subst hp
          exact ContainsBvarsUpTo.fvar
      · -- residual fixes the rigid set `K`
        intro k hk
        rw [Subst.onTy_append, hR_bfix k hk]
        exact Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
          rw [List.mem_singleton] at hp; subst hp
          simp only [Ty.freeVars, List.mem_singleton] at hc
          have := hKΦ k hk; omega)
  | app Df Darg Duni =>
    cases hty with
    | app hf harg =>
      expose_names
      simp only [Expr.tyFreeVars, List.mem_append] at hKe
      -- STEP 1: recurse on `f`.
      obtain ⟨R₁, hagf, htyf, hR₁, hR₁fix⟩ :=
        Infer.complete' Df hwf hbelow hS₀ K hKΦ (fun y hy => hKe y (.inl hy)) hKfix hf
      have hS₁ := (Infer.lc Df hwf).2
      have hbf := Infer.belowFvars Df hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))
      have hle1 := Infer.frontier_le Df
      have hwf₁ := Subst.onCtx_wf hS₁ hwf
      have hbelow₁ := Subst.onCtx_below hbf.2 hle1 hbelow
      -- STEP 2: recurse on `arg`.
      have hctxeq : R₁.onCtx (S₁.onCtx ctx) = S₀.onCtx ctx := by
        rw [← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => (hagf v hv).symm) hbelow
      have harg' : TypeOfHM (R₁.onCtx (S₁.onCtx ctx)) arg argTy := by
        rw [hctxeq]; exact harg
      obtain ⟨R₂, haga, htya, hR₂, hR₂fix⟩ :=
        Infer.complete' Darg hwf₁ hbelow₁ hR₁ K (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle1)
          (fun y hy => hKe y (.inr hy)) hR₁fix harg'
      have hS₂ := (Infer.lc Darg hwf₁).2
      have hba := Infer.belowFvars Darg hbelow₁
        (fun y hy => lt_of_lt_of_le (hKΦ y (hKe y (.inr hy))) hle1)
      have hle2 := Infer.frontier_le Darg
      -- STEP 3: key equalities.
      have hcongr_f : R₁.onTy τf = (S₂ ++ R₂).onTy τf := Subst.onTy_congr haga hbf.1
      have hP : R₂.onTy (S₂.onTy τf) = Ty.arrow argTy τ₀ := by
        rw [← Subst.onTy_append, ← hcongr_f]; exact htyf.symm
      have hΦ₂τf : Φ₂ ∉ (S₂.onTy τf).freeVars := fun hm => by
        have := (Subst.onTy_belowFvars hba.2 (hbf.1.mono hle2)).mem_lt _ hm
        omega
      have hΦ₂τa : Φ₂ ∉ τa.freeVars := fun hm => by
        have := hba.1.mem_lt _ hm
        omega
      have hτ₀LC : τ₀.IsLC := by
        have hreg := TypeOfHM.regular hf
        cases hreg with
        | arrow _ hT => exact hT
      -- STEP 4: a fresh variable `W`.
      obtain ⟨W, hWge, hWfresh⟩ := exists_fresh_block
        (R₂.map Prod.fst ++ R₂.flatMap (fun p => p.2.freeVars) ++ argTy.freeVars ++ τ₀.freeVars) Φ₂ 1
      have hWdom : ∀ p ∈ R₂, p.1 ≠ W := by
        intro p hp he
        have := hWfresh p.1 (List.mem_append_left _ (List.mem_append_left _
          (List.mem_append_left _ (List.mem_map.mpr ⟨p, hp, rfl⟩))))
        omega
      have hWrange : ∀ p ∈ R₂, W ∉ p.2.freeVars := by
        intro p hp hc
        have := hWfresh W (List.mem_append_left _ (List.mem_append_left _
          (List.mem_append_right _ (List.mem_flatMap.mpr ⟨p, hp, hc⟩))))
        omega
      have hWargTy : W ∉ argTy.freeVars := fun hc => by
        have := hWfresh W (List.mem_append_left _ (List.mem_append_right _ hc)); omega
      have hWτ₀ : W ∉ τ₀.freeVars := fun hc => by
        have := hWfresh W (List.mem_append_right _ hc); omega
      have hR₂Wfvar : R₂.onTy (Ty.fvar W) = Ty.fvar W := by
        apply Ty.substFvars_eq_self_of_no_key
        intro p hp hc
        simp only [Ty.freeVars, List.mem_singleton] at hc
        exact hWdom p hp hc
      -- STEP 5: the explicit unifier `U`.
      obtain ⟨U, hUdef⟩ : ∃ U : Subst, U = [(Φ₂, Ty.fvar W)] ++ R₂ ++ [(W, τ₀)] := ⟨_, rfl⟩
      have hsingle : ∀ (Z : Nat) (V y : Ty), Subst.onTy [(Z, V)] y = Ty.substFvar Z V y :=
        fun _ _ _ => rfl
      have hsubArrow : ∀ (Z : Nat) (V a b : Ty),
          Ty.substFvar Z V (Ty.arrow a b) = Ty.arrow (Ty.substFvar Z V a) (Ty.substFvar Z V b) :=
        fun _ _ _ _ => rfl
      have hUonTy : ∀ x, U.onTy x = Ty.substFvar W τ₀ (R₂.onTy (Ty.substFvar Φ₂ (Ty.fvar W) x)) := by
        intro x
        rw [hUdef, Subst.onTy_append, Subst.onTy_append, hsingle, hsingle]
      have e1 : Ty.substFvar Φ₂ (Ty.fvar W) (Ty.fvar Φ₂) = Ty.fvar W := by simp [Ty.substFvar]
      have e2 : Ty.substFvar W τ₀ (Ty.fvar W) = τ₀ := by simp [Ty.substFvar]
      have hUniL : U.onTy (S₂.onTy τf) = Ty.arrow argTy τ₀ := by
        rw [hUonTy, Ty.substFvar_fresh hΦ₂τf, hP, hsubArrow,
            Ty.substFvar_fresh hWargTy, Ty.substFvar_fresh hWτ₀]
      have hUniR : U.onTy (Ty.arrow τa (Ty.fvar Φ₂)) = Ty.arrow argTy τ₀ := by
        rw [hUonTy, hsubArrow, Ty.substFvar_fresh hΦ₂τa, e1, Subst.onTy_arrow,
            hR₂Wfvar, ← htya, hsubArrow, Ty.substFvar_fresh hWargTy, e2]
      have hUni : Unifies U (S₂.onTy τf) (Ty.arrow τa (Ty.fvar Φ₂)) := by
        show U.onTy (S₂.onTy τf) = U.onTy (Ty.arrow τa (Ty.fvar Φ₂))
        rw [hUniL, hUniR]
      -- STEP 6: the MGU `S₃` is the *given* `Duni`.
      have h₃ := Duni
      -- STEP 7: factor `U` through `S₃`.
      have hUlc : ∀ p ∈ U, p.2.IsLC := by
        rw [hUdef]
        intro p hp
        rcases List.mem_append.mp hp with hp' | hp'
        · rcases List.mem_append.mp hp' with hp'' | hp''
          · obtain rfl := List.mem_singleton.mp hp''
            exact ContainsBvarsUpTo.fvar
          · exact hR₂ p hp''
        · obtain rfl := List.mem_singleton.mp hp'
          exact hτ₀LC
      have hUK : ∀ k ∈ K, U.onTy (Ty.fvar k) = Ty.fvar k := by
        intro k hk
        have hklt : k < Φ₂ := lt_of_lt_of_le (hKΦ k hk) (le_trans hle1 hle2)
        have h1 : Ty.substFvar Φ₂ (Ty.fvar W) (Ty.fvar k) = Ty.fvar k :=
          Ty.substFvar_fresh (by simp only [Ty.freeVars, List.mem_singleton]; omega)
        rw [hUonTy, h1, hR₂fix k hk]
        exact Ty.substFvar_fresh (by simp only [Ty.freeVars, List.mem_singleton]; omega)
      obtain ⟨R₃, hR₃, hR₃lc, hR₃fix⟩ := UnifyRel.greatest_K h₃ U hUlc hUni hUK
      -- STEP 8: assemble.
      refine ⟨R₃, ?_, ?_, hR₃lc, hR₃fix⟩
      · intro v hv
        have ht1 : Ty.BelowFvars Φ₁ (S₁.onTy (Ty.fvar v)) :=
          Subst.onTy_belowFvars hbf.2 (Ty.BelowFvars.fvar (by omega))
        have ht : Ty.BelowFvars Φ₂ (S₂.onTy (S₁.onTy (Ty.fvar v))) :=
          Subst.onTy_belowFvars hba.2 (ht1.mono hle2)
        have hΦ₂t : Φ₂ ∉ (S₂.onTy (S₁.onTy (Ty.fvar v))).freeVars := fun hm => by
          have := ht.mem_lt _ hm; omega
        have hWt : W ∉ (S₂.onTy (S₁.onTy (Ty.fvar v))).freeVars := fun hm => by
          have := ht.mem_lt _ hm; omega
        have hWR₂t : W ∉ (R₂.onTy (S₂.onTy (S₁.onTy (Ty.fvar v)))).freeVars :=
          Subst.not_mem_onTy_freeVars hWrange hWt
        simp only [Subst.onTy_append]
        rw [← hR₃ (S₂.onTy (S₁.onTy (Ty.fvar v))), hUonTy,
            Ty.substFvar_fresh hΦ₂t, Ty.substFvar_fresh hWR₂t,
            hagf v hv, Subst.onTy_append, Subst.onTy_congr haga ht1, Subst.onTy_append]
      · rw [← hR₃ (Ty.fvar Φ₂), hUonTy, e1, hR₂Wfvar, e2]
  | letIn Drhs Dbody =>
    cases hty with
    | letIn hMwf hann hcofin heq hbody =>
      subst heq
      expose_names
      simp only [Expr.openBoundTyVars] at hcofin
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append] at hKe
      obtain ⟨Xs, hXlen, hXnodup, hXavoid⟩ :=
        exists_fresh_names
          (L ++ M.body.freeVars ++ (S₀.onCtx ctx).env.freeVars ++ rhs.tyFreeVars) M.paramCount
      have hXfreshL : FreshNames L M.paramCount Xs :=
        ⟨hXlen, hXnodup, fun x hx hc =>
          hXavoid x hx (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_left _ hc)))⟩
      have hXMbody : ∀ x ∈ Xs, x ∉ M.body.freeVars :=
        fun x hx hc =>
          hXavoid x hx (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_right _ hc)))
      have hXenv : ∀ x ∈ Xs, x ∉ (S₀.onCtx ctx).env.freeVars :=
        fun x hx hc => hXavoid x hx (List.mem_append_left _ (List.mem_append_right _ hc))
      have hXrhs : ∀ x ∈ Xs, x ∉ rhs.tyFreeVars :=
        fun x hx hc => hXavoid x hx (List.mem_append_right _ hc)
      obtain ⟨R₁, haga, htya, hR₁, hR₁fix⟩ :=
        Infer.complete' Drhs hwf hbelow hS₀ K hKΦ (fun y hy => hKe y (.inl hy)) hKfix
          (hcofin Xs hXfreshL)
      have hS₁ := (Infer.lc Drhs hwf).2
      have hτ₁lc := (Infer.lc Drhs hwf).1
      have hle := Infer.frontier_le Drhs
      have hbelowτ₁ := (Infer.belowFvars Drhs hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))).1
      have hbelowS₁ := (Infer.belowFvars Drhs hbelow (fun y hy => hKΦ y (hKe y (.inl hy)))).2
      have hwf₁ := Subst.onCtx_wf hS₁ hwf
      have hbelow₁ := Subst.onCtx_below hbelowS₁ hle hbelow
      have hctxeq : R₁.onCtx (S₁.onCtx ctx) = S₀.onCtx ctx := by
        rw [← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => (haga v hv).symm) hbelow
      have hwfBody : CtxWF { (S₁.onCtx ctx) with
          env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env } := by
        intro N hN
        rcases List.mem_cons.mp hN with rfl | hN
        · exact genScheme_wf hτ₁lc
        · exact hwf₁ N hN
      have hbelowBody : CtxBelow Φ₁ { (S₁.onCtx ctx) with
          env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env } := by
        intro N hN
        rcases List.mem_cons.mp hN with rfl | hN
        · show Ty.BelowFvars Φ₁ (Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) τ₁)
          exact hbelowτ₁.closeOver
        · exact hbelow₁ N hN
      have hXM'' : ∀ x ∈ Xs,
          x ∉ (R₁.onPolyTy (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)).body.freeVars := by
        intro x hx hmem
        change x ∈ (R₁.onTy (Ty.closeOver (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) τ₁)).freeVars
          at hmem
        obtain ⟨v, hv, hxv⟩ := Ty.mem_freeVars_onTy_iff.mp hmem
        have hvτ₁ : v ∈ τ₁.freeVars := Ty.closeOver_freeVars_subset hv
        have hvg : v ∉ genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :=
          fun hg => Ty.not_mem_closeOver_freeVars hg hv
        by_cases hvrhs : v ∈ rhs.tyFreeVars
        · have hfix : R₁.onTy (Ty.fvar v) = Ty.fvar v := hR₁fix v (hKe v (.inl hvrhs))
          rw [hfix] at hxv
          simp only [Ty.freeVars, List.mem_singleton] at hxv
          exact hXrhs x hx (hxv ▸ hvrhs)
        · have hvenv : v ∈ (S₁.onCtx ctx).env.freeVars := by
            by_contra hc
            apply hvg
            simp only [genVars, List.mem_filter, Bool.and_eq_true]
            exact ⟨hvτ₁, by simpa using hc, by simpa using hvrhs⟩
          obtain ⟨pt, hpt, hvpt⟩ := Env.mem_freeVars_iff.mp hvenv
          have hx_onTy : x ∈ (R₁.onTy pt.body).freeVars :=
            Ty.mem_freeVars_onTy_iff.mpr ⟨v, hvpt, hxv⟩
          have hpt_mem : R₁.onPolyTy pt ∈ (S₀.onCtx ctx).env := by
            have hmem2 : R₁.onPolyTy pt ∈ (R₁.onCtx (S₁.onCtx ctx)).env := by
              simp only [Subst.onCtx, Subst.onEnv]
              exact List.mem_map.mpr ⟨pt, hpt, rfl⟩
            rw [hctxeq] at hmem2; exact hmem2
          exact hXenv x hx (Env.mem_freeVars_iff.mpr ⟨R₁.onPolyTy pt, hpt_mem, hx_onTy⟩)
      have hgen : (R₁.onPolyTy (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)).Generalizes M :=
        genScheme_generalizes hτ₁lc hR₁ hMwf hXnodup hXlen hXMbody htya hXM''
      have hbody' : TypeOfHM
          ⟨R₁.onPolyTy (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁) :: (S₀.onCtx ctx).env,
            (S₀.onCtx ctx).ctors⟩ body τ₀ :=
        TypeOfHM.weaken_scheme (env_post := []) (env := (S₀.onCtx ctx).env) (M := M) hgen hbody
      have hbody'' : TypeOfHM (R₁.onCtx { (S₁.onCtx ctx) with
          env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env }) body τ₀ := by
        show TypeOfHM ⟨R₁.onPolyTy (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)
            :: R₁.onEnv (S₁.onCtx ctx).env, (S₁.onCtx ctx).ctors⟩ body τ₀
        rw [show R₁.onEnv (S₁.onCtx ctx).env = (S₀.onCtx ctx).env from congrArg Ctx.env hctxeq]
        exact hbody'
      obtain ⟨R₂, hagb, htyb, hR₂, hR₂fix⟩ :=
        Infer.complete' Dbody hwfBody hbelowBody hR₁ K (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle)
          (fun y hy => hKe y (.inr hy)) hR₁fix hbody''
      refine ⟨R₂, ?_, htyb, hR₂, hR₂fix⟩
      exact Subst.AgreesBelow.trans_append hle haga hbelowS₁ hagb
  | letInAnn hσwf hΦN Drhs huni hesc1 hesc2 Dbody =>
    cases hty with
    | letIn hMwf hann hcofin heq hbody =>
      subst heq
      expose_names
      have hMσ := hann σ rfl
      rw [hMσ] at hcofin hbody
      simp only [Expr.openBoundTyVars] at hcofin
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at hKe
      have hKσ : ∀ y ∈ σ.body.freeVars, y ∈ K := fun y hy => hKe y (.inl (.inl hy))
      have hKrhs : ∀ y ∈ rhs.tyFreeVars, y ∈ K := fun y hy => hKe y (.inl (.inr hy))
      have hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKe y (.inr hy)
      have hle1 := Infer.frontier_le Drhs
      -- Re-skolemize: choose a fresh block `[M₀, M₀+pc)` above everything, so that
      -- conjugating `S₀` by the swap `[N,N+pc) ↔ [M₀,M₀+pc)` fixes the skolem block
      -- `Ys = freshVars N σ.paramCount` while reproducing `S₀` below `Φ` (mapped back
      -- via `blockListBack`).
      obtain ⟨M₀, hd, hM₀fresh⟩ := exists_fresh_block
        (S₀.map Prod.fst ++ S₀.flatMap (fun p => p.2.freeVars) ++ τ₀.freeVars) N σ.paramCount
      have hS₀key_lt : ∀ p ∈ S₀, p.1 < M₀ := fun p hp =>
        hM₀fresh p.1 (List.mem_append_left _ (List.mem_append_left _ (List.mem_map.mpr ⟨p, hp, rfl⟩)))
      have hS₀range_lt : ∀ p ∈ S₀, ∀ v ∈ p.2.freeVars, v < M₀ := fun p hp v hv =>
        hM₀fresh v (List.mem_append_left _ (List.mem_append_right _ (List.mem_flatMap.mpr ⟨p, hp, hv⟩)))
      have hτ₀_lt : ∀ v ∈ τ₀.freeVars, v < M₀ := fun v hv =>
        hM₀fresh v (List.mem_append_right _ hv)
      have hS₀belowM₀ : ∀ p ∈ S₀, Ty.BelowFvars M₀ p.2 :=
        fun p hp => Ty.BelowFvars.of_freeVars_lt (fun v hv => hS₀range_lt p hp v hv)
      have hτ₀_block : ∀ v ∈ τ₀.freeVars, ¬ (M₀ ≤ v ∧ v < M₀ + σ.paramCount) :=
        fun v hv => by have := hτ₀_lt v hv; omega
      have finj : Function.Injective (blockSwap N M₀ σ.paramCount) := blockSwap_injective hd
      have hffix : ∀ v, v < N → blockSwap N M₀ σ.paramCount v = v :=
        fun v hv => blockSwap_lt (by omega) hv
      -- The conjugated substitution agrees with `S₀` below `N` after renaming.
      have hconj_below : ∀ v, v < N → (Subst.conj (blockSwap N M₀ σ.paramCount) S₀).onTy (Ty.fvar v)
          = Ty.rename (blockSwap N M₀ σ.paramCount) (S₀.onTy (Ty.fvar v)) := by
        intro v hv
        have h := Subst.onTy_conj finj S₀ (Ty.fvar v)
        rw [Ty.rename_fvar, hffix v hv] at h
        exact h
      have hS₀'_K : ∀ k ∈ K, (Subst.conj (blockSwap N M₀ σ.paramCount) S₀).onTy (Ty.fvar k)
          = Ty.fvar k := by
        intro k hk
        have hklt : k < N := by have := hKΦ k hk; omega
        rw [hconj_below k hklt, hKfix k hk, Ty.rename_fvar, hffix k hklt]
      have hS₀'_Ys : ∀ Y ∈ freshVars N σ.paramCount,
          (Subst.conj (blockSwap N M₀ σ.paramCount) S₀).onTy (Ty.fvar Y) = Ty.fvar Y := by
        intro Y hY
        apply Ty.substFvars_eq_self_of_no_key
        intro p hp hc
        simp only [Ty.freeVars, List.mem_singleton] at hc
        simp only [Subst.conj, List.mem_map] at hp
        obtain ⟨q, hq, rfl⟩ := hp
        have hqlt : q.1 < M₀ := hS₀key_lt q hq
        have hYge : N ≤ Y := freshVars_ge Y hY
        have hYlt : Y < N + σ.paramCount := freshVars_lt Y hY
        simp only [blockSwap] at hc
        split_ifs at hc <;> omega
      -- Context-rename identity: the forward block-list realizes the conjugation.
      have hctxeq_gen : (blockList N M₀ σ.paramCount).onCtx (S₀.onCtx ctx)
          = (Subst.conj (blockSwap N M₀ σ.paramCount) S₀).onCtx ctx := by
        simp only [Subst.onCtx, Subst.onEnv, List.map_map]
        congr 1
        apply List.map_congr_left
        intro M hM
        simp only [Function.comp_apply, Subst.onPolyTy]
        congr 1
        rw [blockList_onTy hd (fun v hv => by
              have hlt := (Subst.onTy_belowFvars hS₀belowM₀ ((hbelow M hM).mono (by omega))).mem_lt v hv
              omega)]
        conv_rhs => rw [← Ty.rename_eq_self (f := blockSwap N M₀ σ.paramCount) (τ := M.body)
          (fun v hv => hffix v (by have := (hbelow M hM).mem_lt v hv; omega))]
        rw [Subst.onTy_conj finj]
      -- The block-list is the identity on the (block-avoiding) opened rhs / scheme.
      have hexpr_id : ∀ Xs : List Nat, (∀ x ∈ Xs, x ∉ freshVars N σ.paramCount) →
          (rhs.openTyVars Xs).substTyFvars (blockList N M₀ σ.paramCount) = rhs.openTyVars Xs := by
        intro Xs hXYs
        apply Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars
        intro p hp hc
        simp only [blockList, List.mem_map] at hp
        obtain ⟨i, hi, rfl⟩ := hp
        rcases Expr.tyFreeVars_openTyVars hc with h | h
        · have := hKΦ (N + i) (hKrhs (N + i) h); omega
        · exact hXYs (N + i) h (by simp only [freshVars, List.mem_map, List.mem_range]; exact ⟨i, List.mem_range.mp hi, rfl⟩)
      have htype_id : ∀ Xs : List Nat, (∀ x ∈ Xs, x ∉ freshVars N σ.paramCount) →
          (blockList N M₀ σ.paramCount).onTy (σ.openVars Xs) = σ.openVars Xs := by
        intro Xs hXYs
        apply Ty.substFvars_eq_self_of_no_key
        intro p hp hc
        simp only [blockList, List.mem_map] at hp
        obtain ⟨i, hi, rfl⟩ := hp
        rcases Ty.freeVars_openVars_subset (N + i) hc with h | h
        · have := hKΦ (N + i) (hKσ (N + i) h); omega
        · exact hXYs (N + i) h (by simp only [freshVars, List.mem_map, List.mem_range]; exact ⟨i, List.mem_range.mp hi, rfl⟩)
      -- Cofinite typing for the conjugated context, hence the typing at block `Ys`.
      have hcofin' : ∀ Xs : List Nat,
          FreshNames (L ++ freshVars N σ.paramCount) σ.paramCount Xs →
          TypeOfHM ((Subst.conj (blockSwap N M₀ σ.paramCount) S₀).onCtx ctx)
            (rhs.openTyVars Xs) (σ.openVars Xs) := by
        intro Xs hXs
        obtain ⟨hXlen, hXnodup, hXavoid⟩ := hXs
        have hXL : FreshNames L σ.paramCount Xs :=
          ⟨hXlen, hXnodup, fun x hx hc => hXavoid x hx (List.mem_append_left _ hc)⟩
        have hXYs : ∀ x ∈ Xs, x ∉ freshVars N σ.paramCount :=
          fun x hx hc => hXavoid x hx (List.mem_append_right _ hc)
        have ren := TypeOfHM.onSubst (blockList N M₀ σ.paramCount) (blockList_lc N M₀ σ.paramCount)
          (hcofin Xs hXL)
        rw [hctxeq_gen, hexpr_id Xs hXYs, htype_id Xs hXYs] at ren
        exact ren
      have hrhs_Ys' : TypeOfHM ((Subst.conj (blockSwap N M₀ σ.paramCount) S₀).onCtx ctx)
          (rhs.openTyVars (freshVars N σ.paramCount)) (σ.openVars (freshVars N σ.paramCount)) :=
        typeOfHM_at_block (freshVars_length N σ.paramCount) hcofin'
      -- Recurse on the *opened* rhs at rigid set `K ∪ Ys`.
      have hbelowN : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hbelow M hM).mono (by omega)
      obtain ⟨R₁, haga, htya, hR₁lc, hR₁K⟩ :=
        Infer.complete' Drhs hwf hbelowN (Subst.conj_lc hS₀) (K ++ freshVars N σ.paramCount)
          (fun k hk => by
            rcases List.mem_append.mp hk with h | h
            · have := hKΦ k h; omega
            · have := freshVars_lt k h; omega)
          (fun y hy => by
            rcases Expr.tyFreeVars_openTyVars hy with h | h
            · exact List.mem_append_left _ (hKrhs y h)
            · exact List.mem_append_right _ h)
          (fun k hk => by
            rcases List.mem_append.mp hk with h | h
            · exact hS₀'_K k h
            · exact hS₀'_Ys k h)
          hrhs_Ys'
      have hτ₁lc := (Infer.lc Drhs hwf).1
      have hS₁lc := (Infer.lc Drhs hwf).2
      have hbf := Infer.belowFvars Drhs hbelowN (fun y hy => by
        rcases Expr.tyFreeVars_openTyVars hy with h | h
        · have := hKΦ y (hKrhs y h); omega
        · have := freshVars_lt y h; omega)
      have hbelowτ₁ := hbf.1
      have hbelowS₁ := hbf.2
      have hR₁_K : ∀ k ∈ K, R₁.onTy (Ty.fvar k) = Ty.fvar k :=
        fun k hk => hR₁K k (List.mem_append_left _ hk)
      have hR₁_Ys : ∀ Y ∈ freshVars N σ.paramCount, R₁.onTy (Ty.fvar Y) = Ty.fvar Y :=
        fun Y hY => hR₁K Y (List.mem_append_right _ hY)
      -- `R₁` unifies `τ₁` with `σ.openVars Ys`; factor it through the given MGU `Schk`.
      have hBlc : (σ.openVars (freshVars N σ.paramCount)).IsLC := PolyTy.openVars_isLC hσwf (by simp)
      have hUuni : Unifies R₁ τ₁ (σ.openVars (freshVars N σ.paramCount)) := by
        show R₁.onTy τ₁ = R₁.onTy (σ.openVars (freshVars N σ.paramCount))
        rw [← htya]
        refine (Subst.onTy_eq_self_of_fixes (fun z hz => ?_)).symm
        rcases Ty.freeVars_openVars_subset z hz with h | h
        · exact hR₁_K z (hKσ z h)
        · exact hR₁_Ys z h
      obtain ⟨V, hVeq, hVlc, hVK⟩ := UnifyRel.greatest_K huni R₁ hR₁lc hUuni hR₁K
      have hSchk_lc : ∀ p ∈ Schk, p.2.IsLC := UnifyRel.lc huni hτ₁lc hBlc
      -- Map the conjugation back: `V' = V ++ blockListBack` recovers `S₀` below `Φ`.
      have hV'lc : ∀ p ∈ V ++ blockListBack N M₀ σ.paramCount, p.2.IsLC := by
        intro p hp
        rcases List.mem_append.mp hp with h | h
        · exact hVlc p h
        · exact blockListBack_lc N M₀ σ.paramCount p h
      have hV'_K : ∀ k ∈ K, (V ++ blockListBack N M₀ σ.paramCount).onTy (Ty.fvar k) = Ty.fvar k := by
        intro k hk
        have hklt : k < N := by have := hKΦ k hk; omega
        rw [Subst.onTy_append, hVK k (List.mem_append_left _ hk)]
        apply Ty.substFvars_eq_self_of_no_key
        intro p hp hc
        simp only [blockListBack, List.mem_map] at hp
        obtain ⟨i, _, rfl⟩ := hp
        simp only [Ty.freeVars, List.mem_singleton] at hc
        omega
      -- agreement `S₀ ≈ S₁ ∘ Schk ∘ V'` below `Φ`
      have hagSchkV' : Subst.AgreesBelow Φ S₀ (S₁ ++ Schk ++ (V ++ blockListBack N M₀ σ.paramCount)) := by
        intro v hv
        have hvN : v < N := by omega
        have hS₀v_block : ∀ w ∈ (S₀.onTy (Ty.fvar v)).freeVars,
            ¬ (M₀ ≤ w ∧ w < M₀ + σ.paramCount) := by
          intro w hw
          have := (Subst.onTy_belowFvars hS₀belowM₀ (Ty.BelowFvars.fvar (by omega))).mem_lt w hw
          omega
        calc S₀.onTy (Ty.fvar v)
            = (blockListBack N M₀ σ.paramCount).onTy
                (Ty.rename (blockSwap N M₀ σ.paramCount) (S₀.onTy (Ty.fvar v))) :=
              (blockListBack_onTy_rename hd hS₀v_block).symm
          _ = (blockListBack N M₀ σ.paramCount).onTy
                ((Subst.conj (blockSwap N M₀ σ.paramCount) S₀).onTy (Ty.fvar v)) := by
                rw [hconj_below v hvN]
          _ = (blockListBack N M₀ σ.paramCount).onTy ((S₁ ++ R₁).onTy (Ty.fvar v)) := by
                rw [haga v (by omega)]
          _ = (blockListBack N M₀ σ.paramCount).onTy (R₁.onTy (S₁.onTy (Ty.fvar v))) := by
                rw [Subst.onTy_append]
          _ = (blockListBack N M₀ σ.paramCount).onTy
                (V.onTy (Schk.onTy (S₁.onTy (Ty.fvar v)))) := by rw [hVeq _]
          _ = (S₁ ++ Schk ++ (V ++ blockListBack N M₀ σ.paramCount)).onTy (Ty.fvar v) := by
                simp only [Subst.onTy_append]
      -- body-context well-formedness / below-frontier
      have hwf₁ := Subst.onCtx_wf hS₁lc hwf
      have hwfSchk := Subst.onCtx_wf hSchk_lc hwf₁
      have hwfB : CtxWF { (Schk.onCtx (S₁.onCtx ctx)) with
          env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env } := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact hσwf
        · exact hwfSchk M hM
      have hσbelow : Ty.BelowFvars Φ₁ σ.body :=
        Ty.BelowFvars.of_freeVars_lt (fun v hv => by have := hKΦ v (hKσ v hv); omega)
      have hσopen' : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
        Ty.openVars_belowFvars hσbelow (fun x hx => by have := freshVars_lt x hx; omega)
      have hSchkbelow : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 :=
        UnifyRel.belowFvars huni hbelowτ₁ hσopen'
      have hbelowSchk : CtxBelow Φ₁ (Schk.onCtx (S₁.onCtx ctx)) :=
        Subst.onCtx_below hSchkbelow (le_refl _) (Subst.onCtx_below hbelowS₁ (by omega) hbelow)
      have hbelowB : CtxBelow Φ₁ { (Schk.onCtx (S₁.onCtx ctx)) with
          env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env } := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact hσbelow
        · exact hbelowSchk M hM
      have hmain : (V ++ blockListBack N M₀ σ.paramCount).onCtx (Schk.onCtx (S₁.onCtx ctx))
          = S₀.onCtx ctx := by
        have hc : (S₁ ++ Schk ++ (V ++ blockListBack N M₀ σ.paramCount)).onCtx ctx = S₀.onCtx ctx :=
          Subst.onCtx_congr (fun v hv => (hagSchkV' v hv).symm) hbelow
        rw [Subst.onCtx_append (S₁ ++ Schk) (V ++ blockListBack N M₀ σ.paramCount) ctx,
            Subst.onCtx_append S₁ Schk ctx] at hc
        exact hc
      have hV'σ_fix : (V ++ blockListBack N M₀ σ.paramCount).onPolyTy σ = σ := by
        obtain ⟨pcc, b⟩ := σ
        simp only [Subst.onPolyTy, PolyTy.mk.injEq, true_and]
        exact Subst.onTy_eq_self_of_fixes (fun v hv => hV'_K v (hKσ v hv))
      have hbodyV : TypeOfHM ((V ++ blockListBack N M₀ σ.paramCount).onCtx
          { (Schk.onCtx (S₁.onCtx ctx)) with env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env }) body τ₀ := by
        show TypeOfHM ⟨(V ++ blockListBack N M₀ σ.paramCount).onPolyTy σ ::
            (V ++ blockListBack N M₀ σ.paramCount).onEnv (Schk.onCtx (S₁.onCtx ctx)).env,
            (Schk.onCtx (S₁.onCtx ctx)).ctors⟩ body τ₀
        rw [hV'σ_fix, show (V ++ blockListBack N M₀ σ.paramCount).onEnv (Schk.onCtx (S₁.onCtx ctx)).env
              = (S₀.onCtx ctx).env from congrArg Ctx.env hmain]
        exact hbody
      obtain ⟨R₂, hagb, htyb, hR₂lc, hR₂fix⟩ :=
        Infer.complete' Dbody hwfB hbelowB hV'lc K (fun k hk => by have := hKΦ k hk; omega)
          hKbody hV'_K hbodyV
      refine ⟨R₂, ?_, htyb, hR₂lc, hR₂fix⟩
      refine Subst.AgreesBelow.trans_append (by omega) hagSchkV' ?_ hagb
      intro p hp
      rcases List.mem_append.mp hp with hp | hp
      · exact hbelowS₁ p hp
      · exact hSchkbelow p hp
  | match_ Dscrut hne Dbr =>
    expose_names
    simp only [Expr.tyFreeVars, List.mem_append] at hKe
    have hKscrut : ∀ y ∈ scrut.tyFreeVars, y ∈ K := fun y hy => hKe y (.inl hy)
    have hKbr : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars branches, y ∈ K :=
      fun y hy => hKe y (.inr hy)
    -- Extract the declarative scrutinee/branch typings (scrutinee type stays free).
    obtain ⟨scrutTyD, hscrutD, hbrsD⟩ :
        ∃ scrutTy, TypeOfHM (S₀.onCtx ctx) scrut scrutTy ∧
          ∀ br ∈ branches, TypeOfMatchBranch (S₀.onCtx ctx) br scrutTy τ₀ := by
      cases hty with
      | match_ h1 _ h3 => exact ⟨_, h1, h3⟩
    -- STEP 1: scrutinee IH (from the *given* `Dscrut`).
    obtain ⟨R₁, hags, htys, hR₁, hR₁fix⟩ :=
      Infer.complete' Dscrut hwf hbelow hS₀ K hKΦ hKscrut hKfix hscrutD
    have hS₁ := (Infer.lc Dscrut hwf).2
    have hτsLC := (Infer.lc Dscrut hwf).1
    have hbs := Infer.belowFvars Dscrut hbelow (fun y hy => hKΦ y (hKscrut y hy))
    have hle1 := Infer.frontier_le Dscrut
    have hwf₁ := Subst.onCtx_wf hS₁ hwf
    have hbelow₁ := Subst.onCtx_below hbs.2 hle1 hbelow
    have hctxeq : R₁.onCtx (S₁.onCtx ctx) = S₀.onCtx ctx := by
      rw [← Subst.onCtx_append]
      exact Subst.onCtx_congr (fun v hv => (hags v hv).symm) hbelow
    have hτ₀_lc : τ₀.IsLC := by
      obtain ⟨hd, tl, hcons⟩ := List.exists_cons_of_ne_nil hne
      exact TypeOfMatchBranch.regular (hbrsD hd (by rw [hcons]; exact List.mem_cons_self ..))
    -- STEP 2: fresh result-target variable `W` and residual `U` (= `Φ₁ ↦ τ₀`).
    obtain ⟨W, hWge, hWfresh⟩ := exists_fresh_block
      (R₁.map Prod.fst ++ R₁.flatMap (fun p => p.2.freeVars) ++ τ₀.freeVars) Φ₁ 1
    have hWdom : ∀ p ∈ R₁, p.1 ≠ W := by
      intro p hp he
      have := hWfresh p.1 (List.mem_append_left _ (List.mem_append_left _ (List.mem_map.mpr ⟨p, hp, rfl⟩)))
      omega
    have hWrange : ∀ p ∈ R₁, W ∉ p.2.freeVars := by
      intro p hp hc
      have := hWfresh W (List.mem_append_left _ (List.mem_append_right _ (List.mem_flatMap.mpr ⟨p, hp, hc⟩)))
      omega
    have hR₁Wfvar : R₁.onTy (Ty.fvar W) = Ty.fvar W := by
      apply Ty.substFvars_eq_self_of_no_key
      intro p hp hc
      simp only [Ty.freeVars, List.mem_singleton] at hc
      exact hWdom p hp hc
    obtain ⟨U, hUdef⟩ : ∃ U : Subst, U = [(Φ₁, Ty.fvar W)] ++ R₁ ++ [(W, τ₀)] := ⟨_, rfl⟩
    have hsingle : ∀ (Z : Nat) (V y : Ty), Subst.onTy [(Z, V)] y = Ty.substFvar Z V y :=
      fun _ _ _ => rfl
    have hUonTy : ∀ x, U.onTy x = Ty.substFvar W τ₀ (R₁.onTy (Ty.substFvar Φ₁ (Ty.fvar W) x)) := by
      intro x; rw [hUdef, Subst.onTy_append, Subst.onTy_append, hsingle, hsingle]
    have hUlc : ∀ p ∈ U, p.2.IsLC := by
      rw [hUdef]; intro p hp
      rcases List.mem_append.mp hp with hp' | hp'
      · rcases List.mem_append.mp hp' with hp'' | hp''
        · obtain rfl := List.mem_singleton.mp hp''; exact ContainsBvarsUpTo.fvar
        · exact hR₁ p hp''
      · obtain rfl := List.mem_singleton.mp hp'; exact hτ₀_lc
    have hUeqR₁ : ∀ v, v < Φ₁ → U.onTy (Ty.fvar v) = R₁.onTy (Ty.fvar v) := by
      intro v hv
      have hvΦ₁ : Φ₁ ∉ (Ty.fvar v).freeVars := by simp only [Ty.freeVars, List.mem_singleton]; omega
      have hvW : W ∉ (R₁.onTy (Ty.fvar v)).freeVars := by
        apply Subst.not_mem_onTy_freeVars hWrange
        simp only [Ty.freeVars, List.mem_singleton]; omega
      rw [hUonTy, Ty.substFvar_fresh hvΦ₁, Ty.substFvar_fresh hvW]
    have hUΦ₁ : U.onTy (Ty.fvar Φ₁) = τ₀ := by
      rw [hUonTy, show Ty.substFvar Φ₁ (Ty.fvar W) (Ty.fvar Φ₁) = Ty.fvar W from by simp [Ty.substFvar],
          hR₁Wfvar, show Ty.substFvar W τ₀ (Ty.fvar W) = τ₀ from by simp [Ty.substFvar]]
    have hUK : ∀ k ∈ K, U.onTy (Ty.fvar k) = Ty.fvar k := fun k hk =>
      (hUeqR₁ k (lt_of_lt_of_le (hKΦ k hk) hle1)).trans (hR₁fix k hk)
    -- STEP 3: recast branch declarative typings under `U`.
    have hbr' : ∀ br ∈ branches,
        TypeOfMatchBranch (U.onCtx (S₁.onCtx ctx)) br (U.onTy τs) (U.onTy (Ty.fvar Φ₁)) := by
      intro br hbr
      rw [Subst.onCtx_congr hUeqR₁ hbelow₁, hctxeq, Subst.onTy_congr hUeqR₁ hbs.1, htys.symm, hUΦ₁]
      exact hbrsD br hbr
    -- STEP 4: thread the branch list with the *given* `Dbr`.
    obtain ⟨R₂, hagbr, hR₂lc, hR₂fix⟩ :=
      InferBranches.complete' (R₀ := U) Dbr hwf₁
        (Subst.onCtx_below (fun p hp => (hbs.2 p hp).mono (by omega)) (by omega) hbelow)
        hτsLC (hbs.1.mono (by omega)) ContainsBvarsUpTo.fvar (.fvar (by omega))
        hUlc K (fun k hk => by have := hKΦ k hk; have := hle1; omega) hKbr hUK hbr'
    -- STEP 5: assemble.
    refine ⟨R₂, ?_, ?_, hR₂lc, hR₂fix⟩
    · intro v hv
      have ht1 : Ty.BelowFvars Φ₁ (S₁.onTy (Ty.fvar v)) :=
        Subst.onTy_belowFvars hbs.2 (Ty.BelowFvars.fvar (by omega))
      calc S₀.onTy (Ty.fvar v)
          = (S₁ ++ R₁).onTy (Ty.fvar v) := hags v hv
        _ = R₁.onTy (S₁.onTy (Ty.fvar v)) := by rw [Subst.onTy_append]
        _ = U.onTy (S₁.onTy (Ty.fvar v)) := (Subst.onTy_congr hUeqR₁ ht1).symm
        _ = (S₂ ++ R₂).onTy (S₁.onTy (Ty.fvar v)) := Subst.onTy_congr hagbr (ht1.mono (by omega))
        _ = ((S₁ ++ S₂) ++ R₂).onTy (Ty.fvar v) := by simp only [Subst.onTy_append]
    · calc τ₀ = U.onTy (Ty.fvar Φ₁) := hUΦ₁.symm
        _ = (S₂ ++ R₂).onTy (Ty.fvar Φ₁) := Subst.onTy_congr hagbr (Ty.BelowFvars.fvar (by omega))
        _ = R₂.onTy (S₂.onTy (Ty.fvar Φ₁)) := by rw [Subst.onTy_append]
  | letRec hgroup hbody =>
    expose_names
    cases hty with
    | letRec hlen hlen2 hlc hG hgen hcofin heqc hbodyD =>
      subst heqc
      expose_names
      simp only [Expr.tyFreeVars, List.mem_append] at hKe
      have hKgrp : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K :=
        fun y hy => hKe y (.inl hy)
      have hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKe y (.inr hy)
      have hKrigid : ∀ y ∈ bindings.flatMap Expr.tyFreeVars, y ∈ K :=
        fun y hy => hKgrp y (Expr.mem_recGroup_tyFreeVars.mpr hy)
      have hctxβWF : CtxWF (⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
          ++ ctx.env, ctx.ctors⟩ : Ctx) := by
        intro M hM
        rcases List.mem_append.mp hM with hM | hM
        · obtain ⟨x, _, rfl⟩ := List.mem_map.mp hM; exact ContainsBvarsUpTo.fvar
        · exact hwf M hM
      have hctxβbelow : CtxBelow (Φ + bindings.length)
          (⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
            ++ ctx.env, ctx.ctors⟩ : Ctx) := by
        intro M hM
        rcases List.mem_append.mp hM with hM | hM
        · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
          exact Ty.BelowFvars.fvar (by have := freshVars_lt x hx; omega)
        · exact (hbelow M hM).mono (by omega)
      have hgle : Φ + bindings.length ≤ Φ₁ := InferRecGroup.frontier_le hgroup
      have hβlc : ∀ τ ∈ (freshVars Φ bindings.length).map Ty.fvar, τ.IsLC := by
        intro τ hτ; obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ; exact ContainsBvarsUpTo.fvar
      have hβbelow : ∀ τ ∈ (freshVars Φ bindings.length).map Ty.fvar,
          Ty.BelowFvars (Φ + bindings.length) τ := by
        intro τ hτ; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
        exact Ty.BelowFvars.fvar (by have := freshVars_lt x hx; omega)
      have hS₁lc : ∀ p ∈ S₁, p.2.IsLC := InferRecGroup.lc hgroup hctxβWF hβlc
      have hS₁below : ∀ p ∈ S₁, Ty.BelowFvars Φ₁ p.2 :=
        InferRecGroup.belowFvars hgroup hctxβbelow hβbelow
          (fun y hy => by have := hKΦ y (hKgrp y hy); omega)
      have hwf₁ : CtxWF (S₁.onCtx ctx) := Subst.onCtx_wf hS₁lc hwf
      have hbelow₁ : CtxBelow Φ₁ (S₁.onCtx ctx) :=
        Subst.onCtx_below hS₁below (le_trans (Nat.le_add_right Φ _) hgle) hbelow
      set τsinf := (freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)) with hτsinf_def
      have hτsinf_lc : ∀ τ ∈ τsinf, τ.IsLC := by
        intro τ hτ; rw [hτsinf_def] at hτ
        obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ
        exact Subst.onTy_lc hS₁lc ContainsBvarsUpTo.fvar
      have hτsinf_below : ∀ τ ∈ τsinf, Ty.BelowFvars Φ₁ τ := by
        intro τ hτ; rw [hτsinf_def] at hτ
        obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
        exact Subst.onTy_belowFvars hS₁below (.fvar (by have := freshVars_lt x hx; omega))
      set Ginf := genGroupVars (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env τsinf with hGinf_def
      obtain ⟨Xs, hXlen, hXnodup, hXavoid⟩ := exists_fresh_names
        (L ++ G ++ Ty.freeVarsList τs ++ (S₀.onCtx ctx).env.freeVars
          ++ bindings.flatMap Expr.tyFreeVars) G.length
      have hXL : ∀ x ∈ Xs, x ∉ L := fun x hx hc =>
        hXavoid x hx (by simp only [List.mem_append]; tauto)
      have hXG : ∀ x ∈ Xs, x ∉ G := fun x hx hc =>
        hXavoid x hx (by simp only [List.mem_append]; tauto)
      have hXτs : ∀ x ∈ Xs, x ∉ Ty.freeVarsList τs := fun x hx hc =>
        hXavoid x hx (by simp only [List.mem_append]; tauto)
      have hXenv : ∀ x ∈ Xs, x ∉ (S₀.onCtx ctx).env.freeVars := fun x hx hc =>
        hXavoid x hx (by simp only [List.mem_append]; tauto)
      have hXrigid : ∀ x ∈ Xs, x ∉ bindings.flatMap Expr.tyFreeVars := fun x hx hc =>
        hXavoid x hx (by simp only [List.mem_append]; tauto)
      have hXfresh : FreshNames L G.length Xs := ⟨hXlen, hXnodup, hXL⟩
      obtain ⟨R₀, hR₀lc, hR₀K, hR₀agΦ, hR₀block⟩ :=
        exists_recgroup_residual (Φ := Φ) (n := bindings.length) (S₀ := S₀)
          (vs := τs.map (Ty.renameG G Xs)) (K := K)
          (by rw [List.length_map]; omega) hS₀
          (fun t ht => by
            obtain ⟨τ', hτ', rfl⟩ := List.mem_map.mp ht
            rw [← Subst.onTy_zip_fvar_eq_renameG]
            exact Subst.onTy_lc (fun p hp => by
              obtain ⟨w, _, hw⟩ := List.mem_map.mp (List.of_mem_zip hp).2
              rw [← hw]; exact ContainsBvarsUpTo.fvar) (hlc τ' hτ'))
          hKΦ hKfix
      have hR₀ctx : R₀.onCtx ctx = S₀.onCtx ctx := Subst.onCtx_congr hR₀agΦ hbelow
      have hR₀β : (freshVars Φ bindings.length).map (fun i => R₀.onTy (Ty.fvar i))
          = τs.map (Ty.renameG G Xs) := by
        rw [← hR₀block]; simp only [List.map_map, Function.comp_def]
      have hgrp : ∀ p ∈ bindings.zip (((freshVars Φ bindings.length).map Ty.fvar).map R₀.onTy),
          TypeOfHM (R₀.onCtx (⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
            ++ ctx.env, ctx.ctors⟩ : Ctx)) p.1 p.2 := by
        have hbmap : ((freshVars Φ bindings.length).map Ty.fvar).map R₀.onTy
            = τs.map (Ty.renameG G Xs) := by rw [List.map_map]; exact hR₀β
        have hctxβeq : R₀.onCtx (⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
              ++ ctx.env, ctx.ctors⟩ : Ctx)
            = ⟨(τs.map (Ty.renameG G Xs)).map PolyTy.mkTrivial ++ (S₀.onCtx ctx).env, ctx.ctors⟩ := by
          show (⟨((freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
              ++ ctx.env).map R₀.onPolyTy, ctx.ctors⟩ : Ctx) = _
          rw [List.map_append]
          congr 1
          congr 1
          · rw [← hR₀β, List.map_map, List.map_map]
            apply List.map_congr_left
            intro i _
            simp only [Function.comp_apply, Subst.onPolyTy, PolyTy.mkTrivial]
          · exact congrArg Ctx.env hR₀ctx
        rw [hbmap, hctxβeq]
        exact fun p hp => hcofin Xs hXfresh p hp
      obtain ⟨R₁, hag_group, hR₁lc, hR₁fix⟩ :=
        InferRecGroup.complete' hgroup hctxβWF hctxβbelow hβlc hβbelow hR₀lc K
          (fun k hk => by have := hKΦ k hk; omega) hKgrp hR₀K hgrp
      have hctxeq : R₁.onCtx (S₁.onCtx ctx) = S₀.onCtx ctx := by
        rw [← Subst.onCtx_append,
          Subst.onCtx_congr (fun v hv => (hag_group v (by omega)).symm) hbelow]
        exact hR₀ctx
      have hconn : ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i))).map R₁.onTy
          = τs.map (Ty.renameG G Xs) := by
        rw [← hR₀β, List.map_map]
        apply List.map_congr_left
        intro i hi
        simp only [Function.comp_apply]
        rw [← Subst.onTy_append]
        exact (hag_group i (freshVars_lt i hi)).symm
      have hbody'' := letRec_body_retype hlen hlen2 hlc hG hgen hXlen hXnodup
        (fun g hg hc => hXG g hc hg) hXτs hXenv hXrigid hS₁lc hR₁lc hctxeq hKrigid hR₁fix hconn hbodyD
      have hwfBodyInf : CtxWF (⟨genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env τsinf
            ++ (S₁.onCtx ctx).env, (S₁.onCtx ctx).ctors⟩ : Ctx) := by
        intro M hM
        rcases List.mem_append.mp hM with hM | hM
        · simp only [genGroupSchemes] at hM
          obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
          exact PolyTy.genGroup_wf (hτsinf_lc τ hτ)
        · exact hwf₁ M hM
      have hbelowBodyInf : CtxBelow Φ₁
          (⟨genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env τsinf
            ++ (S₁.onCtx ctx).env, (S₁.onCtx ctx).ctors⟩ : Ctx) := by
        intro M hM
        rcases List.mem_append.mp hM with hM | hM
        · simp only [genGroupSchemes] at hM
          obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
          exact (hτsinf_below τ hτ).closeOver
        · exact hbelow₁ M hM
      obtain ⟨R₂, hagb, htyb, hR₂lc, hR₂fix⟩ :=
        Infer.complete' hbody hwfBodyInf hbelowBodyInf hR₁lc K
          (fun k hk => lt_of_lt_of_le (hKΦ k hk) (le_trans (Nat.le_add_right Φ _) hgle))
          hKbody hR₁fix hbody''
      refine ⟨R₂, ?_, htyb, hR₂lc, hR₂fix⟩
      have hag1 : Subst.AgreesBelow Φ S₀ (S₁ ++ R₁) := fun v hv =>
        (hR₀agΦ v hv).symm.trans (hag_group v (by omega))
      exact Subst.AgreesBelow.trans_append (le_trans (Nat.le_add_right Φ _) hgle) hag1 hS₁below hagb
  | @letRecAnn _ _ schemes bindings body Φ₁ Φ₂ S₁ S₂ bindingsOut bodyOut τ₂ hwf' hlen hgroup hbody =>
    cases hty with
    | letRecAnn hlen' hwf'' hcofin heqc hbodyD =>
      subst heqc
      simp only [Expr.tyFreeVars, List.mem_append] at hKe
      have hKsch : ∀ σ ∈ schemes, ∀ y ∈ σ.body.freeVars, y ∈ K := fun σ hσ y hy =>
        hKe y (.inl (.inl (Expr.mem_tyFreeVars_schemeList hσ hy)))
      have hKgrp : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K := fun y hy =>
        hKe y (.inl (.inr hy))
      have hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKe y (.inr hy)
      have hschB : ∀ σ ∈ schemes, Ty.BelowFvars Φ σ.body := fun σ hσ =>
        Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hKsch σ hσ v hv))
      have hctxSWF : CtxWF { ctx with env := schemes ++ ctx.env } := by
        intro M hM; rcases List.mem_append.mp hM with hM | hM
        · exact hwf' M hM
        · exact hwf M hM
      have hctxSbelow : CtxBelow Φ { ctx with env := schemes ++ ctx.env } := by
        intro M hM; rcases List.mem_append.mp hM with hM | hM
        · exact hschB M hM
        · exact hbelow M hM
      have hSschmap : schemes.map (Subst.onPolyTy S₀) = schemes := by
        conv_rhs => rw [← List.map_id schemes]
        refine List.map_congr_left (fun σ hσ => ?_)
        obtain ⟨pcc, b⟩ := σ
        simp only [Subst.onPolyTy, PolyTy.mk.injEq, true_and, id_eq]
        exact Subst.onTy_eq_self_of_fixes (fun v hv => hKfix v (hKsch ⟨pcc, b⟩ hσ v hv))
      have hctxS_eq : S₀.onCtx { ctx with env := schemes ++ ctx.env }
          = { (S₀.onCtx ctx) with env := schemes ++ (S₀.onCtx ctx).env } := by
        simp only [Subst.onCtx, Subst.onEnv, List.map_append]
        rw [hSschmap]
      obtain ⟨R₁, hag_group, hR₁lc, hR₁fix⟩ :=
        InferRecGroupAnn.complete' hgroup hctxSWF hctxSbelow hwf' hS₀ K hKΦ hKgrp hKsch hKfix
          (fun p hp Xs hX => by rw [hctxS_eq]; exact hcofin p hp Xs hX)
      have hgrle : Φ ≤ Φ₁ := InferRecGroupAnn.frontier_le hgroup
      have hKgrpΦ : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ :=
        fun y hy => hKΦ y (hKgrp y hy)
      have hS₁lc : ∀ p ∈ S₁, p.2.IsLC := InferRecGroupAnn.lc hgroup hctxSWF hwf'
      have hS₁below : ∀ p ∈ S₁, Ty.BelowFvars Φ₁ p.2 :=
        InferRecGroupAnn.belowFvars hgroup hctxSbelow hschB hKgrpΦ
      have hbodyWF : CtxWF (S₁.onCtx { ctx with env := schemes ++ ctx.env }) :=
        Subst.onCtx_wf hS₁lc hctxSWF
      have hbodybelow : CtxBelow Φ₁ (S₁.onCtx { ctx with env := schemes ++ ctx.env }) :=
        Subst.onCtx_below hS₁below hgrle hctxSbelow
      have hbodyctx_eq : R₁.onCtx (S₁.onCtx { ctx with env := schemes ++ ctx.env })
          = S₀.onCtx { ctx with env := schemes ++ ctx.env } := by
        rw [← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => (hag_group v hv).symm) hctxSbelow
      have hbody'' : TypeOfHM (R₁.onCtx (S₁.onCtx { ctx with env := schemes ++ ctx.env })) body τ₀ := by
        rw [hbodyctx_eq, hctxS_eq]; exact hbodyD
      obtain ⟨R₂, hagb, htyb, hR₂lc, hR₂fix⟩ :=
        Infer.complete' hbody hbodyWF hbodybelow hR₁lc K
          (fun k hk => lt_of_lt_of_le (hKΦ k hk) hgrle) hKbody hR₁fix hbody''
      refine ⟨R₂, ?_, htyb, hR₂lc, hR₂fix⟩
      exact Subst.AgreesBelow.trans_append hgrle hag_group hS₁below hagb
termination_by e.size
decreasing_by all_goals (try subst_vars; try simp only [Expr.size, Expr.size_openTyVars]; omega)

theorem InferBranches.complete' {Φ ctx scrutTy ρ branches Φ' S brsOut}
    (h : InferBranches Φ ctx scrutTy ρ branches Φ' S brsOut)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx)
    (hscrutLC : scrutTy.IsLC) (hbscrut : Ty.BelowFvars Φ scrutTy)
    (hρ : ρ.IsLC) (hbρ : Ty.BelowFvars Φ ρ)
    {R₀ : Subst} (hR₀ : ∀ p ∈ R₀, p.2.IsLC) (K : List Nat)
    (hKΦ : ∀ k ∈ K, k < Φ)
    (hKbr : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars branches, y ∈ K)
    (hKfix : ∀ k ∈ K, R₀.onTy (.fvar k) = .fvar k)
    (hbr : ∀ br ∈ branches, TypeOfMatchBranch (R₀.onCtx ctx) br (R₀.onTy scrutTy) (R₀.onTy ρ)) :
    ∃ R', Subst.AgreesBelow Φ R₀ (S ++ R') ∧ (∀ p ∈ R', p.2.IsLC) ∧
      (∀ k ∈ K, R'.onTy (.fvar k) = .fvar k) := by
  cases h with
  | nil => exact ⟨R₀, fun v _ => by rw [List.nil_append], hR₀, hKfix⟩
  | cons hlook hn huni0 Dbody Duni Drest =>
      expose_names
      have hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKbr y (by
        simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy)
      have hKrest : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars rest, y ∈ K := fun y hy => hKbr y (by
        simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hy)
      cases hbr (.named c n, body) (List.mem_cons_self ..) with
      | @mk _ _ _ _ _ _ ctorD _ _ _ tyArgs hlookD hscrutEqD hpcD _ hforallD hpbD hbctxD hbodytyD =>
        have hco : ctor = ctorD := Option.some.inj (hlook.symm.trans hlookD)
        subst hco
        have hinsts := instContents_eq_openWith hforallD (fun c hc => hpcD ▸ ctor.bound c hc)
        rw [hbctxD, hpbD, hinsts] at hbodytyD
        have htyArgs_lc : ∀ t ∈ tyArgs, t.IsLC := by
          have hreg : (Ty.customTy ctor.tyName tyArgs).IsLC := hscrutEqD ▸ Subst.onTy_lc hR₀ hscrutLC
          cases hreg with | customTy h => exact h
        -- STEP 0: factor `R₀` through the *given* branch MGU `S₀` (= `huni0`).
        obtain ⟨R_s, hR_s_lc, hR_s_K, hR_s_ag, hfresh_R_s⟩ :=
          customTy_factor_dodge huni0 hbscrut hR₀ hKΦ hKfix htyArgs_lc hpcD hscrutEqD
        have key_t : ∀ {t : Ty}, Ty.BelowFvars Φ t → R_s.onTy (S₀.onTy t) = R₀.onTy t := by
          intro t ht
          rw [← Subst.onTy_append]
          exact Subst.onTy_congr (fun v hv => by rw [Subst.onTy_append]; exact (hR_s_ag v hv).symm) ht
        have hctx0 : R_s.onCtx (S₀.onCtx ctx) = R₀.onCtx ctx := by
          rw [← Subst.onCtx_append]
          exact Subst.onCtx_congr (fun v hv => by rw [Subst.onTy_append]; exact (hR_s_ag v hv).symm) hbelow
        have hS₀ := UnifyRel.lc huni0 hscrutLC
          (.customTy (fun t ht => by obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht; exact ContainsBvarsUpTo.fvar))
        have hS₀below : ∀ p ∈ S₀, Ty.BelowFvars (Φ + ctor.paramCount) p.2 := by
          apply UnifyRel.belowFvars huni0 (hbscrut.mono (by omega))
          apply Ty.BelowFvars.customTy
          intro t ht
          obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
          exact Ty.BelowFvars.fvar (by have := freshVars_lt x hx; omega)
        have hbodyWF : CtxWF { S₀.onCtx ctx with
            env := (ctor.contents.map (Ty.openWith
                (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
              ++ (S₀.onCtx ctx).env } :=
          branchBindings_wf (ctorr := ctor)
            (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
            (Subst.onCtx_wf hS₀ hwf)
            (fun t ht => by
              obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
              obtain ⟨x, _, rfl⟩ := List.mem_map.mp hv
              exact Subst.onTy_lc hS₀ ContainsBvarsUpTo.fvar)
            (by simp)
        have hbodyBelow : CtxBelow (Φ + ctor.paramCount) { S₀.onCtx ctx with
            env := (ctor.contents.map (Ty.openWith
                (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
              ++ (S₀.onCtx ctx).env } :=
          branchBindings_below (ctorr := ctor)
            (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
            (Subst.onCtx_below hS₀below (by omega) hbelow)
            (fun t ht => by
              obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
              obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv
              exact Subst.onTy_belowFvars hS₀below (.fvar (by have := freshVars_lt x hx; omega)))
        have hbodyty2 : TypeOfHM (R_s.onCtx { S₀.onCtx ctx with
            env := (ctor.contents.map (Ty.openWith
                (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
              ++ (S₀.onCtx ctx).env }) body (R_s.onTy (S₀.onTy ρ)) := by
          rw [Subst.onCtx_branchBindings hR_s_lc, hctx0, hfresh_R_s, key_t hbρ]
          exact hbodytyD
        -- STEP: head body principality from the *given* `Dbody`.
        obtain ⟨R_b, hagbody, htybody, hR_b, hR_bfix⟩ :=
          Infer.complete' Dbody hbodyWF hbodyBelow hR_s_lc K
            (fun k hk => by have := hKΦ k hk; omega) hKbody hR_s_K hbodyty2
        have hbodyLC := Infer.lc Dbody hbodyWF
        have hτb_lc := hbodyLC.1
        have hS_b := hbodyLC.2
        have hbb := Infer.belowFvars Dbody hbodyBelow (fun y hy => by have := hKΦ y (hKbody y hy); omega)
        have hle_b : Φ + ctor.paramCount ≤ Φ₁ := Infer.frontier_le Dbody
        have hbS₀ρ : Ty.BelowFvars (Φ + ctor.paramCount) (S₀.onTy ρ) :=
          Subst.onTy_belowFvars hS₀below (hbρ.mono (by omega))
        -- STEP: result MGU is the *given* `Duni`; factor `R_b` through it.
        have hUni : Unifies R_b τb (S₁.onTy (S₀.onTy ρ)) := by
          show R_b.onTy τb = R_b.onTy (S₁.onTy (S₀.onTy ρ))
          rw [← htybody]
          exact (Subst.onTy_congr hagbody hbS₀ρ).trans (Subst.onTy_append S₁ R_b (S₀.onTy ρ))
        have h_u := Duni
        obtain ⟨R_u, hR_u_eq, hR_u, hR_ufix⟩ := UnifyRel.greatest_K h_u R_b hR_b hUni hR_bfix
        have hS_u := UnifyRel.lc h_u hτb_lc (Subst.onTy_lc hS_b (Subst.onTy_lc hS₀ hρ))
        have hS_u_below := UnifyRel.belowFvars h_u hbb.1 (Subst.onTy_belowFvars hbb.2 (hbS₀ρ.mono hle_b))
        -- STEP: recursion hypotheses on `rest`.
        have hwf' : CtxWF (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx))) :=
          Subst.onCtx_wf hS_u (Subst.onCtx_wf hS_b (Subst.onCtx_wf hS₀ hwf))
        have hbelow' : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx))) :=
          Subst.onCtx_below hS_u_below (le_refl _)
            (Subst.onCtx_below hbb.2 (le_refl _)
              (Subst.onCtx_below (fun p hp => (hS₀below p hp).mono hle_b) (by omega) hbelow))
        have hbS₀scrut : Ty.BelowFvars (Φ + ctor.paramCount) (S₀.onTy scrutTy) :=
          Subst.onTy_belowFvars hS₀below (hbscrut.mono (by omega))
        have hscrutLC' : (S₂.onTy (S₁.onTy (S₀.onTy scrutTy))).IsLC :=
          Subst.onTy_lc hS_u (Subst.onTy_lc hS_b (Subst.onTy_lc hS₀ hscrutLC))
        have hbscrut' : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (S₀.onTy scrutTy))) :=
          Subst.onTy_belowFvars hS_u_below (Subst.onTy_belowFvars hbb.2 (hbS₀scrut.mono hle_b))
        have hρ' : (S₂.onTy (S₁.onTy (S₀.onTy ρ))).IsLC :=
          Subst.onTy_lc hS_u (Subst.onTy_lc hS_b (Subst.onTy_lc hS₀ hρ))
        have hbρ' : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (S₀.onTy ρ))) :=
          Subst.onTy_belowFvars hS_u_below (Subst.onTy_belowFvars hbb.2 (hbS₀ρ.mono hle_b))
        have key_full : ∀ {t : Ty}, Ty.BelowFvars Φ t →
            R_u.onTy (S₂.onTy (S₁.onTy (S₀.onTy t))) = R₀.onTy t := by
          intro t ht
          have hbS₀t : Ty.BelowFvars (Φ + ctor.paramCount) (S₀.onTy t) :=
            Subst.onTy_belowFvars hS₀below (ht.mono (by omega))
          rw [← hR_u_eq, ← Subst.onTy_append, Subst.onTy_congr hagbody hbS₀t |>.symm]
          exact key_t ht
        have hctx_full : R_u.onCtx (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx))) = R₀.onCtx ctx := by
          rw [← Subst.onCtx_append, ← Subst.onCtx_append, ← Subst.onCtx_append]
          exact Subst.onCtx_congr (fun v hv => by
            simp only [Subst.onTy_append]; exact key_full (Ty.BelowFvars.fvar hv)) hbelow
        have hdecl' : ∀ br ∈ rest, TypeOfMatchBranch (R_u.onCtx (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx))))
            br (R_u.onTy (S₂.onTy (S₁.onTy (S₀.onTy scrutTy))))
            (R_u.onTy (S₂.onTy (S₁.onTy (S₀.onTy ρ)))) := by
          intro br hbr2
          rw [hctx_full, key_full hbscrut, key_full hbρ]
          exact hbr br (List.mem_cons_of_mem _ hbr2)
        obtain ⟨R_r, hagrest, hR_r, hR_rfix⟩ :=
          InferBranches.complete' Drest hwf' hbelow' hscrutLC' hbscrut' hρ' hbρ' hR_u K
            (fun k hk => lt_of_lt_of_le (hKΦ k hk) (le_trans (Nat.le_add_right _ _) hle_b))
            hKrest hR_ufix hdecl'
        refine ⟨R_r, ?_, hR_r, hR_rfix⟩
        intro v hv
        have hb0 : Ty.BelowFvars (Φ + ctor.paramCount) (S₀.onTy (.fvar v)) :=
          Subst.onTy_belowFvars hS₀below (.fvar (by omega))
        have hb1 : Ty.BelowFvars Φ₁ (S₁.onTy (S₀.onTy (.fvar v))) :=
          Subst.onTy_belowFvars hbb.2 (hb0.mono hle_b)
        have hb2 : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (S₀.onTy (.fvar v)))) :=
          Subst.onTy_belowFvars hS_u_below hb1
        calc R₀.onTy (.fvar v)
            = R_s.onTy (S₀.onTy (.fvar v)) := hR_s_ag v hv
          _ = (S₁ ++ R_b).onTy (S₀.onTy (.fvar v)) := Subst.onTy_congr hagbody hb0
          _ = R_b.onTy (S₁.onTy (S₀.onTy (.fvar v))) := by rw [Subst.onTy_append]
          _ = R_u.onTy (S₂.onTy (S₁.onTy (S₀.onTy (.fvar v)))) := hR_u_eq _
          _ = (S₃ ++ R_r).onTy (S₂.onTy (S₁.onTy (S₀.onTy (.fvar v)))) := Subst.onTy_congr hagrest hb2
          _ = ((S₀ ++ S₁ ++ S₂ ++ S₃) ++ R_r).onTy (.fvar v) := by simp only [Subst.onTy_append]
  | consWild Dbody Duni Drest =>
      expose_names
      have hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKbr y (by
        simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy)
      have hKrest : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars rest, y ∈ K := fun y hy => hKbr y (by
        simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hy)
      have hbodyty2 : TypeOfHM (R₀.onCtx ctx) body (R₀.onTy ρ) := by
        cases hbr (.wildcard, body) (List.mem_cons_self ..) with
        | wildcard hbodytyD => exact hbodytyD
      obtain ⟨R_b, hagbody, htybody, hR_b, hR_bfix⟩ :=
        Infer.complete' Dbody hwf hbelow hR₀ K hKΦ hKbody hKfix hbodyty2
      have hbodyLC := Infer.lc Dbody hwf
      have hτb_lc := hbodyLC.1
      have hS_b := hbodyLC.2
      have hbb := Infer.belowFvars Dbody hbelow (fun y hy => hKΦ y (hKbody y hy))
      have hle_b := Infer.frontier_le Dbody
      have hUni : Unifies R_b τb (S₁.onTy ρ) := by
        show R_b.onTy τb = R_b.onTy (S₁.onTy ρ)
        rw [← htybody, ← Subst.onTy_append]
        exact Subst.onTy_congr hagbody hbρ
      have h_u := Duni
      obtain ⟨R_u, hR_u_eq, hR_u, hR_ufix⟩ := UnifyRel.greatest_K h_u R_b hR_b hUni hR_bfix
      have hS_u := UnifyRel.lc h_u hτb_lc (Subst.onTy_lc hS_b hρ)
      have hS_u_below := UnifyRel.belowFvars h_u hbb.1 (Subst.onTy_belowFvars hbb.2 (hbρ.mono hle_b))
      have hwf' : CtxWF (S₂.onCtx (S₁.onCtx ctx)) :=
        Subst.onCtx_wf hS_u (Subst.onCtx_wf hS_b hwf)
      have hbelow_Sb : CtxBelow Φ₁ (S₁.onCtx ctx) := Subst.onCtx_below hbb.2 hle_b hbelow
      have hbelow' : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx ctx)) :=
        Subst.onCtx_below hS_u_below (le_refl _) hbelow_Sb
      have key_t : ∀ {t : Ty}, Ty.BelowFvars Φ t →
          R_u.onTy (S₂.onTy (S₁.onTy t)) = R₀.onTy t := by
        intro t ht
        rw [← hR_u_eq (S₁.onTy t), ← Subst.onTy_append]
        exact (Subst.onTy_congr hagbody ht).symm
      have hscrutLC' : (S₂.onTy (S₁.onTy scrutTy)).IsLC :=
        Subst.onTy_lc hS_u (Subst.onTy_lc hS_b hscrutLC)
      have hbscrut' : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy scrutTy)) :=
        Subst.onTy_belowFvars hS_u_below (Subst.onTy_belowFvars hbb.2 (hbscrut.mono hle_b))
      have hρ' : (S₂.onTy (S₁.onTy ρ)).IsLC := Subst.onTy_lc hS_u (Subst.onTy_lc hS_b hρ)
      have hbρ' : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy ρ)) :=
        Subst.onTy_belowFvars hS_u_below (Subst.onTy_belowFvars hbb.2 (hbρ.mono hle_b))
      have hctx_eq : R_u.onCtx (S₂.onCtx (S₁.onCtx ctx)) = R₀.onCtx ctx := by
        rw [← Subst.onCtx_comp_of_onTy_eq hR_u_eq, ← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => (hagbody v hv).symm) hbelow
      have hdecl' : ∀ br ∈ rest, TypeOfMatchBranch (R_u.onCtx (S₂.onCtx (S₁.onCtx ctx)))
          br (R_u.onTy (S₂.onTy (S₁.onTy scrutTy))) (R_u.onTy (S₂.onTy (S₁.onTy ρ))) := by
        intro br hbr2
        rw [hctx_eq, key_t hbscrut, key_t hbρ]
        exact hbr br (List.mem_cons_of_mem _ hbr2)
      obtain ⟨R_r, hagrest, hR_r, hR_rfix⟩ :=
        InferBranches.complete' Drest hwf' hbelow' hscrutLC' hbscrut' hρ' hbρ' hR_u K
          (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle_b) hKrest hR_ufix hdecl'
      refine ⟨R_r, ?_, hR_r, hR_rfix⟩
      intro v hv
      have hbv1 : Ty.BelowFvars Φ₁ (S₁.onTy (.fvar v)) :=
        Subst.onTy_belowFvars hbb.2 (.fvar (by omega))
      have hbv2 : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (.fvar v))) :=
        Subst.onTy_belowFvars hS_u_below hbv1
      calc R₀.onTy (.fvar v)
          = (S₁ ++ R_b).onTy (.fvar v) := hagbody v hv
        _ = R_b.onTy (S₁.onTy (.fvar v)) := by rw [Subst.onTy_append]
        _ = R_u.onTy (S₂.onTy (S₁.onTy (.fvar v))) := hR_u_eq (S₁.onTy (.fvar v))
        _ = (S₃ ++ R_r).onTy (S₂.onTy (S₁.onTy (.fvar v))) := Subst.onTy_congr hagrest hbv2
        _ = ((S₁ ++ S₂ ++ S₃) ++ R_r).onTy (.fvar v) := by simp only [Subst.onTy_append]
termination_by Expr.sizeBranches branches
decreasing_by all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)

/-- **Recursive-group completeness** companion of `Infer.complete'`: given the
    *given* `InferRecGroup` derivation and declarative typings of every binding at
    its (substituted) target, the typings factor through the threader's output via
    an LC residual `R'` (`R₀ = R' ∘ S` below `Φ`). The `letRec` analogue of
    `InferBranches.complete'`; each binding threads like the `consWild` case but
    against its own per-binding target `β`. -/
theorem InferRecGroup.complete' {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx)
    (htargets : ∀ τ ∈ targets, τ.IsLC) (hbtargets : ∀ τ ∈ targets, Ty.BelowFvars Φ τ)
    {R₀ : Subst} (hR₀ : ∀ p ∈ R₀, p.2.IsLC) (K : List Nat)
    (hKΦ : ∀ k ∈ K, k < Φ)
    (hKbr : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K)
    (hKfix : ∀ k ∈ K, R₀.onTy (.fvar k) = .fvar k)
    (hgrp : ∀ p ∈ bindings.zip (targets.map R₀.onTy), TypeOfHM (R₀.onCtx ctx) p.1 p.2) :
    ∃ R', Subst.AgreesBelow Φ R₀ (S ++ R') ∧ (∀ p ∈ R', p.2.IsLC) ∧
      (∀ k ∈ K, R'.onTy (.fvar k) = .fvar k) := by
  cases h with
  | nil => exact ⟨R₀, fun v _ => by rw [List.nil_append], hR₀, hKfix⟩
  | cons he huni hrest =>
      expose_names
      have hKbody : ∀ y ∈ e.tyFreeVars, y ∈ K := fun y hy => hKbr y (by
        simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inl hy)
      have hKrest : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars rest, y ∈ K := fun y hy => hKbr y (by
        simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inr hy)
      have hβ_lc : β.IsLC := htargets β List.mem_cons_self
      have hbβ : Ty.BelowFvars Φ β := hbtargets β List.mem_cons_self
      have hhead : TypeOfHM (R₀.onCtx ctx) e (R₀.onTy β) :=
        hgrp (e, R₀.onTy β) (by rw [List.map_cons, List.zip_cons_cons]; exact List.mem_cons_self)
      obtain ⟨R_b, hagbody, htybody, hR_b, hR_bfix⟩ :=
        Infer.complete' he hwf hbelow hR₀ K hKΦ hKbody hKfix hhead
      have hbodyLC := Infer.lc he hwf
      have hτ_lc := hbodyLC.1
      have hS_b := hbodyLC.2
      have hbb := Infer.belowFvars he hbelow (fun y hy => hKΦ y (hKbody y hy))
      have hle_b := Infer.frontier_le he
      have hUni : Unifies R_b τ (S₁.onTy β) := by
        show R_b.onTy τ = R_b.onTy (S₁.onTy β)
        rw [← htybody, ← Subst.onTy_append]
        exact Subst.onTy_congr hagbody hbβ
      obtain ⟨R_u, hR_u_eq, hR_u, hR_ufix⟩ := UnifyRel.greatest_K huni R_b hR_b hUni hR_bfix
      have hS_u := UnifyRel.lc huni hτ_lc (Subst.onTy_lc hS_b hβ_lc)
      have hS_u_below := UnifyRel.belowFvars huni hbb.1
        (Subst.onTy_belowFvars hbb.2 (hbβ.mono hle_b))
      have hwf' : CtxWF (S₂.onCtx (S₁.onCtx ctx)) :=
        Subst.onCtx_wf hS_u (Subst.onCtx_wf hS_b hwf)
      have hbelow_Sb : CtxBelow Φ₁ (S₁.onCtx ctx) := Subst.onCtx_below hbb.2 hle_b hbelow
      have hbelow' : CtxBelow Φ₁ (S₂.onCtx (S₁.onCtx ctx)) :=
        Subst.onCtx_below hS_u_below (le_refl _) hbelow_Sb
      have key_t : ∀ {t : Ty}, Ty.BelowFvars Φ t →
          R_u.onTy (S₂.onTy (S₁.onTy t)) = R₀.onTy t := by
        intro t ht
        rw [← hR_u_eq (S₁.onTy t), ← Subst.onTy_append]
        exact (Subst.onTy_congr hagbody ht).symm
      have htargets' : ∀ τ' ∈ (βs.map (fun b => S₂.onTy (S₁.onTy b))), τ'.IsLC := by
        intro τ' hτ'; obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hτ'
        exact Subst.onTy_lc hS_u (Subst.onTy_lc hS_b (htargets b (List.mem_cons_of_mem _ hb)))
      have hbtargets' : ∀ τ' ∈ (βs.map (fun b => S₂.onTy (S₁.onTy b))), Ty.BelowFvars Φ₁ τ' := by
        intro τ' hτ'; obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hτ'
        exact Subst.onTy_belowFvars hS_u_below
          (Subst.onTy_belowFvars hbb.2 ((hbtargets b (List.mem_cons_of_mem _ hb)).mono hle_b))
      have hctx_eq : R_u.onCtx (S₂.onCtx (S₁.onCtx ctx)) = R₀.onCtx ctx := by
        rw [← Subst.onCtx_comp_of_onTy_eq hR_u_eq, ← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => (hagbody v hv).symm) hbelow
      have hmapeq : (βs.map (fun b => S₂.onTy (S₁.onTy b))).map R_u.onTy = βs.map R₀.onTy := by
        rw [List.map_map]
        apply List.map_congr_left
        intro b hb
        exact key_t (hbtargets b (List.mem_cons_of_mem _ hb))
      have hgrp' : ∀ p ∈ rest.zip ((βs.map (fun b => S₂.onTy (S₁.onTy b))).map R_u.onTy),
          TypeOfHM (R_u.onCtx (S₂.onCtx (S₁.onCtx ctx))) p.1 p.2 := by
        rw [hmapeq, hctx_eq]
        intro p hp
        exact hgrp p (by rw [List.map_cons, List.zip_cons_cons]; exact List.mem_cons_of_mem _ hp)
      obtain ⟨R_r, hagrest, hR_r, hR_rfix⟩ :=
        InferRecGroup.complete' hrest hwf' hbelow' htargets' hbtargets' hR_u K
          (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle_b) hKrest hR_ufix hgrp'
      refine ⟨R_r, ?_, hR_r, hR_rfix⟩
      intro v hv
      have hbv1 : Ty.BelowFvars Φ₁ (S₁.onTy (.fvar v)) :=
        Subst.onTy_belowFvars hbb.2 (.fvar (by omega))
      have hbv2 : Ty.BelowFvars Φ₁ (S₂.onTy (S₁.onTy (.fvar v))) :=
        Subst.onTy_belowFvars hS_u_below hbv1
      calc R₀.onTy (.fvar v)
          = (S₁ ++ R_b).onTy (.fvar v) := hagbody v hv
        _ = R_b.onTy (S₁.onTy (.fvar v)) := by rw [Subst.onTy_append]
        _ = R_u.onTy (S₂.onTy (S₁.onTy (.fvar v))) := hR_u_eq (S₁.onTy (.fvar v))
        _ = (S₃ ++ R_r).onTy (S₂.onTy (S₁.onTy (.fvar v))) := Subst.onTy_congr hagrest hbv2
        _ = ((S₁ ++ S₂ ++ S₃) ++ R_r).onTy (.fvar v) := by simp only [Subst.onTy_append]
termination_by Expr.sizeRecGroup bindings
decreasing_by all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)

/-- **Principality of a given annotated recursion-group derivation** (consumer,
    analogue of `InferRecGroup.complete'`). Each binding is skolem-opened at the
    derivation's own block `freshVars N pc`; a block-swap re-skolemization (as in
    `complete'`'s `letInAnn` arm) reconciles that block with the arbitrary input
    residual `R₀`, produces a per-binding residual `V'`, and threads onto the rest. -/
theorem InferRecGroupAnn.complete' {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx)
    (hschemesWF : ∀ σ ∈ schemes, σ.WF)
    {R₀ : Subst} (hR₀ : ∀ p ∈ R₀, p.2.IsLC) {L : List Nat} (K : List Nat)
    (hKΦ : ∀ k ∈ K, k < Φ)
    (hKbr : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K)
    (hKsch : ∀ σ ∈ schemes, ∀ y ∈ σ.body.freeVars, y ∈ K)
    (hKfix : ∀ k ∈ K, R₀.onTy (.fvar k) = .fvar k)
    (hcofin : ∀ p ∈ bindings.zip schemes, ∀ Xs, FreshNames L p.2.paramCount Xs →
        TypeOfHM (R₀.onCtx ctx) (p.1.openTyVars Xs) (p.2.openVars Xs)) :
    ∃ R', Subst.AgreesBelow Φ R₀ (S ++ R') ∧ (∀ p ∈ R', p.2.IsLC) ∧
      (∀ k ∈ K, R'.onTy (.fvar k) = .fvar k) := by
  cases h with
  | nil => exact ⟨R₀, fun v _ => by rw [List.nil_append], hR₀, hKfix⟩
  | @cons _ N _ σ schtl e rest Φ₁ Φ₂ S₁ Schk S₂ eOut restOut τ₁ hΦN Drhs huni hesc1 hesc2 hrest =>
    have hσwf : σ.WF := hschemesWF σ List.mem_cons_self
    have hKrhs : ∀ y ∈ e.tyFreeVars, y ∈ K := fun y hy =>
      hKbr y (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self hy)
    have hKσ : ∀ y ∈ σ.body.freeVars, y ∈ K := hKsch σ List.mem_cons_self
    have hKrest : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars rest, y ∈ K := fun y hy =>
      hKbr y (by simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inr hy)
    have hcofin_head : ∀ Xs, FreshNames L σ.paramCount Xs →
        TypeOfHM (R₀.onCtx ctx) (e.openTyVars Xs) (σ.openVars Xs) := fun Xs hX =>
      hcofin (e, σ) (by rw [List.zip_cons_cons]; exact List.mem_cons_self) Xs hX
    have hle1 := Infer.frontier_le Drhs
    -- Re-skolemize: fresh block `[M₀, M₀+pc)` above `R₀`, conjugate `R₀` by the swap.
    obtain ⟨M₀, hd, hM₀fresh⟩ := exists_fresh_block
      (R₀.map Prod.fst ++ R₀.flatMap (fun p => p.2.freeVars)) N σ.paramCount
    have hR₀key_lt : ∀ p ∈ R₀, p.1 < M₀ := fun p hp =>
      hM₀fresh p.1 (List.mem_append_left _ (List.mem_map.mpr ⟨p, hp, rfl⟩))
    have hR₀range_lt : ∀ p ∈ R₀, ∀ v ∈ p.2.freeVars, v < M₀ := fun p hp v hv =>
      hM₀fresh v (List.mem_append_right _ (List.mem_flatMap.mpr ⟨p, hp, hv⟩))
    have hR₀belowM₀ : ∀ p ∈ R₀, Ty.BelowFvars M₀ p.2 :=
      fun p hp => Ty.BelowFvars.of_freeVars_lt (fun v hv => hR₀range_lt p hp v hv)
    have finj : Function.Injective (blockSwap N M₀ σ.paramCount) := blockSwap_injective hd
    have hffix : ∀ v, v < N → blockSwap N M₀ σ.paramCount v = v :=
      fun v hv => blockSwap_lt (by omega) hv
    have hconj_below : ∀ v, v < N → (Subst.conj (blockSwap N M₀ σ.paramCount) R₀).onTy (Ty.fvar v)
        = Ty.rename (blockSwap N M₀ σ.paramCount) (R₀.onTy (Ty.fvar v)) := by
      intro v hv
      have h := Subst.onTy_conj finj R₀ (Ty.fvar v)
      rw [Ty.rename_fvar, hffix v hv] at h
      exact h
    have hR₀'_K : ∀ k ∈ K, (Subst.conj (blockSwap N M₀ σ.paramCount) R₀).onTy (Ty.fvar k)
        = Ty.fvar k := by
      intro k hk
      have hklt : k < N := by have := hKΦ k hk; omega
      rw [hconj_below k hklt, hKfix k hk, Ty.rename_fvar, hffix k hklt]
    have hR₀'_Ys : ∀ Y ∈ freshVars N σ.paramCount,
        (Subst.conj (blockSwap N M₀ σ.paramCount) R₀).onTy (Ty.fvar Y) = Ty.fvar Y := by
      intro Y hY
      apply Ty.substFvars_eq_self_of_no_key
      intro p hp hc
      simp only [Ty.freeVars, List.mem_singleton] at hc
      simp only [Subst.conj, List.mem_map] at hp
      obtain ⟨q, hq, rfl⟩ := hp
      have hqlt : q.1 < M₀ := hR₀key_lt q hq
      have hYge : N ≤ Y := freshVars_ge Y hY
      have hYlt : Y < N + σ.paramCount := freshVars_lt Y hY
      simp only [blockSwap] at hc
      split_ifs at hc <;> omega
    have hctxeq_gen : (blockList N M₀ σ.paramCount).onCtx (R₀.onCtx ctx)
        = (Subst.conj (blockSwap N M₀ σ.paramCount) R₀).onCtx ctx := by
      simp only [Subst.onCtx, Subst.onEnv, List.map_map]
      congr 1
      apply List.map_congr_left
      intro M hM
      simp only [Function.comp_apply, Subst.onPolyTy]
      congr 1
      rw [blockList_onTy hd (fun v hv => by
            have hlt := (Subst.onTy_belowFvars hR₀belowM₀ ((hbelow M hM).mono (by omega))).mem_lt v hv
            omega)]
      conv_rhs => rw [← Ty.rename_eq_self (f := blockSwap N M₀ σ.paramCount) (τ := M.body)
        (fun v hv => hffix v (by have := (hbelow M hM).mem_lt v hv; omega))]
      rw [Subst.onTy_conj finj]
    have hexpr_id : ∀ Xs : List Nat, (∀ x ∈ Xs, x ∉ freshVars N σ.paramCount) →
        (e.openTyVars Xs).substTyFvars (blockList N M₀ σ.paramCount) = e.openTyVars Xs := by
      intro Xs hXYs
      apply Expr.substTyFvars_eq_self_of_not_mem_tyFreeVars
      intro p hp hc
      simp only [blockList, List.mem_map] at hp
      obtain ⟨i, hi, rfl⟩ := hp
      rcases Expr.tyFreeVars_openTyVars hc with h | h
      · have := hKΦ (N + i) (hKrhs (N + i) h); omega
      · exact hXYs (N + i) h (by simp only [freshVars, List.mem_map, List.mem_range]; exact ⟨i, List.mem_range.mp hi, rfl⟩)
    have htype_id : ∀ Xs : List Nat, (∀ x ∈ Xs, x ∉ freshVars N σ.paramCount) →
        (blockList N M₀ σ.paramCount).onTy (σ.openVars Xs) = σ.openVars Xs := by
      intro Xs hXYs
      apply Ty.substFvars_eq_self_of_no_key
      intro p hp hc
      simp only [blockList, List.mem_map] at hp
      obtain ⟨i, hi, rfl⟩ := hp
      rcases Ty.freeVars_openVars_subset (N + i) hc with h | h
      · have := hKΦ (N + i) (hKσ (N + i) h); omega
      · exact hXYs (N + i) h (by simp only [freshVars, List.mem_map, List.mem_range]; exact ⟨i, List.mem_range.mp hi, rfl⟩)
    have hcofin' : ∀ Xs : List Nat,
        FreshNames (L ++ freshVars N σ.paramCount) σ.paramCount Xs →
        TypeOfHM ((Subst.conj (blockSwap N M₀ σ.paramCount) R₀).onCtx ctx)
          (e.openTyVars Xs) (σ.openVars Xs) := by
      intro Xs hXs
      obtain ⟨hXlen, hXnodup, hXavoid⟩ := hXs
      have hXL : FreshNames L σ.paramCount Xs :=
        ⟨hXlen, hXnodup, fun x hx hc => hXavoid x hx (List.mem_append_left _ hc)⟩
      have hXYs : ∀ x ∈ Xs, x ∉ freshVars N σ.paramCount :=
        fun x hx hc => hXavoid x hx (List.mem_append_right _ hc)
      have ren := TypeOfHM.onSubst (blockList N M₀ σ.paramCount) (blockList_lc N M₀ σ.paramCount)
        (hcofin_head Xs hXL)
      rw [hctxeq_gen, hexpr_id Xs hXYs, htype_id Xs hXYs] at ren
      exact ren
    have hrhs_Ys' : TypeOfHM ((Subst.conj (blockSwap N M₀ σ.paramCount) R₀).onCtx ctx)
        (e.openTyVars (freshVars N σ.paramCount)) (σ.openVars (freshVars N σ.paramCount)) :=
      typeOfHM_at_block (freshVars_length N σ.paramCount) hcofin'
    have hbelowN : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hbelow M hM).mono (by omega)
    obtain ⟨R₁, haga, htya, hR₁lc, hR₁K⟩ :=
      Infer.complete' Drhs hwf hbelowN (Subst.conj_lc hR₀) (K ++ freshVars N σ.paramCount)
        (fun k hk => by
          rcases List.mem_append.mp hk with h | h
          · have := hKΦ k h; omega
          · have := freshVars_lt k h; omega)
        (fun y hy => by
          rcases Expr.tyFreeVars_openTyVars hy with h | h
          · exact List.mem_append_left _ (hKrhs y h)
          · exact List.mem_append_right _ h)
        (fun k hk => by
          rcases List.mem_append.mp hk with h | h
          · exact hR₀'_K k h
          · exact hR₀'_Ys k h)
        hrhs_Ys'
    have hτ₁lc := (Infer.lc Drhs hwf).1
    have hS₁lc := (Infer.lc Drhs hwf).2
    have hbf := Infer.belowFvars Drhs hbelowN (fun y hy => by
      rcases Expr.tyFreeVars_openTyVars hy with h | h
      · have := hKΦ y (hKrhs y h); omega
      · have := freshVars_lt y h; omega)
    have hbelowτ₁ := hbf.1
    have hbelowS₁ := hbf.2
    have hR₁_K : ∀ k ∈ K, R₁.onTy (Ty.fvar k) = Ty.fvar k :=
      fun k hk => hR₁K k (List.mem_append_left _ hk)
    have hR₁_Ys : ∀ Y ∈ freshVars N σ.paramCount, R₁.onTy (Ty.fvar Y) = Ty.fvar Y :=
      fun Y hY => hR₁K Y (List.mem_append_right _ hY)
    have hBlc : (σ.openVars (freshVars N σ.paramCount)).IsLC := PolyTy.openVars_isLC hσwf (by simp)
    have hUuni : Unifies R₁ τ₁ (σ.openVars (freshVars N σ.paramCount)) := by
      show R₁.onTy τ₁ = R₁.onTy (σ.openVars (freshVars N σ.paramCount))
      rw [← htya]
      refine (Subst.onTy_eq_self_of_fixes (fun z hz => ?_)).symm
      rcases Ty.freeVars_openVars_subset z hz with h | h
      · exact hR₁_K z (hKσ z h)
      · exact hR₁_Ys z h
    obtain ⟨V, hVeq, hVlc, hVK⟩ := UnifyRel.greatest_K huni R₁ hR₁lc hUuni hR₁K
    have hSchk_lc : ∀ p ∈ Schk, p.2.IsLC := UnifyRel.lc huni hτ₁lc hBlc
    have hV'lc : ∀ p ∈ V ++ blockListBack N M₀ σ.paramCount, p.2.IsLC := by
      intro p hp
      rcases List.mem_append.mp hp with h | h
      · exact hVlc p h
      · exact blockListBack_lc N M₀ σ.paramCount p h
    have hV'_K : ∀ k ∈ K, (V ++ blockListBack N M₀ σ.paramCount).onTy (Ty.fvar k) = Ty.fvar k := by
      intro k hk
      have hklt : k < N := by have := hKΦ k hk; omega
      rw [Subst.onTy_append, hVK k (List.mem_append_left _ hk)]
      apply Ty.substFvars_eq_self_of_no_key
      intro p hp hc
      simp only [blockListBack, List.mem_map] at hp
      obtain ⟨i, _, rfl⟩ := hp
      simp only [Ty.freeVars, List.mem_singleton] at hc
      omega
    have hagSchkV' : Subst.AgreesBelow Φ R₀ (S₁ ++ Schk ++ (V ++ blockListBack N M₀ σ.paramCount)) := by
      intro v hv
      have hvN : v < N := by omega
      have hR₀v_block : ∀ w ∈ (R₀.onTy (Ty.fvar v)).freeVars,
          ¬ (M₀ ≤ w ∧ w < M₀ + σ.paramCount) := by
        intro w hw
        have := (Subst.onTy_belowFvars hR₀belowM₀ (Ty.BelowFvars.fvar (by omega))).mem_lt w hw
        omega
      calc R₀.onTy (Ty.fvar v)
          = (blockListBack N M₀ σ.paramCount).onTy
              (Ty.rename (blockSwap N M₀ σ.paramCount) (R₀.onTy (Ty.fvar v))) :=
            (blockListBack_onTy_rename hd hR₀v_block).symm
        _ = (blockListBack N M₀ σ.paramCount).onTy
              ((Subst.conj (blockSwap N M₀ σ.paramCount) R₀).onTy (Ty.fvar v)) := by
              rw [hconj_below v hvN]
        _ = (blockListBack N M₀ σ.paramCount).onTy ((S₁ ++ R₁).onTy (Ty.fvar v)) := by
              rw [haga v (by omega)]
        _ = (blockListBack N M₀ σ.paramCount).onTy (R₁.onTy (S₁.onTy (Ty.fvar v))) := by
              rw [Subst.onTy_append]
        _ = (blockListBack N M₀ σ.paramCount).onTy
              (V.onTy (Schk.onTy (S₁.onTy (Ty.fvar v)))) := by rw [hVeq _]
        _ = (S₁ ++ Schk ++ (V ++ blockListBack N M₀ σ.paramCount)).onTy (Ty.fvar v) := by
              simp only [Subst.onTy_append]
    have hσopen' : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
      Ty.openVars_belowFvars
        (Ty.BelowFvars.of_freeVars_lt (fun v hv => by have := hKΦ v (hKσ v hv); omega))
        (fun x hx => by have := freshVars_lt x hx; omega)
    have hSchkbelow : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 :=
      UnifyRel.belowFvars huni hbelowτ₁ hσopen'
    have hwf₁ := Subst.onCtx_wf hS₁lc hwf
    have hwfSchk := Subst.onCtx_wf hSchk_lc hwf₁
    have hbelowSchk : CtxBelow Φ₁ (Schk.onCtx (S₁.onCtx ctx)) :=
      Subst.onCtx_below hSchkbelow (le_refl _) (Subst.onCtx_below hbelowS₁ (by omega) hbelow)
    have hmain : (V ++ blockListBack N M₀ σ.paramCount).onCtx (Schk.onCtx (S₁.onCtx ctx))
        = R₀.onCtx ctx := by
      have hc : (S₁ ++ Schk ++ (V ++ blockListBack N M₀ σ.paramCount)).onCtx ctx = R₀.onCtx ctx :=
        Subst.onCtx_congr (fun v hv => (hagSchkV' v hv).symm) hbelow
      rw [Subst.onCtx_append (S₁ ++ Schk) (V ++ blockListBack N M₀ σ.paramCount) ctx,
          Subst.onCtx_append S₁ Schk ctx] at hc
      exact hc
    have hdecl_rest : ∀ p ∈ rest.zip schtl, ∀ Xs, FreshNames L p.2.paramCount Xs →
        TypeOfHM ((V ++ blockListBack N M₀ σ.paramCount).onCtx (Schk.onCtx (S₁.onCtx ctx)))
          (p.1.openTyVars Xs) (p.2.openVars Xs) := by
      intro p hp Xs hX
      rw [hmain]
      exact hcofin p (by rw [List.zip_cons_cons]; exact List.mem_cons_of_mem _ hp) Xs hX
    obtain ⟨R_r, hagrest, hR_r, hR_rK⟩ :=
      InferRecGroupAnn.complete' hrest hwfSchk hbelowSchk
        (fun σ' hσ' => hschemesWF σ' (List.mem_cons_of_mem _ hσ')) hV'lc K
        (fun k hk => by have := hKΦ k hk; have := hle1; have := hΦN; omega) hKrest
        (fun σ' hσ' => hKsch σ' (List.mem_cons_of_mem _ hσ')) hV'_K hdecl_rest
    refine ⟨R_r, ?_, hR_r, hR_rK⟩
    refine Subst.AgreesBelow.trans_append (by omega) hagSchkV' ?_ hagrest
    intro p hp
    rcases List.mem_append.mp hp with hp | hp
    · exact hbelowS₁ p hp
    · exact hSchkbelow p hp
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end

/-! ### Source↔elaborated bridges (Phase 5 step 2)

`eraseVarTyArgs` strips the type-application decorations on `var` nodes
(`.var i _ ↦ .var i []`) — the skeleton both the declarative `TypeOfHM` and the
source program share. With the insensitivity lemma (`Infer.eraseVarTyArgs_invariant`),
the forward decoration bridge (`TypeOfHM.toElab`), and the backward `Infer.sourceSound`,
we expose the declarative `TypeOfHM` at the user-facing principality/typeability
boundary while keeping all internal machinery against `TypeOfHM`. -/

mutual
/-- Strip every `var` node's type-application decoration. -/
def Expr.eraseVarTyArgs : Expr → Expr
  | .primLit p          => .primLit p
  | .lambda ann body    => .lambda ann body.eraseVarTyArgs
  | .app f arg          => .app f.eraseVarTyArgs arg.eraseVarTyArgs
  | .letIn ann rhs body => .letIn ann rhs.eraseVarTyArgs body.eraseVarTyArgs
  | .var i _            => .var i []
  | .ctor c             => .ctor c
  | .match_ scrut branches =>
      .match_ scrut.eraseVarTyArgs (BranchList.eraseVarTyArgs branches)
  | .letRec bindings body =>
      .letRec (RecGroup.eraseVarTyArgs bindings) body.eraseVarTyArgs
  | .letRecAnn schemes bindings body =>
      .letRecAnn schemes (RecGroup.eraseVarTyArgs bindings) body.eraseVarTyArgs
def BranchList.eraseVarTyArgs : List (MatchPattern × Expr) → List (MatchPattern × Expr)
  | []                  => []
  | (pat, body) :: rest => (pat, body.eraseVarTyArgs) :: BranchList.eraseVarTyArgs rest
def RecGroup.eraseVarTyArgs : List Expr → List Expr
  | []        => []
  | e :: rest => e.eraseVarTyArgs :: RecGroup.eraseVarTyArgs rest
end

/-- Membership-aware `List.Forall₂` implication. -/
theorem List.Forall₂.imp_mem {α β : Type*} {R S : α → β → Prop} :
    ∀ {l₁ : List α} {l₂ : List β}, (∀ a ∈ l₁, ∀ b, R a b → S a b) →
      List.Forall₂ R l₁ l₂ → List.Forall₂ S l₁ l₂ := by
  intro l₁
  induction l₁ with
  | nil => intro l₂ _ h; cases h; exact .nil
  | cons a l₁' ih =>
    intro l₂ H h
    cases h with
    | cons hab hrest =>
      exact .cons (H a (List.mem_cons_self ..) _ hab)
        (ih (fun a' ha' b' hb' => H a' (List.mem_cons_of_mem _ ha') b' hb') hrest)

/-- `eraseVarTyArgs` commutes with the per-branch `substTyFvars`-map. -/
theorem BranchList.eraseVarTyArgs_map_substTyFvars {S : Subst}
    (brs : List (MatchPattern × Expr))
    (h : ∀ pat e, (pat, e) ∈ brs →
        (e.substTyFvars S).eraseVarTyArgs = (e.eraseVarTyArgs).substTyFvars S) :
    BranchList.eraseVarTyArgs (brs.map (fun pb => (pb.1, pb.2.substTyFvars S)))
      = (BranchList.eraseVarTyArgs brs).map (fun pb => (pb.1, pb.2.substTyFvars S)) := by
  induction brs with
  | nil => rfl
  | cons hd tl ih =>
    obtain ⟨pat, body⟩ := hd
    simp only [List.map_cons, BranchList.eraseVarTyArgs]
    rw [h pat body (List.mem_cons_self ..)]
    congr 1
    exact ih (fun p e hpe => h p e (List.mem_cons_of_mem _ hpe))

/-- `eraseVarTyArgs` commutes with the per-binding `substTyFvars`-map. -/
theorem RecGroup.eraseVarTyArgs_map_substTyFvars {S : Subst} (bs : List Expr)
    (h : ∀ e ∈ bs, (e.substTyFvars S).eraseVarTyArgs = (e.eraseVarTyArgs).substTyFvars S) :
    RecGroup.eraseVarTyArgs (bs.map (·.substTyFvars S))
      = (RecGroup.eraseVarTyArgs bs).map (·.substTyFvars S) := by
  induction bs with
  | nil => rfl
  | cons hd tl ih =>
    simp only [List.map_cons, RecGroup.eraseVarTyArgs]
    rw [h hd (List.mem_cons_self ..)]
    congr 1
    exact ih (fun e he => h e (List.mem_cons_of_mem _ he))

/-- `eraseVarTyArgs` commutes with `substTyFvars` (both zero var decorations;
    annotations are substituted identically). -/
theorem Expr.eraseVarTyArgs_substTyFvars {e : Expr} {S : Subst} :
    (e.substTyFvars S).eraseVarTyArgs = (e.eraseVarTyArgs).substTyFvars S := by
  induction e using Expr.rec_strong with
  | primLit p => simp only [Expr.substTyFvars_primLit, Expr.eraseVarTyArgs]
  | var n tyArgs => simp only [Expr.substTyFvars_var, Expr.eraseVarTyArgs, List.map_nil]
  | ctor nm => simp only [Expr.substTyFvars_ctor, Expr.eraseVarTyArgs]
  | lambda ann body ih => simp only [Expr.substTyFvars_lambda, Expr.eraseVarTyArgs, ih]
  | app f arg ihf iharg => simp only [Expr.substTyFvars_app, Expr.eraseVarTyArgs, ihf, iharg]
  | letIn ann be body ihbe ihbo =>
    simp only [Expr.substTyFvars_letIn, Expr.eraseVarTyArgs, ihbe, ihbo]
  | match_ scrut branches ihs ihbs =>
    simp only [Expr.substTyFvars_match, Expr.eraseVarTyArgs, ihs]
    rw [BranchList.eraseVarTyArgs_map_substTyFvars branches ihbs]
  | letRec bindings body ihbs ihbo =>
    simp only [Expr.substTyFvars_letRec, Expr.eraseVarTyArgs, ihbo]
    rw [RecGroup.eraseVarTyArgs_map_substTyFvars bindings ihbs]
  | letRecAnn schemes bindings body ihbs ihbo =>
    simp only [Expr.substTyFvars_letRecAnn, Expr.eraseVarTyArgs, ihbo]
    rw [RecGroup.eraseVarTyArgs_map_substTyFvars bindings ihbs]

/-! `eraseVarTyArgs` commutes with scoped-tyvar opening (both zero var decorations;
    annotations are opened identically). Needed for `insensitive`'s annotated
    cofinite cases. -/
mutual
theorem Expr.eraseVarTyArgs_openTyVarsAux {Xs : List Nat} : ∀ (d : Nat) (e : Expr),
    (e.openTyVarsAux d Xs).eraseVarTyArgs = (e.eraseVarTyArgs).openTyVarsAux d Xs
  | _, .primLit _ => rfl
  | _, .ctor _ => rfl
  | _, .var _ _ => rfl
  | d, .lambda ann body => by
      simp only [Expr.openTyVarsAux, Expr.eraseVarTyArgs, Expr.eraseVarTyArgs_openTyVarsAux d body]
  | d, .app f arg => by
      simp only [Expr.openTyVarsAux, Expr.eraseVarTyArgs,
        Expr.eraseVarTyArgs_openTyVarsAux d f, Expr.eraseVarTyArgs_openTyVarsAux d arg]
  | d, .letIn (some σ) rhs body => by
      simp only [Expr.openTyVarsAux, Expr.eraseVarTyArgs,
        Expr.eraseVarTyArgs_openTyVarsAux (d + σ.paramCount) rhs,
        Expr.eraseVarTyArgs_openTyVarsAux d body]
  | d, .letIn none rhs body => by
      simp only [Expr.openTyVarsAux, Expr.eraseVarTyArgs,
        Expr.eraseVarTyArgs_openTyVarsAux d rhs, Expr.eraseVarTyArgs_openTyVarsAux d body]
  | d, .match_ scrut branches => by
      simp only [Expr.openTyVarsAux, Expr.eraseVarTyArgs,
        Expr.eraseVarTyArgs_openTyVarsAux d scrut, BranchList.eraseVarTyArgs_openTyVarsAux d branches]
  | d, .letRec bindings body => by
      simp only [Expr.openTyVarsAux, Expr.eraseVarTyArgs,
        RecGroup.eraseVarTyArgs_openTyVarsAux d bindings, Expr.eraseVarTyArgs_openTyVarsAux d body]
  | d, .letRecAnn schemes bindings body => by
      simp only [Expr.openTyVarsAux, Expr.eraseVarTyArgs,
        RecGroupAnn.eraseVarTyArgs_openTyVarsAux d schemes bindings,
        Expr.eraseVarTyArgs_openTyVarsAux d body]
theorem BranchList.eraseVarTyArgs_openTyVarsAux {Xs : List Nat} :
    ∀ (d : Nat) (brs : List (MatchPattern × Expr)),
      BranchList.eraseVarTyArgs (BranchList.openTyVarsAux d Xs brs)
        = BranchList.openTyVarsAux d Xs (BranchList.eraseVarTyArgs brs)
  | _, [] => rfl
  | d, (pat, body) :: rest => by
      simp only [BranchList.openTyVarsAux, BranchList.eraseVarTyArgs,
        Expr.eraseVarTyArgs_openTyVarsAux d body, BranchList.eraseVarTyArgs_openTyVarsAux d rest]
theorem RecGroup.eraseVarTyArgs_openTyVarsAux {Xs : List Nat} : ∀ (d : Nat) (bs : List Expr),
    RecGroup.eraseVarTyArgs (RecGroup.openTyVarsAux d Xs bs)
      = RecGroup.openTyVarsAux d Xs (RecGroup.eraseVarTyArgs bs)
  | _, [] => rfl
  | d, e :: rest => by
      simp only [RecGroup.openTyVarsAux, RecGroup.eraseVarTyArgs,
        Expr.eraseVarTyArgs_openTyVarsAux d e, RecGroup.eraseVarTyArgs_openTyVarsAux d rest]
theorem RecGroupAnn.eraseVarTyArgs_openTyVarsAux {Xs : List Nat} :
    ∀ (d : Nat) (schemes : List PolyTy) (bs : List Expr),
      RecGroup.eraseVarTyArgs (RecGroupAnn.openTyVarsAux d Xs schemes bs)
        = RecGroupAnn.openTyVarsAux d Xs schemes (RecGroup.eraseVarTyArgs bs)
  | _, _, [] => rfl
  | d, [], e :: rest => by
      simp only [RecGroupAnn.openTyVarsAux, RecGroup.eraseVarTyArgs,
        Expr.eraseVarTyArgs_openTyVarsAux d e, RecGroupAnn.eraseVarTyArgs_openTyVarsAux d [] rest]
  | d, σ :: ss, e :: rest => by
      simp only [RecGroupAnn.openTyVarsAux, RecGroup.eraseVarTyArgs,
        Expr.eraseVarTyArgs_openTyVarsAux (d + σ.paramCount) e,
        RecGroupAnn.eraseVarTyArgs_openTyVarsAux d ss rest]
end

theorem Expr.eraseVarTyArgs_openTyVars {Xs : List Nat} {e : Expr} :
    (e.openTyVars Xs).eraseVarTyArgs = (e.eraseVarTyArgs).openTyVars Xs :=
  Expr.eraseVarTyArgs_openTyVarsAux 0 e

/-- `eraseVarTyArgs` congruence through `openBoundTyVars` (both `ann` cases). -/
theorem Expr.eraseVarTyArgs_openBoundTyVars_congr {ann : Option PolyTy} {Xs : List Nat}
    {e e' : Expr} (h : e.eraseVarTyArgs = e'.eraseVarTyArgs) :
    (Expr.openBoundTyVars ann Xs e).eraseVarTyArgs
      = (Expr.openBoundTyVars ann Xs e').eraseVarTyArgs := by
  cases ann with
  | none => simpa only [Expr.openBoundTyVars] using h
  | some σ =>
    simp only [Expr.openBoundTyVars, Expr.eraseVarTyArgs_openTyVars, h]

/-- Output-uniqueness up to instance: any two `Infer` results on the same input
    are mutual substitution-instances. (From `complete'` of the first applied to
    the `sound` typing of the second.) -/
theorem Infer.output_unique {Φ ctx e Φ'₁ S₁ eOut₁ τ₁ Φ'₂ S₂ eOut₂ τ₂}
    (h₁ : Infer Φ ctx e Φ'₁ S₁ eOut₁ τ₁) (h₂ : Infer Φ ctx e Φ'₂ S₂ eOut₂ τ₂)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hclosed : e.tyFreeVars = []) :
    ∃ R, τ₂ = Subst.onTy R τ₁ ∧ (∀ p ∈ R, p.2.IsLC) := by
  -- `h₂`'s output types the source `e` (`sourceSound`); factor it through `h₁`'s
  -- output via `complete'`.
  have hty₂ := Infer.sourceSound h₂ hwf hbelow [] (by simp) (by rw [hclosed]; simp) (by simp)
  obtain ⟨R, _, hτeq, hRlc, _⟩ :=
    Infer.complete' h₁ hwf hbelow (Infer.lc h₂ hwf).2 [] (by simp) (by rw [hclosed]; simp) (by simp) hty₂
  exact ⟨R, hτeq, hRlc⟩

/-- `(S, τ)` is a *principal typing* of `e` in `ctx`: a valid declarative typing
    of which every declarative typing (under any LC specialization of `ctx`) is a
    substitution instance. -/
structure Infer.IsPrincipal (ctx : Ctx) (e : Expr) (S : Subst) (τ : Ty) : Prop where
  typing : TypeOfHM (S.onCtx ctx) e τ
  principal : ∀ {S₀ : Subst} {τ₀ : Ty}, (∀ p ∈ S₀, p.2.IsLC) →
    TypeOfHM (S₀.onCtx ctx) e τ₀ → ∃ R, τ₀ = Subst.onTy R τ

/-- Every `Infer` result is a principal typing. With `Infer.complete`'s existence
    half, `Infer` computes the principal type of any typeable `e`. -/
theorem Infer.isPrincipal {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hclosed : e.tyFreeVars = []) :
    Infer.IsPrincipal ctx e S τ where
  typing := Infer.sourceSound h hwf hbelow [] (by simp) (by rw [hclosed]; simp) (by simp)
  principal := fun {S₀ τ₀} hS₀ hty => by
    obtain ⟨R, _, hτeq, _, _⟩ :=
      Infer.complete' h hwf hbelow hS₀ [] (by simp) (by rw [hclosed]; simp) (by simp) hty
    exact ⟨R, hτeq⟩





/-! ## Stage 4: the executable `unify` and `infer`

Everything above specifies and certifies the *relations* `UnifyRel` and `Infer`.
This final stage gives the actual *functions* (`unify`, `infer`) and proves they
**refine** those relations. The only non-trivial part is `unify`'s termination,
discharged with the standard lexicographic measure `(#distinct free vars, size)`:
each var-elimination step strictly drops the variable count, while the structural
decompositions (`arrow`/`pair`/`customTy`) drop `Ty.size`. The supporting
variable-tracking lemmas about `UnifyRel`-substitutions are proved first. -/

/-! (Variable-tracking lemmas for substitutions and `UnifyRel` relocated to the
    soundness-prerequisites section above, before `Infer.sound`.) -/

/-! ### Annotated-`let` completeness (relocated after `complete'` / `dom_mem`)

The annotated-`let` principality core (`Infer.complete_letIn_ann_aux`) is
assembled here, now that its ingredients are in scope: the rigidity-aware
unification completeness `UnifyRel.complete_K` (for `hesc1` — the MGU avoids the
skolems by construction), the `greatest_K` factoring (for the direct `hesc2`
argument), and the `UnifyRel.dom_mem` domain bound. `Infer.completeAt` itself is
assembled here too. -/


/-- The **annotated** `letIn` principality core (rigidity-aware). Choose a skolem
    block `Ys = freshVars N pc` with `N ≥ Φ` fresh for `S₀` (domain + range),
    `ctx`, and `L`; infer the *opened* rhs `rhs.openTyVars Ys` at rigid set
    `K ∪ Ys` (so `S₁` avoids `Ys`); the residual `R₁` (which fixes `K ∪ Ys`) is a
    unifier of `τ₁` and `σ.openVars Ys`, so `complete_K` yields the MGU `Schk`
    avoiding `K ∪ Ys` (hesc1). hesc2 is direct: the factor `V` of `R₁` through
    `Schk` fixes each `Y`, so a leak would land in `(S₀.onCtx ctx).env`, which
    avoids `Ys` by freshness. -/
theorem Infer.complete_letIn_ann_aux {Φ : Nat} {ctx : Ctx} {S₀ : Subst}
    {rhs body : Expr} {σ : PolyTy} {L : List Nat} {τ₀ : Ty} {K : List Nat}
    (iha : ∀ Ys, Infer.CompleteAt (rhs.openTyVars Ys)) (ihb : Infer.CompleteAt body)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hS₀ : ∀ p ∈ S₀, p.2.IsLC)
    (hKΦ : ∀ k ∈ K, k < Φ)
    (hKrhs : ∀ y ∈ rhs.tyFreeVars, y ∈ K) (hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K)
    (hKσ : ∀ y ∈ σ.body.freeVars, y ∈ K)
    (hKfix : ∀ k ∈ K, S₀.onTy (.fvar k) = .fvar k)
    (hσwf : σ.WF)
    (hcofin : ∀ Xs : List Nat, FreshNames L σ.paramCount Xs →
      TypeOfHM (S₀.onCtx ctx) (rhs.openTyVars Xs) (σ.openVars Xs))
    (hbody : TypeOfHM { (S₀.onCtx ctx) with env := σ :: (S₀.onCtx ctx).env } body τ₀) :
    ∃ Φ' S eOut τ R,
      Infer Φ ctx (.letIn (some σ) rhs body) Φ' S eOut τ ∧
      Subst.AgreesBelow Φ S₀ (S ++ R) ∧
      τ₀ = R.onTy τ ∧
      (∀ p ∈ R, p.2.IsLC) ∧
      (∀ k ∈ K, R.onTy (.fvar k) = .fvar k) ∧
      (∀ p ∈ S, p.1 ∉ K) := by
  -- Skolem block `Ys = freshVars N pc`, `N ≥ Φ`, fresh for `S₀`/`ctx`/`L`.
  obtain ⟨N, hΦN, hNfresh⟩ := exists_fresh_block
    (S₀.map Prod.fst ++ S₀.flatMap (fun p => p.2.freeVars) ++ L) Φ σ.paramCount
  have hYsdom : ∀ Y ∈ freshVars N σ.paramCount, Y ∉ S₀.map Prod.fst := fun Y hY hc => by
    have := hNfresh Y (List.mem_append_left _ (List.mem_append_left _ hc))
    have := freshVars_ge Y hY; omega
  have hYsrange : ∀ Y ∈ freshVars N σ.paramCount, ∀ p ∈ S₀, Y ∉ p.2.freeVars :=
    fun Y hY p hp hc => by
      have := hNfresh Y (List.mem_append_left _ (List.mem_append_right _
        (List.mem_flatMap.mpr ⟨p, hp, hc⟩)))
      have := freshVars_ge Y hY; omega
  have hYsL : ∀ Y ∈ freshVars N σ.paramCount, Y ∉ L := fun Y hY hc => by
    have := hNfresh Y (List.mem_append_right _ hc)
    have := freshVars_ge Y hY; omega
  have hS₀Ys : ∀ Y ∈ freshVars N σ.paramCount, S₀.onTy (Ty.fvar Y) = Ty.fvar Y := fun Y hY =>
    Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
      simp only [Ty.freeVars, List.mem_singleton] at hc
      exact hYsdom Y hY (hc ▸ List.mem_map.mpr ⟨p, hp, rfl⟩))
  -- Infer the opened rhs at rigid set `K ∪ Ys`.
  have hbelowN : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hbelow M hM).mono (by omega)
  obtain ⟨Φ₁, S₁, eOut₁, τ₁, R₁, Drhs, haga, htya, hR₁lc, hR₁K, hS₁K⟩ :=
    iha (freshVars N σ.paramCount) (K ++ freshVars N σ.paramCount) hwf hbelowN hS₀
      (fun k hk => by
        rcases List.mem_append.mp hk with hk | hk
        · have := hKΦ k hk; omega
        · have := freshVars_lt k hk; omega)
      (fun y hy => by
        rcases Expr.tyFreeVars_openTyVars hy with h | h
        · exact List.mem_append_left _ (hKrhs y h)
        · exact List.mem_append_right _ h)
      (fun k hk => by
        rcases List.mem_append.mp hk with hk | hk
        · exact hKfix k hk
        · exact hS₀Ys k hk)
      (hcofin (freshVars N σ.paramCount) ⟨freshVars_length _ _, freshVars_nodup, hYsL⟩)
  have hτ₁lc := (Infer.lc Drhs hwf).1
  have hS₁lc := (Infer.lc Drhs hwf).2
  have hle1 := Infer.frontier_le Drhs
  have hbf := Infer.belowFvars Drhs hbelowN (fun y hy => by
    rcases Expr.tyFreeVars_openTyVars hy with h | h
    · have := hKΦ y (hKrhs y h); omega
    · have := freshVars_lt y h; omega)
  have hbelowτ₁ := hbf.1
  have hbelowS₁ := hbf.2
  have hR₁_K : ∀ k ∈ K, R₁.onTy (Ty.fvar k) = Ty.fvar k :=
    fun k hk => hR₁K k (List.mem_append_left _ hk)
  have hR₁_Ys : ∀ Y ∈ freshVars N σ.paramCount, R₁.onTy (Ty.fvar Y) = Ty.fvar Y :=
    fun Y hY => hR₁K Y (List.mem_append_right _ hY)
  have hS₁_K : ∀ p ∈ S₁, p.1 ∉ K := fun p hp hc => hS₁K p hp (List.mem_append_left _ hc)
  have hS₁_Ys : ∀ p ∈ S₁, p.1 ∉ freshVars N σ.paramCount :=
    fun p hp hc => hS₁K p hp (List.mem_append_right _ hc)
  -- `Schk`: MGU of `τ₁` and `σ.openVars Ys`, avoiding `K ∪ Ys`, via `R₁`.
  have hBlc : (σ.openVars (freshVars N σ.paramCount)).IsLC := PolyTy.openVars_isLC hσwf (by simp)
  have hUuni : Unifies R₁ τ₁ (σ.openVars (freshVars N σ.paramCount)) := by
    show R₁.onTy τ₁ = R₁.onTy (σ.openVars (freshVars N σ.paramCount))
    rw [← htya]
    refine (Subst.onTy_eq_self_of_fixes (fun z hz => ?_)).symm
    rcases Ty.freeVars_openVars_subset z hz with h | h
    · exact hR₁_K z (hKσ z h)
    · exact hR₁_Ys z h
  obtain ⟨Schk, huni, hSchkKYs⟩ := UnifyRel.complete_K hτ₁lc hBlc hR₁lc hUuni hR₁K
  have hSchk_K : ∀ p ∈ Schk, p.1 ∉ K := fun p hp hc => hSchkKYs p hp (List.mem_append_left _ hc)
  have hSchk_Ys : ∀ p ∈ Schk, p.1 ∉ freshVars N σ.paramCount :=
    fun p hp hc => hSchkKYs p hp (List.mem_append_right _ hc)
  have hSchk_lc : ∀ p ∈ Schk, p.2.IsLC := UnifyRel.lc huni hτ₁lc hBlc
  have hesc1 : ∀ Y ∈ freshVars N σ.paramCount, Y ∉ (S₁ ++ Schk).map Prod.fst := by
    intro Y hY hc
    obtain ⟨p, hp, hpY⟩ := List.mem_map.mp hc
    rcases List.mem_append.mp hp with hp | hp
    · exact hS₁_Ys p hp (by rw [hpY]; exact hY)
    · exact hSchk_Ys p hp (by rw [hpY]; exact hY)
  -- Factor `R₁` through `Schk` → `V` (fixes `K ∪ Ys`); `S₀ = V ∘ Schk ∘ S₁` below `Φ`.
  obtain ⟨V, hVeq, hVlc, hVK⟩ := UnifyRel.greatest_K huni R₁ hR₁lc hUuni hR₁K
  have hagSchkV : Subst.AgreesBelow Φ S₀ (S₁ ++ Schk ++ V) := by
    intro v hv
    calc S₀.onTy (Ty.fvar v)
        = (S₁ ++ R₁).onTy (Ty.fvar v) := haga v (by omega)
      _ = R₁.onTy (S₁.onTy (Ty.fvar v)) := by rw [Subst.onTy_append]
      _ = V.onTy (Schk.onTy (S₁.onTy (Ty.fvar v))) := hVeq _
      _ = (S₁ ++ Schk ++ V).onTy (Ty.fvar v) := by rw [Subst.onTy_append, Subst.onTy_append]
  have hmain : V.onCtx (Schk.onCtx (S₁.onCtx ctx)) = S₀.onCtx ctx := by
    have hc : (S₁ ++ Schk ++ V).onCtx ctx = S₀.onCtx ctx :=
      Subst.onCtx_congr (fun v hv => (hagSchkV v hv).symm) hbelow
    rwa [Subst.onCtx_append, Subst.onCtx_append] at hc
  -- hesc2 (direct): `V` fixes `Y`, so a leak survives into `(S₀.onCtx ctx).env`.
  have hesc2 : ∀ Y ∈ freshVars N σ.paramCount,
      Y ∉ (Schk.onCtx (S₁.onCtx ctx)).env.freeVars := by
    intro Y hY hmem
    have hVfixY : V.onTy (Ty.fvar Y) = Ty.fvar Y := hVK Y (List.mem_append_right _ hY)
    obtain ⟨M, hM, hYM⟩ := Env.mem_freeVars_iff.mp hmem
    have hYS₀ : Y ∈ (S₀.onCtx ctx).env.freeVars := by
      have hgo : Y ∈ (V.onCtx (Schk.onCtx (S₁.onCtx ctx))).env.freeVars := by
        refine Env.mem_freeVars_iff.mpr ⟨V.onPolyTy M, ?_, ?_⟩
        · simp only [Subst.onCtx, Subst.onEnv]; exact List.mem_map.mpr ⟨M, hM, rfl⟩
        · simp only [Subst.onPolyTy]
          exact Ty.mem_freeVars_onTy_iff.mpr ⟨Y, hYM, by rw [hVfixY]; simp [Ty.freeVars]⟩
      rwa [hmain] at hgo
    obtain ⟨M', hM', hYM'⟩ := Env.mem_freeVars_iff.mp hYS₀
    simp only [Subst.onCtx, Subst.onEnv] at hM'
    obtain ⟨M0, hM0, rfl⟩ := List.mem_map.mp hM'
    simp only [Subst.onPolyTy] at hYM'
    obtain ⟨v, hv, hYv⟩ := Ty.mem_freeVars_onTy_iff.mp hYM'
    have hvlt : v < Φ := (hbelow M0 hM0).mem_lt v hv
    have hYnev : Y ∉ (Ty.fvar v).freeVars := by
      simp only [Ty.freeVars, List.mem_singleton]
      have := freshVars_ge Y hY; omega
    exact Subst.not_mem_onTy_freeVars (hYsrange Y hY) hYnev hYv
  -- Thread the body with `V` as input specialization.
  have hwf₁ := Subst.onCtx_wf hS₁lc hwf
  have hwfSchk := Subst.onCtx_wf hSchk_lc hwf₁
  have hwfB : CtxWF { (Schk.onCtx (S₁.onCtx ctx)) with
      env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env } := by
    intro M hM; rcases List.mem_cons.mp hM with rfl | hM
    · exact hσwf
    · exact hwfSchk M hM
  have hσbelow : Ty.BelowFvars Φ₁ σ.body :=
    Ty.BelowFvars.of_freeVars_lt (fun v hv => by have := hKΦ v (hKσ v hv); omega)
  have hσopen' : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
    Ty.openVars_belowFvars hσbelow (fun x hx => by have := freshVars_lt x hx; omega)
  have hSchkbelow : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 :=
    UnifyRel.belowFvars huni hbelowτ₁ hσopen'
  have hbelowSchk : CtxBelow Φ₁ (Schk.onCtx (S₁.onCtx ctx)) :=
    Subst.onCtx_below hSchkbelow (le_refl _) (Subst.onCtx_below hbelowS₁ (by omega) hbelow)
  have hbelowB : CtxBelow Φ₁ { (Schk.onCtx (S₁.onCtx ctx)) with
      env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env } := by
    intro M hM; rcases List.mem_cons.mp hM with rfl | hM
    · exact hσbelow
    · exact hbelowSchk M hM
  have hVσ_fix : V.onPolyTy σ = σ := by
    obtain ⟨pcc, b⟩ := σ
    simp only [Subst.onPolyTy, PolyTy.mk.injEq, true_and]
    exact Subst.onTy_eq_self_of_fixes (fun v hv => hVK v (List.mem_append_left _ (hKσ v hv)))
  have hbodyV : TypeOfHM (V.onCtx { (Schk.onCtx (S₁.onCtx ctx)) with
      env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env }) body τ₀ := by
    show TypeOfHM ⟨V.onPolyTy σ :: V.onEnv (Schk.onCtx (S₁.onCtx ctx)).env,
        (Schk.onCtx (S₁.onCtx ctx)).ctors⟩ body τ₀
    rw [hVσ_fix, show V.onEnv (Schk.onCtx (S₁.onCtx ctx)).env = (S₀.onCtx ctx).env from
        congrArg Ctx.env hmain]
    exact hbody
  obtain ⟨Φ₂, S₂, eOut₂, τ₂, R₂, Dbody, hagb, htyb, hR₂lc, hR₂K, hS₂K⟩ :=
    ihb K hwfB hbelowB hVlc (fun k hk => by have := hKΦ k hk; omega) hKbody
      (fun k hk => hVK k (List.mem_append_left _ hk)) hbodyV
  refine ⟨Φ₂, S₁ ++ Schk ++ S₂, _, τ₂, R₂,
    Infer.letInAnn hσwf (by omega) Drhs huni hesc1 hesc2 Dbody, ?_, htyb, hR₂lc, hR₂K, ?_⟩
  · refine Subst.AgreesBelow.trans_append (by omega) hagSchkV ?_ hagb
    intro p hp
    rcases List.mem_append.mp hp with hp | hp
    · exact hbelowS₁ p hp
    · exact hSchkbelow p hp
  · intro p hp
    rcases List.mem_append.mp hp with hp | hp
    · rcases List.mem_append.mp hp with hp | hp
      · exact hS₁_K p hp
      · exact hSchk_K p hp
    · exact hS₂K p hp

/-- Principality, `letIn` case. -/
theorem Infer.complete_letIn {ann : Option PolyTy} {rhs body : Expr}
    (iha : Infer.CompleteAt rhs)
    (ihao : ∀ Ys, Infer.CompleteAt (rhs.openTyVars Ys))
    (ihb : Infer.CompleteAt body) :
    Infer.CompleteAt (.letIn ann rhs body) := by
  intro Φ ctx S₀ τ₀ K hwf hbelow hS₀ hKΦ hKe hKfix hty
  cases hty with
  | letIn hMwf hann hcofin heq hbody =>
    subst heq
    cases ann with
    | none =>
      simp only [Expr.openBoundTyVars] at hcofin
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append] at hKe
      exact Infer.complete_letIn_aux iha ihb hwf hbelow hS₀ hKΦ
        (fun y hy => hKe y (.inl hy)) (fun y hy => hKe y (.inr hy)) hKfix hMwf hcofin hbody
    | some σ =>
      have hMσ := hann σ rfl
      subst hMσ
      simp only [Expr.openBoundTyVars] at hcofin
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at hKe
      exact Infer.complete_letIn_ann_aux ihao ihb hwf hbelow hS₀ hKΦ
        (fun y hy => hKe y (.inl (.inr hy))) (fun y hy => hKe y (.inr hy))
        (fun y hy => hKe y (.inl (.inl hy))) hKfix hMwf hcofin hbody

/-- Principality, `letRec` case (producer). Mirrors the `complete'` `letRec` case
    but *produces* the inference: a fresh block residual (`exists_recgroup_residual`)
    sends the group's monotype vars to a chosen declarative opening, the producing
    group threader (`InferRecGroup.complete`) builds the group derivation, and
    `letRec_body_retype` retypes the body under the inferred schemes for the body
    IH. -/
theorem Infer.complete_letRec {bindings : List Expr} {body : Expr}
    (ihbindings : ∀ e ∈ bindings, Infer.CompleteAt e) (ihbody : Infer.CompleteAt body) :
    Infer.CompleteAt (.letRec bindings body) := by
  intro Φ ctx S₀ τ₀ K hwf hbelow hS₀ hKΦ hKe hKfix hty
  simp only [Expr.tyFreeVars, List.mem_append] at hKe
  have hKgrp : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K := fun y hy => hKe y (.inl hy)
  have hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKe y (.inr hy)
  have hKrigid : ∀ y ∈ bindings.flatMap Expr.tyFreeVars, y ∈ K :=
    fun y hy => hKgrp y (Expr.mem_recGroup_tyFreeVars.mpr hy)
  cases hty with
  | letRec hlen hlen2 hlc hG hgen hcofin heqc hbodyD =>
    subst heqc
    expose_names
    have hctxβWF : CtxWF (⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
        ++ ctx.env, ctx.ctors⟩ : Ctx) := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · obtain ⟨x, _, rfl⟩ := List.mem_map.mp hM; exact ContainsBvarsUpTo.fvar
      · exact hwf M hM
    have hctxβbelow : CtxBelow (Φ + bindings.length)
        (⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
          ++ ctx.env, ctx.ctors⟩ : Ctx) := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
        exact Ty.BelowFvars.fvar (by have := freshVars_lt x hx; omega)
      · exact (hbelow M hM).mono (by omega)
    have hβlc : ∀ τ ∈ (freshVars Φ bindings.length).map Ty.fvar, τ.IsLC := by
      intro τ hτ; obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ; exact ContainsBvarsUpTo.fvar
    have hβbelow : ∀ τ ∈ (freshVars Φ bindings.length).map Ty.fvar,
        Ty.BelowFvars (Φ + bindings.length) τ := by
      intro τ hτ; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
      exact Ty.BelowFvars.fvar (by have := freshVars_lt x hx; omega)
    obtain ⟨Xs, hXlen, hXnodup, hXavoid⟩ := exists_fresh_names
      (L ++ G ++ Ty.freeVarsList τs ++ (S₀.onCtx ctx).env.freeVars
        ++ bindings.flatMap Expr.tyFreeVars) G.length
    have hXL : ∀ x ∈ Xs, x ∉ L := fun x hx hc => hXavoid x hx (by simp only [List.mem_append]; tauto)
    have hXG : ∀ x ∈ Xs, x ∉ G := fun x hx hc => hXavoid x hx (by simp only [List.mem_append]; tauto)
    have hXτs : ∀ x ∈ Xs, x ∉ Ty.freeVarsList τs := fun x hx hc =>
      hXavoid x hx (by simp only [List.mem_append]; tauto)
    have hXenv : ∀ x ∈ Xs, x ∉ (S₀.onCtx ctx).env.freeVars := fun x hx hc =>
      hXavoid x hx (by simp only [List.mem_append]; tauto)
    have hXrigid : ∀ x ∈ Xs, x ∉ bindings.flatMap Expr.tyFreeVars := fun x hx hc =>
      hXavoid x hx (by simp only [List.mem_append]; tauto)
    have hXfresh : FreshNames L G.length Xs := ⟨hXlen, hXnodup, hXL⟩
    obtain ⟨R₀, hR₀lc, hR₀K, hR₀agΦ, hR₀block⟩ :=
      exists_recgroup_residual (Φ := Φ) (n := bindings.length) (S₀ := S₀)
        (vs := τs.map (Ty.renameG G Xs)) (K := K)
        (by rw [List.length_map]; omega) hS₀
        (fun t ht => by
          obtain ⟨τ', hτ', rfl⟩ := List.mem_map.mp ht
          rw [← Subst.onTy_zip_fvar_eq_renameG]
          exact Subst.onTy_lc (fun p hp => by
            obtain ⟨w, _, hw⟩ := List.mem_map.mp (List.of_mem_zip hp).2
            rw [← hw]; exact ContainsBvarsUpTo.fvar) (hlc τ' hτ'))
        hKΦ hKfix
    have hR₀ctx : R₀.onCtx ctx = S₀.onCtx ctx := Subst.onCtx_congr hR₀agΦ hbelow
    have hR₀β : (freshVars Φ bindings.length).map (fun i => R₀.onTy (Ty.fvar i))
        = τs.map (Ty.renameG G Xs) := by
      rw [← hR₀block]; simp only [List.map_map, Function.comp_def]
    have hgrp : ∀ p ∈ bindings.zip (((freshVars Φ bindings.length).map Ty.fvar).map R₀.onTy),
        TypeOfHM (R₀.onCtx (⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
          ++ ctx.env, ctx.ctors⟩ : Ctx)) p.1 p.2 := by
      have hbmap : ((freshVars Φ bindings.length).map Ty.fvar).map R₀.onTy = τs.map (Ty.renameG G Xs) := by
        rw [List.map_map]; exact hR₀β
      have hctxβeq : R₀.onCtx (⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
            ++ ctx.env, ctx.ctors⟩ : Ctx)
          = ⟨(τs.map (Ty.renameG G Xs)).map PolyTy.mkTrivial ++ (S₀.onCtx ctx).env, ctx.ctors⟩ := by
        show (⟨((freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
            ++ ctx.env).map R₀.onPolyTy, ctx.ctors⟩ : Ctx) = _
        rw [List.map_append]; congr 1; congr 1
        · rw [← hR₀β, List.map_map, List.map_map]
          apply List.map_congr_left; intro i _
          simp only [Function.comp_apply, Subst.onPolyTy, PolyTy.mkTrivial]
        · exact congrArg Ctx.env hR₀ctx
      rw [hbmap, hctxβeq]
      exact fun p hp => hcofin Xs hXfresh p hp
    obtain ⟨Φ₁, S₁, bindingsOut, R₁, hgroup, hag_group, hR₁lc, hR₁fix, hS₁K⟩ :=
      InferRecGroup.complete (bindings := bindings)
        (targets := (freshVars Φ bindings.length).map Ty.fvar) (R := R₀) K
        (by rw [List.length_map, freshVars_length]) ihbindings hctxβWF hctxβbelow hβlc hβbelow
        hR₀lc (fun k hk => by have := hKΦ k hk; omega) hKgrp hR₀K hgrp
    have hgle : Φ + bindings.length ≤ Φ₁ := InferRecGroup.frontier_le hgroup
    have hS₁lc : ∀ p ∈ S₁, p.2.IsLC := InferRecGroup.lc hgroup hctxβWF hβlc
    have hS₁below : ∀ p ∈ S₁, Ty.BelowFvars Φ₁ p.2 :=
      InferRecGroup.belowFvars hgroup hctxβbelow hβbelow
        (fun y hy => by have := hKΦ y (hKgrp y hy); omega)
    have hwf₁ : CtxWF (S₁.onCtx ctx) := Subst.onCtx_wf hS₁lc hwf
    have hbelow₁ : CtxBelow Φ₁ (S₁.onCtx ctx) :=
      Subst.onCtx_below hS₁below (le_trans (Nat.le_add_right Φ _) hgle) hbelow
    have hctxeq : R₁.onCtx (S₁.onCtx ctx) = S₀.onCtx ctx := by
      rw [← Subst.onCtx_append,
        Subst.onCtx_congr (fun v hv => (hag_group v (by omega)).symm) hbelow]
      exact hR₀ctx
    have hconn : ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i))).map R₁.onTy
        = τs.map (Ty.renameG G Xs) := by
      rw [← hR₀β, List.map_map]
      apply List.map_congr_left; intro i hi
      simp only [Function.comp_apply]
      rw [← Subst.onTy_append]
      exact (hag_group i (freshVars_lt i hi)).symm
    have hbody'' := letRec_body_retype hlen hlen2 hlc hG hgen hXlen hXnodup
      (fun g hg hc => hXG g hc hg) hXτs hXenv hXrigid hS₁lc hR₁lc hctxeq hKrigid hR₁fix hconn hbodyD
    have hτsinf_lc : ∀ τ ∈ (freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)), τ.IsLC := by
      intro τ hτ; obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ
      exact Subst.onTy_lc hS₁lc ContainsBvarsUpTo.fvar
    have hτsinf_below : ∀ τ ∈ (freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)),
        Ty.BelowFvars Φ₁ τ := by
      intro τ hτ; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
      exact Subst.onTy_belowFvars hS₁below (.fvar (by have := freshVars_lt x hx; omega))
    have hwfBodyInf : CtxWF (⟨genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
        ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i))) ++ (S₁.onCtx ctx).env,
        (S₁.onCtx ctx).ctors⟩ : Ctx) := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · simp only [genGroupSchemes] at hM; obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
        exact PolyTy.genGroup_wf (hτsinf_lc τ hτ)
      · exact hwf₁ M hM
    have hbelowBodyInf : CtxBelow Φ₁ (⟨genGroupSchemes (bindings.flatMap Expr.tyFreeVars)
        (S₁.onCtx ctx).env ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)))
        ++ (S₁.onCtx ctx).env, (S₁.onCtx ctx).ctors⟩ : Ctx) := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · simp only [genGroupSchemes] at hM; obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
        exact (hτsinf_below τ hτ).closeOver
      · exact hbelow₁ M hM
    obtain ⟨Φ₂, S₂, eOut₂, τ₂, R₂, hbodyInf, hagb, htyb, hR₂lc, hR₂K, hS₂K⟩ :=
      ihbody K hwfBodyInf hbelowBodyInf hR₁lc
        (fun k hk => lt_of_lt_of_le (hKΦ k hk) (le_trans (Nat.le_add_right Φ _) hgle))
        hKbody hR₁fix hbody''
    refine ⟨Φ₂, S₁ ++ S₂, _, τ₂, R₂, Infer.letRec hgroup hbodyInf, ?_, htyb, hR₂lc, hR₂K, ?_⟩
    · have hag1 : Subst.AgreesBelow Φ S₀ (S₁ ++ R₁) := fun v hv =>
        (hR₀agΦ v hv).symm.trans (hag_group v (by omega))
      exact Subst.AgreesBelow.trans_append (le_trans (Nat.le_add_right Φ _) hgle) hag1 hS₁below hagb
    · intro p hp
      rcases List.mem_append.mp hp with hp | hp
      · exact hS₁K p hp
      · exact hS₂K p hp

/-- **Annotated recursion-group threading completeness** (producer). The analogue
    of `InferRecGroup.complete`, but each binding is checked *scheme-relatively*:
    per-binding skolem opening + rigidity-aware unify + escape checks (the
    `complete_letIn_ann_aux` core), threaded over the annotated group. The schemes
    are given and rigid (no `genGroup`/`renameG`). -/
theorem InferRecGroupAnn.complete {bindings : List Expr} :
    ∀ {Φ : Nat} {ctx : Ctx} {schemes : List PolyTy} {R : Subst} {L : List Nat} (K : List Nat),
    bindings.length = schemes.length →
    (∀ e ∈ bindings, ∀ Ys, Infer.CompleteAt (e.openTyVars Ys)) →
    CtxWF ctx → CtxBelow Φ ctx →
    (∀ σ ∈ schemes, σ.WF) →
    (∀ p ∈ R, p.2.IsLC) →
    (∀ k ∈ K, k < Φ) →
    (∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K) →
    (∀ σ ∈ schemes, ∀ y ∈ σ.body.freeVars, y ∈ K) →
    (∀ k ∈ K, R.onTy (.fvar k) = .fvar k) →
    (∀ p ∈ bindings.zip schemes, ∀ Xs, FreshNames L p.2.paramCount Xs →
        TypeOfHM (R.onCtx ctx) (p.1.openTyVars Xs) (p.2.openVars Xs)) →
    ∃ Φ' S bindingsOut R',
      InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut ∧
      Subst.AgreesBelow Φ R (S ++ R') ∧
      (∀ p ∈ R', p.2.IsLC) ∧
      (∀ k ∈ K, R'.onTy (.fvar k) = .fvar k) ∧
      (∀ p ∈ S, p.1 ∉ K) := by
  induction bindings with
  | nil =>
    intro Φ ctx schemes R L K hlen _ _ _ _ hR hKΦ _ _ hKfix _
    cases schemes with
    | nil => exact ⟨Φ, [], [], R, InferRecGroupAnn.nil, fun v _ => by rw [List.nil_append], hR, hKfix, by simp⟩
    | cons => simp at hlen
  | cons e rest ih =>
    intro Φ ctx schemes R L K hlen ihbr hwf hbelow hschWF hR hKΦ hKbr hKsch hKfix hdecl
    cases schemes with
    | nil => simp at hlen
    | cons σ schtl =>
      have hσwf : σ.WF := hschWF σ List.mem_cons_self
      have hKrhs : ∀ y ∈ e.tyFreeVars, y ∈ K := fun y hy =>
        hKbr y (Expr.mem_recGroupTyFreeVars_of List.mem_cons_self hy)
      have hKσ : ∀ y ∈ σ.body.freeVars, y ∈ K := hKsch σ List.mem_cons_self
      have hKrest : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars rest, y ∈ K := fun y hy =>
        hKbr y (by simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inr hy)
      have hKsch' : ∀ σ' ∈ schtl, ∀ y ∈ σ'.body.freeVars, y ∈ K := fun σ' hσ' y hy =>
        hKsch σ' (List.mem_cons_of_mem _ hσ') y hy
      have hcofin_head : ∀ Xs, FreshNames L σ.paramCount Xs →
          TypeOfHM (R.onCtx ctx) (e.openTyVars Xs) (σ.openVars Xs) := fun Xs hX =>
        hdecl (e, σ) (by rw [List.zip_cons_cons]; exact List.mem_cons_self) Xs hX
      -- Skolem block `Ys = freshVars N pc`, `N ≥ Φ`, fresh for `R`/`ctx`/`L`.
      obtain ⟨N, hΦN, hNfresh⟩ := exists_fresh_block
        (R.map Prod.fst ++ R.flatMap (fun p => p.2.freeVars) ++ L) Φ σ.paramCount
      have hYsdom : ∀ Y ∈ freshVars N σ.paramCount, Y ∉ R.map Prod.fst := fun Y hY hc => by
        have := hNfresh Y (List.mem_append_left _ (List.mem_append_left _ hc))
        have := freshVars_ge Y hY; omega
      have hYsrange : ∀ Y ∈ freshVars N σ.paramCount, ∀ p ∈ R, Y ∉ p.2.freeVars :=
        fun Y hY p hp hc => by
          have := hNfresh Y (List.mem_append_left _ (List.mem_append_right _
            (List.mem_flatMap.mpr ⟨p, hp, hc⟩)))
          have := freshVars_ge Y hY; omega
      have hYsL : ∀ Y ∈ freshVars N σ.paramCount, Y ∉ L := fun Y hY hc => by
        have := hNfresh Y (List.mem_append_right _ hc)
        have := freshVars_ge Y hY; omega
      have hRYs : ∀ Y ∈ freshVars N σ.paramCount, R.onTy (Ty.fvar Y) = Ty.fvar Y := fun Y hY =>
        Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
          simp only [Ty.freeVars, List.mem_singleton] at hc
          exact hYsdom Y hY (hc ▸ List.mem_map.mpr ⟨p, hp, rfl⟩))
      have hbelowN : CtxBelow (N + σ.paramCount) ctx := fun M hM => (hbelow M hM).mono (by omega)
      obtain ⟨Φ₁, S₁, eOut₁, τ₁, R₁, Drhs, haga, htya, hR₁lc, hR₁K, hS₁K⟩ :=
        ihbr e List.mem_cons_self (freshVars N σ.paramCount) (K ++ freshVars N σ.paramCount)
          hwf hbelowN hR
          (fun k hk => by
            rcases List.mem_append.mp hk with hk | hk
            · have := hKΦ k hk; omega
            · have := freshVars_lt k hk; omega)
          (fun y hy => by
            rcases Expr.tyFreeVars_openTyVars hy with h | h
            · exact List.mem_append_left _ (hKrhs y h)
            · exact List.mem_append_right _ h)
          (fun k hk => by
            rcases List.mem_append.mp hk with hk | hk
            · exact hKfix k hk
            · exact hRYs k hk)
          (hcofin_head (freshVars N σ.paramCount) ⟨freshVars_length _ _, freshVars_nodup, hYsL⟩)
      have hτ₁lc := (Infer.lc Drhs hwf).1
      have hS₁lc := (Infer.lc Drhs hwf).2
      have hle1 := Infer.frontier_le Drhs
      have hbf := Infer.belowFvars Drhs hbelowN (fun y hy => by
        rcases Expr.tyFreeVars_openTyVars hy with h | h
        · have := hKΦ y (hKrhs y h); omega
        · have := freshVars_lt y h; omega)
      have hbelowτ₁ := hbf.1
      have hbelowS₁ := hbf.2
      have hR₁_K : ∀ k ∈ K, R₁.onTy (Ty.fvar k) = Ty.fvar k :=
        fun k hk => hR₁K k (List.mem_append_left _ hk)
      have hR₁_Ys : ∀ Y ∈ freshVars N σ.paramCount, R₁.onTy (Ty.fvar Y) = Ty.fvar Y :=
        fun Y hY => hR₁K Y (List.mem_append_right _ hY)
      have hS₁_K : ∀ p ∈ S₁, p.1 ∉ K := fun p hp hc => hS₁K p hp (List.mem_append_left _ hc)
      have hS₁_Ys : ∀ p ∈ S₁, p.1 ∉ freshVars N σ.paramCount :=
        fun p hp hc => hS₁K p hp (List.mem_append_right _ hc)
      have hBlc : (σ.openVars (freshVars N σ.paramCount)).IsLC := PolyTy.openVars_isLC hσwf (by simp)
      have hUuni : Unifies R₁ τ₁ (σ.openVars (freshVars N σ.paramCount)) := by
        show R₁.onTy τ₁ = R₁.onTy (σ.openVars (freshVars N σ.paramCount))
        rw [← htya]
        refine (Subst.onTy_eq_self_of_fixes (fun z hz => ?_)).symm
        rcases Ty.freeVars_openVars_subset z hz with h | h
        · exact hR₁_K z (hKσ z h)
        · exact hR₁_Ys z h
      obtain ⟨Schk, huni, hSchkKYs⟩ := UnifyRel.complete_K hτ₁lc hBlc hR₁lc hUuni hR₁K
      have hSchk_K : ∀ p ∈ Schk, p.1 ∉ K := fun p hp hc => hSchkKYs p hp (List.mem_append_left _ hc)
      have hSchk_Ys : ∀ p ∈ Schk, p.1 ∉ freshVars N σ.paramCount :=
        fun p hp hc => hSchkKYs p hp (List.mem_append_right _ hc)
      have hSchk_lc : ∀ p ∈ Schk, p.2.IsLC := UnifyRel.lc huni hτ₁lc hBlc
      have hesc1 : ∀ Y ∈ freshVars N σ.paramCount, Y ∉ (S₁ ++ Schk).map Prod.fst := by
        intro Y hY hc
        obtain ⟨p, hp, hpY⟩ := List.mem_map.mp hc
        rcases List.mem_append.mp hp with hp | hp
        · exact hS₁_Ys p hp (by rw [hpY]; exact hY)
        · exact hSchk_Ys p hp (by rw [hpY]; exact hY)
      obtain ⟨V, hVeq, hVlc, hVK⟩ := UnifyRel.greatest_K huni R₁ hR₁lc hUuni hR₁K
      have hagSchkV : Subst.AgreesBelow Φ R (S₁ ++ Schk ++ V) := by
        intro v hv
        calc R.onTy (Ty.fvar v)
            = (S₁ ++ R₁).onTy (Ty.fvar v) := haga v (by omega)
          _ = R₁.onTy (S₁.onTy (Ty.fvar v)) := by rw [Subst.onTy_append]
          _ = V.onTy (Schk.onTy (S₁.onTy (Ty.fvar v))) := hVeq _
          _ = (S₁ ++ Schk ++ V).onTy (Ty.fvar v) := by rw [Subst.onTy_append, Subst.onTy_append]
      have hmain : V.onCtx (Schk.onCtx (S₁.onCtx ctx)) = R.onCtx ctx := by
        have hc : (S₁ ++ Schk ++ V).onCtx ctx = R.onCtx ctx :=
          Subst.onCtx_congr (fun v hv => (hagSchkV v hv).symm) hbelow
        rwa [Subst.onCtx_append, Subst.onCtx_append] at hc
      have hesc2 : ∀ Y ∈ freshVars N σ.paramCount,
          Y ∉ (Schk.onCtx (S₁.onCtx ctx)).env.freeVars := by
        intro Y hY hmem
        have hVfixY : V.onTy (Ty.fvar Y) = Ty.fvar Y := hVK Y (List.mem_append_right _ hY)
        obtain ⟨M, hM, hYM⟩ := Env.mem_freeVars_iff.mp hmem
        have hYR : Y ∈ (R.onCtx ctx).env.freeVars := by
          have hgo : Y ∈ (V.onCtx (Schk.onCtx (S₁.onCtx ctx))).env.freeVars := by
            refine Env.mem_freeVars_iff.mpr ⟨V.onPolyTy M, ?_, ?_⟩
            · simp only [Subst.onCtx, Subst.onEnv]; exact List.mem_map.mpr ⟨M, hM, rfl⟩
            · simp only [Subst.onPolyTy]
              exact Ty.mem_freeVars_onTy_iff.mpr ⟨Y, hYM, by rw [hVfixY]; simp [Ty.freeVars]⟩
          rwa [hmain] at hgo
        obtain ⟨M', hM', hYM'⟩ := Env.mem_freeVars_iff.mp hYR
        simp only [Subst.onCtx, Subst.onEnv] at hM'
        obtain ⟨M0, hM0, rfl⟩ := List.mem_map.mp hM'
        simp only [Subst.onPolyTy] at hYM'
        obtain ⟨v, hv, hYv⟩ := Ty.mem_freeVars_onTy_iff.mp hYM'
        have hvlt : v < Φ := (hbelow M0 hM0).mem_lt v hv
        have hYnev : Y ∉ (Ty.fvar v).freeVars := by
          simp only [Ty.freeVars, List.mem_singleton]
          have := freshVars_ge Y hY; omega
        exact Subst.not_mem_onTy_freeVars (hYsrange Y hY) hYnev hYv
      -- threaded context WF/below for the recursion
      have hwf₁ := Subst.onCtx_wf hS₁lc hwf
      have hwfSchk := Subst.onCtx_wf hSchk_lc hwf₁
      have hσopen' : Ty.BelowFvars Φ₁ (σ.openVars (freshVars N σ.paramCount)) :=
        Ty.openVars_belowFvars (Ty.BelowFvars.of_freeVars_lt
          (fun v hv => by have := hKΦ v (hKσ v hv); omega))
          (fun x hx => by have := freshVars_lt x hx; omega)
      have hSchkbelow : ∀ p ∈ Schk, Ty.BelowFvars Φ₁ p.2 :=
        UnifyRel.belowFvars huni hbelowτ₁ hσopen'
      have hbelowSchk : CtxBelow Φ₁ (Schk.onCtx (S₁.onCtx ctx)) :=
        Subst.onCtx_below hSchkbelow (le_refl _) (Subst.onCtx_below hbelowS₁ (by omega) hbelow)
      -- rest cofinite transferred to the threaded ctx via `hmain`
      have hdecl_rest : ∀ p ∈ rest.zip schtl, ∀ Xs, FreshNames L p.2.paramCount Xs →
          TypeOfHM (V.onCtx (Schk.onCtx (S₁.onCtx ctx))) (p.1.openTyVars Xs) (p.2.openVars Xs) := by
        intro p hp Xs hX
        rw [hmain]
        exact hdecl p (by rw [List.zip_cons_cons]; exact List.mem_cons_of_mem _ hp) Xs hX
      obtain ⟨Φ', S_r, bindingsOut_r, R_r, hinfrest, hagrest, hR_r, hR_rK, hS_rK⟩ :=
        ih K (by simpa using hlen)
          (fun e' he' Ys => ihbr e' (List.mem_cons_of_mem _ he') Ys)
          hwfSchk hbelowSchk (fun σ' hσ' => hschWF σ' (List.mem_cons_of_mem _ hσ')) hVlc
          (fun k hk => by have := hKΦ k hk; have := hle1; have := hΦN; omega) hKrest hKsch'
          (fun k hk => hVK k (List.mem_append_left _ hk)) hdecl_rest
      refine ⟨Φ', S₁ ++ Schk ++ S_r, _, R_r,
        InferRecGroupAnn.cons (by omega) Drhs huni hesc1 hesc2 hinfrest, ?_, hR_r, hR_rK, ?_⟩
      · refine Subst.AgreesBelow.trans_append (by omega) hagSchkV ?_ hagrest
        intro p hp
        rcases List.mem_append.mp hp with hp | hp
        · exact hbelowS₁ p hp
        · exact hSchkbelow p hp
      · intro p hp
        rcases List.mem_append.mp hp with hp | hp
        · rcases List.mem_append.mp hp with hp | hp
          · exact hS₁_K p hp
          · exact hSchk_K p hp
        · exact hS_rK p hp

/-- Principality, annotated-recursion-group case (producer). Mirrors
    `complete_letRec`'s assembly, but the schemes are GIVEN and rigid: no
    `genGroup`/`renameG`, and the body sees the same declarative schemes, so the
    inverted body typing transfers directly (via `AgreesBelow`). -/
theorem Infer.complete_letRecAnn {schemes : List PolyTy} {bindings : List Expr} {body : Expr}
    (ihbindings : ∀ e ∈ bindings, ∀ Ys, Infer.CompleteAt (e.openTyVars Ys))
    (ihbody : Infer.CompleteAt body) :
    Infer.CompleteAt (.letRecAnn schemes bindings body) := by
  intro Φ ctx S₀ τ₀ K hwf hbelow hS₀ hKΦ hKe hKfix hty
  simp only [Expr.tyFreeVars, List.mem_append] at hKe
  have hKsch : ∀ σ ∈ schemes, ∀ y ∈ σ.body.freeVars, y ∈ K := fun σ hσ y hy =>
    hKe y (.inl (.inl (Expr.mem_tyFreeVars_schemeList hσ hy)))
  have hKgrp : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K := fun y hy =>
    hKe y (.inl (.inr hy))
  have hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKe y (.inr hy)
  cases hty with
  | letRecAnn hlen hwf' hcofin heqc hbodyD =>
    subst heqc
    have hschB : ∀ σ ∈ schemes, Ty.BelowFvars Φ σ.body := fun σ hσ =>
      Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hKsch σ hσ v hv))
    have hctxSWF : CtxWF { ctx with env := schemes ++ ctx.env } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hwf' M hM
      · exact hwf M hM
    have hctxSbelow : CtxBelow Φ { ctx with env := schemes ++ ctx.env } := by
      intro M hM; rcases List.mem_append.mp hM with hM | hM
      · exact hschB M hM
      · exact hbelow M hM
    have hSschmap : schemes.map (Subst.onPolyTy S₀) = schemes := by
      conv_rhs => rw [← List.map_id schemes]
      refine List.map_congr_left (fun σ hσ => ?_)
      obtain ⟨pcc, b⟩ := σ
      simp only [Subst.onPolyTy, PolyTy.mk.injEq, true_and, id_eq]
      exact Subst.onTy_eq_self_of_fixes (fun v hv => hKfix v (hKsch ⟨pcc, b⟩ hσ v hv))
    have hctxS_eq : S₀.onCtx { ctx with env := schemes ++ ctx.env }
        = { (S₀.onCtx ctx) with env := schemes ++ (S₀.onCtx ctx).env } := by
      simp only [Subst.onCtx, Subst.onEnv, List.map_append]
      rw [hSschmap]
    obtain ⟨Φ₁, S₁, bindingsOut, R₁, hgroup, hag_group, hR₁lc, hR₁fix, hS₁K⟩ :=
      InferRecGroupAnn.complete (bindings := bindings) (schemes := schemes) (R := S₀)
        (ctx := { ctx with env := schemes ++ ctx.env }) K
        hlen ihbindings hctxSWF hctxSbelow hwf' hS₀ hKΦ hKgrp hKsch hKfix
        (fun p hp Xs hX => by rw [hctxS_eq]; exact hcofin p hp Xs hX)
    have hgrle : Φ ≤ Φ₁ := InferRecGroupAnn.frontier_le hgroup
    have hKgrpΦ : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < Φ :=
      fun y hy => hKΦ y (hKgrp y hy)
    have hS₁lc : ∀ p ∈ S₁, p.2.IsLC := InferRecGroupAnn.lc hgroup hctxSWF hwf'
    have hS₁below : ∀ p ∈ S₁, Ty.BelowFvars Φ₁ p.2 :=
      InferRecGroupAnn.belowFvars hgroup hctxSbelow hschB hKgrpΦ
    have hbodyWF : CtxWF (S₁.onCtx { ctx with env := schemes ++ ctx.env }) :=
      Subst.onCtx_wf hS₁lc hctxSWF
    have hbodybelow : CtxBelow Φ₁ (S₁.onCtx { ctx with env := schemes ++ ctx.env }) :=
      Subst.onCtx_below hS₁below hgrle hctxSbelow
    have hbodyctx_eq : R₁.onCtx (S₁.onCtx { ctx with env := schemes ++ ctx.env })
        = S₀.onCtx { ctx with env := schemes ++ ctx.env } := by
      rw [← Subst.onCtx_append]
      exact Subst.onCtx_congr (fun v hv => (hag_group v hv).symm) hctxSbelow
    have hbody'' : TypeOfHM (R₁.onCtx (S₁.onCtx { ctx with env := schemes ++ ctx.env })) body τ₀ := by
      rw [hbodyctx_eq, hctxS_eq]; exact hbodyD
    obtain ⟨Φ₂, S₂, eOut₂, τ₂, R₂, hbodyInf, hagb, htyb, hR₂lc, hR₂K, hS₂K⟩ :=
      ihbody K hbodyWF hbodybelow hR₁lc (fun k hk => lt_of_lt_of_le (hKΦ k hk) hgrle)
        hKbody hR₁fix hbody''
    refine ⟨Φ₂, S₁ ++ S₂, _, τ₂, R₂,
      Infer.letRecAnn hwf' hlen hgroup hbodyInf, ?_, htyb, hR₂lc, hR₂K, ?_⟩
    · exact Subst.AgreesBelow.trans_append hgrle hag_group hS₁below hagb
    · intro p hp
      rcases List.mem_append.mp hp with hp | hp
      · exact hS₁K p hp
      · exact hS₂K p hp

/-! ### Principality, assembled -/

/-- Every expression satisfies the principality property `CompleteAt`, assembled
    from the per-form case lemmas by strong induction on `e.size` — the annotated
    `let` case needs the principality of the *opened* rhs `rhs.openTyVars Ys`,
    which is not a structural subterm but has the same size (`Expr.size_openTyVars`). -/
theorem Infer.completeAt (e : Expr) : Infer.CompleteAt e := by
  intro Φ ctx S₀ τ₀ K hwf hbelow hS₀ hKΦ hKe hKfix hty
  suffices h : ∀ n, ∀ e : Expr, e.size ≤ n → Infer.CompleteAt e by
    exact h e.size e (le_refl _) K hwf hbelow hS₀ hKΦ hKe hKfix hty
  intro n
  induction n with
  | zero => intro e he; exfalso; cases e <;> simp only [Expr.size] at he <;> omega
  | succ n ih =>
    intro e he
    cases e with
    | primLit p => exact Infer.complete_prim
    | lambda ann body => exact Infer.complete_lambda (ih body (by simp only [Expr.size] at he; omega))
    | app f arg =>
      exact Infer.complete_app (ih f (by simp only [Expr.size] at he; omega))
        (ih arg (by simp only [Expr.size] at he; omega))
    | letIn ann be body =>
      refine Infer.complete_letIn (ih be (by simp only [Expr.size] at he; omega))
        (fun Ys => ih (be.openTyVars Ys)
          (by rw [Expr.size_openTyVars]; simp only [Expr.size] at he; omega))
        (ih body (by simp only [Expr.size] at he; omega))
    | var i tyArgs => exact Infer.complete_var
    | ctor name => exact Infer.complete_ctor
    | match_ scrut branches =>
      refine Infer.complete_match (ih scrut (by simp only [Expr.size] at he; omega)) ?_
      intro br hbr
      refine ih br.2 ?_
      have hb : ∀ (bs : List (MatchPattern × Expr)), br ∈ bs →
          br.2.size ≤ Expr.sizeBranches bs := by
        intro bs
        induction bs with
        | nil => intro hbs; simp at hbs
        | cons hd tl ihtl =>
          intro hbs
          obtain ⟨p2, b2⟩ := hd
          simp only [Expr.sizeBranches]
          rcases List.mem_cons.mp hbs with h | h
          · have hbb : br.2.size = b2.size := by rw [h]
            omega
          · have := ihtl h; omega
      have hble := hb branches hbr
      simp only [Expr.size] at he; omega
    | letRec bindings body =>
      refine Infer.complete_letRec ?_ (ih body (by simp only [Expr.size] at he; omega))
      intro e' hbr
      refine ih e' ?_
      have hb : ∀ (bs : List Expr), e' ∈ bs → e'.size ≤ Expr.sizeRecGroup bs := by
        intro bs
        induction bs with
        | nil => intro hbs; simp at hbs
        | cons hd tl ihtl =>
          intro hbs
          simp only [Expr.sizeRecGroup]
          rcases List.mem_cons.mp hbs with h | h
          · subst h; omega
          · have := ihtl h; omega
      have hble := hb bindings hbr
      simp only [Expr.size] at he; omega
    | letRecAnn schemes bindings body =>
      refine Infer.complete_letRecAnn ?_ (ih body (by simp only [Expr.size] at he; omega))
      intro e' hbr Ys
      refine ih (e'.openTyVars Ys) ?_
      rw [Expr.size_openTyVars]
      have hb : ∀ (bs : List Expr), e' ∈ bs → e'.size ≤ Expr.sizeRecGroup bs := by
        intro bs
        induction bs with
        | nil => intro hbs; simp at hbs
        | cons hd tl ihtl =>
          intro hbs
          simp only [Expr.sizeRecGroup]
          rcases List.mem_cons.mp hbs with h | h
          · subst h; omega
          · have := ihtl h; omega
      have hble := hb bindings hbr
      simp only [Expr.size] at he; omega

/-- **Principality of `Infer`** (Damas–Milner completeness). For any declarative
    typing of `e` under an LC specialization `S₀` of a WF, frontier-bounded
    context, `Infer` succeeds with `(S, τ)` and the declarative typing factors
    through it via an LC residual `R` (`S₀ = R ∘ S` below the frontier,
    `τ₀ = R.onTy τ`). Combined with `Infer.sound`, this is full principality:
    `Infer` computes a most general typing. -/
theorem Infer.complete {Φ : Nat} {ctx : Ctx} {e : Expr} {S₀ : Subst} {τ₀ : Ty} (K : List Nat)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hS₀ : ∀ p ∈ S₀, p.2.IsLC)
    (hKΦ : ∀ k ∈ K, k < Φ) (hKe : ∀ y ∈ e.tyFreeVars, y ∈ K)
    (hKfix : ∀ k ∈ K, S₀.onTy (.fvar k) = .fvar k)
    (hty : TypeOfHM (S₀.onCtx ctx) e τ₀) :
    ∃ Φ' S eOut τ R,
      Infer Φ ctx e Φ' S eOut τ ∧
      Subst.AgreesBelow Φ S₀ (S ++ R) ∧
      τ₀ = R.onTy τ ∧
      (∀ p ∈ R, p.2.IsLC) := by
  obtain ⟨Φ', S, eOut, τ, R, h1, h2, h3, h4, _, _⟩ :=
    Infer.completeAt e K hwf hbelow hS₀ hKΦ hKe hKfix hty
  exact ⟨Φ', S, eOut, τ, R, h1, h2, h3, h4⟩


/-! ### Cleaner corollaries of principality

`Infer.complete` carries two extra side-conclusions beyond `τ₀ = R.onTy τ` — the
agreement clause `S₀ = R ∘ S` (below `Φ`) and `R` locally-closed — purely to
sustain the induction (the `app`/`pair`/`letIn` cases compose specialisations and
reuse `R` as an inner `S₀`). The corollaries below project that engine down to the
parts a caller usually wants. -/

/-- Principality, type-only form: the type of *any* declarative typing is a
    substitution instance of the type `Infer` computes. -/
theorem Infer.complete_instance {Φ : Nat} {ctx : Ctx} {e : Expr} {S₀ : Subst} {τ₀ : Ty}
    (K : List Nat) (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hS₀ : ∀ p ∈ S₀, p.2.IsLC)
    (hKΦ : ∀ k ∈ K, k < Φ) (hKe : ∀ y ∈ e.tyFreeVars, y ∈ K)
    (hKfix : ∀ k ∈ K, S₀.onTy (.fvar k) = .fvar k)
    (hty : TypeOfHM (S₀.onCtx ctx) e τ₀) :
    ∃ Φ' S eOut τ R, Infer Φ ctx e Φ' S eOut τ ∧ τ₀ = Subst.onTy R τ := by
  obtain ⟨Φ', S, eOut, τ, R, hInfer, _, hτeq, _⟩ := Infer.complete K hwf hbelow hS₀ hKΦ hKe hKfix hty
  exact ⟨Φ', S, eOut, τ, R, hInfer, hτeq⟩

/-- Specialised to the identity input substitution: any declarative type of `e`
    in `ctx` itself is an instance of the inferred type. -/
theorem Infer.complete_id {Φ : Nat} {ctx : Ctx} {e : Expr} {τ₀ : Ty}
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hΦe : ∀ y ∈ e.tyFreeVars, y < Φ)
    (hty : TypeOfHM ctx e τ₀) :
    ∃ Φ' S eOut τ R, Infer Φ ctx e Φ' S eOut τ ∧ τ₀ = Subst.onTy R τ := by
  refine Infer.complete_instance e.tyFreeVars hwf hbelow (S₀ := []) (by simp) hΦe
    (fun y hy => hy) (by simp) ?_
  simpa using hty

/-- **Algorithm W decides typeability.** `e` is declaratively typeable under some
    locally-closed specialisation of `ctx` iff `Infer` succeeds on `e` in `ctx`.
    (The specialisation is necessary: `Infer` may instantiate `ctx`'s free type
    variables — rigid in the declarative system — so success does not entail
    typeability of the *unspecialised* `ctx`.) -/
theorem Infer.iff_typeable {Φ : Nat} {ctx : Ctx} {e : Expr}
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hclosed : e.tyFreeVars = []) :
    (∃ (S : Subst) (τ : Ty), (∀ p ∈ S, p.2.IsLC) ∧ TypeOfHM (S.onCtx ctx) e τ)
      ↔ (∃ Φ' S eOut τ, Infer Φ ctx e Φ' S eOut τ) := by
  constructor
  · rintro ⟨S, τ, hS, hty⟩
    obtain ⟨Φ', S', eOut, τ', R, hInfer, _⟩ :=
      Infer.complete_instance [] hwf hbelow hS (by simp) (by rw [hclosed]; simp) (by simp) hty
    exact ⟨Φ', S', eOut, τ', hInfer⟩
  · rintro ⟨Φ', S, eOut, τ, hInfer⟩
    exact ⟨S, τ, (Infer.lc hInfer hwf).2,
      Infer.sourceSound hInfer hwf hbelow [] (by simp) (by rw [hclosed]; simp) (by simp)⟩

/-- **Algorithm W computes principal types** — soundness and principality bundled
    into one statement. From any declarative typing of a core `e` under an LC
    specialization `S₀` of a WF, frontier-bounded `ctx`, `Infer` succeeds with a
    result `(S, τ)` that

      * is itself a valid declarative typing (`TypeOfElabHM (S.onCtx ctx) e τ`), and
      * subsumes the given typing (`τ₀ = R.onTy τ`, i.e. `τ` is more general).

    (The stronger per-output `IsPrincipal ctx e S τ` predicate — "this very `τ`
    subsumes *every* typing" — is proved below via `Infer.complete'` /
    `Infer.output_unique`; see `Infer.isPrincipal`.) -/
theorem Infer.principal {Φ : Nat} {ctx : Ctx} {e : Expr} {S₀ : Subst} {τ₀ : Ty} (K : List Nat)
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hS₀ : ∀ p ∈ S₀, p.2.IsLC)
    (hKΦ : ∀ k ∈ K, k < Φ) (hKe : ∀ y ∈ e.tyFreeVars, y ∈ K)
    (hKfix : ∀ k ∈ K, S₀.onTy (.fvar k) = .fvar k)
    (hty : TypeOfHM (S₀.onCtx ctx) e τ₀) :
    ∃ Φ' S eOut τ R,
      Infer Φ ctx e Φ' S eOut τ ∧ TypeOfHM (S.onCtx ctx) e τ ∧ τ₀ = Subst.onTy R τ := by
  obtain ⟨Φ', S, eOut, τ, R, hInfer, _, hτeq, _, _, hSK⟩ :=
    Infer.completeAt e K hwf hbelow hS₀ hKΦ hKe hKfix hty
  exact ⟨Φ', S, eOut, τ, R, hInfer,
    Infer.sourceSound hInfer hwf hbelow K hKΦ hKe hSK, hτeq⟩

/-! (`UnifyRel.eliminates` / `UnifyRelList.eliminates` relocated to the
    soundness-prerequisites section above, before `Infer.sound`.) -/

/-! ### Termination measure for `unify`

`pairVars`/`listVars` count the distinct free vars of a pair of types / type
lists; these are the primary (lexicographic) component of `unify`'s measure,
with `Ty.size`/`TyList.size` as the tiebreak. The six `unifyDec_*` lemmas package
the decrease of each recursive call. -/

/-- Distinct free type vars of a pair of monotypes. -/
def pairVars (a b : Ty) : List Nat := (a.freeVars ++ b.freeVars).dedup

/-- Distinct free type vars of a pair of monotype lists. -/
def listVars (as bs : List Ty) : List Nat := (TyList.freeVars as ++ TyList.freeVars bs).dedup

theorem mem_pairVars {a b : Ty} {v : Nat} :
    v ∈ pairVars a b ↔ v ∈ a.freeVars ∨ v ∈ b.freeVars := by
  simp [pairVars, List.mem_dedup, List.mem_append]

theorem mem_listVars {as bs : List Ty} {v : Nat} :
    v ∈ listVars as bs ↔ (∃ t ∈ as, v ∈ t.freeVars) ∨ (∃ t ∈ bs, v ∈ t.freeVars) := by
  simp only [listVars, List.mem_dedup, List.mem_append, mem_TyList_freeVars]

theorem pairVars_nodup {a b : Ty} : (pairVars a b).Nodup := List.nodup_dedup _
theorem listVars_nodup {as bs : List Ty} : (listVars as bs).Nodup := List.nodup_dedup _

theorem nodup_length_le {l₁ l₂ : List Nat} (h : l₁.Nodup) (hsub : l₁ ⊆ l₂) :
    l₁.length ≤ l₂.length := (h.subperm hsub).length_le

theorem nodup_length_lt {l₁ l₂ : List Nat} (h : l₁.Nodup) (hsub : l₁ ⊆ l₂)
    {z : Nat} (hz2 : z ∈ l₂) (hz1 : z ∉ l₁) : l₁.length < l₂.length := by
  have hcons : (z :: l₁).Nodup := List.nodup_cons.mpr ⟨hz1, h⟩
  have hsub2 : (z :: l₁) ⊆ l₂ := List.cons_subset.mpr ⟨hz2, hsub⟩
  have := nodup_length_le hcons hsub2
  simpa using this

theorem lexLt_left {a₁ a₂ b₁ b₂ : Nat} (h : a₁ < a₂) :
    Prod.Lex (· < ·) (· < ·) (a₁, b₁) (a₂, b₂) := Prod.Lex.left _ _ h

theorem lexLt_of_le_of_lt {a₁ a₂ b₁ b₂ : Nat} (ha : a₁ ≤ a₂) (hb : b₁ < b₂) :
    Prod.Lex (· < ·) (· < ·) (a₁, b₁) (a₂, b₂) := by
  rcases Nat.lt_or_ge a₁ a₂ with h | h
  · exact Prod.Lex.left _ _ h
  · have heq : a₁ = a₂ := Nat.le_antisymm ha h
    subst heq; exact Prod.Lex.right _ hb

theorem Subst.map_onTy_nil (ts : List Ty) : ts.map (Subst.onTy []) = ts := by
  induction ts with
  | nil => rfl
  | cons hd tl ih => simp only [List.map_cons, Subst.onTy_nil, ih]

/-- First structural subcall of `arrow`: strictly smaller (by size). -/
theorem unifyDec_arrow1 {a₁ a₂ c₁ c₂ : Ty} :
    Prod.Lex (· < ·) (· < ·)
      ((pairVars a₁ c₁).length, a₁.size + c₁.size)
      ((pairVars (.arrow a₁ a₂) (.arrow c₁ c₂)).length,
        (Ty.arrow a₁ a₂).size + (Ty.arrow c₁ c₂).size) := by
  apply lexLt_of_le_of_lt
  · refine nodup_length_le pairVars_nodup (fun v hv => ?_)
    rw [mem_pairVars] at hv ⊢
    rcases hv with h | h
    · exact Or.inl (Ty.mem_freeVars_arrowL h)
    · exact Or.inr (Ty.mem_freeVars_arrowL h)
  · simp only [Ty.size]; have := @Ty.size_pos a₂; have := @Ty.size_pos c₂; omega

/-- Second subcall of `arrow` (after applying the first unifier): strictly fewer
    distinct vars when the unifier is nontrivial, else strictly smaller by size. -/
theorem unifyDec_arrow2 {a₁ a₂ c₁ c₂ : Ty} {S₁ : Subst} (hS₁ : UnifyRel a₁ c₁ S₁) :
    Prod.Lex (· < ·) (· < ·)
      ((pairVars (S₁.onTy a₂) (S₁.onTy c₂)).length,
        (S₁.onTy a₂).size + (S₁.onTy c₂).size)
      ((pairVars (.arrow a₁ a₂) (.arrow c₁ c₂)).length,
        (Ty.arrow a₁ a₂).size + (Ty.arrow c₁ c₂).size) := by
  have hsub : pairVars (S₁.onTy a₂) (S₁.onTy c₂)
      ⊆ pairVars (.arrow a₁ a₂) (.arrow c₁ c₂) := by
    intro v hv
    rw [mem_pairVars] at hv ⊢
    rcases hv with hv | hv
    · rcases Subst.mem_freeVars_onTy hv with h | ⟨q, hq, hvq⟩
      · exact Or.inl (Ty.mem_freeVars_arrowR h)
      · rcases UnifyRel.range_mem hS₁ q hq v hvq with h' | h'
        · exact Or.inl (Ty.mem_freeVars_arrowL h')
        · exact Or.inr (Ty.mem_freeVars_arrowL h')
    · rcases Subst.mem_freeVars_onTy hv with h | ⟨q, hq, hvq⟩
      · exact Or.inr (Ty.mem_freeVars_arrowR h)
      · rcases UnifyRel.range_mem hS₁ q hq v hvq with h' | h'
        · exact Or.inl (Ty.mem_freeVars_arrowL h')
        · exact Or.inr (Ty.mem_freeVars_arrowL h')
  by_cases hnil : S₁ = []
  · subst hnil
    simp only [Subst.onTy_nil] at hsub ⊢
    apply lexLt_of_le_of_lt (nodup_length_le pairVars_nodup hsub)
    simp only [Ty.size]; have := @Ty.size_pos a₁; have := @Ty.size_pos c₁; omega
  · obtain ⟨p, rest, rfl⟩ := List.exists_cons_of_ne_nil hnil
    apply lexLt_left
    refine nodup_length_lt pairVars_nodup hsub (z := p.1) ?_ ?_
    · rw [mem_pairVars]
      rcases UnifyRel.dom_mem hS₁ p List.mem_cons_self with h | h
      · exact Or.inl (Ty.mem_freeVars_arrowL h)
      · exact Or.inr (Ty.mem_freeVars_arrowL h)
    · rw [mem_pairVars]; push_neg
      exact ⟨UnifyRel.eliminates hS₁ p List.mem_cons_self a₂,
             UnifyRel.eliminates hS₁ p List.mem_cons_self c₂⟩

/-- `customTy` subcall delegates to `unifyList` (strictly smaller by size). -/
theorem unifyDec_customTy {n₁ n₂ : TyName} {ts₁ ts₂ : List Ty} :
    Prod.Lex (· < ·) (· < ·)
      ((listVars ts₁ ts₂).length, TyList.size ts₁ + TyList.size ts₂ + 1)
      ((pairVars (.customTy n₁ ts₁) (.customTy n₂ ts₂)).length,
        (Ty.customTy n₁ ts₁).size + (Ty.customTy n₂ ts₂).size) := by
  apply lexLt_of_le_of_lt
  · refine nodup_length_le listVars_nodup (fun v hv => ?_)
    rw [mem_listVars] at hv; rw [mem_pairVars]
    rcases hv with ⟨t, ht, h⟩ | ⟨t, ht, h⟩
    · exact Or.inl (Ty.mem_freeVars_customTy ht h)
    · exact Or.inr (Ty.mem_freeVars_customTy ht h)
  · simp only [Ty.size]; omega

/-- First subcall of `unifyList`'s `cons` (the head element). -/
theorem unifyDec_cons1 {t₁ t₂ : Ty} {ts₁ ts₂ : List Ty} :
    Prod.Lex (· < ·) (· < ·)
      ((pairVars t₁ t₂).length, t₁.size + t₂.size)
      ((listVars (t₁ :: ts₁) (t₂ :: ts₂)).length,
        TyList.size (t₁ :: ts₁) + TyList.size (t₂ :: ts₂) + 1) := by
  apply lexLt_of_le_of_lt
  · refine nodup_length_le pairVars_nodup (fun v hv => ?_)
    rw [mem_pairVars] at hv; rw [mem_listVars]
    rcases hv with h | h
    · exact Or.inl ⟨t₁, List.mem_cons_self, h⟩
    · exact Or.inr ⟨t₂, List.mem_cons_self, h⟩
  · simp only [TyList.size]; omega

/-- Second subcall of `unifyList`'s `cons` (the tail, after the head unifier). -/
theorem unifyDec_cons2 {t₁ t₂ : Ty} {ts₁ ts₂ : List Ty} {S₁ : Subst}
    (hS₁ : UnifyRel t₁ t₂ S₁) :
    Prod.Lex (· < ·) (· < ·)
      ((listVars (ts₁.map S₁.onTy) (ts₂.map S₁.onTy)).length,
        TyList.size (ts₁.map S₁.onTy) + TyList.size (ts₂.map S₁.onTy) + 1)
      ((listVars (t₁ :: ts₁) (t₂ :: ts₂)).length,
        TyList.size (t₁ :: ts₁) + TyList.size (t₂ :: ts₂) + 1) := by
  have hsub : listVars (ts₁.map S₁.onTy) (ts₂.map S₁.onTy)
      ⊆ listVars (t₁ :: ts₁) (t₂ :: ts₂) := by
    intro v hv
    rw [mem_listVars] at hv ⊢
    rcases hv with ⟨t, ht, h⟩ | ⟨t, ht, h⟩
    · obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
      rcases Subst.mem_freeVars_onTy h with hh | ⟨q, hq, hvq⟩
      · exact Or.inl ⟨t0, List.mem_cons_of_mem _ ht0, hh⟩
      · rcases UnifyRel.range_mem hS₁ q hq v hvq with h' | h'
        · exact Or.inl ⟨t₁, List.mem_cons_self, h'⟩
        · exact Or.inr ⟨t₂, List.mem_cons_self, h'⟩
    · obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
      rcases Subst.mem_freeVars_onTy h with hh | ⟨q, hq, hvq⟩
      · exact Or.inr ⟨t0, List.mem_cons_of_mem _ ht0, hh⟩
      · rcases UnifyRel.range_mem hS₁ q hq v hvq with h' | h'
        · exact Or.inl ⟨t₁, List.mem_cons_self, h'⟩
        · exact Or.inr ⟨t₂, List.mem_cons_self, h'⟩
  by_cases hnil : S₁ = []
  · subst hnil
    simp only [Subst.map_onTy_nil] at hsub ⊢
    apply lexLt_of_le_of_lt (nodup_length_le listVars_nodup hsub)
    simp only [TyList.size]; have := @Ty.size_pos t₁; have := @Ty.size_pos t₂; omega
  · obtain ⟨p, rest, rfl⟩ := List.exists_cons_of_ne_nil hnil
    apply lexLt_left
    refine nodup_length_lt listVars_nodup hsub (z := p.1) ?_ ?_
    · rw [mem_listVars]
      rcases UnifyRel.dom_mem hS₁ p List.mem_cons_self with h | h
      · exact Or.inl ⟨t₁, List.mem_cons_self, h⟩
      · exact Or.inr ⟨t₂, List.mem_cons_self, h⟩
    · rw [mem_listVars]; push_neg
      refine ⟨fun t ht hc => ?_, fun t ht hc => ?_⟩
      · obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
        exact UnifyRel.eliminates hS₁ p List.mem_cons_self t0 hc
      · obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
        exact UnifyRel.eliminates hS₁ p List.mem_cons_self t0 hc

/-! ### The `unify` function

`unify` is the plain-`Option Subst` erasure of `unifyCore`, the verified unifier
that returns a substitution *together with* its `UnifyRel` derivation. `unifyCore`
is the `K = []` instance of the rigidity-aware `unifyCoreK` (defined below), so
soundness is immediate (the derivation is carried) and completeness reduces to
`unifyCoreK_complete` at the empty rigid set. -/

mutual
/-- **Rigidity-aware unifier.** Like `unifyCore`, but refuses to bind any variable
    in the rigid set `K`: at a variable step it orients the binding toward the
    non-rigid side, and fails when forced to equate a rigid var with a non-variable
    or with a different rigid var. This is the executable mirror of the relational
    `UnifyRel.complete_K`; the resulting MGU **avoids `K` by construction** (carried
    in the result), so the executable keeps scoped-type-variable skolems rigid —
    which plain left-leaning `unifyCore` does not (it would bind a skolem sitting on
    the left of a flexible unification, spuriously failing the annotated-`let`
    escape check). With `K = []` it coincides with `unifyCore`. -/
def unifyCoreK (K : List Nat) (a b : Ty) :
    Option { S : Subst // UnifyRel a b S ∧ (∀ p ∈ S, p.1 ∉ K) } :=
  match a, b with
  | .prim p, .prim q =>
      if h : p = q then some ⟨[], by subst h; exact .prim, by simp⟩ else none
  | .fvar n, .fvar m =>
      if h : n = m then some ⟨[], by subst h; exact .fvarRefl, by simp⟩
      else if hnK : n ∈ K then
        if hmK : m ∈ K then none
        else some ⟨[(m, .fvar n)],
          .fvarR (by simp only [ne_eq, Ty.fvar.injEq]; omega)
            (by simp only [Ty.freeVars, List.mem_singleton]; omega),
          by intro p hp; rw [List.mem_singleton] at hp; subst hp; exact hmK⟩
      else some ⟨[(n, .fvar m)],
        .fvarL (by simp only [ne_eq, Ty.fvar.injEq]; omega)
          (by simp only [Ty.freeVars, List.mem_singleton]; omega),
        by intro p hp; rw [List.mem_singleton] at hp; subst hp; exact hnK⟩
  | .arrow a₁ a₂, .arrow c₁ c₂ =>
      match unifyCoreK K a₁ c₁ with
      | none => none
      | some ⟨S₁, hS₁, hav₁⟩ =>
        match unifyCoreK K (S₁.onTy a₂) (S₁.onTy c₂) with
        | none => none
        | some ⟨S₂, hS₂, hav₂⟩ => some ⟨S₁ ++ S₂, .arrow hS₁ hS₂, by
            intro p hp; rcases List.mem_append.mp hp with hp | hp
            · exact hav₁ p hp
            · exact hav₂ p hp⟩
  | .customTy n₁ ts₁, .customTy n₂ ts₂ =>
      if h : n₁ = n₂ then
        match unifyListCoreK K ts₁ ts₂ with
        | none => none
        | some ⟨S, hS, hav⟩ => some ⟨S, by subst h; exact .customTy hS, hav⟩
      else none
  | .fvar n, b =>
      if hnK : n ∈ K then none
      else if h : n ∈ b.freeVars then none
      else some ⟨[(n, b)],
        .fvarL (by intro he; subst he; exact h (by simp [Ty.freeVars])) h,
        by intro p hp; rw [List.mem_singleton] at hp; subst hp; exact hnK⟩
  | a, .fvar n =>
      if hnK : n ∈ K then none
      else if h : n ∈ a.freeVars then none
      else some ⟨[(n, a)],
        .fvarR (by intro he; subst he; exact h (by simp [Ty.freeVars])) h,
        by intro p hp; rw [List.mem_singleton] at hp; subst hp; exact hnK⟩
  | _, _ => none
termination_by ((pairVars a b).length, a.size + b.size)
decreasing_by
  · exact unifyDec_arrow1
  · exact unifyDec_arrow2 hS₁
  · exact unifyDec_customTy

def unifyListCoreK (K : List Nat) (as bs : List Ty) :
    Option { S : Subst // UnifyRelList as bs S ∧ (∀ p ∈ S, p.1 ∉ K) } :=
  match as, bs with
  | [], [] => some ⟨[], .nil, by simp⟩
  | t₁ :: ts₁, t₂ :: ts₂ =>
      match unifyCoreK K t₁ t₂ with
      | none => none
      | some ⟨S₁, hS₁, hav₁⟩ =>
        match unifyListCoreK K (ts₁.map S₁.onTy) (ts₂.map S₁.onTy) with
        | none => none
        | some ⟨S₂, hS₂, hav₂⟩ => some ⟨S₁ ++ S₂, .cons hS₁ hS₂, by
            intro p hp; rcases List.mem_append.mp hp with hp | hp
            · exact hav₁ p hp
            · exact hav₂ p hp⟩
  | _, _ => none
termination_by ((listVars as bs).length, TyList.size as + TyList.size bs + 1)
decreasing_by
  · exact unifyDec_cons1
  · exact unifyDec_cons2 hS₁
end

/-- `unifyCore` is the `K = []` instance of the rigidity-aware `unifyCoreK` (no
    variable is off-limits), projected to drop the now-trivial avoids-`[]`
    component of the carried invariant. -/
def unifyCore (a b : Ty) : Option { S : Subst // UnifyRel a b S } :=
  (unifyCoreK [] a b).map (fun r => ⟨r.1, r.2.1⟩)

/-- `unifyListCore` is the `K = []` instance of `unifyListCoreK`. -/
def unifyListCore (as bs : List Ty) : Option { S : Subst // UnifyRelList as bs S } :=
  (unifyListCoreK [] as bs).map (fun r => ⟨r.1, r.2.1⟩)

/-- The executable unifier, refining `UnifyRel`. -/
def unify (a b : Ty) : Option Subst := (unifyCore a b).map (·.1)

/-- `unify` soundness: a returned substitution is a genuine `UnifyRel` unifier
    (immediate — `unifyCore` carries the derivation). -/
theorem unify_sound {a b : Ty} {S : Subst} (h : unify a b = some S) : UnifyRel a b S := by
  rw [unify] at h
  rcases hc : unifyCore a b with _ | ⟨S', hS'⟩ <;> rw [hc] at h
  · exact absurd h (by simp)
  · simp only [Option.map_some, Option.some.injEq] at h
    subst h
    exact hS'



/-! ### The `infer` function (Algorithm W)

`inferCore`/`inferBranchesCore` mirror `Infer`/`InferBranches` exactly, building
the `Infer` derivation alongside the output (soundness by construction);
recursion is structural on the expression / branch list. The `match_` case reads
the type name + arity off the first branch's constructor (branches are nonempty).
The public `infer` erases the derivation. -/
mutual
def inferCore (K : List Nat) (Φ : Nat) (ctx : Ctx) (e : Expr) :
    Option { r : Nat × Subst × Expr × Ty //
      Infer Φ ctx e r.1 r.2.1 r.2.2.1 r.2.2.2 ∧ (∀ p ∈ r.2.1, p.1 ∉ K) } :=
  match e with
  | .primLit .unit => some ⟨(Φ, [], .primLit .unit, .prim .unit), .primLitUnit, by simp⟩
  | .primLit (.int n) => some ⟨(Φ, [], .primLit (.int n), .prim .int), .primLitInt, by simp⟩
  | .primLit (.nat n) => some ⟨(Φ, [], .primLit (.nat n), .prim .nat), .primLitNat, by simp⟩
  -- | .primLit (.bool b) => some ⟨(Φ, [], .primLit (.bool b), .prim .bool), .primLitBool, by simp⟩
  | .primLit (.str s) => some ⟨(Φ, [], .primLit (.str s), .prim .str), .primLitStr, by simp⟩
  | .lambda none body =>
      match inferCore K (Φ + 1) { ctx with env := PolyTy.mkTrivial (.fvar Φ) :: ctx.env } body with
      | none => none
      | some ⟨(Φ', S, bodyOut, τb), hbody, hav⟩ =>
        some ⟨(Φ', S, .lambda none bodyOut, .arrow (S.onTy (.fvar Φ)) τb), .lambda .none hbody, hav⟩
  | .lambda (some T) body =>
      if hT : Ty.bvarsBelow 0 T = true then
        match inferCore K Φ { ctx with env := PolyTy.mkTrivial T :: ctx.env } body with
        | none => none
        | some ⟨(Φ', S, bodyOut, τb), hbody, hav⟩ =>
          some ⟨(Φ', S, .lambda (some T) bodyOut, .arrow (S.onTy T) τb),
            .lambda (.some T ((Ty.bvarsBelow_iff T).mp hT)) hbody, hav⟩
      else none
  | .app f arg =>
      match inferCore K Φ ctx f with
      | none => none
      | some ⟨(Φ₁, S₁, fOut, τf), hf, hav₁⟩ =>
        match inferCore K Φ₁ (S₁.onCtx ctx) arg with
        | none => none
        | some ⟨(Φ₂, S₂, argOut, τa), harg, hav₂⟩ =>
          match unifyCoreK K (S₂.onTy τf) (.arrow τa (.fvar Φ₂)) with
          | none => none
          | some ⟨S₃, h₃, hav₃⟩ =>
            some ⟨(Φ₂ + 1, S₁ ++ S₂ ++ S₃, .app fOut argOut, S₃.onTy (.fvar Φ₂)), .app hf harg h₃, by
              intro p hp; rcases List.mem_append.mp hp with h | h
              · rcases List.mem_append.mp h with h | h
                · exact hav₁ p h
                · exact hav₂ p h
              · exact hav₃ p h⟩
  | .var i _ =>
      match h : ctx.env[i]? with
      | none => none
      | some polyTy =>
        some ⟨(Φ + polyTy.paramCount, [], .var i ((freshVars Φ polyTy.paramCount).map Ty.fvar),
          polyTy.openVars (freshVars Φ polyTy.paramCount)), .var h, by simp⟩
  | .ctor name =>
      match h : LookupList.get? ctx.ctors name with
      | none => none
      | some ctorr =>
        some ⟨(Φ + ctorr.paramCount, [], .ctor name, ctorr.toTy.openVars (freshVars Φ ctorr.paramCount)),
          .ctor h, by simp⟩
  | .letIn ann rhs body =>
      match ann with
      | none =>
        match inferCore K Φ ctx rhs with
        | none => none
        | some ⟨(Φ₁, S₁, rhsOut, τ₁), hrhs, hav₁⟩ =>
        match inferCore K Φ₁
            { (S₁.onCtx ctx) with
              env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env }
            body with
        | none => none
        | some ⟨(Φ₂, S₂, bodyOut, τ₂), hbody, hav₂⟩ =>
          some ⟨(Φ₂, S₁ ++ S₂,
              .letIn (some (genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁))
                ((rhsOut.substTyFvars S₁).closeTyVars (genVars rhs.tyFreeVars (S₁.onCtx ctx).env τ₁)) bodyOut, τ₂),
            .letIn hrhs hbody, by
            intro p hp; rcases List.mem_append.mp hp with h | h
            · exact hav₁ p h
            · exact hav₂ p h⟩
      | some σ =>
        -- Annotated `let`: skolemize `σ` with `Ys = freshVars Φ pc`, infer the
        -- *opened* rhs `rhs.openTyVars Ys` at frontier `Φ + pc` **with `Ys` added to
        -- the rigid set** (so the rigidity-aware `unifyCoreK` keeps the skolems
        -- rigid), unify the result against the skolem opening, escape-check, and
        -- type the body under `σ` at the outer rigid set `K`. `σ` may carry outer
        -- scoped type variables — no closedness requirement. The output closes the
        -- elaborated rhs back over the skolems.
        if hσwf : Ty.bvarsBelow σ.paramCount σ.body = true then
          match inferCore (K ++ freshVars Φ σ.paramCount) (Φ + σ.paramCount) ctx
              (rhs.openTyVars (freshVars Φ σ.paramCount)) with
          | none => none
          | some ⟨(Φ₁, S₁, rhsOut, τ₁), hrhs, hav₁⟩ =>
            match unifyCoreK (K ++ freshVars Φ σ.paramCount) τ₁
                (σ.openVars (freshVars Φ σ.paramCount)) with
            | none => none
            | some ⟨Schk, hSchk, havS⟩ =>
              if hesc1 : (∀ y ∈ freshVars Φ σ.paramCount, y ∉ (S₁ ++ Schk).map Prod.fst) then
                if hesc2 : (∀ y ∈ freshVars Φ σ.paramCount,
                    y ∉ (Schk.onCtx (S₁.onCtx ctx)).env.freeVars) then
                  match inferCore K Φ₁
                      { (Schk.onCtx (S₁.onCtx ctx)) with
                        env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env }
                      body with
                  | none => none
                  | some ⟨(Φ₂, S₂, bodyOut, τ₂), hbody, hav₂⟩ =>
                    some ⟨(Φ₂, S₁ ++ Schk ++ S₂,
                        .letIn (some σ) ((rhsOut.substTyFvars (S₁ ++ Schk)).closeTyVars (freshVars Φ σ.paramCount)) bodyOut, τ₂),
                      .letInAnn (PolyTy.wf_iff_bvarsBelow.mp hσwf) (Nat.le_refl Φ)
                        hrhs hSchk hesc1 hesc2 hbody, by
                      intro p hp; rcases List.mem_append.mp hp with h | h
                      · rcases List.mem_append.mp h with h | h
                        · exact fun hc => hav₁ p h (List.mem_append_left _ hc)
                        · exact fun hc => havS p h (List.mem_append_left _ hc)
                      · exact hav₂ p h⟩
                else none
              else none
        else none
  | .match_ scrut branches =>
      match inferCore K Φ ctx scrut with
      | none => none
      | some ⟨(Φ₁, S₁, scrutOut, τs), hscrut, hav₁⟩ =>
        match hh : branches.head? with
        | none => none
        | some _ =>
          match inferBranchesCore K (Φ₁ + 1) (S₁.onCtx ctx) τs (.fvar Φ₁) branches with
          | none => none
          | some ⟨(Φ₂, S₂, branchesOut), hbranches, hav₂⟩ =>
            some ⟨(Φ₂, S₁ ++ S₂, .match_ scrutOut branchesOut, S₂.onTy (.fvar Φ₁)),
                  .match_ hscrut (by intro hc; rw [hc] at hh; simp at hh) hbranches, by
                  intro p hp; rcases List.mem_append.mp hp with h | h
                  · exact hav₁ p h
                  · exact hav₂ p h⟩
  | .letRec bindings body =>
      match inferRecGroupCore K (Φ + bindings.length)
          { ctx with env := (freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i)) ++ ctx.env }
          bindings ((freshVars Φ bindings.length).map Ty.fvar) with
      | none => none
      | some ⟨(Φ₁, S₁, bindingsOut), hgroup, hav₁⟩ =>
        match inferCore K Φ₁
            { (S₁.onCtx ctx) with
              env := genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
                       ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)))
                     ++ (S₁.onCtx ctx).env }
            body with
        | none => none
        | some ⟨(Φ₂, S₂, bodyOut, τ₂), hbody, hav₂⟩ =>
          some ⟨(Φ₂, S₁ ++ S₂,
              Expr.letRecElab
                (genGroupVars (bindings.flatMap Expr.tyFreeVars) (S₁.onCtx ctx).env
                  ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i))))
                (bindingsOut.map (·.substTyFvars S₁))
                ((freshVars Φ bindings.length).map (fun i => S₁.onTy (Ty.fvar i)))
                bodyOut, τ₂),
            .letRec hgroup hbody, by
            intro p hp; rcases List.mem_append.mp hp with h | h
            · exact hav₁ p h
            · exact hav₂ p h⟩
  | .letRecAnn schemes bindings body =>
      -- Annotated polymorphic recursion: the `schemes` are GIVEN, so this is
      -- check-and-elaborate. Decidably check each scheme is WF (`bvarsBelow`), then
      -- thread the group through `inferRecGroupAnnCore` (per-binding `letInAnn`
      -- mechanics, schemes in scope throughout) and type the body under the schemes.
      if hwf : (∀ σ ∈ schemes, Ty.bvarsBelow σ.paramCount σ.body = true) then
        if hlen : bindings.length = schemes.length then
          match inferRecGroupAnnCore K Φ { ctx with env := schemes ++ ctx.env } bindings schemes with
          | none => none
          | some ⟨(Φ₁, S₁, bindingsOut), hgroup, hav₁⟩ =>
            match inferCore K Φ₁ (S₁.onCtx { ctx with env := schemes ++ ctx.env }) body with
            | none => none
            | some ⟨(Φ₂, S₂, bodyOut, τ₂), hbody, hav₂⟩ =>
              some ⟨(Φ₂, S₁ ++ S₂, .letRecAnn schemes bindingsOut bodyOut, τ₂),
                .letRecAnn (fun σ hσ => PolyTy.wf_iff_bvarsBelow.mp (hwf σ hσ)) hlen hgroup hbody, by
                intro p hp; rcases List.mem_append.mp hp with h | h
                · exact hav₁ p h
                · exact hav₂ p h⟩
        else none
      else none
termination_by e.size
decreasing_by
  all_goals (try simp only [Expr.size, Expr.size_openTyVars]; omega)

def inferBranchesCore (K : List Nat) (Φ : Nat) (ctx : Ctx) (scrutTy : Ty) (ρ : Ty)
    (branches : List (MatchPattern × Expr)) :
    Option { r : Nat × Subst × List (MatchPattern × Expr) //
      InferBranches Φ ctx scrutTy ρ branches r.1 r.2.1 r.2.2 ∧ (∀ p ∈ r.2.1, p.1 ∉ K) } :=
  match branches with
  | [] => some ⟨(Φ, [], []), .nil, by simp⟩
  | (.wildcard, body) :: rest =>
      match inferCore K Φ ctx body with
      | none => none
      | some ⟨(Φ₁, S₁, bodyOut, τb), hbody, hav₁⟩ =>
        match unifyCoreK K τb (S₁.onTy ρ) with
        | none => none
        | some ⟨S₂, huni, hav₂⟩ =>
          match inferBranchesCore K Φ₁ (S₂.onCtx (S₁.onCtx ctx))
              (S₂.onTy (S₁.onTy scrutTy)) (S₂.onTy (S₁.onTy ρ)) rest with
          | none => none
          | some ⟨(Φ₂, S₃, restOut), hrest, hav₃⟩ =>
            some ⟨(Φ₂, S₁ ++ S₂ ++ S₃, (.wildcard, bodyOut) :: restOut),
                .consWild hbody huni hrest, by
              intro p hp; rcases List.mem_append.mp hp with h | h
              · rcases List.mem_append.mp h with h | h
                · exact hav₁ p h
                · exact hav₂ p h
              · exact hav₃ p h⟩
  | (.named c n, body) :: rest =>
      match hget : LookupList.get? ctx.ctors c with
      | none => none
      | some ctorr =>
        if hcont : n = ctorr.contents.length then
          match unifyCoreK K scrutTy
              (.customTy ctorr.tyName ((freshVars Φ ctorr.paramCount).map (Ty.fvar ·))) with
          | none => none
          | some ⟨S₀, huni0, hav0⟩ =>
            match inferCore K (Φ + ctorr.paramCount)
                { (S₀.onCtx ctx) with
                  env := (ctorr.contents.map (Ty.openWith
                      (((freshVars Φ ctorr.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
                    ++ (S₀.onCtx ctx).env }
                body with
            | none => none
            | some ⟨(Φ₁, S₁, bodyOut, τb), hbody, hav₁⟩ =>
              match unifyCoreK K τb (S₁.onTy (S₀.onTy ρ)) with
              | none => none
              | some ⟨S₂, huni, hav₂⟩ =>
                match inferBranchesCore K Φ₁ (S₂.onCtx (S₁.onCtx (S₀.onCtx ctx)))
                    (S₂.onTy (S₁.onTy (S₀.onTy scrutTy))) (S₂.onTy (S₁.onTy (S₀.onTy ρ))) rest with
                | none => none
                | some ⟨(Φ₂, S₃, restOut), hrest, hav₃⟩ =>
                  some ⟨(Φ₂, S₀ ++ S₁ ++ S₂ ++ S₃, (.named c n, bodyOut) :: restOut),
                      .cons hget hcont huni0 hbody huni hrest, by
                    intro p hp
                    rcases List.mem_append.mp hp with h | h
                    · rcases List.mem_append.mp h with h | h
                      · rcases List.mem_append.mp h with h | h
                        · exact hav0 p h
                        · exact hav₁ p h
                      · exact hav₂ p h
                    · exact hav₃ p h⟩
        else none
termination_by Expr.sizeBranches branches
decreasing_by
  all_goals (try simp only [Expr.sizeBranches]; omega)

def inferRecGroupCore (K : List Nat) (Φ : Nat) (ctx : Ctx) (bindings : List Expr) (targets : List Ty) :
    Option { r : Nat × Subst × List Expr //
      InferRecGroup Φ ctx bindings targets r.1 r.2.1 r.2.2 ∧ (∀ p ∈ r.2.1, p.1 ∉ K) } :=
  match bindings, targets with
  | [], [] => some ⟨(Φ, [], []), .nil, by simp⟩
  | e :: rest, β :: βs =>
      match inferCore K Φ ctx e with
      | none => none
      | some ⟨(Φ₁, S₁, eOut, τ), he, hav₁⟩ =>
        match unifyCoreK K τ (S₁.onTy β) with
        | none => none
        | some ⟨S₂, huni, hav₂⟩ =>
          match inferRecGroupCore K Φ₁ (S₂.onCtx (S₁.onCtx ctx)) rest
              (βs.map (fun b => S₂.onTy (S₁.onTy b))) with
          | none => none
          | some ⟨(Φ₂, S₃, restOut), hrest, hav₃⟩ =>
            some ⟨(Φ₂, S₁ ++ S₂ ++ S₃, eOut :: restOut), .cons he huni hrest, by
              intro p hp; rcases List.mem_append.mp hp with h | h
              · rcases List.mem_append.mp h with h | h
                · exact hav₁ p h
                · exact hav₂ p h
              · exact hav₃ p h⟩
  | _, _ => none
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try simp only [Expr.sizeRecGroup]; omega)

/-- Check-and-elaborate an **annotated** recursive group (the executable analogue of
    `inferRecGroupCore` for `letRecAnn`). The `schemes` are RIGID user annotations,
    threaded unchanged; each binding is handled with `letInAnn` mechanics (skolemise,
    infer the opened binding against the opened scheme, escape-check, close back). -/
def inferRecGroupAnnCore (K : List Nat) (Φ : Nat) (ctx : Ctx) (bindings : List Expr)
    (schemes : List PolyTy) :
    Option { r : Nat × Subst × List Expr //
      InferRecGroupAnn Φ ctx bindings schemes r.1 r.2.1 r.2.2 ∧ (∀ p ∈ r.2.1, p.1 ∉ K) } :=
  match bindings, schemes with
  | [], [] => some ⟨(Φ, [], []), .nil, by simp⟩
  | e :: rest, σ :: ss =>
      match inferCore (K ++ freshVars Φ σ.paramCount) (Φ + σ.paramCount) ctx
          (e.openTyVars (freshVars Φ σ.paramCount)) with
      | none => none
      | some ⟨(Φ₁, S₁, eOut, τ), he, hav₁⟩ =>
        match unifyCoreK (K ++ freshVars Φ σ.paramCount) τ
            (σ.openVars (freshVars Φ σ.paramCount)) with
        | none => none
        | some ⟨Schk, hSchk, havS⟩ =>
          if hesc1 : (∀ y ∈ freshVars Φ σ.paramCount, y ∉ (S₁ ++ Schk).map Prod.fst) then
            if hesc2 : (∀ y ∈ freshVars Φ σ.paramCount,
                y ∉ (Schk.onCtx (S₁.onCtx ctx)).env.freeVars) then
              match inferRecGroupAnnCore K Φ₁ (Schk.onCtx (S₁.onCtx ctx)) rest ss with
              | none => none
              | some ⟨(Φ₂, S₂, restOut), hrest, hav₃⟩ =>
                some ⟨(Φ₂, S₁ ++ Schk ++ S₂,
                    ((eOut.substTyFvars (S₁ ++ Schk)).closeTyVars (freshVars Φ σ.paramCount)) :: restOut),
                  .cons (Nat.le_refl Φ) he hSchk hesc1 hesc2 hrest, by
                  intro p hp; rcases List.mem_append.mp hp with h | h
                  · rcases List.mem_append.mp h with h | h
                    · exact fun hc => hav₁ p h (List.mem_append_left _ hc)
                    · exact fun hc => havS p h (List.mem_append_left _ hc)
                  · exact hav₃ p h⟩
            else none
          else none
  | _, _ => none
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end

/-- The executable type inferer, refining `Infer`. Runs from the empty rigid set
    (top-level programs have no in-scope scoped type variables); annotated `let`s
    extend it internally with their skolem block. -/
def infer (Φ : Nat) (ctx : Ctx) (e : Expr) : Option (Nat × Subst × Expr × Ty) :=
  (inferCore [] Φ ctx e).map (·.1)

/-- Lightweight `Repr` for the elaborated output term, so the `#eval` sanity
    checks (which now print a 4-tuple including `eOut`) elaborate. -/
instance : Repr Expr := ⟨fun _ _ => Std.Format.text "‹elaborated-term›"⟩

/-- `infer` soundness: a returned `(Φ', S, eOut, τ)` is a genuine `Infer` derivation
    (immediate — `inferCore` carries it). -/
theorem infer_sound {Φ : Nat} {ctx : Ctx} {e : Expr} {Φ' : Nat} {S : Subst} {eOut : Expr} {τ : Ty}
    (h : infer Φ ctx e = some (Φ', S, eOut, τ)) : Infer Φ ctx e Φ' S eOut τ := by
  rw [infer] at h
  rcases hc : inferCore [] Φ ctx e with _ | ⟨r, hr⟩ <;> rw [hc] at h
  · exact absurd h (by simp)
  · simp only [Option.map_some, Option.some.injEq] at h
    subst h
    exact hr.1

/-! ### Capstone: the executable inferer computes the principal type

Composing `infer_sound` with `Infer.isPrincipal`: whenever `infer` returns a
type, that type is *the principal typing* — every declarative typing of `e`
(under any LC specialization of a well-formed, frontier-bounded `ctx`) is a
substitution instance of it. The executable Algorithm W computes principal
types, machine-verified. -/
theorem infer_isPrincipal {Φ : Nat} {ctx : Ctx} {e : Expr} {Φ' : Nat} {S : Subst} {eOut : Expr} {τ : Ty}
    (h : infer Φ ctx e = some (Φ', S, eOut, τ)) (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx)
    (hclosed : e.tyFreeVars = []) :
    Infer.IsPrincipal ctx e S τ :=
  Infer.isPrincipal (infer_sound h) hwf hbelow hclosed

/-! ### Sanity checks: the algorithm actually runs

The first time the algorithm is executed (guards against an operationally-wrong
but provable relation). `unify` and `infer` reduce only under the compiler
(`#eval`), since both rest on well-founded recursion. -/

-- `unify (α → α) (Int → β) = [α ↦ Int, β ↦ Int]`
#eval unify (.arrow (.fvar 0) (.fvar 0)) (.arrow (.prim .int) (.fvar 1))
-- `unify Int String = none` (constructor clash)
#eval unify (.prim .int) (.prim .str)
-- `unify α (α → α) = none` (occurs check)
#eval unify (.fvar 0) (.arrow (.fvar 0) (.fvar 0))
-- `infer (λx. x) = α → α`
#eval infer 0 { env := [], ctors := [] } (.lambda none (.var 0 []))
-- `infer (λx. λy. x) = α → β → α`
#eval infer 0 { env := [], ctors := [] } (.lambda none (.lambda none (.var 1 [])))
-- `infer ((λx. x) 5) = Int`
#eval infer 0 { env := [], ctors := [] } (.app (.lambda none (.var 0 [])) (.primLit (.int 5)))
-- `infer (5 5) = none` (Int is not a function)
#eval infer 0 { env := [], ctors := [] } (.app (.primLit (.int 5)) (.primLit (.int 5)))


/-- **Rigidity-aware unify completeness.** When the (LC) inputs admit an LC unifier
    `U` that keeps every `k ∈ K` rigid, the rigidity-aware `unifyCoreK K` succeeds.
    Fuses the size induction of `unifyCore_complete_aux` with the orientation
    reasoning of `UnifyRel.complete_K_aux`: at a variable step the `K`-fixing `U`
    rules out the branch that `unifyCoreK` would refuse (binding a rigid var). -/
theorem unifyCoreK_complete_aux {K : List Nat} : ∀ (N : Nat),
    (∀ {a b : Ty} {U : Subst}, 2 * (U.onTy a).size < N → a.IsLC → b.IsLC →
        (∀ p ∈ U, p.2.IsLC) → Unifies U a b → (∀ k ∈ K, U.onTy (.fvar k) = .fvar k) →
        (unifyCoreK K a b).isSome) ∧
    (∀ {as bs : List Ty} {U : Subst}, 2 * TyList.size (as.map U.onTy) + 1 < N →
        (∀ t ∈ as, t.IsLC) → (∀ t ∈ bs, t.IsLC) → (∀ p ∈ U, p.2.IsLC) →
        as.length = bs.length → as.map U.onTy = bs.map U.onTy →
        (∀ k ∈ K, U.onTy (.fvar k) = .fvar k) →
        (unifyListCoreK K as bs).isSome) := by
  intro N
  induction N with
  | zero => exact ⟨fun h => absurd h (by omega), fun h => absurd h (by omega)⟩
  | succ N ih =>
    obtain ⟨ihU, ihL⟩ := ih
    refine ⟨?_, ?_⟩
    · intro a b U hsz ha hb hUlc hU hUK
      cases a with
      | bvar i => cases ha with | bvar h => omega
      | prim p =>
        cases b with
        | bvar i => cases hb with | bvar h => omega
        | prim q => simp only [Unifies, Subst.onTy_prim, Ty.prim.injEq] at hU; subst hU; simp [unifyCoreK]
        | fvar m =>
          have hmK : m ∉ K := fun hmK => by
            have h1 : U.onTy (Ty.prim p) = U.onTy (Ty.fvar m) := hU
            rw [hUK m hmK, Subst.onTy_prim] at h1; exact absurd h1 (by simp)
          simp [unifyCoreK, hmK, Ty.freeVars]
        | arrow b₁ b₂ => simp [Unifies] at hU
        | customTy nm bs => simp [Unifies] at hU
      | fvar n =>
        cases b with
        | bvar i => cases hb with | bvar h => omega
        | fvar m =>
          by_cases hnm : n = m
          · subst hnm; simp [unifyCoreK]
          · by_cases hnK : n ∈ K
            · have hmK : m ∉ K := fun hmK => by
                have h1 : U.onTy (Ty.fvar n) = U.onTy (Ty.fvar m) := hU
                rw [hUK n hnK, hUK m hmK] at h1; simp only [Ty.fvar.injEq] at h1; exact hnm h1
              simp [unifyCoreK, hnm, hnK, hmK]
            · simp [unifyCoreK, hnm, hnK]
        | prim q =>
          by_cases hocc : n ∈ (Ty.prim q).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · by_cases hnK : n ∈ K
            · exfalso
              have h1 : U.onTy (Ty.fvar n) = U.onTy (Ty.prim q) := hU
              rw [hUK n hnK, Subst.onTy_prim] at h1; exact absurd h1 (by simp)
            · simp [unifyCoreK, hnK, hocc]
        | arrow b₁ b₂ =>
          by_cases hocc : n ∈ (Ty.arrow b₁ b₂).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · by_cases hnK : n ∈ K
            · exfalso
              have h1 : U.onTy (Ty.fvar n) = U.onTy (Ty.arrow b₁ b₂) := hU
              rw [hUK n hnK, Subst.onTy_arrow] at h1; exact absurd h1 (by simp)
            · simp [unifyCoreK, hnK, hocc]
        | customTy nm bs =>
          by_cases hocc : n ∈ (Ty.customTy nm bs).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · by_cases hnK : n ∈ K
            · exfalso
              have h1 : U.onTy (Ty.fvar n) = U.onTy (Ty.customTy nm bs) := hU
              rw [hUK n hnK, Subst.onTy_customTy] at h1; exact absurd h1 (by simp)
            · simp [unifyCoreK, hnK, hocc]
      | arrow a₁ a₂ =>
        cases ha with
        | arrow ha₁ ha₂ =>
        cases b with
        | bvar i => cases hb with | bvar h => omega
        | fvar m =>
          by_cases hocc : m ∈ (Ty.arrow a₁ a₂).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · by_cases hmK : m ∈ K
            · exfalso
              have h1 : U.onTy (Ty.arrow a₁ a₂) = U.onTy (Ty.fvar m) := hU
              rw [hUK m hmK, Subst.onTy_arrow] at h1; exact absurd h1 (by simp)
            · simp [unifyCoreK, hmK, hocc]
        | prim q => simp [Unifies] at hU
        | customTy nm bs => simp [Unifies] at hU
        | arrow b₁ b₂ =>
          cases hb with
          | arrow hb₁ hb₂ =>
          have hpsz : (U.onTy (.arrow a₁ a₂)).size = 1 + (U.onTy a₁).size + (U.onTy a₂).size := by
            simp [Subst.onTy_arrow, Ty.size]
          simp only [Unifies, Subst.onTy_arrow, Ty.arrow.injEq] at hU
          obtain ⟨⟨S₁, h₁, _⟩, he1⟩ := Option.isSome_iff_exists.mp
            (ihU (a := a₁) (b := b₁) (U := U) (by rw [hpsz] at hsz; omega) ha₁ hb₁ hUlc hU.1 hUK)
          have hS₁lc := UnifyRel.lc h₁ ha₁ hb₁
          obtain ⟨R, hR, hRlc, hRK⟩ := UnifyRel.greatest_K h₁ U hUlc hU.1 hUK
          obtain ⟨⟨S₂, h₂, _⟩, he2⟩ := Option.isSome_iff_exists.mp
            (ihU (a := S₁.onTy a₂) (b := S₁.onTy b₂) (U := R) (by rw [← hR a₂]; rw [hpsz] at hsz; omega)
              (Subst.onTy_lc hS₁lc ha₂) (Subst.onTy_lc hS₁lc hb₂) hRlc
              (by show R.onTy (S₁.onTy a₂) = R.onTy (S₁.onTy b₂); rw [← hR a₂, ← hR b₂]; exact hU.2) hRK)
          rw [unifyCoreK]; simp only [he1, he2, Option.isSome_some]
      | customTy nm tys₁ =>
        cases b with
        | bvar i => cases hb with | bvar h => omega
        | fvar m =>
          by_cases hocc : m ∈ (Ty.customTy nm tys₁).freeVars
          · have hlt := Ty.size_onTy_fvar_lt (S := U) hocc (by simp)
            simp only [Unifies] at hU; rw [hU] at hlt; omega
          · by_cases hmK : m ∈ K
            · exfalso
              have h1 : U.onTy (Ty.customTy nm tys₁) = U.onTy (Ty.fvar m) := hU
              rw [hUK m hmK, Subst.onTy_customTy] at h1; exact absurd h1 (by simp)
            · simp [unifyCoreK, hmK, hocc]
        | prim q => simp [Unifies] at hU
        | arrow b₁ b₂ => simp [Unifies] at hU
        | customTy nm' tys₂ =>
          have hcsz : (U.onTy (.customTy nm tys₁)).size = 1 + TyList.size (tys₁.map U.onTy) := by
            simp [Subst.onTy_customTy, Ty.size]
          simp only [Unifies, Subst.onTy_customTy, Ty.customTy.injEq] at hU
          obtain ⟨rfl, hmapeq⟩ := hU
          have hlen : tys₁.length = tys₂.length := by
            have := congrArg List.length hmapeq; simpa using this
          obtain ⟨⟨S, hS, _⟩, heL⟩ := Option.isSome_iff_exists.mp
            (ihL (as := tys₁) (bs := tys₂) (U := U) (by rw [hcsz] at hsz; omega)
              (fun t ht => by cases ha with | customTy h => exact h t ht)
              (fun t ht => by cases hb with | customTy h => exact h t ht)
              hUlc hlen hmapeq hUK)
          rw [unifyCoreK]; simp [heL]
    · intro as bs U hsz has hbs hUlc hlen hmap hUK
      cases as with
      | nil =>
        cases bs with
        | nil => simp [unifyListCoreK]
        | cons t₂ ts₂ => simp at hlen
      | cons t₁ ts₁ =>
        cases bs with
        | nil => simp at hlen
        | cons t₂ ts₂ =>
          simp only [List.map_cons, List.cons.injEq] at hmap
          have htsz : TyList.size ((t₁ :: ts₁).map U.onTy)
              = (U.onTy t₁).size + TyList.size (ts₁.map U.onTy) := by simp [List.map_cons, TyList.size]
          have ht1pos := @Ty.size_pos (U.onTy t₁)
          obtain ⟨⟨S₁, h₁, _⟩, he1⟩ := Option.isSome_iff_exists.mp
            (ihU (a := t₁) (b := t₂) (U := U) (by rw [htsz] at hsz; omega)
              (has t₁ List.mem_cons_self) (hbs t₂ List.mem_cons_self) hUlc hmap.1 hUK)
          have hS₁lc := UnifyRel.lc h₁ (has t₁ List.mem_cons_self) (hbs t₂ List.mem_cons_self)
          obtain ⟨R, hR, hRlc, hRK⟩ := UnifyRel.greatest_K h₁ U hUlc hmap.1 hUK
          have key : ∀ l : List Ty, l.map (R.onTy ∘ S₁.onTy) = l.map U.onTy := by
            intro l; apply List.map_congr_left; intro t _; exact (hR t).symm
          have hkey1 : (ts₁.map S₁.onTy).map R.onTy = ts₁.map U.onTy := by
            rw [List.map_map]; exact key ts₁
          have hmaptail : (ts₁.map S₁.onTy).map R.onTy = (ts₂.map S₁.onTy).map R.onTy := by
            rw [List.map_map, List.map_map, key, key]; exact hmap.2
          obtain ⟨⟨S₂, h₂, _⟩, he2⟩ := Option.isSome_iff_exists.mp
            (ihL (as := ts₁.map S₁.onTy) (bs := ts₂.map S₁.onTy) (U := R)
              (by rw [hkey1]; rw [htsz] at hsz; omega)
              (by intro t ht; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
                  exact Subst.onTy_lc hS₁lc (has t0 (List.mem_cons_of_mem _ ht0)))
              (by intro t ht; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp ht
                  exact Subst.onTy_lc hS₁lc (hbs t0 (List.mem_cons_of_mem _ ht0)))
              hRlc (by simp only [List.length_map]; have := hlen; simpa using this) hmaptail hRK)
          rw [unifyListCoreK]; simp only [he1, he2, Option.isSome_some]

/-- `unifyCoreK` completeness wrapper. -/
theorem unifyCoreK_complete {K : List Nat} {a b : Ty} {U : Subst}
    (ha : a.IsLC) (hb : b.IsLC) (hUlc : ∀ p ∈ U, p.2.IsLC)
    (hU : Unifies U a b) (hUK : ∀ k ∈ K, U.onTy (.fvar k) = .fvar k) :
    (unifyCoreK K a b).isSome :=
  (unifyCoreK_complete_aux (2 * (U.onTy a).size + 1)).1 (by omega) ha hb hUlc hU hUK

/-- `unify` completeness: `unify` succeeds whenever the (LC) inputs are unifiable.
    Reduces to `unifyCoreK_complete` at the empty rigid set — the MGU `S` is LC by
    `UnifyRel.lc`, and the empty-`K` fixing condition is vacuous. -/
theorem unify_complete {a b : Ty} {S : Subst} (h : UnifyRel a b S)
    (ha : a.IsLC) (hb : b.IsLC) : (unify a b).isSome := by
  have hcore : (unifyCore a b).isSome := by
    have := unifyCoreK_complete (K := []) ha hb (UnifyRel.lc h ha hb) h.unifies (by simp)
    simpa [unifyCore, Option.isSome_map] using this
  simpa [unify, Option.isSome_map] using hcore


/-! ### `infer` completeness

`infer` succeeds whenever `e` is declaratively typeable. Like `unify_complete`,
the obstacle is that `inferCore` decomposes through its *own* computed
substitutions/fresh-vars, so this is Algorithm-W completeness *for the function*:
each recursive case transfers the typing to the function's intermediate state
(reusing `Infer.complete'` / `InferBranches.complete'`), and `app`/`fst`/`snd`/
`match_` additionally rebuild the explicit unifier (mirroring the `complete_*_aux`
dodges) to discharge the rigidity-aware `unifyCoreK` via `unifyCoreK_complete`. -/

/-! ### Gap-avoidance (domain/range locality of `Infer` substitutions)

The executable annotated-`let` arm hard-codes its skolem block `Ys = freshVars Φ pc`
(`N = Φ`), while the relation `Infer.letInAnn` permits any `N ≥ Φ`. Bridging the two
in `inferCore_complete` requires that a *given* derivation's output substitution `S`
leaves `Ys` rigid — concretely that `S`'s domain and range avoid the half-open
interval the block occupies. The driver is a structural locality fact: a derivation
inferred from a context/annotations that avoid an interval `[lo, hi)` (with the gap
*below* the input frontier) produces a substitution (domain **and** range) and a
result type that also avoid `[lo, hi)`. Threading the interval through the
`letInAnn` sub-derivations (rhs at frontier `N+pc`, body at `Φ₁`, plus the `Schk`
unifier) — combined with the rule's own escape checks — yields exactly the
`Ys`-rigidity the executable bridge needs. -/

/-- A whole substitution applied to an interval-avoiding type yields an
    interval-avoiding type (the `[lo, hi)` analogue of `Subst.onTy_belowFvars`). -/
theorem Subst.onTy_avoidsItv {lo hi : Nat} :
    ∀ {S : Subst}, (∀ p ∈ S, ∀ v ∈ p.2.freeVars, v < lo ∨ hi ≤ v) →
    ∀ {τ : Ty}, (∀ v ∈ τ.freeVars, v < lo ∨ hi ≤ v) →
    ∀ v ∈ (S.onTy τ).freeVars, v < lo ∨ hi ≤ v := by
  intro S
  induction S with
  | nil => intro _ τ hτ v hv; rw [Subst.onTy_nil] at hv; exact hτ v hv
  | cons hd S' ih =>
    obtain ⟨Z, U⟩ := hd
    intro hS τ hτ v hv
    rw [show ((Z, U) :: S') = [(Z, U)] ++ S' from rfl, Subst.onTy_append] at hv
    refine ih (fun p hp => hS p (List.mem_cons_of_mem _ hp)) ?_ v hv
    intro w hw
    rcases Ty.mem_freeVars_substFvar hw with h | h
    · exact hτ w h
    · exact hS (Z, U) (List.mem_cons_self ..) w h

/-- A whole substitution applied to an interval-avoiding context env stays
    interval-avoiding. -/
theorem Subst.onCtx_avoidsItv {lo hi : Nat} {S : Subst} {ctx : Ctx}
    (hS : ∀ p ∈ S, ∀ v ∈ p.2.freeVars, v < lo ∨ hi ≤ v)
    (hctx : ∀ M ∈ ctx.env, ∀ v ∈ M.body.freeVars, v < lo ∨ hi ≤ v) :
    ∀ M ∈ (S.onCtx ctx).env, ∀ v ∈ M.body.freeVars, v < lo ∨ hi ≤ v := by
  intro M hM
  simp only [Subst.onCtx, Subst.onEnv] at hM
  obtain ⟨M0, hM0, rfl⟩ := List.mem_map.mp hM
  simp only [Subst.onPolyTy]
  exact Subst.onTy_avoidsItv hS (hctx M0 hM0)

/-- Opening with interval-avoiding type args preserves interval-avoidance. -/
theorem Ty.openWith_avoidsItv {lo hi : Nat} {Vs : List Ty}
    (hVs : ∀ t ∈ Vs, ∀ v ∈ t.freeVars, v < lo ∨ hi ≤ v) :
    ∀ {X : Ty}, (∀ v ∈ X.freeVars, v < lo ∨ hi ≤ v) →
    ∀ v ∈ (Ty.openWith Vs X).freeVars, v < lo ∨ hi ≤ v := by
  intro X
  induction X using Ty.rec_strong with
  | prim p => intro _ v hv; simp [Ty.openWith, Ty.instantiate, Ty.freeVars] at hv
  | fvar n =>
    intro hX v hv
    simp only [Ty.openWith, Ty.instantiate, Ty.freeVars, List.mem_singleton] at hv
    rw [hv]; exact hX n (by simp [Ty.freeVars])
  | bvar i =>
    intro _ v hv
    simp only [Ty.openWith, Ty.instantiate] at hv
    cases h : Vs[i]? with
    | none => rw [h] at hv; simp [Ty.freeVars] at hv
    | some t =>
      rw [h] at hv; simp only [Option.getD_some] at hv
      exact hVs t (List.mem_of_getElem? h) v hv
  | arrow a b iha ihb =>
    intro hX v hv
    simp only [Ty.openWith, Ty.instantiate, Ty.freeVars, List.mem_dedup, List.mem_append] at hv
    rcases hv with hv | hv
    · exact iha (fun w hw => hX w (by simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inl hw)) v hv
    · exact ihb (fun w hw => hX w (by simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inr hw)) v hv
  | customTy nm tys ih =>
    intro hX v hv
    simp only [Ty.openWith, Ty.instantiate, TyList.instantiate_eq_map, Ty.freeVars] at hv
    obtain ⟨t', ht', hvt'⟩ := TyList.mem_freeVars_iff.mp hv
    obtain ⟨t, ht, rfl⟩ := List.mem_map.mp ht'
    exact ih t ht (fun w hw => hX w (by rw [Ty.freeVars]; exact TyList.mem_freeVars_of_mem ht hw)) v hvt'

mutual
/-- Unifying interval-avoiding monotypes yields an interval-avoiding substitution
    (both domain and range avoid `[lo, hi)`). -/
theorem UnifyRel.gap_avoid {lo hi : Nat} : {a b : Ty} → {S : Subst} → UnifyRel a b S →
    (∀ v ∈ a.freeVars, v < lo ∨ hi ≤ v) → (∀ v ∈ b.freeVars, v < lo ∨ hi ≤ v) →
    (∀ p ∈ S, p.1 < lo ∨ hi ≤ p.1) ∧ (∀ p ∈ S, ∀ v ∈ p.2.freeVars, v < lo ∨ hi ≤ v)
  | _, _, _, .prim, _, _ => ⟨by simp, by simp⟩
  | _, _, _, .fvarRefl, _, _ => ⟨by simp, by simp⟩
  | _, _, _, @UnifyRel.fvarL n τ _ _, ha, hb => by
    refine ⟨?_, ?_⟩
    · intro p hp; rw [List.mem_singleton] at hp; subst hp; exact ha n (by simp [Ty.freeVars])
    · intro p hp v hv; rw [List.mem_singleton] at hp; subst hp; exact hb v hv
  | _, _, _, @UnifyRel.fvarR n τ _ _, ha, hb => by
    refine ⟨?_, ?_⟩
    · intro p hp; rw [List.mem_singleton] at hp; subst hp; exact hb n (by simp [Ty.freeVars])
    · intro p hp v hv; rw [List.mem_singleton] at hp; subst hp; exact ha v hv
  | _, _, _, @UnifyRel.arrow a b c d S₁ S₂ h₁ h₂, ha, hb => by
    have haa : ∀ v ∈ a.freeVars, v < lo ∨ hi ≤ v := fun v hv =>
      ha v (by simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inl hv)
    have hab : ∀ v ∈ b.freeVars, v < lo ∨ hi ≤ v := fun v hv =>
      ha v (by simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inr hv)
    have hbc : ∀ v ∈ c.freeVars, v < lo ∨ hi ≤ v := fun v hv =>
      hb v (by simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inl hv)
    have hbd : ∀ v ∈ d.freeVars, v < lo ∨ hi ≤ v := fun v hv =>
      hb v (by simp only [Ty.freeVars, List.mem_dedup, List.mem_append]; exact .inr hv)
    obtain ⟨hd₁, hr₁⟩ := UnifyRel.gap_avoid h₁ haa hbc
    obtain ⟨hd₂, hr₂⟩ := UnifyRel.gap_avoid h₂ (Subst.onTy_avoidsItv hr₁ hab) (Subst.onTy_avoidsItv hr₁ hbd)
    refine ⟨?_, ?_⟩
    · intro p hp; rw [List.mem_append] at hp; rcases hp with hp | hp
      · exact hd₁ p hp
      · exact hd₂ p hp
    · intro p hp; rw [List.mem_append] at hp; rcases hp with hp | hp
      · exact hr₁ p hp
      · exact hr₂ p hp
  | _, _, _, @UnifyRel.customTy nm tys₁ tys₂ S hl, ha, hb => by
    exact UnifyRelList.gap_avoid hl
      (fun t ht v hv => ha v (by rw [Ty.freeVars]; exact TyList.mem_freeVars_of_mem ht hv))
      (fun t ht v hv => hb v (by rw [Ty.freeVars]; exact TyList.mem_freeVars_of_mem ht hv))

theorem UnifyRelList.gap_avoid {lo hi : Nat} : {as bs : List Ty} → {S : Subst} → UnifyRelList as bs S →
    (∀ t ∈ as, ∀ v ∈ t.freeVars, v < lo ∨ hi ≤ v) → (∀ t ∈ bs, ∀ v ∈ t.freeVars, v < lo ∨ hi ≤ v) →
    (∀ p ∈ S, p.1 < lo ∨ hi ≤ p.1) ∧ (∀ p ∈ S, ∀ v ∈ p.2.freeVars, v < lo ∨ hi ≤ v)
  | _, _, _, .nil, _, _ => ⟨by simp, by simp⟩
  | _, _, _, @UnifyRelList.cons t₁ t₂ ts₁ ts₂ S₁ S₂ h₁ ht, ha, hb => by
    obtain ⟨hd₁, hr₁⟩ := UnifyRel.gap_avoid h₁ (ha t₁ List.mem_cons_self) (hb t₂ List.mem_cons_self)
    have hmap_a : ∀ t ∈ ts₁.map S₁.onTy, ∀ v ∈ t.freeVars, v < lo ∨ hi ≤ v := by
      intro t htm; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp htm
      exact Subst.onTy_avoidsItv hr₁ (ha t0 (List.mem_cons_of_mem _ ht0))
    have hmap_b : ∀ t ∈ ts₂.map S₁.onTy, ∀ v ∈ t.freeVars, v < lo ∨ hi ≤ v := by
      intro t htm; obtain ⟨t0, ht0, rfl⟩ := List.mem_map.mp htm
      exact Subst.onTy_avoidsItv hr₁ (hb t0 (List.mem_cons_of_mem _ ht0))
    obtain ⟨hd₂, hr₂⟩ := UnifyRelList.gap_avoid ht hmap_a hmap_b
    refine ⟨?_, ?_⟩
    · intro p hp; rw [List.mem_append] at hp; rcases hp with hp | hp
      · exact hd₁ p hp
      · exact hd₂ p hp
    · intro p hp; rw [List.mem_append] at hp; rcases hp with hp | hp
      · exact hr₁ p hp
      · exact hr₂ p hp
end

mutual
/-- **Gap-avoidance locality.** If a derivation's context env and the term's
    annotation free vars all avoid an interval `[lo, hi)` whose top `hi` is at most
    the input frontier `Φ`, then the inferred type and the output substitution's
    domain **and** range all avoid `[lo, hi)`. (The `[lo, hi)` analogue of
    `Infer.belowFvars`, but tracking the substitution *domain* too — `belowFvars`
    only bounds the range from above.) -/
theorem Infer.gap_avoid {lo hi : Nat} {Φ ctx e Φ' S eOut τ} (h : Infer Φ ctx e Φ' S eOut τ) :
    hi ≤ Φ → (∀ M ∈ ctx.env, ∀ v ∈ M.body.freeVars, v < lo ∨ hi ≤ v) →
    (∀ y ∈ e.tyFreeVars, y < lo ∨ hi ≤ y) →
    (∀ v ∈ τ.freeVars, v < lo ∨ hi ≤ v) ∧ (∀ p ∈ S, p.1 < lo ∨ hi ≤ p.1) ∧
      (∀ p ∈ S, ∀ v ∈ p.2.freeVars, v < lo ∨ hi ≤ v) := by
  cases h with
  | primLitUnit => intro _ _ _; refine ⟨?_, by simp, by simp⟩; intro v hv; simp [Ty.freeVars] at hv
  | primLitInt => intro _ _ _; refine ⟨?_, by simp, by simp⟩; intro v hv; simp [Ty.freeVars] at hv
  | primLitNat => intro _ _ _; refine ⟨?_, by simp, by simp⟩; intro v hv; simp [Ty.freeVars] at hv
  -- | primLitBool => intro _ _ _; refine ⟨?_, by simp, by simp⟩; intro v hv; simp [Ty.freeVars] at hv
  | primLitStr => intro _ _ _; refine ⟨?_, by simp, by simp⟩; intro v hv; simp [Ty.freeVars] at hv
  | lambda hseed hbody =>
    intro hhi hctx htfv
    cases hseed with
    | none =>
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append] at htfv
      have hctx' : ∀ M ∈ ({ ctx with env := PolyTy.mkTrivial (.fvar Φ) :: ctx.env }).env,
          ∀ v ∈ M.body.freeVars, v < lo ∨ hi ≤ v := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact fun v hv => Or.inr (by have hvΦ : v = Φ := List.mem_singleton.mp hv; subst hvΦ; exact hhi)
        · exact hctx M hM
      obtain ⟨hbτ, hbD, hbR⟩ := Infer.gap_avoid hbody (by omega) hctx' htfv
      refine ⟨?_, hbD, hbR⟩
      intro v hv; simp only [Ty.freeVars, List.mem_dedup, List.mem_append] at hv
      rcases hv with hv | hv
      · exact Subst.onTy_avoidsItv hbR
          (fun w hw => by simp only [Ty.freeVars, List.mem_singleton] at hw; subst hw; exact Or.inr hhi) v hv
      · exact hbτ v hv
    | some _ hlc =>
      expose_names
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at htfv
      have hTavoid : ∀ v ∈ paramTy.freeVars, v < lo ∨ hi ≤ v := fun v hv => htfv v (.inl hv)
      have hctx' : ∀ M ∈ ({ ctx with env := PolyTy.mkTrivial paramTy :: ctx.env }).env,
          ∀ v ∈ M.body.freeVars, v < lo ∨ hi ≤ v := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact hTavoid
        · exact hctx M hM
      obtain ⟨hbτ, hbD, hbR⟩ := Infer.gap_avoid hbody hhi hctx' (fun y hy => htfv y (.inr hy))
      refine ⟨?_, hbD, hbR⟩
      intro v hv; simp only [Ty.freeVars, List.mem_dedup, List.mem_append] at hv
      rcases hv with hv | hv
      · exact Subst.onTy_avoidsItv hbR hTavoid v hv
      · exact hbτ v hv
  | @app _ _ _ _ Φ₁ Φ₂ S₁ S₂ S₃ _ _ τf τa hf harg huni =>
    intro hhi hctx htfv
    simp only [Expr.tyFreeVars, List.mem_append] at htfv
    obtain ⟨hfτ, hfD, hfR⟩ := Infer.gap_avoid hf hhi hctx (fun y hy => htfv y (.inl hy))
    have hle1 := Infer.frontier_le hf
    obtain ⟨hargτ, hargD, hargR⟩ := Infer.gap_avoid harg (by omega) (Subst.onCtx_avoidsItv hfR hctx)
      (fun y hy => htfv y (.inr hy))
    have hle2 := Infer.frontier_le harg
    have hinR : ∀ v ∈ (Ty.arrow τa (Ty.fvar Φ₂)).freeVars, v < lo ∨ hi ≤ v := by
      intro v hv; simp only [Ty.freeVars, List.mem_dedup, List.mem_append] at hv
      rcases hv with hv | hv
      · exact hargτ v hv
      · simp only [List.mem_singleton] at hv; subst hv; exact Or.inr (by omega)
    obtain ⟨h3D, h3R⟩ := UnifyRel.gap_avoid huni (Subst.onTy_avoidsItv hargR hfτ) hinR
    refine ⟨?_, ?_, ?_⟩
    · exact Subst.onTy_avoidsItv h3R
        (fun v hv => by simp only [Ty.freeVars, List.mem_singleton] at hv; subst hv; exact Or.inr (by omega))
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hfD p hp
      · exact hargD p hp
      · exact h3D p hp
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hfR p hp
      · exact hargR p hp
      · exact h3R p hp
  | @var _ _ _ _ polyTy hlook =>
    intro hhi hctx _
    refine ⟨?_, by simp, by simp⟩
    intro v hv
    rcases Ty.freeVars_openVars_subset v hv with h | h
    · exact hctx polyTy (List.mem_of_getElem? hlook) v h
    · exact Or.inr (by have := freshVars_ge v h; omega)
  | @ctor _ _ _ ctorr hlook =>
    intro hhi _ _
    refine ⟨?_, by simp, by simp⟩
    intro v hv
    rcases Ty.freeVars_openVars_subset v hv with h | h
    · exact absurd h (NoFreeVars.not_mem_freeVars (Ctor.toTy_body_noFreeVars ctorr) v)
    · exact Or.inr (by have := freshVars_ge v h; omega)
  | @letIn _ _ rhs body Φ₁ Φ₂ S₁ S₂ _ _ τ₁ τ₂ hrhs hbody =>
    intro hhi hctx htfv
    simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append] at htfv
    obtain ⟨hrτ, hrD, hrR⟩ := Infer.gap_avoid hrhs hhi hctx (fun y hy => htfv y (.inl hy))
    have hle1 := Infer.frontier_le hrhs
    have hctx' : ∀ M ∈ ({ (S₁.onCtx ctx) with
        env := genScheme rhs.tyFreeVars (S₁.onCtx ctx).env τ₁ :: (S₁.onCtx ctx).env }).env,
        ∀ v ∈ M.body.freeVars, v < lo ∨ hi ≤ v := by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact fun v hv => hrτ v (Ty.closeOver_freeVars_subset hv)
      · exact Subst.onCtx_avoidsItv hrR hctx M hM
    obtain ⟨hbτ, hbD, hbR⟩ := Infer.gap_avoid hbody (by omega) hctx' (fun y hy => htfv y (.inr hy))
    refine ⟨hbτ, ?_, ?_⟩
    · intro p hp; rw [List.mem_append] at hp; rcases hp with hp | hp
      · exact hrD p hp
      · exact hbD p hp
    · intro p hp; rw [List.mem_append] at hp; rcases hp with hp | hp
      · exact hrR p hp
      · exact hbR p hp
  | @letInAnn _ N _ σ rhs body Φ₁ Φ₂ S₁ Schk S₂ _ _ τ₁ τ₂ hσwf hΦN hrhs huni _hesc1 _hesc2 hbody =>
    intro hhi hctx htfv
    simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at htfv
    have hle1 := Infer.frontier_le hrhs
    have hrhsTfv : ∀ y ∈ (rhs.openTyVars (freshVars N σ.paramCount)).tyFreeVars, y < lo ∨ hi ≤ y := by
      intro y hy
      rcases Expr.tyFreeVars_openTyVars hy with hh | hh
      · exact htfv y (.inl (.inr hh))
      · exact Or.inr (by have := freshVars_ge y hh; omega)
    obtain ⟨hrτ, hrD, hrR⟩ := Infer.gap_avoid hrhs (by omega) hctx hrhsTfv
    have hσopenAvoid : ∀ v ∈ (σ.openVars (freshVars N σ.paramCount)).freeVars, v < lo ∨ hi ≤ v := by
      intro v hv
      rcases Ty.freeVars_openVars_subset v hv with hh | hh
      · exact htfv v (.inl (.inl hh))
      · exact Or.inr (by have := freshVars_ge v hh; omega)
    obtain ⟨hSchkD, hSchkR⟩ := UnifyRel.gap_avoid huni hrτ hσopenAvoid
    have hctx2 := Subst.onCtx_avoidsItv hSchkR (Subst.onCtx_avoidsItv hrR hctx)
    have hctx' : ∀ M ∈ ({ (Schk.onCtx (S₁.onCtx ctx)) with
        env := σ :: (Schk.onCtx (S₁.onCtx ctx)).env }).env, ∀ v ∈ M.body.freeVars, v < lo ∨ hi ≤ v := by
      intro M hM; rcases List.mem_cons.mp hM with rfl | hM
      · exact fun v hv => htfv v (.inl (.inl hv))
      · exact hctx2 M hM
    obtain ⟨hbτ, hbD, hbR⟩ := Infer.gap_avoid hbody (by omega) hctx' (fun y hy => htfv y (.inr hy))
    refine ⟨hbτ, ?_, ?_⟩
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hrD p hp
      · exact hSchkD p hp
      · exact hbD p hp
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hrR p hp
      · exact hSchkR p hp
      · exact hbR p hp
  | @match_ _ _ _ _ Φ₁ Φ₂ S₁ S₂ _ _ τs hscrut hne hbr =>
    intro hhi hctx htfv
    simp only [Expr.tyFreeVars, List.mem_append] at htfv
    obtain ⟨hsτ, hsD, hsR⟩ := Infer.gap_avoid hscrut hhi hctx (fun y hy => htfv y (.inl hy))
    have hle1 := Infer.frontier_le hscrut
    obtain ⟨hρτ, h3D, h3R⟩ := InferBranches.gap_avoid hbr (by omega)
      (Subst.onCtx_avoidsItv hsR hctx) hsτ
      (fun w hw => by simp only [Ty.freeVars, List.mem_singleton] at hw; subst hw; exact Or.inr (by omega))
      (fun y hy => htfv y (.inr hy))
    refine ⟨hρτ, ?_, ?_⟩
    · intro p hp; rw [List.mem_append] at hp
      rcases hp with hp | hp
      · exact hsD p hp
      · exact h3D p hp
    · intro p hp; rw [List.mem_append] at hp
      rcases hp with hp | hp
      · exact hsR p hp
      · exact h3R p hp
  | letRec hgroup hbody =>
    intro hhi hctx htfv
    simp only [Expr.tyFreeVars, List.mem_append] at htfv
    have hgle := InferRecGroup.frontier_le hgroup
    obtain ⟨hgD, hgR⟩ := InferRecGroup.gap_avoid hgroup (by omega)
      (by
        intro M hM v hv; rcases List.mem_append.mp hM with hM | hM
        · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
          simp only [PolyTy.mkTrivial, Ty.freeVars, List.mem_singleton] at hv
          exact Or.inr (by have := freshVars_ge x hx; omega)
        · exact hctx M hM v hv)
      (by
        intro β hβ v hv; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hβ
        simp only [Ty.freeVars, List.mem_singleton] at hv
        exact Or.inr (by have := freshVars_ge x hx; omega))
      (fun y hy => htfv y (.inl hy))
    obtain ⟨hbτ, hbD, hbR⟩ := Infer.gap_avoid hbody (by omega)
      (by
        intro M hM v hv; rcases List.mem_append.mp hM with hM | hM
        · simp only [genGroupSchemes] at hM
          obtain ⟨τ, hτ, rfl⟩ := List.mem_map.mp hM
          obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ
          simp only [PolyTy.genGroup] at hv
          exact Subst.onTy_avoidsItv hgR
            (fun w hw => by
              simp only [Ty.freeVars, List.mem_singleton] at hw
              exact Or.inr (by have := freshVars_ge x hx; omega)) v
            (Ty.closeOver_freeVars_subset hv)
        · exact Subst.onCtx_avoidsItv hgR hctx M hM v hv)
      (fun y hy => htfv y (.inr hy))
    refine ⟨hbτ, ?_, ?_⟩
    · intro p hp; rw [List.mem_append] at hp
      rcases hp with hp | hp
      · exact hgD p hp
      · exact hbD p hp
    · intro p hp; rw [List.mem_append] at hp
      rcases hp with hp | hp
      · exact hgR p hp
      · exact hbR p hp
  | @letRecAnn _ _ schemes bindings body _ _ S₁ S₂ _ _ τ₂ hwf' hlen hgroup hbody =>
    intro hhi hctx htfv
    simp only [Expr.tyFreeVars, List.mem_append] at htfv
    have hgle := InferRecGroupAnn.frontier_le hgroup
    have hctxS : ∀ M ∈ ({ ctx with env := schemes ++ ctx.env }).env,
        ∀ v ∈ M.body.freeVars, v < lo ∨ hi ≤ v := by
      intro M hM v hv
      rcases List.mem_append.mp hM with hM | hM
      · exact htfv v (.inl (.inl (Expr.mem_tyFreeVars_schemeList hM hv)))
      · exact hctx M hM v hv
    obtain ⟨hgD, hgR⟩ := InferRecGroupAnn.gap_avoid hgroup (by omega) hctxS
      (fun σ hσ v hv => htfv v (.inl (.inl (Expr.mem_tyFreeVars_schemeList hσ hv))))
      (fun y hy => htfv y (.inl (.inr hy)))
    obtain ⟨hbτ, hbD, hbR⟩ := Infer.gap_avoid hbody (by omega)
      (Subst.onCtx_avoidsItv hgR hctxS) (fun y hy => htfv y (.inr hy))
    refine ⟨hbτ, ?_, ?_⟩
    · intro p hp; rw [List.mem_append] at hp
      rcases hp with hp | hp
      · exact hgD p hp
      · exact hbD p hp
    · intro p hp; rw [List.mem_append] at hp
      rcases hp with hp | hp
      · exact hgR p hp
      · exact hbR p hp
termination_by e.size
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.size, Expr.size_openTyVars]; omega)

theorem InferBranches.gap_avoid {lo hi : Nat} {Φ ctx scrutTy ρ brs Φ' S brsOut}
    (h : InferBranches Φ ctx scrutTy ρ brs Φ' S brsOut) :
    hi ≤ Φ → (∀ M ∈ ctx.env, ∀ v ∈ M.body.freeVars, v < lo ∨ hi ≤ v) →
    (∀ v ∈ scrutTy.freeVars, v < lo ∨ hi ≤ v) → (∀ v ∈ ρ.freeVars, v < lo ∨ hi ≤ v) →
    (∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars brs, y < lo ∨ hi ≤ y) →
    (∀ v ∈ (S.onTy ρ).freeVars, v < lo ∨ hi ≤ v) ∧ (∀ p ∈ S, p.1 < lo ∨ hi ≤ p.1) ∧
      (∀ p ∈ S, ∀ v ∈ p.2.freeVars, v < lo ∨ hi ≤ v) := by
  cases h with
  | nil =>
    intro _ _ _ hρ _
    exact ⟨by rw [Subst.onTy_nil]; exact hρ, by simp, by simp⟩
  | @cons _ _ _ _ _ _ body rest ctorr Φ₁ Φ₂ S₀ S₁ S₂ S₃ _ _ τb hlook hn huni0 hbody huni hrest =>
    intro hhi hctx hscrutTy hρ htfv
    simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append] at htfv
    have hcustomAvoid : ∀ v ∈ (Ty.customTy ctorr.tyName
        ((freshVars Φ ctorr.paramCount).map (Ty.fvar ·))).freeVars, v < lo ∨ hi ≤ v := by
      intro v hv
      rw [Ty.freeVars] at hv
      obtain ⟨t, ht, hvt⟩ := TyList.mem_freeVars_iff.mp hv
      obtain ⟨x, hx, hxeq⟩ := List.mem_map.mp ht
      rw [← hxeq] at hvt
      simp only [Ty.freeVars, List.mem_singleton] at hvt
      exact Or.inr (by have := freshVars_ge x hx; omega)
    obtain ⟨h0D, h0R⟩ := UnifyRel.gap_avoid huni0 hscrutTy hcustomAvoid
    have hta₀ : ∀ t ∈ ((freshVars Φ ctorr.paramCount).map (Ty.fvar ·)).map S₀.onTy,
        ∀ v ∈ t.freeVars, v < lo ∨ hi ≤ v := by
      intro t ht v hv
      obtain ⟨s, hs, rfl⟩ := List.mem_map.mp ht
      obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hs
      exact Subst.onTy_avoidsItv h0R (fun w hw => by
        simp only [Ty.freeVars, List.mem_singleton] at hw
        exact Or.inr (by have := freshVars_ge x hx; omega)) v hv
    have hbind : ∀ M ∈ ({ S₀.onCtx ctx with
        env := (ctorr.contents.map (Ty.openWith
            (((freshVars Φ ctorr.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
          ++ (S₀.onCtx ctx).env }).env,
        ∀ v ∈ M.body.freeVars, v < lo ∨ hi ≤ v := by
      intro M hM
      rw [List.mem_append] at hM
      rcases hM with hM | hM
      · obtain ⟨t, ht, rfl⟩ := List.mem_map.mp hM
        obtain ⟨c, hc, rfl⟩ := List.mem_map.mp ht
        exact Ty.openWith_avoidsItv hta₀
          (fun v hv => absurd hv (NoFreeVars.not_mem_freeVars (ctorr.closed c hc) v))
      · exact (Subst.onCtx_avoidsItv h0R hctx) M hM
    obtain ⟨hτb, hbD, hbR⟩ := Infer.gap_avoid hbody (by omega) hbind (fun y hy => htfv y (.inl hy))
    have hle1 := Infer.frontier_le hbody
    have hS₁S₀ρ : ∀ v ∈ (S₁.onTy (S₀.onTy ρ)).freeVars, v < lo ∨ hi ≤ v :=
      Subst.onTy_avoidsItv hbR (Subst.onTy_avoidsItv h0R hρ)
    obtain ⟨h2D, h2R⟩ := UnifyRel.gap_avoid huni hτb hS₁S₀ρ
    have hscrut' : ∀ v ∈ (S₂.onTy (S₁.onTy (S₀.onTy scrutTy))).freeVars, v < lo ∨ hi ≤ v :=
      Subst.onTy_avoidsItv h2R (Subst.onTy_avoidsItv hbR (Subst.onTy_avoidsItv h0R hscrutTy))
    obtain ⟨hresτ, h3D, h3R⟩ := InferBranches.gap_avoid hrest (by omega)
      (Subst.onCtx_avoidsItv h2R (Subst.onCtx_avoidsItv hbR (Subst.onCtx_avoidsItv h0R hctx)))
      hscrut' (Subst.onTy_avoidsItv h2R hS₁S₀ρ)
      (fun y hy => htfv y (.inr hy))
    refine ⟨?_, ?_, ?_⟩
    · rw [Subst.onTy_append, Subst.onTy_append, Subst.onTy_append]; exact hresτ
    · intro p hp; rw [List.mem_append, List.mem_append, List.mem_append] at hp
      rcases hp with ((hp | hp) | hp) | hp
      · exact h0D p hp
      · exact hbD p hp
      · exact h2D p hp
      · exact h3D p hp
    · intro p hp; rw [List.mem_append, List.mem_append, List.mem_append] at hp
      rcases hp with ((hp | hp) | hp) | hp
      · exact h0R p hp
      · exact hbR p hp
      · exact h2R p hp
      · exact h3R p hp
  | @consWild _ _ _ _ _ _ _ _ S₁ S₂ _ _ _ _ hbody huni hrest =>
    intro hhi hctx hscrutTy hρ htfv
    simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append] at htfv
    obtain ⟨hτb, hbD, hbR⟩ := Infer.gap_avoid hbody hhi hctx (fun y hy => htfv y (.inl hy))
    have hle1 := Infer.frontier_le hbody
    have hS₁ρ : ∀ v ∈ (S₁.onTy ρ).freeVars, v < lo ∨ hi ≤ v := Subst.onTy_avoidsItv hbR hρ
    obtain ⟨h2D, h2R⟩ := UnifyRel.gap_avoid huni hτb hS₁ρ
    have hscrut' : ∀ v ∈ (S₂.onTy (S₁.onTy scrutTy)).freeVars, v < lo ∨ hi ≤ v :=
      Subst.onTy_avoidsItv h2R (Subst.onTy_avoidsItv hbR hscrutTy)
    obtain ⟨hresτ, h3D, h3R⟩ := InferBranches.gap_avoid hrest (by omega)
      (Subst.onCtx_avoidsItv h2R (Subst.onCtx_avoidsItv hbR hctx)) hscrut' (Subst.onTy_avoidsItv h2R hS₁ρ)
      (fun y hy => htfv y (.inr hy))
    refine ⟨?_, ?_, ?_⟩
    · rw [Subst.onTy_append, Subst.onTy_append]; exact hresτ
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hbD p hp
      · exact h2D p hp
      · exact h3D p hp
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hbR p hp
      · exact h2R p hp
      · exact h3R p hp
termination_by Expr.sizeBranches brs
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeBranches]; omega)
theorem InferRecGroup.gap_avoid {lo hi : Nat} {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut) :
    hi ≤ Φ → (∀ M ∈ ctx.env, ∀ v ∈ M.body.freeVars, v < lo ∨ hi ≤ v) →
    (∀ β ∈ targets, ∀ v ∈ β.freeVars, v < lo ∨ hi ≤ v) →
    (∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < lo ∨ hi ≤ y) →
    (∀ p ∈ S, p.1 < lo ∨ hi ≤ p.1) ∧ (∀ p ∈ S, ∀ v ∈ p.2.freeVars, v < lo ∨ hi ≤ v) := by
  cases h with
  | nil => intro _ _ _ _; exact ⟨by simp, by simp⟩
  | cons he huni hrest =>
    intro hhi hctx htargets htfv
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at htfv
    obtain ⟨hτ, hD₁, hR₁⟩ := Infer.gap_avoid he hhi hctx (fun y hy => htfv y (.inl hy))
    have hle1 := Infer.frontier_le he
    obtain ⟨hD₂, hR₂⟩ := UnifyRel.gap_avoid huni hτ
      (Subst.onTy_avoidsItv hR₁ (htargets _ (List.mem_cons_self ..)))
    obtain ⟨hD₃, hR₃⟩ := InferRecGroup.gap_avoid hrest (by omega)
      (Subst.onCtx_avoidsItv hR₂ (Subst.onCtx_avoidsItv hR₁ hctx))
      (by
        intro β' hβ' v hv
        obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hβ'
        exact Subst.onTy_avoidsItv hR₂
          (Subst.onTy_avoidsItv hR₁ (htargets b (List.mem_cons_of_mem _ hb))) v hv)
      (fun y hy => htfv y (.inr hy))
    refine ⟨?_, ?_⟩
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hD₁ p hp
      · exact hD₂ p hp
      · exact hD₃ p hp
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hR₁ p hp
      · exact hR₂ p hp
      · exact hR₃ p hp
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup]; omega)

/-- Gap-avoidance for an annotated recursion group (analogue of
    `InferRecGroup.gap_avoid`). The per-binding skolem block `freshVars N pc`
    (`N ≥ Φ ≥ hi`) is entirely `≥ hi`, so the skolem-opened rhs and scheme opening
    avoid `[lo, hi)`; then thread. -/
theorem InferRecGroupAnn.gap_avoid {lo hi : Nat} {Φ ctx bindings schemes Φ' S bindingsOut}
    (h : InferRecGroupAnn Φ ctx bindings schemes Φ' S bindingsOut) :
    hi ≤ Φ → (∀ M ∈ ctx.env, ∀ v ∈ M.body.freeVars, v < lo ∨ hi ≤ v) →
    (∀ σ ∈ schemes, ∀ v ∈ σ.body.freeVars, v < lo ∨ hi ≤ v) →
    (∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y < lo ∨ hi ≤ y) →
    (∀ p ∈ S, p.1 < lo ∨ hi ≤ p.1) ∧ (∀ p ∈ S, ∀ v ∈ p.2.freeVars, v < lo ∨ hi ≤ v) := by
  cases h with
  | nil => intro _ _ _ _; exact ⟨by simp, by simp⟩
  | cons hΦN he huni hesc1 hesc2 hrest =>
    intro hhi hctx hschemes htfv
    expose_names
    simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append] at htfv
    have hle1 := Infer.frontier_le he
    have hrhsTfv : ∀ y ∈ (e.openTyVars (freshVars N σ.paramCount)).tyFreeVars, y < lo ∨ hi ≤ y := by
      intro y hy
      rcases Expr.tyFreeVars_openTyVars hy with hh | hh
      · exact htfv y (.inl hh)
      · exact Or.inr (by have := freshVars_ge y hh; omega)
    obtain ⟨hrτ, hrD, hrR⟩ := Infer.gap_avoid he (by omega) hctx hrhsTfv
    have hσopenAvoid : ∀ v ∈ (σ.openVars (freshVars N σ.paramCount)).freeVars, v < lo ∨ hi ≤ v := by
      intro v hv
      rcases Ty.freeVars_openVars_subset v hv with hh | hh
      · exact hschemes σ List.mem_cons_self v hh
      · exact Or.inr (by have := freshVars_ge v hh; omega)
    obtain ⟨hSchkD, hSchkR⟩ := UnifyRel.gap_avoid huni hrτ hσopenAvoid
    obtain ⟨hD₃, hR₃⟩ := InferRecGroupAnn.gap_avoid hrest (by omega)
      (Subst.onCtx_avoidsItv hSchkR (Subst.onCtx_avoidsItv hrR hctx))
      (fun σ' hσ' => hschemes σ' (List.mem_cons_of_mem _ hσ'))
      (fun y hy => htfv y (.inr hy))
    refine ⟨?_, ?_⟩
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hrD p hp
      · exact hSchkD p hp
      · exact hD₃ p hp
    · intro p hp; rw [List.mem_append, List.mem_append] at hp
      rcases hp with (hp | hp) | hp
      · exact hrR p hp
      · exact hSchkR p hp
      · exact hR₃ p hp
termination_by Expr.sizeRecGroup bindings
decreasing_by
  all_goals (try subst_vars; try simp only [Expr.sizeRecGroup, Expr.size_openTyVars]; omega)
end

/-- A substitution whose domain avoids `K` fixes every `fvar k` with `k ∈ K`. -/
theorem Subst.fixes_fvar_of_avoids {S : Subst} {K : List Nat}
    (hSK : ∀ p ∈ S, p.1 ∉ K) {k : Nat} (hk : k ∈ K) : S.onTy (.fvar k) = .fvar k :=
  Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
    simp only [Ty.freeVars, List.mem_singleton] at hc; subst hc; exact hSK p hp hk)

/-- **The annotated-`let` block-freshness bridge.** A given `letInAnn` derivation
    leaves the *executable's* skolem block `freshVars Φ pc` rigid: its output
    substitution `S` neither binds (domain) nor leaks (range, via `S.onCtx ctx`)
    any name of that block. This is exactly what `inferCore_complete`'s annotated-
    `let` arm needs to re-infer the opened rhs at the rigid set `K ∪ freshVars Φ pc`.

    The derivation's *own* block `freshVars N pc` (`N ≥ Φ`) need not coincide with
    `freshVars Φ pc`; the proof splits the latter at `N`: the lower part `[Φ, N)` is
    a gap below every allocation (discharged by `Infer.gap_avoid` on each
    sub-derivation), while the upper part `[N, Φ+pc) ⊆ freshVars N pc` is exactly the
    derivation's own skolems (discharged by the rule's escape premises `hesc1`/
    `hesc2` for the rhs/unify part, and `gap_avoid` again for the body part once its
    context is shown block-free). -/
theorem Infer.letInAnn_block_fresh {Φ : Nat} {ctx : Ctx} {σ : PolyTy} {rhs body : Expr}
    {Φ' : Nat} {S : Subst} {eOut : Expr} {τ : Ty}
    (h : Infer Φ ctx (.letIn (some σ) rhs body) Φ' S eOut τ)
    (hbelow : CtxBelow Φ ctx)
    (htfv : ∀ y ∈ (Expr.letIn (some σ) rhs body).tyFreeVars, y < Φ) :
    (∀ y ∈ freshVars Φ σ.paramCount, y ∉ S.map Prod.fst) ∧
    (∀ y ∈ freshVars Φ σ.paramCount, y ∉ (S.onCtx ctx).env.freeVars) := by
  cases h with
  | @letInAnn _ N _ _ _ _ Φ₁ Φ₂ S₁ Schk S₂ _ _ τ₁ τ₂ hσwf hΦN hrhs huni hesc1 hesc2 hbody =>
    simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at htfv
    have hctxGap : ∀ M ∈ ctx.env, ∀ v ∈ M.body.freeVars, v < Φ ∨ N ≤ v :=
      fun M hM v hv => Or.inl ((hbelow M hM).mem_lt v hv)
    have hrhsTfvGap : ∀ y ∈ (rhs.openTyVars (freshVars N σ.paramCount)).tyFreeVars, y < Φ ∨ N ≤ y := by
      intro y hy
      rcases Expr.tyFreeVars_openTyVars hy with hh | hh
      · exact Or.inl (htfv y (.inl (.inr hh)))
      · exact Or.inr (freshVars_ge y hh)
    obtain ⟨hr_τ, hr_D, hr_R⟩ := Infer.gap_avoid hrhs (by omega) hctxGap hrhsTfvGap
    have hσopenGap : ∀ v ∈ (σ.openVars (freshVars N σ.paramCount)).freeVars, v < Φ ∨ N ≤ v := by
      intro v hv
      rcases Ty.freeVars_openVars_subset v hv with hh | hh
      · exact Or.inl (htfv v (.inl (.inl hh)))
      · exact Or.inr (freshVars_ge v hh)
    obtain ⟨hSchk_D, hSchk_R⟩ := UnifyRel.gap_avoid huni hr_τ hσopenGap
    -- The body's threaded context avoids the whole block `[Φ, Φ+pc)`.
    have hbodyEnvGap : ∀ M ∈ (Schk.onCtx (S₁.onCtx ctx)).env, ∀ v ∈ M.body.freeVars,
        v < Φ ∨ Φ + σ.paramCount ≤ v := by
      intro M hM v hv
      rcases Subst.onCtx_avoidsItv hSchk_R (Subst.onCtx_avoidsItv hr_R hctxGap) M hM v hv with hlt | hge
      · exact Or.inl hlt
      · by_cases hvlt : v < Φ + σ.paramCount
        · exact absurd (Env.mem_freeVars_iff.mpr ⟨M, hM, hv⟩) (hesc2 v (by
            simp only [freshVars, List.mem_map, List.mem_range]; exact ⟨v - N, by omega, by omega⟩))
        · exact Or.inr (by omega)
    have hle1 := Infer.frontier_le hrhs
    obtain ⟨hb_τ, hb_D, hb_R⟩ := Infer.gap_avoid hbody (show Φ + σ.paramCount ≤ Φ₁ by omega)
      (by intro M hM v hv
          rcases List.mem_cons.mp hM with rfl | hM
          · exact Or.inl (htfv v (.inl (.inl hv)))
          · exact hbodyEnvGap M hM v hv)
      (fun y hy => Or.inl (htfv y (.inr hy)))
    refine ⟨?_, ?_⟩
    · intro y hy hmem
      have hyge := freshVars_ge y hy
      have hylt := freshVars_lt y hy
      rw [List.map_append, List.map_append, List.mem_append, List.mem_append] at hmem
      rcases hmem with (hmem | hmem) | hmem
      · obtain ⟨p, hp, hpy⟩ := List.mem_map.mp hmem
        by_cases hyN : y < N
        · rcases hr_D p hp with hh | hh <;> rw [hpy] at hh <;> omega
        · exact hesc1 y (by simp only [freshVars, List.mem_map, List.mem_range]; exact ⟨y - N, by omega, by omega⟩)
            (List.mem_map.mpr ⟨p, List.mem_append_left _ hp, hpy⟩)
      · obtain ⟨p, hp, hpy⟩ := List.mem_map.mp hmem
        by_cases hyN : y < N
        · rcases hSchk_D p hp with hh | hh <;> rw [hpy] at hh <;> omega
        · exact hesc1 y (by simp only [freshVars, List.mem_map, List.mem_range]; exact ⟨y - N, by omega, by omega⟩)
            (List.mem_map.mpr ⟨p, List.mem_append_right _ hp, hpy⟩)
      · obtain ⟨p, hp, hpy⟩ := List.mem_map.mp hmem
        rcases hb_D p hp with hh | hh <;> rw [hpy] at hh <;> omega
    · intro y hy hmem
      have hyge := freshVars_ge y hy
      have hylt := freshVars_lt y hy
      have hSenv : ∀ M ∈ ((S₁ ++ Schk ++ S₂).onCtx ctx).env, ∀ v ∈ M.body.freeVars,
          v < Φ ∨ Φ + σ.paramCount ≤ v := by
        rw [Subst.onCtx_append, Subst.onCtx_append]
        exact Subst.onCtx_avoidsItv hb_R hbodyEnvGap
      obtain ⟨M, hM, hyM⟩ := Env.mem_freeVars_iff.mp hmem
      rcases hSenv M hM y hyM with hh | hh <;> omega

/-- The function-completeness property at `e`: given a (well-formed,
    frontier-bounded) `Infer` derivation, `inferCore` succeeds. We induct over a
    *given* derivation (not declarative typeability) so the binder cases compose
    without an α-renaming dodge: `Infer`'s sub-derivations already live in the
    exact intermediate states `inferCore` recurses into for `lambda`/`var`/`ctor`,
    while the compositional cases re-derive the second sub-problem under the
    function's own prefix via `complete'` + `Infer.complete`. -/
def InferCoreComplete (e : Expr) : Prop :=
  ∀ {Φ : Nat} {ctx : Ctx} {Φ' : Nat} {S : Subst} {eOut : Expr} {τ : Ty} (K : List Nat),
    CtxWF ctx → CtxBelow Φ ctx → (∀ k ∈ K, k < Φ) → (∀ y ∈ e.tyFreeVars, y ∈ K) →
    (∀ p ∈ S, p.1 ∉ K) → Infer Φ ctx e Φ' S eOut τ → (inferCore K Φ ctx e).isSome

theorem inferCore_complete_prim {p : PrimLitExpr} : InferCoreComplete (.primLit p) := by
  intro Φ ctx Φ' S eOut τ K _ _ _ _ _ h
  cases h <;> simp [inferCore]

theorem inferCore_complete_var {i : Nat} {tyArgs : List Ty} : InferCoreComplete (.var i tyArgs) := by
  intro Φ ctx Φ' S eOut τ K _ _ _ _ _ h
  cases h with
  | var hlook =>
    rw [inferCore]
    split
    · rename_i heq; rw [heq] at hlook; simp at hlook
    · rfl

theorem inferCore_complete_ctor {name : CtorName} : InferCoreComplete (.ctor name) := by
  intro Φ ctx Φ' S eOut τ K _ _ _ _ _ h
  cases h with
  | ctor hlook =>
    rw [inferCore]
    split
    · rename_i heq; rw [heq] at hlook; simp at hlook
    · rfl

/-- A trivial scheme `mkTrivial (.fvar Φ)` extends a WF context to a WF context. -/
theorem CtxWF.cons_fvar {Φ : Nat} {ctx : Ctx} (hwf : CtxWF ctx) :
    CtxWF { ctx with env := PolyTy.mkTrivial (.fvar Φ) :: ctx.env } := by
  intro M hM
  rcases List.mem_cons.mp hM with rfl | hM
  · exact ContainsBvarsUpTo.fvar
  · exact hwf M hM

theorem CtxBelow.cons_fvar {Φ : Nat} {ctx : Ctx} (hbelow : CtxBelow Φ ctx) :
    CtxBelow (Φ + 1) { ctx with env := PolyTy.mkTrivial (.fvar Φ) :: ctx.env } := by
  intro M hM
  rcases List.mem_cons.mp hM with rfl | hM
  · exact .fvar (by omega)
  · exact (hbelow M hM).mono (by omega)

theorem inferCore_complete_lambda {ann : Option Ty} {body : Expr} (ih : InferCoreComplete body) :
    InferCoreComplete (.lambda ann body) := by
  intro Φ ctx Φ' S eOut τ K hwf hbelow hKΦ hKe hSK h
  cases h with
  | lambda hseed hbody =>
    cases hseed
    case none =>
      simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append] at hKe
      have hsome := ih K hwf.cons_fvar hbelow.cons_fvar
        (fun k hk => by have := hKΦ k hk; omega) hKe hSK hbody
      rw [inferCore]
      split
      · rename_i heq; rw [heq] at hsome; simp at hsome
      · rfl
    case some hcl =>
      expose_names
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at hKe
      have hwf' : CtxWF { ctx with env := PolyTy.mkTrivial paramTy :: ctx.env } := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact hcl
        · exact hwf M hM
      have hbelow' : CtxBelow Φ { ctx with env := PolyTy.mkTrivial paramTy :: ctx.env } := by
        intro M hM; rcases List.mem_cons.mp hM with rfl | hM
        · exact Ty.BelowFvars.of_freeVars_lt (fun v hv => hKΦ v (hKe v (.inl hv)))
        · exact hbelow M hM
      have hsome := ih K hwf' hbelow' hKΦ (fun y hy => hKe y (.inr hy)) hSK hbody
      rw [inferCore, dif_pos ((Ty.bvarsBelow_iff paramTy).mpr hcl)]
      split
      · rename_i heq; rw [heq] at hsome; simp at hsome
      · rfl

/-- The explicit unifier behind `app`'s `unify` step (factored from
    `Infer.complete_app_aux`): with `R₂` factoring the residual and a fresh `W`,
    `[(Φ₂, fvar W)] ++ R₂ ++ [(W, τ₀)]` unifies `A` with `arrow τa (fvar Φ₂)`. -/
theorem exists_app_unifier {A τa τ₀ argTy : Ty} {Φ₂ : Nat} {R₂ : Subst} {K : List Nat}
    (hP : R₂.onTy A = Ty.arrow argTy τ₀) (htya : argTy = R₂.onTy τa)
    (hΦ₂A : Φ₂ ∉ A.freeVars) (hΦ₂τa : Φ₂ ∉ τa.freeVars)
    (hR₂ : ∀ p ∈ R₂, p.2.IsLC) (hτ₀LC : τ₀.IsLC)
    (hR₂K : ∀ k ∈ K, R₂.onTy (.fvar k) = .fvar k) (hΦ₂K : ∀ k ∈ K, k < Φ₂) :
    ∃ U, Unifies U A (Ty.arrow τa (Ty.fvar Φ₂)) ∧ (∀ p ∈ U, p.2.IsLC) ∧
      (∀ k ∈ K, U.onTy (.fvar k) = .fvar k) := by
  obtain ⟨W, hWge, hWfresh⟩ := exists_fresh_block
    (R₂.map Prod.fst ++ R₂.flatMap (fun p => p.2.freeVars) ++ argTy.freeVars ++ τ₀.freeVars) Φ₂ 1
  have hWdom : ∀ p ∈ R₂, p.1 ≠ W := by
    intro p hp he
    have := hWfresh p.1 (List.mem_append_left _ (List.mem_append_left _
      (List.mem_append_left _ (List.mem_map.mpr ⟨p, hp, rfl⟩))))
    omega
  have hWrange : ∀ p ∈ R₂, W ∉ p.2.freeVars := by
    intro p hp hc
    have := hWfresh W (List.mem_append_left _ (List.mem_append_left _
      (List.mem_append_right _ (List.mem_flatMap.mpr ⟨p, hp, hc⟩))))
    omega
  have hWargTy : W ∉ argTy.freeVars := fun hc => by
    have := hWfresh W (List.mem_append_left _ (List.mem_append_right _ hc)); omega
  have hWτ₀ : W ∉ τ₀.freeVars := fun hc => by
    have := hWfresh W (List.mem_append_right _ hc); omega
  have hR₂Wfvar : R₂.onTy (Ty.fvar W) = Ty.fvar W := by
    apply Ty.substFvars_eq_self_of_no_key
    intro p hp hc
    simp only [Ty.freeVars, List.mem_singleton] at hc
    exact hWdom p hp hc
  obtain ⟨U, hUdef⟩ : ∃ U : Subst, U = [(Φ₂, Ty.fvar W)] ++ R₂ ++ [(W, τ₀)] := ⟨_, rfl⟩
  have hsingle : ∀ (Z : Nat) (V y : Ty), Subst.onTy [(Z, V)] y = Ty.substFvar Z V y :=
    fun _ _ _ => rfl
  have hsubArrow : ∀ (Z : Nat) (V a b : Ty),
      Ty.substFvar Z V (Ty.arrow a b) = Ty.arrow (Ty.substFvar Z V a) (Ty.substFvar Z V b) :=
    fun _ _ _ _ => rfl
  have hUonTy : ∀ x, U.onTy x = Ty.substFvar W τ₀ (R₂.onTy (Ty.substFvar Φ₂ (Ty.fvar W) x)) := by
    intro x
    rw [hUdef, Subst.onTy_append, Subst.onTy_append, hsingle, hsingle]
  have e1 : Ty.substFvar Φ₂ (Ty.fvar W) (Ty.fvar Φ₂) = Ty.fvar W := by simp [Ty.substFvar]
  have e2 : Ty.substFvar W τ₀ (Ty.fvar W) = τ₀ := by simp [Ty.substFvar]
  have hUniL : U.onTy A = Ty.arrow argTy τ₀ := by
    rw [hUonTy, Ty.substFvar_fresh hΦ₂A, hP, hsubArrow,
        Ty.substFvar_fresh hWargTy, Ty.substFvar_fresh hWτ₀]
  have hUniR : U.onTy (Ty.arrow τa (Ty.fvar Φ₂)) = Ty.arrow argTy τ₀ := by
    rw [hUonTy, hsubArrow, Ty.substFvar_fresh hΦ₂τa, e1, Subst.onTy_arrow,
        hR₂Wfvar, ← htya, hsubArrow, Ty.substFvar_fresh hWargTy, e2]
  refine ⟨U, by show U.onTy A = U.onTy (Ty.arrow τa (Ty.fvar Φ₂)); rw [hUniL, hUniR], ?_, ?_⟩
  · rw [hUdef]
    intro p hp
    rcases List.mem_append.mp hp with hp' | hp'
    · rcases List.mem_append.mp hp' with hp'' | hp''
      · obtain rfl := List.mem_singleton.mp hp''
        exact ContainsBvarsUpTo.fvar
      · exact hR₂ p hp''
    · obtain rfl := List.mem_singleton.mp hp'
      exact hτ₀LC
  · intro k hk
    rw [hUonTy, Ty.substFvar_fresh (show Φ₂ ∉ (Ty.fvar k).freeVars by
          simp only [Ty.freeVars, List.mem_singleton]; have := hΦ₂K k hk; omega), hR₂K k hk]
    exact Ty.substFvar_fresh (show W ∉ (Ty.fvar k).freeVars by
          simp only [Ty.freeVars, List.mem_singleton]; have := hWge; have := hΦ₂K k hk; omega)

theorem inferCore_complete_app {f arg : Expr}
    (ihf : InferCoreComplete f) (iharg : InferCoreComplete arg) :
    InferCoreComplete (.app f arg) := by
  intro Φ ctx Φ' S eOut τ K hwf hbelow hKΦ hKe hSK h
  -- declarative typing of the whole app (its arrow structure comes from `huni`)
  have happ := Infer.sourceSound h hwf hbelow K hKΦ hKe hSK
  cases h with
  | @app _ _ _ _ Φ₁ Φ₂ S₁ S₂ S₃ _ _ τf τa hf harg huni =>
    simp only [Expr.tyFreeVars, List.mem_append] at hKe
    have hKef : ∀ y ∈ f.tyFreeVars, y ∈ K := fun y hy => hKe y (.inl hy)
    have hKea : ∀ y ∈ arg.tyFreeVars, y ∈ K := fun y hy => hKe y (.inr hy)
    have hKfixS : ∀ k ∈ K, (S₁ ++ S₂ ++ S₃).onTy (.fvar k) = .fvar k :=
      fun k hk => Subst.fixes_fvar_of_avoids hSK hk
    have hSK₁ : ∀ p ∈ S₁, p.1 ∉ K :=
      fun p hp => hSK p (List.mem_append_left _ (List.mem_append_left _ hp))
    -- invert the declarative app typing to expose `argTy` and the result `τ₀`
    obtain ⟨argTy, hfty, hargty⟩ : ∃ argTy,
        TypeOfHM ((S₁ ++ S₂ ++ S₃).onCtx ctx) f (.arrow argTy (S₃.onTy (.fvar Φ₂))) ∧
        TypeOfHM ((S₁ ++ S₂ ++ S₃).onCtx ctx) arg argTy := by
      cases happ with | app hfty hargty => exact ⟨_, hfty, hargty⟩
    have hSlc : ∀ p ∈ S₁ ++ S₂ ++ S₃, p.2.IsLC := (Infer.lc (Infer.app hf harg huni) hwf).2
    -- recurse on `f`, factor the f-typing through the function's principal output
    have hsf := ihf K hwf hbelow hKΦ hKef hSK₁ hf
    obtain ⟨⟨⟨Φ₁', S₁', eOut₁', τf'⟩, hf', havf⟩, hef⟩ := Option.isSome_iff_exists.mp hsf
    obtain ⟨R_f, hagf, hStepf, hR_f, hR_fK⟩ := Infer.complete' hf' hwf hbelow hSlc K hKΦ hKef hKfixS hfty
    have hS₁' := (Infer.lc hf' hwf).2
    have hbf := Infer.belowFvars hf' hbelow (fun y hy => hKΦ y (hKef y hy))
    have hle1 := Infer.frontier_le hf'
    have hwf₁ := Subst.onCtx_wf hS₁' hwf
    have hbelow₁ := Subst.onCtx_below hbf.2 hle1 hbelow
    have hKΦ₁ : ∀ k ∈ K, k < Φ₁' := fun k hk => lt_of_lt_of_le (hKΦ k hk) hle1
    have hctxeq : R_f.onCtx (S₁'.onCtx ctx) = (S₁ ++ S₂ ++ S₃).onCtx ctx := by
      rw [← Subst.onCtx_append]; exact Subst.onCtx_congr (fun v hv => (hagf v hv).symm) hbelow
    -- transfer arg's typing, re-derive under the function's intermediate state
    have hargty' : TypeOfHM (R_f.onCtx (S₁'.onCtx ctx)) arg argTy := by rw [hctxeq]; exact hargty
    obtain ⟨_, _, _, _, _, hinfa, _, _, _, _, hSaK⟩ :=
      Infer.completeAt arg K hwf₁ hbelow₁ hR_f hKΦ₁ hKea hR_fK hargty'
    have hsa := iharg K hwf₁ hbelow₁ hKΦ₁ hKea hSaK hinfa
    obtain ⟨⟨⟨Φ₂', S₂', eOut₂', τa'⟩, harg', hava⟩, hea⟩ := Option.isSome_iff_exists.mp hsa
    obtain ⟨R_a, haga, hStepa, hR_a, hR_aK⟩ :=
      Infer.complete' harg' hwf₁ hbelow₁ hR_f K hKΦ₁ hKea hR_fK hargty'
    -- build the app unifier on the function's `(S₂'.onTy τf')`, `arrow τa' (fvar Φ₂')`
    have hba := Infer.belowFvars harg' hbelow₁ (fun y hy => lt_of_lt_of_le (hKΦ₁ y (hKea y hy)) (le_refl _))
    have hle2 := Infer.frontier_le harg'
    have hcongr_f : R_f.onTy τf' = (S₂' ++ R_a).onTy τf' := Subst.onTy_congr haga hbf.1
    have hP : R_a.onTy (S₂'.onTy τf') = Ty.arrow argTy (S₃.onTy (.fvar Φ₂)) := by
      rw [← Subst.onTy_append, ← hcongr_f]; exact hStepf.symm
    have hΦ₂τf : Φ₂' ∉ (S₂'.onTy τf').freeVars := fun hm => by
      have := (Subst.onTy_belowFvars hba.2 (hbf.1.mono hle2)).mem_lt _ hm; omega
    have hΦ₂τa : Φ₂' ∉ τa'.freeVars := fun hm => by have := hba.1.mem_lt _ hm; omega
    have hτ₀LC : (S₃.onTy (.fvar Φ₂)).IsLC := by
      have hreg := TypeOfHM.regular hfty; cases hreg with | arrow _ hT => exact hT
    have hKΦ₂ : ∀ k ∈ K, k < Φ₂' := fun k hk => lt_of_lt_of_le (hKΦ₁ k hk) hle2
    obtain ⟨U, hUni, hUlc, hUK⟩ :=
      exists_app_unifier hP hStepa hΦ₂τf hΦ₂τa hR_a hτ₀LC hR_aK hKΦ₂
    have hτfLC := (Infer.lc hf' hwf).1
    have hτaLC := (Infer.lc harg' hwf₁).1
    have hS₂' := (Infer.lc harg' hwf₁).2
    have huniSome : (unifyCoreK K (S₂'.onTy τf') (Ty.arrow τa' (Ty.fvar Φ₂'))).isSome :=
      unifyCoreK_complete (Subst.onTy_lc hS₂' hτfLC) (.arrow hτaLC ContainsBvarsUpTo.fvar) hUlc hUni hUK
    obtain ⟨⟨S₃', _, _⟩, he3⟩ := Option.isSome_iff_exists.mp huniSome
    rw [inferCore]; simp only [hef, hea, he3, Option.isSome_some]

theorem inferCore_complete_letIn {ann : Option PolyTy} {rhs body : Expr}
    (iha : InferCoreComplete rhs) (ihao : ∀ Ys, InferCoreComplete (rhs.openTyVars Ys))
    (ihb : InferCoreComplete body) :
    InferCoreComplete (.letIn ann rhs body) := by
  intro Φ ctx Φ' S eOut τ K hwf hbelow hKΦ hKe hSK h
  have hSlc : ∀ p ∈ S, p.2.IsLC := (Infer.lc h hwf).2
  have happ := Infer.sourceSound h hwf hbelow K hKΦ hKe hSK
  cases ann with
  | none =>
    cases h with
    | @letIn _ _ _ _ Φ1d Φ2d S1d S2d rhsOut1 bodyOut1 τ1d τ2 hrhs hbody =>
      cases happ with
      | letIn hMwf hann hcofin heqctx hbodyD =>
        expose_names
        subst heqctx
        simp only [Expr.tyFreeVars, Option.elim_none, List.nil_append, List.mem_append] at hKe
        have hKrhs : ∀ y ∈ rhs.tyFreeVars, y ∈ K := fun y hy => hKe y (.inl hy)
        have hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKe y (.inr hy)
        have hKfixS : ∀ k ∈ K, (S1d ++ S2d).onTy (.fvar k) = .fvar k :=
          fun k hk => Subst.fixes_fvar_of_avoids hSK hk
        have hSK₁ : ∀ p ∈ S1d, p.1 ∉ K := fun p hp => hSK p (List.mem_append_left _ hp)
        -- fresh opening avoiding `L`, `M.body`, the (substituted) env, and the rigid `rhs.tyFreeVars`
        obtain ⟨Xs, hXlen, hXnodup, hXavoid⟩ := exists_fresh_names
          (L ++ M.body.freeVars ++ ((S1d ++ S2d).onCtx ctx).env.freeVars ++ rhs.tyFreeVars) M.paramCount
        have hXfreshL : FreshNames L M.paramCount Xs :=
          ⟨hXlen, hXnodup, fun x hx hc => hXavoid x hx
            (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_left _ hc)))⟩
        have hXMbody : ∀ x ∈ Xs, x ∉ M.body.freeVars := fun x hx hc => hXavoid x hx
          (List.mem_append_left _ (List.mem_append_left _ (List.mem_append_right _ hc)))
        have hXenv : ∀ x ∈ Xs, x ∉ ((S1d ++ S2d).onCtx ctx).env.freeVars := fun x hx hc =>
          hXavoid x hx (List.mem_append_left _ (List.mem_append_right _ hc))
        have hXrhs : ∀ x ∈ Xs, x ∉ rhs.tyFreeVars := fun x hx hc =>
          hXavoid x hx (List.mem_append_right _ hc)
        -- recurse on the given rhs derivation; factor the rhs-opening typing
        have hsa := iha K hwf hbelow hKΦ hKrhs hSK₁ hrhs
        obtain ⟨⟨⟨Φ₁', S₁', eOut₁', τ₁'⟩, hrhs', hav₁⟩, herhs⟩ := Option.isSome_iff_exists.mp hsa
        obtain ⟨R₁, haga, htya, hR₁, hR₁K⟩ :=
          Infer.complete' hrhs' hwf hbelow hSlc K hKΦ hKrhs hKfixS (hcofin Xs hXfreshL)
        have hS₁ : ∀ p ∈ S₁', p.2.IsLC := (Infer.lc hrhs' hwf).2
        have hτ₁lc : τ₁'.IsLC := (Infer.lc hrhs' hwf).1
        have hle : Φ ≤ Φ₁' := Infer.frontier_le hrhs'
        have hbf := Infer.belowFvars hrhs' hbelow (fun y hy => hKΦ y (hKrhs y hy))
        have hbelowτ₁ : Ty.BelowFvars Φ₁' τ₁' := hbf.1
        have hbelowS₁ : ∀ p ∈ S₁', Ty.BelowFvars Φ₁' p.2 := hbf.2
        have hwf₁ : CtxWF (S₁'.onCtx ctx) := Subst.onCtx_wf hS₁ hwf
        have hbelow₁ : CtxBelow Φ₁' (S₁'.onCtx ctx) := Subst.onCtx_below hbelowS₁ hle hbelow
        have hctxeq : R₁.onCtx (S₁'.onCtx ctx) = (S1d ++ S2d).onCtx ctx := by
          rw [← Subst.onCtx_append]
          exact Subst.onCtx_congr (fun v hv => (haga v hv).symm) hbelow
        have hwfBody : CtxWF { (S₁'.onCtx ctx) with
            env := genScheme rhs.tyFreeVars (S₁'.onCtx ctx).env τ₁' :: (S₁'.onCtx ctx).env } := by
          intro N hN
          rcases List.mem_cons.mp hN with rfl | hN
          · exact genScheme_wf hτ₁lc
          · exact hwf₁ N hN
        have hbelowBody : CtxBelow Φ₁' { (S₁'.onCtx ctx) with
            env := genScheme rhs.tyFreeVars (S₁'.onCtx ctx).env τ₁' :: (S₁'.onCtx ctx).env } := by
          intro N hN
          rcases List.mem_cons.mp hN with rfl | hN
          · show Ty.BelowFvars Φ₁' (Ty.closeOver (genVars rhs.tyFreeVars (S₁'.onCtx ctx).env τ₁') τ₁')
            exact hbelowτ₁.closeOver
          · exact hbelow₁ N hN
        have hXM'' : ∀ x ∈ Xs,
            x ∉ (R₁.onPolyTy (genScheme rhs.tyFreeVars (S₁'.onCtx ctx).env τ₁')).body.freeVars := by
          intro x hx hmem
          change x ∈ (R₁.onTy (Ty.closeOver (genVars rhs.tyFreeVars (S₁'.onCtx ctx).env τ₁') τ₁')).freeVars
            at hmem
          obtain ⟨v, hv, hxv⟩ := Ty.mem_freeVars_onTy_iff.mp hmem
          have hvτ₁ : v ∈ τ₁'.freeVars := Ty.closeOver_freeVars_subset hv
          have hvg : v ∉ genVars rhs.tyFreeVars (S₁'.onCtx ctx).env τ₁' :=
            fun hg => Ty.not_mem_closeOver_freeVars hg hv
          by_cases hvrhs : v ∈ rhs.tyFreeVars
          · have hfix : R₁.onTy (Ty.fvar v) = Ty.fvar v := hR₁K v (hKrhs v hvrhs)
            rw [hfix] at hxv
            simp only [Ty.freeVars, List.mem_singleton] at hxv
            exact hXrhs x hx (hxv ▸ hvrhs)
          · have hvenv : v ∈ (S₁'.onCtx ctx).env.freeVars := by
              by_contra hc
              apply hvg
              simp only [genVars, List.mem_filter, Bool.and_eq_true]
              exact ⟨hvτ₁, by simpa using hc, by simpa using hvrhs⟩
            obtain ⟨pt, hpt, hvpt⟩ := Env.mem_freeVars_iff.mp hvenv
            have hx_onTy : x ∈ (R₁.onTy pt.body).freeVars :=
              Ty.mem_freeVars_onTy_iff.mpr ⟨v, hvpt, hxv⟩
            have hpt_mem : R₁.onPolyTy pt ∈ ((S1d ++ S2d).onCtx ctx).env := by
              have hmem2 : R₁.onPolyTy pt ∈ (R₁.onCtx (S₁'.onCtx ctx)).env := by
                simp only [Subst.onCtx, Subst.onEnv]
                exact List.mem_map.mpr ⟨pt, hpt, rfl⟩
              rw [hctxeq] at hmem2; exact hmem2
            exact hXenv x hx (Env.mem_freeVars_iff.mpr ⟨R₁.onPolyTy pt, hpt_mem, hx_onTy⟩)
        have hgen : (R₁.onPolyTy (genScheme rhs.tyFreeVars (S₁'.onCtx ctx).env τ₁')).Generalizes M :=
          genScheme_generalizes hτ₁lc hR₁ hMwf hXnodup hXlen hXMbody htya hXM''
        have hbody' : TypeOfHM
            ⟨R₁.onPolyTy (genScheme rhs.tyFreeVars (S₁'.onCtx ctx).env τ₁') :: ((S1d ++ S2d).onCtx ctx).env,
              ((S1d ++ S2d).onCtx ctx).ctors⟩ body τ :=
          TypeOfHM.weaken_scheme (env_post := []) (env := ((S1d ++ S2d).onCtx ctx).env) (M := M) hgen hbodyD
        have hbody'' : TypeOfHM (R₁.onCtx { (S₁'.onCtx ctx) with
            env := genScheme rhs.tyFreeVars (S₁'.onCtx ctx).env τ₁' :: (S₁'.onCtx ctx).env }) body τ := by
          show TypeOfHM ⟨R₁.onPolyTy (genScheme rhs.tyFreeVars (S₁'.onCtx ctx).env τ₁')
              :: R₁.onEnv (S₁'.onCtx ctx).env, (S₁'.onCtx ctx).ctors⟩ body τ
          rw [show R₁.onEnv (S₁'.onCtx ctx).env = ((S1d ++ S2d).onCtx ctx).env from congrArg Ctx.env hctxeq]
          exact hbody'
        obtain ⟨_, _, _, _, _, hinfb, _, _, _, _, hSbK⟩ := Infer.completeAt body K hwfBody hbelowBody hR₁
          (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle) hKbody hR₁K hbody''
        have hsb := ihb K hwfBody hbelowBody (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle) hKbody hSbK hinfb
        obtain ⟨⟨⟨Φ₂', S₂', eOut₂', τ₂'⟩, hbodyf, hav₂⟩, hebody⟩ := Option.isSome_iff_exists.mp hsb
        rw [inferCore]; simp only [herhs, hebody, Option.isSome_some]
  | some σ =>
    obtain ⟨hAdom, hBenv⟩ :=
      Infer.letInAnn_block_fresh h hbelow (fun y hy => hKΦ y (hKe y hy))
    cases happ with
    | letIn hMwf hann hcofin heqctx hbodyD =>
      subst heqctx
      obtain hMσ := hann σ rfl
      rw [hMσ] at hMwf hcofin hbodyD
      simp only [Expr.openBoundTyVars] at hcofin
      simp only [Expr.tyFreeVars, Option.elim_some, List.mem_append] at hKe
      have hKσ : ∀ y ∈ σ.body.freeVars, y ∈ K := fun y hy => hKe y (.inl (.inl hy))
      have hKrhs : ∀ y ∈ rhs.tyFreeVars, y ∈ K := fun y hy => hKe y (.inl (.inr hy))
      have hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKe y (.inr hy)
      have hσwf : σ.WF := hMwf
      have hbelowN : CtxBelow (Φ + σ.paramCount) ctx := fun M hM => (hbelow M hM).mono (by omega)
      -- rigid-set coverage of `K ∪ Ys` for the opened-rhs reconstruction
      have hKΦ' : ∀ k ∈ K ++ freshVars Φ σ.paramCount, k < Φ + σ.paramCount := by
        intro k hk; rcases List.mem_append.mp hk with hk | hk
        · have := hKΦ k hk; omega
        · have := freshVars_lt k hk; omega
      have hKe' : ∀ y ∈ (rhs.openTyVars (freshVars Φ σ.paramCount)).tyFreeVars,
          y ∈ K ++ freshVars Φ σ.paramCount := by
        intro y hy
        rcases Expr.tyFreeVars_openTyVars hy with h' | h'
        · exact List.mem_append_left _ (hKrhs y h')
        · exact List.mem_append_right _ h'
      have hKfix' : ∀ k ∈ K ++ freshVars Φ σ.paramCount, S.onTy (.fvar k) = .fvar k := by
        intro k hk; rcases List.mem_append.mp hk with hk | hk
        · exact Subst.fixes_fvar_of_avoids hSK hk
        · exact Ty.substFvars_eq_self_of_no_key (fun p hp hc => by
            simp only [Ty.freeVars, List.mem_singleton] at hc
            exact hAdom k hk (hc ▸ List.mem_map.mpr ⟨p, hp, rfl⟩))
      have htyYs : TypeOfHM (S.onCtx ctx) (rhs.openTyVars (freshVars Φ σ.paramCount))
          (σ.openVars (freshVars Φ σ.paramCount)) :=
        typeOfHM_at_block (freshVars_length _ _) hcofin
      -- reconstruct a derivation of the opened rhs (carrying its `K ∪ Ys` avoidance)
      obtain ⟨_, _, _, _, _, Drhs, _, _, _, _, hSrhsK⟩ :=
        Infer.completeAt (rhs.openTyVars (freshVars Φ σ.paramCount)) (K ++ freshVars Φ σ.paramCount)
          hwf hbelowN hSlc hKΦ' hKe' hKfix' htyYs
      have hrhsSome := ihao (freshVars Φ σ.paramCount) (K ++ freshVars Φ σ.paramCount)
        hwf hbelowN hKΦ' hKe' hSrhsK Drhs
      obtain ⟨⟨⟨Φ₁e, S₁e, eOut₁e, τ₁e⟩, hrhse, hav₁⟩, herhs⟩ := Option.isSome_iff_exists.mp hrhsSome
      obtain ⟨R₁, haga, htya, hR₁lc, hR₁K⟩ :=
        Infer.complete' hrhse hwf hbelowN hSlc (K ++ freshVars Φ σ.paramCount) hKΦ' hKe' hKfix' htyYs
      have hτ₁elc : τ₁e.IsLC := (Infer.lc hrhse hwf).1
      have hS₁elc : ∀ p ∈ S₁e, p.2.IsLC := (Infer.lc hrhse hwf).2
      have hle1 : Φ + σ.paramCount ≤ Φ₁e := Infer.frontier_le hrhse
      have hbf := Infer.belowFvars hrhse hbelowN (fun y hy => hKΦ' y (hKe' y hy))
      have hbelowτ₁ : Ty.BelowFvars Φ₁e τ₁e := hbf.1
      have hbelowS₁ : ∀ p ∈ S₁e, Ty.BelowFvars Φ₁e p.2 := hbf.2
      have hBlc : (σ.openVars (freshVars Φ σ.paramCount)).IsLC := PolyTy.openVars_isLC hσwf (by simp)
      have hR₁_K : ∀ k ∈ K, R₁.onTy (.fvar k) = .fvar k :=
        fun k hk => hR₁K k (List.mem_append_left _ hk)
      have hR₁_Ys : ∀ Y ∈ freshVars Φ σ.paramCount, R₁.onTy (.fvar Y) = .fvar Y :=
        fun Y hY => hR₁K Y (List.mem_append_right _ hY)
      have hUuni : Unifies R₁ τ₁e (σ.openVars (freshVars Φ σ.paramCount)) := by
        show R₁.onTy τ₁e = R₁.onTy (σ.openVars (freshVars Φ σ.paramCount))
        rw [← htya]
        refine (Subst.onTy_eq_self_of_fixes (fun z hz => ?_)).symm
        rcases Ty.freeVars_openVars_subset z hz with h' | h'
        · exact hR₁_K z (hKσ z h')
        · exact hR₁_Ys z h'
      have huniSome :
          (unifyCoreK (K ++ freshVars Φ σ.paramCount) τ₁e (σ.openVars (freshVars Φ σ.paramCount))).isSome :=
        unifyCoreK_complete hτ₁elc hBlc hR₁lc hUuni hR₁K
      obtain ⟨⟨Schke, hSchke, havSchk⟩, heuni⟩ := Option.isSome_iff_exists.mp huniSome
      -- escape A: `Ys` is bound neither by the rhs subst nor the unifier
      have hesc1 : ∀ y ∈ freshVars Φ σ.paramCount, y ∉ (S₁e ++ Schke).map Prod.fst := by
        intro y hy hc
        obtain ⟨p, hp, hpy⟩ := List.mem_map.mp hc
        have hyKYs : p.1 ∈ K ++ freshVars Φ σ.paramCount := by rw [hpy]; exact List.mem_append_right _ hy
        rcases List.mem_append.mp hp with hp | hp
        · exact hav₁ p hp hyKYs
        · exact havSchk p hp hyKYs
      -- factor `R₁` through the MGU `Schke` to get `V`, fixing `K ∪ Ys`
      obtain ⟨V, hVeq, hVlc, hVK⟩ := UnifyRel.greatest_K hSchke R₁ hR₁lc hUuni hR₁K
      have hagSchkV : Subst.AgreesBelow Φ S (S₁e ++ Schke ++ V) := by
        intro v hv
        calc S.onTy (Ty.fvar v)
            = (S₁e ++ R₁).onTy (Ty.fvar v) := haga v (by omega)
          _ = R₁.onTy (S₁e.onTy (Ty.fvar v)) := by rw [Subst.onTy_append]
          _ = V.onTy (Schke.onTy (S₁e.onTy (Ty.fvar v))) := hVeq _
          _ = (S₁e ++ Schke ++ V).onTy (Ty.fvar v) := by rw [Subst.onTy_append, Subst.onTy_append]
      have hmain : V.onCtx (Schke.onCtx (S₁e.onCtx ctx)) = S.onCtx ctx := by
        have hc : (S₁e ++ Schke ++ V).onCtx ctx = S.onCtx ctx :=
          Subst.onCtx_congr (fun v hv => (hagSchkV v hv).symm) hbelow
        rwa [Subst.onCtx_append, Subst.onCtx_append] at hc
      -- escape B: a leak of `Ys` into the threaded env survives into `(S.onCtx ctx).env`
      have hesc2 : ∀ y ∈ freshVars Φ σ.paramCount,
          y ∉ (Schke.onCtx (S₁e.onCtx ctx)).env.freeVars := by
        intro Y hY hmem
        have hVfixY : V.onTy (Ty.fvar Y) = Ty.fvar Y := hVK Y (List.mem_append_right _ hY)
        obtain ⟨M, hM, hYM⟩ := Env.mem_freeVars_iff.mp hmem
        have hgo : Y ∈ (V.onCtx (Schke.onCtx (S₁e.onCtx ctx))).env.freeVars := by
          refine Env.mem_freeVars_iff.mpr ⟨V.onPolyTy M, ?_, ?_⟩
          · simp only [Subst.onCtx, Subst.onEnv]; exact List.mem_map.mpr ⟨M, hM, rfl⟩
          · simp only [Subst.onPolyTy]
            exact Ty.mem_freeVars_onTy_iff.mpr ⟨Y, hYM, by rw [hVfixY]; simp [Ty.freeVars]⟩
        rw [hmain] at hgo
        exact hBenv Y hY hgo
      -- body: reconstruct over `V`, then drive the body IH
      have hwf₁ := Subst.onCtx_wf hS₁elc hwf
      have hwfSchk := Subst.onCtx_wf (UnifyRel.lc hSchke hτ₁elc hBlc) hwf₁
      have hwfB : CtxWF { (Schke.onCtx (S₁e.onCtx ctx)) with
          env := σ :: (Schke.onCtx (S₁e.onCtx ctx)).env } := by
        intro N hN; rcases List.mem_cons.mp hN with rfl | hN
        · exact hσwf
        · exact hwfSchk N hN
      have hσbelow : Ty.BelowFvars Φ₁e σ.body :=
        Ty.BelowFvars.of_freeVars_lt (fun v hv => by have := hKΦ v (hKσ v hv); omega)
      have hσopen' : Ty.BelowFvars Φ₁e (σ.openVars (freshVars Φ σ.paramCount)) :=
        Ty.openVars_belowFvars hσbelow (fun x hx => by have := freshVars_lt x hx; omega)
      have hSchkbelow : ∀ p ∈ Schke, Ty.BelowFvars Φ₁e p.2 :=
        UnifyRel.belowFvars hSchke hbelowτ₁ hσopen'
      have hbelowSchk : CtxBelow Φ₁e (Schke.onCtx (S₁e.onCtx ctx)) :=
        Subst.onCtx_below hSchkbelow (le_refl _) (Subst.onCtx_below hbelowS₁ hle1 hbelowN)
      have hbelowB : CtxBelow Φ₁e { (Schke.onCtx (S₁e.onCtx ctx)) with
          env := σ :: (Schke.onCtx (S₁e.onCtx ctx)).env } := by
        intro N hN; rcases List.mem_cons.mp hN with rfl | hN
        · exact hσbelow
        · exact hbelowSchk N hN
      have hVσ_fix : V.onPolyTy σ = σ := by
        obtain ⟨pcc, b⟩ := σ
        simp only [Subst.onPolyTy, PolyTy.mk.injEq, true_and]
        exact Subst.onTy_eq_self_of_fixes (fun v hv => hVK v (List.mem_append_left _ (hKσ v hv)))
      have hbodyV : TypeOfHM (V.onCtx { (Schke.onCtx (S₁e.onCtx ctx)) with
          env := σ :: (Schke.onCtx (S₁e.onCtx ctx)).env }) body τ := by
        show TypeOfHM ⟨V.onPolyTy σ :: V.onEnv (Schke.onCtx (S₁e.onCtx ctx)).env,
            (Schke.onCtx (S₁e.onCtx ctx)).ctors⟩ body τ
        rw [hVσ_fix, show V.onEnv (Schke.onCtx (S₁e.onCtx ctx)).env = (S.onCtx ctx).env from
            congrArg Ctx.env hmain]
        exact hbodyD
      obtain ⟨_, _, _, _, _, Dbody, _, _, _, _, hSbodyK⟩ := Infer.completeAt body K hwfB hbelowB hVlc
        (fun k hk => by have := hKΦ k hk; have := hle1; omega) hKbody
        (fun k hk => hVK k (List.mem_append_left _ hk)) hbodyV
      have hbodySome := ihb K hwfB hbelowB
        (fun k hk => by have := hKΦ k hk; have := hle1; omega) hKbody hSbodyK Dbody
      obtain ⟨⟨⟨Φ₂e, S₂e, eOut₂e, τ₂e⟩, hbodye, hav₂⟩, hebody⟩ := Option.isSome_iff_exists.mp hbodySome
      -- drive the executable annotated-`let` arm
      rw [inferCore, dif_pos (PolyTy.wf_iff_bvarsBelow.mpr hσwf)]
      simp only [herhs, heuni]
      rw [dif_pos hesc1, dif_pos hesc2]
      simp only [hebody, Option.isSome_some]

/-- Function-completeness for a branch list: given a (well-formed,
    frontier-bounded) `InferBranches` derivation, `inferBranchesCore` succeeds.
    Mirrors `InferBranches.complete`, but concludes `.isSome` instead of producing
    a derivation, driving each branch body via `InferCoreComplete`. -/
def InferBranchesCoreComplete (branches : List (MatchPattern × Expr)) : Prop :=
  ∀ {Φ : Nat} {ctx : Ctx} {scrutTy : Ty} {ρ : Ty} {Φ' : Nat} {S : Subst}
    {brsOut : List (MatchPattern × Expr)} (K : List Nat),
    CtxWF ctx → CtxBelow Φ ctx → scrutTy.IsLC → Ty.BelowFvars Φ scrutTy →
    ρ.IsLC → Ty.BelowFvars Φ ρ →
    (∀ k ∈ K, k < Φ) → (∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars branches, y ∈ K) →
    (∀ p ∈ S, p.1 ∉ K) →
    InferBranches Φ ctx scrutTy ρ branches Φ' S brsOut →
    (inferBranchesCore K Φ ctx scrutTy ρ branches).isSome

theorem inferBranchesCore_complete : ∀ (branches : List (MatchPattern × Expr)),
    (∀ br ∈ branches, InferCoreComplete br.2) → InferBranchesCoreComplete branches := by
  intro branches
  induction branches with
  | nil =>
    intro _ Φ ctx scrutTy ρ Φ' S brsOut K _ _ _ _ _ _ _ _ _ _
    simp [inferBranchesCore]
  | cons head rest ih =>
    intro ihbr
    obtain ⟨pat, body⟩ := head
    intro Φ ctx scrutTy ρ Φ' S brsOut K hwf hbelow hscrutLC hbscrut hρ hbρ hKΦ hKbr hSK h
    -- split the rigid-set coverage across the head body and the rest of the list
    have hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKbr y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inl hy)
    have hKrest : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars rest, y ∈ K := fun y hy => hKbr y (by
      simp only [Expr.tyFreeVars.BranchList.tyFreeVars, List.mem_append]; exact Or.inr hy)
    have hKfixS : ∀ k ∈ K, S.onTy (.fvar k) = .fvar k := fun k hk => Subst.fixes_fvar_of_avoids hSK hk
    -- declarative branch typings + LC facts from the given derivation
    have hsound := InferBranches.sourceSound h hwf hbelow hscrutLC hρ hbscrut hbρ K hKΦ hKbr hSK
    have hSlc : ∀ p ∈ S, p.2.IsLC := (InferBranches.lc h hwf hscrutLC hρ).2
    -- invert the head branch's declarative typing (at the residual `S`)
    have hdhead := hsound (pat, body) (List.mem_cons_self ..)
    cases hdhead with
    | mk hlook hscrutEqD hpcD hnD hforallD hpbD hbctxD hbodytyD =>
      expose_names
      simp only [Subst.onCtx] at hlook
      have htyArgs_lc : ∀ t ∈ tyArgs, t.IsLC := by
        have hreg : (Ty.customTy ctor.tyName tyArgs).IsLC := hscrutEqD ▸ Subst.onTy_lc hSlc hscrutLC
        cases hreg with | customTy h => exact h
      have hinsts := instContents_eq_openWith hforallD (fun c hc => hpcD ▸ ctor.bound c hc)
      rw [hbctxD, hpbD, hinsts] at hbodytyD
      have hcustomTy_lc : (Ty.customTy ctor.tyName
          ((freshVars Φ ctor.paramCount).map (Ty.fvar ·))).IsLC :=
        ContainsBvarsUpTo.customTy (fun t ht => by
          obtain ⟨x, _, rfl⟩ := List.mem_map.mp ht; exact ContainsBvarsUpTo.fvar)
      -- STEP 0: drive the per-branch `customTy` unifyCoreK via the dodge unifier.
      obtain ⟨U, hUni0, hUlc, hUK, hUeqR, hfreshU⟩ :=
        customTy_dodge_unifier hbscrut hSlc hKΦ hKfixS htyArgs_lc hpcD hscrutEqD
      have huni0Some := unifyCoreK_complete hscrutLC hcustomTy_lc hUlc hUni0 hUK
      obtain ⟨⟨S₀, huni0, _⟩, he0⟩ := Option.isSome_iff_exists.mp huni0Some
      obtain ⟨R_s, hR_s_eq, hR_s_lc, hR_s_K⟩ := UnifyRel.greatest_K huni0 U hUlc hUni0 hUK
      have hR_s_ag : ∀ v, v < Φ → S.onTy (Ty.fvar v) = R_s.onTy (S₀.onTy (Ty.fvar v)) := by
        intro v hv; rw [← hR_s_eq, hUeqR v hv]
      have hfresh_R_s : (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy).map R_s.onTy
          = tyArgs := by
        rw [List.map_map]; exact Eq.trans (List.map_congr_left (fun t _ => (hR_s_eq t).symm)) hfreshU
      have hS₀ : ∀ p ∈ S₀, p.2.IsLC := UnifyRel.lc huni0 hscrutLC hcustomTy_lc
      have hS₀below : ∀ p ∈ S₀, Ty.BelowFvars (Φ + ctor.paramCount) p.2 := by
        apply UnifyRel.belowFvars huni0 (hbscrut.mono (by omega))
        apply Ty.BelowFvars.customTy
        intro t ht; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp ht
        exact Ty.BelowFvars.fvar (by have := freshVars_lt x hx; omega)
      have key_t : ∀ {t : Ty}, Ty.BelowFvars Φ t → R_s.onTy (S₀.onTy t) = S.onTy t := by
        intro t ht
        rw [← Subst.onTy_append]
        exact Subst.onTy_congr (fun v hv => by rw [Subst.onTy_append]; exact (hR_s_ag v hv).symm) ht
      have hctx0 : R_s.onCtx (S₀.onCtx ctx) = S.onCtx ctx := by
        rw [← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => by rw [Subst.onTy_append]; exact (hR_s_ag v hv).symm) hbelow
      have hbodyWF : CtxWF { S₀.onCtx ctx with
          env := (ctor.contents.map (Ty.openWith
              (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
            ++ (S₀.onCtx ctx).env } :=
        branchBindings_wf (ctorr := ctor)
          (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
          (Subst.onCtx_wf hS₀ hwf)
          (fun t ht => by
            obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
            obtain ⟨x, _, rfl⟩ := List.mem_map.mp hv
            exact Subst.onTy_lc hS₀ ContainsBvarsUpTo.fvar)
          (by simp)
      have hbodyBelow : CtxBelow (Φ + ctor.paramCount) { S₀.onCtx ctx with
          env := (ctor.contents.map (Ty.openWith
              (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
            ++ (S₀.onCtx ctx).env } :=
        branchBindings_below (ctorr := ctor)
          (ta := ((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy)
          (Subst.onCtx_below hS₀below (by omega) hbelow)
          (fun t ht => by
            obtain ⟨v, hv, rfl⟩ := List.mem_map.mp ht
            obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hv
            exact Subst.onTy_belowFvars hS₀below (.fvar (by have := freshVars_lt x hx; omega)))
      have hbodyty2 : TypeOfHM (R_s.onCtx { S₀.onCtx ctx with
          env := (ctor.contents.map (Ty.openWith
              (((freshVars Φ ctor.paramCount).map (Ty.fvar ·)).map S₀.onTy))).map PolyTy.mkTrivial
            ++ (S₀.onCtx ctx).env }) body (R_s.onTy (S₀.onTy ρ)) := by
        rw [Subst.onCtx_branchBindings hR_s_lc, hctx0, hfresh_R_s, key_t hbρ]
        exact hbodytyD
      -- STEP: reconstruct a head-body derivation, drive the body function, factor via `complete'`
      obtain ⟨_, _, _, _, _, hbodyderiv, _, _, _, _, hSbK⟩ :=
        Infer.completeAt body K hbodyWF hbodyBelow hR_s_lc
          (fun k hk => by have := hKΦ k hk; omega) hKbody hR_s_K hbodyty2
      have hbodysome := (ihbr (.named c n, body) (List.mem_cons_self ..)) K hbodyWF hbodyBelow
        (fun k hk => by have := hKΦ k hk; omega) hKbody hSbK hbodyderiv
      obtain ⟨⟨⟨Φ_b, S_b, eOut_b, τ_b⟩, hinfbody, _⟩, hebody⟩ := Option.isSome_iff_exists.mp hbodysome
      obtain ⟨R_b, hagbody, htybody, hR_b, hR_bK⟩ :=
        Infer.complete' hinfbody hbodyWF hbodyBelow hR_s_lc K
          (fun k hk => by have := hKΦ k hk; omega) hKbody hR_s_K hbodyty2
      have hτb_lc : τ_b.IsLC := (Infer.lc hinfbody hbodyWF).1
      have hS_b : ∀ p ∈ S_b, p.2.IsLC := (Infer.lc hinfbody hbodyWF).2
      have hbb := Infer.belowFvars hinfbody hbodyBelow (fun y hy => by have := hKΦ y (hKbody y hy); omega)
      have hle_b : Φ + ctor.paramCount ≤ Φ_b := Infer.frontier_le hinfbody
      have hbS₀ρ : Ty.BelowFvars (Φ + ctor.paramCount) (S₀.onTy ρ) :=
        Subst.onTy_belowFvars hS₀below (hbρ.mono (by omega))
      -- STEP: `R_b` unifies `τ_b` with `S_b.onTy (S₀.onTy ρ)`; drive the result unifyCoreK.
      have hUni : Unifies R_b τ_b (S_b.onTy (S₀.onTy ρ)) := by
        show R_b.onTy τ_b = R_b.onTy (S_b.onTy (S₀.onTy ρ))
        rw [← htybody]
        exact (Subst.onTy_congr hagbody hbS₀ρ).trans (Subst.onTy_append S_b R_b (S₀.onTy ρ))
      have huniSome : (unifyCoreK K τ_b (S_b.onTy (S₀.onTy ρ))).isSome :=
        unifyCoreK_complete hτb_lc (Subst.onTy_lc hS_b (Subst.onTy_lc hS₀ hρ)) hR_b hUni hR_bK
      obtain ⟨⟨S_u, h_u, _⟩, heuni⟩ := Option.isSome_iff_exists.mp huniSome
      obtain ⟨R_u, hR_u_eq, hR_u, hR_uK⟩ := UnifyRel.greatest_K h_u R_b hR_b hUni hR_bK
      have hS_u : ∀ p ∈ S_u, p.2.IsLC :=
        UnifyRel.lc h_u hτb_lc (Subst.onTy_lc hS_b (Subst.onTy_lc hS₀ hρ))
      have hS_u_below : ∀ p ∈ S_u, Ty.BelowFvars Φ_b p.2 :=
        UnifyRel.belowFvars h_u hbb.1 (Subst.onTy_belowFvars hbb.2 (hbS₀ρ.mono hle_b))
      -- STEP: hypotheses for the recursion on `rest`.
      have hwf' : CtxWF (S_u.onCtx (S_b.onCtx (S₀.onCtx ctx))) :=
        Subst.onCtx_wf hS_u (Subst.onCtx_wf hS_b (Subst.onCtx_wf hS₀ hwf))
      have hbelow' : CtxBelow Φ_b (S_u.onCtx (S_b.onCtx (S₀.onCtx ctx))) :=
        Subst.onCtx_below hS_u_below (le_refl _)
          (Subst.onCtx_below hbb.2 (le_refl _)
            (Subst.onCtx_below (fun p hp => (hS₀below p hp).mono hle_b) (by omega) hbelow))
      have hbS₀scrut : Ty.BelowFvars (Φ + ctor.paramCount) (S₀.onTy scrutTy) :=
        Subst.onTy_belowFvars hS₀below (hbscrut.mono (by omega))
      have hscrutLC' : (S_u.onTy (S_b.onTy (S₀.onTy scrutTy))).IsLC :=
        Subst.onTy_lc hS_u (Subst.onTy_lc hS_b (Subst.onTy_lc hS₀ hscrutLC))
      have hbscrut' : Ty.BelowFvars Φ_b (S_u.onTy (S_b.onTy (S₀.onTy scrutTy))) :=
        Subst.onTy_belowFvars hS_u_below (Subst.onTy_belowFvars hbb.2 (hbS₀scrut.mono hle_b))
      have hρ' : (S_u.onTy (S_b.onTy (S₀.onTy ρ))).IsLC :=
        Subst.onTy_lc hS_u (Subst.onTy_lc hS_b (Subst.onTy_lc hS₀ hρ))
      have hbρ' : Ty.BelowFvars Φ_b (S_u.onTy (S_b.onTy (S₀.onTy ρ))) :=
        Subst.onTy_belowFvars hS_u_below (Subst.onTy_belowFvars hbb.2 (hbS₀ρ.mono hle_b))
      have key_full : ∀ {t : Ty}, Ty.BelowFvars Φ t →
          R_u.onTy (S_u.onTy (S_b.onTy (S₀.onTy t))) = S.onTy t := by
        intro t ht
        have hbS₀t : Ty.BelowFvars (Φ + ctor.paramCount) (S₀.onTy t) :=
          Subst.onTy_belowFvars hS₀below (ht.mono (by omega))
        rw [← hR_u_eq, ← Subst.onTy_append, Subst.onTy_congr hagbody hbS₀t |>.symm]
        exact key_t ht
      have hctx_full : R_u.onCtx (S_u.onCtx (S_b.onCtx (S₀.onCtx ctx))) = S.onCtx ctx := by
        rw [← Subst.onCtx_append, ← Subst.onCtx_append, ← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => by
          simp only [Subst.onTy_append]; exact key_full (Ty.BelowFvars.fvar hv)) hbelow
      have hdecl' : ∀ br ∈ rest, TypeOfMatchBranch (R_u.onCtx (S_u.onCtx (S_b.onCtx (S₀.onCtx ctx))))
          br (R_u.onTy (S_u.onTy (S_b.onTy (S₀.onTy scrutTy))))
          (R_u.onTy (S_u.onTy (S_b.onTy (S₀.onTy ρ)))) := by
        intro br hbr
        rw [hctx_full, key_full hbscrut, key_full hbρ]
        exact hsound br (List.mem_cons_of_mem _ hbr)
      obtain ⟨Φ_rr, S_r, brsOut_r2, R_r, hinfrest, _hagrest, _hR_r, _hR_rK, hS_rK⟩ :=
        InferBranches.complete K (fun br _ => Infer.completeAt br.2)
          hwf' hbelow' hscrutLC' hbscrut' hρ' hbρ' hR_u
          (fun k hk => lt_of_lt_of_le (hKΦ k hk) (le_trans (Nat.le_add_right _ _) hle_b))
          hKrest hR_uK hdecl'
      have hrestsome := ih (fun br hbr => ihbr br (List.mem_cons_of_mem _ hbr)) K
        hwf' hbelow' hscrutLC' hbscrut' hρ' hbρ'
        (fun k hk => lt_of_lt_of_le (hKΦ k hk) (le_trans (Nat.le_add_right _ _) hle_b))
        hKrest hS_rK hinfrest
      obtain ⟨⟨⟨Φ_r, S_r2, brsOut_r⟩, hrest', _⟩, herest⟩ := Option.isSome_iff_exists.mp hrestsome
      -- STEP: conclude by unfolding `inferBranchesCore` and discharging the guards.
      rw [inferBranchesCore]
      split
      · rename_i heqn; rw [hlook] at heqn; exact absurd heqn (by simp)
      · rename_i ctorr heqs
        obtain rfl : ctorr = ctor := by rw [hlook] at heqs; exact (Option.some.inj heqs).symm
        rw [dif_pos hnD]
        simp only [he0, hebody, heuni, herest, Option.isSome_some]
    | wildcard hbodyty =>
      obtain ⟨_, _, _, _, _, hbodyderiv, _, _, _, _, hSbK⟩ :=
        Infer.completeAt body K hwf hbelow hSlc hKΦ hKbody hKfixS hbodyty
      have hbodysome := (ihbr (.wildcard, body) (List.mem_cons_self ..)) K hwf hbelow
        hKΦ hKbody hSbK hbodyderiv
      obtain ⟨⟨⟨Φ_b, S_b, eOut_b, τ_b⟩, hinfbody, _⟩, hebody⟩ := Option.isSome_iff_exists.mp hbodysome
      obtain ⟨R_b, hagbody, htybody, hR_b, hR_bK⟩ :=
        Infer.complete' hinfbody hwf hbelow hSlc K hKΦ hKbody hKfixS hbodyty
      have hτb_lc : τ_b.IsLC := (Infer.lc hinfbody hwf).1
      have hS_b : ∀ p ∈ S_b, p.2.IsLC := (Infer.lc hinfbody hwf).2
      have hbb := Infer.belowFvars hinfbody hbelow (fun y hy => hKΦ y (hKbody y hy))
      have hle_b := Infer.frontier_le hinfbody
      have hUni : Unifies R_b τ_b (S_b.onTy ρ) := by
        show R_b.onTy τ_b = R_b.onTy (S_b.onTy ρ)
        rw [← htybody, ← Subst.onTy_append]
        exact Subst.onTy_congr hagbody hbρ
      have huniSome : (unifyCoreK K τ_b (S_b.onTy ρ)).isSome :=
        unifyCoreK_complete hτb_lc (Subst.onTy_lc hS_b hρ) hR_b hUni hR_bK
      obtain ⟨⟨S_u, h_u, _⟩, heuni⟩ := Option.isSome_iff_exists.mp huniSome
      obtain ⟨R_u, hR_u_eq, hR_u, hR_uK⟩ := UnifyRel.greatest_K h_u R_b hR_b hUni hR_bK
      have hS_u : ∀ p ∈ S_u, p.2.IsLC := UnifyRel.lc h_u hτb_lc (Subst.onTy_lc hS_b hρ)
      have hS_u_below : ∀ p ∈ S_u, Ty.BelowFvars Φ_b p.2 :=
        UnifyRel.belowFvars h_u hbb.1 (Subst.onTy_belowFvars hbb.2 (hbρ.mono hle_b))
      have hwf' : CtxWF (S_u.onCtx (S_b.onCtx ctx)) :=
        Subst.onCtx_wf hS_u (Subst.onCtx_wf hS_b hwf)
      have hbelow_Sb : CtxBelow Φ_b (S_b.onCtx ctx) := Subst.onCtx_below hbb.2 hle_b hbelow
      have hbelow' : CtxBelow Φ_b (S_u.onCtx (S_b.onCtx ctx)) :=
        Subst.onCtx_below hS_u_below (le_refl _) hbelow_Sb
      have key_t : ∀ {t : Ty}, Ty.BelowFvars Φ t →
          R_u.onTy (S_u.onTy (S_b.onTy t)) = S.onTy t := by
        intro t ht
        rw [← hR_u_eq (S_b.onTy t), ← Subst.onTy_append]
        exact (Subst.onTy_congr hagbody ht).symm
      have hscrutLC' : (S_u.onTy (S_b.onTy scrutTy)).IsLC :=
        Subst.onTy_lc hS_u (Subst.onTy_lc hS_b hscrutLC)
      have hbscrut' : Ty.BelowFvars Φ_b (S_u.onTy (S_b.onTy scrutTy)) :=
        Subst.onTy_belowFvars hS_u_below (Subst.onTy_belowFvars hbb.2 (hbscrut.mono hle_b))
      have hρ' : (S_u.onTy (S_b.onTy ρ)).IsLC := Subst.onTy_lc hS_u (Subst.onTy_lc hS_b hρ)
      have hbρ' : Ty.BelowFvars Φ_b (S_u.onTy (S_b.onTy ρ)) :=
        Subst.onTy_belowFvars hS_u_below (Subst.onTy_belowFvars hbb.2 (hbρ.mono hle_b))
      have hctx_eq : R_u.onCtx (S_u.onCtx (S_b.onCtx ctx)) = S.onCtx ctx := by
        rw [← Subst.onCtx_comp_of_onTy_eq hR_u_eq, ← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => (hagbody v hv).symm) hbelow
      have hdecl' : ∀ br ∈ rest, TypeOfMatchBranch (R_u.onCtx (S_u.onCtx (S_b.onCtx ctx)))
          br (R_u.onTy (S_u.onTy (S_b.onTy scrutTy))) (R_u.onTy (S_u.onTy (S_b.onTy ρ))) := by
        intro br hbr
        rw [hctx_eq, key_t hbscrut, key_t hbρ]
        exact hsound br (List.mem_cons_of_mem _ hbr)
      obtain ⟨Φ_rr, S_r, brsOut_r2, R_r, hinfrest, _hagrest, _hR_r, _hR_rK, hS_rK⟩ :=
        InferBranches.complete K (fun br _ => Infer.completeAt br.2)
          hwf' hbelow' hscrutLC' hbscrut' hρ' hbρ' hR_u
          (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle_b) hKrest hR_uK hdecl'
      have hrestsome := ih (fun br hbr => ihbr br (List.mem_cons_of_mem _ hbr)) K
        hwf' hbelow' hscrutLC' hbscrut' hρ' hbρ'
        (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle_b) hKrest hS_rK hinfrest
      obtain ⟨⟨⟨Φ_r, S_r2, brsOut_r⟩, hrest', _⟩, herest⟩ := Option.isSome_iff_exists.mp hrestsome
      rw [inferBranchesCore]
      simp only [hebody, heuni, herest, Option.isSome_some]

/-- An `InferRecGroup` derivation has matching binding/target lengths. -/
theorem InferRecGroup.length_eq {Φ ctx bindings targets Φ' S bindingsOut}
    (h : InferRecGroup Φ ctx bindings targets Φ' S bindingsOut) : bindings.length = targets.length := by
  induction bindings generalizing Φ ctx targets Φ' S bindingsOut with
  | nil => cases h with | nil => rfl
  | cons e rest ih =>
    cases h with
    | cons _ _ hrest =>
      have := ih hrest
      simp only [List.length_cons, List.length_map] at this ⊢
      omega

/-- Function-completeness for a recursive group: given a (well-formed,
    frontier-bounded) `InferRecGroup` derivation, `inferRecGroupCore` succeeds.
    Mirrors `inferBranchesCore_complete`, driving each binding via
    `InferCoreComplete` + `complete'`. -/
def InferRecGroupCoreComplete (bindings : List Expr) : Prop :=
  ∀ {Φ : Nat} {ctx : Ctx} {targets : List Ty} {Φ' : Nat} {S : Subst}
    {bindingsOut : List Expr} (K : List Nat),
    CtxWF ctx → CtxBelow Φ ctx → (∀ τ ∈ targets, τ.IsLC) → (∀ τ ∈ targets, Ty.BelowFvars Φ τ) →
    (∀ k ∈ K, k < Φ) → (∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K) →
    (∀ p ∈ S, p.1 ∉ K) →
    InferRecGroup Φ ctx bindings targets Φ' S bindingsOut →
    (inferRecGroupCore K Φ ctx bindings targets).isSome

theorem inferRecGroupCore_complete : ∀ (bindings : List Expr),
    (∀ e ∈ bindings, InferCoreComplete e) → InferRecGroupCoreComplete bindings := by
  intro bindings
  induction bindings with
  | nil =>
    intro _ Φ ctx targets Φ' S bindingsOut K _ _ _ _ _ _ _ h
    cases h
    simp [inferRecGroupCore]
  | cons e rest ih =>
    intro ihbr
    intro Φ ctx targets Φ' S bindingsOut K hwf hbelow htlc htbelow hKΦ hKbr hSK h
    obtain ⟨β, βs, rfl⟩ : ∃ β βs, targets = β :: βs := by cases h with | cons => exact ⟨_, _, rfl⟩
    have hKbody : ∀ y ∈ e.tyFreeVars, y ∈ K := fun y hy => hKbr y (by
      simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inl hy)
    have hKrest : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars rest, y ∈ K := fun y hy => hKbr y (by
      simp only [Expr.tyFreeVars.RecGroup.tyFreeVars, List.mem_append]; exact Or.inr hy)
    have hKfixS : ∀ k ∈ K, S.onTy (.fvar k) = .fvar k := fun k hk => Subst.fixes_fvar_of_avoids hSK hk
    have hβlc : β.IsLC := htlc β List.mem_cons_self
    have hbβ : Ty.BelowFvars Φ β := htbelow β List.mem_cons_self
    have hlenrest : rest.length = βs.length := by have := InferRecGroup.length_eq h; simpa using this
    have hsound := InferRecGroup.sourceSound h hwf hbelow htlc htbelow K hKΦ hKbr hSK
    have hSlc : ∀ p ∈ S, p.2.IsLC := InferRecGroup.lc h hwf htlc
    have hbodyty : TypeOfHM (S.onCtx ctx) e (S.onTy β) :=
      hsound (e, S.onTy β) (by rw [List.map_cons, List.zip_cons_cons]; exact List.mem_cons_self)
    obtain ⟨_, _, _, _, _, hederiv, _, _, _, _, hSeK⟩ :=
      Infer.completeAt e K hwf hbelow hSlc hKΦ hKbody hKfixS hbodyty
    have hesome := (ihbr e List.mem_cons_self) K hwf hbelow hKΦ hKbody hSeK hederiv
    obtain ⟨⟨⟨Φ_e, S_e, eOut_e, τ_e⟩, hinfe, _⟩, heinfer⟩ := Option.isSome_iff_exists.mp hesome
    obtain ⟨R_e, hage, htye, hR_e, hR_eK⟩ :=
      Infer.complete' hinfe hwf hbelow hSlc K hKΦ hKbody hKfixS hbodyty
    have hτe_lc : τ_e.IsLC := (Infer.lc hinfe hwf).1
    have hS_e : ∀ p ∈ S_e, p.2.IsLC := (Infer.lc hinfe hwf).2
    have hbe := Infer.belowFvars hinfe hbelow (fun y hy => hKΦ y (hKbody y hy))
    have hle_e := Infer.frontier_le hinfe
    have hUni : Unifies R_e τ_e (S_e.onTy β) := by
      show R_e.onTy τ_e = R_e.onTy (S_e.onTy β)
      rw [← htye, ← Subst.onTy_append]
      exact Subst.onTy_congr hage hbβ
    have huniSome : (unifyCoreK K τ_e (S_e.onTy β)).isSome :=
      unifyCoreK_complete hτe_lc (Subst.onTy_lc hS_e hβlc) hR_e hUni hR_eK
    obtain ⟨⟨S_u, h_u, _⟩, heuni⟩ := Option.isSome_iff_exists.mp huniSome
    obtain ⟨R_u, hR_u_eq, hR_u, hR_uK⟩ := UnifyRel.greatest_K h_u R_e hR_e hUni hR_eK
    have hS_u : ∀ p ∈ S_u, p.2.IsLC := UnifyRel.lc h_u hτe_lc (Subst.onTy_lc hS_e hβlc)
    have hS_u_below : ∀ p ∈ S_u, Ty.BelowFvars Φ_e p.2 :=
      UnifyRel.belowFvars h_u hbe.1 (Subst.onTy_belowFvars hbe.2 (hbβ.mono hle_e))
    have hwf' : CtxWF (S_u.onCtx (S_e.onCtx ctx)) := Subst.onCtx_wf hS_u (Subst.onCtx_wf hS_e hwf)
    have hbelow_Se : CtxBelow Φ_e (S_e.onCtx ctx) := Subst.onCtx_below hbe.2 hle_e hbelow
    have hbelow' : CtxBelow Φ_e (S_u.onCtx (S_e.onCtx ctx)) :=
      Subst.onCtx_below hS_u_below (le_refl _) hbelow_Se
    have key_t : ∀ {t : Ty}, Ty.BelowFvars Φ t → R_u.onTy (S_u.onTy (S_e.onTy t)) = S.onTy t := by
      intro t ht
      rw [← hR_u_eq (S_e.onTy t), ← Subst.onTy_append]
      exact (Subst.onTy_congr hage ht).symm
    have htlc' : ∀ τ ∈ βs.map (fun b => S_u.onTy (S_e.onTy b)), τ.IsLC := by
      intro τ hτ; obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hτ
      exact Subst.onTy_lc hS_u (Subst.onTy_lc hS_e (htlc b (List.mem_cons_of_mem _ hb)))
    have htbelow' : ∀ τ ∈ βs.map (fun b => S_u.onTy (S_e.onTy b)), Ty.BelowFvars Φ_e τ := by
      intro τ hτ; obtain ⟨b, hb, rfl⟩ := List.mem_map.mp hτ
      exact Subst.onTy_belowFvars hS_u_below
        (Subst.onTy_belowFvars hbe.2 ((htbelow b (List.mem_cons_of_mem _ hb)).mono hle_e))
    have hctx_eq : R_u.onCtx (S_u.onCtx (S_e.onCtx ctx)) = S.onCtx ctx := by
      rw [← Subst.onCtx_comp_of_onTy_eq hR_u_eq, ← Subst.onCtx_append]
      exact Subst.onCtx_congr (fun v hv => (hage v hv).symm) hbelow
    have hmapeq : (βs.map (fun b => S_u.onTy (S_e.onTy b))).map R_u.onTy = βs.map S.onTy := by
      rw [List.map_map]; apply List.map_congr_left; intro b hb
      exact key_t (htbelow b (List.mem_cons_of_mem _ hb))
    have hdecl' : ∀ p ∈ rest.zip ((βs.map (fun b => S_u.onTy (S_e.onTy b))).map R_u.onTy),
        TypeOfHM (R_u.onCtx (S_u.onCtx (S_e.onCtx ctx))) p.1 p.2 := by
      rw [hmapeq, hctx_eq]
      intro p hp
      exact hsound p (by rw [List.map_cons, List.zip_cons_cons]; exact List.mem_cons_of_mem _ hp)
    obtain ⟨Φ_rr, S_r, bsOut_rr, R_r, hinfrest, _, _, _, hS_rK⟩ :=
      InferRecGroup.complete (bindings := rest)
        (targets := βs.map (fun b => S_u.onTy (S_e.onTy b))) (R := R_u) K
        (by rw [List.length_map]; exact hlenrest) (fun e' _ => Infer.completeAt e')
        hwf' hbelow' htlc' htbelow' hR_u
        (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle_e) hKrest hR_uK hdecl'
    have hrestsome := ih (fun e' he' => ihbr e' (List.mem_cons_of_mem _ he')) K
      hwf' hbelow' htlc' htbelow'
      (fun k hk => lt_of_lt_of_le (hKΦ k hk) hle_e) hKrest hS_rK hinfrest
    obtain ⟨⟨⟨Φ_r2, S_r2, bsOut_r2⟩, hrest', _⟩, herest⟩ := Option.isSome_iff_exists.mp hrestsome
    rw [inferRecGroupCore]
    simp only [heinfer, heuni, herest, Option.isSome_some]

theorem inferCore_complete_match {scrut : Expr} {branches : List (MatchPattern × Expr)}
    (ihscrut : InferCoreComplete scrut)
    (ihbranches : ∀ pat e, (pat, e) ∈ branches → InferCoreComplete e) :
    InferCoreComplete (.match_ scrut branches) := by
  intro Φ ctx Φ' S eOut τ K hwf hbelow hKΦ hKe hSK h
  have happ := Infer.sourceSound h hwf hbelow K hKΦ hKe hSK
  have hSlc : ∀ p ∈ S, p.2.IsLC := (Infer.lc h hwf).2
  cases h with
  | @match_ _ _ _ _ Φ1d Φ2d S1d S2d _ _ τsd hscrutderiv hne hbranchesderiv =>
    cases happ with
    | match_ hscrut_decl hne' hbranches_decl =>
      expose_names
      set τ := S2d.onTy (Ty.fvar Φ1d) with hτdef
      obtain ⟨b0, brest, hbeq⟩ := List.exists_cons_of_ne_nil hne'
      subst hbeq
      simp only [Expr.tyFreeVars, List.mem_append] at hKe
      have hKescrut : ∀ y ∈ scrut.tyFreeVars, y ∈ K := fun y hy => hKe y (.inl hy)
      have hKbr : ∀ y ∈ Expr.tyFreeVars.BranchList.tyFreeVars (b0 :: brest), y ∈ K :=
        fun y hy => hKe y (.inr hy)
      have hKfixS : ∀ k ∈ K, (S1d ++ S2d).onTy (.fvar k) = .fvar k :=
        fun k hk => Subst.fixes_fvar_of_avoids hSK hk
      have hSK₁ : ∀ p ∈ S1d, p.1 ∉ K := fun p hp => hSK p (List.mem_append_left _ hp)
      have hτ_lc : τ.IsLC :=
        TypeOfMatchBranch.regular (hbranches_decl b0 (List.mem_cons_self ..))
      -- STEP 1: scrutinee function IH, then factor via `complete'`.
      have hsscrut := ihscrut K hwf hbelow hKΦ hKescrut hSK₁ hscrutderiv
      obtain ⟨⟨⟨Φ₁, S₁, eOuts, τs⟩, hinfs, _⟩, hescrut⟩ := Option.isSome_iff_exists.mp hsscrut
      obtain ⟨R₁, hags, htys, hR₁, hR₁K⟩ :=
        Infer.complete' hinfs hwf hbelow hSlc K hKΦ hKescrut hKfixS hscrut_decl
      have hS₁ : ∀ p ∈ S₁, p.2.IsLC := (Infer.lc hinfs hwf).2
      have hτsLC : τs.IsLC := (Infer.lc hinfs hwf).1
      have hbs : Ty.BelowFvars Φ₁ τs ∧ ∀ p ∈ S₁, Ty.BelowFvars Φ₁ p.2 :=
        Infer.belowFvars hinfs hbelow (fun y hy => hKΦ y (hKescrut y hy))
      have hle1 : Φ ≤ Φ₁ := Infer.frontier_le hinfs
      have hwf₁ : CtxWF (S₁.onCtx ctx) := Subst.onCtx_wf hS₁ hwf
      have hbelow₁ : CtxBelow Φ₁ (S₁.onCtx ctx) := Subst.onCtx_below hbs.2 hle1 hbelow
      have hctxeq : R₁.onCtx (S₁.onCtx ctx) = (S1d ++ S2d).onCtx ctx := by
        rw [← Subst.onCtx_append]
        exact Subst.onCtx_congr (fun v hv => (hags v hv).symm) hbelow
      -- STEP 2: fresh result-target `W`, residual `U` (= `Φ₁ ↦ τ`).
      obtain ⟨W, hWge, hWfresh⟩ := exists_fresh_block
        (R₁.map Prod.fst ++ R₁.flatMap (fun p => p.2.freeVars) ++ τ.freeVars) Φ₁ 1
      have hWdom : ∀ p ∈ R₁, p.1 ≠ W := by
        intro p hp he
        have := hWfresh p.1 (List.mem_append_left _ (List.mem_append_left _ (List.mem_map.mpr ⟨p, hp, rfl⟩)))
        omega
      have hWrange : ∀ p ∈ R₁, W ∉ p.2.freeVars := by
        intro p hp hc
        have := hWfresh W (List.mem_append_left _ (List.mem_append_right _ (List.mem_flatMap.mpr ⟨p, hp, hc⟩)))
        omega
      have hR₁Wfvar : R₁.onTy (Ty.fvar W) = Ty.fvar W := by
        apply Ty.substFvars_eq_self_of_no_key
        intro p hp hc
        simp only [Ty.freeVars, List.mem_singleton] at hc
        exact hWdom p hp hc
      obtain ⟨U, hUdef⟩ : ∃ U : Subst, U = [(Φ₁, Ty.fvar W)] ++ R₁ ++ [(W, τ)] := ⟨_, rfl⟩
      have hsingle : ∀ (Z : Nat) (V y : Ty), Subst.onTy [(Z, V)] y = Ty.substFvar Z V y :=
        fun _ _ _ => rfl
      have hUonTy : ∀ x, U.onTy x = Ty.substFvar W τ (R₁.onTy (Ty.substFvar Φ₁ (Ty.fvar W) x)) := by
        intro x; rw [hUdef, Subst.onTy_append, Subst.onTy_append, hsingle, hsingle]
      have hUlc : ∀ p ∈ U, p.2.IsLC := by
        rw [hUdef]; intro p hp
        rcases List.mem_append.mp hp with hp' | hp'
        · rcases List.mem_append.mp hp' with hp'' | hp''
          · obtain rfl := List.mem_singleton.mp hp''; exact ContainsBvarsUpTo.fvar
          · exact hR₁ p hp''
        · obtain rfl := List.mem_singleton.mp hp'; exact hτ_lc
      have hUeqR₁ : ∀ v, v < Φ₁ → U.onTy (Ty.fvar v) = R₁.onTy (Ty.fvar v) := by
        intro v hv
        have hvΦ₁ : Φ₁ ∉ (Ty.fvar v).freeVars := by simp only [Ty.freeVars, List.mem_singleton]; omega
        have hvW : W ∉ (R₁.onTy (Ty.fvar v)).freeVars := by
          apply Subst.not_mem_onTy_freeVars hWrange
          simp only [Ty.freeVars, List.mem_singleton]; omega
        rw [hUonTy, Ty.substFvar_fresh hvΦ₁, Ty.substFvar_fresh hvW]
      have hUΦ₁ : U.onTy (Ty.fvar Φ₁) = τ := by
        rw [hUonTy, show Ty.substFvar Φ₁ (Ty.fvar W) (Ty.fvar Φ₁) = Ty.fvar W from by simp [Ty.substFvar],
            hR₁Wfvar, show Ty.substFvar W τ (Ty.fvar W) = τ from by simp [Ty.substFvar]]
      have hUK : ∀ k ∈ K, U.onTy (Ty.fvar k) = Ty.fvar k := fun k hk =>
        (hUeqR₁ k (lt_of_lt_of_le (hKΦ k hk) hle1)).trans (hR₁K k hk)
      -- STEP 3: recast branch declarative typings under `U`.
      have hbr' : ∀ br ∈ (b0 :: brest),
          TypeOfMatchBranch (U.onCtx (S₁.onCtx ctx)) br (U.onTy τs) (U.onTy (Ty.fvar Φ₁)) := by
        intro br hbr
        rw [Subst.onCtx_congr hUeqR₁ hbelow₁, hctxeq, Subst.onTy_congr hUeqR₁ hbs.1, htys.symm, hUΦ₁]
        exact hbranches_decl br hbr
      have hbelowbr : CtxBelow (Φ₁ + 1) (S₁.onCtx ctx) :=
        Subst.onCtx_below (fun p hp => (hbs.2 p hp).mono (by omega)) (by omega) hbelow
      -- STEP 4: reconstruct a branch derivation at the function state, then drive the helper.
      obtain ⟨Φ₂, S₂, brsOut₂, R₂, hinfbr, _, _, _, hS₂K⟩ :=
        InferBranches.complete (Φ := Φ₁ + 1) (ρ := Ty.fvar Φ₁) (R := U) K
          (fun br _ => Infer.completeAt br.2) hwf₁ hbelowbr
          hτsLC (hbs.1.mono (by omega)) ContainsBvarsUpTo.fvar (.fvar (by omega))
          hUlc (fun k hk => by have := hKΦ k hk; have := hle1; omega) hKbr hUK hbr'
      have hbranchesSome := inferBranchesCore_complete (b0 :: brest)
        (fun br hbr => by obtain ⟨p, e⟩ := br; exact ihbranches p e hbr) K
        hwf₁ hbelowbr hτsLC (hbs.1.mono (by omega)) ContainsBvarsUpTo.fvar (.fvar (by omega))
        (fun k hk => by have := hKΦ k hk; have := hle1; omega) hKbr hS₂K hinfbr
      obtain ⟨⟨⟨Φ₂b, S₂b, brsOut'⟩, hbrf, _⟩, hbr_eq⟩ := Option.isSome_iff_exists.mp hbranchesSome
      -- STEP 5: conclude by unfolding `inferCore`'s `.match_` arm.
      rw [inferCore]
      simp only [hescrut, List.head?_cons, hbr_eq, Option.isSome_some]

/-- Function-completeness, `letRec` case. The group `inferRecGroupCore` runs at the
    fixed state `(Φ+n, ctxβ, β)`, so `inferRecGroupCore_complete` on the given group
    derivation shows it succeeds; the body `inferCore` runs at the group result
    state, so we factor the group via `InferRecGroup.complete'` and retype the body
    (`letRec_body_retype`) for the body IH. -/
theorem inferCore_complete_letRec {bindings : List Expr} {body : Expr}
    (ihbindings : ∀ e ∈ bindings, InferCoreComplete e) (ihbody : InferCoreComplete body) :
    InferCoreComplete (.letRec bindings body) := by
  intro Φ ctx Φ' S eOut τ K hwf hbelow hKΦ hKe hSK h
  have happ := Infer.sourceSound h hwf hbelow K hKΦ hKe hSK
  have hSlc : ∀ p ∈ S, p.2.IsLC := (Infer.lc h hwf).2
  cases h with
  | letRec hgroup hbody =>
    cases happ with
    | letRec hlen hlen2 hlc hG hgen hcofin heqc hbodyD =>
      subst heqc
      expose_names
      simp only [Expr.tyFreeVars, List.mem_append] at hKe
      have hKgrp : ∀ y ∈ Expr.tyFreeVars.RecGroup.tyFreeVars bindings, y ∈ K :=
        fun y hy => hKe y (.inl hy)
      have hKbody : ∀ y ∈ body.tyFreeVars, y ∈ K := fun y hy => hKe y (.inr hy)
      have hKrigid : ∀ y ∈ bindings.flatMap Expr.tyFreeVars, y ∈ K :=
        fun y hy => hKgrp y (Expr.mem_recGroup_tyFreeVars.mpr hy)
      have hKfixS : ∀ k ∈ K, (S₁ ++ S₂).onTy (.fvar k) = .fvar k :=
        fun k hk => Subst.fixes_fvar_of_avoids hSK hk
      have hSK₁ : ∀ p ∈ S₁, p.1 ∉ K := fun p hp => hSK p (List.mem_append_left _ hp)
      have hctxβWF : CtxWF (⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
          ++ ctx.env, ctx.ctors⟩ : Ctx) := by
        intro M hM; rcases List.mem_append.mp hM with hM | hM
        · obtain ⟨x, _, rfl⟩ := List.mem_map.mp hM; exact ContainsBvarsUpTo.fvar
        · exact hwf M hM
      have hctxβbelow : CtxBelow (Φ + bindings.length)
          (⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
            ++ ctx.env, ctx.ctors⟩ : Ctx) := by
        intro M hM; rcases List.mem_append.mp hM with hM | hM
        · obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hM
          exact Ty.BelowFvars.fvar (by have := freshVars_lt x hx; omega)
        · exact (hbelow M hM).mono (by omega)
      have hβlc : ∀ τ' ∈ (freshVars Φ bindings.length).map Ty.fvar, τ'.IsLC := by
        intro τ' hτ'; obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ'; exact ContainsBvarsUpTo.fvar
      have hβbelow : ∀ τ' ∈ (freshVars Φ bindings.length).map Ty.fvar,
          Ty.BelowFvars (Φ + bindings.length) τ' := by
        intro τ' hτ'; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ'
        exact Ty.BelowFvars.fvar (by have := freshVars_lt x hx; omega)
      -- STEP 1: the group `inferRecGroupCore` succeeds (same state as the given group).
      have hgroupsome := inferRecGroupCore_complete bindings ihbindings K hctxβWF hctxβbelow hβlc hβbelow
        (fun k hk => by have := hKΦ k hk; omega) hKgrp hSK₁ hgroup
      obtain ⟨⟨⟨Φ₁', S₁', bsOut'⟩, hgroup', _⟩, hgroupeq⟩ := Option.isSome_iff_exists.mp hgroupsome
      have hgle' : Φ + bindings.length ≤ Φ₁' := InferRecGroup.frontier_le hgroup'
      have hS₁'lc : ∀ p ∈ S₁', p.2.IsLC := InferRecGroup.lc hgroup' hctxβWF hβlc
      have hS₁'below : ∀ p ∈ S₁', Ty.BelowFvars Φ₁' p.2 :=
        InferRecGroup.belowFvars hgroup' hctxβbelow hβbelow
          (fun y hy => by have := hKΦ y (hKgrp y hy); omega)
      have hwf₁' : CtxWF (S₁'.onCtx ctx) := Subst.onCtx_wf hS₁'lc hwf
      have hbelow₁' : CtxBelow Φ₁' (S₁'.onCtx ctx) :=
        Subst.onCtx_below hS₁'below (le_trans (Nat.le_add_right Φ _) hgle') hbelow
      -- STEP 2: factor the group derivation via the residual + `InferRecGroup.complete'`.
      obtain ⟨Xs, hXlen, hXnodup, hXavoid⟩ := exists_fresh_names
        (L ++ G ++ Ty.freeVarsList τs ++ ((S₁ ++ S₂).onCtx ctx).env.freeVars
          ++ bindings.flatMap Expr.tyFreeVars) G.length
      have hXL : ∀ x ∈ Xs, x ∉ L := fun x hx hc => hXavoid x hx (by simp only [List.mem_append]; tauto)
      have hXG : ∀ x ∈ Xs, x ∉ G := fun x hx hc => hXavoid x hx (by simp only [List.mem_append]; tauto)
      have hXτs : ∀ x ∈ Xs, x ∉ Ty.freeVarsList τs := fun x hx hc =>
        hXavoid x hx (by simp only [List.mem_append]; tauto)
      have hXenv : ∀ x ∈ Xs, x ∉ ((S₁ ++ S₂).onCtx ctx).env.freeVars := fun x hx hc =>
        hXavoid x hx (by simp only [List.mem_append]; tauto)
      have hXrigid : ∀ x ∈ Xs, x ∉ bindings.flatMap Expr.tyFreeVars := fun x hx hc =>
        hXavoid x hx (by simp only [List.mem_append]; tauto)
      have hXfresh : FreshNames L G.length Xs := ⟨hXlen, hXnodup, hXL⟩
      obtain ⟨R₀, hR₀lc, hR₀K, hR₀agΦ, hR₀block⟩ :=
        exists_recgroup_residual (Φ := Φ) (n := bindings.length) (S₀ := S₁ ++ S₂)
          (vs := τs.map (Ty.renameG G Xs)) (K := K)
          (by rw [List.length_map]; omega) hSlc
          (fun t ht => by
            obtain ⟨τ', hτ', rfl⟩ := List.mem_map.mp ht
            rw [← Subst.onTy_zip_fvar_eq_renameG]
            exact Subst.onTy_lc (fun p hp => by
              obtain ⟨w, _, hw⟩ := List.mem_map.mp (List.of_mem_zip hp).2
              rw [← hw]; exact ContainsBvarsUpTo.fvar) (hlc τ' hτ'))
          hKΦ hKfixS
      have hR₀ctx : R₀.onCtx ctx = (S₁ ++ S₂).onCtx ctx := Subst.onCtx_congr hR₀agΦ hbelow
      have hR₀β : (freshVars Φ bindings.length).map (fun i => R₀.onTy (Ty.fvar i))
          = τs.map (Ty.renameG G Xs) := by
        rw [← hR₀block]; simp only [List.map_map, Function.comp_def]
      have hgrp : ∀ p ∈ bindings.zip (((freshVars Φ bindings.length).map Ty.fvar).map R₀.onTy),
          TypeOfHM (R₀.onCtx (⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
            ++ ctx.env, ctx.ctors⟩ : Ctx)) p.1 p.2 := by
        have hbmap : ((freshVars Φ bindings.length).map Ty.fvar).map R₀.onTy = τs.map (Ty.renameG G Xs) := by
          rw [List.map_map]; exact hR₀β
        have hctxβeq : R₀.onCtx (⟨(freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
              ++ ctx.env, ctx.ctors⟩ : Ctx)
            = ⟨(τs.map (Ty.renameG G Xs)).map PolyTy.mkTrivial ++ ((S₁ ++ S₂).onCtx ctx).env, ctx.ctors⟩ := by
          show (⟨((freshVars Φ bindings.length).map (fun i => PolyTy.mkTrivial (Ty.fvar i))
              ++ ctx.env).map R₀.onPolyTy, ctx.ctors⟩ : Ctx) = _
          rw [List.map_append]; congr 1; congr 1
          · rw [← hR₀β, List.map_map, List.map_map]
            apply List.map_congr_left; intro i _
            simp only [Function.comp_apply, Subst.onPolyTy, PolyTy.mkTrivial]
          · exact congrArg Ctx.env hR₀ctx
        rw [hbmap, hctxβeq]
        exact fun p hp => hcofin Xs hXfresh p hp
      obtain ⟨R₁, hag_group, hR₁lc, hR₁fix⟩ :=
        InferRecGroup.complete' hgroup' hctxβWF hctxβbelow hβlc hβbelow hR₀lc K
          (fun k hk => by have := hKΦ k hk; omega) hKgrp hR₀K hgrp
      have hctxeq : R₁.onCtx (S₁'.onCtx ctx) = (S₁ ++ S₂).onCtx ctx := by
        rw [← Subst.onCtx_append,
          Subst.onCtx_congr (fun v hv => (hag_group v (by omega)).symm) hbelow]
        exact hR₀ctx
      have hconn : ((freshVars Φ bindings.length).map (fun i => S₁'.onTy (Ty.fvar i))).map R₁.onTy
          = τs.map (Ty.renameG G Xs) := by
        rw [← hR₀β, List.map_map]
        apply List.map_congr_left; intro i hi
        simp only [Function.comp_apply]
        rw [← Subst.onTy_append]
        exact (hag_group i (freshVars_lt i hi)).symm
      have hbodyretype := letRec_body_retype hlen hlen2 hlc hG hgen hXlen hXnodup
        (fun g hg hc => hXG g hc hg) hXτs hXenv hXrigid hS₁'lc hR₁lc hctxeq hKrigid hR₁fix hconn hbodyD
      -- STEP 3: the body `inferCore` succeeds at the group result state.
      have hτsinf'_lc : ∀ τ' ∈ (freshVars Φ bindings.length).map (fun i => S₁'.onTy (Ty.fvar i)), τ'.IsLC := by
        intro τ' hτ'; obtain ⟨x, _, rfl⟩ := List.mem_map.mp hτ'
        exact Subst.onTy_lc hS₁'lc ContainsBvarsUpTo.fvar
      have hτsinf'_below : ∀ τ' ∈ (freshVars Φ bindings.length).map (fun i => S₁'.onTy (Ty.fvar i)),
          Ty.BelowFvars Φ₁' τ' := by
        intro τ' hτ'; obtain ⟨x, hx, rfl⟩ := List.mem_map.mp hτ'
        exact Subst.onTy_belowFvars hS₁'below (.fvar (by have := freshVars_lt x hx; omega))
      have hwfBodyInf : CtxWF (⟨genGroupSchemes (bindings.flatMap Expr.tyFreeVars) (S₁'.onCtx ctx).env
          ((freshVars Φ bindings.length).map (fun i => S₁'.onTy (Ty.fvar i))) ++ (S₁'.onCtx ctx).env,
          (S₁'.onCtx ctx).ctors⟩ : Ctx) := by
        intro M hM; rcases List.mem_append.mp hM with hM | hM
        · simp only [genGroupSchemes] at hM; obtain ⟨τ', hτ', rfl⟩ := List.mem_map.mp hM
          exact PolyTy.genGroup_wf (hτsinf'_lc τ' hτ')
        · exact hwf₁' M hM
      have hbelowBodyInf : CtxBelow Φ₁' (⟨genGroupSchemes (bindings.flatMap Expr.tyFreeVars)
          (S₁'.onCtx ctx).env ((freshVars Φ bindings.length).map (fun i => S₁'.onTy (Ty.fvar i)))
          ++ (S₁'.onCtx ctx).env, (S₁'.onCtx ctx).ctors⟩ : Ctx) := by
        intro M hM; rcases List.mem_append.mp hM with hM | hM
        · simp only [genGroupSchemes] at hM; obtain ⟨τ', hτ', rfl⟩ := List.mem_map.mp hM
          exact (hτsinf'_below τ' hτ').closeOver
        · exact hbelow₁' M hM
      obtain ⟨_, _, _, _, _, hbodyderiv, _, _, _, _, hSbK⟩ :=
        Infer.completeAt body K hwfBodyInf hbelowBodyInf hR₁lc
          (fun k hk => lt_of_lt_of_le (hKΦ k hk) (le_trans (Nat.le_add_right Φ _) hgle'))
          hKbody hR₁fix hbodyretype
      have hbodysome := ihbody K hwfBodyInf hbelowBodyInf
        (fun k hk => lt_of_lt_of_le (hKΦ k hk) (le_trans (Nat.le_add_right Φ _) hgle'))
        hKbody hSbK hbodyderiv
      obtain ⟨⟨⟨Φ₂', S₂', eOut₂', τ₂'⟩, hinfbody', _⟩, hbodyeq⟩ := Option.isSome_iff_exists.mp hbodysome
      rw [inferCore]
      simp only [hgroupeq, hbodyeq, Option.isSome_some]

/-- **`inferCore` completeness, assembled.** For every expression, a (WF,
    frontier-bounded) `Infer` derivation entails that `inferCore` succeeds. -/
theorem inferCore_complete : ∀ e, InferCoreComplete e := by
  -- Strong induction on `e.size`: the annotated-`let` case needs completeness of
  -- the *opened* rhs `rhs.openTyVars Ys`, which is not a structural subterm but has
  -- the same size (`Expr.size_openTyVars`).
  suffices h : ∀ n, ∀ e : Expr, e.size ≤ n → InferCoreComplete e by
    exact fun e => h e.size e (le_refl _)
  intro n
  induction n with
  | zero => intro e he; exfalso; cases e <;> simp only [Expr.size] at he <;> omega
  | succ n ih =>
    intro e he
    cases e with
    | primLit p => exact inferCore_complete_prim
    | lambda ann body => exact inferCore_complete_lambda (ih body (by simp only [Expr.size] at he; omega))
    | app f arg =>
      exact inferCore_complete_app (ih f (by simp only [Expr.size] at he; omega))
        (ih arg (by simp only [Expr.size] at he; omega))
    | letIn ann be body =>
      refine inferCore_complete_letIn (ih be (by simp only [Expr.size] at he; omega))
        (fun Ys => ih (be.openTyVars Ys)
          (by rw [Expr.size_openTyVars]; simp only [Expr.size] at he; omega))
        (ih body (by simp only [Expr.size] at he; omega))
    | var i tyArgs => exact inferCore_complete_var
    | ctor name => exact inferCore_complete_ctor
    | match_ scrut branches =>
      refine inferCore_complete_match (ih scrut (by simp only [Expr.size] at he; omega)) ?_
      intro pat e' hbr
      refine ih e' ?_
      have hb : ∀ (br : MatchPattern × Expr) (bs : List (MatchPattern × Expr)),
          br ∈ bs → br.2.size ≤ Expr.sizeBranches bs := by
        intro br bs
        induction bs with
        | nil => intro hbs; simp at hbs
        | cons hd tl ihtl =>
          intro hbs
          obtain ⟨p2, b2⟩ := hd
          simp only [Expr.sizeBranches]
          rcases List.mem_cons.mp hbs with h | h
          · have hbb : br.2.size = b2.size := by rw [h]
            omega
          · have := ihtl h; omega
      have hble : e'.size ≤ Expr.sizeBranches branches := hb (pat, e') branches hbr
      simp only [Expr.size] at he; omega
    | letRec bindings body =>
      refine inferCore_complete_letRec ?_ (ih body (by simp only [Expr.size] at he; omega))
      intro e' hbr
      refine ih e' ?_
      have hb : ∀ (bs : List Expr), e' ∈ bs → e'.size ≤ Expr.sizeRecGroup bs := by
        intro bs
        induction bs with
        | nil => intro hbs; simp at hbs
        | cons hd tl ihtl =>
          intro hbs
          simp only [Expr.sizeRecGroup]
          rcases List.mem_cons.mp hbs with h | h
          · subst h; omega
          · have := ihtl h; omega
      have hble : e'.size ≤ Expr.sizeRecGroup bindings := hb bindings hbr
      simp only [Expr.size] at he; omega
    | letRecAnn schemes bindings body =>
      -- RESERVED: executable completeness for annotated recursion groups depends on
      -- the (reserved) `inferRecGroupAnnCore`-completeness; kept `sorry` for the design pass.
      sorry

/-- **`infer` completeness.** Algorithm W succeeds whenever the expression is
    declaratively typeable under a well-formed, frontier-bounded context. -/
theorem infer_complete {Φ : Nat} {ctx : Ctx} {e : Expr} {Φ' : Nat} {S : Subst} {eOut : Expr} {τ : Ty}
    (h : Infer Φ ctx e Φ' S eOut τ) (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx)
    (hclosed : e.tyFreeVars = []) :
    (infer Φ ctx e).isSome := by
  have := inferCore_complete e [] hwf hbelow (by simp) (by rw [hclosed]; simp) (by simp) h
  simpa [infer, Option.isSome_map] using this


/-! ### Sound + complete, packaged as equivalences

Bundling each refinement into a single `↔`: the executable succeeds exactly when
the corresponding relation is inhabited, and (composing with `Infer.iff_typeable`)
exactly when the expression is declaratively typeable. -/

/-- `unify` succeeds iff the (LC) monotypes are unifiable. -/
theorem unify_iff {a b : Ty} (ha : a.IsLC) (hb : b.IsLC) :
    (unify a b).isSome ↔ ∃ S, UnifyRel a b S := by
  constructor
  · intro h
    obtain ⟨S, hS⟩ := Option.isSome_iff_exists.mp h
    exact ⟨S, unify_sound hS⟩
  · rintro ⟨S, hS⟩
    exact unify_complete hS ha hb

/-- `infer` succeeds iff the `Infer` relation is inhabited (for a well-formed,
    frontier-bounded context). -/
theorem infer_iff {Φ : Nat} {ctx : Ctx} {e : Expr}
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hclosed : e.tyFreeVars = []) :
    (infer Φ ctx e).isSome ↔ ∃ Φ' S eOut τ, Infer Φ ctx e Φ' S eOut τ := by
  constructor
  · intro h
    obtain ⟨⟨Φ', S, eOut, τ⟩, he⟩ := Option.isSome_iff_exists.mp h
    exact ⟨Φ', S, eOut, τ, infer_sound he⟩
  · rintro ⟨Φ', S, eOut, τ, hInfer⟩
    exact infer_complete hInfer hwf hbelow hclosed

/-- **The executable typechecker decides typeability.** `infer` succeeds iff `e`
    is declaratively typeable under some LC specialization of `ctx` — the
    full-circle bridge from the executable function all the way to `TypeOfHM`. -/
theorem infer_iff_typeable {Φ : Nat} {ctx : Ctx} {e : Expr}
    (hwf : CtxWF ctx) (hbelow : CtxBelow Φ ctx) (hclosed : e.tyFreeVars = []) :
    (infer Φ ctx e).isSome ↔
      ∃ (S : Subst) (τ : Ty), (∀ p ∈ S, p.2.IsLC) ∧ TypeOfHM (S.onCtx ctx) e τ := by
  rw [infer_iff hwf hbelow hclosed]
  exact (Infer.iff_typeable hwf hbelow hclosed).symm


/-! ### Whole-program typechecking

For a *closed* program — context `⟨[], ctors⟩` with an empty term-variable
environment — the algorithmic side-conditions (`CtxWF`, `CtxBelow 0`) hold
vacuously, and a substitution does nothing to an empty env (`Subst.onCtx_empty`).
So the leaky `Φ`/`S` machinery disappears from the statements entirely.

`principalType` keeps just the inferred monotype (sound, principal, decides
typeability — `principalType_*`). `typecheck` then **generalizes** it: at an
empty environment every remaining free type variable is generalizable, so the
output is a genuine *closed* type scheme — no free type variables, no dangling
bound variables (`typecheck_closed`). `typecheck` is the intended entry point;
everything else is in service of it, up to the declarative `TypeOfHM`. -/

theorem CtxWF.empty {ctors : CtorEnv} : CtxWF ⟨[], ctors⟩ := by
  intro M hM; simp at hM

theorem CtxBelow.empty {Φ : Nat} {ctors : CtorEnv} : CtxBelow Φ ⟨[], ctors⟩ := by
  intro M hM; simp at hM

@[simp] theorem Subst.onCtx_empty {S : Subst} {ctors : CtorEnv} :
    S.onCtx ⟨[], ctors⟩ = ⟨[], ctors⟩ := rfl

/-- A type with no free vars at all (`NoFreeVars`) is exactly one whose `freeVars`
    list is uninhabited (the converse of `NoFreeVars.not_mem_freeVars`). -/
theorem NoFreeVars.of_forall_not_mem {τ : Ty} (h : ∀ Z, Z ∉ τ.freeVars) :
    NoFreeVars τ := by
  induction τ using Ty.rec_strong with
  | prim p => exact .prim
  | bvar i => exact .bvar
  | fvar n => exact absurd (by simp [Ty.freeVars]) (h n)
  | arrow a b iha ihb =>
      refine .arrow (iha ?_) (ihb ?_)
      · intro Z hZ; exact h Z (List.mem_dedup.mpr (List.mem_append.mpr (Or.inl hZ)))
      · intro Z hZ; exact h Z (List.mem_dedup.mpr (List.mem_append.mpr (Or.inr hZ)))
  | customTy nm tys ih =>
      refine .customTy fun t ht => ih t ht ?_
      intro Z hZ; exact h Z (mem_TyList_freeVars.mpr ⟨t, ht, hZ⟩)

/-- A strict upper bound for a list of `fvar` indices: every member is `< this`. -/
def tyVarCeil : List Nat → Nat
  | [] => 0
  | x :: xs => max (x + 1) (tyVarCeil xs)

theorem lt_tyVarCeil {y : Nat} {L : List Nat} (h : y ∈ L) : y < tyVarCeil L := by
  induction L with
  | nil => simp at h
  | cons x xs ih =>
    rcases List.mem_cons.mp h with rfl | h
    · exact Nat.lt_of_lt_of_le (Nat.lt_succ_self _) (Nat.le_max_left _ _)
    · exact Nat.lt_of_lt_of_le (ih h) (Nat.le_max_right _ _)

/-- The fresh-variable floor for `e`: a frontier strictly above every free type
    variable occurring in `e`'s annotations. Inference is started here (rather
    than at `0`) so those in-scope type variables are never re-minted as fresh,
    and — paired with the rigid set `K := e.tyFreeVars` — they stay *rigid*
    (the rigidity-aware `unifyCoreK` refuses to bind them). -/
def Expr.freshFloor (e : Expr) : Nat := tyVarCeil e.tyFreeVars

theorem Expr.lt_freshFloor {e : Expr} {y : Nat} (h : y ∈ e.tyFreeVars) :
    y < e.freshFloor := lt_tyVarCeil h

/-- The principal *monotype* of a program: run Algorithm W from the empty
    environment, keeping just the resulting type (the inferer's `Φ`/`S` are
    internal). `typecheck` generalizes this into a closed scheme.

    Inference seeds the rigid set with `e.tyFreeVars` and the frontier with
    `e.freshFloor`, so any free type variable appearing in a top-level annotation
    is treated as a **rigid scoped constant** (it is never bound by unification).
    For a *closed* program this is exactly `inferCore [] 0 …`. -/
def principalType (ctors : CtorEnv) (e : Expr) : Option Ty :=
  (inferCore e.tyFreeVars e.freshFloor ⟨[], ctors⟩ e).map (·.val.2.2.2)

/-- Monotype soundness: a computed principal type is a genuine declarative type.
    Holds for *any* `e`: a free top-level annotation variable is kept rigid (in
    `K := e.tyFreeVars`), so the inferred substitution never binds it. -/
theorem principalType_sound {ctors : CtorEnv} {e : Expr} {τ : Ty}
    (h : principalType ctors e = some τ) :
    TypeOfHM ⟨[], ctors⟩ e τ := by
  rw [principalType] at h
  rcases hc : inferCore e.tyFreeVars e.freshFloor ⟨[], ctors⟩ e with _ | ⟨⟨Φ', S, eOut, τ'⟩, hInfer, hSK⟩ <;>
    rw [hc] at h
  · simp at h
  · simp only [Option.map_some, Option.some.injEq] at h
    subst h
    exact Infer.sourceSound hInfer CtxWF.empty (fun M hM => by simp at hM)
      e.tyFreeVars (fun k hk => Expr.lt_freshFloor hk) (fun y hy => hy) hSK

/-- Monotype principality: every declarative type of the program is a
    substitution instance of the computed principal type. -/
theorem principalType_principal {ctors : CtorEnv} {e : Expr} {τ : Ty}
    (h : principalType ctors e = some τ) :
    ∀ τ₀, TypeOfHM ⟨[], ctors⟩ e τ₀ → ∃ R : Subst, τ₀ = R.onTy τ := by
  rw [principalType] at h
  rcases hc : inferCore e.tyFreeVars e.freshFloor ⟨[], ctors⟩ e with _ | ⟨⟨Φ', S, eOut, τ'⟩, hInfer, hSK⟩ <;>
    rw [hc] at h
  · simp at h
  · simp only [Option.map_some, Option.some.injEq] at h
    subst h
    intro τ₀ hτ₀
    obtain ⟨R, _, hτeq, _, _⟩ := Infer.complete' hInfer CtxWF.empty (fun M hM => by simp at hM)
      (S₀ := []) (by simp) e.tyFreeVars (fun k hk => Expr.lt_freshFloor hk) (fun y hy => hy)
      (fun k _ => by simp) (by rw [Subst.onCtx_nil]; exact hτ₀)
    exact ⟨R, hτeq⟩

/-- Monotype decidability: `principalType` succeeds iff the program is typeable. -/
theorem principalType_iff {ctors : CtorEnv} {e : Expr} :
    (principalType ctors e).isSome ↔ ∃ τ, TypeOfHM ⟨[], ctors⟩ e τ := by
  constructor
  · intro h
    obtain ⟨τ, hτ⟩ := Option.isSome_iff_exists.mp h
    exact ⟨τ, principalType_sound hτ⟩
  · rintro ⟨τ, hτ⟩
    rw [principalType, Option.isSome_map]
    obtain ⟨Φ', S, eOut, τ', R, hInfer, _, _, _, _, hSK⟩ :=
      Infer.completeAt e (Φ := e.freshFloor) (ctx := ⟨[], ctors⟩) (S₀ := []) e.tyFreeVars
        CtxWF.empty (fun M hM => by simp at hM) (by simp)
        (fun k hk => Expr.lt_freshFloor hk) (fun y hy => hy) (fun k _ => by simp)
        (by rw [Subst.onCtx_nil]; exact hτ)
    exact inferCore_complete e (ctx := ⟨[], ctors⟩) e.tyFreeVars CtxWF.empty
      (fun M hM => by simp at hM)
      (fun k hk => Expr.lt_freshFloor hk) (fun y hy => hy) hSK hInfer

/-- **Type-check a closed program** — the intended entry point. Run Algorithm W
    from the empty environment and *generalize* the result into a closed type
    scheme. At an empty environment every remaining free type variable is
    generalizable, so the output is always a genuine closed scheme. -/
def typecheck (ctors : CtorEnv) (e : Expr) : Option PolyTy :=
  (principalType ctors e).map (genScheme [] [])

/-- **`typecheck`'s output is a genuine closed type scheme.** Its body contains
    no free type variables (`NoFreeVars`) and no dangling bound variables
    (`PolyTy.WF` — every `bvar` is bound by the scheme's own quantifier). So a
    successful `typecheck` always yields a concrete (possibly polymorphic) type:
    never a leftover unification variable, never a naked bound variable. -/
theorem typecheck_closed {ctors : CtorEnv} {e : Expr} {σ : PolyTy}
    (h : typecheck ctors e = some σ) : NoFreeVars σ.body ∧ σ.WF := by
  rw [typecheck, principalType] at h
  rcases hi : inferCore e.tyFreeVars e.freshFloor ⟨[], ctors⟩ e with _ | ⟨⟨Φ', S, eOut, τ⟩, hInfer, hSK⟩ <;>
    rw [hi] at h
  · simp at h
  · simp only [Option.map_some, Option.some.injEq] at h
    subst h
    have hlc : τ.IsLC := (Infer.lc hInfer CtxWF.empty).1
    refine ⟨?_, genScheme_wf hlc⟩
    apply NoFreeVars.of_forall_not_mem
    intro Z hZ
    have hsub : Z ∈ τ.freeVars := Ty.closeOver_freeVars_subset hZ
    have hin : Z ∈ genVars [] [] τ := List.mem_filter.mpr ⟨hsub, rfl⟩
    exact Ty.not_mem_closeOver_freeVars hin hZ

/-- Whole-program soundness: a successful `typecheck` generalizes a genuine
    declarative type of the program. -/
theorem typecheck_sound {ctors : CtorEnv} {e : Expr} {σ : PolyTy}
    (h : typecheck ctors e = some σ) :
    ∃ τ, TypeOfHM ⟨[], ctors⟩ e τ ∧ σ = genScheme [] [] τ := by
  rw [typecheck] at h
  rcases hc : principalType ctors e with _ | τ <;> rw [hc] at h
  · simp at h
  · simp only [Option.map_some, Option.some.injEq] at h
    exact ⟨τ, principalType_sound hc, h.symm⟩

/-- Whole-program principality: the output scheme generalizes a principal monotype
    `τ` of which every declarative type of the program is a substitution
    instance. -/
theorem typecheck_principal {ctors : CtorEnv} {e : Expr} {σ : PolyTy}
    (h : typecheck ctors e = some σ) :
    ∃ τ, σ = genScheme [] [] τ ∧ TypeOfHM ⟨[], ctors⟩ e τ ∧
      ∀ τ₀, TypeOfHM ⟨[], ctors⟩ e τ₀ → ∃ R : Subst, τ₀ = R.onTy τ := by
  rw [typecheck] at h
  rcases hc : principalType ctors e with _ | τ <;> rw [hc] at h
  · simp at h
  · simp only [Option.map_some, Option.some.injEq] at h
    exact ⟨τ, h.symm, principalType_sound hc, principalType_principal hc⟩

/-- Whole-program decidability: `typecheck` succeeds iff the program is
    declaratively typeable. -/
theorem typecheck_iff {ctors : CtorEnv} {e : Expr} :
    (typecheck ctors e).isSome ↔ ∃ τ, TypeOfHM ⟨[], ctors⟩ e τ := by
  simp only [typecheck, Option.isSome_map]
  exact principalType_iff

-- (The whole-program progress/preservation theorems and the type-erasure helper
-- lemmas that lived here have been removed: the type-passing Core has no erasure
-- layer, so `eraseTyAnnots`/`IsTyErased`/`erased_type_safety`/`erase_preserves_typing`
-- no longer exist. Whole-program safety is now the literal `TypeOfHM.type_safety`
-- chain in `Core` (no erasure premise).)

-- `typecheck [] (λx. x) = some ⟨1, bvar 0 → bvar 0⟩`  (i.e. the closed scheme `∀a. a → a`)
#eval (typecheck [] (.lambda none (.var 0 []))).map (fun σ => (σ.paramCount, σ.body))
-- `typecheck [] (5 5) = none`
#eval (typecheck [] (.app (.primLit (.int 5)) (.primLit (.int 5)))).map (fun σ => (σ.paramCount, σ.body))



-- `infer (λx. x) = α → α`
#eval infer 0 { env := [], ctors := [] } (.lambda none (.var 0 []))
-- `infer (λx. λy. x) = α → β → α`
#eval infer 0 { env := [], ctors := [] } (.lambda none (.lambda none (.var 1 [])))
-- `infer ((λx. x) 5) = Int`
#eval infer 0 { env := [], ctors := [] } (.app (.lambda none (.var 0 [])) (.primLit (.int 5)))
-- annotated param: `infer (λ(x : Int). x) = Int → Int`
#eval infer 0 { env := [], ctors := [] } (.lambda (some (.prim .int)) (.var 0 []))
-- free annotation var `λ(x : α). x` ⇒ `α → α` (α is treated as a scoped/rigid
-- type variable by the rigidity-aware inferer — sound: the result type carries α free)
#eval infer 0 { env := [], ctors := [] } (.lambda (some (.fvar 5)) (.var 0 []))

/-! ### Acceptance tests for annotated `let` (threading design) -/

-- THE WITNESS: `λx. let f : Int = x in f`  ⇒  `Int → Int`
-- (the annotation `f : Int` refines the outer param `x : α` via threading `α := Int`)
#eval infer 0 { env := [], ctors := [] }
  (.lambda none (.letIn (some ⟨0, .prim .int⟩) (.var 0 []) (.var 0 [])))
-- `let f : Int → Int = (λx. x) in f`  ⇒  `Int → Int`  (less general than principal, valid)
#eval infer 0 { env := [], ctors := [] }
  (.letIn (some ⟨0, .arrow (.prim .int) (.prim .int)⟩) (.lambda none (.var 0 [])) (.var 0 []))
-- `let id : ∀a. a → a = (λx. x) in id`  ⇒  `α → α`  (exact principal, valid)
#eval infer 0 { env := [], ctors := [] }
  (.letIn (some ⟨1, .arrow (.bvar 0) (.bvar 0)⟩) (.lambda none (.var 0 [])) (.var 0 []))
-- over-general: `let f : ∀a b. a → b = (λx. x) in f`  ⇒  `none`  (skolem escape ⇒ rejected)
#eval infer 0 { env := [], ctors := [] }
  (.letIn (some ⟨2, .arrow (.bvar 0) (.bvar 1)⟩) (.lambda none (.var 0 [])) (.var 0 []))
-- unannotated `let f = λx. x in f`  ⇒  `α → α`  (full generalization, unchanged)
#eval infer 0 { env := [], ctors := [] }
  (.letIn none (.lambda none (.var 0 [])) (.var 0 []))

/-! ### Rigid top-level type variables (the `principalType`/`typecheck` entry seeds
    `K := e.tyFreeVars`, so an unbound annotation var is a rigid scoped constant). -/

-- open identity `λ(x : α). x`  ⇒  `some (α → α)`  (α kept rigid; this IS declaratively typeable)
#eval principalType [] (.lambda (some (.fvar 5)) (.var 0 []))
-- open misuse `(λ(x : α). x) 5`  ⇒  `none`  (forcing α := Int would bind the rigid var —
-- rejected; declaratively untypeable. `infer`/`inferCore []` used to wrongly return `some Int`.)
#eval principalType [] (.app (.lambda (some (.fvar 5)) (.var 0 [])) (.primLit (.int 5)))
-- closed program: seeding is a no-op (`K = []`, floor `= 0`)  ⇒  `∀a. a → a`
#eval (typecheck [] (.lambda none (.var 0 []))).map (fun σ => (σ.paramCount, σ.body))


/-! ## Audit capstone: the headlines fire on concrete programs

A vacuous theorem (an unsatisfiable premise) cannot be *instantiated* to produce a
positive result, so the most convincing anti-vacuity check is to drive the public
pipeline end-to-end on concrete inputs. Because `inferCore`/`unifyCoreK` use
well-founded recursion (they do not reduce by `rfl`), we witness "`typecheck`
succeeds" by building the *declarative* `TypeOfHM` derivation and crossing the `↔`
headlines — never `decide`/`native_decide`. -/

namespace AuditCapstone

/-! ### A typeable program — all three headlines fire -/

/-- `λx. x`. -/
def polyId : Expr := .lambda none (.var 0 [])

theorem polyId_typeable : TypeOfHM ⟨[], []⟩ polyId (.arrow (.fvar 0) (.fvar 0)) :=
  TypeOfHM.lambda .fvar (fun _ h => Option.noConfusion h) rfl
    (TypeOfHM.var (instArgs := []) rfl (by intro t ht; cases ht) .fvar)

/-- `typecheck` succeeds, produces a genuine declarative type (soundness), and that
    type is principal — instantiated on a real program, with no escape hatch. -/
theorem polyId_headlines_fire :
    ∃ σ τ, typecheck [] polyId = some σ ∧ σ = genScheme [] [] τ ∧
      TypeOfHM ⟨[], []⟩ polyId τ ∧
      ∀ τ₀, TypeOfHM ⟨[], []⟩ polyId τ₀ → ∃ R : Subst, τ₀ = R.onTy τ := by
  obtain ⟨σ, hσ⟩ := Option.isSome_iff_exists.mp (typecheck_iff.mpr ⟨_, polyId_typeable⟩)
  obtain ⟨τ, hτeq, hty, hprin⟩ := typecheck_principal hσ
  exact ⟨σ, τ, hσ, hτeq, hty, hprin⟩

/-! ### An ill-typed program — completeness's contrapositive is not vacuous -/

/-- `5 5` — applying a non-function. -/
def appFiveFive : Expr := .app (.primLit (.int 5)) (.primLit (.int 5))

theorem appFiveFive_untypeable : ¬ ∃ τ, TypeOfHM ⟨[], []⟩ appFiveFive τ := by
  rintro ⟨τ, h⟩
  cases h with
  | app hf _ => cases hf

theorem appFiveFive_rejected : ¬ (typecheck [] appFiveFive).isSome := by
  rw [typecheck_iff]; exact appFiveFive_untypeable

/-! ### Open programs — a free top-level annotation var is a RIGID scoped constant
    (the soundness fix this session). -/

/-- `λ(x : α). x`, with `α` free at the top level. -/
def openId : Expr := .lambda (some (.fvar 5)) (.var 0 [])

/-- Accepted, and sound: `α` is rigid, so the only declarative type is `α → α`. -/
theorem openId_typeable : TypeOfHM ⟨[], []⟩ openId (.arrow (.fvar 5) (.fvar 5)) :=
  TypeOfHM.lambda .fvar (fun _ h => Option.some.inj h) rfl
    (TypeOfHM.var (instArgs := []) rfl (by intro t ht; cases ht) .fvar)

/-- `(λ(x : α). x) 5` — forcing `α := Int` would bind the rigid var. Declaratively
    untypeable (`α` cannot equal `Int`); the rigidity-seeded executable correctly
    returns `none`. Before the fix, `inferCore []` wrongly returned `some Int`. -/
def openMisuse : Expr := .app openId (.primLit (.int 5))

theorem openMisuse_untypeable : ¬ ∃ τ, TypeOfHM ⟨[], []⟩ openMisuse τ := by
  rintro ⟨τ, h⟩
  cases h with
  | app hf ha =>
    cases ha with
    | primLitInt =>
      cases hf with
      | lambda hpc hann hbctx hbody => exact Ty.noConfusion (hann _ rfl)

theorem openMisuse_rejected : ¬ (typecheck [] openMisuse).isSome := by
  rw [typecheck_iff]; exact openMisuse_untypeable

/-! ### A polymorphic program — let-generalization + double instantiation -/

/-- `let id : ∀a. a → a = λx. x in id id`. -/
def idid : Expr :=
  .letIn (some ⟨1, .arrow (.bvar 0) (.bvar 0)⟩) (.lambda none (.var 0 []))
    (.app (.var 0 [.arrow (.fvar 0) (.fvar 0)]) (.var 0 [.fvar 0]))

theorem idid_typeable : TypeOfHM ⟨[], []⟩ idid (.arrow (.fvar 0) (.fvar 0)) := by
  apply TypeOfHM.letIn (M := ⟨1, .arrow (.bvar 0) (.bvar 0)⟩) (L := [])
  · show ContainsBvarsUpTo 1 (Ty.arrow (Ty.bvar 0) (Ty.bvar 0))
    exact .arrow (.bvar (by omega)) (.bvar (by omega))
  · intro σ' h; exact Option.some.inj h
  · intro Xs hfresh
    have hlen : Xs.length = 1 := hfresh.length
    rcases Xs with _ | ⟨X, _ | ⟨Y, tl⟩⟩
    · simp at hlen
    · have hterm : Expr.openBoundTyVars (some (⟨1, .arrow (.bvar 0) (.bvar 0)⟩ : PolyTy)) [X]
            (.lambda none (.var 0 [])) = .lambda none (.var 0 []) := rfl
      have htype : (⟨1, .arrow (.bvar 0) (.bvar 0)⟩ : PolyTy).openVars [X]
            = .arrow (.fvar X) (.fvar X) := rfl
      rw [hterm, htype]
      exact TypeOfHM.lambda .fvar (fun _ h => Option.noConfusion h) rfl
        (TypeOfHM.var (instArgs := []) rfl (by intro t ht; cases ht) .fvar)
    · simp at hlen
  · rfl
  · exact TypeOfHM.app
      (TypeOfHM.var (polyTy := ⟨1, .arrow (.bvar 0) (.bvar 0)⟩)
        (instArgs := [.arrow (.fvar 0) (.fvar 0)]) rfl
        (by intro t ht; simp only [List.mem_singleton] at ht; subst ht; exact .arrow .fvar .fvar)
        (.arrow (.bvar rfl) (.bvar rfl)))
      (TypeOfHM.var (polyTy := ⟨1, .arrow (.bvar 0) (.bvar 0)⟩)
        (instArgs := [.fvar 0]) rfl
        (by intro t ht; simp only [List.mem_singleton] at ht; subst ht; exact .fvar)
        (.arrow (.bvar rfl) (.bvar rfl)))

theorem idid_headlines_fire :
    ∃ σ τ, typecheck [] idid = some σ ∧ σ = genScheme [] [] τ ∧
      TypeOfHM ⟨[], []⟩ idid τ ∧
      ∀ τ₀, TypeOfHM ⟨[], []⟩ idid τ₀ → ∃ R : Subst, τ₀ = R.onTy τ := by
  obtain ⟨σ, hσ⟩ := Option.isSome_iff_exists.mp (typecheck_iff.mpr ⟨_, idid_typeable⟩)
  obtain ⟨τ, hτeq, hty, hprin⟩ := typecheck_principal hσ
  exact ⟨σ, τ, hσ, hτeq, hty, hprin⟩

/-! ### Progress / preservation fire on a concrete erased program -/

/-- `(λx. x) 5` — already annotation-free; beta-reduces to `5`. -/
def appIdFive : Expr := .app (.lambda none (.var 0 [])) (.primLit (.int 5))

theorem appIdFive_typeable : TypeOfHM ⟨[], []⟩ appIdFive (.prim .int) :=
  TypeOfHM.app
    (TypeOfHM.lambda .prim (fun _ h => Option.noConfusion h) rfl
      (TypeOfHM.var (instArgs := []) rfl (by intro t ht; cases ht) .prim))
    TypeOfHM.primLitInt

-- (The progress/preservation capstone examples referenced the now-removed type-erasure
-- layer — `Expr.eraseTyAnnots` / `typecheck_progress` / `typecheck_preservation` no longer
-- exist; whole-program safety is the literal `TypeOfHM.type_safety` chain in `Core`.
-- These two examples are dropped accordingly.)

/-! ### Core v2: an all-wildcard match has a principal type (the match-fix witness) -/

/-- `λx. match x with | _ => 0`. Under Core v1's unconditional-`customTy` match rule
    this had NO principal type (the scrutinee's `customTy` name was unconstrained);
    after the §1 fix the scrutinee type is free, so the principal type is `∀α. α → Int`. -/
def matchWild : Expr := .lambda none (.match_ (.var 0 []) [(.wildcard, .primLit (.int 0))])

theorem matchWild_typeable : TypeOfHM ⟨[], []⟩ matchWild (.arrow (.fvar 0) (.prim .int)) := by
  refine TypeOfHM.lambda .fvar (fun _ h => Option.noConfusion h) rfl ?_
  refine TypeOfHM.match_ (scrutTy := .fvar 0)
    (TypeOfHM.var (instArgs := []) rfl (by intro t ht; cases ht) .fvar) (by simp) ?_
  intro branch hbr
  rw [List.mem_singleton] at hbr; subst hbr
  exact TypeOfMatchBranch.wildcard TypeOfHM.primLitInt

/-- All three headlines fire on the all-wildcard match: it typechecks, the produced
    type is a genuine declarative type (soundness), and it is principal. -/
theorem matchWild_headlines_fire :
    ∃ σ τ, typecheck [] matchWild = some σ ∧ σ = genScheme [] [] τ ∧
      TypeOfHM ⟨[], []⟩ matchWild τ ∧
      ∀ τ₀, TypeOfHM ⟨[], []⟩ matchWild τ₀ → ∃ R : Subst, τ₀ = R.onTy τ := by
  obtain ⟨σ, hσ⟩ := Option.isSome_iff_exists.mp (typecheck_iff.mpr ⟨_, matchWild_typeable⟩)
  obtain ⟨τ, hτeq, hty, hprin⟩ := typecheck_principal hσ
  exact ⟨σ, τ, hσ, hτeq, hty, hprin⟩

/-! ### A recursive program — `letRec` typechecks at its principal type

The mutually-recursive group `letRec [f := g; g := f] in f` (`f = g`, `g = f`).
Under the old disjoint-slice `openGroup` rule this group could NOT be given the
polymorphic schemes `[∀a.a, ∀a.a]` (the disjoint opening severed the `f`/`g`
type-sharing — proved unsound vs Damas–Milner for `n > 1`); the shared-monotype
rule does. Its principal type is `∀a. a`. This is the capstone witness that the
recursive-binding inference fires end-to-end at the principal type. -/

/-- `letRec [var 1, var 0] (var 0)` — the `f = g; g = f` mutual loop returning `f`. -/
def mutualRec : Expr := .letRec [.var 1 [], .var 0 []] (.var 0 [.fvar 0])

theorem mutualRec_typeable : TypeOfHM ⟨[], []⟩ mutualRec (.fvar 0) := by
  refine TypeOfHM.letRec (τs := [.fvar 100, .fvar 100]) (Ms := [⟨1, .bvar 0⟩, ⟨1, .bvar 0⟩])
    (G := [100]) (L := []) rfl rfl ?_ (by simp) ?_ ?_ rfl ?_
  · -- every shared monotype is locally closed
    intro τ hτ; have : τ = Ty.fvar 100 := by simpa using hτ
    subst this; exact ContainsBvarsUpTo.fvar
  · -- each body-scheme is the generalisation `∀a. a = genGroup [100] (fvar 100)`
    intro p hp
    rw [show [Ty.fvar 100, Ty.fvar 100].zip [(⟨1, .bvar 0⟩ : PolyTy), ⟨1, .bvar 0⟩]
          = [(Ty.fvar 100, (⟨1, .bvar 0⟩ : PolyTy)), (Ty.fvar 100, ⟨1, .bvar 0⟩)] from rfl] at hp
    simp only [List.mem_cons, List.not_mem_nil, or_false] at hp
    rcases hp with rfl | rfl <;> rfl
  · -- the cofinite SHARED opening: both bindings see the SAME fresh name `X`
    intro Xs hfresh
    obtain ⟨X, rfl⟩ : ∃ X, Xs = [X] := List.length_eq_one_iff.mp (by simpa using hfresh.length)
    intro p hp
    rw [show [Ty.fvar 100, Ty.fvar 100].map (Ty.renameG [100] [X]) = [Ty.fvar X, Ty.fvar X] from rfl] at hp
    rw [show [Expr.var 1 [], Expr.var 0 []].zip [Ty.fvar X, Ty.fvar X]
          = [(Expr.var 1 [], Ty.fvar X), (Expr.var 0 [], Ty.fvar X)] from rfl] at hp
    simp only [List.mem_cons, List.not_mem_nil, or_false] at hp
    rcases hp with rfl | rfl
    · exact TypeOfHM.var (instArgs := []) (polyTy := PolyTy.mkTrivial (.fvar X)) rfl
        (by intro t ht; cases ht) InstantiatesBy.fvar
    · exact TypeOfHM.var (instArgs := []) (polyTy := PolyTy.mkTrivial (.fvar X)) rfl
        (by intro t ht; cases ht) InstantiatesBy.fvar
  · -- body `var 0` instantiates `∀a. a` (the scheme of `f`) at `fvar 0`
    exact TypeOfHM.var (polyTy := ⟨1, .bvar 0⟩) (instArgs := [.fvar 0]) rfl
      (by intro t ht; simp only [List.mem_singleton] at ht; subst ht; exact ContainsBvarsUpTo.fvar)
      (InstantiatesBy.bvar rfl)

/-- All headlines fire on the recursive group: `typecheck` succeeds, the produced
    type is a genuine declarative type (soundness), and it is principal. -/
theorem mutualRec_headlines_fire :
    ∃ σ τ, typecheck [] mutualRec = some σ ∧ σ = genScheme [] [] τ ∧
      TypeOfHM ⟨[], []⟩ mutualRec τ ∧
      ∀ τ₀, TypeOfHM ⟨[], []⟩ mutualRec τ₀ → ∃ R : Subst, τ₀ = R.onTy τ := by
  obtain ⟨σ, hσ⟩ := Option.isSome_iff_exists.mp (typecheck_iff.mpr ⟨_, mutualRec_typeable⟩)
  obtain ⟨τ, hτeq, hty, hprin⟩ := typecheck_principal hσ
  exact ⟨σ, τ, hσ, hτeq, hty, hprin⟩

end AuditCapstone
